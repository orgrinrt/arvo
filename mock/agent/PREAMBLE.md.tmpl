> **ARVO:** the tree holds the bottom three crates and nothing above them; the canon is being written. Do not reason from the removed architecture. No `dyn`, `TypeId`, `std::any`. `#![no_std]`, no `alloc`. Sizes are const.
