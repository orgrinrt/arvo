---
apply_to: ["**/Cargo.toml"]
---

## Cargo workspace rules

> **The crate names below name a tree that no longer exists.** `mock/crates/` was
> cleared for the canon work, and the decomposition is being redecided from
> scratch. Read what follows for the discipline it states, never for the
> architecture it names. Do not restore a crate because this file mentions it,
> and do not cite this file as evidence that a surface exists. **Authority is the
> registry**, `mock/registry/*.toml`, declared as `canon_paths` in
> `mockspace.toml`. Query it with `cargo mock query` and cite a row by its slug.
> A `ruling` row at `rung = "ratified"` governs; anything else, including every
> file under `mock/research/`, is agent output nobody ratified.



### Mock workspace inheritance

- Every `arvo*` crate uses `version.workspace = true`,
  `edition.workspace = true`, `license.workspace = true`.
- Sibling arvo deps use `<name>.workspace = true` (declared once in
  `[workspace.dependencies]` as path deps).
- External deps (if ever needed) are declared in
  `[workspace.dependencies]`, not per-crate.
- Edition 2024 for all crates.

### Layer rules (dependency direction)

The layer table that stood here encoded the removed decomposition and is gone.
Dependency direction as a principle survives: a lower layer never depends on a
higher one, and the direction is part of the design rather than an accident of
what compiled. Which layers exist, and what they are called, is what the canon
work is deciding.

### No external runtime deps (except notko foundation)

- No `std`, no `alloc`. `#![no_std]` crate attribute is mandatory.
- No platform crates: no `libc`, `windows-sys`, `nix`, `mio`,
  `tokio`, `async-std`, `rayon`, `crossbeam`, `parking_lot`.
- No allocator crates: no `bumpalo`, `typed-arena`, `smallvec`.
  Consumers own allocation; arvo never calls out.
- No numeric helper crates (`num`, `num-traits`, `fixed`): arvo
  declares its own numeric traits.

`notko` is the single permitted workspace-foundation dep. It is
the workspace primitive-vocabulary crate (`Just<T>` / `Maybe<T>` /
`Outcome<T, E>` / `Boundable` / `NonZeroable`), ships from
`github.com/orgrinrt/notko`, is `#![no_std]` with zero runtime
deps, and is consumed by every workspace repo (arvo, hilavitkutin,
clause). `notko-macros` and `notko-build` are pulled only when a
crate genuinely needs `#[optimize_for]` or build-time optimiser
distribution.

If any other optional crate is genuinely needed (e.g. cpu-feature
detection for a SIMD fallback), it must be behind a default-off
feature gate and listed explicitly in a design round before landing.

### No proc-macro crates

arvo does not ship a proc-macro crate. `proc_macro_crates = []` in
`mockspace.toml`. If a `_derive` crate becomes necessary, its
addition is a design-round topic, not a routine edit.

### No build script carries the tooling

arvo crates do not depend on mockspace and do not run it from a
build script. The `cargo mock` launcher is installed once on the
machine, reads `mockspace.toml` at the repo root, and resolves its
own engine pin from the `mockspace_branch` there.

This keeps the published crates free of a git dependency. A build
script that pulled the design tooling in would make every consumer
of arvo fetch it too, for a tool that does nothing at their build
time, and would block publishing outright, since a registry refuses
a crate whose dependency resolves to a git source.

`mock/crates/arvo/build.rs` is the one build script that remains. It
declares the `arvo_fast_math` cfg that hilavitkutin-build's FastMath
pragma sets, so rustc does not warn on `#[cfg(arvo_fast_math)]`. It
has nothing to do with the design tooling. A new crate needs no
build script at all.
