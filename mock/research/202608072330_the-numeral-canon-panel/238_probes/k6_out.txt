=== a0_no_gate ===
    gate: none
    error: `Width` is forbidden as the type of a const generic parameter
      = note: the only supported types are integers, `bool`, and `char`
    error: aborting due to 1 previous error
    refused.

=== a1_min_adt_const_params ===
    gate: #![feature(min_adt_const_params)]
    error: the trait `ConstParamTy` may not be implemented for this struct
      |            ^^^^^ struct fields are less visible than the struct
    error: aborting due to 1 previous error
    refused.

=== a2_adt_const_params ===
    gate: #![feature(adt_const_params)]
    BUILT.

=== a3_public_field ===
    gate: #![feature(min_adt_const_params)]
    BUILT.

=== verdict ===
The gate the second option names refuses a newtype whose field is private,
so taking it costs the encapsulation the door's types exist for as well as
the feature. The gate the workspace register carries a row for takes the
same type with the field still private.
