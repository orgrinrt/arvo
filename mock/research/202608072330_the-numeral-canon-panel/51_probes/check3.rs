// generated by gen_check3.py. Links and calls the emitted arms.
use std::process::ExitCode;

#[link(name = "widthmatrix", kind = "static")]
unsafe extern "C" {
    fn w1_typed(p: *const u8) -> u64;
    fn w1_gather(p: *const u8) -> u64;
    fn w1_wide(p: *const u8) -> u64;
    fn w1_hand(p: *const u8) -> u64;
    fn w1_native(p: *const u8) -> u64;
    fn w2_typed(p: *const u8) -> u64;
    fn w2_gather(p: *const u8) -> u64;
    fn w2_wide(p: *const u8) -> u64;
    fn w2_hand(p: *const u8) -> u64;
    fn w2_native(p: *const u8) -> u64;
    fn w3_typed(p: *const u8) -> u64;
    fn w3_gather(p: *const u8) -> u64;
    fn w3_wide(p: *const u8) -> u64;
    fn w3_hand(p: *const u8) -> u64;
    fn w3_native(p: *const u8) -> u64;
    fn w4_typed(p: *const u8) -> u64;
    fn w4_gather(p: *const u8) -> u64;
    fn w4_wide(p: *const u8) -> u64;
    fn w4_hand(p: *const u8) -> u64;
    fn w4_native(p: *const u8) -> u64;
    fn w5_typed(p: *const u8) -> u64;
    fn w5_gather(p: *const u8) -> u64;
    fn w5_wide(p: *const u8) -> u64;
    fn w5_hand(p: *const u8) -> u64;
    fn w5_native(p: *const u8) -> u64;
    fn w6_typed(p: *const u8) -> u64;
    fn w6_gather(p: *const u8) -> u64;
    fn w6_wide(p: *const u8) -> u64;
    fn w6_hand(p: *const u8) -> u64;
    fn w6_native(p: *const u8) -> u64;
    fn w7_typed(p: *const u8) -> u64;
    fn w7_gather(p: *const u8) -> u64;
    fn w7_wide(p: *const u8) -> u64;
    fn w7_hand(p: *const u8) -> u64;
    fn w7_native(p: *const u8) -> u64;
    fn w8_typed(p: *const u8) -> u64;
    fn w8_gather(p: *const u8) -> u64;
    fn w8_wide(p: *const u8) -> u64;
    fn w8_hand(p: *const u8) -> u64;
    fn w8_native(p: *const u8) -> u64;
    fn w9_typed(p: *const u8) -> u64;
    fn w9_gather(p: *const u8) -> u64;
    fn w9_wide(p: *const u8) -> u64;
    fn w9_hand(p: *const u8) -> u64;
    fn w9_native(p: *const u16) -> u64;
    fn w10_typed(p: *const u8) -> u64;
    fn w10_gather(p: *const u8) -> u64;
    fn w10_wide(p: *const u8) -> u64;
    fn w10_hand(p: *const u8) -> u64;
    fn w10_native(p: *const u16) -> u64;
    fn w11_typed(p: *const u8) -> u64;
    fn w11_gather(p: *const u8) -> u64;
    fn w11_wide(p: *const u8) -> u64;
    fn w11_hand(p: *const u8) -> u64;
    fn w11_native(p: *const u16) -> u64;
    fn w12_typed(p: *const u8) -> u64;
    fn w12_gather(p: *const u8) -> u64;
    fn w12_wide(p: *const u8) -> u64;
    fn w12_hand(p: *const u8) -> u64;
    fn w12_native(p: *const u16) -> u64;
    fn w13_typed(p: *const u8) -> u64;
    fn w13_gather(p: *const u8) -> u64;
    fn w13_wide(p: *const u8) -> u64;
    fn w13_hand(p: *const u8) -> u64;
    fn w13_native(p: *const u16) -> u64;
    fn w14_typed(p: *const u8) -> u64;
    fn w14_gather(p: *const u8) -> u64;
    fn w14_wide(p: *const u8) -> u64;
    fn w14_hand(p: *const u8) -> u64;
    fn w14_native(p: *const u16) -> u64;
    fn w15_typed(p: *const u8) -> u64;
    fn w15_gather(p: *const u8) -> u64;
    fn w15_wide(p: *const u8) -> u64;
    fn w15_hand(p: *const u8) -> u64;
    fn w15_native(p: *const u16) -> u64;
    fn w16_typed(p: *const u8) -> u64;
    fn w16_gather(p: *const u8) -> u64;
    fn w16_wide(p: *const u8) -> u64;
    fn w16_hand(p: *const u8) -> u64;
    fn w16_native(p: *const u16) -> u64;
    fn w17_typed(p: *const u8) -> u64;
    fn w17_gather(p: *const u8) -> u64;
    fn w17_wide(p: *const u8) -> u64;
    fn w17_hand(p: *const u8) -> u64;
    fn w17_native(p: *const u32) -> u64;
    fn w18_typed(p: *const u8) -> u64;
    fn w18_gather(p: *const u8) -> u64;
    fn w18_wide(p: *const u8) -> u64;
    fn w18_hand(p: *const u8) -> u64;
    fn w18_native(p: *const u32) -> u64;
    fn w19_typed(p: *const u8) -> u64;
    fn w19_gather(p: *const u8) -> u64;
    fn w19_wide(p: *const u8) -> u64;
    fn w19_hand(p: *const u8) -> u64;
    fn w19_native(p: *const u32) -> u64;
    fn w20_typed(p: *const u8) -> u64;
    fn w20_gather(p: *const u8) -> u64;
    fn w20_wide(p: *const u8) -> u64;
    fn w20_hand(p: *const u8) -> u64;
    fn w20_native(p: *const u32) -> u64;
    fn w21_typed(p: *const u8) -> u64;
    fn w21_gather(p: *const u8) -> u64;
    fn w21_wide(p: *const u8) -> u64;
    fn w21_hand(p: *const u8) -> u64;
    fn w21_native(p: *const u32) -> u64;
    fn w22_typed(p: *const u8) -> u64;
    fn w22_gather(p: *const u8) -> u64;
    fn w22_wide(p: *const u8) -> u64;
    fn w22_hand(p: *const u8) -> u64;
    fn w22_native(p: *const u32) -> u64;
    fn w23_typed(p: *const u8) -> u64;
    fn w23_gather(p: *const u8) -> u64;
    fn w23_wide(p: *const u8) -> u64;
    fn w23_hand(p: *const u8) -> u64;
    fn w23_native(p: *const u32) -> u64;
    fn w24_typed(p: *const u8) -> u64;
    fn w24_gather(p: *const u8) -> u64;
    fn w24_wide(p: *const u8) -> u64;
    fn w24_hand(p: *const u8) -> u64;
    fn w24_native(p: *const u32) -> u64;
    fn w25_typed(p: *const u8) -> u64;
    fn w25_gather(p: *const u8) -> u64;
    fn w25_wide(p: *const u8) -> u64;
    fn w25_hand(p: *const u8) -> u64;
    fn w25_native(p: *const u32) -> u64;
    fn w26_typed(p: *const u8) -> u64;
    fn w26_gather(p: *const u8) -> u64;
    fn w26_wide(p: *const u8) -> u64;
    fn w26_hand(p: *const u8) -> u64;
    fn w26_native(p: *const u32) -> u64;
    fn w27_typed(p: *const u8) -> u64;
    fn w27_gather(p: *const u8) -> u64;
    fn w27_wide(p: *const u8) -> u64;
    fn w27_hand(p: *const u8) -> u64;
    fn w27_native(p: *const u32) -> u64;
    fn w28_typed(p: *const u8) -> u64;
    fn w28_gather(p: *const u8) -> u64;
    fn w28_wide(p: *const u8) -> u64;
    fn w28_hand(p: *const u8) -> u64;
    fn w28_native(p: *const u32) -> u64;
    fn w29_typed(p: *const u8) -> u64;
    fn w29_gather(p: *const u8) -> u64;
    fn w29_wide(p: *const u8) -> u64;
    fn w29_hand(p: *const u8) -> u64;
    fn w29_native(p: *const u32) -> u64;
    fn w30_typed(p: *const u8) -> u64;
    fn w30_gather(p: *const u8) -> u64;
    fn w30_wide(p: *const u8) -> u64;
    fn w30_hand(p: *const u8) -> u64;
    fn w30_native(p: *const u32) -> u64;
    fn w31_typed(p: *const u8) -> u64;
    fn w31_gather(p: *const u8) -> u64;
    fn w31_wide(p: *const u8) -> u64;
    fn w31_hand(p: *const u8) -> u64;
    fn w31_native(p: *const u32) -> u64;
    fn w32_typed(p: *const u8) -> u64;
    fn w32_gather(p: *const u8) -> u64;
    fn w32_wide(p: *const u8) -> u64;
    fn w32_hand(p: *const u8) -> u64;
    fn w32_native(p: *const u32) -> u64;
    fn w33_typed(p: *const u8) -> u64;
    fn w33_gather(p: *const u8) -> u64;
    fn w33_wide(p: *const u8) -> u64;
    fn w33_hand(p: *const u8) -> u64;
    fn w33_native(p: *const u64) -> u64;
    fn w40_typed(p: *const u8) -> u64;
    fn w40_gather(p: *const u8) -> u64;
    fn w40_wide(p: *const u8) -> u64;
    fn w40_hand(p: *const u8) -> u64;
    fn w40_native(p: *const u64) -> u64;
    fn w47_typed(p: *const u8) -> u64;
    fn w47_gather(p: *const u8) -> u64;
    fn w47_wide(p: *const u8) -> u64;
    fn w47_hand(p: *const u8) -> u64;
    fn w47_native(p: *const u64) -> u64;
    fn w48_typed(p: *const u8) -> u64;
    fn w48_gather(p: *const u8) -> u64;
    fn w48_wide(p: *const u8) -> u64;
    fn w48_hand(p: *const u8) -> u64;
    fn w48_native(p: *const u64) -> u64;
}

fn main() -> ExitCode {
    let mut bad = 0usize;
    let mut checked = 0usize;

    {
        const W: usize = 1;
        const MASK: u64 = 1;
        const BYTES: usize = 133;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u8> = vals.iter().map(|&v| v as u8).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w1_typed(packed.as_ptr()),
                w1_gather(packed.as_ptr()),
                w1_wide(packed.as_ptr()),
                w1_hand(packed.as_ptr()),
                w1_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 2;
        const MASK: u64 = 3;
        const BYTES: usize = 258;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u8> = vals.iter().map(|&v| v as u8).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w2_typed(packed.as_ptr()),
                w2_gather(packed.as_ptr()),
                w2_wide(packed.as_ptr()),
                w2_hand(packed.as_ptr()),
                w2_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 3;
        const MASK: u64 = 7;
        const BYTES: usize = 383;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u8> = vals.iter().map(|&v| v as u8).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w3_typed(packed.as_ptr()),
                w3_gather(packed.as_ptr()),
                w3_wide(packed.as_ptr()),
                w3_hand(packed.as_ptr()),
                w3_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 4;
        const MASK: u64 = 15;
        const BYTES: usize = 508;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u8> = vals.iter().map(|&v| v as u8).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w4_typed(packed.as_ptr()),
                w4_gather(packed.as_ptr()),
                w4_wide(packed.as_ptr()),
                w4_hand(packed.as_ptr()),
                w4_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 5;
        const MASK: u64 = 31;
        const BYTES: usize = 633;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u8> = vals.iter().map(|&v| v as u8).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w5_typed(packed.as_ptr()),
                w5_gather(packed.as_ptr()),
                w5_wide(packed.as_ptr()),
                w5_hand(packed.as_ptr()),
                w5_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 6;
        const MASK: u64 = 63;
        const BYTES: usize = 758;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u8> = vals.iter().map(|&v| v as u8).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w6_typed(packed.as_ptr()),
                w6_gather(packed.as_ptr()),
                w6_wide(packed.as_ptr()),
                w6_hand(packed.as_ptr()),
                w6_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 7;
        const MASK: u64 = 127;
        const BYTES: usize = 883;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u8> = vals.iter().map(|&v| v as u8).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w7_typed(packed.as_ptr()),
                w7_gather(packed.as_ptr()),
                w7_wide(packed.as_ptr()),
                w7_hand(packed.as_ptr()),
                w7_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 8;
        const MASK: u64 = 255;
        const BYTES: usize = 1008;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u8> = vals.iter().map(|&v| v as u8).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w8_typed(packed.as_ptr()),
                w8_gather(packed.as_ptr()),
                w8_wide(packed.as_ptr()),
                w8_hand(packed.as_ptr()),
                w8_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 9;
        const MASK: u64 = 511;
        const BYTES: usize = 1133;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u16> = vals.iter().map(|&v| v as u16).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w9_typed(packed.as_ptr()),
                w9_gather(packed.as_ptr()),
                w9_wide(packed.as_ptr()),
                w9_hand(packed.as_ptr()),
                w9_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 10;
        const MASK: u64 = 1023;
        const BYTES: usize = 1258;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u16> = vals.iter().map(|&v| v as u16).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w10_typed(packed.as_ptr()),
                w10_gather(packed.as_ptr()),
                w10_wide(packed.as_ptr()),
                w10_hand(packed.as_ptr()),
                w10_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 11;
        const MASK: u64 = 2047;
        const BYTES: usize = 1383;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u16> = vals.iter().map(|&v| v as u16).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w11_typed(packed.as_ptr()),
                w11_gather(packed.as_ptr()),
                w11_wide(packed.as_ptr()),
                w11_hand(packed.as_ptr()),
                w11_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 12;
        const MASK: u64 = 4095;
        const BYTES: usize = 1508;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u16> = vals.iter().map(|&v| v as u16).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w12_typed(packed.as_ptr()),
                w12_gather(packed.as_ptr()),
                w12_wide(packed.as_ptr()),
                w12_hand(packed.as_ptr()),
                w12_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 13;
        const MASK: u64 = 8191;
        const BYTES: usize = 1633;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u16> = vals.iter().map(|&v| v as u16).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w13_typed(packed.as_ptr()),
                w13_gather(packed.as_ptr()),
                w13_wide(packed.as_ptr()),
                w13_hand(packed.as_ptr()),
                w13_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 14;
        const MASK: u64 = 16383;
        const BYTES: usize = 1758;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u16> = vals.iter().map(|&v| v as u16).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w14_typed(packed.as_ptr()),
                w14_gather(packed.as_ptr()),
                w14_wide(packed.as_ptr()),
                w14_hand(packed.as_ptr()),
                w14_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 15;
        const MASK: u64 = 32767;
        const BYTES: usize = 1883;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u16> = vals.iter().map(|&v| v as u16).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w15_typed(packed.as_ptr()),
                w15_gather(packed.as_ptr()),
                w15_wide(packed.as_ptr()),
                w15_hand(packed.as_ptr()),
                w15_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 16;
        const MASK: u64 = 65535;
        const BYTES: usize = 2008;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u16> = vals.iter().map(|&v| v as u16).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w16_typed(packed.as_ptr()),
                w16_gather(packed.as_ptr()),
                w16_wide(packed.as_ptr()),
                w16_hand(packed.as_ptr()),
                w16_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 17;
        const MASK: u64 = 131071;
        const BYTES: usize = 2133;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w17_typed(packed.as_ptr()),
                w17_gather(packed.as_ptr()),
                w17_wide(packed.as_ptr()),
                w17_hand(packed.as_ptr()),
                w17_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 18;
        const MASK: u64 = 262143;
        const BYTES: usize = 2258;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w18_typed(packed.as_ptr()),
                w18_gather(packed.as_ptr()),
                w18_wide(packed.as_ptr()),
                w18_hand(packed.as_ptr()),
                w18_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 19;
        const MASK: u64 = 524287;
        const BYTES: usize = 2383;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w19_typed(packed.as_ptr()),
                w19_gather(packed.as_ptr()),
                w19_wide(packed.as_ptr()),
                w19_hand(packed.as_ptr()),
                w19_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 20;
        const MASK: u64 = 1048575;
        const BYTES: usize = 2508;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w20_typed(packed.as_ptr()),
                w20_gather(packed.as_ptr()),
                w20_wide(packed.as_ptr()),
                w20_hand(packed.as_ptr()),
                w20_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 21;
        const MASK: u64 = 2097151;
        const BYTES: usize = 2633;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w21_typed(packed.as_ptr()),
                w21_gather(packed.as_ptr()),
                w21_wide(packed.as_ptr()),
                w21_hand(packed.as_ptr()),
                w21_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 22;
        const MASK: u64 = 4194303;
        const BYTES: usize = 2758;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w22_typed(packed.as_ptr()),
                w22_gather(packed.as_ptr()),
                w22_wide(packed.as_ptr()),
                w22_hand(packed.as_ptr()),
                w22_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 23;
        const MASK: u64 = 8388607;
        const BYTES: usize = 2883;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w23_typed(packed.as_ptr()),
                w23_gather(packed.as_ptr()),
                w23_wide(packed.as_ptr()),
                w23_hand(packed.as_ptr()),
                w23_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 24;
        const MASK: u64 = 16777215;
        const BYTES: usize = 3008;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w24_typed(packed.as_ptr()),
                w24_gather(packed.as_ptr()),
                w24_wide(packed.as_ptr()),
                w24_hand(packed.as_ptr()),
                w24_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 25;
        const MASK: u64 = 33554431;
        const BYTES: usize = 3133;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w25_typed(packed.as_ptr()),
                w25_gather(packed.as_ptr()),
                w25_wide(packed.as_ptr()),
                w25_hand(packed.as_ptr()),
                w25_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 26;
        const MASK: u64 = 67108863;
        const BYTES: usize = 3258;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w26_typed(packed.as_ptr()),
                w26_gather(packed.as_ptr()),
                w26_wide(packed.as_ptr()),
                w26_hand(packed.as_ptr()),
                w26_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 27;
        const MASK: u64 = 134217727;
        const BYTES: usize = 3383;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w27_typed(packed.as_ptr()),
                w27_gather(packed.as_ptr()),
                w27_wide(packed.as_ptr()),
                w27_hand(packed.as_ptr()),
                w27_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 28;
        const MASK: u64 = 268435455;
        const BYTES: usize = 3508;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w28_typed(packed.as_ptr()),
                w28_gather(packed.as_ptr()),
                w28_wide(packed.as_ptr()),
                w28_hand(packed.as_ptr()),
                w28_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 29;
        const MASK: u64 = 536870911;
        const BYTES: usize = 3633;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w29_typed(packed.as_ptr()),
                w29_gather(packed.as_ptr()),
                w29_wide(packed.as_ptr()),
                w29_hand(packed.as_ptr()),
                w29_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 30;
        const MASK: u64 = 1073741823;
        const BYTES: usize = 3758;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w30_typed(packed.as_ptr()),
                w30_gather(packed.as_ptr()),
                w30_wide(packed.as_ptr()),
                w30_hand(packed.as_ptr()),
                w30_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 31;
        const MASK: u64 = 2147483647;
        const BYTES: usize = 3883;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w31_typed(packed.as_ptr()),
                w31_gather(packed.as_ptr()),
                w31_wide(packed.as_ptr()),
                w31_hand(packed.as_ptr()),
                w31_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 32;
        const MASK: u64 = 4294967295;
        const BYTES: usize = 4008;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u32> = vals.iter().map(|&v| v as u32).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w32_typed(packed.as_ptr()),
                w32_gather(packed.as_ptr()),
                w32_wide(packed.as_ptr()),
                w32_hand(packed.as_ptr()),
                w32_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 33;
        const MASK: u64 = 8589934591;
        const BYTES: usize = 4133;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u64> = vals.iter().map(|&v| v as u64).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w33_typed(packed.as_ptr()),
                w33_gather(packed.as_ptr()),
                w33_wide(packed.as_ptr()),
                w33_hand(packed.as_ptr()),
                w33_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 40;
        const MASK: u64 = 1099511627775;
        const BYTES: usize = 5008;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u64> = vals.iter().map(|&v| v as u64).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w40_typed(packed.as_ptr()),
                w40_gather(packed.as_ptr()),
                w40_wide(packed.as_ptr()),
                w40_hand(packed.as_ptr()),
                w40_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 47;
        const MASK: u64 = 140737488355327;
        const BYTES: usize = 5883;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u64> = vals.iter().map(|&v| v as u64).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w47_typed(packed.as_ptr()),
                w47_gather(packed.as_ptr()),
                w47_wide(packed.as_ptr()),
                w47_hand(packed.as_ptr()),
                w47_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    {
        const W: usize = 48;
        const MASK: u64 = 281474976710655;
        const BYTES: usize = 6008;
        let vals: Vec<u64> = (0..1000)
            .map(|k| ((k as u64).wrapping_mul(2654435761) ^ ((k as u64) << 7)) & MASK)
            .collect();
        let mut packed = vec![0u8; BYTES];
        for (k, &v) in vals.iter().enumerate() {
            let base = k * W;
            for i in 0..W {
                if (v >> i) & 1 == 1 {
                    packed[(base + i) / 8] |= 1 << ((base + i) % 8);
                }
            }
        }
        let unpacked: Vec<u64> = vals.iter().map(|&v| v as u64).collect();
        let want = vals.iter().fold(0u64, |a, &b| a.wrapping_add(b));
        let got = unsafe {
            (
                w48_typed(packed.as_ptr()),
                w48_gather(packed.as_ptr()),
                w48_wide(packed.as_ptr()),
                w48_hand(packed.as_ptr()),
                w48_native(unpacked.as_ptr()),
            )
        };
        checked += 1;
        let all = [got.0, got.1, got.2, got.3, got.4];
        if all.iter().any(|&g| g != want) {
            bad += 1;
            println!(
                "W={} MISMATCH want={} typed={} gather={} wide={} hand={} native={}",
                W, want, got.0, got.1, got.2, got.3, got.4
            );
        }
        if want == 0 {
            bad += 1;
            println!(
                "W={} DEGENERATE: the reference sum is zero, so agreement proves nothing",
                W
            );
        }
    }
    if bad == 0 {
        println!("all {checked} widths: five arms agree with an independently built reference, and no reference sum is zero");
        ExitCode::SUCCESS
    } else {
        println!("{bad} failures across {checked} widths");
        ExitCode::FAILURE
    }
}
