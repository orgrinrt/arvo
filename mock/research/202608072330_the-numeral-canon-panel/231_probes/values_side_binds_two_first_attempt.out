### V1, the population
  entries: 527
  PASS

### V2, a construction nobody writes
  PASS, 0

### entries whose values side carries a second region
  hits: 8
47:"operation: operations {add, sub, mul, neg} with integer constants for the wrapping fragment, and {saturating add, saturating mul} with nonnegative clamp-embedded constants
55:"arity: arity = 3 for the grouping kind, and arity in 2..=5 for the schedule kind"
60:"threads: threads = 1 for the timed instance and threads any for the compile-time artifacts"
70:"arity: arity = 3 for the algebraic family and 2 for the order family"
74:"arity: arity in {2, 3} for the asserted join laws"
131:"rounding: rounding = nearest, against a phase-zero mutant"
170:"total_width: W in {3, 4} for the end-to-end run, and W in 2..=10 for the absorption sweep"
171:"fraction_width: F = 0 for the end-to-end run, and F in 0..=W for the absorption sweep"

### by the axis whose slug they sit on
     3 arity
     1 total_width
     1 threads
     1 rounding
     1 operation
     1 fraction_width

### V3, a single-region entry must not be reported
  PASS

### V4, the known instance
  FAIL

### what each shape is, stated rather than counted
  , or         a UNION over two axes. The product a per-axis reading gives is wider.
  against      a PAIRING of two axes. Same.
  for the      a region attached to a sub-case that is not an axis at all,
               usually which run it came from, which is coverage in the region slot.
  , and        two regions on ONE axis inside one entry, which is exactly what
               a-predicate-names-an-axis-once forbids across two entries.
