# 74. Consolidation: the number-system concept

**Author lens:** Giesen. Cost models, compression, what a structure has to expose for the machine
underneath it to be reasoned about, and the habit of reading duplication as an un-extracted model.
**Position:** ninth file of the number-systems unit, its consolidation. This is the topic's canon
candidate, per `RULES.md:150-162`: the canon accretes one topic at a time and this file is this
topic's contribution. It is standalone and versioned, never a delta; every section carries its
content in full even where nothing changed. It will be checked by someone who did not write it,
working from the member files forward, and section 12 names where I am least certain so that check
has a floor to start from rather than a scope.
**Probes:** none. A consolidation compresses what the unit established; it builds no new evidence.
Every number below is sourced to a committed instrument in a member's probe directory, cited by the
member file and line that carries it. I re-ran nothing; `68` re-ran `65`'s and `66`'s instruments
and `72` re-ran `71`'s p1, and I rely on those reports where they are the verification.

**The unit consolidated.** `65` and `66`, two blind cold derivations written in parallel from the
premises only. `67`, the telescope. `68`, what the pipeline certifies. `69`, the checkpoint,
carrying three corrections and two retractions. `70`, levels and ownership. `71`, crossings. `72`,
`67` resumed in reply. `73`, membership and scope. Plus the register's unit-three section,
`OPTIONS.md:1578-1744`, of which Q29 through Q31 and the Q21 amendment were appended by `73`.

**Nothing here settles anything.** The mode is explore, per op's standing instruction
(`RULES.md:279-309`). Where the unit converged, this file says so with the rung the convergence
actually earned. Where it did not, the disagreement is located precisely in section 5 and left
standing. Two questions are op's and in flight (section 10); every sentence that would depend on
either carries both branches.

## 0. Gates, and coverage

**Canon gate: passes, situation two.** `mock/canon/` does not exist, `mock/crates/` is empty by the
declared mutation order, and this panel is writing the first canon. Nothing binds but op's intents
in `INTENTS.md`, which I re-read in full before writing. This file is the mechanism that produces
canon text, run for the first time in this panel, and running it is what the cadence requires
(`RULES.md:160-174`).

**Test gate: no suite exists.** The mock workspace has no members, by the mutation order. The
substitute is the probe discipline, applied here as source discipline: every claim below carries
the member `file:line` that establishes it, every such target was opened before being cited, and
no line of any file I did not open end to end is cited directly.

**Read end to end:** `65` (both phases), `66` (both phases), `67`, `68`, `69`, `70`, `71`, `72`,
`73`, `OPTIONS.md` in full, `INTENTS.md` in full, `RULES.md` in full.
**Not read:** `00_brief.md`, `01` through `64`, `DROPLIST.md`, `PERSONA_CALLS.md`, `seed/`,
`archive/`, and every probe source. Everything below about `63`, `55b`, `62`, `35`, `16`, `51` or
the droplist is sourced to the unit's files or the register and inherits their errors if any;
where a claim about `63` is load-bearing I cite the member line that carries it, never `63`
itself, because I did not open it.
**Nothing here is priced.** No member of this unit built a bench, every member said so, and this
file says it once for all of them: every magnitude in this topic is unpriced, and every
cost-flavoured sentence below means "does work a machine performs", never how much.

## 1. How to read this file

Rungs per `RULES.md:9-47`. **RATIFIED** does not occur below: nothing in this unit was brought to
op converged, and `INTENTS.md` currently holds no RATIFIED entry at all (`INTENTS.md:27-33`).
**TWO EXPERTS** requires the second to have derived before reading the first; this unit's blind
protocol produced some, and section 3.9 states the discounts. **ONE EXPERT** is a queue entry
asking for a second read, not a doubt. Claims marked with a member file are that member's; claims
marked "consolidator" are mine and sit at ONE EXPERT by construction.

The candidate canon text is section 4. Each entry is tested against permanence (still true and
useful after a from-scratch rewrite in another language in another decade) and equivalence (three
independent implementations behave the same), per `RULES.md:79-83`. Per the same rule, nothing in
section 4 spells an implementation: notation and named concepts only.

The live option pass is section 6, separate from the results, because a compression grips results
and drops open options (`RULES.md:189-210`), and this panel has lost one option per consolidation
twice. Every option is written out in full there, including five that exist only in member files
and were never appended to the register.

## 2. The unit's course, briefly

`65` (Knuth, cold) derived system, representation and format as three concepts with a change-test (`65:80-86`),
a six-item kernel each item from a quoted intent, an open ring, role-keyed plural representations,
redundancy keyed by role, and twelve candidate sentences. `66` (Dolan, cold, parallel) derived a
five-level hierarchy, the identity result (laws are identity-determining, encoding is not), the
working-against-storage split, erasure-derived admissibility, and the open-concept-closed-contract
shape. Both then reconciled against the panel; both phase ones stand untouched by protocol.

`67` (Kiselyov) replaced the tuple picture with a dependent sequence, showed a law contract is
decided at the pair of identity and adaptation and undecided at identity alone, measured three
crossing classes, found the shared-parameter split inside `63` C2's constancy test, derived the
tropical biconditional, and posed the ambient-family question. `68` (Leroy) sorted the criterion's
four verbs into their evidential bins, found "validate" read two ways by the two blind files,
itemised the trusted base, established the model-width ceiling inside this panel, and attacked
both hierarchies at the bottom. `69` is the checkpoint; it put two questions to op and carries the
unit's corrections, including two of its own overclaims corrected by later members.

`70` (Lattner) showed the two hierarchies are two instruments producing incompatible partitions,
proposed the ownership key, and re-keyed the platform-width question on resolution time. `71`
(Orchard) measured all five crossing classes, refuted `67`'s universal at the two indices it had
not measured, separated the value and pattern relations, found the endpoint-underdetermination of
multi-coordinate crossings, and collapsed conversion and resolution into one obligation. `72`
(Kiselyov resumed) accepted the refutation, showed the refuting rows are constructional and
thereby licensed a stronger true universal, scoped the order obligation to lossy crossings, and
objected to `71`'s role split. `73` (Leijen) split membership from hosting, refuted the standing
admission test's sufficient direction, supplied the repair, measured Q21's discriminator empty
over every total order at two widths, and recast admission as a location.

## 3. What the unit established

Each claim with its rung and its evidence. A rung on a claim here is the rung the unit's record
supports, not the rung any single file asserted; where those differ, section 7 carries the
correction.

### 3.1 The shape: a dependent sequence of five choices, over relations

The concept's components are not a tuple. Each later component ranges over a set the earlier ones
determine, the dependency runs one way, and the sequence as currently established has five
components: an ambient domain, a representable set over it, a reduction from the space that pair
derives, an encoding of the set, a container for the encoding (`67:134-149`, the notation at
`67:143`). The dependency is enforceable, not merely describable: attaching a component declared
over one identity to a term at another is refused by the type system, and the completed term names
only its last component and projects the rest, erasing to the container's size (`67:151-159`).

Amended by `72` on `71`'s evidence: the components range over **sets of relations**, with the
function case the ordinary one, because a redundant encoding is a relation with no canonical
function inside it (`72:214-219`, resting on `71:197-227`).

Supported from the change side by `70`: a change-test's "all else equal" clause is unsatisfiable
for every component except the last, because moving an earlier component forces re-instantiation
of the later ones (`70:109-115`). `70` read `67` first and says so, so this is a second committed
instrument on the same claim, not a second independent instance (`70:271-274`).

**Rung: ONE EXPERT on the shape as a whole** (`67:613-617`'s own marking), with two instruments.
The two dependencies it composes were stated separately by their unit-two authors, carried here
through `67`'s account. The count of components is open at both ends: the ambient-family question
could split the first (`67:440-446`) and the ownership key re-keys the last without removing it
(`70:196-214`).

### 3.2 The quantifier result, and the reconciled count of its failures

A law contract is decided at the pair of identity and selected adaptation, reads neither encoding
nor container, and is undecided by the identity alone: the same ambient domain, representable set,
encoding and container, with only the adaptation moved, flips a bound from accepted to refused
(`67:96-111`; the checkpoint carries it at `69:61-65` as the sharpest thing the unit produced
about where strategy sits). This also filled the gap `68` had named: the claim that the strategy
selects the correctness relation carried zero probe instances anywhere in the panel
(`68:384-394`), and `67`'s fifth instrument, built after reading `68`, instances it, with the same
strategy accepted at one identity and refused at another, so the law tracks the pair
(`67:508-527`).

The consequence for canon prose: a sentence's truth value changes with the prefix it quantifies
over, so every canon sentence about numerals names its prefix (`67`'s K2, `67:618-624`).

**The failure count this rests on needs reconciling, because two files say "the fourth" about two
different instances.** `67:114-121` catalogues three recorded instances of a panel sentence
acquiring the wrong quantifier. `67:252` itself became another: a universal quantified over five
enumerated classes while measuring three (`71:66-89`; accepted without reservation at `72:43-53`,
which calls it "the fourth"). The checkpoint then repeated that universal without checking its
quantifier, and its own correction calls that "the fourth" as well (`69:74-76`). Counted with what
was counted, per the unit's own lesson about numbers and their operands (`69:224-227`): **three
catalogued prior instances, plus `67:252`, plus `69`'s repetition of it, five recorded instances
in total**, two of them committed by authors who had just read or written the correction to an
earlier one. That is the evidence that the shape, not the authors, is at fault (`67:123-124`),
and it is the strongest support K2 has.

**Rung: ONE EXPERT on K2 as a sentence; the five failures are record, not derivation.**

### 3.3 Crossings: five classes, two relations, two depths, and a corrected universal

`67` measured three crossing classes and quantified over all; `71` measured all five and the
universal failed exactly at the two unmeasured indices (`71:66-97`, the table at `71:91-97`,
reproducing all eight of `67`'s numbers first: 192, 111, 136, 80 at index 3; 192, 101 at index 2;
108, 1 at index 1). `72` accepted the refutation and strengthened it: the two refuting rows are
constructional rather than empirical, because the value-level operation reads no coordinate past
the third, demonstrated by mutation with a deliberately broken encoding still reading 256 of 256
(`72:90-110`). That licenses the word "never" at those indices, which a sweep could not
(`72:113-118`), and it retires the number 256/256 from circulation as a measurement
(`72:119-121`).

The corrected universal, `72`'s preferred repair (`72:66-74`): **a crossing preserves the
value-level operation exactly when it moves no coordinate that operation reads, which is at the
encoding and at the container and nowhere earlier.** True by construction, all five classes
covered.

What the five-class measurement established beyond the repair:

- **The value relation and the pattern relation are independent**, and each coordinate decides
  which is the identity: index 4 preserves the value operation totally and destroys the pattern
  operation totally; index 1 preserves every pattern and has no value relation at all
  (`71:120-139`). The pattern column is decided by what both sides carry, not by the index
  (`71:99-118`).
- **Meaning is decided at prefix 3 and cost at the last two coordinates** (`71:232-261`, X2 at
  `71:667-673`). Two hard cases support it from `67`'s own shared-parameter probe, which was built
  for a different question: a packed stride leaves the representable set constant, a block
  exponent moves it, so meaning moves only where the first three coordinates do (`72:200-211`,
  resting on `67:369-380`).
- **Where an encoding is redundant, the pattern relation is not a function**: 81 strings onto 31
  values, 2 of 16 window values with a unique image, the crossing back partial at 27 of 81
  strings, and the pair a section and a retraction rather than a bijection (`71:197-227`). A named
  section is owed before the crossing is a function on patterns, and neither endpoint supplies it
  (`71:263-271`). The redundant encoding also has no pattern-level operation of its own, its
  container operation failing closure in the digit set at 2401 of 6561 pairs (`71:222-227`).
- **What index 4 actually costs, measured rather than constructional**: a biased encoding forfeits
  the raw adder totally, 0 of 256 in both sign domains, where the natural encoding keeps it at 256
  of 256 under wrap (`72:123-148`, the table at `72:131-138`).
- **A crossing's two preservations are the concept's two law families**: order is preserved
  exactly when the target's reduction is monotone, the operation exactly when it is coherent,
  twelve cells, all four combinations inhabited (`71:160-176`). Once the source's own reduction
  has fired, coherence stops sufficing: two adaptation points where the model allows one diverge
  at 990 of 1024 pairs for addition in the measured cell, which is double rounding under its own
  name (`71:184-193`).

**Endpoints do not determine a multi-coordinate crossing, and the obligation is scoped to loss.**
Of six coordinate pairs crossed in both orders, exactly one diverges, the representable-set move
against the reduction move, two distinct functions agreeing on 30 of 256 source values; the
three-coordinate case collapses to the same two classes (`71:287-320`, the table at
`71:301-308`). Both composites are well typed, so the typestate cannot break the tie
(`71:339-344`). `72` then bounded it: a widening gives one function at 16 of 16, an in-range
narrowing gives one function at 16 of 16, every divergent value is lossy, and the fourteen lossy
values that agree anyway have an exact closed form (`72:150-186`). **So an order is owed exactly
where a crossing is lossy and nowhere else** (`72:188-195`), and the widening composites the panel
relies on need no sentence at all (`69:193-195`, the checkpoint's verified restatement).

**Conversion and resolution are one obligation at two arities.** Three schedules for one written
mixed expression are three functions; they coincide exactly where the target's reduction is
coherent and the source's is absorbed by the target's, asserted rather than eyeballed, and no
operand pair anywhere gives three distinct answers at once (`71:346-397`, the table at
`71:366-371`). What fixes a mixed operation's meaning is the number and position of adaptation
points, so the resolution rule the design owes is a schedule, not a precedence order over
strategies (`71:389-404`).

**Rung: ONE EXPERT per sentence, with the constructional rows true by construction.** `72`
reproduced `71`'s p1 byte-identically and its p2 control exactly (`72:27-32`), which is
verification of arithmetic, not a second derivation.

### 3.4 Membership and hosting: two questions, a refuted test, and its repair

**The word "membership" is doing two jobs** (`73:68-117`): whether something is a number system
(structure; nothing about arvo enters) and whether arvo can carry it (residue at runtime; `68`
section 5's three clauses at `68:308-337`). The two-by-two is inhabited, including the cell nobody
had named: things that are hostable and are not systems and are not non-members either, because
they are choices at a later coordinate (`73:104-116`).

**The standing admission test's sufficient direction is refuted.** The prefix-3 exposure test
(`71:464-469`, accepted at `72:227-230`) is passed by every system's collapsed self-declaration,
which names its own computed algebra as its ambient domain, satisfies the list verbatim, computes
the identical function, and reports both law families clean; a mutation set including a reduction
that adapts every value to zero cannot make the collapsed verdict fail (`73:119-183`, the tables
at `73:140-149` and `73:165-171`). Separately, the constant-zero reduction passes both verdicts
under its honest declaration while computing nothing (`73:174-183`). **The necessary direction
survives untouched**, with its argument: prefix 3 is exactly what one system must show another for
a value to cross, and a system that cannot exhibit it composes with nothing (`73:125-131`,
`71:459-475`).

**The repair is additive and it is not a prohibition** (`73:184-247`). Forbidding the collapse
would forbid the closed-operation systems their only honest declaration; for GF(2)^n the collapsed
form is the only honest one (`73:199-206`). The exposure list instead gains the ambient domain's
own law inventory, because the useful facts are conjunctions: the induced operation associates
exactly when the ambient operation associates on the reachable set and the reduction is coherent,
sixteen of sixteen cells, neither conjunct alone predicting (`73:207-218`). And it gains a third
verdict, the retraction: a reduction moves no value already representable, independent of the
other two verdicts in both directions (`73:193-198`). The stronger candidate clause,
distance-minimising, is wrong: it excludes both wrapping rows, which are kernel, while the
retraction excludes no kernel item (`73:231-241`).

This was also a deliberate attack on the law frame the format unit left provisionally marked, from
a direction it was not built to face, and the frame held (`73:220-229`). A failed refutation is
not a proof and the provisional marking stays, now with the record that an attack was made.

**Q21's stated discriminator is measured empty.** Over every total order on the carrier at widths
2 and 3, exhaustively, wrapping addition and xor over GF(2)^n admit zero compatible total orders
each, for the same structural reason (finite groups admit no translation-invariant total order,
an argument holding at every width), while saturating addition and min admit some
(`73:249-291`, the table at `73:264-272`). So an order-or-magnitude boundary groups a kernel item
op's I3 demands (the derivation at `65:258-259`) with the candidate the narrow reading exists to exclude, and splits wrapping from
saturating inside one kernel item. What remains of Q21 is the ambient-family question
(`73:293-317`), and the two register entries should be read as one (`OPTIONS.md:1719-1728`).

**Membership's decidability splits.** Hosting is a typing question, decidable at any width. A
membership verdict is decidable at every width where the law frame supplies a closed form (the
additive congruence condition holds over 2780 windows at four widths with zero residue) and only
at the model width where it does not (the naive multiplicative analogue fails its control), so the
transfer proviso attaches to a nameable part of the test rather than to the concept
(`73:433-491`).

**Admission's useful output is a coordinate, not a boolean** (`73:376-431`): every disputed case
in the register is a member whose location surprised somebody, and the location procedure names
which coordinate a candidate fixes and, with `70`'s key, whose it is.

**Rung: ONE EXPERT throughout**; the refutation of the sufficient direction is one expert
attacking a claim two files had carried, and no second read of the repair has run.

### 3.5 Levels are clients: two instruments, no intrinsic count, and the ownership key

The two hierarchies (`65`'s three levels, `66`'s five) are not the same cut at different zoom and
not rival counts of one thing. They are two instruments: classification of choices by dependency,
and classification of changes by preserved observables. Measured: the effect partition has a
non-contiguous class, with the ambient-domain move and the reduction move observationally
identical while the representable-set move between them differs, so neither partition refines the
other (`70:91-151`, the fourth finding at `70:129-140`). The effect count is
observable-relative: two classes under operation results alone, four under `70`'s set, more under
the raw-pattern observables (`70:142-151`). So "how many levels" has no canon answer; what the
canon owns is the dependency order, the two instruments, and the criterion by which a cut earns a
name, which is having a caller (`67:189-207`, `70:66-79`).

This also diagnoses Q18's persistence: the ambient domain and the reduction are effect-twins, so
the file that classified by effects folded them and the file that classified by dependency
separated them, and each instrument genuinely cannot see what the other's cut is about
(`70:185-194`).

**The ownership key.** Every component of the sequence has an owner and a resolution time: a type,
an aggregate, the compilation target, or a runtime datum (`70:196-214`). It unifies three findings
that arrived separately: the container answer under packing is per-value and per-aggregate
(`68:339-362`), a shared parameter can sit at the realisation layer or the identity layer
(`67:353-407`, the block-float against packed-run measurement at `67:369-380`), and a
platform-width numeral is a target-indexed family whose representable set is constant at every
monomorphisation, satisfying every obligation constancy was demanded for, with the exclusion
reserved for the runtime-owned case (`70:216-265`). The discriminator `63` C2's sentence needed is
resolution time, not dependence (`70:240-250`).

**Rung: ONE EXPERT on the joint statement and on the key** (`70:355-376`'s own marking); the
per-value and per-aggregate pair underneath is carried as TWO EXPERTS from the container unit via
the register (`70:369-376`).

### 3.6 The pipeline: four verbs, four evidential statuses, and a trusted base

Op's criterion is one sentence and four verbs, and the unit sorted them (`68:92-219`; checkpoint
summary at `69:87-104`):

- **Derive** is discharged in this unit's evidence as declared-and-checked, not computed: the
  demand-to-representation association is author-supplied per instance and then validated, a
  checked table rather than a verified function, with the computed form existing only where the
  container unit built it (`68:97-111`).
- **Validate** is two different verbs, and the two blind derivations silently disagreed on which:
  compile-time per type against runtime per datum. Shared premises cannot explain the divergence,
  so the criterion's word is ambiguous and the canon must split the verb or key it by boundary
  (`68:112-133`). The two readings differ observably in the object code: the compile-time shape
  symbol-aliases to the bare operation, the runtime shape carries a per-datum residue of
  instructions (`68:179-195`; `69:19-28`). This is op's Q-A, section 10.
- **What compile-time validation can see is one direction.** A mutant with a deliberately
  overstated declared window and a flipped redundancy flag compiles clean through the entire
  validation suite; the guarantee is carried by round-trips through the executable maps, never by
  declarations checked against declarations (`68:136-151`; `69:94-96`).
- **Erase** decomposes into layout, dispatch and operation erasure, and the unit's evidence
  touches layout only, which is close to a language guarantee; operation erasure is per-instance,
  demonstrated at a trivial instance and refuted at a nontrivial one elsewhere in the panel
  (`68:153-195`). `66`'s erase arm establishes nothing about erasure and its doability sentence
  over-covers it (`68:162-177`; verified at the source by the checkpoint, `69:87-92`).
- **The model-width ceiling is this panel's own now**: the nine-bit exhaustive const check is
  refused by the compiler's default long-running-eval guard, so model-width validation is forced
  and the transfer assumption is load-bearing; uniformity of construction does not carry it
  alone, with a recorded counterexample class of width-keyed dispatch (`68:196-219`).

**The trusted base is itemised, nine items**, of which the canon-shrinkable ones are the transfer
proviso, validation-through-maps, the construction perimeter of every erased type, and per-instance
operation erasure (`68:221-253`). The honest form of any "no caveats left" sentence is that
enumerated base, never a verdict of none.

**Stored bits are not self-describing.** After erasure a byte witnesses none of the systems it is
simultaneously a valid numeral of; within a process the type context is re-established at every
monomorphisation, and the moment bits cross an interchange boundary the system identification
travels out of band or the bits mean nothing. The three roles differ precisely in who
re-establishes the invariant: compute, the compiler; storage, the type system; interchange, an
external agreement plus a mandatory runtime door (`68:283-297`; measured from the crossing side by
`71`'s index-1 row, `71:450-457`).

**The erase clause and the ban list are one statement**: no information residue, no dispatch
residue, no consultation residue, which admits every finite system and constrains only
representation, with the sharper admission clause that a system is hostable exactly when its
ingest predicate is a pure function of type parameters and bits (`68:308-337`). This whole item is
conditional on Q-B, section 10, and `68` and every dependent file say so.

**Rung: ONE EXPERT per claim**, with the mutant, the ceiling and the residue demonstration each a
single committed instrument.

### 3.7 The kernel, the ring, and the concept's closure

`65`'s kernel stands as the unit's statement of what the intents make obligatory, each item
derived from a quoted intent (`65:252-315`): the windowed integers in their three policy systems
over one numeral; fixed point as a value map over integer numerals; IEEE-shaped floating point as
the same family with a dynamic scale; finite ordinals as a named system distinct from wrapping and
from checked windows; the Boolean and GF(2) structures; and redundant compute intermediates. `67`
re-derived a subset independently before reading it and kept it, which is what keeping as a result
looks like (`67:409-412`). The open ring (`65:317-385`) admits RNS, LNS, exact rationals in
bounded costume, intervals, decimal as a radix parameter, signed-digit forms, posits, factoradic
and combinadic index representations, and Gray codes, each with its cost stated, none demanded.

**The concept is closed and the inventory is open**, with admission by supplying the concept's
obligations (`65:387-409`; `66:245-270` reaching near-identical phrasing blind). Three arrivals
exist, and the honest discount applies: `65` and `66` shared their premise set, so their agreement
is two instruments over one premise set, worth more than a read and less than two arrivals from
nothing (`65:568-574`). The obligations list itself is section 3.4's corrected one now, which is
where this unit moved that shape.

**The format does not determine the system.** One container hosts the wrapping ring, the checked
window, the bounded chain, GF(2)^n and the Boolean lattice at once, so the canon types the system,
never the box (`65:87-93`). Nobody else in the panel states the principle, and nobody else puts
the non-arithmetic structures inside the same concept (`65:737-744`). Its runtime corollary is
section 3.6's stored-bits result (`68:283-297`).

**Packing is mixed-radix positional representation**, unifying bitfield packing, tight
non-power-of-two windows and combinatorial ranking in one sentence, composing with rather than
competing against the container unit's stride work (`65:205-211`, `65:749-754`). ONE EXPERT, no
probe, owes one.

**Redundancy is keyed by role**: canonical at rest and at interchange by default with a stated
escape (the phase-two weakening at `65:665-671`, made because the register carries redundant
encodings as unexamined), a tool at the compute role with the normalisation law validated, where
`65`'s probe checked the carry-save law exhaustively in const context and nobody else had
(`65:411-434`, `65:757-759`). `71`'s section-and-retraction result is what the crossing into such
an encoding is (section 3.3).

**Rung: ONE EXPERT per item with the stated discounts.** The fixed-and-float one-family claim at
the format level has three instruments arrived at differently (`66`'s probe, `65`'s K3, unit two's
measurement, per `66:490-495`), all sharing the Flocq literature, and that shared-source discount
is stated by the files themselves.

### 3.8 The two law families are two consumer classes

The adaptation laws are what order transport consumes, and the algorithms whose additive operation
is the order (the tropical family, which op's I11 names as the selling point's arithmetic) are
their consumer; coherence is what a reduction that may be split or reassociated consumes. Measured
as a biconditional: tropical distributivity holds exactly when the reduction is monotone, six of
six cells, both sides observed true and false (`67:283-320`, the cross-table at `67:305-313`).
One cell of six serves both consumer classes, unsigned saturation over a nonnegative window, which
is exactly nonnegative shortest path's shape; at the signed window no cell serves both, and that
is where a general-purpose default under the imitation intent lands (`67:333-341`). Operations
closed on the representable set need no adaptation at all and inherit their ambient laws
unconditionally, which bounds the adaptation layer's subject and is where the magnitude-free
systems live (`67:447-453`, `73:688-695`).

**Rung: ONE EXPERT on the consumer mapping; the two families are unit two's, carried.**

### 3.9 Convergences, with the discounts that keep the rungs honest

Where this unit agreed, how it agreed matters more than that it agreed (`RULES.md:28-47`).

- **The number-numeral distinction, laws-as-identity at the top, and the pipeline's gate-free
  expressibility**: two blind instruments over one shared premise set (`65` phase one, `66` phase
  one), plus the format unit's prior converged text carried through their reconciliations
  (`65:577-595`). Discounted two-instruments-one-premise-set by both files' own statement.
- **The "difference is nominal" reconciliation of Q18** reached by both cold files after reading
  `63` is a read twice over, not TWO EXPERTS, and `67` attacked it: nominal for code, substantive
  for the canon, because the quantifier moves (`67:61-124`). The attack has not been answered by a
  defender of the nominal reading; `70` explains the fold as an instrument artifact without
  restoring it (`70:185-194`). The disagreement stands located, section 5.
- **The crate-table cross-check in `66`** was attacked by `65` phase two, seconded independently
  by `67` before reading `65`'s attack, seconded again by `68`, and is the closest thing in the
  unit to a settled correction: the table describes the dead tier and corroborates nothing
  (`65:705-731`, `67:476-486`, `68:376-381`, `69:120-123`).
- **The three-role model** is ONE EXPERT (`65`'s), and `66`'s adoption of it as two-source
  corroborated rests on a miscitation, section 8.
- **The strategy-selects-the-member claim** went from zero probe instances (`68:384-394`) to one
  in the same unit (`67:508-527`). One instance, honestly counted.

## 4. The candidate canon text

The topic's canon-candidate sentences, consolidated. Each is intent, not implementation; each
carries its rung and its sources; permanence and equivalence are stated where a member argued
them and I concur, and separately where I judged them myself. Merging was done only where two
files state one claim; authorship is kept. Nothing here is settled and every sentence is offered
to op's eventual convergence pass, not around it.

**N1, the shape.** *The numeral concept is a dependent sequence of choices, not a tuple of
independent ones: an ambient domain, a representable set over it, a reduction from the space that
pair derives, an encoding of the set, a container for the encoding. Each choice ranges over a set
the earlier choices determine, the components range over relations with the function case
ordinary, and the dependency runs one way.* Permanence and equivalence: pass (`67:613-617`,
`72:214-219`). ONE EXPERT, two instruments.

**N2, the quantifier.** *Every canon sentence about numerals names the prefix of that sequence it
quantifies over. A property decided by the identity, one decided by the identity with its selected
reduction, and one decided by the encoding are three different claims, and stating one at the
wrong depth is how a true sentence becomes false.* Permanence and equivalence: pass
(`67:618-624`). ONE EXPERT, resting on five recorded failures (section 3.2).

**N3, derivation is completion.** *The consumer supplies a prefix and the typestate completes the
sequence; validation checks that the completion satisfies what the prefix demanded; erasure
discards the completion's evidence and keeps its consequences. A completion may branch, and the
branches are what op's plural names.* Permanence and equivalence: pass (`67:627-631`). ONE
EXPERT, read together with N8's split of the validate verb.

**N4, what a crossing is.** *A value crosses between two systems along the coordinates at which
they differ, one class per coordinate. A crossing carries a relation on values, a relation on
patterns, and a verdict per law family, and none determines another; the two relations need not
even be the same kind of thing, since a redundant encoding makes the pattern relation a section
and a retraction rather than a function, and a named section is then owed by neither endpoint.*
Permanence and equivalence: pass (`71:658-665`, `71:711-718`). ONE EXPERT.

**N5, the corrected universal.** *A crossing preserves the value-level operation exactly when it
moves no coordinate that operation reads, which is at the encoding and at the container and
nowhere earlier.* True by construction, so it holds at any width in any language, which is what a
canon sentence needs. Permanence and equivalence: pass (`72:66-74`, licensed by `72:90-118`).
ONE EXPERT.

**N6, the two depths.** *What a crossing means is decided by the ambient domain, the representable
set and the selected reduction on both sides. What it costs is decided by the encoding and the
container on both sides, and those two can never change what it computes.* Permanence and
equivalence: pass (`71:667-673`; the two hard cases at `72:200-211`). ONE EXPERT, and the unit's
own files name it the sentence most wanting attack, because everything else is scoped by it.

**N7, the crossing's preservations are the law families.** *A crossing preserves order exactly
when the reduction it adapts through is monotone, and preserves operations exactly when that
reduction is coherent; the crossing is the two families' third consumer beside order transport and
reassociating folds.* Permanence and equivalence: pass (`71:684-691`). ONE EXPERT on the pairing;
the families are unit two's.

**N8, lossy crossings owe an order.** *A composite crossing that loses nothing is determined by
its endpoints. Where it loses, it is determined by its endpoints together with an order, the two
candidate orders being the source's policy governing the loss and the target's, and the typestate
cannot break the tie. An order must be named exactly where a crossing is lossy, and nowhere
else.* Permanence and equivalence: pass (`71:693-700`, `72:273-279`). ONE EXPERT on each half;
whose order it is remains op's, section 6.

**N9, conversion and resolution.** *Moving one value into a declared system and combining several
values from disagreeing systems are one obligation at two arities: both name a common ambient
domain, a target representable set, and which reduction governs the loss, and both are schedules,
the number and position of adaptation points being part of what the expression means.* Permanence
and equivalence: pass (`71:702-708`). ONE EXPERT.

**N10, membership and hosting are two questions.** *Whether something is a number system and
whether this implementation can carry one are different questions. The first is about structure
and is answered by locating the candidate on the chain of choices; the second is about residue at
runtime and is answered by what a value at rest may carry. Neither enumerates, both are open, and
a system the implementation cannot host is still a system.* Permanence and equivalence: pass
(`73:667-675`). ONE EXPERT on the split; the hosting half is `68`'s and is Q-B-conditional.

**N11, what a system exposes.** *A system exposes its ambient domain together with that domain's
own law inventory, its representable set, and its selected reduction with that reduction's
verdicts, including that the reduction moves no value already representable. The reduction's
verdicts carry no information alone: the properties a consumer relies on are conjunctions of an
ambient law and a reduction verdict, and a system that names its own computed algebra as its
ambient domain satisfies every verdict while voiding every conclusion.* Permanence and
equivalence: pass (`73:677-686`). ONE EXPERT; supersedes the sufficient direction of the earlier
exposure test while keeping its necessary direction whole.

**N12, closure and the magnitude-free members.** *Where an ambient operation is closed on the
representable set, no adaptation is possible and the reduction is the identity. Such a system
exposes everything the concept asks, needs none of the adaptation machinery, and is a member for
that reason rather than in spite of it.* Permanence and equivalence: pass (`73:688-695`,
`67:447-453`). ONE EXPERT with a construction.

**N13, no order-shaped boundary, and wrapping as the test.** *The concept's edge is not an order
and not a magnitude: order-compatibility is a property of the selected reduction, varies within a
single family the design must contain, and groups a wrapped integer with a bit vector. A wrapped
numeral satisfies fewer of the properties a numeral is expected to satisfy than almost anything
else the concept contains and is not negotiable, so any clause proposed as a boundary of the
concept is checked against wrapping before anything else.* Permanence and equivalence: pass
(`73:697-703`, `73:723-733`). ONE EXPERT; the order half is exhaustive over every total order at
two widths with a structural argument covering all widths.

**N14, admission returns a coordinate.** *Asking whether a candidate is a number system is
usually the wrong question. The useful question is which choice in the chain the candidate fixes
and whose it is, because most things that are not systems are not outside the concept: they are
choices at a later coordinate, and an encoding, a container, a stride and a housing are all
answers rather than rejections.* Permanence and equivalence: pass (`73:705-711`). ONE EXPERT,
composing the sequence, the ownership key and the crossing classes.

**N15, the two instruments and no intrinsic level count.** *Identity and quantification cut the
concept along its dependency order; compatibility and crossings cut it by preserved observables.
Both partitions are real and neither refines the other. The concept commits to its choices and
their order and to no count of levels: a level is a partition class relative to the observables a
client brings, and a cut earns a name by having a caller.* Permanence and equivalence: pass
(`70:352-367`). ONE EXPERT on the joint statement.

**N16, the ownership key.** *Each choice in the sequence has an owner and a resolution time: a
type, an aggregate, or the compilation, resolving at monomorphisation, or a runtime datum. The
concept's per-value obligations are owed by choices resolved at monomorphisation whoever owns
them; a choice owned by an aggregate makes its obligations per-aggregate; and a platform-width
numeral is a target-indexed family of formats whose exclusion grounds apply only to dependence
that survives to runtime.* Permanence and equivalence: pass (`70:369-384`). ONE EXPERT on the
key; the platform-width half carries Q-B's provenance condition.

**N17, validation is two acts keyed by boundary, and validates maps.** *At derivation,
compile-time, per type: the declared representation is checked through its executable maps,
exhaustively at a model width. At ingest, runtime, per datum: bits arriving without their
construction history pass a membership door that is a pure function of the type's parameters and
the bits. Between doors nothing checks and nothing needs to. A property a representation declares
about itself counts only where a check ties it to the maps; a declared constant nothing reads is
a comment with a type.* Permanence and equivalence: pass (`68:420-428`). ONE EXPERT, and it
contains op's open Q-A: whether both acts are wanted, and where, is his, section 10.

**N18, erasure's guarantee is a conditional with a stated base.** *What survives erasure is the
theorem that reachable values of a type satisfy its predicate, conditional on a closed
construction perimeter, validated operations, the model-width transfer proviso, and the
language's own guarantees; the canon lists these, because a guarantee whose perimeter is unstated
is not one. Stored bits are not self-describing: one format hosts many systems, so after erasure
a value witnesses its system only through its type, interchange carries the system identification
out of band, and canonicity at interchange is necessary but not sufficient.* Permanence and
equivalence: pass (`68:429-436`). ONE EXPERT.

**N19, the format does not determine the system.** *One container hosts many systems at once,
arithmetic and non-arithmetic, and the canon types the system, never the box.* Permanence and
equivalence: pass (`65:502-504`, with `67:488-497`'s vocabulary caution applied: the sentence is
stated over the sequence's coordinates, not over either unit's use of the word "format").
ONE EXPERT with a third blind instrument on the arithmetic cells.

**N20, the concept is closed and the inventory is open.** *The canon defines once what a number
system is and what admission requires; the set of admitted instances is open, and a new one earns
admission by supplying the concept's obligations rather than by amending the canon. The kernel
the stated intents demand is guaranteed; everything else is admitted by contract and decided by
measurement.* Permanence and equivalence: pass (`65:519-521`, `66:255-270`). Two instruments over
one premise set plus the format unit's prior text; the obligations list is N11's.

**N21, the roles, stated with their open condition.** *A demand derives representations, plural,
keyed by role; the roles named so far differ in who re-establishes the invariant. A role that
moves nothing before the encoding is a realisation variant and cannot change what anything
computes; whether every proposed role is of that kind is exactly the open question of whether a
compute role may widen the representable set, and a chain's extent is not a role at all but a
schedule.* Permanence and equivalence: pass conditionally; the sentence names its own open
condition (`65:185-196`, `65:505-507`, `71:406-449`, `72:232-255`, `73:415-424`). ONE EXPERT per part.

**N22, the two law families have two consumer classes.** *The adaptation laws are what order
transport consumes, and the algorithms whose additive operation is the order are their consumer;
coherence is what a reduction that may be split or reassociated consumes. The two are
independent, all four combinations occur, and a numeral serves one class, the other, both, or
neither.* Permanence and equivalence: pass (`67:647-654`). ONE EXPERT on the mapping.

**N23, consolidator's composition, offered for attack rather than adoption.** *Every question this
unit disputed was a dispute about an address: which coordinate of the sequence a claim lives at,
and who owns that coordinate. A canon sentence about numerals therefore carries its address, the
prefix it quantifies over and the owner that resolves it, and a candidate for admission, a
crossing, a role and a level are all answered by giving an address rather than a verdict.*
Permanence: passes; no mechanism is named. Equivalence: passes in the same constraint-on-prose
sense as N2. ONE EXPERT, mine, and it is a composition of N2, N14, N15 and N16 rather than a new
claim; if any of those falls, it falls with them.

**Deliberately not offered:** any level count (N15's point); any name for which prefix gets the
word "number system" or "format", which is op's naming call with the caller census as its input
(`67:189-207`); any ruling on whose reduction governs a lossy crossing (section 6); any admission
ruling on block floating point (`70:387` locates it and does not decide it); any name for the
value-preserving operation-destroying crossing (`67:668-673` looked and declined, `72:332-334`
concurs); any magnitude, because nothing in this topic is priced.

## 5. Located disagreements, standing

**D1, where the selected adaptation lives (Q18).** Three positions: inside identity (`65` phase
one), in realisation (unit two's C2 as carried), dissolved by the two-layer split (`66` phase
two). The both-cold-files "nominal" reconciliation is a read twice over and was attacked: nominal
for code, substantive for canon prose, because the quantifier moves with the cut (`67:61-124`).
`70` explains the fold as an instrument artifact (`70:185-194`). Nobody has answered `67`'s
attack in the nominal reading's defence. What would settle it: op's naming call over the caller
census, after a second derivation of the quantifier claim that did not read `67` first.

**D2, the word collision between units.** Two units use "format" for different prefixes of one
chain (`67:488-497`), and the panel separately carries two load-bearing uses of "crossing"
(`71:757-763`) and a known "phase" collision. A candidate sentence that changes meaning between
units is not a canon candidate yet. Vocabulary is op's; the caller census says what each word
must cover.

**D3, the role set's homogeneity (Q23).** `71`: three roles are realisation variants, chain
extent is a schedule, the set splits cleanly (`71:406-449`). `72`: the split runs through the
middle of `65`'s compute role, because the register carries a live reading under which compute
widens the representable set, and the criterion is three questions rather than one
(`72:232-255`). `73`: under the location reading the widening question and the homogeneity
question are one question (`73:415-424`). Not settled; the discriminating fact is whether any
role may move a coordinate before the encoding, and for the compute role that is an open register
entry (`OPTIONS.md:990-998`).

**D4, is the ambient domain's operation family fixed or a parameter.** Posed by `67:440-446`,
declined by its author, collapsed into Q21 by `73:293-317`, and now the unit's largest open fork:
fixed means the concept does not describe the arithmetic op's named selling point computes in;
a parameter means every law sentence acquires a scope and the reduction space varies along an
unenumerated axis. Both coherent, neither free, written in full in section 6.

**D5, Q29's second against third option.** The corrected exposure list against admission relative
to a consumer-supplied ambient domain. `73` attacked both of its own reasons for the gap between
them and both gave way; the remaining discriminator is whether any canon sentence must be true of
a system with no ambient domain named at all, and nobody could construct one either way
(`73:747-752`).

**D6, whose reduction governs a lossy crossing.** Three coherent positions, none forced by
anything measured, observable difference 226 of 256 source values in the measured cell; scoped by
`72` to lossy crossings only. Op's, with the fact of the choice as the panel's deliverable
(`71:329-337`, `72:188-195`).

**D7, the two questions op holds.** Q-A and Q-B, section 10. Both were put to him by the
checkpoint and neither is answered; every dependent sentence above names its branch.

## 6. The live option pass

Every live option this unit touches or produced, written out in full, per `RULES.md:189-210`.
Register entries Q18 through Q32 are restated with the unit's findings folded in; five options
exist only in member files and are marked so, because **they are not in the register and are
exactly the class a compression loses**. Nothing here is ranked. Where an entry says an option is
refuted in one direction, the diagnostic is cited and the surviving direction stated.

**Q18. Does a system's selected adaptation belong to its identity or to its realisation?**
Three positions. **(a) Identity**: wrap and saturate over one window are two systems; fits the law
layer, where the induced algebra is a property of the pair. **(b) Realisation**: a format is the
ambient domain and representable set alone, with the reduction space derived and a strategy
selecting per operation; fits format equality and the observational-equivalence results carried
from unit two. **(c) Dissolved**: both are true at two named layers. What the unit added: the
question's discriminator is not code (type identity agrees under all three) but the quantifier of
a canon sentence, with a compiled demonstration that a law bound is undecided at identity alone
and decided at the pair (`67:534-544`), plus the instrument diagnosis that (a) and (b) are what
the effect instrument and the dependency instrument respectively report (`70:315-319`). The
residue that is genuinely op's: which prefix the word "numeral" or "system" names.

**Q19. Are the proposed level hierarchies the same cut or different ones?** The discriminator ran:
they are different instruments, not different granularities; the effect partition has a
non-contiguous class, so neither refines the other (`70:129-140`). Candidate resolution, ONE
EXPERT and wanting a second derivation that did not read `70`: the canon carries the dependency
order for identity sentences and preserved observables for compatibility sentences, names cuts by
the caller criterion, and commits to no level count (`70:306-313`). The original three-way and
five-way counts stay as record of what each instrument sees.

**Q20. Is the inventory of number systems open or closed?** **Open**, at no cost, is the shape the
unit's evidence supports: both tests are non-enumerating (the location procedure plus the
corrected exposure list for membership; the residue clauses for hosting), so the canon can say
what it covers without listing it (`73:434-439`). **Closed** remains stateable: a canon that
enumerates is checkable, and nothing measured forbids it; its cost is the same as I1's corrected
history, a closure op has already demoted once for the strategy axis (`INTENTS.md:44-53`). The
bound either way: membership verdicts split into computed (closed-form, any width) and swept
(model width, transfer proviso attached), and a canon stating a law verdict says which it is
(`73:487-491`).

**Q21. Is "number system" broad enough to include things not about magnitude?** **Broad**: the
concept is carrier plus operations plus laws, no order required; GF(2)^n and the Boolean lattice
are members. **Narrow**: ordered, magnitude-bearing sets, with a sibling term for the rest. The
amendment stands and is now measured twice over: the order-or-magnitude discriminator groups
wrapping (kernel, from I3) with GF(2)^n (the candidate narrow exists to exclude) at zero
compatible total orders each, and splits wrapping from saturating inside one kernel item
(`73:264-291`; `OPTIONS.md:1719-1728`). The instance count on the broad conclusion is unchanged:
one cold derivation leaning broad, still `65`'s (`65:65-66`); `71` supplies an argument, not an
instance (a narrow concept cannot type the index-1 crossing a mask-and-numeral consumer actually
performs, `71:486-496`); `73` is a read. What remains of Q21 is the ambient-family option below,
and the two should be read as one entry.

**Q22. Are set-valued carriers admitted or scoped out?** **Admitted**: intervals and
error-carrying pairs are first-class carriers; certified accuracy is expressible inside the
concept; the priced cost from the wider panel record is the order-and-law layer. **Scoped out**:
the concept stays small and the molecules live at a composition layer. The unit's movement: `65`
phase two withdrew its own carried version of this in favour of the composition filing carried
from unit two (`65:689-696`), which is one read of that filing, not a closure; the register entry
stays live. `67`'s two-shapes finding (below) bears on it: a point composition and a
shared-parameter aggregate are different things, and intervals are the first kind.

**Q23. Is the role set closed?** **Closed at storage, compute, interchange**; **closed with
chain-extent as a fourth**; **open**. The unit reshaped the question: three roles are realisation
variants exactly if none moves a coordinate before the encoding, chain extent is a schedule
rather than a role (`71:406-449`), the compute role's status is contingent on the open widening
question (`72:232-255`), and under the location reading role-homogeneity and widening are one
question (`73:415-424`). So the entry's honest current form: the closure question is well formed
for realisation roles and malformed for a mixed set, and the discriminating fact is Q-adjacent
material the register already carries (`OPTIONS.md:990-998`).

**Q24. Does the canon speak about cost at all?** **An axis in the canon, numbers on the harness**:
every representation has a cost profile, derivation consults it per I8, magnitudes live in bench
artifacts (`65:173-183`). **Silence**: the canon says nothing about cost; smaller, defensible.
Untouched by the unit's second half; both live.

**Q25. How is the law inventory named?** **Marker contracts per law**; **a structured declaration
the derivation reads**; **both**. The unit's movement: the phase-two amendment that an inventory
is derived rather than declared, with verdicts computing what contracts assert, so the first two
are not rivals (`65:658-663`, `65:781-784`); and section 3.4's finding that verdicts alone carry
no information without the ambient's own inventory, which any naming must accommodate. Which form
the canon describes remains open.

**Q26. What kind of thing is a platform-width type?** Four options now. **(a) Target-resolved
instantiation of the shape family**: an implicit width parameter fixed by the build. **(b) A
genuinely separate axis**, because the value set is not knowable from the type alone. **(c) A
third kind called "storage"**, from unit two's phrase, weighted by the register as one passing
mention. **(d) The resolution-time reading**, in member file only until now: a platform-width
numeral is a target-indexed family of formats; its representable set is owned by the compilation
and constant at every monomorphisation, satisfying every obligation constancy was demanded for
(membership a pure function of type and bits, compile-time law checks per compilation, erasure
with nothing environmental), with exclusion reserved for dependence that survives to runtime;
cost, one clause split in the constancy sentence, and the runtime-half warrant is
Q-B-conditional; what would move it, any obligation a target-owned representable set cannot
satisfy within one compilation, of which none was found among this unit's instruments
(`70:321-331`).

**Q27. Is interoperation conversion, resolution, or neither?** Replaced by the unit's structure,
per its own files: **five crossing classes**, one per coordinate, identified by the smallest index
at which the terms disagree; each preserves values or patterns totally; the three that move a
coordinate the value-level operation reads preserve no operation totally, and the two that do not
preserve it necessarily (`72:76-83`). Inside every crossing, two independent relations (values,
patterns), two independent preservation verdicts (the two law families), a named section owed
where an encoding is redundant, and conversion-and-resolution as one obligation at two arities
whose meaning is a schedule (`71:576-586`). The earlier three-crossing line and the operations
universal are retracted (section 8).

**Q28. Should the canon speak of arbitrary-width demands at all?** **Speak of them**, interacting
with the forbidden-features boundary the container unit owns; **leave width families to design**.
Untouched by this unit beyond `65`'s phase-two withdrawal of its own version in favour of the
container unit's settled ground (`65:763-767`). Live.

**Q29. What does the admission contract ask a candidate to expose?** As appended by `73`
(`OPTIONS.md:1665-1692`). **(1) Prefix 3 with the reduction's two law verdicts**: refuted as a
sufficient condition twice over (the collapsed self-declaration passes verbatim and computes
identically; the constant-zero reduction passes both verdicts honestly while computing nothing);
necessary direction untouched and surviving. **(2) The same, plus the ambient domain's own law
inventory, plus a third verdict, the retraction**: one more item, one more verdict, ambient
inventory inheriting the transfer proviso where swept; buys the collapse becoming harmless, the
closed-operation systems keeping their only honest declaration, and the conjunction expressible
as one blanket implication so a design cannot state half of it. **(3) Admission relative to a
consumer-supplied ambient domain**, the candidate exposing only its representable set and
reduction: the collapse becomes unstateable; cost, a candidate does not determine its own
identity until a frame is chosen; it does not contradict identity-by-(domain, set), only the
unstated proposition that a candidate supplies every component of its own identity. Distinguisher
between (2) and (3): whether any canon sentence must be true of a system with no ambient domain
named; the ingest door is not that case, being a coordinate-2 and coordinate-4 question.

**Q30. Is admission a predicate or a location?** As appended (`OPTIONS.md:1694-1704`).
**A predicate**: discards the coordinate a consumer needs and makes every mislocated member read
as a non-member, which is most of the disputed cases. **A location**: returns which coordinate
the candidate fixes and whose it is; costs an output type rather than a truth value and presumes
a coordinate count the canon may not want to commit to. **A location for membership and a
predicate for hosting**: two procedures where a reader expects one. Distinguisher: whether any
canon sentence needs to quantify over non-members.

**Q31. One word or two for "is a number system" and "can arvo carry it"?** As appended
(`OPTIONS.md:1706-1717`). **One word**: folds the residue clauses into the concept; cost, the
concept then excludes unbounded exact rationals as mathematics, which is false, and every hosting
clause becomes conditional on unratified constraints inside a sentence about arithmetic.
**Two words**: two admission passages, the reader told which is which. **Two words with hosting
scoped to a target**: composes with the target-indexed families; adds a quantifier over
compilations. Distinguisher: whether the canon ever says something true of a system arvo cannot
host, and it already does, since its bounded windows are defined as windows of such systems.

**Q32. The doability claims are workload claims, and no workload evidence exists above one
thread.** Not an option set; the register's standing evidence gap (`OPTIONS.md:1730-1744`),
untouched by this unit, carried so it is not lost.

### Options in member files only, not yet register entries

These five were each written out in full by their authors and never appended to `OPTIONS.md`.
They are carried here in full for that reason.

**O-A. Is the ambient domain's operation family fixed at addition and multiplication, or a
parameter?** (`67:571-584`; merged with Q21 by the amendment.) **Fixed**: the concept describes
one arithmetic; the law layer's entire evidence base is correctly scoped as it stands; the
tropical semiring the algorithm crates compute in is described by the algorithm crates. Cost: the
named selling point computes in something the canon does not cover, and the one law those
algorithms need, addition distributing over min, is quantified over a pair of operations from two
families, which no instrument in the panel measures. **A parameter**: one representable set
carries several ambient domains; the Boolean and GF(2) structures and the tropical semiring
become ordinary members; the index-1 crossing becomes something the concept can talk about.
Cost: every law sentence acquires a scope it did not need, prefix-1 equality becomes a relation
somebody must define, and the reduction space varies along an unenumerated axis. Distinguisher:
whether any consumer needs one generic algorithm spanning both families, or naming two families
separately costs nothing because no code spans them.

**O-B. Two shapes of composition, not one.** (`67:586-598`.) The aggregate sense of "composition"
contains two structurally different things. **Point compositions**: a datum is a tuple of
numerals travelling together (stored-pair rationals, intervals, error-carrying pairs), owing its
own laws at the composition layer. **Shared-parameter aggregates**: many data plus one parameter
participating in each datum; arvo ships one at the realisation layer (the packed stride, which
leaves the representable set constant) and excludes one at the identity layer (a block exponent,
which moves it, eight distinct sets over eight exponents). Distinguisher of treatment: whether
the shared parameter participates in the arithmetic; if not, the aggregate is a layout fact and
the element remains a format; if so, the element has no representable set of its own and the
concept currently has no home for it.

**O-C. The ownership key as a structural axis.** (`70:333-345`.) **Adopt the key**: every chain
component carries an owner and a resolution time (type, aggregate, target, runtime datum;
monomorphisation or runtime), subsuming the per-value and per-aggregate bottom tier, the
two-layer shared parameters, and the platform-width case. Cost: every per-value canon sentence
acquires an implicit whose-components rider, and aggregate-owned cases need a per-aggregate
quantifier, which is new surface. **The alternative**: treat aggregate cases as compositions with
no key on the base concept, keeping the base smaller and forcing every shared-parameter case into
the composition layer, including the platform-width case, where that filing has no obvious
content. Distinguisher: whether any consumer writes one contract generic over ownership, or
naming the cases separately costs nothing because no code spans them.

**O-D. Whose reduction governs a lossy crossing?** (`71:626-637`.) **The source's**: a value
finishes being a value of its system before it becomes one of another; cost, the target's
declared policy does not govern values entering it, which reads badly at an ingest boundary.
**The target's**: the crossing is the target's business; cost, a value can be adapted by a policy
its own system never selected, and this is the route on which a wide wrapping value saturates.
**A third named at the crossing site**: the most expressive, the choice visible where made; cost,
one more thing at every crossing site and no default for a consumer who writes nothing.
Distinguisher: whether any consumer writes a crossing whose correct answer differs from both
endpoints' own selections; the observable difference between the first two is 226 of 256 source
values in the measured cell. Scoped by `72`: the question exists only at lossy crossings.

**O-E. Does the canon name crossing classes at all, and when is an order owed?** (`71:639-650`,
`72:289-299`, two entries with one subject, consolidated with both authors' options intact.)
On naming: **name all five classes**, so a crossing states which coordinate it moves and the law
obligation attaches to the class; cost, five names plus a rule that a composite names its
intermediate. **Name none**, one crossing relation with a declared preservation set; cost, the
classes still exist and get respelled in every sentence needing them. **Name two**, values and
patterns; cheapest, and cannot state the order problem, which is about coordinates. On the order:
**always**, for every multi-coordinate crossing; cost, ceremony with no content at lossless
crossings. **Only where lossy**; stated conservatively on terms (does the target's set contain
the source's). **Never, one global order fixed by the canon**; cost, wrong for one of the two
readings at every lossy site. Distinguishers: whether any canon sentence quantifies over one
class and not another (at least two do, per the independences), and whether any consumer writes a
lossy crossing without an explicit narrowing already in view.

**Adjacent register entries this unit touched, carried by pointer plus the touch, content
unchanged from the register**: Q3 gained the measurement that its explicit-conversion option is a
different function from its inferred option except where the result system's reduction is
coherent (`71:606-614`); Q10 gained a consumer, the losslessness clause of the conversion
contract, and its requested second read has still not run (`71:602-604`, `OPTIONS.md:1053`);
Q16's sense two gained O-B's split; the derivation's-outputs section gained the corroboration
that both new hierarchies undercount its two-question bottom tier (`68:408-410`).

## 7. Corrections the unit generated, for anyone reading its files later

These are repairs to the record, all verified by the member cited, none applied by me to any
member file, because a consolidation does not edit the files it compresses.

1. **`67:252`'s universal is retracted.** "No crossing preserves operations at 100%" quantified
   over five classes while measuring three. Minimal repair, "none of the three"; preferred
   repair, N5's biconditional (`71:66-97`, `72:60-74`). The register-facing line inside `67`
   section 9 falls with it, replaced by Q27's entry above. `67`'s K4 survives as written, its
   "may" being existential (`72:84-88`).
2. **The checkpoint repeated the universal and corrected itself** (`69:67-76`), which is the
   fifth quantifier instance as counted in section 3.2.
3. **`66:520-523` attributes to `63` a sentence that is verbatim `65:511`**, and its "two
   independent sources" is one source cited twice; the three-role model is ONE EXPERT
   (`67:457-474`, grep-confirmed by the coordinator at `69:115-119`). One-word fix.
4. **`66:455-457` attaches 952 to signed saturating multiplication; it belongs to addition.**
   Measured directly: addition 952, multiplication 160, over the 4096 triples of the signed
   4-bit window (`73:495-501`). The vector was a consolidation carrying the count without its
   operation (`69:207-227`). Standing lesson, adopted throughout this file: a number carries
   what was counted.
5. **`66:60-68`'s crate-table cross-check carries no evidential weight**: the table described the
   removed tree. Attacked by `65:705-731`, independently seconded (`67:476-486`, `68:376-381`).
   `66`'s hierarchy stands on op's two nouns without it.
6. **The generated-instruction repair was overclaimed three times**, each correction containing
   the next overclaim, with the final state established by counted greps rather than adjectives
   (`69:115-158`, `70:43-54`, `73:503-519`).
7. **`71`'s index-4 and index-5 value rows are constructional, not measurements**, and are cited
   as true by construction; the number 256/256 is retired from circulation as a measured figure
   (`72:90-121`).
8. **`66`'s probe defects**: a hardcoded headline count in a print literal, two tautological test
   arms, and zero committed output transcripts, all recovered by rerun so nothing is void
   (`68:55-84`; `69:160-169`). `65`'s negative-case transcript was regenerated against committed
   source after being found to name a scratch path (`68:45-53`).
9. **`65`'s phase-two self-amendments**, to be applied by whoever consumes its candidates:
   "declares its laws" becomes derives-and-bounds; the at-rest redundancy ban weakens to a
   default with a stated escape; its open questions 1 and 5 are withdrawn to the composition
   filing and the container unit respectively (`65:769-784`).
10. **`73:559` cites `RULES.md:1674-1678`, which does not exist**: RULES.md is 421 lines. Found
    by this consolidation; the convention meant is the option-carry instruction
    (`RULES.md:287-289`) or the register's own header (`OPTIONS.md:27-29`). The claim the
    citation supports, that options are appended to the register in full, is true and unaffected;
    the anchor is wrong. Consistent with its checker validating quoted text rather than bare
    ranges; one more instance for the class `RULES.md:126-133` records.
11. **`67` carries a count ambiguity a consolidator must not lift verbatim**: "three dependent
    axes" (`67:552-553`) against its own five-component sequence (`67:608-617`), with no statement
    of what each number counts. Resolved by the two-instrument reading, three being a segmentation
    of five for one purpose, and the resolution must be written wherever either sentence is
    carried (`70:279-282`). N1 above carries the five-component form only.

## 8. Evidence state, and what the unit did not do

**Everything exhaustive in this unit runs at model widths**, 2 through 6 bits, under the ceiling
`68` re-established inside this panel (the nine-bit exhaustive const check refused by the
compiler's default guard, `68:196-211`). Transfer upward is a proviso, not a property: no
law-relevant path keyed on width, with a recorded counterexample class, so every universal above
that is swept rather than structural inherits it. The structural results (the constructional
rows, the finite-group order argument, the closed-form additive congruence) do not.

**Everything multiplicative is at zero fraction width.** The wider panel record carries that no
multiplicative structure survives nonzero fraction for any policy; nothing in this unit touches
F > 0, including the additive checks nobody built there (`73:762-766`, `71:802-804`).

**Nothing is priced.** No bench ran in this unit. The two readings of "validate" differ in
instructions retired per datum and that difference is demonstrated, not priced (`68:186-189`).
Where a future dispatch needs a magnitude, the word is unpriced until the harness says otherwise.

**Instruments committed**: 4 with two transcripts (`65`, one regenerated), 4 sources with zero
transcripts recovered by rerun (`66`), 5 (`67`), 16 artifacts (`68`), 3 (`70`), 8 (`71`), 2
(`72`), 9 (`73`), per `69:160-166` and each file's coverage section. Two members ran mechanical
citation checkers over their own anchors and both found and fixed real defects doing it
(`71:779-786`, `73:791-808`).

**Left on the table, named by the unit itself**: the second read Q10 asked for, still unrun
(`71:602-604`); the order-and-adder exclusivity hypothesis against a redundant encoding, now
askable in the crossing vocabulary and still unasked (`71:806-810`); the H1/H2 law frame's
attack, out of this unit's scope, partially discharged by `73`'s C6 attack which failed to break
it (`73:220-229`); a second derivation, order-inverted, for N6, N11, N15 and the Q19 resolution,
each of which its author names as the sentence most wanting attack; an ingest door for a packed
boundary, argued mandatory and built by nobody (`68:459-461`); a correct geometric predicate for
clamped multiplication (`73:826-829`); and the ambient-family fork, which one file's evidence
should not decide (`73:741-745`).

## 9. Fits against the register

**Kills nothing.** This file closes no live option; the refutation of Q29's option (1) in its
sufficient direction is `73`'s and already recorded there. It adds no new option beyond N23,
which is a composition of existing carried claims rather than a design alternative, and it moves
five member-file options into a durable carrier (section 6), which the register should adopt as
numbered entries when next extended.

## 10. The two questions in flight

Both are op's, both were put to him by the checkpoint, and neither is assumed anywhere above.

**Q-A. Which verb is "validate"** (`69:9-33`). Compile-time per type against runtime per datum,
with measurably different machine cost. The unit's material mostly does not move under either
reading: the crossings, the membership arithmetic, the order enumeration and the location
procedure are reading-independent, and each file said so per claim (`71:549-559`, `72:305-311`,
`73:528-540`). What does move: N17 is the sentence that contains the question; the ingest door is
a fourth validation moment his earlier enumeration does not contain (`68:302-306`); and under the
runtime reading the hosting test is the operative one at a boundary while the membership test has
nothing to attach to, which is an argument both tests are wanted and keyed by boundary
(`73:533-540`).

**Q-B. Are the long-standing constraints op's intents** (`69:35-52`). None of the no-std,
no-alloc, const-sizes, no-dyn, no-TypeId set appears in `INTENTS.md`; all are asserted in
agent-authored places. What hangs on it, exactly: the erasure argument's dispatch half, the erase
clause and therefore the hosting test in full, N16's platform-width exclusion half, and N18's
conditional base items that name the ban list (`67:48-59`, `70:56-63`, `71:561-568`,
`73:542-551`). What does not: every arithmetic result, every crossing measurement, the membership
side of section 3.4, and the sequence itself, which would hold in any language under any dispatch
discipline. The split is clean and every candidate sentence above that touches the conditional
half names it.

## 11. Coverage, bounded honestly

Read, not read, and re-run are stated in section 0 and not repeated. Every `file:line` in this
document was opened before being cited, in the files I read end to end; no line of `63`,
`00_brief.md`, `DROPLIST.md` or any probe source is cited directly, and every claim about those
is routed through a member file's account with the member cited. I built no instrument and
therefore add no evidence; where I state a reconciliation (the five-instance count, the
constructional licensing, the roles composition), it is bookkeeping over the members' committed
numbers, and the members' numbers are the authority.

## 12. Where this compression is least certain

Stated for the independent check, as a floor and not a scope; the tarina lesson is that the
author's sense of risk is another of the author's judgements.

1. **The five-instance quantifier count** (section 3.2) is my reconciliation of two files each
   saying "fourth" about different instances. The arithmetic is trivial; the risk is that one of
   the three prior instances catalogued in `67:114-121` is itself a restatement of another,
   which I could not check without opening unit-two files I did not read.
2. **The trusted-base summary** (section 3.6) compresses nine enumerated items into four named
   plus a remainder; the enumeration at `68:226-250` is the authority and my summary is not a
   substitute for it.
3. **The roles composition** (sections 3.3, 5 D3, 6 Q23) merges three files' positions into one
   contingent statement; each source is cited, and if `72`'s reading of the register's
   compute-widening entry is wrong, D3's shape changes.
4. **Anchors into `OPTIONS.md`'s long entries** shift if anyone edits above the unit-three
   section; `73` appended below everything for exactly this reason and I cite into the appended
   region where possible.
5. **Everything sourced to `63` second-hand**, marked as such throughout; if a member misread
   `63`, the corresponding row here inherits it, and the known instance of exactly that (the 952
   vector) is why section 7 item 4 exists.

**Nothing here settles anything.** The consolidation goes to its independent check, then to op
beside the located disagreements of section 5, which are the unit's honest output where
convergence was not reached.
