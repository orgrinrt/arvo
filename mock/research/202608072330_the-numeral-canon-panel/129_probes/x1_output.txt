=== Part A: reproduce 127's shared-threshold construction, own instrument ===
  U=1/5: 0 monotonicity violations over 257 points (predicted 0)
  U=1/2: 0 monotonicity violations over 257 points (predicted 0)
  U=4/5: 0 monotonicity violations over 257 points (predicted 0)
  total: 0 (predicted 0, reproducing 127 Part 1)
  E_U[Q_U(1/3)] = 1/3, matches x: True (predicted True, reproducing 127 Part 2)
  E_U[Q_U(-7/4)] = -7/4, matches x: True (predicted True, reproducing 127 Part 2)
  E_U[Q_U(5/2)] = 5/2, matches x: True (predicted True, reproducing 127 Part 2)
  additivity violations at U=1/3: 12 of 240 (predicted > 0, reproducing 127 Part 3 control)

=== Part B: within-pass decorrelation, a property neither 127 nor my own phase one measured ===
=== A CONSTANT value x=0.5 (a maximal tie) rounded at 40 different positions in ONE pass. ===
=== Dithering exists to break up exactly this case: a flat region should not band. ===
  shared-threshold (one U for the whole pass): 1 distinct output value(s) across 40 positions (predicted 1: every position gets the identical decision)
  independent-per-element draw: 2 distinct output value(s) across 40 positions (predicted 2: decorrelates, at the known cost of monotonicity)
  position-keyed golden-ratio ordered dither: 2 distinct output value(s) across 40 positions (predicted 2: decorrelates, deterministically, without a runtime PRNG)
  position-keyed split at the tie: 19 down, 21 up, of 40 (near-even split is the point: no visible band, no directional bias)

=== Part B continued: is the position-keyed variant globally monotone the way shared-threshold is? ===
  position-keyed on an increasing ramp: 7 monotonicity violations over 40 consecutive pairs (predicted > 0: position-keying decorrelates threshold from value order the same way independent draws do, just deterministically instead of randomly; decorrelation and monotonicity are the same tension either way it is achieved)

=== Summary ===
  Shared-threshold: monotone within a pass, unbiased across passes, ZERO within-pass
  decorrelation (a flat region stays flat: one arbitrary deterministic mode per pass).
  Independent and position-keyed: within-pass decorrelation (the property dithering exists
  for), at the cost of monotonicity within the pass, for BOTH mechanisms alike.
  So the real fork is not deterministic-vs-stochastic and it is not independent-vs-correlated.
  It is WITHIN-PASS DECORRELATION versus WITHIN-PASS MONOTONICITY, and shared-threshold buys
  monotonicity by giving up the one property that motivates reaching for a stochastic or
  dithered rounding scheme in the first place.
