# 57. The grading and the refutation

**Date:** 2026-08-09. **Position:** file three of unit two on the format-concept topic, after `55`
derived it cold, `56` attacked, and `55b` replied. Two jobs: adjudicate the refutation `55b`'s `p5`
raises against `42`, and second-read the grading result `55b`'s `p4` reports.

**Probes:** `57_probes/`, committed with sources and outputs.

**Re-run before argued with.** `42_probes/p3`, `55_probes/p4`, `55_probes/p5` and `56_probes/q1` were
all rebuilt on `nightly-2026-05-28` and diffed against their committed outputs. All four
byte-identical (`57_probes/rerun/`). Every count I take from any of them is a count I regenerated.

**Read for this file:** `00_brief`, `INTENTS`, `42` in full with `42_probes/p3` source and output,
`55b` in full, `56` sections 3.3 and its `q1` source and output, `55_probes/p4` and `p5` sources and
outputs, and the register's Q11/Q12 entries. **I opened `42` directly and before `55b`'s account of
it**, which is what this dispatch was for. I did not open `35`, `18`, `25`, `43` or `40`, and where a
finding depends on that bound I say so.

## Status of this file

Nothing here settles anything, per the standing explore mode. One option is proposed for the
droplist, one register paraphrase is refuted, and one register entry is proposed to gain measured
content it currently lacks.

(File in progress. Sections land as the probes do.)
