S3. the certificate on the cheap carrier, checked against the model
==============================================================================
toolchain: rustc 1.98.0-nightly (57d06900f 2026-05-27)
feature gates in the generated source: 0
references to the expensive carrier in it: 0

  term                        rust (a)  rust (b)  rust cert  model cert  agree
  x + y                           True      True       True        True   True
  x * y                           True      True       True        True   True
  (x + y) - y                    False      True      False       False   True
  (x + y) * z, z in [0,3]         True     False      False       False   True
  (x + y) * z, z in [1,3]         True      True       True        True   True
  (x * y) + z                     True      True       True        True   True
  x - (y - z)                     True      True       True        True   True
  x * (y - y)                    False     False      False       False   True

  every verdict matches the model: True
  terms whose certificate is FALSE: 3 of 8, so the run had cases that could disagree

  root intervals, so the cheap carrier is shown to be computing:
    x + y                      [0, 6]
    x * y                      [0, 9]
    (x + y) - y                [-3, 6]
    (x + y) * z, z in [0,3]    [0, 18]
    (x + y) * z, z in [1,3]    [0, 18]
    (x * y) + z                [0, 12]
    x - (y - z)                [0, 1]
    x * (y - y)                [-9, 9]

MUTATIONS, each dropping one condition

  condition (a) forced true    verdicts that moved: 1  ['(x + y) - y']
  condition (b) forced true    verdicts that moved: 1  ['(x + y) * z, z in [0,3]']

------------------------------------------------------------------------------
reading: the certificate reads intervals and leaf identity and
nothing else, so it is available at the point S2 shows the selection
has to be made. Both conditions are functions of the term's syntax
and the declared bounds' syntax, which is what a macro or a code
generator has in hand before either tower is instantiated.
