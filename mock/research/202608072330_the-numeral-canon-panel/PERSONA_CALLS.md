# Persona calls

Every steer the `orgrinrt` persona gives while op is away is recorded here, in full, marked as the
persona's.

**Nothing in this file is op's word.** Nothing here ratifies, nothing here is canon, and nothing here
may be quoted to an expert as a warrant. The persona exists to give the panel a designer-shaped
perspective to push against overnight; it imitates op's register and it does not carry his authority.

Op, opening the arrangement (`04`):

> Use my persona to stand in, but treat their calls as just that, **not** my calls. Let them steer
> you and aid you, give you different perspectives imitating me, but don't let any of it slip into
> anything as ratified or canon.

Where a persona steer appears to conflict with something op actually said, **op's recorded words
govern**, and the conflict is written down here for the morning rather than resolved silently.

## Format

One entry per call, newest last.

```
### <date> <short title>
**Asked:** what was put to the persona, and why.
**The persona said:** its answer, quoted rather than paraphrased.
**What was done with it:** the effect on the panel, if any.
**Status:** persona-decided. Not ratified.
```

## Calls

### 2026-08-08 Checkpoint on the first stretch (`03`, `06`, `07`, `08`)

**Asked:** to stand in for op at the checkpoint slot after the first four expert files, push on what
they produced in his register, audit the mutual agreement between them for inherited-versus-independent,
say whether the stretch is aimed at the design's purpose, and check `MORNING.md` against the four files.

**The persona said:** the full text is `09_persona_checkpoint.md`. Its calls, quoted rather than
paraphrased, are:

- The three things worth op's attention first are the untaken fresh-eyes task on the container
  derivation with the `01:73-77` against `SETTLED.md:149-153` contradiction inside it; `MORNING.md`
  promoting `07`'s self-flagged inherited claim to independent corroboration; and the cross-kind F2
  witness being the stretch's one three-instance result and having its meaning overturned by `08` in
  the same stretch.
- "The TWO EXPERTS rung is currently unreachable by construction", because `RULES.md:163` mandates
  cumulative sequential reading and `RULES.md:28-30` requires derivation before reading. Proposed fix
  is a dispatch shape change, not a rule change.
- Four refusals: `08`'s question one should not go to op without the compile-time measurement `08:540-550`
  names, on the strength of op's own words at `01:96-98`; the Moore completion should be marked as
  closing an operation with no caller rather than left neutral with a percentage beside it; the
  `canonical_exponent` naming call is not a debt and should leave `08`'s cost list; the
  D0/D1-equals-fibre/index claim should not harden without a properly ordered second read.
- Four defects in `MORNING.md`, the largest being that line 14 doubles `06`'s inference surface from ten
  sites to twenty and deletes `06`'s headline result in the same sentence.
- Named as genuinely good: `06`'s `E0119` result, `06` section 7.1's unasked correctness finding,
  `07` section 4.2's saturating-top question, `08`'s writing of the probe three files said was owed,
  and the evidence discipline across all four.
- One gap put on the map rather than in this canon file: the numeral concept is being made total one
  layer above where the primary consumer's saved bits come from, and the storage-composition question
  is currently on nobody's list.

**What was done with it:** nothing yet. Delivered for op's morning. Recommended `MORNING.md` edits are
recommendations and were not applied.

**Status:** persona-decided. Not ratified. Nothing in `09` settles anything, per `04`.
