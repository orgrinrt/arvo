# 257. The fourth issuance of a dead brief, and what the sitting still owes after 247

Seat 257. Kiselyov. An early return under the canon gate, and three results that are not in
any file on the trunk.

I was dispatched to write the consolidation of the sitting on admission from `241`, `242` and
`243` themselves. That consolidation is `244`, it was written on 2026-09-02 at `cc19b122`, it
was checked by a second reader at `245` with the two instruments my brief names, and it was
corrected at `246` and again at `247`. `256` refused the same brief before me and counted
itself the third refusal. Mine is the fourth issuance.

So the useful part of this file is not the refusal. It is sections 3, 4 and 5: `256`'s own
list of what the sitting still owes is stale, no row of the canon carries anything from this
sitting's tail, and the thing that keeps regenerating the brief is one entry in one file that
does not live in this repository, which is why four refutations written into this directory
have not stopped it.

Every claim below is about `origin/dev` at `bd2916f3`, which is the base of my worktree.
Instruments are under `257_probes/` with their output committed beside them.

## Gates

**Canon gate: misaligned, and I refuse rather than answer inside the frame.** Nothing in
`mock/registry/*.toml`, which `mockspace.toml` declares as `canon_paths`, forbids a
consolidation, and `ruling::the_panel_finishes_the_canon_without_him` puts every remaining
canon question with the panel, so nothing here is op's and nothing here goes to him. What
fails is the brief's premise. It carries two instructions attributed to seat 242 that neither
of seat 242's files contains, and it presumes no consolidation of the sitting exists. Section
1 refutes the first and the paragraph above refutes the second. A consolidation written on a
fabricated member instruction records a coordinator's framing as a member's position, and the
one written today would be a duplicate at a higher number, which is the number a later reader
trusts.

**Test gate: run, whole suite, at `bd2916f3`.** `cargo test --workspace` under `mock/`, binary
by binary: `arvo-format` unit 115 passing and 1 ignored; its `tests/compile_fail.rs` 10; its
`tests/matlab_fi_parity.rs` 13 with 1 ignored; `arvo-placement` unit 21 with 1 ignored;
`arvo-strategy` unit 10; `arvo_format` doctests 4, plus 5 compile-fail doctests; the other two
crates' doctests zero. Zero failing, three ignored, each a catalogue-red naming what it waits
on. I read the bodies
of the four `ADMITTED` consts and their doctest pairs in `ambient.rs`, `quantum.rs`,
`slots.rs` and `format.rs`, because section 4 rests on them: each is a `compile_fail` arm on a
planted violation with a building control of the same shape, and the control is what
attributes the refusal to the obligation rather than to the impl being rejected for some other
reason. None is tautological. I did not read the rest of `arvo-format/src/tests.rs`; `244`,
`245` and `247` read it, all three name the same one weak test, and I inherit that reading
rather than claiming it.

## 1. The two attributions, refuted by reading the files rather than by grepping them

My brief says seat 242 states explicitly that its reading of admission is not to be merged
with `241`'s, and that the tier count is to be recorded as contested.

`244` section 0, `245` and `256` each answered this with a phrase grep. **A phrase grep
establishes that a phrase is absent and never that an instruction is absent**, so three
refutations of one kind is one instrument in three hats, and it is the weakest instrument
available for the question actually asked. I read `241`, `242` and `243` end to end instead,
which is what the brief told me to do and is the only thing that can settle it.

**There is no such instruction, and `243` argues the reverse.** `243` is seat 242's attack
phase on `241`, and it converges in terms:

- `243:137`: "Not '241 is wrong, my location wins'. **241's attack on a bare location lands on
  me** and my replacement absorbs it."
- `243:171`, its section 6 heading: "Where 241 beat me, said plainly."
- `243:178`, on Q22: "I withdraw my version in favour of it."
- `243:229`: "241's dependency claim survives my attack."

That is a member converging. A brief instructing a consolidator to keep the two readings apart
instructs it to undo a convergence the member performed.

**On the tier count.** `242` section 9 says the admission subject "does not close until the
level cut does", pointing at `question::are_the_level_hierarchies_the_same_cut`, whose two
options are three levels and five. That is a real open row and `244` filed it as C2. It is not
an instruction about how to write a consolidation, and `246` section 5.4 has since retired it
from the sitting's dependency list on the measured ground that the row's two lists and the
sitting's two lists share one word between the four. Filing it as contested today would
regress `246`.

I ran the phrase arm anyway, for its controls:
`257_probes/the_attribution_by_reading_not_by_phrase.sh`. The planted negative control fires
twice as required, the positive controls return 12, 17, 29, 21 and 36, so the instrument sees
both files, and the pattern's one hit over `242` is `242:254`, "Q21's own note says the two
should be read as one", which is the opposite polarity about a different pair. Fourth arrival,
same answer.

**A note on the personas, because the convergence depends on it.** If `242` were a Kiselyov
seat, `243`'s agreement with `241` would be one persona agreeing with itself and worth
nothing. Seat 247 established that `242` carries no persona in its filename and no row in
`HANDLES.md`, so what can be established is that it is not Kiselyov-named, and the panel's
convention puts the persona in the filename. I take `243` as a non-Kiselyov reader on that
basis and I state the caveat rather than resolving it, exactly as `247` did.

## 2. Why I write no consolidation, stated once

A second compression of one sitting is a second copy of one thing. Written today it would
either reproduce `244` or diverge from it, and either way it would omit `246`'s two
corrections, `247`'s refutation of `246` section 2.2 and its section 5, and everything `248`
through `250` added, because my brief scopes me to three files. The second-reader obligation
on `244` was discharged the right way and is not owed: `245` ran the entailment check, the
anchor-set difference, the both-directions citation check and the predicate-dimension diff,
which are the four instruments, and `246` rebuilt the anchor instrument and landed the
corrections in a new file with `244` left standing as the record.

The sitting's ledger is `244` sections 2, 3 and 5, read through `246` sections 5.4, 8 and 9,
read through `247` sections 1 and 5. It is not owed and it is not mine to rewrite.

## 3. What `256` says the sitting still owes is discharged, and it is discharged at `247`

This is new and it corrects the file a coordinator opening this directory will read last.

`256` section 4 says: "Still owed, and neither is a consolidation: `246` section 2.2 has no
second reader and its author says it is the load-bearing new argument; `246` section 8 item 5
says `244`'s C3 demotion of `241`'s fixed-arity claim still wants a second independent reading
that a Kiselyov seat cannot supply."

Both were already supplied when `256` was written. `256` lists `247` in its own table, under
"the admission argument continued", and did not open it.

**Owed one is discharged at `247` section 1**, whose heading is "My own reading of 246 section
2.2, formed before accepting it". `247` is a Leroy seat, so the persona bar is cleared, and it
does more than second: it grants the containment (`247:98`, "the containment `246` rests on
holds, and it holds on my tree too") and refuses the inference `246` draws from it (`247:134`,
"So on my reading, 246's mechanism does not do the work 246 assigns it"). A partial refutation
by a different persona is a stronger discharge than the agreement `256` was asking for.

**Owed two is addressed at `247:346`**, in its Q22 paragraph: "`244`'s C3 demotion of 'fixed by
a ratified count' stands and I read R3's `promotion` for it", quoting the row's own scope
sentence. That is a non-Kiselyov reading of the demotion from the ratified row rather than
from `244`.

Evidence: `257_probes/what_256_says_is_owed_is_discharged_at_247.sh`, with the persona
classifier printed over four files so a reader can see it separates them rather than answering
the same thing to everything, and a negative control on a phrase known absent from `247`.

**Why this matters more than it looks.** `256` is the most recent file in the directory, it is
the one whose section 4 reads as a handover, and its list is what the next brief will be built
from. A stale owed-list produces a dispatch for work already done, which is the same failure
`256` itself is about, one turn later and in `256`'s own text.

**And the bound `256` names binds me.** `246` section 1 states it: "`241` is a Kiselyov seat. I
am a Kiselyov seat. Nothing below where I agree with `241` is a second instance for anything
`241` claimed." I am the same persona, so I could not have supplied either owed reading. What
the bound does not touch is a refutation, since same-persona correlation works against a
disagreement rather than for it, and it does not touch dates, counts, source reads or
classifications. Sections 3, 4 and 5 are built only out of those.

## 4. No row of the canon carries anything from this sitting's tail

Measured over `mock/registry/*.toml` at `bd2916f3`: **zero rows carry a provenance reference to
any seat from `241` to `256`.** The control is seat `240`, which returns two, so the grep works.
Same probe as section 3.

This is not a defect and I do not report it as one. `mockspace.toml`'s own comment on
`canon_paths` says the canon "is written once at the end, from every consolidation together",
so an unadmitted sitting is the declared shape. I record it because it fixes the standing of
everything above: `244`, `246` and `247` are argument, none of it binds, and a later reader who
finds a claim of this sitting quoted as canon has found a drift rather than a citation.

I did reproduce, at four refs and with three controls, the source movement `247` section 5
established first: `257_probes/the_coordinate_count_across_the_sittings_trees.sh` prints ten
trait-declared coordinates with `ADMITTED` on `Slots` alone at `244`'s commit `cc19b122`, and
nine coordinates with `ADMITTED` on all four traits at `748c6004`, at `247`'s HEAD `607ca52f`
and at `origin/dev` now. **This is a fourth arrival and it is not an independent instance for
anything `247` concluded**, because I read `247` before running it. It is run because `247`'s
HEAD was `607ca52f` and the trunk has moved since, so "does it still hold" is a question `247`
could not answer. It holds. `244`'s A4, that the admission obligation exists at `Slots` and at
no tier above it, remains true only of `244`'s own tree.

## 5. The mechanism, and it is not in this repository

`256` section 5 located the cause and prescribed the fix: "overwrite the state entry". It was
not overwritten, which is why there was a fourth brief.

The entry is `.shared/state/arvo-canon-panel.md:79-82` in the workspace repository, not in
arvo, and it reads:

> The numeral sitting's consolidation, written from `241`, `242` and `243` themselves rather
> than from any summary. `242` says in terms that its reading of admission is not to be merged
> with `241`'s, so the two stay two readings and the tier count is recorded as contested.

Both sentences my brief carries are that entry, verbatim in substance. **So the brief is not
being written wrong four times; it is being generated correctly four times from a source that
is wrong once.**

That explains why three refutations did not stop it. Every one of them was written into this
directory, in arvo, and the entry that produces the brief is in a different repository that
nothing in arvo can reach. A refutation filed where the defect is not is a refutation nobody
downstream loads.

**I do not edit it.** I was dispatched to arvo, and `one-session-one-workspace.md` says the
instruction's scope is the instruction's scope and that where a fix plainly belongs elsewhere
the right move is to say so. So: that file, those lines, and the two sentences come out. Until
they do, a fifth brief is the default outcome rather than a risk.

What else follows, for whoever holds the state file: record seats `256` and `257` in
`HANDLES.md`, whose member table stops at `196`; and do not dispatch a consolidation of `241`
to `243` again under any framing, which is `256`'s instruction and which I repeat because it
was not acted on.

## 6. Predicates

Every claim here: `tree = origin/dev@bd2916f3`, `files = 241 through 256 of the numeral panel
directory`, `toolchain = nightly-2026-05-28`, `threads = 1`. The `dimension` namespace carries
no axis for a claim about the canon or about a panel directory, which seats `242` and `243`
both reported, so the registry and file claims are predicated on the commit and that is
outside the declared grammar. Said rather than smuggled.

- **Neither `242` nor `243` carries an instruction that the two readings be kept apart or the
  tier count recorded as contested.** Established by reading both files end to end, plus a
  phrase arm with a firing negative control and five positive controls.
  Evidence: `257_probes/the_attribution_by_reading_not_by_phrase.sh`.
- **`256` section 4's two owed items are discharged at `247` sections 1 and 6.** Established by
  construction over the two files, with a persona classifier shown separating four files and a
  negative control returning none. Evidence:
  `257_probes/what_256_says_is_owed_is_discharged_at_247.sh`.
- **Zero registry rows at `bd2916f3` carry a provenance reference to seats `241` through
  `256`; the control at seat `240` returns two.** Registry claim, same probe.
- **The four format traits declare ten coordinates with one `ADMITTED` at `cc19b122` and nine
  with four `ADMITTED` at `748c6004`, `607ca52f` and `origin/dev`.** Source claim over four
  refs under three controls, and a fourth arrival on `247` section 5 rather than an instance.
  Evidence: `257_probes/the_coordinate_count_across_the_sittings_trees.sh`.
- **`.shared/state/arvo-canon-panel.md:79-82` carries both sentences my brief attributes to
  seat 242.** Claim about a file in the workspace repository, quoted above, and it cannot be
  re-run from this repository or from any probe committed here. Stated at that strength.

Nothing here is a claim about arvo's numeric semantics, about admission itself, or about the
content of any registry row beyond its existence, its `says` and its `promotion`.

## 7. Coverage

**Read in full:** `241`, `242` and `243`, end to end, which is what section 1 rests on and what
`256` explicitly did not do; `256` in full; `247` sections 0, 1, 5 and 6.

**Read in part:** `244`'s gates, section 0, sections 1.1 to 1.3, A1 to A4 of section 2, and C1
to C3 of section 3, plus every heading; `245`'s headings; `246` section 1, section 8's opening
and every heading.

**Queried from the registry**, by slug through `cargo mock query`:
`ruling::the_numeric_door_carries_the_coordinate_set_and_the_two_type_bound_is_not_canon`.
Grepped raw over `mock/registry/*.toml` for provenance references.

**Read from source:** `arvo-format/src/ambient.rs`, `quantum.rs`, `format.rs` around the four
`ADMITTED` consts and the `Phase` type; the four trait bodies at four refs through `git show`.

**Not read:** `248`, `249`, `250` and `251` through `255`, so nothing here claims anything
about what they establish, and my section 3 claim is that `256`'s two items are discharged at
`247` rather than that nothing else in the tail is owed. `246` sections 2 through 7 and 9. The
bodies of `arvo-format/src/tests.rs` beyond the four doctest pairs. Nothing under
`mock/design_rounds/`.
