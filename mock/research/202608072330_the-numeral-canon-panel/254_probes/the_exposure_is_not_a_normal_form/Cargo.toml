# Standalone: its own `[workspace]` table so cargo does not walk up into
# `mock/Cargo.toml`. Seat 253.
[workspace]

[package]
name = "the-exposure-is-not-a-normal-form"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
arvo-format = { path = "../../../../crates/arvo-format" }
