# 56. The four-choice model, attacked

**Date:** 2026-08-09
**Position:** file two of unit two on the topic "what is the one format concept, and what must it
cover". `55` derived the model computed = adapt(exact) cold, decomposed a format into four choices
F = (D, Q, R, E), and then read `08`, which is mine. This file attacks that model with my own
derivation, answers what `55` said about `08`, and states what I would put to `55` when it is
resumed.
**Probes:** `56_probes/`, three instruments plus a rerun record, committed as made. Every count
below is from a named output file there.
**Reading:** `INTENTS.md`, `00_brief.md`, `55` in full including its probe sources and outputs,
`08` (my own), and the register sections `55` touches: Q4, Q5, Q6, Q10, Q11, Q12, Q13, Q16, and
the wrapping entry `55` added (`OPTIONS.md:1334-1364`). I did not re-read the seed sweeps beyond
what `08` already carries.
**Register:** nothing here settles anything. Op's explore mode is in force per `00_brief.md`.

## 0. Gates

**Canon gate.** There is no ratified canon; this panel is writing the first one, and the brief says
so. The governing material is `INTENTS.md` plus the acceptance criterion, and nothing below
proposes work either forbids. The strategy set is open per I1, and nothing below presumes a count.

**Test gate.** There is no suite. `find mock/crates -name '*.rs' | wc -l` returns 0; the tree the
brief declares nuked is actually empty. I did not run a suite and I am saying so rather than
implying one passed.

**The brief's cheap factual claims, checked.** The pin resolves:
`rustc +nightly-2026-05-28 --version` reports `rustc 1.98.0-nightly (57d06900f 2026-05-27)`.
`55`'s three probes exist, compile on the pin, and their committed outputs reproduce byte for byte:
I recompiled all three from `55_probes/*.rs` and diffed my reruns against `p1_output.txt`,
`p2_output.txt`, `p3_output.txt`; all three match exactly (`56_probes/RUN.md`, rerun section). So
every number `55` cites from its own probes is a number I have independently regenerated, which is
worth more than reading the outputs and is the first thing I did.

## 1. Verdict, before the argument

(to be completed after the probes; skeleton committed early per the brief)

## 2. My derivation: the model is a tower, not a product

## 3. The R slot carries two law families, and wrap is the proof

## 4. The wrapping fork deflates: options two and three are one mathematics

## 5. The phase parameter belongs in Q, and it has an arithmetic consequence

## 6. The encoding axis: order and arithmetic are exclusive for signed sets

## 7. What 55 said about 08, answered

## 8. Fits and kills against the register

## 9. What I would put to 55 when it is resumed

## 10. What the register should gain

## 11. Coverage, bounded honestly
