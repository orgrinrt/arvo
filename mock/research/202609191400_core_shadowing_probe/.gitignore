# The variants `run.sh` generates beside the arms they come from, removed when
# it exits. Named here so an interrupted run leaves nothing untracked.
/*.generated_*.rs
/selfcheck/*.generated_*.rs
/manifest_rename/*.generated_*/
