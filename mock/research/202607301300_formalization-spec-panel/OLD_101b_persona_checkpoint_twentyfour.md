# Persona checkpoint twenty-four: the six decided, two held as op's own

**Date:** 2026-08-05
**Position:** after `101_knuth_the_periphery_re_audited.md`. Consolidation ten follows.
**Provenance:** op is asleep and delegated tonight's checkpoints to his persona at Fable tier, with the
instruction to imitate his own prior rulings in style and substance. Every ratification below is
**persona-decided** and a line he can strike.

## 1. The elementary functions: adopted, and the family closes the operation surface

**Adopted**, the whole sort. The three classes by carrier kind, the root-residue pair joining the linear
growth class, the parity theorem that roots never tie, the radix-power exponential decidable but refused
at practical width exactly as division's accumulator was, and the transcendentals with no carrier and no
ties. The taxonomy absorbing an entire family without edit is the strongest corroboration any chapter
has produced, and the sentence naming the solution-set derivation the design's general failure
classifier is earned: it has now classified two families it was not built for.

**The `ExactValue` widening is adopted as one clause**: any real with a decidable ordering against the
target grid's points and midpoints. The separation requirement found this one, and it is worth saying
so: the rational-versus-real distinction was vacuous at every operation the review ever checked until
the first irrational input arrived.

**The hardness const is adopted with both of its honest sentences.** It passes the pricing pillar's test
to the letter and its value comes only by exhaustion or citation, so *exhaustively-computed-or-cited*
enters the grounding registry as a provenance class, and the non-transfer statement (hardness at width W
is established at width W, never carried across) goes in the ratifying text verbatim. A cited const is a
trusted-base entry with the citation as its artifact, the same accounting as every hand-laid `Crosses`
entry.

**The closure sentence is adopted in the admission-test form**, not the list form. A list reopens with
every member and a test does not, and the five statements are the right five.

Owed and queued: the signed halves, the ln compile (one artifact shared with the float-division
compile), the argument-reduction laws, and second reads. **Whether any transcendental ships in the first
contracts crate is op's own packaging call.**

## 2. The periphery: the correction accepted, and the guard adopted

File 98's section 0 paragraph three is replaced by file 101's. The periphery was never ignorance; it is
op's own ratified round, thirty-two topics and fifty-two decisions, locked hours before this panel
opened, distilled into the panel's required reading, and row-rechecked at file 74. **Genuine ignorance is
two bounded items**: the sampling spec question and the SIMD saturating-lane residue. The completeness
picture is stronger than 98 reported and the reading was weaker, and 101 said both without flinching.

The anatomy matters more than the error. File 98 ran an `ls` that returned a file literally named
`inherited-state-from-the-formalization-round.md`, recorded the return value, and never opened it,
during a completeness audit. Both standing requirements passed genuinely on a file whose central claim
rested on an unperformed search, because both are inward-facing.

**Adopted, the guard:** in an audit-shaped deliverable, every universally quantified negative ("no file
has", "never", "nothing touched") carries its own freshly performed search, quoted with its date, the
way a judgement carries a `file:line`. A negative claim inherits the date of whoever last searched, and
citing a search is not performing one. Not a fifth design rule; a requirement beside the separation
requirement, one sentence of cost.

Order of work: consolidation ten next. The three remaining content reviews (platform, predicate D15/D17,
bitfield) head the following stretch with the owed second reads. The measured cost of a periphery review
is now known twice over, about one dispatch at the cheap end, and the yield both times was compiled
corrections to stated grounds.

## 3. The round's two errors, and the overtakes

**D10's storage count is corrected and the decision stands.** Degrees of freedom and storage are
different counts, the argument used the first where it needed the second, the divergence starts at rank
4 and the comparison reverses at rank 7, exhibited with exact rational rotors. D10 survives on the
grounds it also gave: rotors generalise, motors compose rigid motion. What changes is the bench:
**D41's matrix must include at least one rank of 4 or above**, because rank 3 compares a representation
against itself, and file 100's requirements table is adopted as the artifact D41 ordered first.

**The closed-interval finding is adopted, both halves.** The purely fractional dyadic numeral misses its
own identity by exactly one quantum, the root family's overflow band opens at the same absent element,
and the fix is a value of a parameter the identity contract already seals, compiled to F = 24, no new
mechanism. Both routes ship and the trade is named, per the toolbox rule; what does not ship silently is
the numeral carrying neither endpoint.

**The shape split is confirmed, op-on-op.** D43 postdates D4 in the same round and its sentence is the
`Lowering` charter one dimension up. **The one-door repair is adopted as a working shape**: section
1.26's sentence has a rank in it nobody wrote down and is false above rank 0, the divergence is a
compiled number, and both halves of the fix are needed. Second read owed at the attack surface file 100
named itself.

**The overtakes, three sentences into the spec text:** the round's `Growth` vocabulary is dead, D34's
content surviving as `StoredWidth`'s doubled instance; D36's open shape is closed by `Adjustment`; and
the FLX drop is recorded as made-by-provenance rather than argued. **The FLX reinstatement question is
op's alone**: the determinism argument that made the unbounded member worth having was his own D49, and
a stand-in does not decide whether a format family grows a member against a ratified two-instance axis.

**The geom seam is adopted as the working shape.** The multilinear half settles now; it composes over
the settled surface at no new growth class and no new failure kind. The model-choice half waits behind
D41 and D11 exactly as ratified, and the convention becomes a type parameter the bench measures rather
than a fork the substrate resolves.

## 4. The footprint bench: ratified, and the retraction stated as bluntly as it deserves

**Ratified.** At the one size where the packed column fits L2 and the dense one does not, the multiple
drops to 1.43x from 1.66x, driven by dense's own cost rising sixteen percent across the sweep while
packed's stays flat, cross-validated at every reported size, decoder-to-decoder multiple flat while the
layout-to-layout multiple moves. That is the correct signature and the first real footprint signal this
review has produced. It is modest, and the file says so instead of dressing it up, which is why it can
be ratified at all. The two named regimes stay open with their artifacts. `Cold`'s standing does not
move either way; the rule's authority plus one honest signal is the current state, said out loud.

**The blocker retraction is accepted and it is the same disease as call two.** Three files and two
checkpoints carried "the by-reference path needs building upstream" when it had shipped, in the same
crate every bitpack bench already depends on. One grep each. The guard adopted in call two covers this
case by construction, because "no mechanism exists" is a universally quantified negative.

One upstream consequence, mockspace-side: the harness silently merging two variants' samples under one
duplicate label into an internally consistent wrong number is a defect rather than a lesson. **The
harness refuses duplicate variant labels**, fixed upstream through a feature branch and PR.

*Dispatcher's correction, recorded because the persona flagged it on file 96's wording:* the trait-doc
clarification did **not** land directly on mockspace's trunk. It went through
`docs/bench-input-ceiling-note`, PR 12, a senior review that returned four findings (an intra-doc link
unresolved under default features, an over-broad "only caller" claim, the fact that `build_input`
remains a required method so the by-value constructor is still written, and equivalence pinned at one
size), all four fixed, then a squash merge at `5c65183`. File 96's phrasing was loose; the flow held.

## 5. The second reads: both disagreements go the challenger's way

**The domain-preservation equation is retired from spec text.** Compiled both ways: under the totality
refusal ratified in the same checkpoint, every declarable pairing has a total decode, the door is
unconditionally domain-preserving, and file 95's case B is constructible only in a model that deletes a
statement adopted at the same sitting. The fact lives at the pairing declaration and nowhere else; the
door-side equation survives as a compile-fail pin. Two organs for one fact is the defect this review has
now paid for three times, and it does not get a fourth instance in the ratifying text.

**The cannot-check against cannot-provide split is adopted**, and this one is not close. Refusing a
declaration because a field is unverifiable converts an honest trusted-base assumption into refusal on
ignorance, which is the policer posture by the design's own boundary sentence, and it is the same line
as the declined refusal at `74b`: the consumer who controls their deployment signs the assumption
knowingly, and unverifiable and false are not the same thing. Cannot-check enters the trusted base with
its artifact gap declared through the verdict-split receipt, four instructions unchanged; cannot-provide
is a statically known falsehood and refuses. The required-field relation is the genuinely new open item
and gets its dispatch.

The rest as file 97 disposed, second reads riding the normal queue. The fourteen-against-fifteen count
stays recorded as what it is: the demonstration that naming a command is necessary and not sufficient.

## 6. Three tautologies, one commit, and it is op's

The disposition ruled at `95b` does not change; the count does. The known one, now twenty-four files
carried. Its sibling at the second implementation, unfound because nobody read the impl beside the test
until file 100 did. And a third in the hash crate, the same computation on both sides of an assertion,
in the green total for one hundred files. All three are in `mock/crates`, which is op's boundary:
**one trivial commit, outside the panel, deletes all three outright.** Not improved, deleted; a tautology
occupies the space where a missing test would be noticed.

## Standing

Consolidation ten next, carrying this checkpoint. Then the three periphery reviews and the owed second
reads, open list order. **Confirming or striking `90b` and `95b` remains the single largest move
available and remains op's alone**, as do the FLX residue and the transcendental packaging call. Only
op's calls are final; nothing here is one until he says so. The panel produces canon, not source;
`mock/research/` and `mock/benches/` are its ground and `mock/crates` is out of bounds until the canon
is complete and earmarked as arvo's first full canon.
