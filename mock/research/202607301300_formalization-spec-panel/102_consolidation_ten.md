# The current shape, tenth consolidation: the elementary functions given a chapter, the periphery corrected from ignorance to ratified ground, two requirements given a moment and owner, and the division fork dissolved to nothing

File 91 stood as the reference after the ninth consolidation hardened the eighth's absorption without
growing it. This document replaces it, and it absorbs a stretch that both hardened and grew: ten
deliverables (files 92 through 101) plus two persona checkpoints (`95b`, `101b`, both persona-decided at
Fable tier during nights op slept through, both strikeable on his word) against material that repeatedly
turned out to be less settled, and more settled, than the file before it believed.

The stretch's own arc explains why. File 92 (Peyton Jones) took the two working shapes `90b` adopted with
second reads owed, the `NicheCarrier` seal and the mutation repair, and found the seal narrower than its
own wording and the repair's domain unstated. File 93 (Lattner) took the division instinct `90b` adopted
and killed it: the `Door` placement smuggles a value onto the one axis the design forbids to carry one,
on four compiled or silicon-read facts, and the fork's other alternative carried the identical smuggle at
a different address, so both were dead the moment the question was asked correctly. File 94 (Muratori)
took the refined naming principle and found its class test keyed on the wrong observer, the compiler
rather than the reader who holds one token at a call site, with `91:850-851`'s "every existing name
passes" false at three names. File 95 (Pesce) attacked all three, one-passed each in different ways, and
then asked the harder question the checkpoint had not posed: does the separation requirement, adopted for
exactly this shape of failure, actually catch what this stretch found. The answer reframed the whole
stretch: one hit, working as designed, missed only because nobody ran it; two misses, both caught instead
by the definitional-completeness line, which had fired zero times in advance because nothing named who
performs it or when. File 96 (Aaltonen) built the footprint bench the review had ranked top of its open
list twice, and found the blocker it was thought to need had shipped before the review started asking for
it. File 97 (Leroy) ran the owed second reads and sent two of `95b`'s nine working shapes back: one
equation retired because the fact it guarded already has a home, one clause reversed because it converted
an honest trusted-base assumption into a refusal on ignorance. File 98 (Knuth) answered op's own waking
question, how complete is the canon, and reported the periphery as the review's last unbounded ignorance.
File 99 (Smith) then wrote the chapter that ignorance was hiding, the elementary functions, and closed it
at the estimate file 98 gave it. File 100 (Quilez) opened the periphery file 98 had flagged and found it
was not empty ground at all. File 101 (Knuth, again) confirmed that finding at every remaining row and
named the exact shape of his own file 98's error: a completeness audit that never opened the one `ls`
result that would have corrected it, over ground that was op's own ratified design round the whole time.

**Verification.** Every claim below tagged compiled, measured, or verified traces to a probe in
`92_probes/` through `101_probes/`, each carrying its own outcomes file, or to the shipped tree at the
cited path and line, re-read fresh for this document. The numeral tower still has no shipped source:
`grep -rln "Adjustment\|Bias\|Numeral" mock/crates/ --include="*.rs"` and the same with
`FullRange\|UTerm\|AddWidth` both exit 1, empty, run fresh at HEAD `53f1b3f`. `cargo test --offline
--workspace` reports **672 passed, 0 failed, 9 ignored across 155 test binaries** from a clean committed
tree with no manifest drift, matching every count files 97 through 101 reported while a concurrent
dispatch's uncommitted footprint-bench work was still in flight; that work is now committed (file 96) and
the count is the tree's own settled state, not an attribution. The pinned toolchain is `rustc
1.98.0-nightly (57d06900f 2026-05-27)`, `aarch64-apple-darwin`, confirmed fresh inside the tree for this
document. The bench harness ran this stretch for the first time in six files (file 96), on the mechanism
`91` and two checkpoints had wrongly believed needed building; its committed CSVs are the footprint
findings in section 1.22.

**The table-diff obligation, executed on this document by its own author before it stands**, per the
standing rule. Every table below was checked against the prose of its section and the source file that
established each member.

**The two requirements' own new clause, performed on this document rather than merely stated.** The
definitional-completeness line's performance is the term list with dispositions, in section 6. The
separation requirement's performance is, per model this document relies on, the axis it separates and the
two instantiations at which it separates, also in section 6. The freshly-performed-search requirement
adopted this stretch is performed inline: every universally quantified negative below (a claim of the
form "no file has", "never", "nothing touched") was checked this session and its search is named at the
claim, not inherited from a source file's own search performed on an earlier date. Three such checks
surfaced fresh findings while drafting this document and are recorded where they land: the three
tautological tests all still stand at source, re-verified this session (section 3); the test count is the
tree's own settled 672/0/9 rather than an attributed drift (above); and one droplist item below (the
`91:12-13` false performance claim) is confirmed still uncorrected in the tree's own committed history.

**On the two sentences below that are this document's own resolution rather than a restatement of a
source finding.** The consolidated loudest-for-op list in section 2 is this document's own judgment about
which items are still live. The explicit marking, throughout section 1, of which content is op-ratified
versus persona-ratified versus panel-converged and checkpoint-silent, is this document's own application
of the provenance discipline the review has held since its founding, sharpened here because this stretch
produced the review's own sharpest instance of the failure that discipline exists to prevent (file 98's
periphery error, section 1.25 and section 4). Everything else below is a compression of a claim a source
file already made, tagged with that file.

## The four design rules, and the three requirements that check the review's own models

Unchanged in count from the ninth consolidation. The four design rules take no new content this stretch;
every finding filed under one of them without needing a fifth. The three requirements, all governing how
the review verifies its own claims rather than what the design contains, gain real content: two of the
two prior requirements are given an owner and a moment for the first time, and a third joins them.

**The spine rule.** Unchanged. No new instance and no new attack this stretch.

**The carrier-at-birth rule.** Unchanged in statement. One candidate instance from the ninth
consolidation, `NicheCarrier`, is now closed material at a narrower name: section 1.12 below.

**The layer-keying rule.** Unchanged in statement. One new instance this stretch: the shape/storage split
(section 1.28) is the `Lowering` charter's own "changes no value, `Encoding` may change which datum
carries it" statement, applied one dimension up to a shape's rank and extents against the bytes that
carry them, confirmed by the same design already having worked the pattern out twice before.

**The pricing pillar.** Unchanged in statement. Three new sites this stretch, each a quantity that is a
function of the type's parameters alone and belongs on the type as an associated const rather than
computed at value position: the capacity pairing's own agreement fact (section 1.28), the environment
receipt's per-field fold (section 1.27), and the hardness constant a transcendental's correct rounding
needs (section 1.16, the one place the pillar's own test is passed to the letter while the value itself
resists computation, section 1.16's own honest exception).

**The definitional-completeness line, widened and given a moment.** *When a structure is ratified, every
term in its definition, including the name being defined, is either defined or named open in the
ratifying text.* The widening (`the name being defined`) is new this stretch, adopted at `95b` from file
95's proposal: a name defined twice with different content is defined nowhere, which is the mechanical
form of file 94's naming finding and requires no separate naming rule to state it. The moment is new,
adopted at `95b` from the same proposal: **the line is performed by the author of the ratifying text, on
that text, before it stands, and the performance is reported rather than cited**, in the same shape the
table-diff obligation already has. The evidence for the moment is blunt: the ninth consolidation's own
claim to have applied the line to everything it absorbed (`91:12-13`) is false at three of its own
sentences, grep-checkable, because the line had fired zero times in advance across the whole corpus
before this stretch. `91:12-13` is on the droplist below as a false performance claim, the same register
as file 79's search sentence.

**The separation requirement, given the same moment.** *A claim about a distinction is checked at an
instantiation where the distinction is nonvacuous, and every model states what it separates.* Unchanged
in statement, given the same owner-and-moment clause this stretch: performed by the author, on their own
models, reported rather than cited. The evidence for keeping it, rather than replacing it: it caught the
largest finding of the last stretch in its own first hour of existence and, applied to this stretch's own
three one-pass files by file 95, it is the requirement that worked, missed only because nobody had run it
in advance. The correct response to a requirement that works but goes unrun is a moment naming when it
runs, not a new requirement; that reasoning is itself the content of the third requirement below.

**The freshly-performed-search requirement, new this stretch.** *In an audit-shaped deliverable, every
universally quantified negative claim ("no file has", "never", "nothing touched", "unexamined") carries
its own freshly performed search, quoted with its date, the way a judgement carries a citation. Citing a
search performed by an earlier file is not performing one.* Adopted at `101b` from file 101's own
diagnosis of file 98's error. Not a fifth design rule; a requirement beside the separation requirement,
costing one search per negative claim. The evidence is the worst single finding of this stretch: file 98
reported the review's periphery as unbounded, unexamined ground, on the authority of two files whose own
searches had gone stale (`11:44-56`, written before file 74's recheck; `72:52-60`, true when written and
falsified two files later), plus an `ls` of the design-round directory whose return value, a file named
`inherited-state-from-the-formalization-round.md`, was recorded and never opened. Both existing
requirements passed genuinely on that file, because both are inward-facing: they audit a file's own terms
and models, not the sources it failed to read. The new requirement audits exactly that gap.

## 1. The agreed shape

### 1.1 What a number is

Unchanged from file 40 and every consolidation since.

### 1.2 The identity contract

Unchanged.

### 1.3 Encoding, nested inside Lowering

Unchanged.

### 1.4 The crossing contract

Unchanged this stretch. Statement 0's hardening, statement P, and statement C all stand exactly as the
ninth consolidation states them; the ratified first clause of section 1.3 ("`Lowering` changes no value")
is this stretch's own load-bearing citation for division's dissolution (section 1.13), confirmed at
source by file 93 and corrected in its own citation by file 95: the guard file 93 quoted
("no law may read `Lowering`") is satisfied vacuously and was never the operative sentence; the operative
sentence is the first clause, and it forbids the smuggled cell outright with no new text.

### 1.5 The quantiser

**Widened this stretch, by one clause, to admit an irrational input for the first time.** Unchanged in
mechanism (round-first, classify second). The quantiser's input, `ExactValue`, had no ratified domain
statement; every operation checked before this stretch delivers a rational exact result (the membership
theorem makes every arvo value one, and the field operations stay inside it), so the gap was invisible
because it was vacuous at every instantiation the review had ever checked. The root and transcendental
families (section 1.16) are the first consumers whose exact result leaves the rationals, and the
separation requirement is what found the gap, file 99's own application of it. Adopted this stretch:
`ExactValue` is any real number equipped with a decidable ordering against the target grid's points and
midpoints. Every existing operation's exact result is rational and the widening changes no ratified
behaviour; it legalises the roots' and the radix-power exponential's irrational inputs and states the
decidability condition that grades the elementary functions' carrier classes (section 1.16). One clause,
owed to the definitional-completeness line the day any of section 1.16 is ratified, supplied here.

*Grounded on: settled shapes (`91:184-195` the membership theorem, `29:111-116` the quantiser signature),
compiled (`99_probes/probe_1` through `probe_4`), reasoned (the vacuity observation, `99` section 0,
two-read status: one pass, file 99 alone).*

### 1.6 Membership and the number-system layer

Unchanged this stretch.

### 1.7 The algebra

Unchanged this stretch.

### 1.8 The fold

Unchanged this stretch.

### 1.9 The multiplicative half

Unchanged this stretch. Reused a third time this stretch, in the elementary functions' derived class
(section 1.16): integer `pow` is an iterated `mul_full` with one quantisation at the root, the fold
chapter verbatim.

### 1.10 Widening and Growth

**Unchanged as a ratified position on the axis table, and one further overtake recorded this stretch.**
`Widening` and `Growth` left the table at `39b`, closed shut. This stretch's periphery re-audit (file
101, section 2.1) found that the design round `202607300800`, decided the day before this panel opened
and unread by the panel in content until this stretch, had independently proposed a `Growth` policy axis
(`FullPrecision`, `KeepLsb`, `KeepMsb`, `Specify`) and had renamed its own D34 finding (`Warm`'s doubled
headroom) as an instance of it. The panel's later ratification at `39b` removed the axis entirely,
op-checkpointed, after the round had already locked its own version. **The round's `Growth` vocabulary is
dead; D34's own content survives, wearing different clothes, as the `StoredWidth` axis's `doubled`
instance on `Lowering`, with the container never declared and always projected** (`91:495-520`, forced
three ways and independently confirmed, unchanged). D34's principle stands; its vocabulary does not; the
overtake is op-on-op, later checkpoint winning over earlier round text, provenance clean at every step.
Adopted at `101b`.

*Grounded on: ratified (`39b`, `101b`), settled shapes (`101` section 2.1, `round:2022-2027`,
`round:2177-2178`), reasoned (the overtake reading, mine, two-read status: one pass, file 101 alone,
though the underlying `39b` removal is fully ratified and unaffected).*

### 1.11 The value-unique encoding

**One overtake recorded this stretch, the round's D36 answered from the panel's own later side.** The
round left "the shape of the fix" for a UNORM-shaped encoding open, naming an MV-chain as one candidate
(`round:1439-1451`). The panel's own ratified `Adjustment` grammar (`40:493`, `91:153-154`) closes it: a
closed-interval numeral, the kind a rotor component, a direction cosine, or a normalised colour channel
all are, is not a new type. It is `Adjustment = 1/(r^F - 1)` on an ordinary significand, a value of a
parameter the identity contract already seals, compiled to F = 24 with no new mechanism (section 1.28
below, file 100). Round and panel converged on the same mechanism under different names; the MV-chain
observation survives as the literature name for what the instantiation is. Adopted at `101b`.

*Grounded on: ratified (`40:493`, `91:153-154`, `101b`), compiled (`100_probes/probe_5`, exhaustive to F
= 24), reasoned (the convergence reading, `101` section 2.1, two-read status: one pass on the framing,
though the underlying `Adjustment` mechanism is fully ratified).*

### 1.12 The seal: eleven firings, capacity closed, the niche vocabulary closed and narrowed, the trusted base shrunk to one sentence

**Two independent reads complete on every clause below except where marked; this section replaces the
ninth consolidation's own section 1.12 in full rather than amending it, because every one of its open
questions closed this stretch.**

Eleven firings stand, capacity needs no seal, both unchanged.

**The `NicheCarrier` vocabulary is renamed `NonZeroCarrier`, narrowed to the closed `NonZero` family, and
its audited entry's trusted base shrinks to one sentence.** File 92's fabrication attack held: the seal
holds over every introduction route by a distinct compiler error class (direct impl `E0277`, supertrait
`E0603`, layout-identity wrapper `E0277`, non-member instantiation `E0277`, re-impl and downstream
blanket closed by the orphan rules with no probe needed), with two positive controls and upstream growth
closed by construction (membership is per-type impl; the language has no way to quantify over "carries a
niche"). What did not hold as adopted was the vocabulary's own wording. `char` is a counterexample, not a
member (its validity set has a non-inhabitant strictly between two inhabitants, so the audited entry's
"single excluded run at zero" sentence is false of it); `bool` is redundant, expressible by an ordinary
field shrink at zero cost; a reference is a locus error (a byte-image carrier has no lifetime parameter).
Only the `NonZero` family is exactly right and exactly alone. File 95's tension reading (the seal's own
principle, one entry per shape rather than one entry per phrase, implies the vocabulary's genus name
should not wear one shape's own name) produced the rename, confirmed at file 97's second read: the token
composes correctly with the level vocabulary (the niche type is the carrier level of its lowering), and
the genus name `NicheCarrier` stays free for a second shape's second vocabulary should one ever earn
admission. **Ratified, two-read complete: `NonZeroCarrier`, the `NonZero` family, closed and enumerated,
one entry per shape.**

The audited entry originally bundled four facts; three of them are const equations over type parameters
and belong at declaration, per the pricing pillar. **Ratified, two-read complete: the trusted base is one
sentence, the excluded pattern's unreachability in safe code per the member's own documented contract in
`core`.** Inhabitant-totality of the decode (domain cardinality equal to `2^w - 1`, or a declared
cohort-style many-to-one decode, the identical two options the fields level already has) is a
declaration-site const refusal on the pairing of numeral and niche carrier, confirmed unconditional at
file 97's second read: under this refusal every declarable pairing has a total decode, so a proposed
door-side domain-preservation equation (adopted as a working shape at `95b` from file 95's own compiled
counterexample) guards a region the ratification in the same checkpoint already forecloses, and is
retired to a compile-fail pin rather than kept as spec text, two organs for one fact being a defect this
review has now paid for three times. The width claim (the "same width as the value" fact) rests on
discriminant elision, documented by std only for the `NonZero` family, and moves to a construction-door
const assertion in the stack's own `Maybe`-shaped vocabulary, the mechanism `notko` already ships
(`notko/src/maybe.rs:40-45`, `MaybeNull`'s per-instantiation sealed-bound-plus-const-assertion pattern).
The no-wrap scope condition is already per-witness arithmetic and needs only to be stated as a
declaration-site refusal.

**Ratified, two-read complete: the mutation gap is real, and the repair is a two-tier construction whose
theorem's domain is now stated rather than implied.** The only-door rule as written covers observation,
not mutation; `repr(transparent)` makes every bit writable through a transmute for the identical reason
it makes every bit observable, and this design ships raw mutable doors regardless. File 92 stated the
repair (a safe surface exposing no raw accessor below the fields' own width closes the gap structurally;
an unsafe escape hatch carries a documented, unenforced trusted-base postcondition) and file 95 compiled
the domain the wording had left unstated: "no raw accessor" undercounted the perimeter, missing a public
field (the workspace rule's own worked example), a safe `union` field write (a route this design has no
instance of but must exclude by name), and the `Bitpacked` layout's own granule, where the byte owner at
column granularity is the column, not the element, so a column exposing `&mut [u8]` of its backing bytes
reopens the gap with zero `unsafe` anywhere (compiled, `95_probes/probe_2`: per-element reads agree after
the write, digests diverge). **Adopted, the repaired statement**: the theorem runs per byte-owner and per
level, mirroring statements 0, P, and C; the safe surface of a level's byte owner (the carrier at Dense,
the column group at Bitpacked) exposes no public field, no `DerefMut` to the representation, no
foreign-bytes constructor outside statement C's named obligation site, and no accessor below that level's
own write granule; whole-value replacement and interior mutability are inside the safe surface and
preserve canonicality by move semantics; a Rust `union` anywhere in the chain would be a safe raw door
and is excluded by name. **The terminating clause, second-read confirmed at file 97 with its one
undefined term repaired**: the statement runs over every level whose byte owner is a type this design
ships and terminates at the first level whose byte owner is a consumer type (not "constructs", which
would terminate nowhere under the no-heap rule); beyond it the postcondition is one trusted-base entry
stated once at the hand-over, discharged provably wherever consumer-held bytes re-enter through statement
C's foreign-bytes constructor, so the entry governs residence rather than return. File 88's finding that a
datum-keyed digest is immune to this gap in one operation narrows its urgency and blast radius (raw-byte-
keyed consumers only) without narrowing the theorem's domain, since the free raw-buffer shortcut is the
economically load-bearing digest path under `Bitpacked`.

**Ratified, two-read complete: an integer-typed door onto a niche carrier is forbidden outright; a
niche-typed door is safe and unconditionally domain-preserving.** File 92's combined-case compile found
the two working shapes interact decisively: the identical integer-typed raw door shape whose violated
postcondition costs decorrelation on a padded carrier costs undefined behaviour on a niche carrier, with
**zero diagnostics on the violating store**, quieter than any transmute the review had previously found.
The repair is to type the door at the niche member, which returns the soundness obligation to the type
system unconditionally: every store is a safely-constructed member of the niche type, which cannot carry
the excluded pattern, so no obligation sits on the door at all. File 97's second read confirmed this
unconditional half and retired the conditional half (the domain-preservation equation, above) as a
duplicate of the entry-level refusal. This is not the policer posture: a door whose violation is
undefined behaviour has no consumer who owns the consequence, because the optimiser owns it, which is a
different case from the toolbox rule's own protected choices.

*Grounded on: ratified (`90b` the working shapes, `95b` the seal narrowing and entry shrink, `97` the
domain-preservation retirement and terminating-clause repair, both two-read), settled shapes (`74:683`
D16, `55`/`62`/`64` the `Arity` seal precedent, `88:228-249`), compiled (`92_probes/` in full,
`95_probes/probe_1`, `probe_1b`, `probe_2`, `probe_3`, `97_probes/probe_1`, `probe_1b`, all fresh this
session's predecessors and re-read for this document), verified at source (`notko/src/maybe.rs:30-45`,
`arvo-transparent/src/lib.rs:64-125`).*

### 1.13 Division: dissolved, not resolved, and the design gained a general failure classifier in the doing

**This is the second-largest single finding of the stretch, and it ends in less spec text than it began
with, which is the correct shape for a fork whose two alternatives turn out to be the same mistake at two
addresses.**

The operation surface adopted at `44b`, exactly as file 43 stated it, stands unchanged: `div_floor` and
`rem`, each exact, each partial on the divisor's nonzero-ness, bound by a compiled Euclidean law; general
`div` kept, the atomic `quantize(exact quotient)`, implemented from the pair.

**The `x/0` fork, adopted at `90b` as an instinct pending a stress test, is dead. Both alternatives were
dead the moment the question was asked correctly.** The instinct (alternative 1, `Hot`'s cell placed on
the `Door`) was the stress-tested candidate. File 93's attack read the instinct's own load-bearing
sentence ("the target's own divide instruction defines the answer") literally and checked it against four
compiled or silicon-read facts. aarch64 returns 0 for a zero divisor at every dividend, **including a zero
dividend**, erasing the very two-way split the placement existed to preserve. x86-64 defines no value at
all; raw `idiv` faults and the process dies. RISC-V (external, primary read owed) defines a third value.
LLVM's own IR carries undefined behaviour on a zero divisor on every target, and the optimiser uses the
licence: a zero-divisor guard placed after a division is deleted, on both compiled targets, so what the
target gives away for free the toolchain takes back before the design ever sees it. A safe, total
operation delivering target-varying values from identical operands has no precedent anywhere in this
design. And file 95 showed the fork's *other* alternative (the finding's sharpest correction) carried the
identical smuggle at a different address: op's own `Warm` derivation, the shipped tree's own convention,
and the instinct's `Door` placement were three independent inventions for the same cell with no
derivation among any of them, which is the strongest evidence available that a value is being authored
where none should be, not merely relocated.

**The ground for the reversal was already ratified, and the deletion test is its decision procedure, not
new canon.** File 93 first cited the weaker half of section 1.3's own ratified sentence ("no law may read
`Lowering`") and found it satisfied vacuously, since every division law is conditioned on a nonzero
divisor. File 95 corrected the citation: the operative clause, unamended since file 40, is the first
sentence, "`Lowering` changes no value." A cell whose content is 0 on ARM, a fault on x86, and a third
value on RISC-V is `Lowering` changing a value, forbidden outright, no new sentence needed. **The
deletion test is adopted as that clause's decision procedure, which it never had**: state the cell with
the `Lowering` deleted; if a value-layer sentence remains (a resolution-vocabulary member, a constant
derived from the numeral's parameters, or a parameter the consumer names), the lowering implements it; if
nothing remains, the lowering authored it, forbidden. File 97's second read closed the remainder
condition's own open enumeration: the admissible remainder is stated as a closed condition (no free term
keyed on the `Lowering` axis survives the deletion), with the three named kinds as examples rather than
the exhaustive list, and the test's scope is value-layer cells; datum-keyed facts (the digest's masks, the
byte image) legitimately read `Encoding` and are outside it by section 1.3's own second sentence.

**The solution-set derivation, adopted, close to file 93's own form, and confirmed the design's general
failure classifier by absorbing a second family (section 1.16) without edit.** Division is the inverse of
multiplication: the exact quotient of `x` by `d` solves `q * d = x`. The solution set has three shapes,
and the failure vocabulary is a function of the shape, not of a target. A singleton is the ordinary
quotient, governed as ratified. An empty set with a determinate divergence direction (`x/0`, `x != 0`) is
where a one-sided limit is genuinely sound (fixing the dividend at exactly zero and driving the divisor,
with no license to also perturb the dividend, which the original limit argument's `0/0` clause had done
without saying so, a false clause repaired here rather than restated); this borrows the range event's own
resolution row in that direction, at every preset, which is IEEE clause 7's two-way split recovered as a
theorem rather than cited. Everything, or empty with no direction (`0/0`, and `Sqrt` of a negative), needs
no limit and privileges no value; the event is `invalid`, resolving to NaN where the numeral carries one
and to partiality (refused at declaration where the divisor's domain is a predicate, carried in a niche
otherwise, at zero layout cost) where it does not. **No third position on the `Resolution` axis. The
"third position" need shrinks to two mechanisms the design already has, NaN and partiality.** The sign
convention for a single-zero fixed-point numeral (no signed datum to derive a side from, so the `+0`
convention is stated once as a citation-shaped clause rather than derived) closes the last unstated term,
per the definitional-completeness line.

**`Hot`'s cell is resolved by ownership, and the record now shows three independent inventions for it as
the evidence the cell belongs to the consumer.** The shipped tree returns the numerator (a stated
convention, tree-fact, why-evidence only); file 89's alternative returned the host's 0; `90b` delegated to
the `Door`. Three authors, three answers, no derivation among them, which by the toolbox rule's own test
(is the consumer the one who knows the answer) settles the cell as the consumer's. **Adopted: a total form
over a possibly-zero divisor takes a fallback the consumer names, `div_or`-shaped, with arity two, not
one.** File 95 caught file 93's own collision: a single fallback for a total `div_or` collapses the
distinction the solution-set derivation just paid to establish, since for a NaN-less numeral the
directional cell and the indeterminate cell share the same input, `x = 0`, and one value cannot answer
both without either denying `0/0`'s own partiality or being unreachable at the directional cell. **The
fallback's arity is the arity of the failure taxonomy at that input: a directional fallback for the
empty-with-direction cell, an indeterminate fallback for the everything-or-nothing cell.** File 97's
second read closed the remaining open question, the slot's own domain: the fallback fills the cells the
numeral's ratified resolution row leaves unanswered, and does not shadow a row that already answers a
cell (at `Warm`/`Cold`/`Precise` the directional cell already has a clamp or far-point answer; only
`Hot`'s `ReduceModulo` and the NaN-less `0/0` are genuinely holes). A per-call override of a ratified
resolution row would be a second home for resolution policy, the two-organs defect this review has now
paid for three times, and the design has one home for it, the `Resolution` axis itself. `Hot`'s fast body
survives with none of the disputed authorship: where a consumer's fallback coincides with the target's own
constant (0 on ARM), the lowering emits the bare divide as an implementation of the stated function, seven
instructions, Kind-1 cfg-gated per `arvo-always-optimal-internals.md`, measured (`93_probes/probe_4`).
**The `Door` decides cost, never content**, is the entire legitimate residue of the original instinct, and
it is enough.

*Grounded on: ratified (`44b`, `90b` the instinct, `95b`/`101b` the dissolution, `91:158` section 1.3),
settled shapes (`43` in full, `84:145-160`, `84:239-263`, `84:419-427`), compiled and silicon-read
(`93_probes/probe_1` through `probe_4`, `95_probes/`, `97_probes/probe_3`), external (ARM DDI 0487,
Intel SDM #DE, RISC-V unprivileged ISA, LLVM LangRef, all secondary, primary reads owed with the existing
IEEE clause 7 bundle).*

**Owed, named with closing artifacts.** The signed halves of file 43's probes 2, 4, and 5. The
float-division compile against a `Specials`-bearing model numeral, now also owed by the elementary
functions chapter's `ln` (section 1.16), one artifact serving both. The IEEE clause 7 and 7.6
primary-source reads, now joined by the ISA bundle (ARM, Intel, RISC-V, LLVM). Division's grading axis:
**genuinely op's own, held since checkpoint ten, untouched by this stretch's dissolution**, which resolved
the fork without touching the axis question.

### 1.14 The grade: two counts at two layers

Unchanged this stretch. The site count and the moved count stand exactly as the ninth consolidation
states them, two-read confirmed, persona-ratified at `90b`. This stretch's elementary functions chapter
extends the moved-count side with a theorem rather than an amendment (section 1.16): for a transcendental
member, the moved count equals the site count at every operand off a finite removable list, because no
grid point or midpoint of an arvo numeral is ever hit by a transcendental result (Lindemann-Weierstrass),
the one place in the design where the expensive count is free.

### 1.15 The exponent forces the spine rule open a second time

Unchanged.

### 1.16 The float model, and the elementary functions given their own chapter

The far point, `Underflow`'s two instances, `Specials` as a four-point product, the `TotalOrd` split, and
`quantize`'s failure kind (an ordinary range event on `At<N, Q>`, not a new failure kind) all stand
exactly as the ninth consolidation states them.

**New this stretch: the elementary functions past division, the operation family the review's own
completeness audit named its last genuine gap (section 1.25) and closed the same week it was named.** File
99's method is the one that carried multiplication and division through: for each function, ask what the
exact result is, what finite object decides its rounding, and how wide that object grows. The answer sorts
the whole family into three classes by the kind of the exact carrier, and the solution-set derivation
(section 1.13) classifies every domain event in the family without edit, which is the derivation's second
family and the strongest evidence it is the design's general failure classifier rather than a
division-specific mechanism.

| Class | Members | Exact carrier | Decision width | Ties | New mechanism |
|---|---|---|---|---|---|
| Derived | `recip`, integer `pow` | division's pair; the fold | inherited | inherited | none |
| Root | `sqrt`, fixed n-th roots | the root-residue pair `(m, r)` | linear, `P + F` bits | impossible, a parity theorem | none |
| Radix-power exponential | `exp2`-shaped, matching-radix grids | integer power comparison | exponential in `F` | impossible off integer exponents | none, refused at practical width like division's accumulator |
| Transcendental | `exp`, `ln`, `sin`, `cos`, `atan` | none exists | empirical, the hardness constant | impossible, Lindemann-Weierstrass | one: the hardness constant's provenance class |

**Two members are derivations, not chapters, and inherit everything.** `recip` is general division with
the dividend fixed at one, `quantize(1/x)`, not the exact subfamily (which lifts the divisor to type
position; `recip` fixes the dividend), inheriting the general quotient's quantiser path, growth class, and
grading. Integer `pow` is an iterated `mul_full` with one quantisation at the root, the fold chapter
verbatim, with `x^0`'s domain corollary (starting at one over an identity-free numeral) falling out of the
`Identity` bound with no new text.

**The root family joins multiplication's linear growth class, the cheapest new instance this stretch
produced.** For same-grid unsigned sqrt at operand index `k` and quantum `2^-F`, the exact result index is
`t = sqrt(k * 2^F)`. The finite exact carrier is the pair `(m, r)` with `m = isqrt(k * 2^F)` and
`r = (k * 2^F) - m^2`, bound by `m^2 + r = k * 2^F`, and **correct rounding to nearest is the single
comparison `r > m`**, by squaring the midpoint, compiled exhaustively at nine `(P, F)` shapes against a
definition-shaped oracle that never computes a root. The widest integer the decision touches is the scaled
operand itself, `P + F` bits, so the root family gains a linear-growth-class member without the design
gaining a fourth growth class. **Ties are structurally impossible, a parity theorem**: a rounding tie
requires `4r = 4m + 1`, even equal to odd, zero ties at every sweep, and the classical shadow of this fact
is that hardware square root has never needed a tie path. The overflow band has a closed-form emptiness
criterion and is inhabited exactly on the identity-free numerals again (section 1.28's own finding, the
same absent element that breaks the multiplicative identity opens the root's overflow band, discovered
independently by two members in the same stretch). Domain: total over `Domain = Unsigned` by the existing
`SignDomain` axis (the divisor-domain-as-predicate clause reused, not a new niche); over signed domains,
the negative half is the solution-set derivation's own everything-or-nothing clause, inherited. In const
position the correctly-rounded sqrt folds to the guaranteed constant per the pricing pillar's own clause;
in value position, after the root extraction (the design's usual `arvo-always-optimal-internals.md` bin),
**the entire correct-rounding decision is three instructions, branchless**.

**The radix-power exponential is decidable and exponentially wide, the elementary functions' own instance
of division's already-known accumulator refusal.** `2^(k/2^F)` is algebraic; exact hits and ties on a
dyadic grid are exact integer comparisons (exact hits only at integer exponents, ties never), but the
comparison object doubles in width per fractional bit (13, 25, 57, 113 bits at `F = 1` through `4`),
decidable in principle and refused as a practical carrier, exactly the shape of division's own `lcm`
accumulator (`43:145-155`). Even the decidable half of this family reaches the operational posture of the
transcendentals below: bounded working precision, correctly rounded where the bound is known sufficient.

**The transcendentals have no exact carrier at all, and the moved count becomes a theorem instead of a
per-value fact.** For nonzero rational `x`, `exp(x)` is transcendental (Lindemann-Weierstrass, external,
cited per the review's standing practice for standard mathematics no toolchain here could check), and so
are `ln(x)`, `sin(x)`, `cos(x)`, `atan(x)` at nonzero rationals. Every grid point and every midpoint of an
arvo numeral is rational (the membership theorem), so a transcendental result never lands on either, off a
finite enumerable list of removable special points per function (`exp(0) = 1`, `ln(1) = 0`, `sin(0) = 0`).
Ties are therefore impossible for a deeper reason than the roots' parity argument, and the domain events
(`ln` at zero, `ln` of a negative) are solution-set-clause theorems that recover IEEE's own log-of-zero
table entry as a derivation rather than a citation, the same way the far-point argument recovered clause 7
for `x/0`.

**The one genuinely new thing this design needed, and it is small: a provenance class for one constant,
not a mechanism.** Correct rounding of a transcendental terminates per value (Ziv's staged refinement,
external), but the working precision sufficient for **every** operand of a numeral is not given by any
formula; it is a well-defined constant of the type (the value set is finite, so the worst-case boundary
distance exists), passing the pricing pillar's own test to the letter while resisting the pillar's usual
derivation economics: the constant is computable only by exhaustion over the value set, or by citation of
a published worst-case search. Measured at three model numerals: 11, 9, and 10 extra bits, zero ties
anywhere, no visible formula connecting the three. **This constant does not transfer across widths**,
unlike the laws whose model-width checks transfer by uniformity: hardness at width `W` is established at
width `W` by exhaustion or citation, never carried across, because it is a maximum over a different set at
every width. **Adopted: the provenance class `exhaustively-computed-or-cited`, entering the grounding
registry beside compiled and measured; where cited rather than exhausted, the constant is a trusted-base
entry with the citation as its named artifact, the same accounting as every hand-laid `Crosses` entry.**

**What the family may promise, derived from the ratified naming principle rather than invented.** The
roots may promise correct rounding, verified by the residue characterisation itself. The radix-power
exponential may promise correct rounding per width, verified by the width's own bounded sufficiency check.
A transcendental may promise correct rounding exactly at the widths where its hardness constant is
exhausted or cited, and elsewhere ships as a licensed approximation: a type-level error bound as the
contract, an internal mechanism (degree, table size, argument reduction) per
`arvo-always-optimal-internals.md`, and the differential parity suite op's own `79b` mandate designates as
the verifier. IEEE's own posture corroborates the split (clause 5 requires correctly-rounded sqrt, clause
9.2 only recommends correctly-rounded transcendentals, secondary, primary read owed).

**The closure sentence, adopted in the admission-test form rather than a list, because a list reopens with
every member and a test does not.** *The operation surface is the following table, and an operation joins
it by stating five things: its solution-set characterisation, its exact-carrier kind and width class, its
grade sites at the site count, its result-numeral rule, and, where its name promises behaviour, its
designated verifier. An operation absent from the table and not admitted through this test is not in the
design.* Every operation ratified as of this document satisfies all five statements.

*Grounded on: ratified (`91:284-294`, `91:420-429`, `95b` section 2 the solution-set derivation, `91:846-
851` the naming principle, `79b`), settled shapes (`93` section 5, `43` sections 2 and 5, `78:723` the
missing identity, `29:111-116`), compiled (`99_probes/probe_1`, `probe_2`, exhaustive const-position),
measured (`99_probes/probe_3`, `probe_4`, exact-integer and 60-digit-decimal Python), external
(Lindemann-Weierstrass, Ziv's staged refinement, IEEE 754-2019 clauses 5, 7.3, 9.2, all secondary, primary
reads owed), reasoned (the unprobed class assignments of `ln`, `sin`, `cos`, `atan`, and the moved-count
theorem, both from the no-boundary-hit fact, file 99 alone, two-read status: one pass, adopted at `101b`).*

### 1.17 Radix ten

Unchanged this stretch.

### 1.18 The numeral notation

Unchanged this stretch.

### 1.19 Claim provenance: the grounding registry, one new discipline item and one new class

The five-row grounding table and the four-member transfer-ground vocabulary stand unchanged. Two
additions this stretch. **A new provenance class, `exhaustively-computed-or-cited`** (section 1.16 above),
for a constant that passes the pricing pillar's test while resisting its usual derivation. **A new
discipline item, the freshly-performed-search requirement** (this document's own third requirement,
above), which governs the review's own audit deliverables the way the definitional-completeness line and
the separation requirement govern its design claims.

### 1.20 The algorithm crates

Unchanged in content from the sixth consolidation.

### 1.21 The strategy door

Both ratified preset tables stand exactly as the ninth consolidation carries them, unchanged this stretch.

### 1.22 External images: three width levels, the digest contract, the mutation perimeter, and the first real footprint signal

**The three width levels, the three maps, the level ordering, the niche collision's resolution, and the
digest contract at two stopping points all stand exactly as the ninth consolidation states them.** The
mutation gap's own repaired statement and its terminating clause are carried forward in full at section
1.12 above rather than restated here, since the ninth consolidation's own placement of the perimeter
material split across two sections (1.12 and 1.22) and this document keeps the seal-and-mutation material
together at 1.12 where the whole perimeter argument now reads as one piece.

**Bitpacking's cost against `Layout::Dense` is measured for the first time at a footprint that actually
exceeds this host's cache, and the review's own designated verifier for `Cold`'s intent produced its first
real signal.** The bandwidth-contention bench the review had ranked top of its open list twice, believing
it blocked on an upstream mockspace fix, was never blocked on what the review believed. File 96 checked
the belief before building around it and found `mockspace_bench_core::Routine::build_input_bytes`'s own
override contract, `ByteRoutine`, already shipped the by-reference input path in the same crate every
bitpack bench in this repository already depends on; three files and two checkpoints had carried the
"needs building upstream" belief forward without the one grep that would have dissolved it. Built on the
mechanism that already existed, cross-validated against an independently-written second decoder per
layout (a gap the first sweep itself found and closed, since the harness does not validate a
single-variant section at all), the sweep separates at the instantiation the separation requirement's own
test demands: a size where the packed region fits this host's L2 and the dense region does not. **At
7,000,000 elements (packed 10.85 MiB, fits; dense 13.35 MiB, does not) the packed/dense multiple for a
plain column sum drops to 1.43x, from a peak of 1.66x at L1-resident sizes, driven by dense's own
per-element cost rising monotonically by roughly sixteen percent across the sweep while packed's stays
flat**, the correct signature for a column that has started leaving cache rather than a decoder that has
improved. **This is a real, cache-residency-driven footprint effect, the first this review's own bitpack
benches have produced.** It is modest, and the file that found it says so rather than dressing it up:
whether the effect grows once a column sits many multiples of L2 past the boundary, and whether it needs
concurrent bandwidth contention across cores to become the dramatic story `Cold`'s own founding intent
gestures at, are both open, named, and not this document's to resolve. `Cold`'s standing does not move
either way; the workspace toolbox rule's own authority for the marker plus this one honest signal is the
current state, stated as such rather than inflated.

*Grounded on: ratified (`77b` via `78:552-556`, `78:142-144`, `78:643-648`, `68:138-139`, `90b`, `101b`
the footprint ratification), settled shapes (`72`, `73` in full, `80` sections 1, 6, `81` in full, `83` in
full, `84:354-411`, `92` through `97` as cited at section 1.12), compiled (`80_probes/`, `81_probes/`,
`83_probes/`, `86_probes/`, `87_probes/`, `88_probes/`), measured (`mock/benches/bitpack-*`,
`mock/benches/bitpack-footprint-{dense,packed}_n*.csv`, forty warm samples per variant per size,
cross-validated at every reported size), reasoned (the crossover byte-count arithmetic and the
dense-cost-rise attribution, file 96 alone, two-read status: one pass, ratified at `101b` as a measured
finding rather than a design claim requiring a second read).*

### 1.23 The assembled trait table

Unchanged this stretch from the ninth consolidation's own text, section 1.23. `Int` stays dropped.
Rewrite cost against the shipped tree remains near zero, verified fresh: no shipped source names
`Adjustment`, `Numeral`, `Bias`, `FullRange`, `UTerm`, or `AddWidth`.

### 1.24 The cost model

Unchanged this stretch.

### 1.25 The downstream contract, and the crate table: the periphery corrected from unbounded ignorance to two bounded items

**This section replaces the ninth consolidation's own section 1.25 in full, because the ground it
describes was misdescribed there and in every consolidation before it.**

File 98 answered op's own waking question, how complete is the canon, against the criterion op himself
would apply: a canon is complete when a competent implementer can build from it without hitting a
question it does not answer. Three of its four conclusions stand unchanged and are carried forward: the
core is covered and mostly attacked rather than merely written down; the open list is small and sorts
cleanly into mechanical artifacts, design-shaped items with named closing artifacts, and decisions
genuinely awaiting op's own word; and everything remains conditional on op confirming or striking the two
persona checkpoints this stretch and the last one produced.

**The fourth conclusion was wrong, and it was the loudest one.** File 98 reported five or six crate-level
subjects (`arvo-shape`, `arvo-geom`, `arvo-platform`, `arvo-float`'s packaging boundary, the unified
predicate concept, `arvo-pseudorand`'s PRNG half) as the review's own unexamined ground, carrying an
unbounded error bar drawn from the historical range of what an unexamined-ground sweep has previously
found. **That periphery is not unexamined ground. It is op-ratified ground**, decided in a design round
(`mock/design_rounds/202607300800/`, thirty-two topic files, fifty-two decisions, each marked "Decision
(op)" inline with rejected alternatives recorded) that locked one to two days before this panel opened,
distilled into a file the panel's own founding context list named as required reading from its first hour
(`00_context.md:93-96`), and row-rechecked at decision granularity, by crate, sixty-three files into the
review, at file 74. File 98's own citation for "nothing has reviewed these rows" (`91:769`, "every other
row survives exactly as the eighth consolidation states it") is itself downstream of that recheck: the
eighth consolidation's row table is file 74's verdict table, carried forward and compressed until its own
provenance was no longer visible in the sentence.

**Genuine ignorance is two bounded items, not five or six with an unbounded error bar: the uniform-
sampling spec question in `arvo-pseudorand` (uniform over values or uniform over data, a real
divergence the moment the grid is non-uniform, undecided by any ratified decision and imposed only as an
obligation to say), and the integer-saturating SIMD lane residue, known-open since file 20 and unmoved.**
Everything else on file 98's periphery list is ratified material that either has since been
content-reviewed (two subjects, section 1.28 and the elementary functions above closed a third) or still
owes a content review that the two reviews run so far show costs about one dispatch each and yields
compiled corrections to stated grounds rather than reopened decisions.

| Subject | Round decisions | Verdict this stretch |
|---|---|---|
| `arvo-shape` | D1-D4, D7, D8, D43, D44 (op) | Ratified, content-reviewed, two details overtaken (section 1.28) |
| `arvo-geom` | D2 contents, D10, D11, D40, D41 (op) | Ratified, content-reviewed, one ground overtaken (section 1.28) |
| `arvo-platform` | D27 (op) | Ratified-unread; one fork already op-queued (Bool below the numeral contracts, or the contracts generic over notko's truth contract) |
| `arvo-float` packaging | D29, D30, D50 (op) | Ratified, overtaken in the strongest sense: the tower absorbed the packaging's contents and forced the boundary rather than the boundary choosing it |
| Predicate concept | D15, D16, D17 (op) | Split: D16 overtaken by op-checkpointed panel work (files 03, 07, generalised into the crossing discipline); D15 and D17 content-unread, sharing the platform fork's one open question |
| `arvo-pseudorand`, PRNG half | D42 (op) | Ratified locus; the sampling spec genuinely open, the one row where file 98's description was right |
| `arvo-container` | D27, D28, D45 (op) | Ratified; D45's placement overtaken twice, its distinction preserved both times, disposition inside the per-row confirmations |
| `arvo-bitfield` | D25 (op) | Ratified-unread; cheapest remaining review |
| `arvo-num-systems` | D38, D39 (op) | Ratified and panel-worked; not periphery at all |
| `notko-hlist` + `Cardinal` | D5, D6, D7, D9, D18b (op) | Ratified; one binding-time sentence still owed |

**The anatomy of the error, recorded because the review's own two standing requirements, both performed
genuinely on file 98's own text, could not have caught it.** The claim rested on two sources whose own
searches had gone stale (a table written before file 74's recheck, a sweep true when written and
falsified two files later) and, most tellingly, on an `ls` of the design-round directory whose return
value named the exact file needed and was never opened during a completeness audit whose central claim
was about exactly that ground. Both requirements audit a file's own terms and models; neither audits the
sources a file failed to read. That gap is what the freshly-performed-search requirement, this document's
own third requirement above, now closes.

Two compiled errors surfaced during the first content review of this ground (file 100), inside material
op's own round and this review's own prior consolidation had each carried: D10's rotor storage count
(section 1.28), and the capacity pairing's own "one construction door" claim (`91:796-802`), false above
rank 0 (section 1.28). Neither overturns the decision it sits beside; both change what gets built.

**Adopted, `101b`: the periphery table above replaces the ninth consolidation's blanket "every other row
survives" sentence, and the two-item ignorance count replaces the five-or-six-item unbounded one.**

*Grounded on: ratified (the design round `202607300800` at the cited decisions, `39b`, `70b`, `74b`,
`77b`, `86b`, `79b`, `101b`), settled shapes (`74:16-19,40-46,61-73,185-204,247-253`, `78:679,870`,
`50:476-490`, `03:56-94`, `07:324,417`), measured (the corpus greps for `Unbounded`/`FLX`, `arvo-
platform`, `D15|D16|D17`, `truth contract`, all re-run fresh for this document per the freshly-performed-
search requirement, matching file 101's own results), reasoned (file 98's assessment and file 101's
correction, two-read status: file 98's periphery claim was one pass and wrong; file 101's correction is
itself one pass, offered as evidence and suggestion, adopted at `101b`).*

### 1.26 Naming: an environment parameter is an assumption, never a witness, and the class test moves from the compiler to the reader

**This section replaces the ninth consolidation's own section 1.27 in full.**

**The refined principle's second clause, the trusted-base entry with a closing artifact, stands.**
Adopted at `90b`, one word repaired this stretch: "designated" becomes **"named in the record"**, per file
94's finding that the stricter reading would eat the design's most-written names (`Cold`, `Hot`) whose
verifier is a bench the review has not yet been willing to run. A named-but-unbuilt verifier makes a
promise an honest trusted-base entry with a closing artifact, the same accounting every hand-laid
`Crosses` entry already gets.

**The first clause, the class test, is replaced rather than repaired, because its own second read found it
keyed on the wrong observer and its replacement in turn could not survive a further second read.** File
90's sweep asked, of each name, whether the compiler checks its claim. File 94 ran the same names against
the question that actually matters to a consumer: what does a reader at a call site, holding one token out
of a declaration's four or five, conclude. The two questions diverge exactly where this design's own good
factoring (identity axes separate from lowering separate from policy) makes a name's true content a
function of its siblings, which a reader cannot see from one token. `91:850-851`'s "every existing name in
the design passes" is false at three found names: `Hot`/`Warm`/`Cold`/`Precise` denote two different rows
each, one per number kind, and `Warm`'s float row diverges from its fixed-point row on the single sharpest
cell the presets have (`94_probes/probe_3`, compiled); `StoredWidth::Minimum` misled the review itself for
two files before its reading was forced by a three-way argument against a different axis's ratified
meaning; `IeeeSpecials` and `E4M3` name inhabitants, not semantics, and a reader who assumes semantics
concludes something the design has explicitly not committed to (E4M3's deployed overflow mode being the
live instance).

File 94's own replacement rule, an instantiation where denotation and "what a competent reader concludes"
diverge, did not survive its own second read. File 95 found it shares the exact defect file 94 correctly
struck the parity suite for: it cannot fail in the hands of the person running it, since the only reader
available to the author of a ratifying text is the author, who has read the panel. **Adopted, file 95's
repair: the class-one clause is replaced by nothing. The widened definitional-completeness line does the
work**, at three mechanical addresses that need no house style about a hypothetical stranger: a name
appearing in more than one ratified table or definition with different content; a name whose correct
reading requires a fact stated on a different axis or in a different section; a name sharing a token with
an external standard whose scope exceeds the design's own claim. All three are the completeness line
reaching the name being defined rather than only the terms inside a definition, which is exactly the
widening this stretch adopted at `95b` for independent reasons (section 0 above). Against file 94's own
divergence list, all six fail at least one address; the four clean names (`Fnv1a`, `quantise`, `TotalOrd`,
the namesake aliases) pass all three, matching file 94's own finding by a different route. **No new naming
rule. The class-two clause stands as adopted, "named in the record" swapped in.**

**Six boundary sentences relocate to the definitions the reader reaches, four of which already exist
elsewhere in the corpus and only move, per file 97's confirmed condition that the move is within the
ratifying text, not a copy: the section that resolved a fork cites, and does not restate.** `Specials`
members name inhabitants, not semantics. `E4M3` names identity axes; the deployed overflow mode is a
separate axis on the lowering. A preset name is a pair of rows keyed by number kind, stated above both
tables. `StoredWidth::Minimum` is the carrier, not the container. `Folded<N>`'s `N` is the site count, an
upper bound on the moved count. `IeeeDefault` denotes the assumed ambient control state.

**`IeeeDefault`'s artifact list loses one artifact incapable of failing and gains one that demonstrates the
others do.** File 94 found the parity suite listed against the deployment residual is structurally
incapable of failing on that claim (a parity suite run in a process either has the assumed control state or
does not, and either way it reports nothing about a different deployment); struck, and kept against the
arithmetic claim, where it can fail. File 95's own probe found the receipt's mask itself missing a field
this host latches (FZ16, half-precision flush-to-zero), which means the remaining list had **no negative
control**: nothing demonstrated the receipt fails when the environment is wrong. **Adopted: a perturbation
arm joins the list**, the artifact probe 2's finding calls for and the honest thing for the receipt to
verify.

**The assumption sentence itself needed one word and one clause, to keep a compile-time-settled decision
from being modelled as a runtime assumption.** File 71 pre-authorised the environment type to carry a
NaN-on-overflow mode, which no control register holds; it is a choice between two instruction forms,
settled where the code is emitted, unperturbable by any linked library. Modelling it as an environment
assumption would defer to a runtime trusted-base entry a fact compile time already settles, the fourth
design rule's own prohibition. **Adopted: an environment parameter denotes the *ambient* control state the
lowering's correctness is conditional on, an assumption, never a witness; a fact the deployment cannot
perturb is not environment, it is a lowering decision, settled where the code is emitted, and belongs on
the lowering.** File 97's second read sharpened the operative test: can a linked library change the fact
at runtime, applied per-fact-as-lowered rather than per-fact, since the identical abstract fact (rounding
mode) is ambient under one lowering (addition) and instruction-encoded under another (a rounding cast) on
the same target, compiled and executed (`97_probes/probe_4`). The environment type's own field set is
therefore keyed on the lowering, not on the target alone, and a compile-time-settled fact adds no **new**
trusted-base entry, riding on the standing toolchain trust every compiled claim in this review already
carries, rather than adding nothing to trust at all.

**The receipt, adopted as a per-target-per-lowering field set folded in const position, with its verdict
split rather than its declaration refused.** File 94 found the hand-written receipt does not portably
check what the name denotes (the x86 form is a different register with a different field set, not three
instructions; a naive transliteration compiles and passes with a denormal-flushing deployment) and
proposed the fix the pricing pillar's own clause already implies: a per-target field set on the type, the
receipt a fold over it in const position, a zero mask meaning the target cannot check the field, never
that the field is satisfied. File 97's second read attacked the finisher this drew ("a numeral whose
correctness depends on an uncheckable field refuses at declaration") and reversed it: the environment
parameter is ratified as an assumption, never a witness; the receipt is a debug-tier spot check against
that assumption, one artifact on its list, and in a release build no field is checked on any target
without the design refusing those declarations. Refusing specifically the uncheckable case converts an
honest trusted-base assumption into refusal on ignorance, the policer posture by the design's own boundary
sentence, and blurs the provable-versus-trusted line from the direction nobody had yet blurred it, treating
unverifiable and false as the same thing. **Adopted, the corrected verdict: the receipt's verdict splits
rather than the declaration refusing.** A target that cannot **check** a field yields a trusted-base entry
with its artifact gap declared, through a split verdict (a checked mask, an expected word, and an
unchecked field set, all const-derived, the consumer's acknowledgment a const assertion that refuses if the
bundle later grows a new unchecked field, emitted body unchanged at four instructions). A target that
cannot **provide** the field's semantics at all (no gradual-underflow path at any cost) is a statically
known falsehood and refuses at declaration, correctly. The required-field relation (which fields a
numeral's correctness actually depends on) is named open, a genuinely new item, plausibly derivable from
the numeral's own axes and unwritten.

*Grounded on: ratified (`79b:20-27`, `90b`, `95b`, `97` the verdict-split reversal, both two-read),
settled shapes (`71:288-310`, `78:325-326`, `78:409-441`), compiled (`94_probes/probe_1` through
`probe_5`, `95_probes/`, `97_probes/probe_2`, `probe_4`), reasoned (the reader-side classification and its
own retirement, the ambient scoping, the verdict split, two-read status: the class test and the receipt
design are two-read complete; the required-field relation is one pass, named open).*

### 1.27 Capacity

Unchanged this stretch from the ninth consolidation's own section 1.26, with one repair. **The "one
construction door" claim needed a rank added to it, and above rank 0 it was false.** Section 1.28 below
carries the finding and the fix in full, since it surfaced during the shape content review rather than as
an independent capacity finding.

### 1.28 Shape and geometry: the index-domain layer settles, the multilinear half composes at no new growth class, and two counts in the round's own reasoning are corrected

**New this stretch, closing two of the periphery's content-review rows (section 1.25) with a yield of
compiled corrections to stated grounds and one repair, no decision overturned.**

**Shape is not a geometry subject. It is the index-domain layer, the same kind of thing as `Precision`,
`Exponent`, and `StoredWidth`, and it already has its rank-1 case: `Capacity`.** A shape is a
multi-dimensional index domain; everything past that (rank, per-axis extents, element count, index and
stride arithmetic, the projection from a logical index domain to a physical container, rank-generic
folds) is numerics, not space. **`arvo-shape` is downstream of the capacity resolution and upstream of
everything with a container, the third instance of a pattern this design has already worked out twice**:
a logical extent, a declared carrier, and an allocated container, one dimension up from the width levels.

D4 (op) declared rank and per-axis extents as a type-level list of capacities with the array grammar
recursively composed from them, written when a capacity was a single const generic doing two jobs at once.
**It survives the capacity resolution verbatim**, compiled: a rank-3 shape's nested-array storage and its
independently-computed element count agree by two unrelated routes, not by restatement. But D4's own
sentence makes the array composition constitutive of the shape, while D43 (op, the same round, one day
later) states the opposite: "the shape abstraction supplies rank and extent and nothing else, so a bit
container implements both and neither one grows the other's surface." **Resolved by writing D43's sentence
literally**: `Shape` carries rank and extents; a separate trait projects a shape to storage, once per
element domain. Not a new mechanism, but the `Lowering` charter one dimension up, exactly the layer-keying
rule's own instance named in this document's opening section. File 74 had already seen this coming from
the other side, sixty-odd files earlier, and file 100's compile confirmed it. **Adopted as a working shape,
second read owed.**

**A real gap surfaced: section 1.27's "one construction door" claim is false above rank 0.** The door the
capacity resolution meant is the inherent method, where the pairing's agreement assertion lives; D4's
recursion is written against the **trait** method instead, which had no check at all, because at rank 1
the inherent door was the only door. Compiled: a rank-3 shape whose middle axis declares a `Nat` of 4
against a literal of 7 has two disagreeing const-evaluable counts and nothing raises, through the trait
route (`E0080` fires through the inherent door; the trait route silently returns the wrong array). **The
repair lifts the agreement to `const AGREES: bool` on `Capacity` itself**, a fact about the type rather
than about one call site, exactly the shape the level-ordering refusal already uses. Both the reachable-
assertion half and a read-only-bypass half need closing (adding the assertion to the count's own
computation as well as to the door), and both are needed, either alone leaving a door. **Adopted as a
working shape, second read owed.**

**`Layout::Bitpacked` at rank N needs no new mechanism, and the exact coincidence condition with the
per-axis reading is derived rather than sampled.** Two readings exist at rank 2 and above (pack each
innermost row independently, or pack the whole shape as one run of `COUNT` elements); the flattened
reading is never worse in footprint over an exhaustive 131,072-case sweep, and under it the group
arithmetic already ratified for one dimension applies unchanged, since it is a function of the stored
width alone. The write granule becomes a shape fact: an outer-axis partition is legal only when
`inner mod P == 0`. The two readings coincide exactly when the whole per-axis padding fits inside one
byte (`outer * rowpad < 8`), a one-line proof once stated, and the sampled-first guess (byte alignment
alone) mismatched over four percent of the swept grid, which the exhaustive sweep caught and a
spot-check would not have. **Adopted, the flattened reading, no new mechanism.**

**A live asymmetry surfaced in a shipped bitmask type, offered as why-evidence for the shape chapter's
own need for a per-axis extent with a refusal behind it, not as a fix to propose.** `BitMatrix`'s two axes
of one rank-2 shape are declared in two vocabularies: the row extent is a `Capacity`, checked and refused
out of range; the column extent is only a `Bits<N>` width, with no shape behind it and no refusal. A safe
write past the declared column extent lands, and two values equal at every index the shape declares end
up with different byte images, reaching the mutation gap's own signature (section 1.12) through an
ordinary safe call, at rank 2, with no unsafe and no niche. Compiled and executed
(`100_probes/probe_7`); no source fix proposed, `mock/crates` being outside the panel's scope.

**Compile time is flat in rank, and the rank-generic fold folds away at small element counts, both
measured rather than assumed.** Metadata compile time sits below rustc's own process-start noise from
rank 1 through rank 16; the rank-generic fold compiles to five instructions at rank 3, count 8, and
vectorises to NEON by rank 8; a dense shape's stack allocation is a function of element count alone
(262,144 bytes at count 65,536, both rank 2 and rank 4), the design's no-heap rule's own consequence
rather than anything rank-specific, worth one sentence in the chapter.

**Geometry's multilinear half (points, orthotopes, affine maps, their predicates, their measure) composes
over the settled operation surface at no new growth class, no new failure kind, and no new vocabulary.**
Every operation is a sum of products; exact accumulator widths for dot product, affine apply, rotor
compose, and rotor sandwich are all derived from the already-settled multiplicative and fold classes.
Renormalisation is the one operation touching the design's exponential division class, and it touches it
only in its statement (a quotient never materialised as a numeral, since normalisation is
`quantize(exact quotient)` through the Euclidean pair), never in a byte. This discharges an open item file
74 had recorded: `arvo-geom`'s motors need normalisation, which needs division, which needed the hold
lifted; the hold lifted at `90b`, the surface adopted at section 1.13, and the discharge is this
paragraph. **Adopted, no growth-class change.**

**D10's rotor component count is wrong from rank 4, and the correction reverses D10's own storage
comparison at rank 7.** D10 (op) grounds rotation on rotors and gives its storage argument in terms of
degrees of freedom, `1 + n(n-1)/2`; the actual coordinate storage a rotor needs, as an element of the even
subalgebra `Spin(n)`, is `2^(n-1)`, since `Spin(n)` is a curved manifold inside that space rather than a
linear subspace of it. The two counts agree at rank 2 and 3 (where the even subalgebra of `Cl(3)` **is**
the quaternions, D10's own comparison case) and diverge from rank 4: 7 against 8, 11 against 16, 29
against 128 at rank 8, exhibited with an exact-rational unit rotor whose grade-4 component is nonzero and
whose degrees-of-freedom slots cannot hold it. **The consequence runs the opposite way from D10's own
argument**: rotor storage first exceeds matrix storage (`n^2`) at rank 7, which the degrees-of-freedom
count would never show, since `1 + n(n-1)/2 < n^2` at every rank. **What this does not touch: D10's
decision stands on the grounds it also gives** (rotors generalise where quaternions do not, motors extend
to rigid motion in one composable object, `Affine` is the rigid-motion-plus-scale type geometry already
wants), and nothing above argues for a matrix. What changes is that the review's own bench matrix for
rotation representations, ordered by op (D41) to run at named requirements before any comparison, needs at
least one rank of 4 or above to measure the generality claim D10 actually rests on, since rank 3 compares
one representation against itself.

**The identity rotation is not representable in the numeral geometry reaches for first, the same absent
element file 99's sqrt overflow band found, independently, in the same stretch.** A rotor component, a
direction cosine, a normalised colour channel, and a barycentric weight all live in a closed interval, and
the obvious numeral is purely fractional, missing its representable one by exactly one quantum at every
width (`78:723`'s known defect, now shown load-bearing for the operation the rotor formulation exists to
make cheap: normalisation itself). **The fix is a value of a parameter the identity contract already
seals**, not a new mechanism: `Adjustment = 1/(r^F - 1)` gives both endpoints exact at the same container
width (Direct3D's UNORM rule, converged on independently by the design round's own sibling colour pass),
compiled at every `F` from 1 to 24. Two routes exist and the design should expose both rather than pick
silently, per the toolbox rule: one bit of integer headroom (keeps the dyadic quantum, spends storage,
crossing the same rank-7 boundary as the rotor-versus-matrix comparison), or the rational adjustment
(exact endpoints, a constant divide per multiply, already priced as the exact division subfamily). **What
the design should not ship is the numeral that carries neither endpoint.** This closes the round's own D36
(section 1.11 above) and answers, on the arvo side, a question the design round's addendum had flagged as
falling between two rounds: a UNORM-shaped type is not a type, it is a parameter value, and the sibling
crate `kirjo` owes no new type and is owed no request, only this sentence.

**D10's flagged const-expression hazard dissolves, and the fallback the round proposed compiles.**
D10 worried that a rotor's `1 + n(n-1)/2` extent as a computed const-position expression is exactly the
hazard `Capacity` exists to avoid, and named a fallback (deriving the basis count as a type-level fold
over the axis list) that the round's own prior-art pass found no library implementing. **Absence is not
impossibility, and the compile settles it**: the bivector-extent count is derivable through the identical
structural-recursion family (`Dec`/`PosPred`, `VAL`, `Cmp`, `Gcd`) the tower already uses elsewhere, one
impl-selection step per axis, zero arithmetic expressions in type position, zero feature gates, checked at
ranks 2 through 8. The derived extent then pairs with its literal exactly as the capacity resolution
requires, and a wrong literal, including D10's own original miscount, is refused with the same `E0080` the
section-1.27 repair above produces.

**The bench requirements D41 named first and nobody had produced, supplied, per row, from what the design
has settled since D41 was made.** Fixed-point representability resolves to the closed-interval question
above, with its two priced routes; which operations must be cheap now names each operation's growth class
and exact accumulator width, with two operations (matrix inversion by transposition, rotor inversion by a
sign flip) free rather than merely cheap, invisible to a naive bench; degradation under repeated
composition restates as the site count, a compile-time number derivable before the bench runs rather than
discovered by it; storage width is the corrected counts above, in bits after the numeral choice, not in
components. Three rows the design has acquired since D41 and could not have named then: whether a form's
identity is representable at all (disqualifying, not merely costly); whether the form's exact carrier
exists (the transcendental class's licence structure governs the Lie-algebra exponential map on the
rotation groups, the one row on D41's own variant list that is not multilinear); and the bench's own
measure obeying the pricing pillar (every per-element quantity that is a function of the type alone
belongs on the type, the same lesson a 4.6x bitpack figure once cost the review for missing it).

*Grounded on: ratified (D1-D4, D7, D8, D10, D11, D40, D41, D43, D44 all op, `91:113-126` the pricing
pillar, `91:157-159` the layer-keying rule's Lowering charter, `91:811-814` the structural-recursion
family, `91:288-291` the exact division subfamily), settled shapes (`74:64-65`, `78:723`, `99` section 2
the root overflow band, `40:493` the Adjustment grammar), compiled (`100_probes/probe_1` through
`probe_8`, all at the pin, zero feature gates except where marked exhaustive Python), external
(Abel-Ruffini, cited not compiled), reasoned (the trait split, the requirements table, the locus
suggestion for `arvo-geom`'s own two halves, file 100 alone, two-read status: one pass throughout, adopted
as working shapes at `101b` with second reads owed on the trait split and the AGREES repair).*

## 2. The lead designer's calls

**Op's earlier checkpoints, restated, unchanged.** D69, D39, the novelty posture, the value-unique
encoding, the convergence directive: unchanged through every checkpoint since, and the intent still
outranks every instruction, is vague on purpose, and only op's calls are final, and even those go stale.

**`79b`, recorded rather than dispatched, binding the implementation phase.** Unchanged, no member is
briefed to design against it this stretch either.

**`90b`, persona-decided, walked individually this stretch, one call dies and the rest stand.** The
counting split, the definitional-completeness line's original form, the naming trail's first two moves
(the `67b` principle dying, the refined principle adopted), statement 0's unamended hardening,
`NicheCarrier` and the mutation repair adopted as working shapes: all stand, most now two-read confirmed
at higher precision this stretch (section 1.12, 1.26). **The division instinct (alternative 1, the `Door`
placement) dies this stretch**, stress-tested exactly as `90b` itself demanded, killed on four compiled or
silicon-read facts, replaced by the solution-set dissolution (section 1.13). `Hot`'s default environment
stays held, untouched.

**`95b`, persona-decided, this stretch's own first checkpoint.** Adopted: the requirement-performance
clause (this document's own opening section), the division dissolution's first pass (later corrected in
two details by file 97, section 1.13), the naming class-test replacement (later corrected by file 97's
"named in the record" swap and confirmed as replaced-by-nothing rather than replaced-by-a-new-rule), the
perimeter block at its two-read tier (section 1.12), the domain-preservation equation and terminating
clause as working shapes (the former later retired, the latter later repaired, both by file 97).

**`101b`, persona-decided, this stretch's own second checkpoint.** Adopted in full: the elementary
functions chapter (section 1.16), the periphery correction and the freshly-performed-search requirement
(section 1.25, this document's opening section), the three overtakes (Growth, D36, FLX-drop-recorded),
the shape and geometry findings as working shapes (section 1.28), the footprint bench ratification
(section 1.22), file 97's two second-read reversals (section 1.12, 1.26), and the three-tautology
disposition (section 3 below).

**Loudest for op's morning read, ordered by op's own stated priority (`86b`: "the open list, not the
interesting list, is the queue").**

1. **Confirming or striking `90b` and `95b` and `101b`.** This single act moves more material between
   tiers than any other available. `90b`'s division instinct is already dead on its own stress test and
   does not need striking to be gone; everything else across all three checkpoints stands only until op
   says otherwise.
2. **The FLX-family reinstatement question.** The determinism argument that made an unbounded-exponent
   format family worth having was op's own D49; a stand-in does not decide whether a format axis grows a
   member against a ratified two-instance `Underflow` (section 1.10). One line either way.
3. **The two content reviews still owed at the periphery**: `arvo-platform`'s Bool-placement fork (shared
   with the predicate concept's D17), and `arvo-bitfield`, the cheapest remaining row (section 1.25).
4. **The rotation and curve benches, D41 and D11's own**, now with named requirements (section 1.28) and a
   named floor on the bench matrix's own rank.
5. **Division's grading axis**, held since checkpoint ten, untouched by this stretch's dissolution of the
   fork it sat beside.
6. **`Hot`'s default float environment**, held three checkpoints running.
7. **The uniform-sampling spec question in `arvo-pseudorand`**, the one item this stretch's periphery
   correction confirms is genuinely open rather than merely unread.
8. **The workspace perimeter rule's own provable-versus-trusted clause**, op's hand, lean toward yes,
   unchanged.
9. **The eleven-crate taxonomy's remaining per-row confirmations**, carried forward, five rows enriched
   further this stretch (`arvo-shape`, `arvo-geom`, `arvo-container` via `arvo-platform`'s shared fork).
10. **`FromConstant`'s breaking-change fix**, untouched this stretch.
11. **The three `unstable-features.md` rule-wording edits**, awaiting op's own wording.
12. **Whether any transcendental ships in the first contracts crate**, a packaging call, genuinely op's,
    named and priced by section 1.16 rather than decided by it.
13. **The digest chapter's type-level history split**, still flagged as a lead deserving its own dispatch,
    still not built.
14. **The three tautological tests**, now known rather than one, all disposed by op's own trivial
    boundary: one commit outside the panel, `mock/crates` being op's own line the panel does not cross
    (section 3).
15. **The membership uniqueness scoping (D39) and the seven upper vocabulary members' reading**, both
    genuinely op's, unchanged.

## 3. The live-defect registry

**Three tautological tests are now known, not one, all inside `mock/crates`, all disposed by the same
ruling.** `arvo-tensor/tests/capacity.rs:14-18` (flagged since file 76, `assert_eq!(cap(3), cap(3))` by
substitution against `src/capacity.rs:48`), its sibling at the second impl `arvo-tensor/tests/
const_capacity.rs:49-53` (identical shape against `src/capacity.rs:117`, found unflagged this stretch by
file 100), and `arvo-hash/tests/aliases.rs:16-23`'s `content_hash_roundtrip` (the same computation on both
sides of one `assert_eq!`, found unflagged this stretch by file 101). All three re-verified at source this
session, per the freshly-performed-search requirement: present, unchanged, still counted in the green
total. **Ruled at `95b`, unchanged: op's own trivial commit outside the panel, `mock/crates` being op's
own boundary.** Not improved; deleted, since a tautology occupies the space where a missing test would be
noticed and a fabricated pass carried through a hundred files of writing about verification discipline is,
in file 95's own words, a standing embarrassment worth naming plainly rather than smoothing over.

Entries 1 through 7 from the ninth consolidation's own registry stand unchanged; none was touched this
stretch. See the ninth consolidation for their full text.

## 4. What is open

**Every item below names a closing artifact, per the discipline adopted at `82b`.** Items closed this
stretch are named once and not repeated; items open carry forward with their artifact.

**Closed this stretch.** The `NicheCarrier` seal's own second read, resolved to a narrower vocabulary and
a one-sentence trusted base (section 1.12). The mutation repair's second read and combined case, resolved
to a repaired theorem with a terminating clause and a forbidden door shape (section 1.12). Division's
`x/0` fork, dissolved entirely (section 1.13). The naming refinement's own second read, resolved to a
replaced class test and a corrected receipt design (section 1.26). `Hot`'s default environment's naming
condition, closed in shape (section 1.26); the concrete choice stays op's. The `Cold` footprint bench's
own by-reference-input-path blocker, retracted as never having existed (section 1.22). The definitional-
completeness line's and separation requirement's own owner and moment (this document's opening section).
The elementary functions chapter (section 1.16). The periphery's misdescription as unbounded ignorance
(section 1.25). Two of the round's own compiled count errors (section 1.28).

**Owed, each with its closing artifact.**

- The domain-preservation equation, retired to a compile-fail pin rather than kept as spec text.
  **Artifact:** the pin itself, when the real `NonZeroCarrier` vocabulary lands.
- The signed halves of file 43's division probes 2, 4, and 5. **Artifact:** a signed re-run of each.
- The float-division compile against a real `Specials`-bearing `Numeral` instance, now serving both
  division's own owed path and the elementary functions' `ln`. **Artifact:** the single compile.
- The IEEE clause 7 and 7.6 primary-source reads, now joined by the ISA bundle file 93 opened (ARM DDI
  0487, Intel SDM, RISC-V unprivileged ISA, LLVM LangRef) and IEEE clause 5, 7.3, and 9.2 for the
  elementary functions. **Artifact:** verbatim, position-cited quotations, in the bundle already carried
  forward.
- The required-field relation for the environment receipt (which fields a numeral's correctness actually
  depends on). **Artifact:** a derivation per numeral axis, compiled at a binary16 `Underflow::Gradual`
  witness.
- The x86 and RISC-V forms of the verdict-split receipt. **Artifact:** compiled probes parallel to the
  aarch64 form.
- The shape trait split (D4/D43) and the `AGREES` repair, both adopted as working shapes with one read.
  **Artifact:** a second independent read, per file 100's own named attack surface (whether widening
  `AGREES` onto `Capacity` itself over-obligates a capacity with no literal to pair with).
- The signed half of the closed-interval numeral, and the bit domain's own layout projection at rank N.
  **Artifact:** two compiles, one against the `NonZeroCarrier` vocabulary, one against a real
  `Layout`-bearing shape; the latter is the same artifact `foldnum`'s compile already owes in a different
  chapter.
- `foldnum` compiled against the real four-member `Numeral` contract with `Exponent` held fixed. **Artifact:**
  the compile, still unperformed since file 78 first named it.
- The non-default `Canonical` compile. **Artifact:** a declaration selecting the non-default cohort rule.
- The constructive-extensibility compile. **Artifact:** the compile, still unperformed, named owed by four
  files in succession now.
- The eleven-crate taxonomy's remaining per-row confirmations, five rows enriched this stretch. **Artifact:**
  op's own confirmation or correction per row.
- `IeeeDefault`'s concrete default-environment pick, and the FLX-family reinstatement question. **Artifact:**
  op's own choice, both.
- The workspace perimeter rule's own provable-versus-trusted clause. **Artifact:** op's own hand.
- The three `unstable-features.md` wording edits. **Artifact:** op's own wording.
- `FromConstant`'s breaking-change fix. **Artifact:** vehicle held for its own second reads, unchanged.
- The `arvo-platform` / predicate-concept D17 shared fork. **Artifact:** op's pick between two spellings,
  both on record.
- `arvo-bitfield`'s content review. **Artifact:** one dispatch, the cheapest remaining periphery row.
- The rotation and curve benches (D41, D11). **Artifact:** the bench matrix, at a rank of at least 4,
  against the requirements table this stretch supplied.
- The uniform-sampling spec question. **Artifact:** one dispatch on the named question, when its turn
  comes.
- The digest chapter's type-level history split. **Artifact:** its own dispatch.
- The `Cold` footprint bench's own two remaining regimes (past 33,554,432 elements; concurrent multi-column
  bandwidth contention). **Artifact:** two further bench runs, named in section 1.22.

**Unchanged from the ninth consolidation, untouched this stretch.** The reduction firing site and whether
`FullRange` survives as its own named constructor. The dither-versus-`Refuse` interaction.
`SC_WRAP<n>`/`SC_WRAP_SM<n>` with `n_bits > 0`. Richer canonicalisation's branchlessness and cross-word
bitpacked field extraction. `DatumDeterministic`. The `Gcd`-for-a-local-`Rhs`-on-a-sealed-`Self` coherence
question, confined to the proposed bottom carrier crate. `notko-hlist`'s binding-time sentence. The
membership uniqueness scoping (D39) and the seven upper vocabulary members' reading.

## 5. The droplist

Carried forward from files 26, 40, 49 through 91's own section 5; nothing this stretch resurrected any of
it. New entries follow, each stated as a correction to record it rather than as a silent revision.

`91:12-13`'s own claim, "the definitional-completeness line and the separation requirement applied to
everything this document absorbs": false, confirmed by grep at three of the ninth consolidation's own
sentences ("safe surface" and "raw accessor" undefined anywhere in the corpus; "designated" ambiguous on
the axis that decides whether the naming principle is useful or a rename machine), found by file 98 and
reconfirmed for this document. The register this belongs in is file 79's own search sentence: a fabricated
diligence claim, not a design error, and it is precisely why both requirements now carry an owner and a
moment (this document's opening section).

`90b`'s division instinct, alternative 1, `Hot`'s cell placed on the `Door`: dead. Killed on four compiled
or silicon-read facts by file 93; the fork's other alternative carried the identical smuggle at a
different address, so neither survives (section 1.13).

File 92's proposed door-side domain-preservation equation: superseded. Guards a region the entry-level
totality refusal, ratified at the same checkpoint that adopted the equation, already forecloses
unconditionally (section 1.12).

File 94's own reader-quantified replacement naming test: superseded. Cannot fail in the hands of the
person running it, the identical defect it correctly diagnosed in the parity suite it struck; the widened
definitional-completeness line does the work instead, at three mechanical addresses (section 1.26).

File 93's citation of section 1.3's second, weaker sentence ("no law may read `Lowering`") as the guard the
Door placement violates: corrected. The operative sentence is the first, unamended since file 40
("`Lowering` changes no value"); the deletion test is its decision procedure, not new canon (section
1.13).

File 98's own periphery assessment, "five or six crate-level subjects no panel file has ever examined,"
with an unbounded error bar: false. The ground is op-ratified, distilled into the panel's required
reading from its first hour, and row-rechecked at file 74 before file 98 wrote its own claim (section
1.25).

D10's own storage argument for rotors over matrices, `1 + n(n-1)/2` components: wrong from rank 4,
reversing its own comparison against matrix storage at rank 7. The decision D10 reaches survives on the
grounds it also gives; the count does not (section 1.28).

Section 1.27's own capacity claim, "checked to agree in an inline const block at the one construction
door": false above rank 0. The trait-method route bypasses the door entirely (section 1.28).

## 6. Verification

Every claim tagged compiled, measured, or reasoned in files 92 through 101 traces to a probe, a bench
artifact, or a verbatim source read, each carrying its own outcomes file or committed CSV, cross-checked
for this document. The canon gate reproduces fresh: `grep -rln "Adjustment\|Bias\|Numeral" mock/crates/
--include="*.rs"` and the same with `FullRange\|UTerm\|AddWidth` both exit 1, empty, at HEAD `53f1b3f`.
`cargo test --offline --workspace`, run fresh for this document from a clean committed tree with no
manifest drift, reports **672 passed, 0 failed, 9 ignored across 155 test binaries**, matching every count
files 97 through 101 reported while the tree still carried a concurrent dispatch's uncommitted work; that
work (file 96's footprint bench crates) is now committed and the count is the tree's own settled state.
No shipped crate under `mock/crates` is touched by any deliverable this stretch beyond the three
tautological tests named and left in place at op's own boundary (section 3); the panel's scope remains
design only. The toolchain is `rustc 1.98.0-nightly (57d06900f 2026-05-27)`, `aarch64-apple-darwin`,
resolved from `rust-toolchain.toml`, confirmed fresh inside the tree for this document.

**The bench harness ran this stretch, for the first time in six files, and it ran on a mechanism that had
shipped the whole time.** File 96 checked the belief that a by-reference input path needed building
upstream before building around it, and found `mockspace_bench_core::Routine::build_input_bytes`'s own
override contract already shipped it. Three files and two checkpoints had carried the false belief
forward without the one grep that would have dissolved it; this is the same failure shape the
freshly-performed-search requirement now names and governs, found independently before that requirement
existed and cited here as its own strongest supporting instance.

**The two requirements' own performances, on this document, before it stands.**

**The definitional-completeness line, performed.** Terms this document introduces or leans on, with
dispositions. *Overtake*, in the periphery table's own three-way sense (section 1.25): defined there,
refined by naming the provenance of the overtaking material (op-checkpoint versus panel suggestion)
because the value is not one thing. *Ratified-unread* and *genuinely open*: defined at section 1.25,
inherited from file 101's own three-way scale without modification. *Working shape*: used throughout
section 1 in the sense the ninth consolidation established (a mechanism with one read, owing a second),
not redefined here. *Solution-set derivation*: named at section 1.13, its three shapes (singleton,
empty-with-direction, everything-or-nothing) stated there in full rather than cited, since this document
is the first to state them as the design's general failure classifier rather than as one file's finding.
*Exhaustively-computed-or-cited*: defined at section 1.16, the new provenance class. *Byte owner*, *write
granule*, *residence*: used from the record (files 92, 97, 91) without redefinition. Named open rather
than defined: the required-field relation (section 1.26), the convention parameter for `arvo-geom`'s
model-choice half (section 1.28), which this document does not define because defining it would presume a
fork that is D41's own to resolve. No term in this document's own new prose is left undefined or
uncited.

**The separation requirement, performed.** Three models this document states as its own rather than as a
restatement of a source file's model, checked at their nonvacuous instantiation. First, the periphery
table's three-way scale (section 1.25) separates *ratified-unread* from *overtaken* within a single
source: the design round's D15, D16, and D17 sit in the same topic file and land in different verdicts
(D16 overtaken by later op-checkpointed panel work, D15 and D17 still content-unread), which an
"examined or not" binary could not state. Second, section 1.13's own solution-set derivation separates
*empty-with-direction* from *everything-or-nothing* exactly at the nonvacuous input, `x/0` for a NaN-less
numeral, where a single fallback would otherwise collapse the two; at any numeral carrying a NaN the
distinction is vacuous, since `invalid` resolves to NaN directly and no fallback question arises, and this
document's own division section says so rather than letting the general statement imply the distinction
matters everywhere. Third, section 1.28's rotor-versus-matrix storage separation is nonvacuous only from
rank 4, and this document's own table states that scope rather than letting the corrected count read as
universally decisive.

The honest limit of both performances, inherited from files 97, 98, and 101 in turn: they verify that this
document's own terms are placed and its models have content. They do not verify that every verdict above
is correct. This document is a compression of ten deliverables and two checkpoints, not a fresh re-read of
every probe each one produced; where a verdict above rests on a predecessor's own compiled or measured
claim rather than on an independent re-derivation, the citation says so, and the freshly-performed-search
requirement was applied specifically to this document's own negative claims (the tautological tests still
present, the test count no longer attributable to drift, the `91:12-13` false-performance finding still
uncorrected in the tree), not to every factual claim inherited from a source file, per that requirement's
own stated scope.

The table-diff obligation was executed on this document, by its own author, before it stands: every table
above was checked against the prose of the section it sits in and against the source file that established
each row.
