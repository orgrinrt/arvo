# Op's fourth checkpoint, after the fresh read

**Date:** 2026-07-31
**Position in the panel:** written after `12_lattner_fresh_read.md`. **Required reading** with the
numbered files and the earlier checkpoints `04b`, `06b`, `08b`.

The panel continues. Op will say when it is ready for synthesis.

## Why file 11 and file 12 happened

Recorded because it changes what later members should be doing. After ten experts, op's read was that
the review had gone very deep on a few threads and lost the wider design:

> I believe this is already pretty good, but its hyperfocusing on a few things and has forgotten the
> bigger picture and the taxonomy at large, the other sides of all this that aren't really considered
> for talked at all

So `11_current_shape_draft.md` was written as a standalone statement of the shape, and file 12 was
dispatched to read **only that**, with the panel transcripts deliberately withheld, to see what
someone uncarried by the road could see. It worked: file 12 found a missing axis, an entire absent
area of the taxonomy, and a rule collision that ten prior members had walked past.

The lesson for anyone dispatched from here: depth on an open thread is not the only contribution
available, and the widest question in front of you may not be the one you were handed.

## The three calls at this checkpoint

**The arithmetic-fidelity axis is a proposal, not an adoption.** File 12 established that `FastFloat`
and `StrictFloat` differ only in whether reassociation, reciprocal approximation and contraction are
licensed, which changes answers over an identical representable set and therefore sorts as `Policy`
under D54, and that no axis carries it. Op holds it open rather than reopening the ratified set,
consistent with how the panel's other standing design results are held. It stays attackable, and so
does the claim that the ten-axis set is complete.

**The location of the algebraic laws goes to a dispatch of its own.** File 12's claim: the ladder's
first clients are arvo's own algorithm crates; if they bound on it, signed non-`Hot` weights stop
compiling, which is arvo policing its consumers and collides with `arvo-toolbox-not-policer.md`. Its
proposed dissolution is that a sequential fold needs only a magma plus a documented order, and that
associativity is really the contract of *parallel* reduction, belonging one layer up in hilavitkutin
where the reordering actually happens.

Op's call is that this is neither taken nor refused here. It is a relocation across two repositories
touching the engine's scheduling contracts, which no member of this panel has read, and it gets worked
through properly.

**The verification spine gates the crate relocations.** Adopted. File 12 established that the spine
has never met `Bits`-backed storage, and that the argument letting a check at a small width stand for
a large one does not cover `WideBits` limb arithmetic, whose code path diverges per bucket, a hole
invisible to the checking apparatus by construction.

So no crate moves until the spine is proven against real storage at one native width and one
`WideBits` width. This is a change to the round's sequencing: the document changelist cannot be
written from the current shape yet, and the packaging trails the mechanism rather than accompanying
it.

## Standing, unchanged

Only op's calls are final, and even those go stale. Six of this panel's findings have now been
overturned by a later member compiling something rather than reading, and the widest finding so far
came from a member forbidden to read the panel at all.
