# Persona checkpoint twenty-three: the requirements get a moment, the Door authors nothing

**Date:** 2026-08-05
**Position:** after `95_pesce_does_the_requirement_hold.md`. Consolidation ten follows.
**Provenance:** op is asleep and delegated tonight's checkpoints to his persona at Fable tier, with the
instruction to imitate his own prior rulings in style and substance. Every call below is
**persona-decided** and dies the moment he says otherwise.

## 1. The two requirements get a moment and an owner. No third is minted.

**Adopted, file 95's shape close to verbatim.** The evidence is one-sided: the consolidation's claim at
`91:12-13` to have applied the definitional-completeness line is false at three of its own sentences,
grep-checkable, and the line has fired in advance zero times because nothing names who performs it or
when. The table-diff obligation, which does have an owner and a moment, gets performed every
consolidation. That contrast is the whole finding.

So both requirements are performed **by the author of the ratifying text, on that text, before it
stands**, in the table-diff obligation's own shape, and the performance is **reported rather than
cited**. The line's performance is the term list with dispositions; the separation requirement's is, per
model, the axis and the two instantiations at which it separates. A text without both performances has
not been checked, whatever it cites.

The one-word widening rides with it: the line covers **the name being defined**, and a name defined
twice with different content is defined nowhere. That sentence produces the preset finding, the
`StoredWidth` finding and the `Folded<N>` finding mechanically, which is what a requirement is for.

**No third requirement.** The wrong-subject residue is not requirement-shaped; file 94 tried to make it
one and produced a check that cannot fail. What finds wrong subjects is a dispatch from the other chair,
which is a composition habit for briefs rather than spec text. Keep composing panels that way and write
nothing into the design about it.

File 95's reframing of the three instances is accepted as stated: one is the separation requirement
working while unrun, two are the completeness line failing while never run. The requirement earns its
keep; the line was underpriced, and the correct response to an underpriced check is to charge it.

`91:12-13` goes to the droplist as a false performance claim, the same register class as file 79's search
sentence. The consolidation is otherwise the hardening pass it was asked to be; the claim is what fails,
not the document. And "fired zero times in advance" is a corpus-wide count, so it enters ratifying text
with its command or not at all, per `82b`'s own rule.

## 2. Division: the fork dissolves, and the persona instinct at `90b` dies

The stress test came back and the instinct loses on facts read off silicon and the compiler, not on
taste: the ARM instruction returns 0 for `0/0` too and erases the very split the placement existed to
preserve; x86 defines no value and faults; RISC-V defines a third answer; and LLVM treats the cell as
undefined behaviour on every target, deleting the check, so what the target gives away the toolchain
takes back. Four facts, three executed. The `Door` placement smuggled, and the fork's other alternative
housed the identical smuggle at a different address. Both alternatives are dead because the question was.

**Adopted: file 93 section 5, with file 95's two corrections riding on it.**

- **The ground is the ratified first clause, "`Lowering` changes no value", standing since file 40.**
  File 93 cited the weaker second clause and found it vacuous; the first forbids the cell outright with
  no new sentence. The deletion test is adopted as that clause's **decision procedure**, which it never
  had, and not as new canon.
- **The fallback's arity is two.** A one-parameter `div_or` collapses the distinction the solution-set
  derivation just paid to establish, and collides with file 93's own `0/0` partiality clause.
  Directional fallback and indeterminate fallback; a consumer naming one value for both is collapsing
  two cells, which the toolbox posture permits and the text says out loud.

The solution-set derivation is adopted: it repairs the false `0/0` limit clause (an exact-value design
has no licence to perturb an operand that is exactly zero), makes both halves of IEEE clause 7 theorems,
dissolves the kind-sort collision without editing either organ's sentence, and the sign convention for
single-zero numerals gets its one citation-shaped sentence per the completeness line.

`Hot` keeps its fast body with none of the authorship: where the consumer's fallback coincides with the
ISA constant, the lowering emits the bare divide as an implementation of the stated function, seven
instructions, Kind-1 cfg-gated. **The Door decides cost, never content.** That sentence is the entire
legitimate residue of the instinct and it is enough.

Value-position against type-position for the fallback is a surface question for the operation chapter,
not this fork. The owed list carries the signed probe re-runs, the float-division compile, and the
primary-source ISA and IEEE citations beside the executed probes.

**Division's grading axis stays op's own, held since checkpoint ten. Nothing here touches it.**

## 3. Naming: the divergences stand, the reader-quantified test does not

File 94's diagnosis is right and its replacement rule is wrong, and file 95 names why in the only
register that matters: a test whose verdict is a hypothetical stranger's inference cannot fail in the
hands of the person running it, which is the identical defect file 94 correctly struck the parity suite
for. **We do not adopt a check that cannot fail**, whatever it caught on the way in.

So the six divergences stand (one compiled, and with 95's correction on record: the `Cold` prose
contradicted its own probe, and the coincidence surface was one preset rather than two), and they are
caught mechanically by the widened completeness line at 95's three addresses: two definitions,
definition elsewhere, definition in someone else's document. **The class-one clause is replaced by
nothing; the widened line does its work.** The class-two clause stands as adopted with one word swapped:
**"named in the record"**, which is what `90b` meant and what stops the principle dying `67b`'s death.

Adopted with it: the six boundary sentences relocate to the definitions the reader reaches, four of which
already exist and only move; "ambient" plus the lowering-decision clause with its test stated (can a
linked library change it at runtime; if yes it is environment and gets a receipt, if no it is a lowering
decision and gets a type), which also gives NaN-on-overflow a better home than file 71 gave it; the
receipt as a fold over a per-target field set in const position, never hand-written per target, with
95's finishing clause that a zero mask declares the target cannot check the field and a numeral whose
correctness depends on an uncheckable field refuses at declaration rather than passing a receipt; and
the parity suite struck from the artifact list **with the perturbation arm added**, since striking one
that cannot fail and adding one that demonstrates the others do are the same repair.

**`Hot`'s default environment stays op's own, held. Nothing above forces it.**

## 4. The perimeter: ratified where twice-read, working shapes where once

The second reads held at `86b` have been performed, and the second reader was itself caught by the
third, which is the discipline working rather than embarrassing anyone.

**Ratified, two independent reads complete:**

- The `NicheCarrier` seal over its four introduction routes, four distinct error classes, two positive
  controls, upstream growth closed by construction, with the standing condition written down: **no
  granting blanket, ever**.
- The vocabulary narrowed to the `NonZero` family, closed and enumerated; "and whatever else std
  documents" deleted. One entry per shape, never one entry per phrase; a second shape earns a second
  audited entry.
- The audited entry's trusted base shrunk to **the one unreachability sentence**; inhabitant-totality,
  the width claim and the no-wrap condition all move to declaration-site const refusals, per the pricing
  pillar. Facts that can refuse at compile time do not get promised instead.
- The mutation theorem quantified per byte-owner and per level, mirroring statements 0, P and C, with
  the perimeter named in the workspace rule's own terms and a Rust `union` excluded by name. File 95's
  compiled counterexample (column-granularity mutable bytes, zero `unsafe`, digests diverge while every
  read agrees) is the proof the quantification has content.
- **The integer-typed door onto a niche carrier is forbidden outright; the niche-typed door is safe.** A
  door whose stated tier is correctness while its violation tier is unsoundness, with zero diagnostics,
  is exactly what the provable-versus-trusted sentence was adopted to end. On the policing question,
  95's boundary sentence rides with it: policing refuses a choice whose cost the consumer pays; this
  refuses a choice whose cost nobody can pay, because the optimiser owns the consequence.

**Adopted as working shapes, second read owed**, because they are file 95's own one-pass additions and
the discipline does not bend for the file that restated it: the domain-preservation const equation on
the door (flagged as likely to survive, since 95 caught 92 testing its amendment at the one
instantiation where the distinction is vacuous, using 92's own compiled divergent case from another
section of the same file); the terminating clause, that the per-level statement runs to the outermost
level arvo constructs and beyond that the postcondition is a trusted-base entry at the hand-over rather
than a theorem about code arvo did not write; the rename to `NonZeroCarrier`, taken, one token now
against a rename later and the same pattern as `Capacity`; and the requirement-performance clause from
section 1, which is also one-pass and gets no exemption.

The negative result stands recorded: the perimeter cannot be a bound on the permitted feature set, so
its domain is prose by necessity, which is why the completeness line is the right instrument and the
harness is not.

## What the questions did not name

**The capacity tautology.** Files 94 and 95 both said it plainly and they are right: an assertion
comparing a computation to itself has been carried through nineteen files while the record wrote a
verification mandate. But `mock/crates` is op's own ratified boundary and a stand-in does not open it,
even for this. Recommendation: say the word, as a trivial fix commit outside the panel, not as panel
work.

**The `Cold` footprint bench outranks every design item on the open list.** It is now the designated
verifier under the naming principle, the only artifact that could price `Cold`'s own intent, and the one
open item where the review cannot currently find out whether it is wrong. The section-filter half is
fixed; the by-reference input path is the whole remaining blocker and it is a mockspace-side fix. Top of
the queue.

**Still op's alone, prepared and untouched:** `Hot`'s default environment, division's grading axis, the
workspace perimeter rule's provable-versus-trusted clause (lean toward yes, unchanged), the seven upper
vocabulary members' reading, and the `mock/crates` deletion above.

## Standing

Only op's calls are final, and every ratification here is persona-decided and surfaced for his morning
read. The panel produces canon, not source; `mock/research/` and `mock/benches/` are its ground and
`mock/crates` is out of bounds until the canon is complete. The verification mandate at `79b` binds the
implementation phase and is not a panel topic.
