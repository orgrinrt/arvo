# 73_probes outcomes

All probes compiled and run fresh this session, from inside the repo tree (so the directory-scoped
toolchain override applies). Toolchain confirmed immediately before compiling:
`rustc --version` from inside `73_probes/` resolves to `1.98.0-nightly (57d06900f 2026-05-27)`,
`aarch64-apple-darwin`, matching `rust-toolchain.toml`. The identical command from `/tmp`, outside the
repo tree, resolves to `1.94.0 (4a4ef493e 2026-03-02)` stable, confirming the dispatch's own warning.
Flags: `--edition 2024`, `-O` on the runnable probes, plain (no optimisation flag) on the
expected-fail probe. No `#![feature(...)]` line in any probe.

| Probe | Question | Outcome |
|---|---|---|
| `probe_1_byte_cap_is_a_type.rs` | Is a numeral's byte-serialisation buffer expressible gate-free, and const-callable? | **WORKS.** `type ByteCap: ByteCapacity` with a declaration-site coverage check (`const _: () = assert!(C4::BYTES >= byte_width_of(13))`), `zero_bytes::<N>()` is `const fn`, exercised in `const` context (`_CHECK_13: [u8;4]`, `_CHECK_64: [u8;8]`). Runtime assertions also pass. Compile 0.12s. |
| `probe_1b_byte_array_length_is_gce_shaped.rs` | Is the byte-buffer-length question a genuine spine-rule firing site, same shape as file 72's `ShortCap`? | **EXPECTED FAIL, confirmed.** `fn zero_bytes<N: HasStoredWidthBits>() -> [u8; byte_width_of(N::STORED_WIDTH_BITS) as usize]` refuses: `error: generic parameters may not be used in const operations`, `help: add #![feature(generic_const_exprs)]`, plus `error: constant expression depends on a generic parameter`, the identical error shape file 72's `probe_3b` reproduced for the text buffer. Compile 0.04s. |
| `probe_2_padding_purity_and_the_perimeter.rs` | (a) Must a one-argument carrier constructor canonicalise its padding to stay pure? (b) Does `repr(transparent)` expose the raw padding regardless of the safe API's own choices? | **WORKS, both halves.** (a) `From<u16> for Carrier` called twice on the same datum (`0x1a5c`) produces bit-identical carriers with zero padding bits, confirmed pure; the alternative "preserve existing padding" semantics is compiled as a *different*, two-argument operation (`embed_preserving_padding(old, new_datum)`, producing padding `0xE000` carried over from `old`), demonstrating it cannot be expressed as a one-argument `From` impl. (b) `unsafe { transmute::<Carrier, [u8;2]> }` observes exactly the bytes `From<u16>` committed to (`c1.0.to_ne_bytes()`, byte-for-byte equal), and the reconstructed `u16` masks to zero padding, confirming a bit-cast sees precisely what the constructor chose, with no arvo API involved. Compile 0.13s. |
| `probe_3_bitpacked_field_has_no_own_bytes.rs` | Does a bitpacked field packed with zero inter-value padding have its own independent byte image? | **WORKS, general law confirmed on one instance.** Four 13-bit fields packed contiguously into one 64-bit word: 3 of the word's 8 bytes are touched by more than one field (byte 1 by fields 0 and 1, byte 3 by fields 1 and 2, byte 4 by fields 2 and 3), 1 byte is pure headroom, 4 bytes belong to exactly one field. Confirms the general divisibility law (a byte is shared whenever field width mod 8 != 0, here 13 mod 8 = 5 != 0) rather than reporting a sampled instance: every field-boundary/byte-boundary interaction for this width is checked, not a subset. Each field's own datum is separately extractable as a fresh 13-bit value (`0x0c21`, `0x1550`, `0x1fca`, `0x003f`) but that datum's own byte image is not a sub-slice of the word's bytes. Compile 0.17s. |
