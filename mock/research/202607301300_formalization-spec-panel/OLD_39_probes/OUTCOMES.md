# Probe outcomes, file 39

All probes compiled and run against the workspace pin, `rustc 1.98.0-nightly (57d06900f
2026-05-27)`, confirmed with `rustc --version` from inside the repo. Every probe is
const-evaluated or type-checked; no probe times anything. Build shape per probe: plain
`rustc --edition 2021`, no optimisation flags, because every claim is a compile-time claim.

| Probe | Question | Outcome |
|---|---|---|
| `probe_1_matlab_slope_bias.rs` | Does MATLAB slope-and-bias scaling survive the merged shape, and does `Bias = Int` (36:222, 38:337-338) carry it? | WORKS as a refutation of `Bias = Int`: witness slope 1, bias 1/2, checked exhaustively over integer biases -1000..=1000 plus the subgroup argument (integer bias reaches exactly (1/q)Z for slope p/q reduced). The ratified biased-multiplication closure formula (31:397-400) closes exactly over NON-integer rational biases (17 x 17 operand pairs, exhaustive), so the contract's own formula computes in rational-bias algebra. MATLAB's slope decomposition (F in [1,2), reduced ratio) lands on the existing `Adjustment` carrier shape. |
| flipped copy of probe 1 | negative control | FAILS with `error[E0080]: evaluation panicked: assertion failed: is_int_multiple(...)` at the witness loop, so the checks demonstrably run. |
| `probe_2_ieee_systemc_spelling.rs` | Do the post-collapse trait shapes (38:318-356, no `Widening`, no `Growth`) spell IEEE decimal32 as one format under two encodings, `sc_fixed<8,3,SC_RND,SC_SAT>`, and the deferred-vs-per-operation quantisation split? | WORKS. One `Numeral` (radix ten, precision 7, `Ranged<-95, 96, Gradual, Ieee754Specials>`) with `Bid` and `Dpd` as two `Encoding`s type-checks under a function demanding shared numeral and distinct encodings. `sc_fixed` maps to a numeral plus a `Quant<Mid: Direction, Top: Resolution, Bot: Resolution>` instance; SC_SAT_SYM is a `Domain`, not a quantisation, per 31 section 1.2. SystemC per-assignment and MATLAB SpecifyPrecision are two call placements of one `mul_full`/`quantize` pair with no axis anywhere. This is a SPELLING probe over local stand-in traits mirroring 38 section 5; the arithmetic behind the spellings is files 25/33/35's work, not re-verified here. |
| scratch copies of probe 2 | negative controls, two | (i) `type Illegal = Quant<ReduceModulo, ...>` as a bare type alias COMPILED CLEAN, killing my first control: Rust does not enforce a struct's bounds at a type-alias declaration. (ii) The same illegal instance in a fn-parameter position FAILS with `error[E0277]: the trait bound ReduceModulo: Direction is not satisfied`. The compile gate at 26:47-48 therefore holds at use sites and not at aliases that merely name a type; recorded in the probe header. |
| `probe_3_flags_are_views.rs` | Are the three standards' observable side channels views of file 37's grade? | WORKS. Exhaustive over 256 x 256 grade pairs (two cause classes, two event classes, multiplicities 0..=3): the Presence projection is a monoid homomorphism (grade join lands on flag OR), which is IEEE 754 section 7's sticky-flag register and SystemC's per-variable `overflow_flag()`/`quantization_flag()`; the Exact (identity) view is MATLAB's overflow logging (counts). IEEE's deliver-NaN-and-raise-invalid is file 37's reification with the grade kept: same grade, different value presence, every view blind to the difference, exactly the standard's semantics. |
| flipped copy of probe 3 | negative control | Replacing OR with XOR in the presence law FAILS with `error[E0080]: evaluation panicked: assertion failed: feq(presence(gjoin(a, b)), ...)`. |
