# Persona checkpoint fourteen: seven calls, made in op's place

**Date:** 2026-08-04
**Position:** after `62_knuth_the_verification_backlog.md`, immediately before consolidation six.
**Provenance:** same as `48b`, `53b`, `57b`. Op is asleep and delegated tonight's checkpoints to his
persona at Fable tier by the instruction recorded at `48b`. Every call below is **persona-decided, not
op-decided**, and every one is a line for him to overrule. The final synthesis still waits on his word.

The persona read `58` in full, op's four checkpoints, all three predecessor checkpoints, and files 59
through 62 in full, and verified five claims at source before deciding: the committed reproduction
recipe at `08_probes/README.md:8-11`, the repaired `mock/benches/.gitignore` and its 60 tracked
artifacts, the `generic_const_exprs` gate and its stale comment at `arvo-strategy/src/lib.rs:11`, the
`62_probes/` contents, and the notation sketch directory. The first mattered most: the recipe is there,
byte for byte as file 62 quotes it, which settles call 1 before any reasoning starts.

## 1. The `unreproducible` exhibit collapses; the ground survives on a better one, and checkpoint thirteen eats the correction

File 57's compiled fact was true and its universal sentence was false. `a_union.rs` alone does fail
`E0583`; "cannot be reproduced by anyone from what is committed" is refuted by the README sitting in the
directory its own probe ran in, whose opening paragraph is the recipe and whose module mapping names the
two files 57 declared missing. File 62 followed the recipe, rebuilt the crate in under a second, and
reproduced the five-shape table's structure and branch counts.

**The marker on file 08's table is struck in consolidation six.** The table returns to ordinary grounds
with a fresh independent rebuild behind it. Op's loudest-list item 8 closes in the good direction: no
ratified rule rests on an unreproducible measurement. The width-ceiling citation keeps its ground,
re-derived under 62's own name, structure exact (quadrupling per bit, step-budget refusal at nine,
machine-independent), constant machine-shaped. 62's wording proposal goes to op endorsed: the rule
should cite the refusal and the quadrupling as the durable facts and mark the 28.45 seconds as one
machine's wall clock, because the seconds are what a future reader will fail to reproduce and the
refusal is what the argument rests on. That edit is his.

**The ground state itself stays.** The same authority that minted it can re-ground it, and the
vocabulary did real work one file after minting: file 59's bench-artifact finding is a genuine instance
and becomes the exhibit. A registry ground for "derived once, trail cannot rebuild it" is a real third
state distinct from struck and unknown; losing the founding exhibit does not unmake the category. One
line for op to kill the ground entirely if he reads it otherwise.

**Checkpoint thirteen's error is named, not softened.** `57b` adopted the ground on an unverified
universal negative and escalated it onto op's morning list. The check that would have caught it cost one
`cat`, and demanding a targeted re-derivation of the dependent while never demanding a read of the
committed trail for the exhibit itself is exactly the compounding 62 diagnoses. Two conventions are
adopted from it: **a universal "cannot" claim owes an exhaustive read of the committed trail before it
ships**, the same way a cannot-compile claim owes the whole matrix; and file 57's restraint half (never
reconstruct someone else's artifact and present it as their reproduction) survives unchanged, since 62
honoured it and it was never the wrong part. Also corrected in six: `58:722-724`'s "the sweep generator
is committed" was false when written; 62's seven drivers now are the generator.

## 2. The forbidden-feature premise is falsified at two crates; the ban is not touched, the record of the drift is

Two independent experts, both compiled at whole-crate grain: 16 refusal sites in `arvo-strategy` with the
gate stripped, 478 in the facade, `min_generic_const_args` refusing and its const-block escape refusing
behind it. The two-expert convention is met and the factual half closes: **the two remaining
`generic_const_exprs` gates are structural, not stale annotations.** `UFixed` over
`Bits<{ ufixed_bits(I, F) }, S>` is the shape itself, at the crate's identity.

This does not reopen op's 2026-07-28 FORBIDDEN ruling and neither file asked to. The ban stands. What is
falsified is the rule's remediation record: "everything the stack needs works under the `min_` version"
is true of the migrated algorithm crates and false of the L0 dispatch and the facade, and the DRIFT
entry's cheap reading undersells the real work by the full size of a spine-rule migration. The review
records the finding as settled, enters the shipped dispatch in the live-defect registry, and **proposes
62's three-part rewording to op**: scope the sentence to the migrated crates; restate the remediation as
the real thing (the computed width becomes a type, which is what `Nat` already is in the tower, touching
every consumer of `Bits`, `UFixed`, `IFixed`); and state the open violation plainly until the migration
lands, because a rule that names its own violation is trustworthy and one that undersells it by two
orders of magnitude of rewrite cost schedules an afternoon for a quarter's work. The happier sentence
rides with it: the numeral tower is the migration's target shape already built on paper, so the
remediation is the existing design arriving at L0 rather than new design work. Nothing here edits the
file.

## 3. The clause 5.2 sharpenings: adopted, and the decimal default taken presumptively

File 62 read the standard at the paragraph and quoted it with position. The deviation the design carries
is smaller and better-shaped than the sentence claiming it, and all three sharpenings become spec text:
the deviation is confined to exact results (the inexact branch selects by least possible exponent, a
value-keyed rule `Canonical` can express, one file 54 already compiled as a candidate); multiplication on
`Implicit` decimal numerals delivers the standard's preferred exponent by construction, since `mulnum`
computes the exponent sum at the type level, and the same holds for the held `divnum` shape, which turns
"not conformant" into "conformant by construction exactly where the exponent is a type"; and quantize is
the standard's own named special case, datum-dependent by its own text, stated separately rather than
folded into the cohort deviation, because conflating them overstates one and understates the other.

The OFP8 corrections likewise: cite Table 1, page 13 by position, since the document's own definitions
section transposes the two biases and a reader sent to "the PDF" can extract exactly the wrong figures
with primary-source authority; split the FNUZ attribution out of the OCP row, since the document does not
contain that variant. The general rule 62 extracts is adopted: **a primary-source citation owes the
position within the source**, because primary sources carry typos with more authority than secondary
sources carry truths.

The call 62 declined, the persona makes presumptively: **`Canonical = least possible exponent` becomes
the stated default for decimal `Ranged` numerals**, marked reasoned, one line for op. The reasoning is
not close. Op's standard is representability against MATLAB, IEEE 754 and SystemC; a default that makes
every inexact decimal result clause-5.2-conformant, using a cohort rule file 54 already compiled, at zero
new mechanism, is that standard met rather than approximated. It is a default, not a policed choice; the
toolbox rule keeps the other cohort rule available. Second read owed before it hardens.

## 4. The notation vehicle: closed, and closed harder than the fallback clause anticipated

`48b` ratified the intent and left the vehicle sketch-decidable, declarative first, proc-macro acceptable
if hairy or slow. File 61 resolved it in the strongest form the clause admits: the declarative form is
not hairy, it **cannot start**, because a decimal literal is one atomic token and no fragment specifier,
restringify trick or const-generic escape reaches its digits, all compiled, with the value-to-type route
walled identically to the exponent case. The fallback condition is met by structural impossibility, which
is the "cannot, because impossible" branch of op's own distinction, so no novelty attempt is owed against
it.

**The proc-macro vehicle is adopted**: zero external dependencies (lighter than the notko precedent,
not a departure from it), the two entry points matching file 56's adopted resolution, and the unsealed
bridge trait declared in the tower crate. 61's sharpening becomes spec text: once the encoding trait is
sealed, the bridge is not one option among shapes, it is the only shape, since no macro expanding in
another crate can reach a private supertrait and Cargo forces the proc-macro crate to be another crate.
The 923-assertion whole-matrix test ships with it; that it caught the vehicle's own reduction bug before
any reviewer could is the discipline working. Both of 61's droplist entries are adopted, the unused-alias
pricing hazard especially, since it is file 56's unchecked-spec hazard recurring inside a measurement.

**The ceilings are corrected to the bisected figures.** The consolidation carried "roughly `2^127`" and
two mutually disagreeing readout figures. Corrected: a `Pos` is nameable through 128 bits and refused at
129 (E0275); `Pos::VAL` reads back through exactly `2^64 - 1` and refuses at 65 bits (E0080). The `2^63`
figure at `58:621-622` was wrong; the `10^20` figure stays open as plausibly the radix-ten accumulation
figure, unresolved rather than silently folded. The two-tier refusal recommendation is adopted: the
vehicle refuses at the tighter readout wall, prints the actual decimal magnitude, and names the open
widening question, because deferring to the looser structural figure defers an identical failure into a
worse position for a 64-bit-wide band of magnitudes. The staging result is the number the question was
for: host-side reduction is roughly 4.5x cheaper per declaration than making the type checker reduce,
measured, and the framing (the macro is the design's own binding-time decision rather than a
convenience) goes into six as stated.

## 5. The total order: split adopted, fork scoped to where it is live, one new live defect

File 60 corrected its own brief before answering it, which is the right first move: the fork is a
per-numeral fact about injectivity rather than a design-wide verdict, so the dispatch's "none of the
algorithm crates' outputs is law-expressible" was overstated by exactly what injectivity buys back.
`arvo-graph` and `arvo-comb` clear it for free; the fork is live for `arvo-spectral` alone.

**The trait split is adopted as the settled shape.** The shipped `TotalOrd` is IEEE's `totalOrder` under
the wrong name, compiled (probe 3a's refused `const` is the cannot-compile finding at its cleanest: there
is no expected value to write down, because the definition itself places the cohort apart). The design's
answer was already stated at `58:547-551`; 60 is the second independent compiled arrival at it, so the
two-expert threshold is met on the direction: rename the shipped mechanism `totalOrder`, non-law-usable,
kept for consumers that want bit-order on purpose; give `TotalOrd` the canonicalise-then-compare body;
move `arvo-spectral`'s bound. Pre-1.0 churn covers the rename. Open, correctly: NaN-class placement in
the value order, and two traits against one trait with two methods, both preferences rather than results.

**New live-defect entry:** `arvo-spectral`'s partition classifies a degenerate NaN Fiedler component by
its sign bit alone, so which class a node lands in is decided by which NaN payload arithmetic happened to
produce, `partition.rs:59,156,181`, compiled. It sits beside entry 1; the mechanism is different.

**The foldnum width question is moot for the one checkable consumer, and six corrects a framing this
exposes.** hilavitkutin's shipped chain calls neither `upward_rank` nor `bin_pack`; its rank is a
hand-rolled unweighted hop count, and its spectral consumption reads one sign and discards the magnitude.
So section 1.20's consumer-pressure framing overstates: the defects in entry 1 stand on their own
correctness regardless of caller, and six says so, but the urgency story loses its named consumer. The
hand-rolled rank is surfaced to hilavitkutin's side as a possible use-the-stack question and not ruled on
here; that repo's canon is the authority. The general widened-default question stays a proposal.

One instruction for the second read this area owes: 60's probe 5 defends its three-window sampling with a
structural argument, honestly stated, but the second reader runs the injectivity identity exhaustively at
eight bits, where the whole matrix is cheap, rather than inheriting the windows.

## 6. The strategy door: the compiled table replaces the presumptive one, and the printed bench numbers get their correction

File 59 threaded the axis and the presumptive marker drops. Every row's content survived; every
justification is replaced, and the replacement is better in kind: **`Hot` is the only preset whose
meaning is about the instruction; the other three are about the intermediate, and an operation whose
meaning is its intermediate cannot lower to an instruction that has none.** Warm, Cold and Precise are
forced by their own shipped doc comments rather than chosen; `Hot` gains the condition the presumptive
table lacked (host-implementedness, sealed marker, refusal rather than fallback, on the shipped
container-projection precedent). The projection-from-strategy-alone shape is closed twice over (E0119,
and `min_specialization` refusing both halves) and goes on the droplist. The brief's own wording is
corrected: the strategy selects a default lowering; the door is the lowering's member. The
uniformly-`Hot` theorem (hardware door reachable in exactly one of sixteen cells, whole-matrix probe)
becomes spec text; it is the keep-the-shape-where-it-serves instruction paying out at zero rewrite cost.
The three build-layer obligations land in 1.24.

Two narrowed items for op, exactly as 59 scoped them: whether `IeeeDefault` is the right default
environment for `Hot`, or whether `Hot` meeting a float should force an explicit lowering choice; that
one is genuinely his and the persona holds it. The refusal-versus-fallback half the persona calls:
refusal, because the shipped tree already made that call at the container projection and a silent
13x-plus semantic-cost substitution is the policer posture by definition. The `HostImplemented` locus:
cfg-gated per target inside arvo, because it is a target fact and 59's three-facts derivation already
places it there under Kind 1 structural lowering; the receipt, a deployment fact, stays the build
layer's. One line each.

**The gitignore finding is the stretch's second-loudest item and the repair is ratified.** Every bench
artifact this repository ever produced was discarded by three ignore patterns, in direct drift against
the op-stated tracked-bench-history rule and against the discipline file the repo generates to justify
its own harness. 59 removed the patterns, committed the artifacts as its own run's, correctly not under
file 57's name, and re-ran the headline bench. The consequence is a printed-table edit, flagged loudest:
**the "13x to 17x at every point of the sweep" sentence softens to what two runs support, roughly an
order of magnitude, ten to seventeen across two runs**, with the software column reproducing tightly and
the hardware column moving 15 to 25 percent near the harness's resolution floor. Nothing built on the
number changes; the door is worth having at 10x exactly as at 17x. This finding is also the
`unreproducible` ground's new exhibit per call 1.

**The radix-ten measurement is adopted with its readings kept apart.** Decimal costs about twice binary
in software, stated as an upper bound rather than a floor, with the dominant term a division by a
compile-time-bounded constant nobody has strength-reduced, named as an attempt per the novelty posture.
The odd-radix tie vacuity becomes a spec sentence, because offering a tie-breaking choice at radix three
is offering a choice with one outcome. The kernel finding extends 1.17: the part of the quantiser that
carries the design was already radix-general; what was radix-two was the arithmetic underneath, now
repaired and regression-checked bit-for-bit at radix two against silicon.

## 7. Direction: consolidation six now, then the four

Consolidation six is written immediately, per the restated loop, absorbing 59 through 62. Beyond the
standing absorption it carries: the exhibit swap and the struck marker; the GCE settled fact, registry
entry and proposal text; the clause 5.2 and OFP8 sharpenings with the presumptive decimal default; the
vehicle closed with the bisected ceilings replacing both disagreeing figures; the trait split, the new
spectral defect and the corrected consumer framing in 1.20; the compiled door table, the softened bench
sentence and the odd-radix sentence; the spine-rule ordinal recount, done once from the founding
instances instead of carrying the 55-against-56 disagreement a third time; and the table-diff executed on
itself.

Then the four, ordered by unblocking:

1. **The owed-reads dispatch, pinned to a name, first.** It has slipped through three stretches and
   everything ratification-shaped queues behind it. The ledger: `foldnum` and `Unbounded` each carry one
   read (62's) and owe one, `Unbounded` with 62's sealed-`Arity` condition attached; the membership second
   read is owed to someone who is neither Knuth nor file 39's sources, and 62's recusal is the convention
   working; `float_algebraic`'s second vetting read; the `Hot`-door second read 59 asked for; the
   decimal-default second read from call 3; the spectral-split second read with the exhaustive-at-eight-bits
   instruction. The `Rad<P>` second read closes tonight: 62 re-ran all four routes and compiled a fifth.
2. **Price the L0 spine-rule migration.** Not the migration, the budget: a member scopes width-as-type
   through `Bits`/`UFixed`/`IFixed` far enough that op's decision on the rewording has a cost attached
   instead of the two wrong costs currently on offer. Highest leverage available, because it joins the
   panel's tower to the shipped tree's largest defect.
3. **The float model's two remaining residuals**: the model-width transfer argument extended to a
   `Ranged` numeral's two-width shape (believed and unproved, and it underwrites every model-width
   validation the float side does), and `Underflow = Abrupt` under an unnormalised encoding, modelled
   rather than reasoned.
4. **The notation residuals plus the audit tail**: the `Adjustment` entry point and cross-call-site face
   identity, riding with the loop-body-grain codegen sweep 62 left open now that sweeping is possible.

Division stays held as `44b` left it. Per-application against per-value-moved stays op's, declined a
fourth time.

## What none of the questions asked

The four files are the cleanest stretch this review has produced. 62 recusing itself from corroborating
its own file 39 rather than quietly confirming it is the two-expert convention observed at personal cost,
and it goes into six's narrative as the counter-exhibit to what checkpoint thirteen did wrong. 61 finding
and reporting the defect in its own first measurement, and 59's oracle finding its own overflow bug
before finding anything else, are the same discipline.

The one systemic note: three of tonight's four files corrected their own briefs before answering them,
and all three corrections were right. The briefs are written from the consolidation's compressions; the
members read the primary material. That is the review working, and it is also a signal that six's
compressions should be tighter about marking which sentences are derived and which are summary.

## Loudest for op's morning read

1. **Item 8 closes in the good direction**: the width-ceiling citation rebuilds; file 57's
   unreproducibility finding was false, refuted by a committed README one `cat` away; checkpoint thirteen
   adopted a ground state on that false exhibit. The ground survives on file 59's bench-artifact finding.
2. **`unstable-features.md` has a falsified sentence and an underscoped drift entry**: the two remaining
   `generic_const_exprs` gates are structural, 16 and 478 compiled refusal sites, two experts. Proposal
   text drafted; the ban itself untouched.
3. **The printed bench table softens**: every bench artifact ever produced here was gitignored against
   op's own tracked-history rule; the repair landed, and "13x to 17x" becomes "ten to seventeen across two
   runs".
4. **Three presumptive-to-settled moves**: the strategy-door table compiled with three rows forced and two
   narrow questions left for op (`IeeeDefault`, and refusal-at-`Hot`, which the persona called refusal);
   the `TotalOrd` split; the decimal `Canonical` default, the persona's and one line to kill.
5. **The notation vehicle is a proc-macro by structural necessity**, not by the hairiness fallback; zero
   dependencies, ceilings bisected to 128 and 64 bits exactly.

## Standing

Convergence and the novelty posture hold unchanged. The intent outranks every instruction, is vague on
purpose, and is inferred rather than read literally. Only op's calls are final, and none of the above is
one.
