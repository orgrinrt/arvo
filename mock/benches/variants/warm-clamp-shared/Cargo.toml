[package]
name = "bench-warm-clamp-shared"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
name = "bench_warm_clamp_shared"
path = "src/lib.rs"

[dependencies]
mockspace-bench-core = { git = "ssh://git@github.com/hiisi-digital/mockspace.git", branch = "dev", features = ["std"] }
