[package]
name = "bench-bitpack-plan-native"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
name = "bench_bitpack_plan_native"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
bench-bitpack-plan-shared = { path = "../bitpack-plan-shared" }
mockspace-bench-core = { git = "ssh://git@github.com/hiisi-digital/mockspace.git", branch = "dev", features = ["std"] }
mockspace-bench-macro = { git = "ssh://git@github.com/hiisi-digital/mockspace.git", branch = "dev" }
