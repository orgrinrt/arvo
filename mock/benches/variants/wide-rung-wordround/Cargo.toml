[package]
name = "bench-wide-rung-wordround"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
name = "bench_wide_rung_wordround"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
bench-wide-rung-shared = { path = "../wide-rung-shared" }
mockspace-bench-core = { git = "ssh://git@github.com/hiisi-digital/mockspace.git", branch = "dev", features = ["std"] }
mockspace-bench-macro = { git = "ssh://git@github.com/hiisi-digital/mockspace.git", branch = "dev" }
