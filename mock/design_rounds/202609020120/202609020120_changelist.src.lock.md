# Src changelist: no source changes, and that is the finding

**Date:** 2026-09-02
**Round:** a trybuild case does reach an obligation, when a const binds it
**Scope:** none

## CHANGE: nothing, because the source is already the corrected reading
File: none
Verification: `grep -rn "Where the obligation is forced"
mock/crates/arvo-format/src/quantum.rs mock/crates/arvo-format/src/slots.rs` returns a hit in each,
`mock/crates/arvo-format/src/tests/obligations.rs` states it in its module documentation, and the
five `trybuild` cases under `mock/crates/arvo-format/tests/ui/` run and pass with their `E0080`
diagnostics committed beside them.

The paragraph this round corrected claimed a refusal cannot be a `trybuild` case. Every part of the
crate that implements the refusals already contradicts it, and the suite already runs the
counterexamples. **So the design was the only tier that was wrong**, which is the shape worth naming:
a design sentence arriving through a trunk merge, accurate about the tree it came from and false about
the tree it landed in, with the source underneath it correct the whole time and nothing comparing the
two.

The window is opened and locked with nothing in it rather than skipped, because the round's phases are
what record that the source was read against the corrected sentence and found to need no change.
