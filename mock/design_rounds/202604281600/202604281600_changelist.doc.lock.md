**Date:** 2026-04-28
**Phase:** DOC
**Round:** 202604281600. Promote strategy-bound trilemma to DEEPDIVE
**Source topics:** `202604281600_topic.strategy-trilemma-deepdive.md`

This round is doc-only. Three doc-side changes promote the trilemma
research note to a shipped DEEPDIVE template, cross-link from the arvo
DESIGN, and pin the research source as a "promoted" pointer.

## Doc.1. New DEEPDIVE template

`mock/crates/arvo/DEEPDIVE_strategy-bound-trilemma.md.tmpl` (new file).

Body adapts `mock/research/strategy-bound-trilemma.md` for shipping doc
style. The structural shape mirrors `DEEPDIVE_newtype-deref.md.tmpl`:

- Title: `# arvo. Deep Dive: The Strategy-Bound Trilemma`.
- `## Context` block stating who hits this and where in the substrate.
- `## The intent` carrying the three-property statement (static validity,
  strategy-driven layout, ergonomic call sites).
- `## The constraint` explaining the rustc mechanism (projection through
  trait-bound on const-expression over generic params).
- `## Why the alternatives compromise principle` (three rejected
  alternatives A/B/C from the research note).
- `## Creative unsafe options that don't help` (seven traced angles).
- `## How to work within the constraint` (three-layer access pattern).
- `## The discipline` (trait-first, alias-first, never bit-width-generic).
- `## Forward-looking: CrossStrategyOp and the diagnostic surface` (new
  section, post-Pass-G context). Names `CrossStrategyOp<S1, S2>` from
  `arvo-strategy/src/cross_strategy.rs` as the marker trait that surfaces
  the trilemma's discipline at the type level. Explains how the
  `#[diagnostic::on_unimplemented]` message points consumers at explicit
  cast sites instead of resolving cross-strategy ops silently.
- `## Watch-item: implied_bounds` (carried over from the research note).
- `## Sources` referencing prior rounds and rustc tracking issues.

Phrasing updates:

- All references to `Bits<const N: u8>` updated to `Bits<const N: u16>` to
  reflect Pass F state on `dev`.
- Research note's `## Sources` block kept; the `Round 202604500000`
  references stay (they predate the post-rename `dev` shape but are still
  the historical source).
- Em-dash sweep applied to every paragraph. The research note's prose
  reads close to em-dash-free already, but the DEEPDIVE template is
  writing-style-gated, so any present em-dash is replaced with period /
  comma / colon / semicolon per the writing-style rule.

## Doc.2. DESIGN cross-link

`mock/crates/arvo/DESIGN.md.tmpl`, between the existing `## Backlog` and
`## Sources` sections (around line 1056), insert a new section:

```markdown
## Deep dives

Per-topic deep dives live in sibling `DEEPDIVE_*.md.tmpl` templates and
render alongside this DESIGN.

- `DEEPDIVE_newtype-deref.md.tmpl`: rationale for the three-path bridge
  from arvo's primitive newtypes (`Bool`, `USize`) to their inner Rust
  primitives (`bool`, `usize`). Covers `?` (Try), `*` (Deref), and
  `as_bool()` / `as_usize()` (AsBool / AsUSize traits) and when each
  applies.
- `DEEPDIVE_strategy-bound-trilemma.md.tmpl`: the structural constraint
  that makes consumer-facing generic-over-bit-width APIs a smell.
  Explains why `where S: UContainerFor<{ ufixed_bits(I, F) }>` restatement
  is intrinsic on current rustc, the three rejected alternatives, the
  three-layer discipline (trait-first / alias-first / bit-width-generic
  only at arvo-internal layer 3), and the `CrossStrategyOp<S1, S2>`
  marker trait that surfaces the discipline at the type level.
```

The two existing inline `See DEEPDIVE_newtype-deref` references at lines
235 and 294 stay as-is. They are body-of-section references, not the
canonical cross-link surface; the new section is the canonical surface.

## Doc.3. Research note: "promoted" pointer

`mock/research/strategy-bound-trilemma.md`, top of file (above the
existing `> Status` blockquote), insert:

```markdown
> **Promoted 2026-04-28**: this analysis is now shipped as
> `mock/crates/arvo/DEEPDIVE_strategy-bound-trilemma.md.tmpl`. That
> template is the canonical source going forward. This research file
> remains as audit trail for the round 202604500000 investigation that
> produced the analysis; do not edit here, edit the DEEPDIVE template.
```

The existing `> **Status**: research note. Captured during round
202604500000 ...` blockquote stays. The new "promoted" blockquote is
above it, signalling that the file's role has changed from
investigation-active to history.

## Out of scope

- Source files referencing the trilemma (`arvo-strategy/src/cross_strategy.rs`
  in particular) could grow `// see DEEPDIVE_strategy-bound-trilemma`
  doc comments. Out of scope here; future round if anyone asks. The src CL
  for this round declares no source changes.
- Algorithm crates (arvo-graph, arvo-sparse, arvo-spectral, arvo-comb)
  that currently restate the bound have layer-3 friction inline. Adding
  doc-comment references to the DEEPDIVE at those sites is also out of
  scope.

## Phase notes

This CL is living during DOC phase only. Lock with `cargo mock lock`.
After lock, write a no-op src CL declaring zero source changes; lock and
close to traverse the state machine.
