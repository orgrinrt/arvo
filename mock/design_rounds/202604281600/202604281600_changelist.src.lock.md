**Date:** 2026-04-28
**Phase:** SRC
**Round:** 202604281600. Promote strategy-bound trilemma to DEEPDIVE
**Source topics:** `202604281600_topic.strategy-trilemma-deepdive.md`
**Locked doc CL:** `202604281600_changelist.doc.lock.md`

## No source changes

This round is doc-only. The doc CL covers all changes:

- New DEEPDIVE template at `mock/crates/arvo/DEEPDIVE_strategy-bound-trilemma.md.tmpl`.
- Cross-link section added to `mock/crates/arvo/DESIGN.md.tmpl`.
- "Promoted" pointer added to `mock/research/strategy-bound-trilemma.md`.

Source files are untouched. The trilemma's design has shipped (it is
embedded in every UFixed/IFixed/Bits use site already, and the
`CrossStrategyOp<S1, S2>` marker trait shipped in Round D Pass G).
The DEEPDIVE documents that shipped state; it does not introduce new
substrate behaviour.

## Why a no-op src CL still exists

The mockspace state machine requires every round to traverse SRC-PLAN
and SRC phases before close. Even a doc-only round needs an explicit
src CL declaring "no source changes" for audit-trail completeness; the
phase transitions then proceed cleanly through `cargo mock lock` and
`cargo mock close`.

## Validation

After this CL locks and the round closes:

- `cargo check --workspace` clean (no source touched, so unchanged from
  pre-round state).
- `cargo mock --lint-only --commit` clean: writing-style gate accepts
  the new DEEPDIVE template (em-dash sweep applied during DOC
  execution); design-doc-source-mismatch lint accepts the new DESIGN
  cross-link section (no new shipping-claim type names).
- `cargo test --workspace` clean: no source changed, no test impact.

## Phase notes

This CL is living during SRC phase only. Lock with `cargo mock lock`
then `cargo mock close` to traverse to DONE.
