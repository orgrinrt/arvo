# Source changelist: nothing changes in source

Source topic: `202609011520_topic.the-rule-and-the-render-say-what-the-source-does.md`.
Doc changelist: `202609011520_changelist.doc.lock.md`.

## No source change

This round exists because three documents said something the source does not do.
The source was right in all three cases, so every fix landed in a document and
nothing under `mock/crates/**/src/` moves.

The round still needs this changelist, because the phase machine reaches a
closable state through it, and a round that stays open on a branch about to merge
leaves state behind for whoever opens the next one.

## What was checked rather than assumed

The three corrections are each against a source fact, and each fact was read
before the document was edited rather than after.

`arvo-strategy/src/lib.rs:48` declares `type Objective: ObjectiveKind;`. The rule
had shown a const, and lines 44 to 47 of that same file explain why a const
cannot be written there.

The four presets at lines 92, 110, 127 and 145 each declare an `Adaptation`, and
each carries a `FIXME` above it saying the value is a carried default rather than
a ratified one. The rule had said the mode must not be filled in.

`docs/PLACEMENT.md` was three paragraphs behind
`mock/crates/arvo-placement/DESIGN.md.tmpl`, established by running `cargo mock`
and reading `git status` rather than by reading the diff.

## Verification

`git diff --stat <base>..HEAD -- 'mock/crates/**/*.rs'` is empty, which is the
claim this changelist makes.
