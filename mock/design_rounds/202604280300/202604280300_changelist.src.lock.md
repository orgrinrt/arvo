# Source Changelist: arvo-spectral sweep

**Date:** 2026-04-21
**Phase:** SRC

## Files

- `src/fiedler.rs:92` — inline `lint:allow(no-bare-numeric) reason: arvo::traits::FromConstant::from_constant takes u8; boundary conversion until #123 retrofits FromConstant to take USize; tracked: #123`.
- `src/partition.rs:116` — `stack_len: usize = 1;` → `USize(1)` with `.0` extraction.
- `src/partition.rs:119` — `partition_count: usize = 1;` → `USize(1)` with `.0` extraction.
- `src/partition.rs:185` — rewrite `(pos_big as usize) + (neg_big as usize)` as 2x2 match over `(bool, bool)` yielding `USize`.

## Verification

- `cargo test -p arvo-spectral` all tests pass.
- `cargo mock --lint-only` drift 10 → 0 for arvo-spectral (8 unique sites after dedup = 4 sources; same sources fire both arvo-types-only + no-bare-numeric lints).
