# Topic: arvo-spectral sweep — eliminate bare primitives

**Date:** 2026-04-21
**Status:** TOPIC phase
**Task:** #120 round 5

## Drift inventory (4 unique sites)

| File | Line | Kind |
|------|-----:|------|
| fiedler.rs | 92 | `let n_as_u8 = n as u8;` — boundary to `arvo::traits::FromConstant::from_constant(u8)` |
| partition.rs | 116 | `let mut stack_len: usize = 1;` counter |
| partition.rs | 119 | `let mut partition_count: usize = 1;` counter |
| partition.rs | 185 | `let want = (pos_big as usize) + (neg_big as usize);` bool→usize cast |

## Decisions (mechanical)

- partition.rs:116/119 → `USize(1)` with `.0` extraction for arithmetic.
- partition.rs:185 → rewrite as match:
  ```rust
  let want = match (pos_big, neg_big) {
      (true, true) => USize(2),
      (false, false) => USize(0),
      _ => USize(1),
  };
  ```
  No bool→usize cast in source.
- fiedler.rs:92 → `lint:allow(no-bare-numeric)` inline with reason pointing at a new task: `arvo::traits::FromConstant::from_constant signature takes u8; wrapping conversion needed until FromConstant is retrofitted to take USize`. New task (P2): **FromConstant should take USize instead of u8**. Once resolved, the lint:allow lifts.

## Out of scope

- `FromConstant` redesign itself (arvo L0 concern, own round).

## Execution notes

Mechanical. No public API change to arvo-spectral. Create the new FromConstant-retrofit task; reference it in the lint:allow.
