# Doc changelist: the eleventh review's claim audit

**Date:** 2026-09-19
**Round:** the eleventh review's findings on `feat/platform-width-types-on-a-wider-slot-range`
**Scope:** `crates/arvo-format/DESIGN.md.tmpl`.
**Topics consumed:** `202609191840_topic.eleventh-review-claim-audit.md`, the only flat topic.
**Evidence:** `mock/research/202609191400_core_shadowing_probe/`, whose `run.sh` now generates a flipped
control per checking item of every arm expected to build, and runs two new arms, an unaliased `use` of
`core` and a `self` under `core`. Committed in `TOPIC` phase.

## Canon

Same governing row as the earlier platform-width changelists
(`a_platform_width_type_is_a_target_bound_member_of_the_format_family`). No row changes.

## `crates/arvo-format/DESIGN.md.tmpl`, internal paths out of the rendered text

The three research paths (the sixty-four-bit sketch, the foreign-target probes, the core-shadowing probe)
and `cargo mock rounding-vocabulary` come out. Each sentence keeps the finding and loses the path; the
sentence that was only a pointer to the probe directory goes whole. The rounding-vocabulary sentence now
says `away from zero` is not one of the six names the vocabulary carries, which is what the six-name ruling
lists, and drops "canon question" and the reference to the tool.

Verification: the five leakage greps from the documentation-writing skill's `surface-tiers.md`, plus a
`(^|[^a-z_])mock/` grep, return 0 on the template for the machinery, path, tool, provenance and `mock/`
classes; the `-w` grep returns 5, read one by one: two ordinary uses of `round`/`Round` and three
`ratified` that are on `origin/dev` already.

## `crates/arvo-format/DESIGN.md.tmpl`, what the leading `::` resists

"The leading `::` resists the first two on its own" becomes a list of exactly the shapes an arm shows it
for: a module named `core`, a `use` alias `as core`, an unaliased `use` whose last segment is `core`
outside braces, and a `self` under a `core` segment. The braced unaliased form, which the lint refuses and
no arm tries, is named as outside that list.
