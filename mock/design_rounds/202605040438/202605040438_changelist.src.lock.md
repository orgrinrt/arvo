**Date:** 2026-05-04
**Phase:** SRC
**Round:** 202605040438
**Scope:** One source-doc-comment edit. No type, trait, or signature changes.

# Source-side execution plan

## arvo-bitmask

### CHANGE: `node.rs` module doc updates `BitMatrix64` / `BitMatrix256` reference to chassis form

File: `mock/crates/arvo-bitmask/src/node.rs:5`

Before (current source state):

```rust
//! Used by `BitMatrix64` / `BitMatrix256` and any consumer that speaks in
//! nodes rather than raw integers.
```

After:

```rust
//! Used by `BitMatrix<W, N>` and any consumer that speaks in nodes
//! rather than raw integers.
```

Verification: `! grep -q "BitMatrix64\|BitMatrix256" mock/crates/arvo-bitmask/src/node.rs && grep -q "BitMatrix<W, N>" mock/crates/arvo-bitmask/src/node.rs`.

This single edit is the only source change in this round.

## Pre-lock verification

- `cargo +nightly check --workspace --lib` clean (no semantic change).
- The verification grep above succeeds.
- `cargo mock --lint-only --commit` clean.

## Cross-references

- Doc CL: `202605040438_changelist.doc.lock.md`.
- Topic file: `202605040438_topic_round_3_doc_drift_cleanup.md`.
- Prior round: `202605031748/` (closed, merged via PR #44).
