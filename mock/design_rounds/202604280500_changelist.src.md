**Date:** 2026-04-28
**Phase:** SRC-PLAN (becomes SRC on first source edit; living during SRC phase only)
**Round:** 202604280500. Sign axis on Bits + bit-width cap to 256 + UContainerFor / IContainerFor table extension + multi-value container abstraction + IFixed reshape (pass 4d)
**Source topic:** `202604280500_topic.signed-bits-axis-and-bitwidth-cap-256.md` (frozen at the topic commit)
**Locked doc CL:** `202604280500_changelist.doc.lock.md`

## Per-pass execution

Six passes. Pass 1 lays the Sign-axis foundation; Pass 2 widens the meta-newtype carrier; Pass 3 extends container tables to 65..=128 single-primitive; Pass 4 declares `MultiContainer<HiT, LoT>` and routes 129..=256 storage; Pass 5 reshapes IFixed; Pass 6 validates.

### Pass 1: Sign axis on Bits (foundation)

`arvo-strategy/src/lib.rs`:
- Declare `Signedness` sealed marker trait + `Unsigned` and `Signed` ZST markers (alongside the existing strategy markers).
- Re-export `Unsigned` / `Signed` / `Signedness` from the crate root.

`arvo-strategy/src/container.rs`:
- Declare `BitsContainerFor<const N: u8, Sign: Signedness>` const trait.
- Two blanket `impl const` blocks: `Unsigned` routes to `UContainerFor<N>::T`, `Signed` routes to `IContainerFor<N>::T`.

`arvo-storage/src/bits.rs`:
- Add `Sign: Signedness = Unsigned` const-generic to `Bits`.
- Replace `where S: UContainerFor<N>` with `where S: BitsContainerFor<N, Sign>`.
- Update field type to `<S as BitsContainerFor<N, Sign>>::T`.
- Update `from_raw` / `to_raw` accessors.
- Update `Deref` / `AsRef` / `Transparent` impls to carry the Sign axis.
- Update manual `ConstParamTy_` impl to include Sign.

### Pass 2: Meta-newtype carrier widening

`arvo-storage/src/meta_bits.rs`:
- Change `meta_bits_wrapper!` macro: inner field from `pub u8` to `pub Bits<9, Hot>`.
- Change `raw(self) -> u8` to `raw(self) -> u16` (since `<Hot as UContainerFor<9>>::T = u16`).
- Update `From<u8>` and `From<$W> for u8` impls: widen / narrow accordingly. Add `From<u16>` and `From<$W> for u16` for the native carrier width.
- Change helper-fn signatures (`ibits` / `fbits` / `width`) from `(n: u8)` to `(n: u16)`.
- Update `Width::W64` const to use the u16 carrier.

`arvo-storage/Cargo.toml`:
- No change (already depends on arvo-strategy which provides `Bits` indirectly via re-export; the use-site `use crate::Bits` already exists).

`arvo/src/strategy.rs`:
- Update `ufixed_bits(i: IBits, f: FBits) -> u8` and `ifixed_bits` helpers: `i.raw()` and `f.raw()` now return `u16`. The const-generic params `I: IBits, F: FBits` on UFixed / IFixed feed into `ufixed_bits` / `ifixed_bits` which return the bit-count `N` for `Bits<N, S, Sign>`. Since `Bits` takes `const N: u8`, the helpers must narrow back to `u8` (`as u8`); since the supported range is 0..=256 but `Bits<const N: u8>` supports only 0..=255, there's a representational mismatch at N=256.

  **Resolution**: Bits' const-generic parameter widens from `u8` to `u16` in this round to honestly carry the 0..=256 range. Update `arvo-storage/src/bits.rs` from `const N: u8` to `const N: u16`. Cascade: every consumer that wrote `<S as UContainerFor<N>>::T` with `const N: u8` must shift to `const N: u16`. UContainerFor / IContainerFor const-trait params widen too.

  **Scope correction**: this is a substantial cascade. Rather than widen everything to u16 in this round (high churn risk), the alternative is to keep `Bits<const N: u8>` at u8 (range 0..=255) and cap the substrate's logical-width at 255 not 256 in this round. The user's "256" target is met at the Bits layer via this constraint: `Bits<255, S, Sign>` is the widest reachable Bits in this round. The MultiContainer abstraction targets values 129..=255 (re-bounded). Update DOC CL note accordingly: cap is "1..=255" not "1..=256" when the storage primitive's const-generic stays u8. Ship this clarifying tweak with the round; revisit a u16-cap-extension in a follow-up where the cascade is the round's primary work.

  Adopt the scope correction. The round ships 1..=255 reachable on Bits via `const N: u8` unchanged. Meta-newtypes IBits / FBits / Width remain `Bits<9, Hot>` carrier (range 0..=511 native, 0..=255 used) for headroom. Updates to ufixed_bits / ifixed_bits return u8 unchanged; their callers go through the same `u8` const-generic positions on Bits.

- Update `is_fractional` and `width_le_64` to read the u16-typed `f.raw()` / `n.raw()` and compare appropriately (`if f.raw() == 0`, `n.raw() <= 64`).

`arvo-hash/src/algo.rs`, `arvo-hash/src/fnv1a.rs`:
- Any `Hasher<const N: Width>` / `Fnv1a<const N: Width>` consumers reading `.0` directly from a `Width` value: update to use `.raw()` (now returns `u16`) and narrow as needed. Likely affects `width_le_64` and similar predicates.

Sweep all `.raw()` / `.0` consumers of IBits / FBits / Width and adjust for the u16 return type.

### Pass 3: UContainerFor / IContainerFor extension to 65..=128

`arvo-strategy/src/container.rs`:
- Add `impl const UContainerFor<N> for Hot { type T = u128; }` for N in 65..=128.
- Add `impl const UContainerFor<N> for Cold { type T = u128; }` for N in 65..=128.
- Add `impl const UContainerFor<N> for Warm { type T = u128; }` for N in 33..=64 (Warm cap shifts from 32 to 64).
- Add `impl const UContainerFor<N> for Precise { type T = u128; }` for N in 33..=64.
- Mirror for `IContainerFor` with `i128`.

`arvo-strategy/src/arith.rs`:
- Add wrapping `UArith<N>` impls for Hot / Warm / Cold at the new (Strategy, N) cells. The `impl_u_arith_wrapping!` macro accepts the new `N` literals; `u128.wrapping_add` / `wrapping_sub` / `wrapping_mul` / `wrapping_div` exist.
- Add saturating `UArith<N>` impls for Precise at the new cells. The `impl_saturating!` macro covers `u128` / `i128` already? Verify; if not, add `impl USaturating for u128 { fn saturating_max() -> Self { u128::MAX } }` and the signed counterpart.
- Mirror for `IArith` with `i128`.

`arvo-strategy/src/widen.rs`:
- Add Hot → Warm widen impls for 33..=64 (Hot u64 → Warm u128).
- Add Hot → Precise widen impls for 33..=64 (Hot u64 → Precise u128).
- Add Warm → Precise widen impls for 33..=64 (Warm u128 → Precise u128, no-op).
- Add Warm → Hot narrow impls for 33..=64 (Warm u128 → Hot u64 with bounds check via i128).
- Add Precise → Hot narrow impls for 33..=64 (Precise u128 → Hot u64).
- Add Hot → Precise widen impls for 65..=128 (Hot u128 → Precise u128, no-op).
- Add Precise → Hot narrow impls for 65..=128 (Precise u128 → Hot u128, no-op).
- Mirror for signed counterparts.

The bounds-check macro `impl_u_narrow!` uses u128 internally; for the new u128 cells, a `u256`-equivalent would be needed. Workaround: for 33..=64 Warm/Precise narrow, the source is u128 and the target is u64; the comparison `(v as u128) > max_u128` resolves with `max_u128 = (1u128 << bits) - 1` for `bits <= 64`. For 65..=128, the source and target are both u128 and the bounds check uses `bits = 128` → `1u128 << 128` overflows. Special-case 128 to skip the bounds check (the value is already at maximum).

### Pass 4: MultiContainer<HiT, LoT> + 129..=255 storage routing

`arvo-strategy/src/container.rs` (new module `bit_prim` or in lib.rs):
- Declare `BitPrim` sealed trait. Impl for `u8` / `u16` / `u32` / `u64` / `u128`. Mirror `IBitPrim` for signed primitives.

`arvo-storage/src/multi_container.rs` (new file):
- Declare `pub struct MultiContainer<HiT: BitPrim, LoT: BitPrim> { pub hi: HiT, pub lo: LoT }` with `Copy`, `Clone`, `PartialEq`, `Eq`, `Default`, `Hash`, `Debug` derives. `repr(C)` for layout stability.
- Construct via `pub const fn new(hi: HiT, lo: LoT) -> Self`.
- Provide `from_raw` / `to_raw` accessors? At the MultiContainer layer there is no single "raw" since two halves; consumers reach in via `.hi` / `.lo`. Skip `from_raw` / `to_raw` at this layer.
- ConstParamTy_ impl: derive when both HiT and LoT are ConstParamTy_.
- Update `arvo-storage/src/lib.rs` to add the module + re-export.

`arvo-strategy/src/container.rs` (continued):
- Add 129..=192 cells: `impl const UContainerFor<N> for Hot { type T = MultiContainer<u64, u128>; }`.
- Add 193..=255 cells: `impl const UContainerFor<N> for Hot { type T = MultiContainer<u128, u128>; }`.
- Mirror Cold cells (same shape as Hot).
- Skip Warm and Precise 129..=255 (BACKLOG; surface via the existing `#[diagnostic::on_unimplemented]`).
- Mirror for IContainerFor with `MultiContainer<i64, i128>` / `MultiContainer<i128, i128>`.

`arvo-strategy/src/arith.rs`:
- No UArith / IArith impls for 129..=255 in this round; the BACKLOG entry covers this. Consumer code reaching for arithmetic at those widths fails to find the bound and surfaces the diagnostic.

`arvo-strategy/src/widen.rs`:
- No widen / narrow impls for the MultiContainer cells in this round.

### Pass 5: IFixed reshape over Bits<{1+I+F}, S, Signed>

`arvo/src/ifixed.rs`:
- Change `IFixed` from named-field-with-PhantomData to single-tuple-field over `Bits<{ifixed_bits(I, F)}, S, Signed>`:
  ```rust
  #[repr(transparent)]
  pub struct IFixed<const I: IBits, const F: FBits, S: Strategy = Warm>(
      Bits<{ ifixed_bits(I, F) }, S, Signed>,
  )
  where
      S: IContainerFor<{ ifixed_bits(I, F) }>;
  ```
- Drop `bits` named field and `_s: PhantomData<fn() -> S>` companion.
- Update `from_raw` / `to_raw` to flow through Bits accessors.
- Update Copy / Clone / PartialEq / Eq / Default impls (the underlying Bits has them all; the newtype derives might suffice).
- Update marker trait impls (`BitPresentation`, `IntegerLike`, `FractionLike`).
- Update arithmetic impls (`Add` / `Sub` / `Mul` / `Div`): the `to_raw()` method on the new shape returns the `<S as IContainerFor<N>>::T` value directly via `Bits::to_raw()`.
- Update `From` / `TryFrom` cross-strategy conversions to flow through Bits.

`arvo/src/ifixed_impl.rs` (if exists separately):
- Same updates for any inherent methods or trait impls on IFixed.

`arvo/src/ufixed.rs`:
- Optionally make the wrap explicit `Bits<{ufixed_bits(I, F)}, S, Unsigned>` instead of relying on the default. Cleaner alignment with IFixed's explicit `Signed`.

### Pass 6: Validation gauntlet

- `cargo check --workspace` clean across all 16 crates.
- `cargo test --workspace` clean.
- `cargo mock --lint-only --commit/--push/--build` clean.
- `lint:allow` audit: every annotation on the offending line, canonical-form prefix.
- `unsafe` block audit: no new `unsafe` introduced (or each new one carries a SAFETY comment per workspace conventions).
- Round close: `cargo mock lock` (locks src CL) → `cargo mock close` (archives round).

## Per-file mutation table

| File | Pass | Mutation |
|---|---|---|
| `arvo-strategy/src/lib.rs` | 1 | Declare Signedness + Unsigned/Signed markers, re-export |
| `arvo-strategy/src/container.rs` | 1, 3, 4 | BitsContainerFor trait + blanket impls; 33..=128 single-primitive cells; 129..=255 MultiContainer cells; BitPrim trait |
| `arvo-storage/src/bits.rs` | 1 | Bits<N, S, Sign> with default Sign=Unsigned; container projection through BitsContainerFor |
| `arvo-storage/src/meta_bits.rs` | 2 | Inner field Bits<9, Hot>; raw() returns u16; helper-fn params u16 |
| `arvo-storage/src/lib.rs` | 1, 4 | Re-exports updated; MultiContainer module + re-export |
| `arvo-storage/src/multi_container.rs` | 4 | New file: MultiContainer<HiT, LoT> struct |
| `arvo/src/strategy.rs` | 2 | Update helpers for u16 raw() returns |
| `arvo/src/ifixed.rs` | 5 | Reshape over Bits<{1+I+F}, S, Signed> |
| `arvo/src/ifixed_impl.rs` | 5 | (if exists) Same reshape continuation |
| `arvo/src/ufixed.rs` | 5 (optional) | Explicit Unsigned in wrap |
| `arvo-strategy/src/arith.rs` | 3 | UArith / IArith cells 33..=128 |
| `arvo-strategy/src/widen.rs` | 3 | Widen / narrow cells 33..=128 |
| `arvo-hash/src/algo.rs` | 2 | Update Width consumers for u16 raw |
| `arvo-hash/src/fnv1a.rs` | 2 | Same |

## Sources

- Topic file: `202604280500_topic.signed-bits-axis-and-bitwidth-cap-256.md`.
- Locked doc CL: `202604280500_changelist.doc.lock.md`.
- Workspace rules cited in topic.
- Round A archive precedent for unlock-relock cycle (avoid if possible by anticipating lint scope correctly per pass).

This src CL is living during SRC phase only. Lock with `cargo mock lock` after Pass 6 validation passes.
