# Source changelist: nothing changes in source

Source topic: `202609011520_topic.the-rule-and-the-render-say-what-the-source-does.md`.
Doc changelist: `202609011520_changelist.doc.lock.md`.

## Comparison to deprecated changelist

`202609011520_changelist.src.deprecated.md` said the same thing and named a
verification command that does not check it. It gave

```
git diff --stat <base>..HEAD -- 'mock/crates/**/*.rs'
```

with the branch base, so the range covered every commit on the branch rather than
this round's, and it returned `arvo-strategy/src/lib.rs | 20 +++---` from work
that landed before the round opened. The claim was true and the stated check was
false, which is worse than a wrong claim: it reads as verified.

Replaced rather than amended, so the wrong command stays visible in the audit
trail beside the right one.

## No source change

This round exists because three documents said something the source does not do.
The source was right in all three cases, so every fix landed in a document and
nothing under `mock/crates/**/src/` moves.

The round still needs this changelist, because the phase machine reaches a
closable state through it, and a round left open on a branch about to merge is
state handed to whoever opens the next one.

## What was checked rather than assumed

Each correction is against a source fact read before the document was edited.

`arvo-strategy/src/lib.rs` declares `type Objective: ObjectiveKind;`, and the
rustdoc immediately above it says why a const cannot be written there: it becomes
a const generic argument depending on a generic parameter at the call site.

The four presets each declare an `Adaptation` and each carries a `FIXME` above it
saying the value is a carried default rather than a ratified one. The rule had
said the mode must not be filled in.

`docs/PLACEMENT.md` was three paragraphs behind its template, established by
running `cargo mock` and reading `git status` rather than by reading the diff.

## Verification

The range is this round's own commits, from the doc lock's parent forward, which
is what the deprecated changelist got wrong.

```
git diff --stat 86300862^^..HEAD -- 'mock/crates/**/*.rs'
```

Empty, and the four commits in that range are the regeneration, the doc lock, this
changelist and its lock.
