# DOC changelist: three sentences that claim more than they hold

## CHANGE: the spine paragraph FROM an instance claim TO a resemblance
File: `mock/crates/arvo-format/DESIGN.md.tmpl`
Verification: the paragraph no longer says `Fraction::of` is an instance of the spine's shape, and it names the clause that stops it being one.

The paraphrase drops the spine's second sentence, which is the one that decides
this case: the adaptation is a first-class object, and an operation fusing one
invisibly is not something the model's statements apply to. `Fraction::of` fuses
them in one `const fn`, and `Fraction` is not a format. The heading already says
one tier down; the paragraph says it too, and says why the resemblance stops
short of an instance.

## CHANGE: the exact-region cell FROM the corner TO the region
File: `mock/crates/arvo-format/DESIGN.md.tmpl`
Verification: the cell names the positive-denominator case, which is where most of the region is and where the constructor does neither of the two things the cell used to describe.

Moving the sign and cancelling the shared power of two are both what the
`i64::MIN` branch does. Where the denominator is positive the pair is returned
as given, which `a_positive_denominator_is_kept_rather_than_replaced` pins.
