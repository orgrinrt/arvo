**Date:** 2026-05-03
**Phase:** DOC
**Scope:** Round 4 follow-up — senior PR #43 review fix-ups
**Source topic:** `202605031658_topic_round_4_pr_review_fixes.md` (frozen)

# Doc CL — round 4 PR review fixes

Five fix passes. Doc-side surface changes are minimal: add canonical bridge impl notes, document the XxHash3 streaming truncation diagnostic.

## Pass 1: arvo-hash test crate doc updates

`tests/algo.rs` and `tests/fnv1a.rs` are test files; their doc comments mention `HasherExt` and per-N inherents. Refresh.

## Pass 2: arvo-hash xxhash3 doc — streaming truncation diagnostic

`mock/crates/arvo-hash/DESIGN.md.tmpl` `## Concrete algorithm: XxHash3` section gains a sentence on the streaming-buffer hard cap:

> The streaming `update` is bounded by a 256-byte stack buffer. Bytes beyond the buffer trigger `debug_assert!` in debug builds (silent in release; consumers who need a higher cap chunk explicitly or use `ConstHash::hash_const` directly with the full byte slice). The streaming surface fits the substrate's content-addressing band (16-128 bytes typical); above that, the one-shot `ConstHash` path is the recommended consumer surface.

## Pass 3: writing-style cleanup in arvo-bits-contracts

The em-dash in `narrow_from.rs:1` is replaced with a period. No prose change.

## Pass 4: arvo-storage meta_bits.rs doc refresh

`mock/crates/arvo-storage/src/meta_bits.rs` doc comments at lines ~299-301 update from `Hasher<const N: Width>` / `Fnv1a<const N: Width>` to `Hasher<const N: u16>` / `Fnv1a<const N: u16>` (matches post-#316 / round 202605031400 source state).

## Pass 5: bridge family canonical impls

`arvo-strategy/DESIGN.md.tmpl` `## ConstFrom and ConstTryFrom bridges` section gains a `## Canonical impls` subsection:

> - `impl const ConstFrom<u16> for Width` — wraps a `u16` literal into the `Width` newtype carrier. Replaces the prior `width_u16(n: u16) -> Width` helper as the bridge-aware form.

`arvo-transparent/DESIGN.md.tmpl` `## ConstDeref / ConstAsRef<T> bridges` section gains a `## Canonical impls` subsection:

> - `impl const ConstDeref for WideBits<BYTES, A>` with `type Target = [u8; BYTES]`.
> - `impl<const BYTES: usize, A: Align> const ConstAsRef<[u8; BYTES]> for WideBits<BYTES, A>`.

`ConstTryFrom` defers — no obvious canonical impl this round; lands when a consumer surfaces the need.

## Items intentionally NOT in this round

- Bridge bodies on every substrate type. One canonical impl per bridge exercises the trait shape; full coverage is per-consumer-need work.
- `_bound::<Signed>()` workaround removal in `algo.rs`. Nit; not a blocker. Tracked in next session's todo.
- Streaming XxHash3 chunking API. Out of scope; the `debug_assert!` plus doc note is the agreed contract.
