# DOC changelist: euclid/split overflow fixes

**Round:** 202606232300
**Phase:** DOC
**Touches:** `mock/crates/arvo/DESIGN.md.tmpl`

## CHANGE: arvo/DESIGN.md.tmpl

- In "## Composable traits", extend the `EuclidDiv` entry: with a sub-unit `Self` divisor the whole-unit
  quotient can exceed the type range; the result saturates to the logical bound rather than panicking (the
  ULP `ScalarEuclid` path cannot overflow, since its divisor is an integer count `>= 1`).
- Extend the `EvenSplittable` entry: `split_evenly(USize(0))` yields an empty iterator (a zero-way split has
  no shares); contrast the scalar div-by-zero path, which returns the numerator.

Backticked names introduced: none (clarifies `EuclidDiv` / `EvenSplittable`).
