**Date:** 2026-04-28
**Phase:** DOC
**Round:** 202604281000. Consolidated substrate completion batch
**Source topics:** `202604281000_topic.substrate-completion-batch.md`

This doc CL covers the doc-side changes for all seven passes of the consolidated round. Each pass section names the files to change and the section-level edits. The CL is a living document during DOC phase; recommit as needed during execution.

The src CL (`202604281000_changelist.src.md`) is written separately, after this CL locks.

## Pass A — Soundness extensions

Documents the three additive sub-passes layered on top of Round D-aux's compile-time gates.

### A.1 — `HasAxes` correctness check

`mock/crates/arvo-strategy/DESIGN.md.tmpl`
- Section: "Strategy axis decomposition".
- Add a paragraph after the existing `OverflowPolicy` / `ContainerWidth` / `StorageLayout` description: each axis surface gains a `const DISCRIMINANT: u8` (or `Bits<8, Hot, Unsigned>` once Pass F lifts the const-generic param) for compile-time projection. The discriminant lets `HasAxes::Container` cross-check against the strategy's `ContainerWidth::Output`, pinning documentation to behaviour.

`mock/crates/arvo-storage/DESIGN.md.tmpl`
- Section: "Layout-soundness gates" (new section, originally added by Round D-aux).
- Add a paragraph: the Round 202604280841 layout assertions cover layout. The Round 202604281000 axis-correctness assertions cover trait projections. Both sets live in `arvo-storage/src/layout_assertions.rs`; the axis assertions appear as a Tier-5 block.

### A.2 — Full Bits-cell assertion coverage

`mock/crates/arvo-storage/DESIGN.md.tmpl`
- Section: "Layout-soundness gates".
- Add a paragraph: the boundary-cell assertions from Round 202604280841 stay default-on. A `cfg(arvo_full_layout_audit)` gate opens the full (Strategy, N, Sign) tuple expansion (~2000 cells). Periodic CI nightly builds run the full audit; default builds keep the bounded set.

### A.3 — `ConstParamTy` soundness audit

`mock/crates/arvo-storage/DESIGN.md.tmpl`
- Section: "Meta-bit newtypes".
- Add a paragraph: every newtype in const-generic position (`IBits`, `FBits`, `Width`, `MetaCarrier`, future `Bits<const N: u16>` parameter cell) is layout-asserted to satisfy `StructuralEq + Copy + Eq + ConstParamTy`. The assertions live alongside the layout assertions in `arvo-storage/src/layout_assertions.rs`.

## Pass B — Original Round D content

### B.1 — Hash family adoption

`mock/crates/arvo-hash/DESIGN.md.tmpl`
- Section: "Default hash family" (rewrite).
- Replace the FNV-1a-as-default framing. New default is `XxHash3`. FNV-1a stays as `Fnv1aHasher`, accessible via `ContentHasher` for consumers with known-good fits. The rewrite explains the bandwidth tradeoff (xxhash3 wins on inputs >= 32 bytes; FNV-1a competitive at 8-16 bytes) and names the consumer's primary input shape (16-128 byte entity content addressing).
- Section: "Strategy table".
- Add a row: `XxHash3` strategy with its `ContainerWidth = Hot` projection.

`mock/crates/arvo-hash/BACKLOG.md.tmpl`
- Promote the "better hash family" entry out of BACKLOG (the entry exists today as a promissory note).
- Add a new entry: bench wyhash variants against xxhash3 for substrate-representative payload distributions. Future round absorbs.

### B.2 — `ContentHash` widens to `Bits<64, Hot>`

`mock/crates/arvo-hash/DESIGN.md.tmpl`
- Section: "Content-addressed hash".
- Replace `Bits<32, Hot, Unsigned>` references with `Bits<64, Hot, Unsigned>`. Document that 64 bits matches xxhash3's natural output and reduces collision probability for million-entity workloads.

### B.3 — Narrow surface expansion

`mock/crates/arvo/DESIGN.md.tmpl`
- Section: "Narrow / Mask traits".
- Add `narrow_to_unmasked(self) -> Self::Out` to the `Narrow` trait surface description. Document the precondition (high bits already zero; consumer responsibility) and the use case (chained narrow, extract-then-narrow). The unmasked variant skips the mask op for the hot path.

### B.4 — arvo-narrow population

`mock/crates/arvo-narrow/DESIGN.md.tmpl`
- Section: "Concrete narrow impls" (new section).
- Document the per-source-target primitive narrowing table (u16 → u8, u32 → u16, u64 → u32, u128 → u64, plus signed variants). Document the typed `Bits<M, S, Sign> → Bits<N, S, Sign>` for M > N. The `arvo-narrow` crate stops being empty.

`mock/crates/arvo-narrow/BACKLOG.md.tmpl`
- Promote the populate-impls entry out.

## Pass C — Bitfield `S` parameterisation

`mock/crates/arvo/DESIGN.md.tmpl`
- Section: "Bitfield primitive".
- Add the `S: Strategy` parameter to the `bitfield!` macro signature description. Default stays `Hot` for backwards compat. Document the use case: Cold-bitpacked column-store bitfields for million-entity workloads. Note the toolbox-not-policer rule: substrate does not pre-judge; the consumer turns the knob.

`mock/crates/arvo-bitmask/DESIGN.md.tmpl`
- Section: any reference to `bitfield!` consumer usage.
- Update consumer examples to show the optional `S` parameter.

`mock/crates/arvo/BACKLOG.md.tmpl`
- Promote the bitfield-S-param entry out.

## Pass D — Precise / Warm 33..=64 inconsistency fix

`mock/crates/arvo-storage/DESIGN.md.tmpl`
- Section: "Container projection table".
- Update the Precise row at 33..=64: container is `u128` / `i128` (was `u64` / `i64`).
- Update the prose description noting the 2x-logical-width contract holds at every cell for both Warm and Precise.

`mock/crates/arvo/DESIGN.md.tmpl`
- Section: "Strategy markers".
- Update the Precise description: container at 33..=64 is u128, matching Warm. The 2x rule is now consistent across the Warm / Precise band.

`mock/crates/arvo-storage/BACKLOG.md.tmpl`
- If a "Precise/Warm 33..=64 inconsistency" entry exists, promote it out.
- The macro-generated impls in `arvo/src/traits.rs` (`impl_abs_ifixed_integer_sat`, `impl_from_constant_ufixed`) are touched in src CL Pass D; doc-side just notes the consistency.

## Pass E — Strategy-axis-driven arithmetic dispatch

`mock/crates/arvo/DESIGN.md.tmpl`
- Section: "Numeric arithmetic traits" (`UArith`, `IArith`, `UWidenFrom`, `UNarrowFrom`).
- Replace bound descriptions: where today's docs say "where S: Strategy", new docs say "where S: HasAxes<Container = ..., Overflow = ..., Layout = ...>". The dispatch projects through axes; specific strategy markers compose via `HasAxes`.
- Add a paragraph: this enables `Hot + Saturating` (uses Hot's container width with saturating overflow), `Cold + Saturating` (Cold's container width, saturating), `Warm + Wrapping` (Warm's width, wrapping), etc. The codegen path picks the appropriate intrinsic for each axis combination.

`mock/crates/arvo-strategy/DESIGN.md.tmpl`
- Section: "Strategy axis decomposition".
- Promote the "axis-driven arith dispatch is consumed by `arvo`'s arith traits" prose from BACKLOG / TODO into shipping description.

`mock/crates/arvo/BACKLOG.md.tmpl`
- Promote the axis-driven-dispatch entry out.

## Pass F — `Bits<const N: u16>` widening

`mock/crates/arvo-storage/DESIGN.md.tmpl`
- Section: "Bits storage primitive".
- Update `Bits<const N: u8>` to `Bits<const N: u16>`. Document that the const-generic parameter is `u16` to support reachable widths up to 65535 (current cell projections cover 1..=256). The widening makes future cell-table extensions a parameter-free addition.

`mock/crates/arvo-storage/BACKLOG.md.tmpl`
- Promote the const-generic-u16-widening entry out.

`mock/crates/arvo/BACKLOG.md.tmpl`
- Promote the cap-related entry out (was a sibling).

## Pass G — Cross-strategy resolution warning

`mock/crates/arvo/DESIGN.md.tmpl`
- Section: "Strategy resolution".
- Add a paragraph after the `Resolve<S1, S2>::Out` table: the marker trait `CrossStrategyOp<S1, S2>` carries `#[diagnostic::on_unimplemented]` for the unresolved projection cells. Cross-strategy ops resolve silently per the table; the diagnostic warns the consumer at compile time when the resolution shifts overflow / SIMD / storage semantics. Consumer opt-out via `lint:allow(cross-strategy-resolution)` annotation. The diagnostic text names the axis that shifted and the resulting projection.

`mock/crates/arvo/BACKLOG.md.tmpl`
- Promote the cross-strategy-warning entry out.
- Add a new entry: iterate diagnostic text per consumer feedback. Future-round concern.

## Validation checklist

After each pass's doc execution:

1. Em-dash check: `grep -n '—' mock/crates/*/DESIGN.md.tmpl mock/crates/*/BACKLOG.md.tmpl`. Expect zero hits.
2. Hype-word check: `grep -nEi 'blazing|seamless|powerful|amazing|incredible|game-changing|best-in-class' mock/crates/*/DESIGN.md.tmpl`. Expect zero hits.
3. Backticked-type names in DESIGN.md.tmpl correspond to actual designed types (the `design-doc-source-mismatch` lint runs on `cargo mock --lint-only --commit`).
4. BACKLOG entries removed are not left dangling: each promoted entry's prose either lands in the corresponding DESIGN.md.tmpl section or in the topic / src CL.

## Comparison to prior changelists

This CL does not deprecate any prior CL. It builds on Rounds A through D-aux, which ship as their own closed CLs.

The CL is intentionally large. Per the topic decision, all seven passes ship in one round; each pass commits separately during DOC execution, with the CL recommitted to mark progress. Locking happens after the last pass commits.
