**Date:** 2026-05-25
**Phase:** DOC
**Scope:** arvo Debug on UFixed + IFixed DOC CL. No new DESIGN sections; the existing numeric-type sections cover the trait surface. One BACKLOG entry edit confirming task #428's two outstanding types ship in this round.
**Source TOPIC:** `mock/design_rounds/202605250730_topic.arvo-debug-ufixed-ifixed.md`.

# Doc changes

## CHANGE: BACKLOG entry for arvo Debug task ADDED to BACKLOG.md.tmpl
File: `mock/crates/arvo/BACKLOG.md.tmpl`
Verification: `grep -q "Debug on UFixed and IFixed" mock/crates/arvo/BACKLOG.md.tmpl`.

Adds short retrospective entry recording that task #428 ships Debug for `UFixed` and `IFixed` in this round; the other named numeric types (`Bits`, `USize`, `Bool`, `FastFloat`, `StrictFloat`) already had Debug via derive in previous rounds. Per the no-legacy-shims-pre-1.0 convention, this is a retrospective record entry naming the two types that shipped via manual impl in this round.

# Verification target

After the round ships: both `UFixed` and `IFixed` formatted via Debug renders their type-tag string. Smoke tests pass.
