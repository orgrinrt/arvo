**Date:** 2026-05-25
**Phase:** SRC
**Scope:** arvo Debug on UFixed + IFixed src CL. Adds manual `impl core::fmt::Debug` to both numeric newtypes. Verification is compile-pass: the trait method body is small and forwards to the inner `Bits`'s derived Debug, which already has coverage. A runtime smoke test was considered but rejected: arvo is `no_std + no-alloc`, so `format!` is unavailable, and a hand-rolled stack-buffer `fmt::Write` impl would itself need three `lint:allow(no-bare-numeric)` + one `lint:allow(no-bare-string)` markers tracking #115. The compile pass is the verification.
**Source TOPIC:** `mock/design_rounds/202605250730_topic.arvo-debug-ufixed-ifixed.md`.

# Source changes

## CHANGE: `impl Debug for UFixed<I, F, S>` ADDED to `mock/crates/arvo/src/ufixed.rs`
File: `mock/crates/arvo/src/ufixed.rs`
Verification: `grep "core::fmt::Debug for UFixed" mock/crates/arvo/src/ufixed.rs` returns one match. `cargo build -p arvo` clean.

Manual impl matching the existing `impl Copy / Clone / PartialEq / Eq` pattern's where-clause shape. Body forwards to `f.debug_tuple("UFixed").field(&self.0).finish()`. The bound includes `Bits<{ ufixed_bits(I, F) }, S, Unsigned>: core::fmt::Debug` so the container projection produces a Debug-capable Bits.

## CHANGE: `impl Debug for IFixed<I, F, S>` ADDED to `mock/crates/arvo/src/ifixed.rs`
File: `mock/crates/arvo/src/ifixed.rs`
Verification: `grep "core::fmt::Debug for IFixed" mock/crates/arvo/src/ifixed.rs` returns one match. `cargo build -p arvo` clean.

Symmetric counterpart to UFixed. Bound uses `Signed` in the container projection and `f.debug_tuple("IFixed")` as the type tag.

# Verification target

- `cargo build -p arvo` clean.
- `cargo test -p arvo` clean (no new tests, existing suite continues to pass).
- Both Debug impls exist as named in the CHANGE blocks.
