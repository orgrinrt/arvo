# src changelist

Executes the doc changelist: the lint's header and the platform-width test module narrowed to the three
forms the lint reads, the manifest-rename condition corrected to what the probe's new arms show, the
oracle's two readings taken from the implementation named where the oracle is described, the sweep
widened in a file of its own, and the history narration and the undocumented dither range fixed.

## CHANGE: lint `the-platform-width-points-read-the-pointer-width` NARROWS its header and its rename condition
File: `mock/lints/the_platform_width_points_read_the_pointer_width.rs`
Verification: `grep -c 'nothing in \`arvo-format\` binds the name' mock/lints/the_platform_width_points_read_the_pointer_width.rs` returns 0, and the opening paragraph says no source file binds `core` "through a module, a `use` item or an `extern crate` alias" and that the other ways are listed as unguarded. `grep -c "re-exports the real \`core\`'s contents" mock/lints/the_platform_width_points_read_the_pointer_width.rs` returns 0; the rename sentence names `core::prelude::rust_2024` and says an empty module at that path is enough. The file is 370 lines.

## CHANGE: `tests/the_platform_width.rs` NARROWS its closing comment to the three forms
File: `mock/crates/arvo-format/src/tests/the_platform_width.rs`
Verification: the comment says the lint's second arm refuses a module, a `use` item binding `core` anywhere in its tree and an `extern crate ... as core`, and names a manifest rename, a glob import, another kind of item named `core` and a macro-expanded form as outside what it reads. It says the alias equals the literal point whenever it reads `::core::primitive::usize::BITS` and that path resolves to the real `core`.

## CHANGE: `apply/tests/the_oracle.rs` NAMES the two readings it takes from the implementation
File: `mock/crates/arvo-format/src/apply/tests/the_oracle.rs`
Verification: the module doc no longer says the oracle "answers from what each mode and each policy is documented to do". It lists `Clamp` read as `Saturate`, with the `Clamp` cells checking that collapse and nothing about a clamp to a bound, and a `HalfUp` tie read away from zero, citing `question::which_operation_half_up_denotes` as open. A comment on each of the two match arms says the same. The two tests are renamed to what they assert: `the_oracle_reads_each_mode_as_its_rustdoc_says` and `the_oracle_wraps_by_the_span_and_pins_saturate_and_clamp_alike`. `grep -c 'crate::apply' mock/crates/arvo-format/src/apply/tests/the_oracle.rs` still returns 0.

## CHANGE: `apply/tests/the_oracle_sweep.rs` SAYS what its `Clamp` and `HalfUp` cells check
File: `mock/crates/arvo-format/src/apply/tests/the_oracle_sweep.rs`
Verification: the module doc's first paragraph ends by saying the oracle reads `Clamp` as `Saturate` and a `HalfUp` tie away from zero, both taken from the implementation.

## CHANGE: `apply/tests/the_wide_oracle_sweep.rs` ADDS the sweep over the wider region
File: `mock/crates/arvo-format/src/apply/tests/the_wide_oracle_sweep.rs`, `mock/crates/arvo-format/src/apply/tests/mod.rs`
Verification: the ranges are `[0, 0]`, `[5, 5]`, `[i128::MIN, i128::MIN]`, `[i128::MAX, i128::MAX]`, `[0, 2^64 - 1]`, `[-2^63, 2^63 - 1]`, the `2^64`-slot ranges at both ends of the index and `[-4, 3]`; the anchors are both ends of the index and their neighbours, both ends of the range, one past each, and zero; the ratios are whole offsets from `i64::MIN / 2` to `i64::MAX / 2` at remainders 0, 1, half and one under the denominator, over the denominators 1, 2, 3, 4, 7 and `i64::MAX`, plus `i64::MIN` and `i64::MAX` over each; the dithers are 0, 1, 3 and 7 eighths. `the_shipped_map_answers_as_the_oracle_does_over_the_wide_region` runs the shipped map over every mode and policy; `the_platform_width_points_answer_as_the_oracle_does` runs `adapt` and `panic_on_overflow` at all eighteen signatures over `USize` and at all eighteen over `ISize`; `the_wide_sweep_reports_every_broken_map` asserts each of the nine maps in `the_broken_maps.rs` disagrees with the oracle over this region; `the_wide_feed_reaches_past_either_end_of_the_index_by_more_than_2_to_the_62` pins that some fed position carries a distance past the index above `2^62` on each side. The file uses no `std` and is 318 lines. `cargo test -p arvo-format --lib apply::tests` runs 73 tests green.

## CHANGE: the step-onto-the-bottom mutation turns both sweeps red, recorded and reversed
File: `mock/crates/arvo-format/src/apply.rs` (the mutation reversed within this changelist)
Verification: with the Saturate/Clamp condition in `complete_slot` replaced by `(rounded.past < 0 && !(rounded.past == -1 && rounded.up.get())) || (rounded.past == 0 && rounded.down() < lo)`, `cargo test -p arvo-format --lib` reported `208 passed; 5 failed`: `a_quarter_under_the_index_ceils_onto_its_bottom_and_saturates_low`, `the_oracle_sweep::the_shipped_map_answers_as_the_oracle_does`, `the_oracle_sweep::every_declared_signature_answers_as_the_oracle_does`, `the_wide_oracle_sweep::the_platform_width_points_answer_as_the_oracle_does` (first case `TowardZero`, `Saturate`, range `[0, 2^64 - 1]`, anchor `i128::MIN`, ratio `-1/2`, got `2^64 - 1`, want `0`) and `the_wide_oracle_sweep::the_shipped_map_answers_as_the_oracle_does_over_the_wide_region`. The file was restored from a copy taken before the edit, `diff` printed nothing, it was touched, and the same command reported 213 passed.

## CHANGE: `Dither::at` DOCUMENTS a ratio outside `[0, 1)`, and a test pins it
File: `mock/crates/arvo-format/src/apply/position.rs`, `mock/crates/arvo-format/src/apply/tests/the_edges.rs`
Verification: the rustdoc says the ratio is kept as given, that a dither below zero steps every off-grid position up as `Ceil` does, one at or above one steps none as `Floor` does, and a position on the grid is not moved by any dither. No code in `position.rs` changes. `a_dither_below_zero_rounds_as_ceil_and_one_at_or_above_one_as_floor` asserts the stochastic signature over `Integer<3>` equals the `Ceil` one under dithers `-1/8`, `-1` and `i64::MIN`, and the `Floor` one under `1`, `9/8` and `i64::MAX`, at every edge slot and every quarter from `-8/4` to `8/4`, with a control that inside `[0, 1)` a quarter past zero goes down under a half and up under an eighth.

## CHANGE: history narration REMOVED from `panic_on_overflow` and a test constructor
File: `mock/crates/arvo-format/src/apply.rs`, `mock/crates/arvo-format/src/apply/tests/the_carry_past_the_index.rs`
Verification: `grep -rn 'the slot verdict it replaces\|before it kept one' mock/crates` returns nothing. `git diff -U0 origin/dev -- mock/crates | grep -E '^\+.*\b(used to|previously|earlier version|no longer|first version|old version)\b'` returns nothing.

## CHANGE: `quantum.rs` DROPS a claim about what every test would do
File: `mock/crates/arvo-format/src/quantum.rs`
Verification: `grep -c 'every test still passed' mock/crates/arvo-format/src/quantum.rs` returns 0. The paragraph says a narrower integer is an argument over the coordinates' widths rather than a property of the check, so the check does not rest on it.

## Not actioned

The pre-existing history narration in `arvo-bits/src/tests.rs`, `arvo-placement/src/tests.rs`, `arvo-strategy/src/tests.rs`, `tests/compile_fail.rs`, `tests/matlab_fi_parity.rs` and `DESIGN.md.tmpl`'s join paragraph is not added or changed by this branch, and is left for a round that touches those files. The ruling and question rows describing the shipped tree as unable to declare 64 bits are canon, which code does not edit.
