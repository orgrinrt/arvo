**Date:** 2026-05-29
**Phase:** DOC
**Round:** 202605290259 tensor cap constructor

# Doc changelist

## arvo-tensor/DESIGN.md.tmpl: add a `## cap` section

Add a section mirroring the existing `## cap_size` section, documenting `cap` as
the canonical `usize -> Cap` constructor (the inverse of `cap_size`), with the
same `generic_const_exprs` rationale: a named `const fn` is the accepted form for
const-generic position where the literal `Cap(USize(n))` construction is
rejected. Note it is the bridge consumers use to form `Cap`-parameterised types
from `usize` const-params.

No other doc surface changes. `cap` is a new pub `const fn`; the
`design-doc-source-mismatch` lint checks backticked names in DESIGN exist as pub
items, which the source changelist satisfies.
