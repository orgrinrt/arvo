**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605290259 tensor cap constructor

# Source changelist

## CHANGE: add `cap` to `arvo-tensor/src/cap.rs`
File: `mock/crates/arvo-tensor/src/cap.rs`
Add `pub const fn cap(n: usize) -> Cap { Cap(USize(n)) }`, the inverse of `cap_size`, with a `lint:allow(no-bare-numeric) lint:allow(arvo-types-only)` carrying the `#121` reason (the `usize` parameter is the definition-site bridge boundary, same as `cap_size`'s `usize` return). Add `use arvo::USize;` to the imports.
Verification: `grep "pub const fn cap(" mock/crates/arvo-tensor/src/cap.rs` returns one match.

## CHANGE: export `cap` from `arvo-tensor/src/lib.rs`
File: `mock/crates/arvo-tensor/src/lib.rs`
Add `cap` to the `pub use cap::cap_size;` re-export so consumers reach `arvo_tensor::cap`.
Verification: `grep "pub use cap::{cap, cap_size}\|pub use cap::cap;" mock/crates/arvo-tensor/src/lib.rs` returns a match.

## CHANGE: test the round-trip in `arvo-tensor`
File: `mock/crates/arvo-tensor/tests/cap.rs` (new) or an inline `#[cfg(test)]` module in `cap.rs`
Assert `cap_size(cap(n)) == n` at several values and `cap(n) == Cap(USize(n))`.
Verification: `cargo test -p arvo-tensor` runs the round-trip test green.
