# Doc changelist: no template changes

**Date:** 2026-09-01
**Round:** the alias table claims more than it enforces
**Scope:** none.

## CHANGE: nothing FROM the templates TO the templates
File: none
Verification: `git diff --name-only` over this round names no `*.md.tmpl` and no generated document.

The round changes what one tool prints about why a rewrite is safe. `mock/tools/` is not a template
surface and nothing under it renders into `docs/`, so no `*.md.tmpl` moves and no generated document
moves with it.

The tool's own description string, which `mock tools` prints and which the generated tool catalogue
snapshots, is unchanged: the change is to a section blurb in the report body and to one doc comment on
an internal enum. So the catalogue does not go stale from this round.

No registry row moves either, and that is a decision rather than an omission. The blurb overreaches
about `half_up`, and what `half_up` denotes is an open naming call with no row of its own. Filing that
row means writing options and naming a decider, which is canon work and not a tool edit, so the round
reports it and leaves it. `mock/registry/*.toml` is untouched.

This changelist exists because the phase machine requires a doc window to close before a src one opens,
not because there is doc work in it.
