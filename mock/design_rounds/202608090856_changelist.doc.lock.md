**Round:** bench-workspace-exclude-regression. **Topic:**
`202608090855_topic.bench-workspace-exclude-regression.md`.

## Scope

No `mock/crates/*.md.tmpl` document exists to change (the tree is empty), so
this changelist carries no design-document edits. Its only purpose is to move
the round out of `TOPIC` phase so `mock/Cargo.toml` (gated at the repo root
independently of the per-crate doc/src gate) can be edited to restore the
bench harness's workspace-exclude entry, per the topic file.

## Change

`mock/Cargo.toml`: add `exclude = ["benches"]` under `[workspace]`, with a
comment recording why. No other line changes.

## Why this needs a round at all

The mockspace write-guard gates the literal `Cargo.toml` at the mock root the
same way it gates `crates/*.rs` and `crates/*.md.tmpl`: blocked in `TOPIC` or
`CLOSED`, allowed otherwise. This fix is infrastructure rather than design, and
carries no doc-CL content beyond this note; the round exists to satisfy the
gate, not because the fix is a design decision.
