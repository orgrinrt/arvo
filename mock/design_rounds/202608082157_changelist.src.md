# Src changelist: delete the crates, empty the workspace

**Date:** 2026-08-08. **Round:** nuking the tree for the canon.
**Doc changelist:** `202608082157_changelist.doc.lock.md`.

## CHANGE: every crate under `mock/crates` FROM `present` TO `deleted`
File: `mock/crates/`
Verification: `ls mock/crates | wc -l` returns 0. The directory itself remains, because mockspace
panics rather than erroring when it is absent (`can't read crates dir: NotFound`, `src/parse.rs:12`).

## CHANGE: `mock/Cargo.toml` `[workspace] members` FROM `16 crates + 40 bench variants` TO `[]`
File: `mock/Cargo.toml`
Verification: `grep -A1 '^members' mock/Cargo.toml` shows `members = []`.

## CHANGE: `mock/Cargo.toml` `[workspace.dependencies]` FROM `notko + 16 path deps` TO `notko only`
File: `mock/Cargo.toml`
Verification: `grep -c 'path = "crates/' mock/Cargo.toml` returns 0.

## CHANGE: `mock/Cargo.lock` FROM `present` TO `deleted`
File: `mock/Cargo.lock`
Verification: `test -f mock/Cargo.lock` fails. It pinned the deleted members.

## What is untouched, and why

`mock/benches` keeps every artifact and every variant crate. The artifacts are committed evidence and
the canon panel's doability claims cite them; the variant crates stop being workspace members with
everything else and will not build until there is something to measure. That is correct rather than
incidental: there is nothing to measure yet.

`mock/research`, `mock/design_rounds`, `mock/agent`, `mock/lints` and the root `*.md.tmpl` are not
code and are not in scope.

## Recoverability

Git history holds every deleted byte. `origin/dev` still carries the full tree, so consumers pinned to
it are unaffected. The branch is `feat/arvo-shape-topic`, not a trunk.
