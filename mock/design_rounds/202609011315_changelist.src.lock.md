# Src changelist: five compile-fail arms, from two sides that did not know about each other

**Date:** 2026-09-01
**Round:** the trunk's refusal and this branch's three are one file
**Scope:** `mock/crates/arvo-format/tests/compile_fail.rs`

The merge of `dev` into this branch conflicts in one file, and this is the resolution.

## CHANGE: `compile_fail.rs` FROM four arms on either side TO five
File: `mock/crates/arvo-format/tests/compile_fail.rs`
Verification: `cargo test -p arvo-format --test compile_fail` reports five passing, and
`grep -c '#\[test\]' mock/crates/arvo-format/tests/compile_fail.rs` returns 5.

Both sides are kept whole. The trunk's `an_arvo_type_as_a_const_parameter_is_refused` and this
branch's three coordinate refusals name different things, compile independently, and pass together.
Selecting either side would have deleted refusals that hold, which is what the workspace's conflict
rule forbids.

## CHANGE: the module doc's count FROM three TO the arms that are actually there
File: `mock/crates/arvo-format/tests/compile_fail.rs`
Verification: `grep -c 'The other three' mock/crates/arvo-format/tests/compile_fail.rs` returns zero.

The doc read "the other three", which was true of this branch alone. After the merge it is four, and
the fifth runs the other way: three pin what the coordinate types buy, and the last pins the position
where a coordinate type is refused. The doc says both, because a reader uses that sentence to know
whether the file is complete and an off-by-one there is the file lying about its own coverage.
