# Doc changelist: the fifth review's shadowing hole and two open items

**Date:** 2026-09-19
**Round:** the fifth review's blocking and catalogue findings on
`feat/platform-width-types-on-a-wider-slot-range`
**Scope:** `crates/arvo-format/DESIGN.md.tmpl`.
**Topics consumed:** `202609191341_topic.fifth-review-shadowing-and-cross-end-fixes.md`, the only flat
topic.
**Evidence:** a compiled probe against the pinned toolchain, committed in the src changelist beside the
respelling it backs, showing `mod core`, `use ... as core` and `extern crate self as core` each defeat
the bare `core::primitive::usize::BITS` path or one another, and that the leading-`::` path resists the
first two but not `extern crate self as core` at the crate root.

## Canon

Same governing row as the fourth review's changelist (`a_platform_width_type_is_a_target_bound_member_of_the_format_family`).
No row changes; every edit here narrows a claim about the spelling that closes the shadowing hole, or
files two already-known gaps as open items. Neither touches what the canon states.

## `crates/arvo-format/DESIGN.md.tmpl`

The platform-width points section: the two aliases in the code block, and every prose mention of their
spelling, move from `core::primitive::usize::BITS` to `::core::primitive::usize::BITS`, with the leading
`::`. The paragraph explaining why the spelling closes the shadow is rewritten to say precisely what the
leading `::` closes (an item or import named `usize` or `core` inside a module) and what it does not (a
crate reference bound to the name `core` at the crate root), and to say the second is closed by a lint
arm rather than by the path alone. "The laws the platform-width points owe tests for" gets the same
narrowing: "nothing in this crate can shadow it" becomes a claim about the leading-`::` path plus the
lint's refusal of every way this crate could bind the name `core`, stated as two mechanisms rather than
one.

This does not touch the free-`const _`-item sentence a few paragraphs down (already correct since the
fourth review); the src changelist narrows the two sites the fifth review found still carrying the
stale "a const assertion... would be evaluated" wording, `the_platform_width.rs` and the lint's own
module doc comment, since both are Rust source rather than this template.

A new "Open items" section, at the end of the file, files two gaps `146d` named and no round has closed:
`Exact`'s distance past the index has no public reader, and `sum_position` saturates rather than
carrying an exact position for an operand outside the range. Neither is a defect against a stated
obligation; both are recorded so the next round that touches either area finds them here rather than
back in a closed review's own file.

No other prose in this crate's DESIGN.md.tmpl is touched by this round.
