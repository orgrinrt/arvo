# Doc changelist: no crate document moves, and one figure in the record is corrected

**Date:** 2026-09-24
**Round:** the third review of pull request #150, blocker 1 and findings 2 to 8, and the last round on
`half-up-is-not-a-magnitude-rule`.
**Topics consumed:** `202609241413_topic.the-reader-attributes-a-reading-by-what-its-segment-names.md`.
It is the only flat topic in `mock/design_rounds/` on any remote ref, checked with `git ls-tree -r
--name-only` over every ref under `refs/remotes/origin`.
**Scope:** this file. No crate document changes.

The topic is about the lint's reader, which is harness rather than crate design, so every change it
asks for is the source changelist's. It is named here so no topic goes unmentioned.

## Canon

`ruling::half_up_denotes_ties_toward_positive_infinity` governs, unchanged. `ruling::the_rounding_mode_vocabulary`
names the six modes the reader's list of other rules is built around, unchanged. The registry is not
edited in this round.

## Claims of earlier rounds that are not true at head

Locked changelists are immutable, so the correction goes here, which is the only surface the flow
offers a reader of a closed round.

`mock/design_rounds/202609200239/202609200239_changelist.doc.lock.md:106` says of the command at
`mock/design_rounds/202609192215/202609192220_changelist.src.lock.md:159` that "the lint suite passes
forty tests, where it passes sixty-nine at head". Two things are wrong with it. The figure was taken at
`fc71ffb1`, not at head, and it was taken with the filter `half_up` rather than with the filter
`half_up_is_not` the cited command names. Re-run in this round, the figures are:

| Filter            | at `fc71ffb1` | at `db634837` |
|-------------------|---------------|---------------|
| `half_up`         | 69            | 81            |
| `half_up_is_not`  | 52            | 60            |

The command at `:159` passes fifty-two tests at `fc71ffb1` and sixty at `db634837`. The sibling topic
`202609200233_topic.the-record-carries-two-claims-that-stopped-being-true.md:28` carries the same
sixty-nine and is corrected by this paragraph as well. The `fc71ffb1` figures were taken in a detached
worktree at that commit after `cargo mock` had generated its lint crate, and the `db634837` figures in
this round's worktree before any source change.

Every figure this round's source changelist gives names the filter and the ref it was taken at.
