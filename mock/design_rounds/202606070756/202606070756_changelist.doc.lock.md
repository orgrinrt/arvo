**Date:** 2026-06-07
**Phase:** DOC
**Scope:** arvo-tensor (L2)
**Round:** const-capacity-surface (202606070756)

## Doc changes

One file: `mock/crates/arvo-tensor/DESIGN.md.tmpl`.

Add a `ConstCapacity` section after the existing `Capacity and Dim` section.
The section documents the additive const sibling of `Capacity`: why a separate
trait (the `from_fn` const blocker), the const surface (`filled` / `get` /
`set`), the `Array<T: Copy>: Copy` GAT, the `Dim<N>` impl, and the
`const_trait_impl` gate. Every backticked type the section names
(`ConstCapacity`, `Dim`, `Cap`, `USize`) resolves to a real source item once the
src CL lands, satisfying the design-doc-source-mismatch lint.

No other doc template changes. README.md.tmpl stays as is (the const surface is
a design-deep-dive concern, not a one-line crate summary). BACKLOG.md.tmpl
unaffected.
