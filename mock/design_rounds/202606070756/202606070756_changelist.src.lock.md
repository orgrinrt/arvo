**Date:** 2026-06-07
**Phase:** SRC
**Scope:** arvo-tensor (L2)
**Round:** const-capacity-surface (202606070756)

Additive const sibling of `Capacity`. No existing item changes shape; `Capacity`,
`Dim`'s `Capacity` impl, and every consumer are untouched. The src CL adds one
feature gate, one trait + impl, one re-export, and one test file.

## CHANGE: feature gate `const_trait_impl` ADDED TO arvo-tensor crate root
File: `mock/crates/arvo-tensor/src/lib.rs`
Verification: `grep "feature(const_trait_impl)" mock/crates/arvo-tensor/src/lib.rs` returns one match.
Note: WATCH-tier ALLOWED per the unstable-feature sweep; already enabled across arvo-bits / arvo-bitmask / arvo-storage / the contract crates.

## CHANGE: trait `ConstCapacity` ADDED `const trait` with `Dim<N>` impl
File: `mock/crates/arvo-tensor/src/capacity.rs`
Verification: `grep "const trait ConstCapacity" mock/crates/arvo-tensor/src/capacity.rs` and `grep "impl<const N: usize> const ConstCapacity for Dim" mock/crates/arvo-tensor/src/capacity.rs` each return one match.
Surface: `type Array<T: Copy>: Copy; const CAP: Cap; fn filled<T: Copy>(v) -> Self::Array<T>; fn get<T: Copy>(&Self::Array<T>, USize) -> T; fn set<T: Copy>(&mut Self::Array<T>, USize, T)`. The `Dim<N>` impl maps `Array<T> = [T; N]`, `CAP = cap(N)`, with const `[v; N]` / `a[i.0]` bodies. Existing `Capacity` trait + impl in the same file are unchanged.

## CHANGE: re-export `ConstCapacity` FROM arvo-tensor crate root
File: `mock/crates/arvo-tensor/src/lib.rs`
Verification: `grep "ConstCapacity" mock/crates/arvo-tensor/src/lib.rs` shows it in the `pub use capacity::{...}` line.

## CHANGE: integration test `tests/const_capacity.rs` ADDED
File: `mock/crates/arvo-tensor/tests/const_capacity.rs`
Verification: file exists; `cargo test -p arvo-tensor --test const_capacity` passes.
Covers: const construction + indexed read/write in a forced-const-eval context (a `const` item computed by a const fn generic over `C: [const] ConstCapacity`), at two distinct N (no per-N GCE blowup), plus the typed-CAP assertion. This is the real-type analog of the proven R0 sketch.
