# Source Changelist: arvo L0 core::TryFrom annotations

**Date:** 2026-04-21
**Phase:** SRC

## Files

- `src/ifixed.rs:233` — inline `lint:allow(no-bare-result)` trailing the `fn try_from` sig.
- `src/ifixed.rs:248` — same.
- `src/ufixed.rs:262` — same.
- `src/ufixed.rs:277` — same.

Each annotation text: `// lint:allow(no-bare-result) reason: core::convert::TryFrom::try_from trait-method signature returns Result<Self, Self::Error>; tracked: #115`.

## Verification

- `cargo test -p arvo` passes.
- `cargo mock --lint-only` arvo drift 4 → 0.
- arvo L2/L3 sweep series complete — total violations from 123 → 0 (modulo graceful-ramp block removal in a follow-up PR once all rounds merge).
