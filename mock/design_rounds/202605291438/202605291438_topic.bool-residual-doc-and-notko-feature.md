**Date:** 2026-05-29
**Phase:** TOPIC
**Scope:** follow-on to round 202605291419 (Bool try_trait_v2 residual adaptation). Two changes the prior round could not carry: the design-doc accuracy fix, and enabling notko's `try_trait_v2` feature.
**Source:** round 202605291419 (closed), its doc CL note deferring the tmpl fix, the workspace decision to enable notko `try_trait_v2` across consumers.

Round 202605291419 adapted `Bool`'s `Try` to an owned `BoolResidual` and shipped source-only: the pre-commit `cargo check` could not pass on the pinned nightly until that source fix landed, so a DOC-phase `.md.tmpl` edit could not share the round. Now that arvo compiles on `nightly-2026-05-28`, the two deferred pieces land here.

## Decision

1. **Doc accuracy fix.** `arvo-storage/DESIGN.md.tmpl` line 284 still claims `Bool` implements `Try` with `Residual = Infallible`. Source now uses `BoolResidual`. Update the line. The const-path lines (`notko::ConstTry` / `notko::ConstFromResidual<core::convert::Infallible>`) stay: notko's `ConstTry` has an unbounded `type Residual;`.
2. **Enable notko `try_trait_v2` in arvo.** `mock/Cargo.toml` notko dep `features = ["const"]` becomes `["const", "try_trait_v2"]`, so notko's `?`-on-`Maybe`/`Outcome`/`Just` impls compile in arvo's build. This exercises the previously-dormant pathway (the dormancy is what hid the core `Residual`-bound change). notko's `try_trait_v2` cargo feature transitively enables its `try_trait_v2_residual` rustc gate, so notko's fixed `Try` + `Residual` impls compile. Requires pulling notko dev (the merged fix) into the lockfile.

## Consequences

- The DESIGN.md.tmpl edit is a DOC-phase change; the Cargo.toml edit is a SRC-phase change. Both fit one round now that compilation is fixed.
- No source-type changes beyond round 202605291419; this round is doc + build-config only.
