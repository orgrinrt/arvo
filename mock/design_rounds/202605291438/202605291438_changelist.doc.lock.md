**Date:** 2026-05-29
**Phase:** DOC
**Round:** 202605291438 (Bool residual doc fix + notko try_trait_v2 enable)

One `.md.tmpl` edit: correct the stale `Bool` `Try::Residual` claim that round 202605291419 deferred.

## FILE: `mock/crates/arvo-storage/DESIGN.md.tmpl`

Line 284 reads "`Bool` implements `Try` with `Residual = Infallible`". Source (shipped in round 202605291419) now uses the owned uninhabited `BoolResidual`. Update to "`Residual = BoolResidual` (an owned uninhabited type)". `BoolResidual` exists as a `pub` re-exported item (`design-doc-source-mismatch` satisfied). The following "zero runtime cost / Continue path is the only inhabited path" sentence is unchanged. The const-path paragraph (lines 289 to 293) is unchanged: notko's `ConstTry` carries an unbounded `type Residual;`, so the const path keeps `Infallible`.
