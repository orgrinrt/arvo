**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605291438 (Bool residual doc fix + notko try_trait_v2 enable)

Build-config change: enable notko's `try_trait_v2` feature so notko's core `Try` / `Residual` impls for `Maybe` / `Outcome` / `Just` compile in arvo's build (the previously-dormant pathway, whose dormancy hid the core bound change). Pull notko dev (the merged residual fix) into the lockfile.

## CHANGE: notko dep features FROM `["const"]` TO `["const", "try_trait_v2"]`
File: `mock/Cargo.toml`
notko's `try_trait_v2` cargo feature transitively enables its `try_trait_v2_residual` rustc gate, so notko's fixed `Try` + `Residual` impls compile.
Verification: the notko line in `mock/Cargo.toml` carries `features = ["const", "try_trait_v2"]`; `cargo +nightly-2026-05-28 check` builds arvo with notko's try impls active.

## CHANGE: Cargo.lock notko pin advanced to the merged dev fix
File: `mock/Cargo.lock`
`cargo update -p notko` pulls notko dev including the `Residual` adaptation merged on notko dev.
Verification: `cargo +nightly-2026-05-28 check` compiles notko's `Try` / `Residual` impls clean.
