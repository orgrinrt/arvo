**Date:** 2026-05-29
**Phase:** DOC
**Round:** 202605291419 (Bool try_trait_v2 residual adaptation, source side)

Source-only round. No `.md.tmpl` documentation-template edits land here.

The companion design-doc accuracy fix (arvo-storage `DESIGN.md.tmpl` line 284, "`Residual = Infallible`" becomes "`Residual = BoolResidual`") is deferred to a follow-up doc round on the same branch. The reason is an ordering constraint, not a deferral of intent: the pre-commit gate runs `cargo check`, which cannot pass until the `Bool` source fix lands (IMPL phase), but a `.md.tmpl` edit must commit in DOC phase, before the source fix. The two cannot share one round. Once this round's source fix is committed and arvo compiles on the pinned nightly, the follow-up doc round's tmpl commit passes the gate.

The stale line is lint-clean in the meantime: `Infallible` still exists in source (the const-path `notko::ConstFromResidual<core::convert::Infallible>` impl on `Bool`), so the `design-doc-source-mismatch` lint does not fire on the backticked name.

This round's substance is in the src CL: adapt `Bool`'s `core::ops::Try` to an owned uninhabited residual (`BoolResidual`) plus the paired `core::ops::Residual` impl, and enable the `try_trait_v2_residual` feature gate.
