**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605291419 (Bool try_trait_v2 residual adaptation, source side)

Adapt `Bool`'s `core::ops::Try` impl to core's `Try::Residual: Residual<Self::Output>` bound (nightly-2026-05-28). Bare `core::convert::Infallible` is no longer a valid residual for our type (the orphan rule forbids arvo implementing `core`'s `Residual` for the foreign `Infallible`), so `Bool` moves to an owned uninhabited residual. `?` on a `Bool` is unchanged: infallible, `branch` always `Continue`, zero runtime cost. The `notko::ConstTry` / `ConstFromResidual` impls are untouched (notko's `ConstTry` has an unbounded `type Residual;`, so `Infallible` stays valid there).

## CHANGE: add `BoolResidual` to `arvo-storage` platform module
File: `mock/crates/arvo-storage/src/platform.rs`
A new `pub enum BoolResidual {}` (uninhabited), the owned residual for `Bool`'s infallible `Try`. Not numeric, so no `S: Strategy`.
Verification: `grep "pub enum BoolResidual" mock/crates/arvo-storage/src/platform.rs` returns one match.

## CHANGE: `impl Try for Bool` Residual FROM `Infallible` TO `BoolResidual`
File: `mock/crates/arvo-storage/src/platform.rs`
`type Residual = BoolResidual`; `branch(self) -> ControlFlow<BoolResidual, bool>` (body unchanged, always `Continue`); `impl FromResidual<BoolResidual> for Bool` (was `FromResidual<Infallible>`).
Verification: `grep "type Residual = BoolResidual" mock/crates/arvo-storage/src/platform.rs` returns one match; `grep "impl FromResidual<BoolResidual> for Bool" mock/crates/arvo-storage/src/platform.rs` returns one match.

## CHANGE: add `impl Residual<bool> for BoolResidual`
File: `mock/crates/arvo-storage/src/platform.rs`
The paired reverse direction the new core bound requires: `impl Residual<bool> for BoolResidual { type TryType = Bool; }`.
Verification: `grep "impl Residual<bool> for BoolResidual" mock/crates/arvo-storage/src/platform.rs` returns one match.

## CHANGE: enable `try_trait_v2_residual` feature gate
File: `mock/crates/arvo-storage/src/lib.rs`
Add `#![feature(try_trait_v2_residual)]` (unconditional, matching the existing `#![feature(try_trait_v2)]`).
Verification: `grep "feature(try_trait_v2_residual)" mock/crates/arvo-storage/src/lib.rs` returns one match.

## CHANGE: re-export `BoolResidual` at the crate root
File: `mock/crates/arvo-storage/src/lib.rs`
Add `BoolResidual` to `pub use platform::{...}` (platform module is private; `Bool`'s public `Try` impl exposes the residual, so it must be reachable, E0446).
Verification: `grep "BoolResidual" mock/crates/arvo-storage/src/lib.rs` returns one match.
