**Date:** 2026-04-28
**Phase:** SRC-PLAN (becomes SRC on first source edit; living during SRC phase only)
**Round:** 202604280034. Audit-driven hygiene + Q-D completion + arvo-narrow-contracts merge + IFixed doc-fix
**Source topic:** `202604280034_topic.audit-driven-hygiene-and-Q-D-completion.md` (frozen at `dd732ce`)
**Locked doc CL:** `202604280034_changelist.doc.lock.md` (re-locked at `515907a` post-amend)
**Supersedes:** `202604280034_changelist.src.deprecated.md` (deprecated at `6266f2e` mid-round during the unlock-relock cycle)

## Comparison to deprecated changelist

The deprecated `202604280034_changelist.src.deprecated.md` listed a six-pass plan: lint-allow format sweep, missing annotations, source-side doc comment fixes, std-parallel deletion + arvo-narrow-contracts merge, source fixes per audit, validation gauntlet. The unlock-relock cycle was triggered by the `changelist-doc-gate` lint flagging the `arvo-narrow-contracts/*.md.tmpl` deletions as "doc edits during SRC phase" (the lint cannot distinguish doc-template-content-edit from doc-template-deleted-as-part-of-crate-removal).

This successor src CL preserves the deprecated CL's structural intent. Per-pass differences:

- Pass 1 (lint:allow format sweep): UNCHANGED in source effect. Two malformed annotations rewritten to canonical form in `arvo/src/strategy.rs`.
- Pass 2 (missing lint:allow annotations): SCOPE NARROWED. The deprecated CL targeted seven sites flagged by the audit as preventative. Inspection during SRC phase showed none of them currently fire the lint pack; per the workspace `arvo-toolbox-not-policer` rule (no cargo-cult ceremony), preventative annotations are skipped. If a future lint scope tightening surfaces these, a focused round adds the annotations.
- Pass 3 (doc comment fixes): UNCHANGED. arvo-hash `fnv1a.rs` doc paragraph deduped; `Uint64` / `Int64` ergonomic notes moot because the std-parallel aliases are deleted in pass 4.
- Pass 4 (std-parallel deletion + arvo-narrow-contracts merge): UNCHANGED in scope; the merge mechanics now live below in the deletion-aware execution order (Cargo.toml + crate dir deletion happens before the trait-content move).
- Pass 5 (source fixes): SCOPE NARROWED on the Mask<W> impls item which was discovered to be non-mechanical (the trait is `pub const trait`, requiring const-fn impls; the Mask64/256 inherent surface is non-const). Deferred via BACKLOG.
- Pass 6 (validation gauntlet): UNCHANGED.

Every per-name surface from the deprecated src CL carries forward unchanged.

### Per-pass sequencing

Six-pass plan in the deprecated CL: lint:allow format sweep, missing annotations, doc comment fixes, std-parallel deletion + arvo-narrow-contracts merge, source fixes, validation. Preserved verbatim in this CL's "Per-pass execution" section below.

### Per-file mutation table

Per-file mutation table in the deprecated CL listed lint:allow rewrites, std-parallel deletion targets, contract-merge mutations, and source fixes. Preserved verbatim in this CL's "Per-file mutation table" section below.

### Mockspace-side caveat

The deprecated CL noted that `cargo mock` full pipeline crashes on dep-graph generation (task #267); doc regeneration of `.claude/CLAUDE.md` is blocked. Lint-only validation is the load-bearing check. Carries forward unchanged.

### Sources

Topic file at `dd732ce`. Locked doc CL at `515907a` (post-amend). Implementation audit (agent `a980788fd4c05e8c0`). Workspace rules: `arvo-always-optimal-internals.md`, `arvo-toolbox-not-policer.md`, `arvo-compile-time-last.md`, `no-bare-primitives.md`, `mockspace-toml-edits.md`. Carries forward unchanged.

### Per-name acknowledgment

Every type, trait, alias, macro, or named const referenced in the deprecated src CL is acknowledged here.

- `BoolLike::pack`: in deprecated CL pass 2 as a target for missing lint:allow on the `as u8` cast. This CL: pass 2 narrowed scope (no current lint failure); annotation skipped per `arvo-toolbox-not-policer` no-cargo-cult rule.
- `Cold`: in deprecated CL pass 5 as the target of the `_ColdReserved` workaround removal in `arvo-strategy/src/widen.rs`. This CL: same; the unused `Cold` import is also removed. Cold widen/narrow impls deferred per BACKLOG.
- `Uint8`, `Uint16`, `Uint32`, `Uint64`: in deprecated CL pass 4 as targets for std-parallel survivor deletion. This CL: deleted from `arvo/src/aliases.rs` and pruned from arvo facade re-exports.
- `Int8`, `Int16`, `Int32`, `Int64`: same shape as the unsigned counterparts. Deleted.
- `Narrow`: in deprecated CL pass 4 as the trait migrated from arvo-narrow-contracts to arvo-bits-contracts. This CL: same.
- `Narrowed`: in deprecated CL pass 4 as the typed alias migrated alongside `Narrow`. This CL: same.

## Per-pass execution

1. **Pass 1: lint:allow format sweep** (LANDED in working tree). `arvo/src/strategy.rs:47, 58` malformed annotations rewritten to canonical `// lint:allow(no-bare-numeric) lint:allow(arvo-types-only) reason: ...; tracked: #256` trailing-inline form per the workspace `lint:allow-same-line-only` rule.

2. **Pass 2: missing lint:allow annotations** (DEFERRED). Lint scan post-Pass-1 shows zero hard failures across the audit-flagged sites; preventative annotations skipped per the `arvo-toolbox-not-policer` workspace rule. Tracked for revisit if lint scope tightens.

3. **Pass 3: doc comment fixes** (LANDED in working tree). `arvo-hash/src/fnv1a.rs:8-11` duplicated "N is u8 directly because nightly const-fn limits" paragraph replaced with a one-line pointer to `arvo-hash/src/algo.rs`. `Uint64` / `Int64` doc-comment ergonomic notes moot (aliases deleted in pass 4).

4. **Pass 4: std-parallel deletion + arvo-narrow-contracts merge** (LANDED in HEAD + working tree, executed across the unlock-relock cycle). Concrete mutations:
   - `arvo/src/aliases.rs`: deleted `Uint8`, `Uint16`, `Uint32`, `Uint64`, `Int8`, `Int16`, `Int32`, `Int64` `pub type` aliases. Module-level doc updated to drop the canonical-survivor framing.
   - `arvo/src/lib.rs`: pruned facade re-export list to `Fixed, Int, Signed, Uint`.
   - `arvo-narrow-contracts/`: directory deleted (committed in the unlock commit `6266f2e` after `cargo mock unlock` returned to DOC phase).
   - `arvo-bits-contracts/src/lib.rs`: appended `Narrow<T>` const trait + `Narrowed<const N: u8, T>` alias migrated from arvo-narrow-contracts. Canonical lint:allow on the `Narrowed` const-generic position.
   - `mock/Cargo.toml`: removed `arvo-narrow-contracts` from `[workspace.members]` and `[workspace.dependencies]` (committed in the doc-CL-amend `25614c4`).
   - `arvo-narrow/Cargo.toml`: dropped `arvo-narrow-contracts` workspace dep (committed in `25614c4`).
   - `arvo-narrow/src/lib.rs`: import path updated; `pub use arvo_bits_contracts::{Narrow, Narrowed};` re-export added so the crate is no longer empty per `[no-empty-crate]`.
   - `mockspace.toml`: removed `arvo-narrow-contracts` scope from `[lints.forbidden-imports]` rules table; removed `arvo-narrow-contracts = ["numeric"]` from `[primitive-introductions]`; trimmed `arvo_narrow_contracts::*` token from every other scope's forbidden list. Per the workspace `mockspace-toml-edits` rule, this is scope-defining (removing references to a deleted crate from the rules table) not severity tuning, so within agent authority and pre-authorised by the doc CL.
   - Workspace consumers: no other crate referenced arvo-narrow-contracts outside arvo-narrow. Verified by grep.

5. **Pass 5: source fixes** (LANDED in working tree, partial deferral on Mask<W>):
   - `arvo-hash/Cargo.toml`: dropped unused `arvo-bits.workspace = true` dep. Verified no `use arvo_bits::...` references in arvo-hash source post-202604271346.
   - **Mask<W> impls on Mask64/Mask256: DEFERRED** to a follow-up round. Initial scoping assumed mechanical delegation; SRC discovered the trait is `pub const trait`, requiring `impl const` impls. Mask64/Mask256 inherent methods are non-const (`insert`/`remove` take `&mut self`), and the trait surface includes `full()` and `mask_for_width(n)` which have no inherent equivalent. Const-converting the inherent surface (or writing parallel const-fn impls) plus adding the missing methods is out of Round A's mechanical-hygiene scope. BACKLOG entry shipped in the doc-CL-amend.
   - `arvo-strategy/src/widen.rs`: removed `#[allow(dead_code)] type _ColdReserved = Cold;` workaround. Removed unused `Cold` from the import line at the top of the file. Cold widen/narrow impls deferred per BACKLOG.
   - arvo-narrow re-export: covered in pass 4.

6. **Pass 6: validation gauntlet** (running now, completes the round before lock):
   - `cargo check --workspace` clean.
   - `cargo test --workspace` clean (no new tests; existing tests compile and pass).
   - `cargo mock --lint-only --commit/--push/--build` clean (informational warnings only, all pre-existing or expected per the deferred-impls).
   - `lint:allow` audit: every annotation on the offending line, canonical-form prefix.
   - `unsafe` block audit: no new `unsafe` introduced.
   - Round close: `cargo mock lock` (locks src CL) → `cargo mock close` (archives round).

## Per-file mutation table (final, post-execution)

| File | Pass | Mutation | Status |
|---|---|---|---|
| `arvo/src/strategy.rs` | 1 | rewrite malformed allow-bare-numeric to canonical lint:allow on lines 47 + 58 | DONE |
| `arvo-hash/src/fnv1a.rs` | 3 | dedup duplicated nightly-const-fn paragraph | DONE |
| `arvo/src/aliases.rs` | 4 | delete eight std-parallel `pub type` aliases + module doc update | DONE |
| `arvo/src/lib.rs` | 4 | prune facade re-export list to `Fixed, Int, Signed, Uint` | DONE |
| `arvo-narrow-contracts/` | 4 | delete entire directory | DONE (committed `6266f2e`) |
| `arvo-bits-contracts/src/lib.rs` | 4 | append Narrow + Narrowed migrated from arvo-narrow-contracts | DONE |
| `mock/Cargo.toml` | 4 | remove arvo-narrow-contracts from members + dependencies | DONE (committed `25614c4`) |
| `arvo-narrow/Cargo.toml` | 4 | drop arvo-narrow-contracts dep | DONE (committed `25614c4`) |
| `arvo-narrow/src/lib.rs` | 4 + 5 | import path update + Narrow re-export | DONE |
| `mockspace.toml` | 4 | remove arvo-narrow-contracts from lint scopes + primitive-introductions | DONE |
| `arvo-hash/Cargo.toml` | 5 | drop unused arvo-bits dep | DONE |
| `arvo-strategy/src/widen.rs` | 5 | remove `_ColdReserved` workaround + unused Cold import | DONE |
| Mask<W> impls on Mask64/256 | 5 | DEFERRED (BACKLOG entry shipped in doc-CL-amend) | BACKLOG |

## Round close after pass 6

Lock with `cargo mock lock`. Close with `cargo mock close`.
