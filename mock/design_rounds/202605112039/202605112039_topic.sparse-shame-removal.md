**Date:** 2026-05-11
**Phase:** TOPIC
**Scope:** `mock/crates/arvo-sparse/SHAME.md.tmpl`
**Source topics:** post-merge cleanup for round 202605111719 (PR #63).

# Round 202605112039: arvo-sparse SHAME entry removal

Round 202605111719 introduced `CsrBidirectional<ROWS, NNZ, W>` along
with the rest of the `SparseAdjacency` / `BidirectionalSparseAdjacency`
trait family. While the round was in DOC phase, source did not yet
contain the type, so `mock/crates/arvo-sparse/SHAME.md.tmpl` carried
a `## CsrBidirectional` entry to silence the push-gate
`design-doc-source-mismatch` lint until the src CL landed.

The src CL of round 202605111719 landed the type. PR #63 merged at
`a6d8cd7`. The SHAME entry was supposed to be removed inside that
src CL, but the `changelist-doc-gate` lint blocks `*.md.tmpl` edits
during IMPL phase. The intended pattern was already named in the
src CL: defer SHAME removal to a follow-up DOC-phase round. This
round is that follow-up.

## Decision

Remove the `## CsrBidirectional` heading and its body paragraph from
`mock/crates/arvo-sparse/SHAME.md.tmpl`. No other changes.

## Exit conditions for SRC phase

Source-only side is empty. No `*.rs` edits. The doc CL is the entire
round.

- `mock/crates/arvo-sparse/SHAME.md.tmpl` no longer contains a
  `## CsrBidirectional` heading.
- `mockspace --lint-only` clean.
- Round closes with the src CL declaring an empty source delta.

## Out of scope

- Other SHAME entries (none exist on arvo-sparse beyond the one
  above).
- Any source change. The corrective is doc-template-only.
- Renumbering or reformatting of `SHAME.md.tmpl`'s remaining content
  (the intro paragraph stays as-is for future entries).

## Cross-references

- Round 202605111719 (closed; archived under
  `mock/design_rounds/202605111719/`).
- PR #63 (merged at `a6d8cd7`).
- Pattern parallels round 202605112230 (corrective round that
  followed round 202605112200).
- Workspace rules: `cl-claim-sketch-discipline.md`,
  `no-legacy-shims-pre-1.0.md` (SHAME entries are pre-1.0 audit
  trail, not legacy shims).
