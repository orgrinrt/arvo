# Doc changelist: the readme gains the surface the branch added

**Date:** 2026-09-01
**Round:** three agents fixed one lint by three routes, and none of them knew
**Scope:** `README.md`, plus an addendum beside a locked changelist.

## CHANGE: the readme says the industrial conventions can be written directly
File: `README.md`
Verification: the paragraph enumerating what `arvo-format` carries read against the crate's public
surface, and the conventions named there resolving to items the crate exports.

"Where this is right now" enumerates the crate down to the six rounding modes and the overflow policies
and does not mention that MATLAB `fi` and `fimath` can be named over a declared signature. That is what
this branch adds and it is the thing a stranger landing from the repository page would most want to know.
`the-public-surface-is-checked-on-every-pr.md` calls omitting a surface the crate gained a blocking
defect, and it is right to: a reader has no way to discover it.

One sentence in that paragraph, in the register the rest of the file is in.

## CHANGE: an addendum beside 202609010936's locked src changelist
File: `mock/design_rounds/202609010936/202609010936_changelist.src.lock.addendum.md`
Verification: the locked file's bytes unchanged; `git log --all --diff-filter=A --name-only` finding no
commit adding any file under an `arvo-standards` path, on any branch.

That changelist's "fifteen positions" measurement was taken against a crate called `arvo-standards`
which was never committed anywhere and is not on disk. The number is void as written, because nobody can
check it.

**The claim it supports is true and is now measured properly.** The round that landed as pull request
#55 built its probe in a committed crate and counted ten associated constants and fourteen const generic
parameters. The addendum says the figure is void, says where the real one is, and says the headline
survives: no crate outside `arvo-format` could implement the contract.

## CHANGE: nothing else under `mock/` moves
File: none
Verification: `git diff --name-only` over this round naming no other `*.md.tmpl`.

The design template for `arvo-format` moves in this round only through the merge, carrying the trunk's
own already-locked work, and nothing this round writes reaches it.
