# Src changelist: no source changes

One sentence of published prose and the regeneration that carries it. No
declaration, signature, module or behaviour moves.

The question this round refuses to answer, whether `Phase::of` should refuse a
zero denominator at const time, is the one that would produce a source change,
and it is on the agenda rather than here.

## Verification

`git diff` over this round's commits touches no `mock/crates/**/*.rs`.
