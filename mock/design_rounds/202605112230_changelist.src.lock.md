**Date:** 2026-05-11
**Phase:** SRC
**Scope:** arvo-storage tests/from_bridges.rs
**Doc CL:** `202605112230_changelist.doc.lock.md`

# Source changelist: round 202605112230

Test discipline fix. The const-context assertion blocks in `tests/from_bridges.rs` use raw `.0` field access on arvo primitives (`USIZE_FROM_CAP_ONE.0`, `CAP_FROM_USIZE_SEVEN.0.0`, `BOOL_TRUE.0`, etc.). PRINCIPLES.md.tmpl flags this as a smell. Test code should model the discipline; route through `<T as Transparent>::raw(...)` instead.

No source-side behaviour change. Const assertions still hold (same numeric values, same compile-time eval). Runtime tests unchanged (they already used trait dispatch via `.into()`).

## CHANGE blocks

## CHANGE: const-context unwraps route through `Transparent::raw` instead of `.0`
File: `mock/crates/arvo-storage/tests/from_bridges.rs`
Verification: `! grep -E '(USIZE_|CAP_|BOOL_|RAW_).*\.0' mock/crates/arvo-storage/tests/from_bridges.rs` (no const constant followed by `.0` field access).

## CHANGE: import `arvo_transparent::Transparent` for the unwrap path
File: `mock/crates/arvo-storage/tests/from_bridges.rs`
Verification: `grep -q "use arvo_transparent::Transparent" mock/crates/arvo-storage/tests/from_bridges.rs`.

## Lock criteria

1. `cargo test -p arvo-storage --test from_bridges` green (4 runtime tests + all const blocks compile-time-evaluate).
2. `! grep -E '(USIZE_|CAP_|BOOL_|RAW_).*\.0' tests/from_bridges.rs` (no `.0` on arvo primitives).
3. `mockspace --lint-only` clean.

## Out of scope

Same as round 202605112200. This round adds no new types, no new impls, no new functions; the const From bridges already shipped.
