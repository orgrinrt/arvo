# Src changelist: no source changes

The round is four sentences of published prose and the regeneration that carries
them. No declaration, signature, module or behaviour moves, so there is no
`## CHANGE:` block to write.

## Verification

`git diff <base>...HEAD -- 'mock/crates/**/*.rs'` is empty for this round's two
commits.
