**Date:** 2026-05-11
**Phase:** DOC
**Scope:** arvo-storage DESIGN.md.tmpl
**Source topics:** `202605112230_topic.from-bridges-review-followup.md`

# Doc changelist: round 202605112230

Single edit: rewrite the stale Bool/From paragraph in `mock/crates/arvo-storage/DESIGN.md.tmpl` lines 330-332 to acknowledge the round-202605112200 const lift and disambiguate from the substrate's separate `ConstFrom` bridge family.

## File 1: `mock/crates/arvo-storage/DESIGN.md.tmpl`

### Section: existing trailing paragraph of the `## Bool` section (lines 330-332)

**Rewrite.** Old text:

> Stdlib `From<bool> for Bool` and `From<Bool> for bool` impls remain non-const for boundary coverage. The parallel `ConstFrom` impls land in Round 4 (#314) once the `ConstFrom` arvo bridge ships.

New text:

> Stdlib `From<bool> for Bool` and `From<Bool> for bool` are `impl const From` (round 202605112200 lifted them; see `## From bridges` below). The substrate's separate `ConstFrom` bridge trait family (distinct from this stdlib lift, per `arvo-bridge-home-rule`) shipped in Round 4 (#314).

## Lock criteria

1. The paragraph reflects source state.
2. `cargo mock` regenerates docs clean.
3. `mockspace --lint-only` clean.
