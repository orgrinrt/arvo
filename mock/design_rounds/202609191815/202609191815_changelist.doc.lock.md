# Doc changelist: the tenth review's claim audit

**Date:** 2026-09-19
**Round:** the tenth review's findings on `feat/platform-width-types-on-a-wider-slot-range`
**Scope:** `crates/arvo-format/DESIGN.md.tmpl`.
**Topics consumed:** `202609191810_topic.tenth-review-claim-audit.md`, the only flat topic.
**Evidence:** `mock/research/202609191400_core_shadowing_probe/`, whose `run.sh` now also runs a
const-checked std-dependent arm with its own wrong-width control, and a bare-prelude arm isolating
whether the exact `rust_2024` path is needed. Both landed in `TOPIC` phase, per this repo's
discipline on research committed ahead of the doc window.

## Canon

Same governing row as the earlier platform-width changelists
(`a_platform_width_type_is_a_target_bound_member_of_the_format_family`). No row changes.

## `crates/arvo-format/DESIGN.md.tmpl`, the manifest-rename paragraph

The sentence naming what a `no_std` dependent needs of the renamed crate now says it is the exact
`core::prelude::rust_2024` path, not any `prelude` module, and cites the bare-prelude arm's refusal.
The sentence about a std dependent needing none of this now says its own checks are evaluated at check
time the same way the `no_std` dependents' checks are, rather than leaving unstated how the check runs,
since B1 found that sentence resting on a runtime `assert!` inside `fn main` that `cargo check` never
executes.

## `crates/arvo-format/DESIGN.md.tmpl`, the second sweep's ranges

The sentence describing `the_wide_oracle_sweep.rs`'s ranges now lists `[5, 5]`, the `2^64`-slot ranges
at both ends of the index, `[-4, 3]`, `[-100, 99]` and `[1000, 1255]`, which the test's own module doc
and its `ranges()` function already fed and the design prose understated.

## Not actioned

C3, C4 and the lint module doc (B1, C1) are source changes and are claimed in the src changelist. C2
(`MANIFEST_RENAME_IS_UNGUARDED.md:7-9`) and everything else inside the research directory are research,
already committed in `TOPIC` phase.
