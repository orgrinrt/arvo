# tenth-review-claim-audit

review146j on PR #146 at 0ef75b6f found one blocking finding and five
catalogue items, all inside `mock/research/202609191400_core_shadowing_probe/`
and the prose citing its arms. This round fixes them.

B1. The std-dependent manifest-rename arm (`a_std_dependent_of_the_crate_without_the_glob`)
is a binary whose two checks are runtime `assert!`s in `fn main`. `run.sh`
judges it with `cargo check`, which never runs `main`, so the arm certifies
only that the path type-checks, not that it holds the claimed value. The
review's B1 is stated as a class: audit every probe arm that checks a value,
not only this one, and give each such arm a check evaluated at check time
plus a wrong-value sibling wired into `run.sh` and expected refused.

C1. The `rust_2024` component of the empty-prelude path is not isolated: no
arm shows a `prelude` module with no `rust_2024` inside it refused. Add one.

C2. `MANIFEST_RENAME_IS_UNGUARDED.md:7-9` claims something about
`[target.'cfg(..)'.dependencies]` with no arm exercising a target table.

C3. `tests/compile_fail.rs`'s module doc undercounts by one arm.

C4. `tests/ui/a_range_past_the_ladder_is_refused_where_it_is_used.rs:6-7`
narrates an uncommitted mutation run.

C5. `DESIGN.md.tmpl` ~667-669 understates the second sweep's ranges against
what `the_wide_oracle_sweep.rs` now feeds.

No canon row is touched. This is a probe-and-prose fix inside one research
directory and the design/lint docs citing it, scoped exactly to review146j's
findings.
