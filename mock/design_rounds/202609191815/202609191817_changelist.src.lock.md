# Src changelist: the tenth review's claim audit

**Date:** 2026-09-19
**Round:** the tenth review's findings on `feat/platform-width-types-on-a-wider-slot-range`
**Scope:** `mock/lints/the_platform_width_points_read_the_pointer_width.rs`,
`crates/arvo-format/tests/compile_fail.rs`,
`crates/arvo-format/tests/ui/a_range_past_the_ladder_is_refused_where_it_is_used.rs`.

## CHANGE: lint module doc names the exact `rust_2024` path and the std check's own timing
File: `mock/lints/the_platform_width_points_read_the_pointer_width.rs:52-63`
Verification: `grep -n 'a prelude.*module with nothing in it' mock/lints/the_platform_width_points_read_the_pointer_width.rs`
matches the sentence naming the bare-prelude refusal, and `grep -n 'checked at check.*time the same way' mock/lints/the_platform_width_points_read_the_pointer_width.rs`
matches the sentence naming how the std dependent's checks run.

## CHANGE: `tests/compile_fail.rs`'s module doc gains the missing group
File: `crates/arvo-format/tests/compile_fail.rs:13-18`
Verification: the doc now enumerates 1+1+3+2+1+3+1+5 = 17 cases, matching `grep -c '#\[test\]'
crates/arvo-format/tests/compile_fail.rs`, which returns 17. Before this changelist the doc's groups
summed to 16 against 17 tests, missing `a_range_past_the_ladder_is_refused_where_it_is_used`.

## CHANGE: the ladder-refusal ui test drops its uncommitted-mutation narration
File: `crates/arvo-format/tests/ui/a_range_past_the_ladder_is_refused_where_it_is_used.rs:6-8`
Verification: `! grep -n 'Removing that bound left' crates/arvo-format/tests/ui/a_range_past_the_ladder_is_refused_where_it_is_used.rs`.
The `.stderr` is unchanged (`git diff --stat` empty against it after a `TRYBUILD=overwrite` run), since
the file's error text points into `src/slots.rs` and the Rust panic machinery, never into this file's own
line numbers.

## Not actioned in source

Every other finding is research (already committed in `TOPIC` phase) or doc (the doc changelist above).
