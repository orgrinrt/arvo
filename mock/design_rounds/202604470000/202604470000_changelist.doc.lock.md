**Date:** 2026-04-21
**Phase:** DOC
**Scope:** Predicate trait family (#122)
**Source topics:** 202604470000_topic.predicate-trait-family.md

# Doc template edits

## mock/crates/arvo/DESIGN.md.tmpl

Add a short section under the existing numeric-traits list announcing
the predicate family. Lives in the same paragraph that enumerates
`TotalOrd` / `Sqrt` / `Recip` / `Abs` / `FromConstant`. New entry:

- `Pred<A>`, `Pred2<A, B>`, `Pred3<A, B, C>` — named predicate bounds
  for consumer callbacks. Blanket-impl'd for any
  `Fn(&A) -> Bool` / `Fn(&A, &B) -> Bool` / `Fn(&A, &B, &C) -> Bool`
  closure; custom callable types implement `test(&self, ...)`.

## mock/crates/arvo-comb/DESIGN.md.tmpl

Update the DP and greedy grouping sections to name the new bound.
If current prose says "takes an `impl Fn(...) -> Bool` feasibility
predicate", change to "takes an `impl Pred2<...>` feasibility
predicate" (or the single-arity variant) — no meaning change, just
the named bound.

# Validation

- `cargo mock --lint-only` clean after doc edits.
