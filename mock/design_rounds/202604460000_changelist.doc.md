**Date:** 2026-04-21
**Phase:** DOC
**Scope:** FromConstant USize retrofit (#123)
**Source topics:** 202604460000_topic.from-constant-usize.md

# Doc template edits

Doc-template changes are confined to `mock/crates/arvo/DESIGN.md.tmpl`
and `mock/crates/arvo-spectral/DESIGN.md.tmpl`:

- Arvo trait surface table: `from_constant(u8) -> Self` →
  `from_constant(USize) -> Self`. Tracked: #123 note removed.
- arvo-spectral trait bounds `FromConstant` mention stays (bound
  name unchanged). Any "takes `u8`" prose updated to "takes
  `USize`".

# Validation

- `cargo mock --lint-only` clean after doc edits; no new
  writing-style hits.
