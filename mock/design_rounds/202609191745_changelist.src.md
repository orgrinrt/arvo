# Src changelist: the ninth review's catalogue, plus the lint's own no_std scope

**Date:** 2026-09-19
**Round:** the ninth review's findings on `feat/platform-width-types-on-a-wider-slot-range`
**Scope:** `crates/arvo-format/src/**`, `mock/lints/the_platform_width_points_read_the_pointer_width.rs`.

## CHANGE: lint module doc names the `no_std` scope and the std case
File: `mock/lints/the_platform_width_points_read_the_pointer_width.rs:48-60`
Verification: `grep -n 'no_std.*core.*std' mock/lints/the_platform_width_points_read_the_pointer_width.rs`
matches the sentence naming `arvo-format` as `no_std` and the std dependent's own case.

## CHANGE: `Phase::of` rustdoc drops the history narration
File: `crates/arvo-format/src/format/phase.rs:48`
Verification: `! grep -n 'an earlier constructor did' crates/arvo-format/src/format/phase.rs`.

## CHANGE: three more narration hits, found by the branch-wide grep C6 asks for
Files: `crates/arvo-format/src/apply/tests/the_top_of_the_index.rs`,
`crates/arvo-format/tests/ui/width_above_the_bound.rs`,
`crates/arvo-format/src/apply/tests/the_edges.rs`,
`crates/arvo-format/src/tests/the_identity/the_phase_extremes.rs`.
Verification: `git diff 34558620.. -- crates/arvo-format | grep '^+' | grep -Ei
'\bearlier\b|\bused to\b|\bpreviously\b|\bno longer\b'` returns one hit
(`extern_crate_self_as_core_hijacks_the_absolute_path.rs`'s "no longer saves it",
which is a logical contrast within that fixture's own scenario rather than a
reference to a prior version of the code, and is not touched); a second pass for
`\bbefore\b` and `\bwas\b` over the same diff turns up counterfactual assert
strings ("was refused", "was admitted") and ordinary computational
before-in-the-algorithm phrasing, neither of which is history narration, and the
four files above, which are. Five total across the branch, phase.rs counted.

## CHANGE: `width_above_the_bound.stderr` regenerated against the shorter comment
File: `crates/arvo-format/tests/ui/width_above_the_bound.stderr`
Verification: `TRYBUILD=overwrite cargo test -p arvo-format --test compile_fail
a_width_the_slot_range_cannot_carry_is_refused`, then `cargo test -p arvo-format
--test compile_fail` green at 17 passed.

## CHANGE: `the_wide_oracle_sweep.rs` feeds a 200-slot and a 256-slot range
File: `crates/arvo-format/src/apply/tests/the_wide_oracle_sweep.rs:55-68`
Verification: `ranges()` returns `[(i128, i128); 11]`, with `(-100, 99)` and
`(1000, 1255)` appended; `cargo test -p arvo-format the_wide_oracle_sweep` stays
green at 4 passed, so every broken map `the_broken_maps.rs` keeps is still
reported across the widened set. The file's own doc, at the top and on
`wholes()`, is corrected to name exactly what is fed rather than a span neither
range had before this changelist.

## CHANGE: `the_edges.rs`'s dither-below-zero test feeds two extreme remainders
File: `crates/arvo-format/src/apply/tests/the_edges.rs:258-296`
Verification: `Fraction::of(1, i64::MAX)` and `Fraction::of(i64::MAX - 1,
i64::MAX)` are chained onto the quarter-remainder iterator; `cargo test -p
arvo-format a_dither_below_zero` stays green.

## Not actioned in source

C1's control (`user_of_the_crate_with_an_empty_prelude_and_a_wrong_width`) and
the std-dependent arm for B1 are research, committed in TOPIC phase, per this
repo's discipline on research committed ahead of the doc window. C3, C7, C8 and
C9 are not actioned, for the reasons the topic file gives.
