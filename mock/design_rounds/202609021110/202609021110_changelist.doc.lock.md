# DOC changelist: the predicate nobody searches

## CHANGE: none

The round adds a search to the suite and deletes a leftover binding. Neither
changes what the crate is or what it guarantees, so no design template moves.
The design already states the two regions and the four laws; what is missing is
inside the suite rather than inside the description of it.
