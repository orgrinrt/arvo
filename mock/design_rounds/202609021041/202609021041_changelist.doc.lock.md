# Doc changelist: no template changes

**Date:** 2026-09-02
**Round:** the arms that guard a public surface are on it
**Scope:** none.

## CHANGE: nothing FROM the templates TO the templates

File: none
Verification: `git diff --name-only` over this round names no `*.md.tmpl` and no generated document.

The round rewrites comments inside shipped tests. A comment lives in `*.rs`, which is the source tier,
and nothing here reaches a template or renders into `docs/`.

## CHANGE: nothing in the registry

File: none
Verification: `cargo mock` reports 708 rows across 10 namespaces before and after.

Taking a process noun and an account of a prior state out of a comment is not a canon act, for the
same reason it was not one the last two times.
