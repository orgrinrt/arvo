# Source changelist: eight comments, no assertion

**Date:** 2026-09-02
**Round:** the arms that guard a public surface are on it
**Scope:** `mock/crates/arvo-format/src/apply/tests/the_ratio_coordinate.rs`,
`mock/crates/arvo-format/src/apply/tests/mod.rs`

Nothing here changes what any arm asserts, what any arm is called, or what any arm reaches. Every
change is inside a comment or a doc comment, and the suite count is the same before and after.

## CHANGE: the `#[ignore]` reason FROM naming the ceremony that declined it TO naming what is unbuilt

File: `mock/crates/arvo-format/src/apply/tests/the_ratio_coordinate.rs`
Verification: `grep -riwc 'round' mock/crates/arvo-format/src/apply/tests/the_ratio_coordinate.rs` returns zero.

The reason has to carry what the gap is and what would close it, which is what `catalogue` reasons in
this workspace do. It does not have to say which unit of design work decided not to. The string is
printed by the test runner, so it is the most-read comment in the file.

## CHANGE: five comments FROM an account of a prior state TO what the arm guards

File: `mock/crates/arvo-format/src/apply/tests/the_ratio_coordinate.rs`
Verification: `grep -niE 'the previous|used to|by accident|would have failed' mock/crates/arvo-format/src/apply/tests/the_ratio_coordinate.rs` returns nothing.

At lines 31, 32, 178, 240 and 293 as the file stands. In each case the reason the arm exists survives
the rewrite and only the narration goes: the matrix carries both parities because parity is what
decides whether an exact form exists, the tie arm reaches a remainder whose doubling leaves the
coordinate's own carrier, and the cancelling arm names pairs that have an exact form.

## CHANGE: two comments in the module root, which the split moved rather than wrote

File: `mock/crates/arvo-format/src/apply/tests/mod.rs`
Verification: `grep -niE 'earlier tests|the old predicate' mock/crates/arvo-format/src/apply/tests/mod.rs` returns nothing.

The module doc opens by saying these arms replace four earlier ones, and a section heading calls a
sibling section's subject what an old predicate only declared. Both say something true and useful
about what the arms do, which is that each one runs the map and looks at the answer rather than
reading a declaration back. That half stays and the comparison to what is gone does not.

## CHANGE: nothing anywhere else, and the rest of the class is named rather than swept

File: none
Verification: `cargo test --workspace` reports the same counts as before this round: 171 passing, 0 failing, 3 ignored.

The topic names roughly twenty-five further instances across `arvo-format`, `arvo-placement` and
`arvo-strategy`, and one `ratified` inside another `#[ignore]` reason. They are out of this round's
scope and are written down so the next reader inherits them rather than rediscovering them.

## The zeros above were checked against a control

Every verification in this changelist is a grep returning nothing, and a grep that has only ever
returned nothing has not been shown to work.

Both were run against a file that does contain what they look for: the word grep returns 3 on
`mock/crates/arvo-format/tests/matlab_fi_parity.rs`, and the prior-state grep returns 2 on
`mock/crates/arvo-format/src/tests/the_inventory.rs`. Both were then run against a planted line in the
file under test and returned 1 each, with the plant removed afterwards.

So the zeros are a fact about the two files rather than about the pipeline.
