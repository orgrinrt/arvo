# SRC changelist: the two comments

## CHANGE: doc comment on `Fraction::of` FROM the magnitude gloss TO the operand
File: `mock/crates/arvo-format/src/apply.rs:66`
Verification: `grep -c "the magnitude pins to the largest" mock/crates/arvo-format/src/apply.rs` returns zero.

Same defect as the design table row, in the doc comment the table was written
from. The sentence below it about the relative `1 / i64::MAX` bound and the sign
is correct and stays.

## CHANGE: control comment FROM an unreachable case TO the reason it is unreachable
File: `mock/crates/arvo-format/src/apply/tests/the_ratio_coordinate.rs:309`
Verification: `grep -c "whose doubling also leaves the type" mock/crates/arvo-format/src/apply/tests/the_ratio_coordinate.rs` returns zero.

The comment introduces the assertion under it as a tie whose doubling overflows.
No such pair exists, because a tie is `2*num == den` and a numerator that
overflows on doubling forces a denominator past `i64::MAX`. The assertion is a
whole number, which is a real control on the widening and is what the comment
should say. No assertion moves.
