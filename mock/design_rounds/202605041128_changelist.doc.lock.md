**Date:** 2026-05-04
**Phase:** DOC
**Scope:** none (impl-only round; no doc tmpl changes)
**Topic source files:**

- `202605041128_topic_ufixed_ifixed_bounded.md`

# Round 7 doc changelist (arvo side)

This CL covers arvo's doc-template surface for Round 7. The round adds two `impl const Bounded` blanket impls on UFixed and IFixed plus extends two existing const tests with MIN/MAX assertions. No doc-template file is touched. The doc CL exists to formally declare the empty doc surface so the SRC phase opens cleanly.

The Bounded blanket forward is already named as canonical surface in source-side documentation. The Round 6 PR #48 senior review observation (NIT 5) was that the impls were missing, not that the design intent was unclear. Round 7 lands the impls; no doc tmpl edit is needed because the design is already documented at three layers: the SAFETY comment in ufixed.rs:42-45 (and the matching block in ifixed.rs:47-48) explicitly names Bounded among "the canonical const surface (ConstPartialEq / ConstEq / ConstBitEq / ConstOrd / Bounded / Identity)"; the predicate-bundling pattern is documented in Round 5 doc CL Decision 4 (UFixed const surface) and Decision 5 (IFixed const surface); the Bounded trait declaration in arvo-strategy/src/arith.rs:418 already states "Substrate types that wrap these (Bits, UFixed, IFixed, Mask families) gain Bounded via blanket impls keyed on the underlying primitive."

## Doc files touched in this round

| Path | Action |
|------|--------|
| _(none)_ | _(none)_ |

## Rationale: why nothing is touched

The relevant doc surface is already in place across three layers:

1. **Source SAFETY comments (ufixed.rs:42-45, ifixed.rs:47-48)** name Bounded as part of the canonical const surface for UFixed / IFixed. The comment was authored when the Identity blanket landed (Round 1, #310, Decision 5) and the predicate-bundling pattern was established. The source comment was the design statement; the impl gap was a follow-up filed at Round 6 close.

2. **`mock/crates/arvo-strategy/src/arith.rs:418` Bounded trait doc** explicitly states: "Substrate types that wrap these (Bits, UFixed, IFixed, Mask families) gain Bounded via blanket impls keyed on the underlying primitive." This is the substrate-level claim the round realizes.

3. **DESIGN.md.tmpl (mock/crates/arvo/DESIGN.md.tmpl) section on the canonical const-trait family** documents the predicate-bundling pattern through the Ieee + FastFloat / StrictFloat case (lines 1041-1076) and the bitfield case (lines 493-504). UFixed and IFixed inherit the same pattern through Round 1 Decision 5 and Round 5 Decisions 4-5.

The DESIGN.md.tmpl does not currently call out UFixed and IFixed Bounded by name in a dedicated subsection. Adding such a subsection would be a useful organizational improvement but is out of scope for this round per the topic out-of-scope list. If we add it later, it goes through a separate doc-only round.

## Comparison to deprecated changelist

This is the first changelist for round 202605041128. There is no deprecated changelist to compare against.

## Lock criterion

This doc CL is ready to lock when:

1. The empty doc surface is justified by the three-layer documentation cross-reference above (verified by file:line citations).
2. No doc tmpl file is staged for modification in this round.
3. The src CL is opened referencing this doc CL by filename.

## Sketches

None. The topic file already documented the no-sketch criterion: the predicate-bundling pattern is exercised by Identity, ConstPartialEq, ConstEq, ConstBitEq, ConstOrd, ConstDefault on the same UFixed and IFixed types. The Bounded blanket uses the exact same shape; there is no new trait-solver surface.

## Cross-references

- `mock/crates/arvo/src/ufixed.rs:42-45` (SAFETY comment naming Bounded as canonical).
- `mock/crates/arvo/src/ifixed.rs:47-48` (mirror SAFETY comment).
- `mock/crates/arvo-strategy/src/arith.rs:418` (Bounded trait doc claiming blanket impls on UFixed / IFixed).
- `mock/crates/arvo/src/ufixed.rs:60-67` (Identity blanket; pattern reference).
- `mock/crates/arvo/src/ifixed.rs:62-69` (Identity blanket; pattern reference for signed).
- `mock/design_rounds/202605040438/` and `mock/design_rounds/202605040602/` (Round 5 doc CL Decisions 4-5 establishing the const-surface family).
- `.claude/rules/cl-claim-sketch-discipline.md` (no sketches needed).
- `.claude/rules/no-legacy-shims-pre-1.0.md` (clean addition; no deprecation).
- Task #325 (this round closes it).
- PR #48 review (Round 6 NIT 5 disposition).
