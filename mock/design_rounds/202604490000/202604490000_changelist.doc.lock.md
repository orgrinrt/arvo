**Date:** 2026-04-21
**Phase:** DOC
**Scope:** Refresh arvo crate DESIGN / BACKLOG templates against current source state (#124)
**Source topics:** 202604490000_topic.arvo-docs-refresh.md

# Doc template edits

Per-file edit list. All edits are in `mock/crates/*/{DESIGN,BACKLOG}.md.tmpl`.
No `.rs` edits.

## `mock/crates/arvo/DESIGN.md.tmpl`

Expand the `FromConstant` bullet under "Composable traits" to show
the current signature so the #123 retrofit (`u8 -> USize` input) is
visible from the design doc.

## `mock/crates/arvo-bits/DESIGN.md.tmpl`

Fix the `bitfield!` example to match the shipped macro grammar. The
shipped grammar is `$field: $field_bits at $lo:literal` (single
low-bit index). The current example shows `reserved: 3 at 28..=30`
and `id: 28 at 0..=27` range syntax that is not implemented. The
module-level docs in `bitfield.rs` already note that range syntax
is "a future refinement"; the DESIGN example is the last place
still showing the unimplemented form.

## `mock/crates/arvo-bits/BACKLOG.md.tmpl`

Remove the shipped item:

- "BitSequence impl on the Bits type. Lands if a consumer surfaces
  a meaningful iterate-bits-as-a-stream pattern over opaque
  hashes. Current design ships BitWidth + BitAccess only."

Shipped in `bits.rs` line 191 (`impl<const N: u8, S: Strategy>
BitSequence for Bits<N, S>`).

## `mock/crates/arvo-bitmask/BACKLOG.md.tmpl`

Remove the shipped item:

- "cap_size helper promotion to arvo" (entire section).

`cap_size` was promoted to arvo-tensor in round 202604280000;
arvo-bitmask now re-exports via `pub use arvo_tensor::cap_size`.
The promotion question is settled.

## `mock/crates/arvo-graph/BACKLOG.md.tmpl`

Remove the shipped item:

- "cap_size helper promotion to arvo L0" (entire section).

Same as arvo-bitmask — cap_size lives in arvo-tensor; arvo-graph
reaches it via `arvo_bitmask::cap_size` re-export.

## `mock/crates/arvo-graph/DESIGN.md.tmpl`

Update the sentence under "Capacity" that says `cap_size` is
"re-exported from arvo-bitmask" to note the canonical home
(arvo-tensor), with arvo-bitmask re-exporting.

## `mock/crates/arvo-sparse/BACKLOG.md.tmpl`

Remove the shipped item:

- "cap_size helper promotion to arvo L0" (entire section).

## `mock/crates/arvo-sparse/DESIGN.md.tmpl`

Update the sentence under "Capacity" that says `cap_size` is
"re-exported from arvo-bitmask" to note the canonical home
(arvo-tensor), with arvo-bitmask re-exporting.

## `mock/crates/arvo-comb/BACKLOG.md.tmpl`

Remove the shipped item:

- "cap_size helper promotion to arvo L0" (entire section).

## `mock/crates/arvo-spectral/BACKLOG.md.tmpl`

Remove the obsolete item:

- "Matrix-in-arvo-sparse promotion" (entire section).

Matrix was promoted to arvo-tensor (not arvo-sparse) in round
202604280000. The DESIGN already re-exports from arvo-tensor;
BACKLOG note is stale.

## Files untouched

- `mock/crates/arvo-tensor/{DESIGN,BACKLOG}.md.tmpl` — in sync.
- `mock/crates/arvo-comb/DESIGN.md.tmpl` — `impl Pred2<...>` usage
  already matches source (dp.rs, greedy.rs).
- `mock/crates/arvo-hash/{DESIGN,BACKLOG}.md.tmpl` — in sync.

# Validation

- `cargo mock --lint-only` clean.
- Each removed BACKLOG item has been verified against the
  corresponding `src/*.rs` to ensure it ships 1:1.
