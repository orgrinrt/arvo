**Date:** 2026-04-21
**Phase:** TOPIC
**Scope:** Refresh arvo crate DESIGN / BACKLOG templates against current source state (#124)

# Current state

Recent impl rounds have shipped several changes that the per-crate
`DESIGN.md.tmpl` / `BACKLOG.md.tmpl` templates do not yet reflect:

- #122 — `arvo::Predicate` trait family (`Pred` / `Pred2` / `Pred3`)
  shipped with blanket `Fn(...) -> Bool` bridge; arvo-comb dp.rs and
  greedy.rs use `impl Pred2<...>` in their public signatures.
- #123 — `FromConstant::from_constant` input changed from `u8` to
  `USize`; float impls bridge via `FromU8Ieee` internally.
- #127 — `bitfield!` macro shipped in arvo-bits. The current grammar
  uses `at $lo:literal` (single low-bit index). DESIGN example shows
  `lo..=hi` range syntax that is not yet implemented.
- The Matrix and cap_size primitives were promoted to arvo-tensor
  in round 202604280000 as the canonical home. Several BACKLOG
  entries still describe them as pending promotion.
- arvo-bits `BitSequence` impl on the `Bits` type is shipped; the
  BACKLOG entry proposing that addition is now obsolete.

# Drift enumerated

1. `arvo/DESIGN.md.tmpl`: `FromConstant` listed by name only; no
   signature shown. Worth noting the `USize` input (#123).
2. `arvo-bits/DESIGN.md.tmpl`: `bitfield!` example uses `at 28..=30`
   range syntax not implemented in the shipped macro.
3. `arvo-bits/BACKLOG.md.tmpl`: "BitSequence impl on the Bits type"
   — shipped (`bits.rs` line 191).
4. `arvo-graph/BACKLOG.md.tmpl`, `arvo-sparse/BACKLOG.md.tmpl`,
   `arvo-comb/BACKLOG.md.tmpl`, `arvo-bitmask/BACKLOG.md.tmpl`:
   "cap_size helper promotion to arvo L0" — the helper has already
   been promoted (to arvo-tensor, the canonical home, with
   arvo-bitmask re-exporting). The promotion question is settled.
5. `arvo-spectral/BACKLOG.md.tmpl`: "Matrix-in-arvo-sparse
   promotion" — Matrix was promoted to arvo-tensor, not arvo-sparse.
6. `arvo-graph/DESIGN.md.tmpl`, `arvo-sparse/DESIGN.md.tmpl`:
   both say `cap_size` is "re-exported from arvo-bitmask". Still
   true transitively, but the canonical home is arvo-tensor.

# Non-goals

- No `.rs` edits. Pure DOC-phase round.
- No prose rewrites, no restructuring. Only fix actual drift.
- No new BACKLOG items. Only remove items that fully shipped.

# Validation

- `cargo mock --lint-only` clean (design-doc-source-mismatch lint
  does not regress — every added type / trait name corresponds to a
  source item; every removed item was shipped).
