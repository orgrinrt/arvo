# Src changelist: the eleventh review's claim audit

**Date:** 2026-09-19
**Round:** the eleventh review's findings on `feat/platform-width-types-on-a-wider-slot-range`
**Scope:** `mock/lints/the_platform_width_points_read_the_pointer_width.rs`, and `docs/FORMAT.md` regenerated.

## CHANGE: the lint's module doc names the `use` shapes the probe shows the leading `::` resisting
File: `mock/lints/the_platform_width_points_read_the_pointer_width.rs:38-48`
Verification: `grep -n 'an unaliased .use a::core;. and a .use' mock/lints/the_platform_width_points_read_the_pointer_width.rs`
matches, and `run.out` in the probe shows `the_leading_double_colon_resists_an_unaliased_use_of_core.rs`
and `the_leading_double_colon_resists_a_self_under_core.rs` building, each with two generated controls
refused.

## CHANGE: the lint's module doc drops the contrast with a runtime assertion
File: `mock/lints/the_platform_width_points_read_the_pointer_width.rs:63-64`
Verification: `! grep -n 'runtime assertion nothing in this fixture' mock/lints/the_platform_width_points_read_the_pointer_width.rs`.

## CHANGE: `docs/FORMAT.md` regenerated
File: `docs/FORMAT.md`
Verification: `cargo mock` regenerates it from the locked doc changelist's template; the leakage greps over
it are in the round's report.
