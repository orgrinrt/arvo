**Date:** 2026-05-11
**Phase:** TOPIC
**Scope:** arvo-storage DESIGN.md.tmpl + tests/from_bridges.rs
**Source topics:** pr-reviewer-senior findings on round 202605112200 (PR #62).

# Round 202605112230: From bridges review follow-up

The pr-reviewer-senior pass on PR #62 (round 202605112200's `Cap` <-> `USize` const From bridges) returned two findings:

1. **Doc drift (RECOMMEND).** `mock/crates/arvo-storage/DESIGN.md.tmpl` lines 330-332 still say "Stdlib `From<bool> for Bool` and `From<Bool> for bool` impls remain non-const for boundary coverage. The parallel `ConstFrom` impls land in Round 4 (#314)". After round 202605112200 those `From` impls ARE const. The note conflates two distinct things (stdlib `From` const-lift vs the substrate's separate `ConstFrom` bridge trait family per `arvo-bridge-home-rule`), and reads as a false claim about source state. Per `cl-claim-sketch-discipline.md`, doc claims must match source.

2. **Test discipline (NIT).** `mock/crates/arvo-storage/tests/from_bridges.rs` uses raw `.0` field access (`USIZE_FROM_CAP_ONE.0 == 1`, `CAP_FROM_USIZE_SEVEN.0.0 == 7`, `BOOL_TRUE.0`, etc.) in const-context assertions. PRINCIPLES.md.tmpl lines 78-106 flag `.0` access on arvo primitives as a smell; the workspace lint forbidding it is the next round. Test code that models the discipline is the right shape.

This round addresses both. Scope is narrow: one doc-template edit + one test-file rewrite. No new types, no new impls, no behaviour change.

## Decision

- Rewrite the stale Bool/From paragraph in DESIGN.md.tmpl to acknowledge the const lift and disambiguate from the substrate's `ConstFrom` bridge.
- Route test-file unwraps through `<T as Transparent>::raw(...)` for arvo newtypes; the const-context assertions exercise the From trait method, not field access.

## Exit conditions for SRC phase

- DESIGN.md.tmpl paragraph updated to match source state.
- tests/from_bridges.rs uses `Transparent::raw` for newtype unwrap; const assertions still pass at compile time; runtime tests still pass.
- `cargo test -p arvo-storage --test from_bridges` green.
- `mockspace --lint-only` clean.

## Out of scope

Same out-of-scope set as round 202605112200 (usize edges, `.0` lint, pub-field removal, consumer call-site sweep). This corrective round adds nothing new.

## Cross-references

- PR #62 (round 202605112200).
- pr-reviewer-senior report inline in agent session 2026-05-11.
- Workspace rules: `cl-claim-sketch-discipline.md`, `arvo-bridge-home-rule.md`, PRINCIPLES.md.tmpl `No .0 field-access` section.
