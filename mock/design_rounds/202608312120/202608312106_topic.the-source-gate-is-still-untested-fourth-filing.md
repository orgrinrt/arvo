# Topic: the source gate is still untested, fourth filing

**Date:** 2026-09-01
**Phase:** TOPIC
**Status:** re-filed, unchanged, owned elsewhere

Fourth filing, same subject, same mechanism: `cargo mock close` sweeps every loose flat file into the
closing round's archive whatever round it belonged to, so a filing left in the way is taken out of it
by the next close. Filings two and three both predicted this in writing and both were then swept.

The content is at
`mock/design_rounds/202608312030/202608312008_topic.the-source-gate-is-still-untested.md` and its
predecessors, and the substance has not moved: five lints gate every line under `mock/crates/` and
none has a test; `no_std_enforcer` misses every qualified path expression and `no_alloc_enforcer`
matches against the untrimmed line; the other three are unexamined, which is not the same as sound.
Two mockspace lints are separately defective, `undocumented-type` unsatisfiable for a name with no
lowercase letter and `no-std` reading only the first thirty lines of a crate root.

**Owned elsewhere**, and named here only so the next round inherits the subject rather than having to
find it inside a closed archive nothing points at. Four filings is enough evidence that the sweep is
the thing to fix, and that fix belongs to mockspace.
