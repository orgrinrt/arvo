**Date:** 2026-04-28
**Phase:** DOC (becomes locked at end of doc execution; living during DOC phase only)
**Round:** 202604280034. Audit-driven hygiene + Q-D completion + arvo-narrow-contracts merge + IFixed doc-fix
**Source topic:** `202604280034_topic.audit-driven-hygiene-and-Q-D-completion.md` (frozen at `dd732ce`)

This doc CL operationalises the topic decisions into doc-template mutations. Round A is mostly source-side; the doc-side changes are limited to (1) MAIN.md.tmpl Identity dedupe, (2) IFixed deferred-pass-4d note in arvo's DESIGN.md.tmpl, (3) BACKLOG entries for items deferred to Rounds B-E, (4) DESIGN doc updates reflecting the arvo-narrow-contracts merge, (5) DESIGN doc updates reflecting the std-parallel alias deletion, (6) doc comment fixes in source files (ergonomic notes on `Uint64`/`Int64`, dedup of arvo-hash docstring), and (7) the malformed `allow-bare-numeric:` reference in arvo's DESIGN.md.tmpl line 104.

## Per-file mutations

### `mock/agent/MAIN.md.tmpl`

**Goal:** Identity section reflects the post-202604271346 16-crate (post-Round-A 15-crate) layout. Remove duplicated old-style listings.

Current state: lines 18-46 contain the new layered listing (L0a / L0 / L0.5 / L1) which is correct in shape but uses the L1 implementation crates layer for arvo-bitmask + arvo-tensor + arvo-narrow which actually live at L2 per the architecture rules below. Lines 50-67 contain the older "L2 — substrate concretes" + "L3 — spectral methods" listing, which the rendered CLAUDE.md actually shows (overrides the newer block due to template-render order or section structure). Both blocks need consolidation.

**Mutation:** Replace the entire Identity-section body with a single, accurate, layered listing of all 15 (post-merge) crates:

- L0 foundation: `arvo-transparent`, `arvo-strategy`, `arvo-storage`.
- L0.5 trait contracts: `arvo-bits-contracts` (post-merge holds `Narrow` too), `arvo-mask-contracts`, `arvo-numeric-contracts`. (`arvo-narrow-contracts` removed per Round A merge.)
- L1: `arvo-bits` (storage aliases + blanket impls of bit-level traits).
- L2 substrate concretes: `arvo-bitmask`, `arvo-tensor`, `arvo-narrow` (concrete Narrow impls), `arvo-hash`.
- L2 generic algorithms: `arvo-graph`, `arvo-sparse`, `arvo-comb`.
- L3: `arvo-spectral`.
- Facade: `arvo` (re-exports + `UFixed` / `IFixed` newtypes + aliases).

Remove the duplicated lines 50-67 block. Architecture rules below the Identity section stay as-is (already reference all crates correctly).

### `mock/crates/arvo/DESIGN.md.tmpl`

**Goal:** Reflect that std-parallel aliases are gone, IFixed is deferred-pass-4d, and the malformed `allow-bare-numeric:` reference is corrected.

Mutations:

- **Aliases section**: Delete the listing of `Uint8`/`Uint16`/`Uint32`/`Uint64`/`Int8`/`Int16`/`Int32`/`Int64` as canonical-width survivors. Replace with a paragraph naming the generic `Uint<N, S>` and `Int<N, S>` forms as the only public alias surface, with the framing that "exact-width thinking is the substrate's central premise; std-parallel aliases were retained briefly during round 202604271346 pass 7 as a transitional bridge and removed in round 202604280034 to lock the discipline in".
- **IFixed section**: Add a "Deferred: pass 4d" note at the head. State plainly that IFixed is currently a `repr(transparent)` newtype over `<S as IContainerFor<{ifixed_bits(I, F)}>>::T` directly (with `PhantomData<fn() -> S>` companion), not over `Bits` like `UFixed`. Round B (#261) reshapes IFixed to wrap `Bits<{1+I+F}, S, Signed>` once the Sign axis on `Bits` is introduced. The current shape is correct but does not match the symmetric "`UFixed` and `IFixed` both wrap `Bits`" framing the doc previously implied.
- **Line 104** (currently: `Each carries an `allow-bare-numeric: tracked:` ...): Rewrite to canonical form `Each carries a \`lint:allow(no-bare-numeric) lint:allow(arvo-types-only) reason: ...; tracked: #N\` annotation per the workspace lint:allow-same-line-only rule`.

### `mock/crates/arvo/BACKLOG.md.tmpl`

**Goal:** Capture the Round A-deferred items so the audit findings live somewhere committed-and-visible.

Add or update entries:

- "IFixed reshape over Bits with Sign axis (Round B / #261)": pass 4d completion. Reshape IFixed to `repr(transparent)` newtype over `Bits<{1+I+F}, S, Signed>` via the new `Sign` const-generic on Bits.
- "std-parallel alias deletion completed Round 202604280034": note the Q-D Round 7 completion. Cross-reference the deleted alias list and the `Uint<N, S>` / `Int<N, S>` generic-only public surface.

### `mock/crates/arvo-storage/DESIGN.md.tmpl`

**Goal:** No structural change; cross-reference the upcoming Sign axis (Round B) so any reader knows the current `Bits<N, S>` is going to gain a Sign parameter.

Add a "Coming in Round B" note at the bottom: Bits will gain `Sign: Signedness` const-generic (Round B / #261); current `Bits<N, S>` continues to be `Bits<N, S, Sign = Unsigned>` post-reshape with no consumer-visible change.

### `mock/crates/arvo-bits-contracts/DESIGN.md.tmpl`

**Goal:** Reflect the merge of `arvo-narrow-contracts` into this crate.

Add a section:

- `## Narrow<T>`: trait declaration moved here from arvo-narrow-contracts during round 202604280034. Body identical to the pre-merge declaration. Cross-reference `arvo-narrow` for concrete impls (still ship-pending per Round D / #263).

Move the existing `Narrowed<const N: u8, T>` typed alias declaration into this section.

### `mock/crates/arvo-narrow-contracts/`

**Goal:** Crate is being deleted in source. Doc templates archive into the round artefact directory naturally on `cargo mock close`. No template mutation needed because the `cargo mock close` step moves the entire crate's templates wholesale.

Wait: that is wrong. `cargo mock close` archives `design_rounds/<id>/` files only, not crate dirs. The crate's `mock/crates/arvo-narrow-contracts/*.md.tmpl` files need explicit deletion alongside the source removal. Mark this as a SRC-CL action; doc CL has nothing to add for the deleted crate.

### `mock/crates/arvo-narrow/DESIGN.md.tmpl`

**Goal:** Reflect the post-merge import path.

Mutation: any "concrete `Narrow<T>` impls (trait declared in arvo-narrow-contracts)" wording becomes "concrete `Narrow<T>` impls (trait declared in arvo-bits-contracts)".

### `mock/crates/arvo-bitmask/DESIGN.md.tmpl`

**Goal:** Reflect that `Mask64` and `Mask256` now implement `Mask<const W: u8>` (per the source change).

Mutation: in the `Mask64` and `Mask256` sections, add an "implements `Mask<64>`" / "implements `Mask<256>`" backtick-bullet so the design-doc-source-mismatch lint is satisfied. Brief one-line statement.

### `mock/crates/arvo-hash/DESIGN.md.tmpl`

**Goal:** Reflect that the `arvo-bits` Cargo.toml dep is dropped, plus dedupe-pointer into the `Hasher` trait declaration's docstring.

Mutation: dependency listing trims `arvo-bits` from arvo-hash deps. Hasher / Fnv1a doc references stay; no design content change.

### Per-source-file doc comment fixes (these are doc-tier fixes happening during DOC phase per the changelist-doc-gate rule)

These are inline doc comments inside `.rs` files. Per the workspace rule, doc comments fall under the writing-style and primitive-vocabulary disciplines but their EDIT is gated by the SRC phase, not DOC phase, because the file extension is `.rs` not `.md.tmpl`. The mockspace gate routes accordingly. **Move these to the SRC CL.** Listed here only for cross-reference:

- `arvo/src/aliases.rs` `Uint64<S>` and `Int64<S>` doc comments: add Warm-not-default ergonomic note.
- `arvo-hash/src/fnv1a.rs:8-10` and `arvo-hash/src/algo.rs:18-21` doc comment dedup: keep `algo.rs` version, point `fnv1a.rs` at it.

## Doc-CL section ordering and tracking

This doc CL is small. Doc-side mutations limited to seven `.md.tmpl` files; all other Round A work (lint:allow sweeps, missing annotations, source deletions, source merges, source-side doc comments) routes through the SRC CL.

## Validation

After doc execution:

- `cargo mock` regenerates `.claude/CLAUDE.md` from the updated MAIN.md.tmpl. Inspect the rendered CLAUDE.md to confirm it shows the 15-crate layout post-merge.
- All edited `.md.tmpl` files free of em-dashes, leading flat bullets, or hype words per the workspace writing-style rule.

## Sources

- Topic file: `202604280034_topic.audit-driven-hygiene-and-Q-D-completion.md` (frozen at `dd732ce`).
- Audit reports as cited in topic file.
- Workspace rules `arvo-always-optimal-internals.md`, `arvo-toolbox-not-policer.md`, `arvo-compile-time-last.md`.

This doc CL is living during DOC phase only. Lock with `cargo mock lock` before transitioning to SRC.
