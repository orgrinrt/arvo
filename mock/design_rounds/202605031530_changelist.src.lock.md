# Src CL: post-#316 drift cleanup

**Date:** 2026-05-03
**Phase:** SRC (open).
**Round:** 202605031530
**Topic:** `202605031530_topic_post_316_drift_cleanup.md`.
**Doc CL:** `202605031530_changelist.doc.lock.md`.

## Scope

Mechanical sweep of stale references to deleted types in `.rs` module
docs and inline comments, plus removal of three em-dashes that landed
against the workspace-wide hard ban in `writing-style.md`.

No type definitions change. No trait surface changes. No behaviour
changes. Comment + module-doc text only.

## CHANGE: stale `UContainerFor` / `IContainerFor` / `MultiContainer` / `AlignedWideBits16` references in module docs and inline comments REMOVED

**Files (each touched in module-doc or comment text only):**
- `mock/crates/arvo-strategy/src/lib.rs`: module doc + `Signedness` doc cite deleted U/IContainerFor traits and incorrectly state Warm doesn't cover `33..=64` (it's `65..=128` that has no native Warm bucket).
- `mock/crates/arvo-storage/src/bits.rs`: module doc cites `<S as UContainerFor<N>>::T` / `<S as IContainerFor<N>>::T`; SAFETY notice mentions `MultiContainer<HiT, LoT>`.
- `mock/crates/arvo-strategy/src/widebits.rs`: module doc still narrates against the deleted `MultiContainer` and references `AlignedWideBits16` for the SIMD baseline.
- `mock/crates/arvo-storage/src/layout_assertions.rs`: module doc references `MultiContainer<HiT, LoT>` `repr(C)` invariant; section comment names `MultiContainer` replacement narrative.
- `mock/crates/arvo-storage/src/meta_bits.rs:45`: comment names `Hot: UContainerFor<{ ufixed_bits(I, F) }>`. Update to `Hot: BitsContainerFor<{ ufixed_bits(I, F) }, Unsigned>`.
- `mock/crates/arvo-strategy/src/arith.rs:28`: comment "Keyed on the same `N` that `UContainerFor` uses". Update.

**Edit shape:** rewrite each module-doc / comment paragraph to describe the actual surface (`BitsContainerFor<N, Sign>` + `Project` + `WideBits<BYTES, A>`). Where the prose names the SIMD-aligned baseline as `AlignedWideBits16`, replace with `WideBits<BYTES, A16>`. Where it cites `MultiContainer<HiT, LoT>` as a possible inner shape, replace with `WideBits<BYTES, A>`. The Warm coverage statement in `arvo-strategy/src/lib.rs` is corrected: Warm covers `1..=64` natively (2x-logical ladder), and Warm at `65..=128` has no native bucket.

**Verification:**
- `! grep -rn "UContainerFor\|IContainerFor\|MultiContainer\|AlignedWideBits16" mock/crates/*/src/*.rs | grep -v "// .*deleted\|// .*was \|// .*previous\|// .*history" | grep -v "\.deprecated"` returns zero matches outside historical-context comments.
- For the more conservative narrow grep: `! grep -n "UContainerFor\|IContainerFor" mock/crates/arvo-strategy/src/lib.rs mock/crates/arvo-storage/src/bits.rs mock/crates/arvo-strategy/src/arith.rs mock/crates/arvo-storage/src/meta_bits.rs` returns zero matches in active comments (history-mentions retained where they record the deletion).

## CHANGE: three em-dashes in `arvo-strategy/src/container.rs` REMOVED

**File:** `mock/crates/arvo-strategy/src/container.rs:178, 193, 205`.

**Edit shape:** replace the three em-dashes (`—`) inside section-header comments with periods (or restructure the sentence). Per `writing-style.md`, em-dashes are a hard zero across all written content; the three landed against the ban during the cascade and the workspace-style gate did not catch them.

**Verification:** `! grep -P "—" mock/crates/arvo-strategy/src/container.rs` returns zero matches.

## Out of scope (Round 4 / #314)

The senior reviewer's findings about per-N `impl_fnv1a!` / `impl_xxhash3!` macros + per-N `hash_const` inherents remain Round 4 scope. They land via #314's broader ConstHash trait lift + bounded-generic narrowing rewrite. `mock/crates/arvo-hash/src/{fnv1a,xxhash3}.rs` are not touched in this drift round; they will be substantially rewritten in #314 anyway.

## Lock criteria

This src CL locks when:

- Both verification greps return the expected results.
- `cargo check` is clean across the workspace.
- `cargo mock --lint-only --commit` is clean.
