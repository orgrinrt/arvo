# doc changelist

**Date:** 2026-09-03
**Phase:** DOC
**Scope:** `arvo-format`
**Source topics:** `202609031451_topic.the-counts-the-consolidation-left-stale.md`

## Current state

The design is correct as it stands and this round changes none of it. The three
findings the topic names are all in source: two comments carrying a count that
disagrees with what is around them, and three fixtures the compiler reports as
never constructed. The design's own statement of the routes, at
`crates/arvo-format/DESIGN.md.tmpl`, is what the arm comment is being corrected
against, so it is the oracle here rather than the thing being edited.

## Changes proposed

None to any `*.md.tmpl`. This round exists so the source fixes are covered by a
changelist, which is what the gate requires and what makes the fixes readable a
year out.

The one thing worth stating for the next round, because it is a claim the design
carries and nothing checks: the design says three routes reach or defeat the
obligation, and the count of routes is a number a later design change can move
without anything reporting that the tests' comments went stale. The fix landing
in the source changelist restores agreement today. It does not build an
instrument, and the honest position is that the next reader of these comments is
the instrument.

## Why there is a deprecated src changelist beside this one

The first source changelist was locked before its source landed, which puts the
round in CLOSED with staged edits the gate then refuses, correctly. The order the
phases want is edit, commit, lock, and the deprecated file is the record of
getting it the wrong way round. Its content is carried forward unchanged; nothing
about the fixes moved.

## Topics consumed

`202609031451_topic.the-counts-the-consolidation-left-stale.md` is the only loose
topic on this branch and it is fully covered: its first and second findings are
source comment corrections and its third is an attribute, all three carried into
the source changelist. Its closing section records the correction to the previous
round's locked source changelist, which is not a change to anything and is
recorded rather than acted on.
