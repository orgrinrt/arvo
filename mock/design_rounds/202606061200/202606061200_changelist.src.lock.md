**Date:** 2026-06-06
**Phase:** SRC
**Round:** 202606061200 bitmask-contract-trait-reexport

# Src changelist

## FILE: mock/crates/arvo-bitmask/src/lib.rs

### CHANGE: re-export the three bit-contract traits from the crate root

Add `pub use arvo_bits_contracts::{BitAccess, BitLogic, BitSequence};` to the
crate-root re-export block, beside the existing chassis re-exports
(`Mask`, `BitMatrix`, `cap_size`, `NodeId`, `SetBitsIter`).

- Claim: after this change, `arvo_bitmask::BitSequence`,
  `arvo_bitmask::BitAccess`, and `arvo_bitmask::BitLogic` resolve to the
  arvo-bits-contracts trait declarations.
- Claim: no behaviour change. arvo-bitmask already depends on
  arvo-bits-contracts and imports all three traits in `matrix.rs` and
  `mask.rs`; this only widens the crate root's public re-exports.
- Claim: the `W` bound on `BitMatrix<W, C>` (`BitSequence + BitAccess +
  BitLogic + Copy + Default`) becomes spellable by a consumer that depends
  only on arvo-bitmask (plus arvo for `Bits` and `Copy`/`Default`), without a
  direct arvo-bits-contracts dependency.

Verification: `cargo build -p arvo-bitmask` clean; the workspace builds and
arvo-bitmask tests pass (re-export is additive, no signature change).
