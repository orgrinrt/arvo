# Doc changelist: the const generic sentence names the refusal that holds it

**Date:** 2026-09-01
**Round:** the trunk's refusal and this branch's three are one file
**Scope:** `mock/crates/arvo-format/DESIGN.md.tmpl`

The design states that the coordinate types do not touch a const generic parameter and leaves the
statement resting on nothing. A refusal pinning it exists, on the trunk, and the design does not know
about it.

## CHANGE: the const generic paragraph FROM stating the exception TO naming what refuses it
File: `mock/crates/arvo-format/DESIGN.md.tmpl`
Verification: `grep -c 'an_arvo_type_as_a_const_parameter' mock/crates/arvo-format/DESIGN.md.tmpl`
returns 1.

The paragraph gains the sentence that the position is refused by the compiler rather than by
convention, and names the arm that holds it. A design sentence with no refusal behind it is a claim
about the current source, which is the tier that gets nuked; naming the arm makes it a claim about
something that fails loudly when it stops being true.
