**Date:** 2026-05-03
**Phase:** SRC-PLAN → SRC
**Scope:** Round 4 PR review fixes (senior PR #43 findings)
**Doc CL:** `mock/design_rounds/202605031658_changelist.doc.lock.md` (locked)

# Src CL — round 202605031658 PR review fixes

## Pass 1: arvo-hash test crate updates

## CHANGE: `tests/algo.rs` REPLACES `HasherExt::hash(...)` calls with streaming `update`/`finalize`
File: `mock/crates/arvo-hash/tests/algo.rs`
Verification: `! grep -q "HasherExt" mock/crates/arvo-hash/tests/algo.rs && ! grep -q "\.hash(" mock/crates/arvo-hash/tests/algo.rs`

## CHANGE: `tests/fnv1a.rs` REPLACES per-N inherent `Fnv1a::<N>::hash_const(...)` with `<Fnv1a<N> as ConstHash<N, Hot, Unsigned>>::hash_const(...)`
File: `mock/crates/arvo-hash/tests/fnv1a.rs`
Verification: `! grep -q "Fnv1a::<.*>::hash_const" mock/crates/arvo-hash/tests/fnv1a.rs && grep -q "ConstHash" mock/crates/arvo-hash/tests/fnv1a.rs`

## Pass 2: XxHash3 streaming truncation diagnostic

## CHANGE: `XxHash3<N>::update` ADDS `debug_assert!` for buffer overflow
File: `mock/crates/arvo-hash/src/xxhash3.rs`
Verification: `grep -n "debug_assert" mock/crates/arvo-hash/src/xxhash3.rs` returns one match in `update` body.

## Pass 3: writing-style fix in narrow_from.rs

## CHANGE: em-dash REMOVED from `arvo-bits-contracts/src/narrow_from.rs:1` module-level doc
File: `mock/crates/arvo-bits-contracts/src/narrow_from.rs`
Verification: `! grep -P "—" mock/crates/arvo-bits-contracts/src/narrow_from.rs`.

## Pass 4: arvo-storage meta_bits.rs stale Width refresh

## CHANGE: doc references `Hasher<const N: Width>` / `Fnv1a<const N: Width>` UPDATE TO `const N: u16`
File: `mock/crates/arvo-storage/src/meta_bits.rs`
Verification: `! grep -q "Hasher<const N: Width>" mock/crates/arvo-storage/src/meta_bits.rs && ! grep -q "Fnv1a<const N: Width>" mock/crates/arvo-storage/src/meta_bits.rs`

## Pass 5: bridge family canonical impls

## CHANGE: `impl const ConstFrom<u16> for Width` ADDED in arvo-strategy/src/width.rs
File: `mock/crates/arvo-strategy/src/width.rs`
Verification: `grep -n "impl const ConstFrom<u16> for Width" mock/crates/arvo-strategy/src/width.rs` returns one match.

## CHANGE: `impl const ConstDeref for WideBits<BYTES, A>` ADDED in arvo-strategy/src/widebits.rs
File: `mock/crates/arvo-strategy/src/widebits.rs`
Verification: `grep -n "ConstDeref for WideBits" mock/crates/arvo-strategy/src/widebits.rs` returns one match.

## CHANGE: `impl const ConstAsRef<[u8; BYTES]> for WideBits<BYTES, A>` ADDED in arvo-strategy/src/widebits.rs
File: `mock/crates/arvo-strategy/src/widebits.rs`
Verification: `grep -n "ConstAsRef.*for WideBits" mock/crates/arvo-strategy/src/widebits.rs` returns one match.

## Lock criteria

- All `## CHANGE:` blocks above have their verification commands return the expected result.
- `cargo check --workspace` passes clean.
- `cargo test --workspace` passes (this round directly addresses the test breakage).
- `cargo mock --lint-only --commit` passes scoped to all touched crates.

## Comparison to deprecated changelist

Not applicable. This is a fresh src CL.
