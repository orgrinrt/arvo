# src changelist

Executes the doc CL: closes the `core`-shadowing enumeration gap (B1), the cross-end matrix's
on-grid-only feed (B2), the process narration left in shipped test source (B4), and C1 through C3
from the sixth review of PR #146. B3 (the stale respelling) landed entirely in the doc CL.

## CHANGE: lint `the-platform-width-points-read-the-pointer-width`'s second arm ENUMERATES four forms instead of three, each with its own message
File: `mock/lints/the_platform_width_points_read_the_pointer_width.rs`
Verification: `grep -c 'fn use_leaf_core\|fn keyword_scoped_as_core\|fn strip_word_allowing_raw' mock/lints/the_platform_width_points_read_the_pointer_width.rs` returns 3. The `mod core` message no longer ends in the clause that did not parse, and the `use`/`extern crate` messages read distinctly rather than sharing one string.

## CHANGE: lint test module ADDS coverage for `r#core`, braced `use` aliases, and `use ...::core;`
File: `mock/lints/the_platform_width_points_read_the_pointer_width/tests.rs`
Verification: `cargo test -p mockspace-lints the_platform_width_points_read_the_pointer_width` (via `cargo mock test`) reports every new test green: `a_raw_ident_mod_core_fires`, `a_raw_ident_extern_crate_self_alias_fires`, `a_raw_ident_use_alias_fires`, `a_braced_use_alias_fires`, `a_braced_use_alias_closing_at_the_final_brace_fires`, `a_use_path_ending_bare_in_core_fires`, `a_bare_use_of_core_at_the_crate_root_fires`, `a_multiline_cfg_gated_extern_crate_alias_fires`, `a_use_path_continuing_past_core_is_silent`. The duplicate `the_control_no_core_binding_present_is_silent` is deleted.

## CHANGE: probe directory ADDS three arms and a note
File: `mock/research/202609191400_core_shadowing_probe/the_use_alias_does_not_reach_the_leading_colon_path.rs`, `.../the_raw_ident_extern_crate_self_hijacks_the_leading_colon_path.rs`, `.../the_bare_spelling_without_the_leading_colon_is_shadowed.rs`, `.../MANIFEST_RENAME_IS_UNGUARDED.md`
Verification: `ls mock/research/202609191400_core_shadowing_probe/*.rs` lists four arms; the negative control's own comment states its outcome is a compile failure by construction, which is the class of evidence C4 asked for (every existing arm exited 0). The manifest-rename note states the hazard is a hypothesis, not a checked result, per `evidence-lives-in-the-repo-or-it-never-happened.md`.

## CHANGE: the original probe file's header comment NARROWED to what the file actually declares
File: `mock/research/202609191400_core_shadowing_probe/the_leading_double_colon_resists_both_shadows.rs`
Verification: the file no longer claims a `use ... as core` is in scope of it; the unused `mod fake` is gone.

## CHANGE: `the_cross_end_saturation.rs` feeds off-grid remainders (1/4, 1/2, 3/4) alongside on-grid depths, over every mode and both policies
File: `mock/crates/arvo-format/src/apply/tests/the_cross_end_saturation.rs`
Verification: `grep -c 'past_the_bottom_off_grid\|past_the_top_off_grid' mock/crates/arvo-format/src/apply/tests/the_cross_end_saturation.rs` returns more than the 2 declarations alone, confirming call sites beyond the definitions. `cargo test -p arvo-format apply::tests::the_cross_end_saturation` is green.

## CHANGE: `the_broken_maps.rs` ADDS `no_far_lo_guard_when_stepped_up`, a second, independent near-bottom guard mutant
File: `mock/crates/arvo-format/src/apply/tests/the_broken_maps.rs`
Verification: `grep -c 'pub(super) fn no_far_lo_guard_when_stepped_up' mock/crates/arvo-format/src/apply/tests/the_broken_maps.rs` returns 1. Applying the same narrowing directly to `apply.rs:237` (`(rounded.past < 0 && (lo == i128::MIN || !rounded.up.get())) || ...`) and running `cargo test -p arvo-format --lib` turned `apply::tests::the_cross_end_saturation::the_shipped_map_pins_at_the_near_end_regardless_of_where_the_far_range_sits` red, confirmed by hand during this round; `apply.rs` was then restored exactly (`diff` against the pre-mutation copy showed no difference) and touched.

## CHANGE: `the_cross_end_saturation.rs` ADDS `wrap_residues_hand_reduced_off_grid_into_integer_3`, two hand-reduced Wrap answers
File: `mock/crates/arvo-format/src/apply/tests/the_cross_end_saturation.rs`
Verification: `cargo test -p arvo-format wrap_residues_hand_reduced` passes; the test's own comments carry the by-hand reduction (`i128::MIN.rem_euclid(8) = 0`, `i128::MAX.rem_euclid(8) = 7`) a reader can check independently of running the code.

## CHANGE: process narration REMOVED from test doc comments, a test name, and assertion messages
File: `mock/crates/arvo-format/src/apply/tests/the_cross_end_saturation.rs`, `the_broken_maps.rs`, `the_carry_past_the_index.rs`, `the_edges.rs`
Verification: `grep -rniE '\breview\b|\bM1\b|\bM2\b|\b146[a-z]?\b' mock/crates/arvo-format/src mock/crates/arvo-format/DESIGN.md.tmpl` returns only the unrelated type alias `F1` in `the_verdict.rs`, naming a `Grid` signature rather than a finding id.

## Not actioned

None. Every blocking and catalogue item the sixth review named (B1 through B4, C1, C2) has a source
or doc change above; C3 through C7 were resolved in earlier rounds or are prose-only, already fixed in
the doc CL (B3) or by this src CL's message changes (C3).
