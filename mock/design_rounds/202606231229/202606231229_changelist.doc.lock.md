# Doc changelist: precise logical-bound saturation contract

Round: 202606231229
Phase: DOC
Scope: arvo-strategy

## Changes proposed

Clarify in `arvo-strategy/DESIGN.md.tmpl` that the `Precise` strategy saturates at the LOGICAL bound (the
i_MIN..i_MAX / 0..u_MAX of the logical width N), not the container bound, and that the fixed-point multiply
widens to a 2N-bit intermediate (native for logical <= 64, a 256-bit limb product above that). This documents
the contract the implementation is being fixed to honour (see the round topic + roadmap). The current source
violates this contract; the src CL + the follow-up mechanical fix bring source into line.

## Comparison to deprecated changelist

None.
