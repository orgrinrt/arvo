# Source changelist: the alias sweep, the reader that binds, and the note's lint

**Date:** 2026-09-20
**Round:** the source half of `202609200040_changelist.doc.lock.md`.
**Topics consumed:** `202609200033_topic.the-ties-away-alias-has-no-gap.md`,
`202609200034_topic.the-lint-binds-a-reading-to-a-name.md`,
`202609200035_topic.the-note-travels-and-the-claims-are-swept.md`.
**Scope:** `mock/crates/arvo-format/tests/**`, `mock/crates/arvo-format/src/standards.rs`,
`src/rounding.rs`, `src/symmetry.rs`, `src/addition/tests/the_verdict.rs`,
`mock/lints/half_up_is_not_a_magnitude_rule/**`, one new lint under `mock/lints/`, and two sketches
under `mock/research/sketches/`.

## Canon

`ruling::half_up_denotes_ties_toward_positive_infinity`, declared by the design. Every change below is
the locked doc changelist's sentence carried into source, except the two lints, which are harness: what
they read and how they read it is this changelist's, and the ruling is what they enforce.

## CHANGE: the alias arms leave the parity file for one of their own
Files: `mock/crates/arvo-format/tests/matlab_fi_parity.rs`,
`mock/crates/arvo-format/tests/the_ties_away_alias.rs` (new),
`mock/crates/arvo-format/tests/ties_away/mod.rs` (new)
`ties_away`, `a_consumer_can_write_the_ties_away_alias_and_it_is_matlab_round` and
`the_alias_cannot_be_written_at_a_denominator_past_half_the_fraction` go from the parity file. The two
spellings of the alias live in `tests/ties_away/mod.rs`, which is a module rather than a test target, so
both test files read one definition of them: `shift`, from the slot and the ratio, and `select`, read off
the position. The parity file keeps MATLAB's four positions and asserts them against `select`.
Verification: `grep -c "fn ties_away" mock/crates/arvo-format/tests/matlab_fi_parity.rs` returns zero,
and `cargo test -p arvo-format --test matlab_fi_parity` passes with nothing ignored.

## CHANGE: the alias is swept against the integer rule, with six planted spellings
File: `mock/crates/arvo-format/tests/the_ties_away_alias.rs` (new)
The sweep the design names. Both spellings against `sign(x) floor(|x| + 1/2)` computed as a step above
the slot, itself checked against a brute-force reading of the whole position wherever that fits, then
completed over the format's range. Slots at both ends of the index and around zero, denominators of both
parities from one to `i64::MAX`, numerators at both ends and at the midpoint, `Integer<8>` and
`Integer<64>` under `Wrap` and `Saturate`, and a second domain whose numerators carry out of `[0, 1)`.
Six planted spellings, each required to be reported: `half_up` alone, `toward_zero` where `floor`
belongs, a zero slot read as negative, the shift always upward, the shift composed with `floor`, and the
doubled shift. The one region where `shift` cannot be formed is asserted as a region rather than as three
cells, with the tie-freeness of that region asserted beside it.
Verification: `cargo test -p arvo-format --test the_ties_away_alias` passes; commenting out any one
planted spelling's arm fails it.

## CHANGE: two arms that assert nothing go
File: `mock/crates/arvo-format/tests/matlab_fi_parity.rs`
`the_bound_is_attained_and_not_merely_respected` computes over its own literals and never touches the
crate, and its second assertion is implied by its first. It goes, and the property it was for, that the
bound is reached so the comparison may not become strict, is asserted inside the `parity!` macro, where
the comparison is: each arm states whether it sits on the bound and asserts it, so the one arm that
attains the bound is what refuses a tightening and the other four are pinned off it. The
`assert_ne!(MATLAB_NEAREST, MATLAB_ROUND)` in `no_shipped_mode_is_matlab_round` relates two declarations
to each other and goes.
Verification: `grep -c "the_bound_is_attained" mock/crates/arvo-format/tests/matlab_fi_parity.rs`
returns zero; turning the macro's `<=` into `<` fails `fi_pi_signed_eight`.

## CHANGE: `standards`'s FIXME FROM a limit of the alias TO where Round is written
File: `mock/crates/arvo-format/src/standards.rs`
The FIXME's claim that a caller can form the shift only up to `i64::MAX / 2` goes. What replaces it says
Round is ties away from zero, that it is not one of the six and is not meant to be, and that the alias is
written from the public surface, `floor` at a tie whose slot is below zero and `half_up` everywhere else.
The `rounding_method` module's doc carries the note.
Verification: `grep -c "i64::MAX / 2" mock/crates/arvo-format/src/standards.rs` returns zero, and
`grep -c "HALF_UP" mock/crates/arvo-format/src/standards.rs` returns at least one.

## CHANGE: the note travels with the name on three more rustdoc pages
Files: `mock/crates/arvo-format/src/rounding.rs`, `mock/crates/arvo-format/src/symmetry.rs`,
`mock/crates/arvo-format/src/standards.rs`
`pub struct HalfUp`, `behaviour_of` and `rounding_method` each state what `half_up` denotes and each is a
page a reader reaches on its own, so each carries the note.
Verification: the new lint passes over the tree.

## CHANGE: a lint refusing a published surface that names the mode without the note
Files: `mock/lints/half_up_carries_its_note.rs` (new),
`mock/lints/half_up_carries_its_note/tests.rs` (new)
`half-up-carries-its-note`, at hard error. It reads the repository's root `README.md`, every `.md.tmpl`
under the mock directory and every doc comment in a `.rs` file there, outside `research/`,
`design_rounds/`, `target/` and hidden directories. A markdown document naming any spelling of the mode
carries `HALF_UP` somewhere in it. A doc passage carries it where the passage states the denotation,
which is where it names the mode and says what happens at a tie, at a midpoint or which neighbour is
nearest. It does not read the registry, the generated documents under `docs/`, or plain comments. The
spellings and the passage reader come from the sibling lint rather than a second copy, whose
`SPELLINGS`, `spellings_in` and `passages` widen to `pub(crate)` for it.
Verification: `cargo test --manifest-path mock/target/mockspace-lints/Cargo.toml half_up_carries`
passes; `cargo mock --lint-only` reports every lint passing over this tree; planting the note-free
sentence in a template and in a doc comment fires, and the same sentence with the note does not.

## CHANGE: the magnitude lint's reader binds an escape to what it governs
File: `mock/lints/half_up_is_not_a_magnitude_rule/reading.rs`
A clause is cut into segments at `,`, `(`, `)`, `{` and `}`. A negator voids a pairing where it sits in
the reading's segment before the reading with no conjunction between the two, or in the name's segment
before the name, or in the segment before one the reading opens with a relative pronoun. `listed_apart`
becomes a test over bare items: both the name's segment and the reading's segment have to hold nothing
but the term, a conjunction, an article and punctuation. `ELSEWHERE` splits into `CONDITIONS`, which void
the clause, and `QUALIFIERS`, which void only where they sit in the name's or the reading's segment ahead
of the reading. `in magnitude` counts as a reading only behind a direction word. A clause with no name of
its own takes the previous clause's where it opens with a pronoun for the mode or continues it with a
conjunction and a verb, bounded at a blank line.
Verification: the corpus below passes, and every mutant of the new arms is caught.

## CHANGE: a corpus of sentences, each with its near-twin
File: `mock/lints/half_up_is_not_a_magnitude_rule/sentences.rs` (new)
Two tables, one of sentences that must fire and one of sentences that must stay silent, each entry paired
with the twin on the other side that differs by one thing. The eight the review measured are entries in
it. Contrastive, conditional, exception, multi-sentence, numeric-example, magnitude and sign vocabulary
on the firing side; error bounds, correct statements, genuine negation, parentheticals and lists on the
silent side.
Verification: `cargo test --manifest-path mock/target/mockspace-lints/Cargo.toml half_up_is_not` passes,
and the count of entries is asserted so a deletion is visible.

## CHANGE: an answered question loses one field rather than its whole row
File: `mock/lints/half_up_is_not_a_magnitude_rule.rs`
`answered` joins `NOT_PROSE` and the row-level skip for an answered question goes, so `asks`, `note` and
`because` on an answered question are read. The ratified-ruling skip stands.
Verification: `corpus_tests::control_what_is_not_prose_or_is_the_record_is_not_read` and its sibling are
rewritten to the new rule and pass, and `cargo mock --lint-only` stays clean over the real registry.

## CHANGE: a test that asserted a paraphrase quotes the note
File: `mock/lints/half_up_is_not_a_magnitude_rule/tests.rs`
`the_rulings_own_note_and_the_crates_own_docs_read_clean` becomes
`the_rulings_own_note_read_verbatim_is_clean` and holds the `note` field's words as the registry carries
them. `it_answers_to_the_name_the_gate_and_the_config_use` drops the assertion comparing the lint's
`name()` with the constant it returns.
Verification: the two arms pass, and the quoted text matches
`cargo mock query ruling half_up_denotes_ties_toward_positive_infinity`.

## CHANGE: the verdict asserts a relation where it pinned copied numbers
File: `mock/crates/arvo-format/src/addition/tests/the_verdict.rs`
The `half up` row of `fractional_refused_by_cell` is asserted equal to the `ceil` row rather than written
out again, and the four modes reading nothing besides the residue are asserted to have no associative
refusal as a group.
Verification: `cargo test -p arvo-format --lib a_licensed_cell` passes, and changing any pinned row's
measured value fails it, as does making half up and ceil differ. The rows are looked up by mode, so the
assertions no longer depend on the order `ALL_MODES` lists them in.

## CHANGE: the mutants sketch says 28, and a second sketch covers the new arms
Files: `mock/research/sketches/the-half-up-lint-mutants/README.md`,
`mock/research/sketches/the-half-up-lint-reader-mutants/` (new)
The first says 28 rather than 29, which is what its own list and `out/runs.txt` hold. The second is the
same exercise over the arms this round adds, with the mutant copy of the reader and the raw run committed
beside it.
Verification: the new sketch's `out/runs.txt` holds one run per mutant and each fails at least one arm.
