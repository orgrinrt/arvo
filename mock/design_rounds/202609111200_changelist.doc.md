# Doc changelist: addition admitted on a declared signature

Consumes `202609111010_topic.addition-admitted-on-a-declared-signature.md`, the one open topic, including
the section it gained after the first doc changelist locked. It rests on the sketch
`mock/research/sketches/202609111010_addition-obligation-and-window/`, which establishes that the
obligation shape refuses at the tool the round pins it with, that a window family of slot ranges builds
without `generic_const_exprs`, that the exact step reproduces the canon's witness counts, and that the
carry refusal is reachable only from an outside format.

## Comparison to deprecated changelist

`202609111027_changelist.doc.deprecated.md` carried the whole of the design below except two things, and
its template edits landed while it was active, in `915c26d9` and `abf0b845`. They stand: nothing here
takes any of them back.

What it missed, found while planning the source against it:

- A third refusal. A phase declared over `i64::MIN` with an odd numerator has a fractional part whose
  reduced denominator is `2^63`, which no `Fraction` holds. `Fraction::of` saturates it, at the largest
  odd numerator to exactly one, and `Exact::between` carries that into the slot, so the position lands a
  whole slot above the sum. Built to the deprecated changelist, the source would have had to refuse this
  without the design saying so, or return the wrong slot on an admitted format.
- The force sites. It named `sum_position` as the verb forcing the obligation. `addition_reach` and
  `addition_is_associative` compute the same positions and would otherwise narrow them into a `Slot` for a
  format whose sum leaves the coordinate.

Everything else is carried over unchanged, and is listed again below so this changelist stands alone.

## `crates/arvo-format/DESIGN.md.tmpl`

Canon declaration, at the top. `ruling::the_additive_and_absorption_verdicts_are_canon`, the two
propositions it ratifies, `law::additive_associativity_under_wrapping`,
`law::additive_associativity_under_saturation` and
`law::additive_associativity_recovered_under_declared_nonnegative_operand_window`. Landed.

The admission rule section: addition is the instance this crate ships, one member under the rule. Landed.

What this crate does not decide: `mixed_numeral_addition`, `reduction_order_or_associativity`,
`what_a_numeral_guarantees_to_a_fold`, and a numeral value type. Landed.

The surface table's `Operation` row, the free-function list and the verdict list. Landed.

The panic section: `panic_on_overflow` takes the position and the dither, and no operation panics on its
own account. Landed.

The rounding region's fourth disjunct, no position off the grid. Landed.

The reach's grid state, one coordinate of three values, the strict upper excursion on the grid, six facts
rather than five, the on-grid arm in the owed laws, `on_grid` and `reaches_off_the_grid` in the surface,
and the closing paragraph saying addition reads the predicates. Landed.

The addition section. Landed, and changed here in two places:

- What it refuses gains the third refusal, a remainder the position cannot hold, with why it is refused
  rather than approximated: the saturation `Fraction` performs is exact enough for a ratio and not for a
  position, because the carry turns it into a whole slot.
- The obligation paragraph names its three force sites, `sum_position`, `addition_reach` and
  `addition_is_associative`, rather than one.
- The owed compile-fail cases gain the remainder refusal, over an outside format.

## `crates/arvo-format/README.md.tmpl`

The addition paragraph. Landed; unchanged.

## `DESIGN.md.tmpl` at the root

The crate table's `arvo-format` row names addition, and the contracts paragraph lists `Add`. Landed;
unchanged.

## The generated pages

`cargo mock` regenerates `docs/` from the template changed here, in the same commit as the template.

## What the source changelist will carry

The deletion of the tautological tests the topic names, before any addition source lands; the vocabulary
and inventory facts restated as exhaustive matches; totality over every admitted width of both slot
families; the grid state in `Reach` with the cross extended to it; `panic_on_overflow` at a position; the
`addition` module, its obligation with its three refusals and three force sites, and its tests; the
`trybuild` cases for the five refusals.
