# Src changelist: the three the glob and the filter could not reach

**Date:** 2026-09-02
**Round:** three comments the first sweep could not see
**Scope:** two files, three comment lines.

## CHANGE: provenance FROM three comments TO the claim they were attached to
File: `arvo-strategy/src/lib.rs:118`, `arvo-placement/src/tests/the_open_inventory.rs:8,18`
Verification: the corrected sweep, recursive over `mock/crates/*/src/` and `mock/crates/*/tests/`,
extracting matched words rather than filtering lines, returns zero; and its positive control returns
4 when a line of provenance is planted, so the zero is a measurement rather than a broken pipeline.

`arvo-strategy/src/lib.rs:118` is rustdoc on `pub struct Precise` and said its objective "had no
measurement in the panel". The gap is the fact and it stays; where the gap was noticed is not the
reader's business and not something they can act on.

The two in `the_open_inventory.rs` cite a proposal as "ratified through" a ruling, and describe a
check as the one "the ratified clause asks for". The slugs stay, because a slug is an address a
reader can resolve. What goes is the tier, which says who settled it.

## CHANGE: nothing behavioural
File: none
Verification: 165 passing, 0 failing, 2 ignored, unchanged across the round.

Three comment lines. No signature, no body, no test.

## CHANGE: nothing in the registry
File: none
Verification: `cargo mock` reports 708 rows across 10 namespaces before and after.
