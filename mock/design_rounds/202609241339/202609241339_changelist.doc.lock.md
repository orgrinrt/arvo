# Doc changelist: no design document changes, the split is internal

**Date:** 2026-09-24
**Round:** `202609241339`, findings 9 of the fifth review of pull request #150.
**Topics consumed:** `202609241339_topic.two-arvo-format-files-past-five-hundred-lines-split-on-their-seams.md`,
the only flat topic in `mock/design_rounds/`, checked with `git ls-tree -r`.
**Scope:** none. No `crates/*/DESIGN.md.tmpl`, no root `*.md.tmpl`, no rendered `docs/` page names
`symmetry.rs`, `reach.rs`, `the_verdict.rs` or any of its four new submodules by path, and none of them
gains or loses a public item this round.

## Why there is no CHANGE entry

`mock/crates/arvo-format/DESIGN.md.tmpl` documents `Reach` and the equivariance predicates by name and by
what they do, never by which file under `src/` carries them. Grepped: no occurrence of `symmetry.rs`,
`the_verdict.rs`, or a `src/` path in that document. The split moves declarations between files in the same crate. `symmetry.rs` keeps its own path and gains
`mod reach; pub use reach::Reach;`, so `crate::symmetry::Reach` resolves exactly as before, and the test
module `the_verdict` keeps its name and its test function names. Nothing a design document could cite
moved.

## Canon

None governs module layout in this repo's registry. `ruling::half_up_denotes_ties_toward_positive_infinity`
and the other rulings this branch touches are unaffected; this round does not read or write the registry.
