**Round:** bench-workspace-exclude-regression. **Doc CL:**
`202608090856_changelist.doc.lock.md`.

## CHANGE: workspace `mock/Cargo.toml` FROM no `benches` exclude TO `exclude = ["benches"]`
File: `mock/Cargo.toml`
Verification: `grep 'exclude = \["benches"\]' mock/Cargo.toml` returns one match; `cd
mock/benches/variants/bitpack-contend-shared && cargo check` succeeds (predates this round and was
broken by `929c46ed` until this line landed).

## CHANGE: coordinator `mock/benches/Cargo.toml` FROM `bench-structural-decomposition` /
`bench-spectral-bisection` listed as hard dependencies TO both removed, with a comment explaining why
File: `mock/benches/Cargo.toml`
Verification: `grep -c 'bench-spectral-bisection\|bench-structural-decomposition'
mock/benches/Cargo.toml` returns 0 for the dependency lines (the explanatory comment names them in
prose, which the grep above does not match as a dependency declaration); `cd mock/benches && cargo
build --release` succeeds.

## CHANGE: `mock/benches/src/main.rs`'s `Fiedler`/`Rcm` imports and their three match arms each FROM
active TO commented out with the same explanation
File: `mock/benches/src/main.rs`
Verification: `grep -c 'use bench_spectral_bisection::Fiedler\|use bench_structural_decomposition::Rcm'
mock/benches/src/main.rs` returns 0 for active (non-commented) uses.

## Why no crate under `mock/crates/` is touched

There is nothing there to touch; the tree is empty for the canon work. Every change in this round is
bench-infrastructure plumbing (workspace membership, a coordinator's own dependency list, and its
routine dispatch table), not design or canon.
