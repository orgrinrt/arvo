# Src changelist: the twelfth review's premise controls

**Date:** 2026-09-19
**Round:** the twelfth review's findings on `feat/platform-width-types-on-a-wider-slot-range`
**Scope:** no crate source and no lint source; `docs/` regenerated.

The round's change sits in `mock/research/202609191400_core_shadowing_probe/` and was committed in
`TOPIC` phase. The locked doc changelist changes no template, so no source follows from it.

## CHANGE: nothing in `mock/crates/` or `mock/lints/`
Verification: `git diff --stat 2d80b771 HEAD -- mock/crates mock/lints` is empty at this round's close.

## CHANGE: `docs/` regenerated
File: `docs/`
Verification: `cargo mock` exits 0 and `git status --porcelain` is empty afterwards, since no template
changed.
