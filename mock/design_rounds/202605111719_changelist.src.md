**Date:** 2026-05-11
**Phase:** SRC
**Scope:** arvo-sparse + arvo-spectral source
**Doc CL:** `202605111719_changelist.doc.lock.md`

# Source changelist: round 202605111719

Mechanical plan executing the locked doc CL. Per `cl-claim-sketch-discipline.md`, structured `## CHANGE:` blocks claim each source-state transition with a verification command. Sketches from `mock/research/sketches/202605111719_graph_spectral/` inform the implementation; S1 already validated the `pub const trait SparseAdjacency` shape, S2-S4 are mechanical extensions verified at compile.

## Order of operations

1. Add trait declarations in arvo-sparse (`SparseAdjacency`, `BidirectionalSparseAdjacency`).
2. Wire impls onto `BitMatrix` in arvo-sparse via the trait-defining crate's orphan rule.
3. Add `Csr` `SparseAdjacency` impl, `CsrBidirectional`, `Csr::with_transpose` constructor.
4. Add problem-shaped traits (`BandwidthReducer`, `BlockPartitioner`, `BipartiteStructuralAnalysis`) with named-algorithm default impls.
5. Lift W-generic on `rcm_reorder`, `block_diagonal`, `dulmage_mendelsohn`; add `_via` overloads.
6. Reshape `DulmageMendelsohn<N>` struct + return shape; add `classification_to_mask` helper.
7. arvo-spectral: declare `LinearOperator<F, N>` trait + impl on `Matrix<W, N>`.
8. Add `SparseLaplacian<T, W>` struct + impl + constructor.
9. Lift `power_iteration` + `fiedler_vector` to operator-generic.
10. Reshape `spectral_bisection` return to `(USize, [USize; cap_size(N)])`.
11. Add `SpectralBipartitioner` trait with Fiedler-default impl.
12. Lift `k_way_partition` to operator-generic.
13. Add `feature(const_index)` crate-root attribute to arvo-sparse lib.rs.
14. Run benches; populate findings file.
15. Test rehab.

## CHANGE blocks

### arvo-sparse trait declarations

## CHANGE: trait `SparseAdjacency<const N: Cap>` ADDED
File: `mock/crates/arvo-sparse/src/adjacency.rs` (new module)
Verification: `grep -q "pub const trait SparseAdjacency" mock/crates/arvo-sparse/src/adjacency.rs`

## CHANGE: trait `BidirectionalSparseAdjacency<const N: Cap>` ADDED
File: `mock/crates/arvo-sparse/src/adjacency.rs`
Verification: `grep -q "pub const trait BidirectionalSparseAdjacency" mock/crates/arvo-sparse/src/adjacency.rs`

## CHANGE: module `adjacency` MOUNTED at crate root with pub re-exports
File: `mock/crates/arvo-sparse/src/lib.rs`
Verification: `grep -q "pub mod adjacency" mock/crates/arvo-sparse/src/lib.rs`

## CHANGE: crate attribute `feature(const_index)` DEFERRED at arvo-sparse root
File: `mock/crates/arvo-sparse/src/lib.rs`
Deviation: The doc CL projected this feature would be needed once `BitMatrix::successors`/`predecessors` lift to const fn, supporting `impl const SparseAdjacency<N>`. Per the round's open-decisions block, the impl landed as plain `impl SparseAdjacency` because the underlying methods are not yet const fn (out-of-scope substrate work). The unused feature attribute is dropped per `no-legacy-shims-pre-1.0.md`; the feature returns once the impl actually lifts.
Verification: `! grep -q "feature(const_index)" mock/crates/arvo-sparse/src/lib.rs`

### BitMatrix impls of SparseAdjacency

## CHANGE: blanket `impl<W: BitPrim, const N: Cap> const SparseAdjacency<N> for BitMatrix<W, N>` ADDED
File: `mock/crates/arvo-sparse/src/adjacency.rs`
Verification: `grep -q "impl<W:.*BitPrim.*> const SparseAdjacency<N> for BitMatrix" mock/crates/arvo-sparse/src/adjacency.rs`

## CHANGE: blanket `impl<W: BitPrim, const N: Cap> const BidirectionalSparseAdjacency<N> for BitMatrix<W, N>` ADDED
File: `mock/crates/arvo-sparse/src/adjacency.rs`
Verification: `grep -q "impl<W:.*BitPrim.*> const BidirectionalSparseAdjacency<N> for BitMatrix" mock/crates/arvo-sparse/src/adjacency.rs`

### Csr SparseAdjacency impl + CsrBidirectional

## CHANGE: `impl<const ROWS: Cap, const NNZ: Cap, W: Copy> const SparseAdjacency<ROWS> for Csr<ROWS, NNZ, W>` ADDED
File: `mock/crates/arvo-sparse/src/csr.rs`
Verification: `grep -q "impl<const ROWS: Cap, const NNZ: Cap, W: Copy> const SparseAdjacency<ROWS> for Csr" mock/crates/arvo-sparse/src/csr.rs`

## CHANGE: struct `CsrBidirectional<const ROWS: Cap, const NNZ: Cap, W: Copy>` ADDED
File: `mock/crates/arvo-sparse/src/csr.rs`
Verification: `grep -q "pub struct CsrBidirectional" mock/crates/arvo-sparse/src/csr.rs`

## CHANGE: method `Csr::with_transpose` ADDED
File: `mock/crates/arvo-sparse/src/csr.rs`
Verification: `grep -q "pub fn with_transpose" mock/crates/arvo-sparse/src/csr.rs`

## CHANGE: `impl const SparseAdjacency<ROWS> for CsrBidirectional<...>` ADDED
File: `mock/crates/arvo-sparse/src/csr.rs`
Verification: `grep -q "const SparseAdjacency<ROWS> for CsrBidirectional" mock/crates/arvo-sparse/src/csr.rs`

## CHANGE: `impl const BidirectionalSparseAdjacency<ROWS> for CsrBidirectional<...>` ADDED
File: `mock/crates/arvo-sparse/src/csr.rs`
Verification: `grep -q "const BidirectionalSparseAdjacency<ROWS> for CsrBidirectional" mock/crates/arvo-sparse/src/csr.rs`

### Problem-shaped traits + defaults

## CHANGE: trait `BandwidthReducer<const N: Cap>` ADDED with default impl chaining `rcm_reorder`
File: `mock/crates/arvo-sparse/src/adjacency.rs`
Verification: `grep -q "pub const trait BandwidthReducer" mock/crates/arvo-sparse/src/adjacency.rs`

## CHANGE: trait `BlockPartitioner<const N: Cap>` ADDED with default impl chaining `block_diagonal`
File: `mock/crates/arvo-sparse/src/adjacency.rs`
Verification: `grep -q "pub const trait BlockPartitioner" mock/crates/arvo-sparse/src/adjacency.rs`

## CHANGE: trait `BipartiteStructuralAnalysis<const N: Cap>` ADDED with default impl chaining `dulmage_mendelsohn`
File: `mock/crates/arvo-sparse/src/adjacency.rs`
Verification: `grep -q "pub const trait BipartiteStructuralAnalysis" mock/crates/arvo-sparse/src/adjacency.rs`

### Algorithm signature W-generic lift

## CHANGE: `rcm_reorder` signature FROM `<const N: Cap>(adjacency: &BitMatrix<Bits<64, Hot, Unsigned>, N>)` TO `<W, const N: Cap>(adjacency: &BitMatrix<W, N>) where W: BitPrim`
File: `mock/crates/arvo-sparse/src/rcm.rs`
Verification: `grep -q "pub fn rcm_reorder<W, const N: Cap>" mock/crates/arvo-sparse/src/rcm.rs` AND `! grep -q "Bits<64, Hot, Unsigned>" mock/crates/arvo-sparse/src/rcm.rs`

## CHANGE: `rcm_reorder_via<T, const N: Cap>` ADDED (trait-driven overload)
File: `mock/crates/arvo-sparse/src/rcm.rs`
Verification: `grep -q "pub fn rcm_reorder_via" mock/crates/arvo-sparse/src/rcm.rs`

## CHANGE: `block_diagonal` signature W-generic lift
File: `mock/crates/arvo-sparse/src/block.rs`
Verification: `grep -q "pub fn block_diagonal<W, const N: Cap>" mock/crates/arvo-sparse/src/block.rs` AND `! grep -q "Bits<64, Hot, Unsigned>" mock/crates/arvo-sparse/src/block.rs`

## CHANGE: `block_diagonal_via<T, const N: Cap>` ADDED (trait-driven overload)
File: `mock/crates/arvo-sparse/src/block.rs`
Verification: `grep -q "pub fn block_diagonal_via" mock/crates/arvo-sparse/src/block.rs`

### DulmageMendelsohn reshape

## CHANGE: struct `DulmageMendelsohn<const N: Cap>` FROM `{ horizontal: Mask<...>, vertical: Mask<...>, square: Mask<...> }` TO `{ class_count: USize, class: [USize; cap_size(N)] }`
File: `mock/crates/arvo-sparse/src/dm.rs`
Verification: `grep -q "pub class_count: USize" mock/crates/arvo-sparse/src/dm.rs` AND `! grep -q "horizontal:" mock/crates/arvo-sparse/src/dm.rs`

## CHANGE: `dulmage_mendelsohn` signature W-generic lift
File: `mock/crates/arvo-sparse/src/dm.rs`
Verification: `grep -q "pub fn dulmage_mendelsohn<W, const N: Cap>" mock/crates/arvo-sparse/src/dm.rs`

## CHANGE: `dulmage_mendelsohn_via<T, const N: Cap>` ADDED (bound on BidirectionalSparseAdjacency)
File: `mock/crates/arvo-sparse/src/dm.rs`
Verification: `grep -q "pub fn dulmage_mendelsohn_via" mock/crates/arvo-sparse/src/dm.rs`

## CHANGE: fn `classification_to_mask<W: BitPrim, const N: Cap>` ADDED
File: `mock/crates/arvo-sparse/src/dm.rs`
Verification: `grep -q "pub fn classification_to_mask" mock/crates/arvo-sparse/src/dm.rs`

### arvo-spectral LinearOperator + SparseLaplacian

## CHANGE: trait `LinearOperator<F, const N: Cap>` ADDED
File: `mock/crates/arvo-spectral/src/operator.rs` (new module)
Verification: `grep -q "pub trait LinearOperator" mock/crates/arvo-spectral/src/operator.rs`

## CHANGE: `impl<F, W, const N: Cap> LinearOperator<F, N> for Matrix<W, N>` ADDED
File: `mock/crates/arvo-spectral/src/operator.rs`
Verification: `grep -q "LinearOperator<F, N> for Matrix" mock/crates/arvo-spectral/src/operator.rs`

## CHANGE: struct `SparseLaplacian<'data, T, W>` ADDED
File: `mock/crates/arvo-spectral/src/operator.rs`
Verification: `grep -q "pub struct SparseLaplacian" mock/crates/arvo-spectral/src/operator.rs`

## CHANGE: `impl<F, T, W, const N: Cap> LinearOperator<F, N> for SparseLaplacian<'_, T, W>` ADDED
File: `mock/crates/arvo-spectral/src/operator.rs`
Verification: `grep -q "LinearOperator<F, N> for SparseLaplacian" mock/crates/arvo-spectral/src/operator.rs`

## CHANGE: module `operator` MOUNTED at arvo-spectral root with pub re-exports
File: `mock/crates/arvo-spectral/src/lib.rs`
Verification: `grep -q "pub mod operator" mock/crates/arvo-spectral/src/lib.rs`

### Operator-generic spectral algorithms

## CHANGE: `power_iteration` signature lift to `<Op, const N: Cap, F>(operator: &Op, ...) where Op: LinearOperator<F, N>`
File: `mock/crates/arvo-spectral/src/power.rs`
Verification: `grep -q "pub fn power_iteration<Op, const N: Cap, F>" mock/crates/arvo-spectral/src/power.rs`

## CHANGE: `fiedler_vector` signature lift to operator-generic
File: `mock/crates/arvo-spectral/src/fiedler.rs`
Verification: `grep -q "pub fn fiedler_vector<Op, const N: Cap, F>" mock/crates/arvo-spectral/src/fiedler.rs`

## CHANGE: `spectral_bisection` return FROM `(Mask<Bits<64, Hot, Unsigned>>, Mask<Bits<64, Hot, Unsigned>>)` TO `(USize, [USize; cap_size(N)])`
File: `mock/crates/arvo-spectral/src/partition.rs`
Verification: `grep -q "(USize, \[USize" mock/crates/arvo-spectral/src/partition.rs` AND `! grep -q "Mask<Bits<64, Hot, Unsigned>>" mock/crates/arvo-spectral/src/partition.rs`

## CHANGE: `k_way_partition` signature lift to operator-generic
File: `mock/crates/arvo-spectral/src/partition.rs`
Verification: `grep -q "pub fn k_way_partition<Op, const N: Cap, const K: Cap, F>" mock/crates/arvo-spectral/src/partition.rs`

## CHANGE: trait `SpectralBipartitioner<const N: Cap>` ADDED with Fiedler default impl
File: `mock/crates/arvo-spectral/src/partition.rs`
Verification: `grep -q "pub const trait SpectralBipartitioner" mock/crates/arvo-spectral/src/partition.rs`

## CHANGE: `assert!(n <= 64, ...)` runtime gate REMOVED from `fiedler_vector` and `spectral_bisection`
File: `mock/crates/arvo-spectral/src/fiedler.rs`, `mock/crates/arvo-spectral/src/partition.rs`
Verification: `! grep -E "assert!\(.*<= 64" mock/crates/arvo-spectral/src/fiedler.rs mock/crates/arvo-spectral/src/partition.rs`

### Cargo dep additions

## CHANGE: arvo-spectral Cargo.toml ADDS `arvo-sparse` direct dep
File: `mock/crates/arvo-spectral/Cargo.toml`
Verification: `grep -q "arvo-sparse" mock/crates/arvo-spectral/Cargo.toml`

### Bench bundles

## CHANGE: bench bundle `structural-decomposition` ADDED
File: `mock/benches/variants/structural-decomposition/` (new variant dir)
Verification: directory exists with `Cargo.toml` and `src/main.rs`.

## CHANGE: bench bundle `spectral-bisection` ADDED
File: `mock/benches/variants/spectral-bisection/`
Verification: directory exists with `Cargo.toml` and `src/main.rs`.

## CHANGE: `mock/research/findings_graph_spectral_202605111719.md` POPULATED with Bundle 1 + Bundle 2 results
File: `mock/research/findings_graph_spectral_202605111719.md`
Verification: file contains numeric results (not just placeholder text) under both `## Bundle 1` and `## Bundle 2`.

### Test rehab

## CHANGE: arvo-sparse tests migrate to new DulmageMendelsohn struct shape + W-generic signatures
File: `mock/crates/arvo-sparse/tests/*.rs`
Verification: `cargo test -p arvo-sparse` passes.

## CHANGE: arvo-spectral tests migrate to `(USize, [USize; cap_size(N)])` partition shape + operator-generic signatures
File: `mock/crates/arvo-spectral/tests/*.rs`
Verification: `cargo test -p arvo-spectral` passes.

### SHAME entry removal at round close

## CHANGE: SHAME.md.tmpl `## CsrBidirectional` entry REMOVED
File: `mock/crates/arvo-sparse/SHAME.md.tmpl`
Verification: `! grep -q "## CsrBidirectional" mock/crates/arvo-sparse/SHAME.md.tmpl`

## Lock criteria (src CL)

1. `cargo check --workspace` clean. Pre-existing warnings in unrelated crates do not block.
2. `cargo test -p arvo-sparse` and `cargo test -p arvo-spectral` green.
3. `mockspace --lint-only` clean at commit + push severity. The `design-doc-source-mismatch` lint passes with NO SHAME bypass.
4. Every `## CHANGE:` block above is verified by its named command.
5. Findings file fully populated.
6. SHAME entry for `CsrBidirectional` removed.

## Out of scope

- arvo-graph W-generic sweep (Category 1 of audit round 202605111741; follow-up round).
- arvo-hash `ContentHash<N>` width-generic (audit Category 2).
- Workspace-wide lint catching `Bits<64, Hot, Unsigned>` literals (audit Category 5).
- Restricted-Laplacian per-component eigen (arvo-spectral BACKLOG).
- SpMV (arvo-sparse BACKLOG).
