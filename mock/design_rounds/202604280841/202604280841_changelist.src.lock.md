**Date:** 2026-04-28
**Phase:** SRC-PLAN
**Round:** 202604280841. Layout-soundness gates
**Locked doc CL:** `202604280841_changelist.doc.lock.md`

Two new private modules with compile-time `assert_layout_eq!` invocations covering Tiers 1-4 from the topic.

## Pass 1: arvo-storage layout assertions

`arvo-storage/src/layout_assertions.rs` (new file):
- Define `assert_layout_eq!($a:ty, $b:ty)` macro at module scope (size + align checks).
- **Tier 1 — meta-layer**: `MetaCarrier ↔ Bits<9, Hot, Unsigned>`, `MetaCarrier ↔ u16`, `IBits ↔ MetaCarrier`, `FBits ↔ MetaCarrier`, `Width ↔ MetaCarrier`.
- **Tier 2 — Bits projection (Hot/Cold)**: `Bits<N, Hot, Unsigned>` ↔ projection target at boundary cells. Full set: 1, 8, 9, 16, 17, 32, 33, 64, 65, 128, 129, 192, 193, 255 for unsigned + signed.
- **Tier 2 — Bits projection (Warm/Precise)**: same boundary cells where `UContainerFor` is implemented (1, 8, 9, 16, 17, 32, 33, 64).
- **Tier 4 — MultiContainer**: layout invariants on the canonical pairs. Use `>=` on size (because of padding) and `==` on align (which is max of the two halves).

`arvo-storage/src/lib.rs`:
- Add `mod layout_assertions;` (no `pub use`; module is private — assertions fire at compile time).

## Pass 2: arvo wrapper layout assertions

`arvo/src/layout_assertions.rs` (new file):
- Define `assert_layout_eq!` macro locally.
- **Tier 3 — UFixed/IFixed transparency**: representative cells.
  - `UFixed<{ibits(8)}, {fbits(0)}, Hot>` ↔ `u8`.
  - `UFixed<{ibits(8)}, {fbits(0)}, Hot>` ↔ `Bits<8, Hot, Unsigned>`.
  - `UFixed<{ibits(13)}, {fbits(3)}, Hot>` ↔ `u16` (16 logical bits → u16).
  - `UFixed<{ibits(8)}, {fbits(0)}, Warm>` ↔ `u16` (Warm 2x at 8 bits → u16).
  - `UFixed<{ibits(8)}, {fbits(0)}, Cold>` ↔ `u8`.
  - `IFixed<{ibits(7)}, {fbits(0)}, Hot>` ↔ `i8` (1+7 = 8 → i8).
  - `IFixed<{ibits(7)}, {fbits(0)}, Hot>` ↔ `Bits<8, Hot, Signed>`.
  - `IFixed<{ibits(15)}, {fbits(0)}, Hot>` ↔ `i16`.
  - `IFixed<{ibits(31)}, {fbits(0)}, Hot>` ↔ `i32`.
  - `IFixed<{ibits(63)}, {fbits(0)}, Hot>` ↔ `i64`.

`arvo/src/lib.rs`:
- Add `mod layout_assertions;` (private).

## Pass 3: Validation

- `cargo check --workspace` clean.
- `cargo test --workspace` clean (no new tests; existing tests still pass).
- `cargo mock --lint-only --commit/--push` clean.
- Round close.

## Sources

- Topic file: `202604280841_topic.layout-soundness-gates.md`.
- Locked doc CL: `202604280841_changelist.doc.lock.md`.
