**Date:** 2026-04-28
**Phase:** DOC (becomes locked at end of doc execution)
**Round:** 202604280841. Layout-soundness gates
**Source topic:** `202604280841_topic.layout-soundness-gates.md`

This round adds source-side compile-time layout assertions; the doc surface barely changes. The doc CL is minimal: one paragraph in `arvo-storage/DESIGN.md.tmpl` documenting the `MetaCarrier` invariant, and a corresponding note in `arvo/DESIGN.md.tmpl` for the UFixed/IFixed transparency chain.

## Per-file mutations

### `mock/crates/arvo-storage/DESIGN.md.tmpl`

- **`## MetaCarrier` section**: Append a paragraph documenting the layout-equivalence invariant against `Bits<9, Hot, Unsigned>` is now compile-time-asserted via `arvo-storage/src/layout_assertions.rs`. Any divergence between MetaCarrier's hardcoded u16 and the projection's choice for `Bits<9, Hot, Unsigned>::T` breaks the build at compile time, surfacing the drift before any runtime soundness violation.
- **`## Bits` section**: Append a paragraph noting that `repr(transparent)` of `Bits<N, S, Sign>` over `<S as BitsContainerFor<N, Sign>>::T` is compile-time-asserted at boundary cells (1, 8, 9, 16, 17, 32, 33, 64, 65, 128, 129, 192, 193, 255 for Hot / Cold; 1, 8, 9, 16, 17, 32, 33, 64 for Warm / Precise) in `arvo-storage/src/layout_assertions.rs`.
- **`## MultiContainer` section**: Append a paragraph noting that `repr(C)` layout invariants on `MultiContainer<HiT, LoT>` are asserted for the canonical pairs (`u64, u128`; `u128, u128`; `i64, i128`; `i128, i128`).

### `mock/crates/arvo/DESIGN.md.tmpl`

- **`## Fixed-point types` section** (or wherever UFixed / IFixed transparency is documented): append a paragraph noting that `repr(transparent)` of `UFixed<I, F, S>` over `Bits<{I+F}, S, Unsigned>` and `IFixed<I, F, S>` over `Bits<{1+I+F}, S, Signed>` is compile-time-asserted at representative cells in `arvo/src/layout_assertions.rs`.

### `mock/crates/arvo/BACKLOG.md.tmpl`

- New `## Round 202604280841 forward-looking entries` section noting the deferred items:
  - Strategy-axis HasAxes correctness check (compile-time cross-check of axis combinations).
  - Auto-generated assertions for every Bits cell (currently boundary-cell coverage).
  - ConstParamTy soundness audit.
  - Mockspace lint to auto-generate layout assertions.

## Validation

After doc execution:

- `cargo mock --lint-only --commit` clean across all `.md.tmpl` files.

## Sources

- Topic file: `202604280841_topic.layout-soundness-gates.md`.
- Round C archive: `mock/design_rounds/202604280806/` (MetaCarrier rationale).

This doc CL is living during DOC phase only. Lock with `cargo mock lock`.
