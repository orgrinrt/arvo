# Doc Changelist: arvo-graph sweep

**Date:** 2026-04-21
**Phase:** DOC
**Scope:** No doc changes. arvo-graph's DESIGN.md.tmpl does not document the
  specific internal mutable-state patterns (counters, flag arrays) that this
  round touches; public API is unchanged.
**Topic:** `202604280100_topic.arvo-graph-sweep.md`

## Motivation

Pure internal remediation — replace `let mut x: usize = 0;` with
`let mut x = USize(0);` and `[bool; N]` with `[Bool; N]` inside three
function bodies. No public API change, no design surface change, no
template edits.

## Out of scope

Everything except internal loop / counter variable declarations across
`components.rs`, `waist.rs`, `spanning.rs`.

## Execution notes

DOC phase is a formality for this round. Lock immediately to open SRC-PLAN.
