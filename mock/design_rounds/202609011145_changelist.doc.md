# Doc changelist: no template changes

**Date:** 2026-09-01
**Round:** the arm that could not fail, and two words the schema does not have
**Scope:** none.

## CHANGE: nothing FROM the templates TO the templates
File: none
Verification: `git diff --name-only` over this round's commits names no `*.md.tmpl` and no generated
document.

Every edit is under `mock/tools/obligation-coverage/src/`, which is not a template surface and renders
into no page.

## CHANGE: nothing generated names this tool, and that is searched rather than assumed
File: none
Verification: `grep -rn "what reaches each obligation" .` finds the string in
`mock/tools/obligation-coverage/src/lib.rs` and nowhere else, and no file under `docs/` or any
`*.md.tmpl` names `obligation-coverage` at all.

`description()` does not move this round in any case. The two blockers are an arm that asserts over one
arrangement and a sentence claiming a greppability three of its seven words do not have, and neither
reaches that string.

The check is named here rather than waved past because 202609011102's changelist named a different one
that cannot be run: it says a generated tool catalogue snapshots the string, and this repository has no
such catalogue. The addendum below records that. The search above is what settles the question in its
place, and it is the form to use next time, because it fails loudly when a generated page does start
naming a tool.

## CHANGE: an addendum beside a locked changelist, not an edit to it
File: `mock/design_rounds/202609011102/202609011102_changelist.doc.lock.md.addendum.md`
Verification: the locked file's bytes unchanged, and the addendum naming the claim and what the diff
shows instead.

202609011102's doc changelist says regeneration runs at the close and the catalogue picks the new
`description` line up there. The round closed and the generated catalogue did not move, so the claim
stands over something that did not happen. A locked changelist is frozen forever, so the correction is
a file beside it saying what the claim was and what actually holds.
