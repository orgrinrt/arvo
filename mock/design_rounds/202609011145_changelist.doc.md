# Doc changelist: no template changes

**Date:** 2026-09-01
**Round:** the arm that could not fail, and two words the schema does not have
**Scope:** none.

## CHANGE: nothing FROM the templates TO the templates
File: none
Verification: `git diff --name-only` over this round's commits names no `*.md.tmpl` and no generated
document.

Every edit is under `mock/tools/obligation-coverage/src/`, which is not a template surface and renders
into no page.

## CHANGE: nothing about the tool catalogue, and it is checked rather than assumed
File: none
Verification: the generated catalogue's entry for this tool read against the `description()` the round
leaves behind, which is the string 202609011102 last moved.

`description()` does not move this round. The two blockers are an arm that asserts over one arrangement
and a sentence claiming a greppability three of its seven words do not have, and neither reaches the one
string of this tool that a generated document snapshots.

That check is named here rather than waved past because it is the only path by which a `mock/tools/`
edit can reach a generated page, and the previous round's changelist made a claim about regeneration
that its own diff does not support. The addendum below records that; this line is the reason it will
not happen twice.

## CHANGE: an addendum beside a locked changelist, not an edit to it
File: `mock/design_rounds/202609011102/202609011102_changelist.doc.lock.md.addendum.md`
Verification: the locked file's bytes unchanged, and the addendum naming the claim and what the diff
shows instead.

202609011102's doc changelist says regeneration runs at the close and the catalogue picks the new
`description` line up there. The round closed and the generated catalogue did not move, so the claim
stands over something that did not happen. A locked changelist is frozen forever, so the correction is
a file beside it saying what the claim was and what actually holds.
