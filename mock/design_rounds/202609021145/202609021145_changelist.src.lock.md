# SRC changelist: drop the path, widen the walk

## CHANGE: doc comment on `has_an_exact_form` FROM naming the sketch TO naming what it found
File: `mock/crates/arvo-format/src/apply/tests/the_ratio_coordinate.rs`
Verification: `grep -rc "mock/research" mock/crates/*/src/**/*.rs` returns zero everywhere, which is what the base of this branch measures.

The comment cites a directory under `mock/research/`, which
`documentation-writing.md` forbids on a tier 1 surface, and a shipped test is
one. What it says the search established stays, at the widths it ran over. Where
the search lives goes, and the two round topic files naming it are internal and
keep it.

## CHANGE: the four properties FROM `every_ratio` TO both walks
File: `mock/crates/arvo-format/src/apply/tests/the_ratio_coordinate.rs`
Verification: the arm counts the saturating pairs each walk entered and asserts the wide one passed fifty. Measured by raising the floor until it failed, which reports the count: 105, against the eleven the hand-written matrix carries.

The body of the arm becomes a function taking an iterator, and the test calls it
over `every_ratio()` and over `every_pair_worth_reducing()`. The region counters
move into the function so each walk asserts it entered both regions on its own,
which is what makes the second walk a second measurement rather than a longer
one.

The saturating count is asserted against a floor rather than an exact number,
because the exact one is a property of the value list and would fail on any
addition to it. The floor is what the finding is about: eleven is thin, a
hundred is not.
