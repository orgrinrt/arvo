# DOC changelist: the round broke its own rule inside its own diff

## CHANGE: none

Both changes are inside the suite: a doc comment on a test helper loses an
internal path, and the four properties gain a second walk. Neither moves what
the crate is or what it guarantees, so no design template moves.
