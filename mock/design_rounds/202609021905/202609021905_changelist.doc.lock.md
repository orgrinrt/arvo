# DOC changelist: the forcing sets are per obligation

## CHANGE: mock/crates/arvo-format/DESIGN.md.tmpl

Two sentences, both about which verbs force which obligation.

The first says `contains` forces nothing. It forces `Quantum::ADMITTED` through
`magnitude_in_range` and `Slots::ADMITTED` through `slot_in_range`, so what the
sentence should say is which two it does not force, and that is the pair the
section is about.

The second says each obligation fires exactly where the slot range's does. The
paragraph 68 lines above it, added by the previous round, says they have
different sets. The four sets are pairwise disjoint from the slot range's, so
the older sentence is the wrong one and comes out.
