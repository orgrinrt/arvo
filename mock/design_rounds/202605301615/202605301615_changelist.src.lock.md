**Date:** 2026-05-30
**Phase:** SRC
**Round:** 202605301615 bitmask-rustdoc-capacity-fixup

## Intent

Correct three arvo-bitmask rustdoc surfaces that still describe the pre-migration `BitMatrix<W, const N: Cap>` / `BitMatrix<W, N>` signature. They render to docs.rs and now misdescribe the migrated `BitMatrix<W, C: Capacity>` public shape. Comment-only edits; no code, no behaviour change.

## CHANGE: arvo-bitmask `lib.rs` module doc capacity signature
File: `mock/crates/arvo-bitmask/src/lib.rs`
The module doc `BitMatrix<W, const N: Cap>` becomes `BitMatrix<W, C: Capacity>`; the example `BitMatrix<Bits<64, Hot, Unsigned>, N>` becomes `BitMatrix<Bits<64, Hot, Unsigned>, C>`.
Verification: `! grep "BitMatrix<W, const N: Cap>" mock/crates/arvo-bitmask/src/lib.rs`.

## CHANGE: arvo-bitmask `dirty.rs` and `node.rs` doc capacity signature
File: `mock/crates/arvo-bitmask/src/dirty.rs`, `mock/crates/arvo-bitmask/src/node.rs`
The `BitMatrix<W, N>` rustdoc references become `BitMatrix<W, C>`.
Verification: `! grep "BitMatrix<W, N>" mock/crates/arvo-bitmask/src/dirty.rs mock/crates/arvo-bitmask/src/node.rs`.
