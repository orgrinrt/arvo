# Topic: the restructure proceeds, and widens

**Date:** 2026-07-29
**Phase:** TOPIC
**Status:** carries one decision, already made by op.

This records op's decision on the scope of the restructure. It sits in its own file rather than in
`202607281220_topic.the-ndim-and-shape-design.md`, where it was drafted, because that topic had
already committed and a committed topic is frozen. The decision is unchanged from the draft; only its
location moved.

## D14. The restructure proceeds, and widens to cover everything touching the same crates

**Decision (op, 2026-07-28).** D1 and D2 stand, argued on crate purpose rather than on necessity. The
"unsatisfiable rule" argument recorded in the ndim topic is **incorrect and withdrawn**: `Cap` is
declared at `arvo-storage/src/platform.rs:73`, not in the facade, so `arvo-tensor`'s rule was
satisfiable as written. Fifty of the fifty-one facade imports name a symbol already reachable from a
permitted dependency, and none of the seven crates imports `UFixed` or `IFixed`. See
`mock/research/202607281616_prior_art/07_in_stack_facade_dependency_audit.md`.

The round widens rather than splitting into overlapping passes over the same crates. It covers:

1. The four new crates of D2, and the facade becoming terminal.
2. The fifty-one facade imports, rewritten to name their declaring crate, plus rehoming the `Pred`
   family so the one genuine exception resolves.
3. The document disagreements the audit found: `mock/DESIGN.md.tmpl` contradicts itself about where
   `Cap` lives (line 43 says `arvo-tensor`, line 86 says the facade, and it is `arvo-storage`), and
   the layer labels differ between `mock/DESIGN.md.tmpl` and `mockspace.toml`, which lists seven
   entries for sixteen crates with "L2 substrate" four times.
4. The forbidden-gate migration of `202607282100_topic.remove-the-forbidden-feature-gates.md`, since
   it lands in `arvo-strategy` and the facade, which this round is already opening.

**The reason the two fold together cleanly:** the container projection's migration to typestate is the
same move `Capacity` already made, and `arvo-capacity` is one of the crates D2 creates. Lifting the
capacity foundation and removing the last const-expression-in-type-position are one piece of work seen
from two sides.

## What item 2 now depends on

"Rehoming the `Pred` family" was written when `Pred` looked like one misplaced trait family. It is
not. See `202607290100_topic.one-predicate-concept-implemented-twice.md`: arvo has two predicate
families that do not reference each other, and the rehoming question cannot be answered without
deciding which of them is the predicate concept. That topic carries the finding; this item waits on
it.
