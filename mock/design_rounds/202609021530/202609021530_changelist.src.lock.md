# Src changelist: no source changes

**Date:** 2026-09-02
**Phase:** SRC
**Doc changelist:** `202609021530_changelist.doc.lock.md`

## Current state

The doc round rewrote one paragraph in
`mock/crates/arvo-format/DESIGN.md.tmpl` so it carries what both sides of the
merge had established. `docs/FORMAT.md` follows by regeneration.

## Changes proposed

None. The paragraph describes behaviour the source already has, and the round
was opened because two branches wrote the same paragraph differently, not
because anything under it moved.

Both claims the paragraph makes were checked against source rather than assumed:

- Nothing normalises the sign and nothing reduces the ratio, and a phase comes
  back exactly as it went in. `mock/crates/arvo-format/src/format.rs:59` says
  the constructor is total and lossless on every pair, with `of(3, i64::MIN)`
  named as the case.
- A zero denominator is representable and is answered rather than refused.
  `mock/crates/arvo-format/src/format.rs:64` says it is not refused and not
  reinterpreted, that it names no position, and that `Format::ADMITTED` is where
  the coordinates are checked together.

## Verification

`grep -c 'Total, and lossless on every pair' mock/crates/arvo-format/src/format.rs`
returns one. `grep -c 'is not refused here and is not reinterpreted' mock/crates/arvo-format/src/format.rs`
returns one.
