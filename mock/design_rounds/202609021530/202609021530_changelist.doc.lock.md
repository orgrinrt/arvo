# Doc changelist: the phase paragraph carries both branches

**Date:** 2026-09-02
**Phase:** DOC
**Source topics:** `202609021530_topic.the-phase-paragraph-carries-two-branches-worth.md`

## Current state

`mock/crates/arvo-format/DESIGN.md.tmpl` has one paragraph on the phase
coordinate, and two branches wrote it differently. The trunk carries the sign
and reduction facts with the paragraph's old provenance framing still on it.
This branch carries the provenance dropped and the zero denominator corrected,
and it never learned the sign and reduction facts.

## Changes proposed

Rewrite the paragraph once so it states, in the voice this branch uses
throughout:

- The phase is one coordinate and one const, and `Phase` carries the numerator
  and denominator as a pair rather than as one host integer, so the halves are
  never unpacked in a const.
- The pair is kept as written. Nothing normalises the sign, nothing reduces the
  ratio, and a phase comes back exactly as it went in.
- Whether the ratio is a whole number, and which whole number it is, are both
  independent of the sign of the denominator, and no other reader consults it.
- The predicates below are total over the pair, the zero denominator that names
  no position included.

No sentence about what the crate used to do, what was enforced where, or which
round settled it. That is the whole point of the branch this lands on.

## Files

`mock/crates/arvo-format/DESIGN.md.tmpl`, one paragraph. `docs/FORMAT.md`
follows by regeneration and is not hand-edited.
