# Topic: fixed-point multiply PR review fixes

Round: 202606231218
Phase: TOPIC
Scope: arvo (tests/fixed_point_mul.rs)

## Current state

The fixed-point multiply round (`202606231001`, PR #79) landed `IFixed`/`UFixed` `*` rescaling +
fixed-point ONE, with guard tests. A senior PR review found a test-quality defect and coverage gaps.

## Changes proposed

Test-only fixes on the same branch (`feat/fixed-point-mul-widening`):

- Replace `i_mul_fixed_precise_saturates_on_overflow`: it passed raw 200 to an 8-bit-logical Precise type
  (out of logical range) and asserted the spuriously-saturated 2047. For in-range Precise operands the 2x
  container holds the product so no saturation fires; the test must use in-range values and assert the
  correct rescaled product (green-on-a-bad-test otherwise).
- Add signed negative-operand guards (the multiply had only non-negative coverage).
- Add catalogue reds (`#[ignore = "catalogue: ...; tracked #3"]`) for the genuine gaps: Precise
  logical-range saturation on true overflow, the deferred WideBits widen for >64-bit-logical widths, and
  the `I = 0` (Q0.F) `ONE` overflow edge. Tracked by task #3.

No source/behaviour change; the Hot/Cold/Warm in-range paths are correct as shipped.
