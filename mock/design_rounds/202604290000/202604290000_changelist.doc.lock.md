# Doc Changelist: Strategy::NAME debug-gating

**Date:** 2026-04-21
**Phase:** DOC
**Topic:** `202604290000_topic.strategy-name-debug-gate.md`

## `mock/crates/arvo/DESIGN.md.tmpl`

Replace paragraph at L37–45 that begins `` `&'static str` is permitted (used on `Strategy::NAME`). ``

New text: the sentence becomes "`&'static str` is debug-gated: `Strategy::NAME` carries `#[cfg(debug_assertions)]` so release builds drop the strings entirely. Strategy identity at runtime flows through `RANK: u8`; `NAME` is for diagnostics and tests only." The remaining sentences about the three const-generic accessors (`ibits_u8`, `fbits_u8`, `is_fractional`) keep their existing form.

## No other templates

`PRINCIPLES.md.tmpl` §127 "`Named::NAME` single source of truth" is a cross-stack design principle and is unchanged by this narrow round. The `&str` row in the Primitive-vocabulary table there reflects a broader stack decision on static strings; if that evolves it's a separate round.

`BACKLOG.md.tmpl` — no entry (this round closes a concrete drift, does not introduce deferred design).
