# Source changelist: none

**Date:** 2026-07-30
**Round:** the restructure and formalization round opened 2026-07-27 (`fe7f56e`)
**Scope:** no source changes.

## Comparison to deprecated changelist

Supersedes `202607300800_changelist.src.deprecated.md`, which was deprecated by `mock unlock` rather
than by any fault of its own. The unlock was needed because its companion doc changelist claimed the
round changed no documentation templates, which was false: six changed, and the corrected doc
changelist now records them.

**The source claim is unchanged and was correct in both.** Nothing under `mock/crates/*/src/` was
touched this round. The deprecated version stated that and this version states it again, with the
verification below.

Substantively there is no delta between the two beyond this section and a tightened verification
statement.

## Why this changelist is empty

The round was a design talk. Its decisions describe a restructure and a formalization that the next
round executes, and `202607300400` lists seven earlier calls the formalization proposal reopens or
changes in kind, so applying them against the pre-proposal picture would encode a superseded state.

The mockspace state machine reaches `CLOSED` only through a locked source changelist. This file walks
the round there honestly, rather than leaving it open or archiving it as abandoned, which it was not.

## What was written, and where it lives

Nothing under `mock/crates/*/src/`. Two sketches were written and run this round, and they live where
sketches live, outside the source tree:

| Sketch | Outcome |
|---|---|
| `mock/research/sketches/202607300500_format-as-exponent-function/` | WORKS. Three probes: the format concept compiles gate-free, const-evaluates under `const_trait_impl`, and a six-axis composition resolves to a concrete container with no type-position arithmetic |
| `mock/research/sketches/202607300600_law-markers-derived-over-composition/` | WORKS. Laws derive over the `(Overflow, Signedness)` pair, and the composition the mathematics forbids is refused with `error[E0277]` naming the exact pair |

Per `bench-and-sketch-discipline.md` these are feasibility checks with WORKS outcomes, not benchmarks,
and neither carries a timing claim.

## Verification

```
git diff --name-only fe7f56e^..HEAD -- 'mock/crates/*/src/*'
```

Returns nothing, which is the whole claim.

Every `.rs` file changed on this branch sits under `mock/research/sketches/`, confirmed by
`git diff --name-only dev...HEAD -- '*.rs'`, whose every entry is a sketch path.

## Files changed

None.
