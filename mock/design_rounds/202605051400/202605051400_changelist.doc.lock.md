**Date:** 2026-05-05
**Phase:** DOC
**Scope:** arvo (mock-root tmpls, every per-crate tmpl)
**Topic source files:**

- `202605051400_topic_doc_audit_2026_05_05.md`

# Round doc CL: documentation audit consolidation (arvo)

This CL applies the scope decisions from `202605051400_topic_doc_audit_2026_05_05.md` mechanically across every affected file. Findings are inherited from `mock/research/2026_05_05_doc_audit.md` (per-crate audit, ~95 findings) and `mock/research/2026_05_05_doc_audit_entrypoints.md` (entrypoint audit). Both research docs are the load-bearing source of file:line citations; this CL summarises the action per file rather than re-enumerating each citation.

This is a doc-only CL. Source touch is reserved for the follow-up src CL.

## Files touched in this round

### Mock-root templates

| Path | Action |
|------|--------|
| `mock/DESIGN.md.tmpl` | Strip 7+ round-id references (Decision 3); strip mockspace/cargo mock/.tmpl/design_rounds vocabulary at lines 115-116, 125, 131-136 (Decision 7); resolve internal contradictions on Warm cap (Decision 4) |
| `mock/PRINCIPLES.md.tmpl` | Strip 7+ round-id references (Decision 3); rewrite topology heading to drop the round id; rewrite the `### No .0 field-access on arvo primitives (round 202605021200)` heading to drop the round id |
| `mock/WORKFLOW.md.tmpl` | Verify rendering target per Decision 8; if Tier-1-rendered, rewrite body to consumer focus (state what consumers can do with the crate, version compatibility); if Tier-2-only, no edit |
| `README.md` | No edit. Repo-root README is clean per the entrypoint audit. |

### Per-crate templates (16 crates)

For each of the following crates, the doc CL touches the listed file(s) per the per-crate audit's enumeration. The action column collapses multiple finding categories into the high-level scope; reference the audit research file for line-by-line citations.

| Crate | Files touched | Action |
|-------|--------------|--------|
| `arvo` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl` | Resolve `MultiContainer → WideBits` drift; resolve `const N: Width → const N: u16` drift; remove deleted `Uint8`/`Int8`-family aliases from advertised surface; resolve internal contradictions on Warm cap (32/64/65535 → 128); fix `Width` newtype shape; strip `## Sources` block (Decision 2, move to `mock/research/arvo-history.md`); 1 substrate hit at L1288 (Decision per `vocabulary.md`) |
| `arvo-bitmask` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl` | Strip polka-dots cross-references at the BACKLOG entries (audit cites L1022-1041, L1079-1087, L1173-1179); strip `## Sources` block; round-id removal in body prose |
| `arvo-bits` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl` | `MultiContainer → WideBits` drift in narrative prose; `const N: Width` updates per audit; strip `## Sources` block; round-id removal |
| `arvo-bits-contracts` | `DESIGN.md.tmpl` | Doc-vs-source drift fixes per audit; strip `## Sources` block; round-id removal |
| `arvo-comb` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl` | Replace polka-dots `T3 decision #70` cross-references with the design rationale itself per Decision 5; same for the four other polka-dots cross-references; strip `## Sources` block; convert -ize to -ise (1 site per audit); round-id removal |
| `arvo-graph` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl` | Replace polka-dots `T5 L1044-1094`, `T6 L2490-2501`, `T6 L2710-2712`, `T3 L1694-1750` cross-references with the design rationale (Decision 5); strip `## Sources` block; round-id removal |
| `arvo-hash` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl` | Doc-vs-source drift fixes per audit; strip `## Sources` block; round-id removal |
| `arvo-mask-contracts` | `DESIGN.md.tmpl` | Doc-vs-source drift fixes per audit; strip `## Sources` block; round-id removal |
| `arvo-numeric-contracts` | `DESIGN.md.tmpl` | Doc-vs-source drift fixes per audit; strip `## Sources` block; round-id removal |
| `arvo-refit` | `DESIGN.md.tmpl` | Re-export gateway shape verification per audit; strip `## Sources` block; round-id removal (Renamed-from-arvo-narrow history goes to Tier 2 history file) |
| `arvo-sparse` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl` | Replace polka-dots `T3 L1581-1820`, `T3 L1629-1692`, `T3 L1694-1750`, `T3 decision #66-68` cross-references with the design rationale (Decision 5); resolve "clause-dev primitive-vocabulary discipline" line 24 leakage; strip `## Sources` block; convert -ize to -ise (per audit); round-id removal |
| `arvo-spectral` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl` | Doc-vs-source drift fixes per audit; strip `## Sources` block; convert -ize to -ise (per audit); round-id removal |
| `arvo-storage` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl` | Doc-vs-source drift fixes per audit; resolve cross-reference at line 599 (already pointed to `crates/arvo/DEEPDIVE_strategy-bound-trilemma.md.tmpl` per the prior cleanup; verify); strip `## Sources` block; round-id removal |
| `arvo-strategy` | `DESIGN.md.tmpl` | Verify `Width` newtype shape matches source per audit; strip `## Sources` block; round-id removal |
| `arvo-tensor` | `DESIGN.md.tmpl` | Doc-vs-source drift fixes per audit; strip `## Sources` block; round-id removal |
| `arvo-transparent` | `DESIGN.md.tmpl` | 1 substrate hit at L66 (Decision per `vocabulary.md`); strip `## Sources` block; round-id removal |

### Tier 2 history files

The doc CL also creates the following Tier 2 history files to receive the migrated `## Sources` blocks per Decision 2:

| Path | Origin |
|------|--------|
| `mock/research/arvo-history.md` | `arvo/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-bitmask-history.md` | `arvo-bitmask/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-bits-history.md` | `arvo-bits/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-bits-contracts-history.md` | `arvo-bits-contracts/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-comb-history.md` | `arvo-comb/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-graph-history.md` | `arvo-graph/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-hash-history.md` | `arvo-hash/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-mask-contracts-history.md` | `arvo-mask-contracts/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-numeric-contracts-history.md` | `arvo-numeric-contracts/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-refit-history.md` | `arvo-refit/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-sparse-history.md` | `arvo-sparse/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-spectral-history.md` | `arvo-spectral/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-storage-history.md` | `arvo-storage/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-strategy-history.md` | `arvo-strategy/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-tensor-history.md` | `arvo-tensor/DESIGN.md.tmpl` `## Sources` |
| `mock/research/arvo-transparent-history.md` | `arvo-transparent/DESIGN.md.tmpl` `## Sources` |

Each history file lands with the same content the original `## Sources` block carried, framed as a Tier 2 audit-trail file. The DESIGN.md.tmpl that the block was extracted from gets a one-line generic pointer at the bottom: `Round-by-round history is recorded in mock/research/`.

## Lock criteria

The doc CL is ready to lock when every row in the per-crate table has been applied, every Tier 2 history file has been created, the workspace `~/Dev/clause-dev/.shared/scripts/word-frequency.sh` tag-cloud reports zero hits on banned vocabulary across `mock/crates/**/*.md.tmpl` and `mock/{DESIGN,PRINCIPLES,WORKFLOW}.md.tmpl`, the Tier 1 leakage grep (per `documentation-writing.md`'s checklist) reports zero hits on the same set, and `cargo mock` regenerates `docs/*.md` cleanly from the updated templates.

## Verification per file

The mockspace `design-doc-source-mismatch` lint covers backticked-type-name verification automatically. The narrative-drift verification (Decision 4) is manual: for each affected DESIGN.md.tmpl, the reviewer reads the affected paragraph against the current source state in `crates/<crate>/src/`. The audit research file's "## Per-crate findings" section enumerates the specific drift items per crate; the lock-time review walks that list.

## Following src CL (mentioned, not written)

After this doc CL is locked and `cargo mock` regenerates the rendered docs, a separate src CL is opened against this topic file. The src CL covers:

1. Rustdoc on every `pub` item in `crates/**/src/**.rs` audited against the updated DESIGN.md.tmpl wording.
2. Final em-dash sweep across all `.rs` files (the writing-style enforcement covers `crates/**/*.rs` per scope).
3. Per-crate `Cargo.toml` `description` and `keywords` audit.

The src CL references this topic file by id and does not duplicate its decisions.
