# eleventh-review-claim-audit

review146k on PR #146 at 09e1041e found three blocking findings and five
catalogue items. This round fixes B1 to B3 and C1 to C4; C5 is op's and is
not touched.

B1. `crates/arvo-format/DESIGN.md.tmpl` renders into `docs/FORMAT.md`, which is
public, and names `mock/research/...` paths at 704, 1012 and 1050. C4 is the
same class at 1220, `cargo mock rounding-vocabulary`. Every internal-machinery
mention comes out of the rendered prose, stated without the path, since this
template has no portion that does not render.

B2. `MANIFEST_RENAME_IS_UNGUARDED.md` miscounts the dependents that build,
describes checks the arms do not hold, and says both that a check "would have
to refuse" the target tables and that whether they need it "is open". It is
rewritten so every statement about an arm is either the table `run.sh` writes
or one sentence checked against `run.out` and the arm's source. C2, the
`assert!` grant, is replaced by two arms naming `assert!` under the empty and
the bare stand-in prelude.

B3. Ten value-checking arms expected to build had no control of their own.
`run.sh` now derives one control per checking item of every arm expected to
build, with that item's comparison flipped, expected refused by that item
alone, and flags an arm that checks nothing. A self-check feeds the loop three
arms built to trip it. The two hand-written wrong-width siblings go, since the
generated controls cover them.

C1. The lint's module doc loses the clause contrasting with a runtime
assertion that no longer exists.

C3. The leading `::` is claimed to resist the whole second form, and the probe
had arms only for `use ... as core`. Two arms are added, an unaliased `use` of
`core` and a `self` under `core`, each with generated controls.

No canon row is touched.
