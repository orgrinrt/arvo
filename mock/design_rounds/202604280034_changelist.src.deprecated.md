**Date:** 2026-04-28
**Phase:** SRC-PLAN (becomes SRC on first source edit; living during SRC phase only)
**Round:** 202604280034. Audit-driven hygiene + Q-D completion + arvo-narrow-contracts merge + IFixed doc-fix
**Source topic:** `202604280034_topic.audit-driven-hygiene-and-Q-D-completion.md` (frozen at `dd732ce`)
**Locked doc CL:** `202604280034_changelist.doc.lock.md` (locked at `8b00bca`)

This src CL operationalises the doc CL and topic decisions into source mutations. Round A is mostly source-side cleanup; the substantive design work lands in subsequent rounds.

## Per-pass sequencing

1. **Pass 1: lint:allow format sweep**: rewrite every `// allow-bare-numeric: ...` malformed annotation to canonical `// lint:allow(no-bare-numeric) lint:allow(arvo-types-only) reason: ...; tracked: #N` form. Confirmed locations: `arvo/src/strategy.rs:47,58`. Sweep all of `mock/crates/` for the malformed prefix; expect ~2 occurrences per the pre-round grep.

2. **Pass 2: missing lint:allow annotations**: add canonical-form annotations on every line that the post-pass-1 audit shows as bare-primitive-without-suppression. Targets per the implementation audit:
   - `arvo/src/markers.rs:89`: `BoolLike::pack` `as u8` cast.
   - `arvo/src/ufixed_impl.rs:28-119`: `as u32` casts at BitPrim bridge call sites.
   - `arvo/src/ifixed_impl.rs:27-117`: same shape on the signed side.
   - `arvo/src/float.rs:28+36`: `Ieee::WIDTH: u8` algorithm-fixed constants.
   - `arvo-narrow-contracts/src/lib.rs:27`: `Narrowed<const N: u8, T>` const-generic position.
   - `arvo-hash/src/algo.rs:22`: `Hasher<const N: u8>` const-generic position.
   - `arvo-hash/src/fnv1a.rs:36`: `Fnv1a<const N: u8>` same.
   The annotations are post-merge (Pass 4); the arvo-narrow-contracts file annotation is moot once the file is deleted, so it lives temporarily on the merged location in `arvo-bits-contracts/src/lib.rs`.

3. **Pass 3: source-side doc comment fixes**: scoped to inline `///` and `//!` doc comments inside `.rs` files (these gate-route through SRC phase per mockspace, not DOC).
   - `arvo/src/aliases.rs`: doc comments on `Uint64<S>` and `Int64<S>` get the "no Warm default; pick Hot/Cold/Precise explicitly" ergonomic note. (Pass 4 deletes these aliases entirely; this pass becomes moot for them but covers any `Uint<N>` doc comment that needs the same note for the >32-bit case at the generic alias.)
   - `arvo-hash/src/fnv1a.rs:8-10` doc comment: replace the duplicated "N is u8 directly because nightly const-fn limits" paragraph with a one-line pointer to the canonical statement in `arvo-hash/src/algo.rs:18-21`. Keep the algo.rs version as canonical.

4. **Pass 4: std-parallel survivor deletion + arvo-narrow-contracts merge** (combined because both touch shared workspace state):
   - **Delete std-parallel canonicals from `arvo/src/aliases.rs`**: lines 86-94. Eight `pub type` deletions: `Uint8`, `Uint16`, `Uint32`, `Uint64`, `Int8`, `Int16`, `Int32`, `Int64`. Update module-level doc comment to drop the canonical-survivor framing.
   - **Update `arvo/src/lib.rs`** facade re-export: drop the eight aliases from the `pub use aliases::{...}` list. Verify with `cargo check`.
   - **Audit consumer code across all 16 crates** for any usage of the deleted aliases: grep for `\bUint8\b|\bUint16\b|\bUint32\b|\bUint64\b|\bInt8\b|\bInt16\b|\bInt32\b|\bInt64\b` under `mock/crates/*/src` and `mock/crates/*/tests`. Rewrite each hit to `Uint<N, S>` or `Int<N, S>` with explicit strategy parameter where needed (e.g. `Uint<64, Hot>` since Warm doesn't reach 64 bits). The audit found the only references were inside arvo's own files; verify in-round.
   - **Merge `arvo-narrow-contracts` into `arvo-bits-contracts`**:
     - Move `Narrow<T>` const trait declaration and `Narrowed<const N: u8, T>` alias from `arvo-narrow-contracts/src/lib.rs` to `arvo-bits-contracts/src/lib.rs`. Trait body identical; module doc references updated.
     - Apply the canonical-form `lint:allow` annotation on the `Narrowed<const N: u8, T>` declaration during the move (Pass 2 target).
     - Delete `mock/crates/arvo-narrow-contracts/` directory entirely (Cargo.toml, src/, README.md.tmpl, DESIGN.md.tmpl, BACKLOG.md.tmpl, anything else).
     - Update workspace `mock/Cargo.toml`: remove `arvo-narrow-contracts` from `[workspace.members]` and `[workspace.dependencies]`.
     - Update consumer Cargo.toml files: `arvo-narrow/Cargo.toml` (drops `arvo-narrow-contracts`, may add `arvo-bits-contracts` if not already present), `arvo/Cargo.toml` if it depends on arvo-narrow-contracts directly (verify), and any other crate that listed it.
     - Update consumer `use` statements: every `use arvo_narrow_contracts::Narrow` (or `Narrowed`) → `use arvo_bits_contracts::Narrow`.
     - Update `mock/mockspace.toml`: drop `arvo-narrow-contracts` from `[primitive-introductions]` and any `[lints.forbidden-imports]` scope tables that name it. The `arvo-bits-contracts` scope already covers what arvo-narrow-contracts had.
     - Verify `cargo check` clean post-merge.

5. **Pass 5: source fixes per the implementation audit**:
   - **arvo-hash unused dep**: drop `arvo-bits.workspace = true` from `arvo-hash/Cargo.toml`. Verify all `use arvo_bits::...` references are absent from arvo-hash source (grep). The arvo-hash imports go through `arvo::Bits` / `arvo::Hot` per the doc CL.
   - **Mask<W> impls**: DEFERRED to a follow-up round. Initial scoping assumed mechanical delegation, but the `arvo-mask-contracts::Mask<const W: u8>` trait is declared `pub const trait`, requiring `impl const` impls. Mask64/Mask256 inherent methods are non-const today (`insert`/`remove` take `&mut self`), and the trait surface includes `full()` and `mask_for_width(n)` which have no inherent equivalent. Adding the impls requires either (a) const-converting the inherent methods first, or (b) writing parallel const-fn impls. Both are out of Round A's mechanical-hygiene scope. Tracked in arvo's BACKLOG.md.tmpl as a separate follow-up item; the doc CL claim that "Mask64/Mask256 implement Mask<W>" stands as a design commitment that the source ships in the follow-up.
   - **`_ColdReserved` cleanup**: in `arvo-strategy/src/widen.rs:224`, remove `#[allow(dead_code)] type _ColdReserved = Cold;` plus the now-unused `Cold` import in this file. Add a BACKLOG entry; Cold widen/narrow is planned per `arvo-toolbox-not-policer.md`.
   - **arvo-narrow re-export**: in `arvo-narrow/src/lib.rs`, add `pub use arvo_bits_contracts::Narrow;` (post-merge import path) so the crate is no longer empty per `[no-empty-crate]`.

6. **Pass 6: validation gauntlet**:
   - `cargo check --workspace` clean.
   - `cargo test --workspace` all passing.
   - `cargo mock --lint-only --commit/--push/--build` clean (no regressions on the 5 informational warnings; `[no-empty-crate]` on arvo-narrow should resolve via the re-export in Pass 5).
   - `lint:allow` audit: every annotation on the offending line, canonical-form prefix, has reason + tracked.
   - `unsafe` block audit: no new `unsafe` introduced; existing `transmute_copy` SAFETY comments unchanged.
   - Trybuild stderr re-bless if needed (no expected change for this round).
   - Inspect `arvo-narrow-contracts` directory absence: confirmed deleted.
   - Inspect post-merge `arvo-bits-contracts` exposes `Narrow` and `Narrowed`.

## Per-file mutation table

| File | Pass | Mutation |
|---|---|---|
| `mock/crates/arvo/src/strategy.rs:47,58` | 1 | rewrite malformed `allow-bare-numeric:` to canonical |
| `mock/crates/arvo/src/markers.rs:89` | 2 | add canonical lint:allow on BoolLike pack `as u8` |
| `mock/crates/arvo/src/ufixed_impl.rs:28-119` | 2 | add canonical lint:allow on each `as u32` cast |
| `mock/crates/arvo/src/ifixed_impl.rs:27-117` | 2 | same on signed side |
| `mock/crates/arvo/src/float.rs:28,36` | 2 | add canonical lint:allow on `Ieee::WIDTH: u8` |
| `mock/crates/arvo-hash/src/algo.rs:22` | 2 | add canonical lint:allow on `Hasher<const N: u8>` |
| `mock/crates/arvo-hash/src/fnv1a.rs:36` | 2 | same on `Fnv1a<const N: u8>` |
| `mock/crates/arvo-hash/src/fnv1a.rs:8-10` | 3 | dedup duplicate doc paragraph |
| `mock/crates/arvo/src/aliases.rs:86-94` | 4 | delete eight std-parallel `pub type` aliases |
| `mock/crates/arvo/src/aliases.rs:1-15` (module doc) | 4 | drop canonical-survivor framing |
| `mock/crates/arvo/src/lib.rs` | 4 | prune `pub use aliases::{...}` re-export list |
| `mock/crates/arvo-bits-contracts/src/lib.rs` | 4 | add `Narrow<T>` const trait + `Narrowed` alias from merged crate; canonical lint:allow on `Narrowed<const N: u8, T>` |
| `mock/crates/arvo-narrow-contracts/` | 4 | delete entire directory |
| `mock/Cargo.toml` | 4 | remove arvo-narrow-contracts from members + dependencies |
| `mock/crates/arvo-narrow/Cargo.toml` | 4 | drop arvo-narrow-contracts dep |
| `mock/crates/arvo-narrow/src/lib.rs` | 4 + 5 | update Narrow imports to arvo-bits-contracts; add `pub use arvo_bits_contracts::Narrow;` re-export |
| `mock/crates/arvo/Cargo.toml` (if applicable) | 4 | drop arvo-narrow-contracts dep |
| `mock/mockspace.toml` | 4 | remove arvo-narrow-contracts from primitive-introductions + forbidden-imports scopes |
| `mock/crates/arvo-hash/Cargo.toml` | 5 | drop unused arvo-bits dep |
| `mock/crates/arvo-bitmask/src/mask.rs` | 5 | add `impl Mask<64> for Mask64` + `impl Mask<256> for Mask256` (delegating to inherent methods) |
| `mock/crates/arvo-strategy/src/widen.rs:224` | 5 | remove `_ColdReserved` workaround + unused Cold import; BACKLOG entry for planned Cold widen/narrow impls |
| `mock/crates/arvo/BACKLOG.md.tmpl` (or arvo-strategy BACKLOG) | 5 | new entry: planned Cold widen/narrow impls (DOC CL already added IFixed reshape and other Round B-E entries) |

Pass 6 is the validation gauntlet; runs over the cumulative effect of passes 1-5. No file mutations beyond bug fixes that gauntlet surfaces.

## Mockspace-side caveat

Per task #267, `cargo mock` full pipeline crashes on dep-graph generation. Doc regeneration (`cargo mock` without `--lint-only`) is blocked, which means `.claude/CLAUDE.md` will not regenerate from the round 202604280034 MAIN.md.tmpl edit. The lint-gate validation (`cargo mock --lint-only --commit/--push/--build`) works fine and is the load-bearing check for this round.

Acceptable for this round: rendered CLAUDE.md stays stale until the mockspace fix lands. The round closes regardless because the source-of-truth (the `.tmpl` files) is updated correctly.

## Sources

- Topic file: `202604280034_topic.audit-driven-hygiene-and-Q-D-completion.md` (frozen at `dd732ce`).
- Locked doc CL: `202604280034_changelist.doc.lock.md` (locked at `8b00bca`).
- Implementation audit (agent `a980788fd4c05e8c0`).
- Workspace rules: `arvo-always-optimal-internals.md`, `arvo-toolbox-not-policer.md`, `arvo-compile-time-last.md`, `no-bare-primitives.md`, `mockspace-toml-edits.md`.

This src CL is living during SRC phase only. Lock with `cargo mock lock` after pass 6 passes; close the round with `cargo mock close`.
