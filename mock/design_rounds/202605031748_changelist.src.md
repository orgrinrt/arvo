**Date:** 2026-05-03
**Phase:** SRC
**Round:** 202605031748
**Scope:** Source-side execution against the locked doc CL.

# Round 202605031748 — src CL

The locked doc CL specifies the design surface. This CL is the
mechanical execution plan that brings source into alignment with
docs, organised by crate.

The structured `## CHANGE:` blocks track per-symbol movements per
`cl-claim-sketch-discipline.md`. Each block names the verification
that confirms the change landed.

## arvo-bits-contracts

### CHANGE: trait `BitPrim` ADDS method `mask_low(n: USize) -> Self`

File: `mock/crates/arvo-bits-contracts/src/lib.rs`
Verification: `grep -n "fn mask_low" mock/crates/arvo-bits-contracts/src/lib.rs` returns at least one match for `BitPrim`.

Default body composes through `Identity::ZERO`, `Bounded::MAX`,
`Identity::ONE`, and the const-stable shift bridge.

### CHANGE: trait `IBitPrim` ADDS method `mask_low(n: USize) -> Self`

File: `mock/crates/arvo-bits-contracts/src/lib.rs`
Verification: `grep -n "fn mask_low" mock/crates/arvo-bits-contracts/src/lib.rs` returns at least two matches.

### CHANGE: per-primitive macros `impl_bit_prim_u!` and `impl_bit_prim_i!` ADD `mask_low` body

File: `mock/crates/arvo-bits-contracts/src/lib.rs`
Verification: `cargo check -p arvo-bits-contracts` succeeds; the per-primitive impls of `mask_low` exist for `u8`/`u16`/`u32`/`u64`/`u128` and `i8`/`i16`/`i32`/`i64`/`i128`.

### CHANGE: `WideBits<BYTES, A>` adds `mask_low(n: USize)` to its BitPrim impl

File: `mock/crates/arvo-bits-contracts/src/widebits.rs`
Verification: `grep -n "fn mask_low" mock/crates/arvo-bits-contracts/src/widebits.rs` returns one match.

## arvo-strategy

### CHANGE: free fn `mask_low_bits(n: u16) -> u64` REMOVED

File: `mock/crates/arvo-strategy/src/width.rs`
Verification: `! grep -q "fn mask_low_bits" mock/crates/arvo-strategy/src/width.rs`.

Round 4 introduced the helper as an interim shape; Round 3 retires
it in favor of per-primitive `BitPrim::mask_low`.

### CHANGE: `Ieee::WIDTH` typed FROM `u16` TO `Width`

File: `mock/crates/arvo-strategy/src/ieee.rs`
Verification: `grep -n "const WIDTH: Width" mock/crates/arvo-strategy/src/ieee.rs` returns at least one match; `! grep -q "const WIDTH: u16" mock/crates/arvo-strategy/src/ieee.rs`.

### CHANGE: `impl const Ieee for f32` and `f64` use `Width::from_u16`

File: `mock/crates/arvo-strategy/src/ieee.rs`
Verification: `grep -n "Width::from_u16(32)\|Width::from_u16(64)" mock/crates/arvo-strategy/src/ieee.rs` returns two matches.

## arvo-mask-contracts

### CHANGE: trait `Mask` RENAMED TO `MaskOps`

File: `mock/crates/arvo-mask-contracts/src/lib.rs`
Verification: `grep -n "pub const trait MaskOps" mock/crates/arvo-mask-contracts/src/lib.rs` returns one match; `! grep -q "pub const trait Mask<" mock/crates/arvo-mask-contracts/src/lib.rs`.

### CHANGE: `MaskOps<const W: u16>` LIFTED TO `MaskOps<const W: Width>`

File: `mock/crates/arvo-mask-contracts/src/lib.rs`
Verification: `grep -n "MaskOps<const W: Width>" mock/crates/arvo-mask-contracts/src/lib.rs` returns one match.

## arvo-bitmask

### CHANGE: struct `Mask256` REMOVED

File: `mock/crates/arvo-bitmask/src/mask.rs`
Verification: `! grep -q "pub struct Mask256" mock/crates/arvo-bitmask/src/mask.rs`.

### CHANGE: type alias `Mask64` REMOVED

File: `mock/crates/arvo-bitmask/src/mask.rs`
Verification: `! grep -q "pub type Mask64" mock/crates/arvo-bitmask/src/mask.rs`.

### CHANGE: `Mask<W>::empty()` ROUTES through `<Self as Identity>::ZERO`

File: `mock/crates/arvo-bitmask/src/mask.rs`
Verification: `grep -n "fn empty.*Identity::ZERO\|Identity>::ZERO" mock/crates/arvo-bitmask/src/mask.rs` returns at least one match in the chassis impl.

### CHANGE: struct `BitMatrix64<const N: Cap>` REMOVED

File: `mock/crates/arvo-bitmask/src/matrix.rs`
Verification: `! grep -q "pub struct BitMatrix64" mock/crates/arvo-bitmask/src/matrix.rs`.

### CHANGE: struct `BitMatrix256<const N: Cap>` REMOVED

File: `mock/crates/arvo-bitmask/src/matrix.rs`
Verification: `! grep -q "pub struct BitMatrix256" mock/crates/arvo-bitmask/src/matrix.rs`.

### CHANGE: struct `BitMatrix<W, const N: Cap>` ADDED

File: `mock/crates/arvo-bitmask/src/matrix.rs`
Verification: `grep -n "pub struct BitMatrix<W" mock/crates/arvo-bitmask/src/matrix.rs` returns one match.

### CHANGE: free fn `propagate_dirty<W, const N: Cap>` REPLACES `propagate_dirty_64` and `propagate_dirty_256`

File: `mock/crates/arvo-bitmask/src/dirty.rs`
Verification: `! grep -q "fn propagate_dirty_64\|fn propagate_dirty_256" mock/crates/arvo-bitmask/src/dirty.rs && grep -q "pub fn propagate_dirty<W" mock/crates/arvo-bitmask/src/dirty.rs`.

### CHANGE: `SetBitsIter<W>` REPLACES `SetBitsIter64` and `SetBitsIter256`

File: `mock/crates/arvo-bitmask/src/ops.rs`
Verification: `! grep -q "SetBitsIter64\|SetBitsIter256" mock/crates/arvo-bitmask/src/ops.rs && grep -q "pub struct SetBitsIter<W>" mock/crates/arvo-bitmask/src/ops.rs`.

### CHANGE: set-ops impls FROM per-flavour inherents TO single generic `impl<W> Mask<W>`

File: `mock/crates/arvo-bitmask/src/ops.rs`
Verification: source compiles; ops methods (`union`, `intersection`, `difference`, `complement`, `is_empty`, `intersects`, `contains`, `insert`, `remove`, `count`) live in one `impl<W>` block (or one per trait-bound family if where-clauses force splitting).

### CHANGE: `arvo_mask_contracts::MaskOps<{ Width::from_u16(64) }>` IMPL ADDED for `Mask<Bits<64, Hot, Unsigned>>`

File: `mock/crates/arvo-bitmask/src/mask_ops_impl.rs` (new) or in `mask.rs`.
Verification: `grep -n "impl const MaskOps<" mock/crates/arvo-bitmask/src/` returns at least one match for the W=64 instantiation.

### CHANGE: `arvo_mask_contracts::MaskOps<{ Width::from_u16(256) }>` IMPL ADDED for `Mask<Bits<256, Hot, Unsigned>>`

Same file/area as W=64 impl.
Verification: `grep -n "MaskOps<{ Width::from_u16(256) }>" mock/crates/arvo-bitmask/src/` returns at least one match.

### CHANGE: re-exports in `lib.rs` updated to remove `Mask256`, `Mask64`, `BitMatrix64`, `BitMatrix256`, `propagate_dirty_64`, `propagate_dirty_256`, `SetBitsIter64`, `SetBitsIter256`

File: `mock/crates/arvo-bitmask/src/lib.rs`
Verification: `! grep -E "Mask256|Mask64|BitMatrix64|BitMatrix256|propagate_dirty_64|propagate_dirty_256|SetBitsIter64|SetBitsIter256" mock/crates/arvo-bitmask/src/lib.rs`.

## arvo-graph

### CHANGE: every `BitMatrix64<N>` use site renamed to `BitMatrix<Bits<64, Hot, Unsigned>, N>`

Files: `mock/crates/arvo-graph/src/topo.rs`, `mock/crates/arvo-graph/src/rank.rs`, `mock/crates/arvo-graph/src/components.rs`, `mock/crates/arvo-graph/src/path.rs`, `mock/crates/arvo-graph/src/spanning.rs`, `mock/crates/arvo-graph/src/waist.rs`, `mock/crates/arvo-graph/src/lib.rs`.
Verification: `! grep -rn "BitMatrix64\|Mask64" mock/crates/arvo-graph/src/`.

### CHANGE: every `Mask64` use site renamed to `Mask<Bits<64, Hot, Unsigned>>`

Same files as above. Verification covered.

## arvo-sparse

### CHANGE: every `BitMatrix64<N>` / `Mask64` use site renamed

Files: `mock/crates/arvo-sparse/src/rcm.rs`, `mock/crates/arvo-sparse/src/block.rs`, `mock/crates/arvo-sparse/src/dm.rs`, `mock/crates/arvo-sparse/src/lib.rs`.
Verification: `! grep -rn "BitMatrix64\|Mask64\|BitMatrix256\|Mask256" mock/crates/arvo-sparse/src/`.

## arvo-spectral

### CHANGE: every `Mask64` use site renamed

Files: `mock/crates/arvo-spectral/src/fiedler.rs`, `mock/crates/arvo-spectral/src/partition.rs`, `mock/crates/arvo-spectral/src/lib.rs`.
Verification: `! grep -rn "Mask64\|Mask256" mock/crates/arvo-spectral/src/`.

## arvo-hash

### CHANGE: `fnv1a.rs` `Hasher::finalize` ROUTES through `BitPrim::mask_low`

File: `mock/crates/arvo-hash/src/fnv1a.rs`
Verification: `! grep -q "1u64 << \|u64::MAX" mock/crates/arvo-hash/src/fnv1a.rs` (at the mask construction sites; the const fn helpers remain free of the bare-primitive shift).

### CHANGE: `xxhash3.rs` `Hasher::finalize` ROUTES through `BitPrim::mask_low`

File: `mock/crates/arvo-hash/src/xxhash3.rs`
Verification: same shape; the inline `1u64 << N` / `u64::MAX` patterns retire from `finalize`.

### CHANGE: imports drop `arvo_strategy::width::mask_low_bits`

Files: `mock/crates/arvo-hash/src/fnv1a.rs`, `mock/crates/arvo-hash/src/xxhash3.rs`.
Verification: `! grep -q "mask_low_bits" mock/crates/arvo-hash/src/`.

## arvo (facade)

### CHANGE: `bitfield.rs` mask construction ROUTES through `BitPrim::mask_low`

File: `mock/crates/arvo/src/bitfield.rs`
Verification: `! grep -q "1u64 << " mock/crates/arvo/src/bitfield.rs` (at mask sites; allow other shifts).

## arvo-bitmask tests

### CHANGE: every `Mask64` / `Mask256` / `BitMatrix64` / `BitMatrix256` reference renamed to chassis form

Files: `mock/crates/arvo-bitmask/tests/*.rs`.
Verification: `! grep -rn "Mask64\|Mask256\|BitMatrix64\|BitMatrix256" mock/crates/arvo-bitmask/tests/`.

## arvo-graph / arvo-sparse / arvo-spectral tests

### CHANGE: same rename pattern in tests

Files: `mock/crates/{arvo-graph,arvo-sparse,arvo-spectral}/tests/*.rs`.
Verification: `! grep -rn "Mask64\|Mask256\|BitMatrix64\|BitMatrix256" mock/crates/{arvo-graph,arvo-sparse,arvo-spectral}/tests/`.

## Pre-lock verification

Before running `cargo mock lock` on this src CL:

- `cargo +nightly check --workspace` passes.
- `cargo +nightly test --workspace` passes.
- Every `## CHANGE:` block's verification command produces the
  expected outcome.
- `cargo mock --lint-only --commit` is clean at error severity.

## Cross-references

- Doc CL: `202605031748_changelist.doc.lock.md`.
- Topic file: `202605031748_topic_round_3_mask_bits_generic.md`.
- Round 4 (`202605031548`): the round that added `mask_low_bits` to
  `arvo-strategy`. This round retires it in favor of per-primitive
  `BitPrim::mask_low`.
- Audit findings F1, F7, F9, F10, F11, F12 closed by this round.
