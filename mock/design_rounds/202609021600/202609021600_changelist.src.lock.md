# SRC changelist: no source changes

The round resolved a merge across four documents. Two are authored templates and
two are generated from them, so nothing under `src/` is this round's to touch.

The merge itself carries source, because the trunk landed two rounds of its own
while this branch was out: the ratio coordinate under `apply/tests/` and the
paragraph corrections it came with. Those are the trunk's changes under the
trunk's own locked changelists, and they arrived through the merge without a
conflict. Restating them here would claim them for a round that did not make
them.

## CHANGE: no Rust source changes in this round
Files: none
Verification: `git diff --stat origin/dev HEAD -- '*.rs'` is empty, so every Rust file on this branch is byte-identical to the trunk's and the round contributed none of them. Measuring against the branch's own parent would count the trunk's rounds as this one's, which is the reading to avoid.

The one thing worth checking is that the resolved templates still describe the
source the merge brought in, since that is where the two halves could disagree.
The phase paragraph is the case: the template now says `Phase::of` takes the pair
as written and that the refusal sits on `Phase::denotes` and `Format::ADMITTED`,
and `mock/crates/arvo-format/src/format.rs` says the same. That is the trunk's
version of the paragraph, kept for exactly this reason.

Verification: `grep -n 'pub const fn of' mock/crates/arvo-format/src/format.rs` shows a total constructor with no refusal in it, and `grep -n 'denotes' mock/crates/arvo-format/src/format.rs` shows the predicate the template names.
