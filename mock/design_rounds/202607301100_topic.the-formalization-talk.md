# Topic: the formalization talk

**Date:** 2026-07-30
**Phase:** TOPIC
**Status:** live transcript. Appended as each call is made, not summarised afterward.

The round's subject: arvo's primitives become named compositions over one format concept, with the
number systems and the derived algebraic laws underneath. The inherited state is
`202607301000_topic.inherited-state-from-the-formalization-round.md`, which carries the previous
round's 52 decisions with their provenance markings and both sketch findings, and is the only other
file in this round.

This file is the second and last. Every call lands here in the turn it is made.

## Research: what the documents currently say

Recorded before any question is put, because the answer to "what do the docs say" turned out to be
the first finding.

**The shipped documents describe the pre-restructure taxonomy and nothing else.** `mock/DESIGN.md.tmpl`
lists fifteen crates in a layer table: `arvo-transparent` and `arvo-strategy` at L0a, `arvo-storage` at
L0, three contracts crates at L0.5, four crates at L1 including `arvo-refit`, four at L2, one at L3.

None of the previous round's decisions appear. `arvo-capacity`, `arvo-shape`, `arvo-numeric`,
`arvo-geom`, `arvo-algebra-contracts`, `arvo-algebra`, `arvo-num-systems`, `arvo-platform`,
`arvo-container`, `arvo-bitfield`, `arvo-float` and `arvo-pseudorand` are absent. `arvo-storage` and
`arvo-refit`, both of which D27 and D28 dissolve, are present and documented as current.

So the documents do not answer this round's questions, and they do not contradict its decisions
either. They describe a state the previous round decided to leave behind and never wrote down. The
doc changelist for this round is therefore large by construction.

**`arvo-strategy`'s documented identity is narrower than what it does.** Its DESIGN says it is "the
strategy-marker and container-projection layer" and defines strategy as "a compile-time tag that picks
the underlying primitive container for a given bit width". Its README repeats the container framing.

The prior round's research established the markers actually bundle three axes: container width policy,
overflow policy, and intermediate-precision policy. D34 had to prise the third apart by necessity to
establish what `Warm` means above 64 bits. So the document describes one of the three things the
marker does, which is why `202607300400` listed the crate's identity as reopened.

## The open questions

Compiled from the inherited file rather than from any recorded agenda, because
`202607292100` records that trusting a recorded agenda as the open list is what produced two wasted
questions in a single sitting. Every row below was checked against the inherited file's own decision
record first, so a question that a prior call already answered does not appear here. Four rows (A1, A3,
B2, C3) are not carried forward from any earlier file; they surfaced while reading the two sketches
against the proposal and are stated with their derivation in their own sections.

Each row has a section below it, in order. The sections are scaffolding at the time of writing: each
carries a status line that the call replaces when it lands. Nothing already recorded is rewritten, only
the placeholders are filled, which is the shape this file was opened to have.

| ID | Question | What it gates |
|----|----------|---------------|
| A1 | Whether one `Format` can express a denominator of `2^F - 1`, or whether UNORM sits outside the format family | The format's parameterisation, D36, the membership table |
| A2 | `Fixed<F>` plus `Floating<PREC, U>` with three underflow policies, against Flocq's four flat formats | The format instance list, the underflow axis |
| A3 | The rounding instance set, and where `Stochastic` lives given it needs a generator | The rounding axis, a possible dependency cycle |
| A4 | Which of the four missing overflow instances ship, and whether `Trap` makes the operation fallible | The overflow axis, every arithmetic signature |
| A5 | The growth instance set, and whether growth selects its own intermediate container | The growth axis, D34's 2x rule, the projection |
| A6 | The axis set as a whole, ratified or amended | Everything below |
| B1 | The composition type: name, parameter list, home | The whole of layer 4 |
| B2 | Whether `UFixed<I, F, S>` stays three parameters once rounding is an axis | D48, every consumer call site |
| B3 | What a preset is mechanically: alias over a composition, or a marker projecting the axes | `Hot` / `Warm` / `Cold` / `Precise`, D52 |
| C1 | `arvo-strategy`'s identity and possibly its name | The crate graph |
| C2 | Whether the format concept gets its own crate, its name, its layer | The crate graph |
| C3 | Dependency direction between the format crate and `arvo-num-systems` | The crate graph, D39 |
| C4 | What `arvo-numeric` is once definitions move to the format layer | D2's crate list |
| C5 | Whether `IntegerLike` and its siblings survive once derivable from the format | D32, D33 |
| C6 | Whether the representational-limits contract is declared or derived, and where | D45 |
| D1 | Whether the law table is hand-written per pair or derived from a statement about clamping | D51, the second sketch |
| D2 | Multiplication, rounding, and distributivity: the scope of the next sketch | D47's per-rung obligation |
| D3 | Whether the ladder's rungs are named now or grown as they are needed | `arvo-algebra-contracts` |
| E1 | The name of notko's truth contract | D17, the predicate work |
| E2 | Whether `Combine<Op>` is simply `Semigroup<Op>` | `arvo-algebra-contracts` |
| E3 | Constant-time execution as an axis, or not an axis | D49's untouched half |
| E4 | Whether the meta-newtypes compose at the format's positions | The sketches' one unestablished item |
| E5 | The curve research lead | D11, nothing in this round |

### A1. The scale question, whether a format can have a denominator of `2^F - 1`

**Status:** open.

### A2. The format decomposition and the underflow axis

**Status:** open.

### A3. The rounding instance set, and where `Stochastic` can live

**Status:** open.

### A4. The overflow instance set, and what `Trap` costs a signature

**Status:** open.

### A5. The growth instance set, and the intermediate container

**Status:** open.

### A6. The axis set, ratified

**Status:** open.

### B1. The composition type

**Status:** open.

### B2. The public spelling once rounding is an axis

**Status:** open.

### B3. What a preset is mechanically

**Status:** open.

### C1. `arvo-strategy`'s identity and name

**Status:** open.

### C2. The format crate

**Status:** open.

### C3. Format against number systems, which way the dependency runs

**Status:** open.

### C4. What `arvo-numeric` becomes

**Status:** open.

### C5. The marker family, derived or declared

**Status:** open.

### C6. The representational-limits contract

**Status:** open.

### D1. Whether the law table is derived from clamping

**Status:** open.

### D2. Multiplication, rounding, and distributivity

**Status:** open.

### D3. How deep the ladder is named now

**Status:** open.

### E1. The truth contract's name

**Status:** open.

### E2. `Combine<Op>` against `Semigroup<Op>`

**Status:** open.

### E3. Constant-time execution

**Status:** open.

### E4. The meta-newtypes at format positions

**Status:** open.

### E5. The curve research lead

**Status:** open.
