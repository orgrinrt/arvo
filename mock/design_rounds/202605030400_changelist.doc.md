# Round 202605030400 — Doc Changelist (corrected scope)

**Date:** 2026-05-03
**Phase:** DOC
**Source topics:**
- `202605030400_topic_round_2_typed_width_and_ieee_relocation.md` (original; Deliverables A and B carry forward; Deliverable C dropped)
- `202605030500_topic_round_2_scope_correction.md` (correction record)

**Supersedes:** `202605030400_changelist.doc.deprecated.md` (which spanned Deliverables A + B + C; deprecated 2026-05-03 because Deliverable C added split-macro and BitsView-trait surface area for marginal value).

## Comparison to deprecated changelist

The deprecated CL covered three deliverables:

- A: Ieee relocation (kept)
- B: LOGICAL_WIDTH internals (kept)
- C: Width / IBits / FBits / MetaCarrier relocation from arvo-storage to arvo-strategy (dropped)

The active CL covers Deliverables A and B only. Width and friends stay in
arvo-storage. Round 3 (#313) reconsiders the relocation if the
`Bits<const N: Width>` lift requires it.

The deprecated CL named several substrate items in the context of
Deliverable C. Each carries forward unchanged into Round 2 because the
relocation is dropped:

- `meta_bits_wrapper!` macro: stays in arvo-storage. No split. Continues
  to emit the full surface (type def + Bounded + Identity + ConstX
  bridges + Deref/AsRef/From + helper const-fn).
- `Width::W64` constant: stays as an inherent constant on `Width` in
  arvo-storage. Unchanged.
- `Width::const_add` const-trait `Add` op: not introduced in this round.
  LOGICAL_WIDTH bodies use a free-fn helper instead (the const-trait
  `Add` lift waits for Round 3 alongside the broader bare-`u16` to
  typed-`Width` sweep).
- `ZERO` / `ONE` constants on the meta-newtypes: stay as inherent
  constants in arvo-storage. Unchanged.
- `impl_bounded_identity_f!` macro: stays in arvo-strategy. The Ieee
  relocation rides alongside this macro since both target f32 / f64;
  the macro itself is unchanged.
- `BitsView` lift trait: not introduced. The `as_bits()` accessor stays
  as an inherent method on the meta-newtypes in arvo-storage where
  Bits is reachable.

## arvo-strategy/DESIGN.md.tmpl

### New section: "Ieee trait (relocated from arvo facade)"

Insert a new top-level section after `## Resolve` (currently at line 433 in
the source template) and before `## Const-trait arithmetic surface`.

Section content:

- One-paragraph framing: `Ieee` is the IEEE float-width seal. Relocates this
  round (202605030400) to live alongside its supertraits `[const] Identity`
  and `[const] Bounded`, both already shipping from `arvo-strategy::arith`.
  The previous home at `arvo/src/float.rs` left the trait declaration one
  layer above its supertraits with no need for the higher-layer placement.

- Canonical signature in a Rust code fence:

  ```rust
  pub const trait Ieee:
      sealed::Sealed + Copy + Default + PartialEq + PartialOrd
      + [const] Identity + [const] Bounded + 'static
  {
      const WIDTH: u16;
  }

  impl const Ieee for f32 { const WIDTH: u16 = 32; }
  impl const Ieee for f64 { const WIDTH: u16 = 64; }
  ```

- Companion trait note: `FromU8Ieee` rides along, same orphan-rule
  constraints, same logical family. Show its const-trait shape:

  ```rust
  pub const trait FromU8Ieee {
      fn from_u8_ieee(n: u8) -> Self;
  }

  impl const FromU8Ieee for f32 { fn from_u8_ieee(n: u8) -> Self { n as f32 } }
  impl const FromU8Ieee for f64 { fn from_u8_ieee(n: u8) -> Self { n as f64 } }
  ```

- Seal commitment paragraph: the seal admits `f32` and `f64` today. Future
  widenings (`f16` / `bf16` / `f128`) extend the seal in this file plus the
  existing `impl_bounded_identity_f!` macro invocation in
  `arvo-strategy::arith`. The seal lives in a private `sealed` module with
  marker impls for the admitted types only.

- WIDTH typing note: `WIDTH: u16` stays bare-primitive-typed for Round 2.
  Round 3 (#313) lifts it to `Width` once the broader bare-`u16` to typed
  `Width` substrate sweep cascades through the bridge family.

- Re-export note: arvo facade re-exports `Ieee` and `FromU8Ieee` from
  arvo-strategy so consumer imports through `arvo::Ieee` and
  `arvo::FromU8Ieee` keep working.

## arvo/DESIGN.md.tmpl (facade)

### Section: existing "Float wrappers" / `Ieee` block

Locate the existing block (currently around line 985 to 1003 in the source
template) that documents the inline `Ieee` declaration with the
`pub const trait Ieee: ...` code fence and the `impl const Ieee for f32`
/ `impl const Ieee for f64` rows.

Replace the inline declaration block with a re-export note:

- One-paragraph note: as of round 202605030400, `Ieee` lives in
  `arvo-strategy::ieee` co-located with its `[const] Identity + [const]
  Bounded` supertraits. The arvo facade re-exports `Ieee` and `FromU8Ieee`
  unchanged so consumer paths through `arvo::Ieee` keep working.
- Keep the seal-commitment paragraph from the prior block, repointed at the
  new home (`arvo-strategy::ieee`).
- Keep the `Identity` / `Bounded` impl notes for `FastFloat<F>` and
  `StrictFloat<F>` since those impls remain at the facade layer (the wrapper
  types live here).

### Section: existing `BitPresentation::LOGICAL_WIDTH` body block

Locate the existing block documenting `LOGICAL_WIDTH`'s composition. Add a
sentence to the body narrative:

> Round 202605030400 replaces the prior `as usize` cast composition in the
> impl bodies with typed arithmetic at the meta-bit layer. The trait surface
> stays at `USize`. The change is internal to `UFixed` and `IFixed`'s
> `BitPresentation` impls; consumers see no API difference.

## arvo-storage/DESIGN.md.tmpl

### Section: meta-bit newtype documentation

No changes. Width / IBits / FBits / MetaCarrier stay where they are.

### New paragraph: typed-arithmetic helper note (optional, source-CL-time decision)

If the SRC CL chooses the helper-fn path for Deliverable B (a const-fn
`logical_width(IBits, FBits) -> USize` shipping from arvo-storage), add a
short paragraph documenting that helper alongside the existing
`ibits` / `fbits` / `width` const-fn helper documentation. If the SRC CL
chooses the per-newtype const-trait `Add` path instead, add a sentence
under the meta-bit-newtype section noting the const `Add` impl ships from
arvo-storage. The choice between the two paths is made at SRC-PLAN time
based on which produces cleaner LOGICAL_WIDTH bodies.

## arvo-strategy/BACKLOG.md.tmpl

### New entry

Add an unbacktick BACKLOG entry tracking Round 3's lift work:

> Round 3 Bits-const-Width-lift: lift the substrate const-generic positions
> from bare u16 to typed Width across Bits, UContainerFor, IContainerFor,
> BitsContainerFor, UBitContainer, IBitContainer, Narrow, Widen. Includes
> Ieee::WIDTH typing (deferred from Round 2). Spans 70+ sites across five
> crates and may require relocating Width / IBits / FBits / MetaCarrier
> from arvo-storage to arvo-strategy if the lift hits a trait-solver cycle
> resolution that needs the meta-newtypes reachable below the bridge
> layer. Tracked under #313.

## arvo/BACKLOG.md.tmpl

### Removed entries

If the existing BACKLOG carries an entry tracking Ieee relocation, mark it
resolved (Round 2 ships).

If it carries an entry on `LOGICAL_WIDTH` typed bodies, mark resolved.

## What this CL does NOT cover

- No relocation of Width / IBits / FBits / MetaCarrier. Round 3 if needed.
- No `Ieee::WIDTH` typed lift. Round 3.
- No bridge-trait const-generic surface changes. Round 3.
- No Mask const-generic surface changes. Round 3.

## Locked decisions reaffirmed

The three decisions surviving the scope correction (per the corrective topic
file `202605030500_topic_round_2_scope_correction.md`):

1. Ieee lives in arvo-strategy.
2. LOGICAL_WIDTH bodies route through typed arithmetic.
3. Bits<const N: u16> stays for Round 2.
