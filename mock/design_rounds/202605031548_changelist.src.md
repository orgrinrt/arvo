**Date:** 2026-05-03
**Phase:** SRC-PLAN → SRC
**Scope:** Round 4 / #314 — source application of locked doc CL `202605031548_changelist.doc.lock.md`
**Doc CL:** `mock/design_rounds/202605031548_changelist.doc.lock.md` (locked)
**Sketches:** `mock/research/sketches/202605031548_round_4_const_hash_and_narrowing/` (frozen, both WORKS)

# Src CL — Round 4

This src CL applies the locked doc CL changes to source. Per the `cl-claim-sketch-discipline.md` workspace rule, every claim about source state appears under a `## CHANGE: <symbol> FROM <state> TO <state>` block with verification.

## Pass 1: arvo-strategy/src/width.rs — mask_low_bits helper

## CHANGE: const fn `mask_low_bits` ADDED in `arvo-strategy/src/width.rs`
File: `mock/crates/arvo-strategy/src/width.rs`
Verification: `grep -n "pub const fn mask_low_bits" mock/crates/arvo-strategy/src/width.rs` returns one match.

```rust
#[inline(always)]
pub const fn mask_low_bits(n: u16) -> u64 {
    if n >= 64 { u64::MAX } else { (1u64 << n) - 1 }
}
```

## CHANGE: re-export `mask_low_bits` FROM private TO `pub use` in `arvo-strategy/src/lib.rs`
File: `mock/crates/arvo-strategy/src/lib.rs`
Verification: `grep -n "mask_low_bits" mock/crates/arvo-strategy/src/lib.rs` returns one match in the `pub use width::{...};` line.

## Pass 2: arvo-strategy/src/container.rs — Project macro deduplication + sealing

## CHANGE: `Project` trait FROM unsealed TO sealed via `sealed::Sealed` supertrait
File: `mock/crates/arvo-strategy/src/container.rs`
Verification: `grep -n "pub trait Project.*: sealed::Sealed" mock/crates/arvo-strategy/src/container.rs` returns one match.

The `mod sealed { pub trait Sealed {} }` declaration moves to (or is re-used from) `arvo-strategy/src/lib.rs` (already exists there for Strategy/Signedness). `Project` adds the supertrait bound.

## CHANGE: `Picker` STAYS `pub` (per sketch finding 2)
File: `mock/crates/arvo-strategy/src/container.rs`
Verification: `grep -n "^pub struct Picker;" mock/crates/arvo-strategy/src/container.rs` returns one match.

Sketch 02 Finding 2 documented that `Picker` cannot be `pub(crate)` because it appears in the public associated-type body of every `BitsContainerFor` impl (`type T = <Picker as Project<...>>::T`). The trait sealing on `Project` is the gate against downstream impls; `Picker` remains `pub`.

## CHANGE: macro_rules! `impl_native_bucket` ADDED in `arvo-strategy/src/container.rs`
File: `mock/crates/arvo-strategy/src/container.rs`
Verification: `grep -n "macro_rules! impl_native_bucket" mock/crates/arvo-strategy/src/container.rs` returns one match.

The macro takes `(Strategy, [unsigned-list], [signed-list])` and emits 8-10 `Project<TAG, Sign, BYTES, S>` impls per invocation (4 buckets x 2 sign for Warm/Precise; 5 buckets x 2 sign for Hot/Cold). Replaces the 40 hand-written native-bucket impls with 4 macro invocations.

## CHANGE: 40 hand-written `impl Project<...>` blocks REPLACED with 4 `impl_native_bucket!(...)` invocations
File: `mock/crates/arvo-strategy/src/container.rs`
Verification: `grep -c "^impl<const BYTES: usize> Project<" mock/crates/arvo-strategy/src/container.rs` returns 0 (the per-N native blocks are gone). `grep -c "impl_native_bucket!" mock/crates/arvo-strategy/src/container.rs` returns 4.

## Pass 3: arvo-bits-contracts — NarrowFromU64<N, S, Sign>

## CHANGE: const trait `NarrowFromU64<const N: u16, S: Strategy, Sign: Signedness>` ADDED in `arvo-bits-contracts/src/narrow_from.rs`
File: `mock/crates/arvo-bits-contracts/src/narrow_from.rs` (new file)
Verification: `grep -n "pub const trait NarrowFromU64" mock/crates/arvo-bits-contracts/src/narrow_from.rs` returns one match.

## CHANGE: blanket impls of `NarrowFromU64` per native primitive ADDED in `arvo-bits-contracts/src/narrow_from.rs`
File: `mock/crates/arvo-bits-contracts/src/narrow_from.rs`
Verification: 36 native primitive impls, partitioned `(S, Sign)`-keyed:
- Hot+Unsigned: 5 (u8/u16/u32/u64/u128).
- Hot+Signed: 5 (i8/i16/i32/i64/i128).
- Cold+Unsigned: 5. Cold+Signed: 5.
- Warm+Unsigned: 4 (u16/u32/u64/u128). Warm+Signed: 4.
- Precise+Unsigned: 4. Precise+Signed: 4.
- Plus 1 wide-bucket impl over `WideBits<BYTES, A>` parameterised on (S, Sign, BYTES, A).

`grep -c "^impl<const N: u16> const NarrowFromU64<" mock/crates/arvo-bits-contracts/src/narrow_from.rs` returns ~36.

## CHANGE: `pub use narrow_from::NarrowFromU64;` ADDED in `arvo-bits-contracts/src/lib.rs`
File: `mock/crates/arvo-bits-contracts/src/lib.rs`
Verification: `grep -n "pub use narrow_from::NarrowFromU64" mock/crates/arvo-bits-contracts/src/lib.rs` returns one match.

## Pass 4: arvo-hash — ConstHash trait + bounded-generic Hasher impl + macro deletion + HasherExt deletion

## CHANGE: const trait `ConstHash<const N: u16, S: Strategy, Sign: Signedness>` ADDED in `arvo-hash/src/algo.rs`
File: `mock/crates/arvo-hash/src/algo.rs`
Verification: `grep -n "pub const trait ConstHash" mock/crates/arvo-hash/src/algo.rs` returns one match.

## CHANGE: `HasherExt<N>` trait + blanket impl REMOVED from `arvo-hash/src/algo.rs`
File: `mock/crates/arvo-hash/src/algo.rs`
Verification: `! grep -q "trait HasherExt" mock/crates/arvo-hash/src/algo.rs && ! grep -q "HasherExt" mock/crates/arvo-hash/src/lib.rs`.

## CHANGE: `impl_fnv1a!` macro DELETED from `arvo-hash/src/fnv1a.rs`
File: `mock/crates/arvo-hash/src/fnv1a.rs`
Verification: `! grep -q "macro_rules! impl_fnv1a" mock/crates/arvo-hash/src/fnv1a.rs`.

## CHANGE: 64 per-N inherent `impl Fnv1a<$n>::hash_const` REMOVED + bounded-generic `impl<const N: u16> Hasher<N> for Fnv1a<N>` + bounded-generic `impl<const N: u16> const ConstHash<N, Hot, Unsigned> for Fnv1a<N>` ADDED
File: `mock/crates/arvo-hash/src/fnv1a.rs`
Verification:
- `! grep -q "impl Fnv1a<" mock/crates/arvo-hash/src/fnv1a.rs` (no per-N inherents).
- `grep -c "^impl<const N: u16> Hasher<N> for Fnv1a<N>" mock/crates/arvo-hash/src/fnv1a.rs` returns 1.
- `grep -c "^impl<const N: u16> const ConstHash<N, Hot, Unsigned> for Fnv1a<N>" mock/crates/arvo-hash/src/fnv1a.rs` returns 1.

## CHANGE: `impl_xxhash3!` macro DELETED from `arvo-hash/src/xxhash3.rs` + bounded-generic impls ADDED
File: `mock/crates/arvo-hash/src/xxhash3.rs`
Verification: same shape as Fnv1a above. `! grep -q "macro_rules! impl_xxhash3"` and matching `impl<const N: u16>` blocks present.

## Pass 5: arvo-hash — bench shims switch back to `<T as ConstHash<...>>::hash_const`

## CHANGE: bench shims update at `mock/benches/variants/{fnv1a,xxhash3}/src/lib.rs`
Files: `mock/benches/variants/fnv1a/src/lib.rs`, `mock/benches/variants/xxhash3/src/lib.rs`
Verification: `grep -n "ConstHash" mock/benches/variants/fnv1a/src/lib.rs mock/benches/variants/xxhash3/src/lib.rs` returns matches.

## Pass 6: arvo-strategy — ConstFrom / ConstTryFrom bridges

## CHANGE: const trait `ConstFrom<T>` ADDED in `arvo-strategy/src/const_convert.rs` (new file)
File: `mock/crates/arvo-strategy/src/const_convert.rs`
Verification: `grep -n "pub const trait ConstFrom" mock/crates/arvo-strategy/src/const_convert.rs` returns one match.

## CHANGE: const trait `ConstTryFrom<T>` ADDED in `arvo-strategy/src/const_convert.rs`
File: `mock/crates/arvo-strategy/src/const_convert.rs`
Verification: `grep -n "pub const trait ConstTryFrom" mock/crates/arvo-strategy/src/const_convert.rs` returns one match.

## CHANGE: `pub use const_convert::{ConstFrom, ConstTryFrom};` ADDED in `arvo-strategy/src/lib.rs`
File: `mock/crates/arvo-strategy/src/lib.rs`
Verification: `grep -n "ConstFrom" mock/crates/arvo-strategy/src/lib.rs` returns matches.

## Pass 7: arvo-transparent — ConstDeref / ConstAsRef bridges

## CHANGE: const trait `ConstDeref` ADDED in `arvo-transparent/src/const_deref.rs` (new file)
File: `mock/crates/arvo-transparent/src/const_deref.rs`
Verification: `grep -n "pub const trait ConstDeref" mock/crates/arvo-transparent/src/const_deref.rs` returns one match.

## CHANGE: const trait `ConstAsRef<T>` ADDED in `arvo-transparent/src/const_deref.rs`
File: `mock/crates/arvo-transparent/src/const_deref.rs`
Verification: `grep -n "pub const trait ConstAsRef" mock/crates/arvo-transparent/src/const_deref.rs` returns one match.

## CHANGE: `pub use const_deref::{ConstDeref, ConstAsRef};` ADDED in `arvo-transparent/src/lib.rs`
File: `mock/crates/arvo-transparent/src/lib.rs`
Verification: `grep -n "ConstDeref" mock/crates/arvo-transparent/src/lib.rs` returns matches.

## Pass 8: Algorithm-crate USize sweep (mechanical, scoped to public-API positions only)

## CHANGE: any `usize` in const-generic positions on consumer-API surfaces in arvo-graph / arvo-sparse / arvo-comb / arvo-spectral REPLACED with `arvo::USize` or `arvo::Cap<N>` where applicable
Files: `mock/crates/arvo-graph/src/**/*.rs`, `mock/crates/arvo-sparse/src/**/*.rs`, `mock/crates/arvo-comb/src/**/*.rs`, `mock/crates/arvo-spectral/src/**/*.rs`
Verification: per-file diff inspection at apply time. Const-generic-position internal helpers (e.g. `cap_size(c: Cap) -> usize`) and array-sizing positions (`[T; N.0.0 as usize]`) stay as-is per arvo's six definition-site exceptions; only consumer-facing surfaces change.

## Pass 9: Cargo.toml dep updates

## CHANGE: arvo-hash adds direct dep on `arvo-bits-contracts` in `mock/crates/arvo-hash/Cargo.toml`
File: `mock/crates/arvo-hash/Cargo.toml`
Verification: `grep -n "arvo-bits-contracts" mock/crates/arvo-hash/Cargo.toml` returns one match in `[dependencies]`.

## Apply-time findings

(Recorded inline as src apply progresses, per cl-claim-sketch-discipline.)

## Comparison to deprecated changelist

Not applicable. This is a fresh src CL; no prior CL was deprecated.

## Lock criteria

- Every `## CHANGE:` block above has its verification command return the expected result.
- `cargo check` passes for every affected crate (`arvo-strategy`, `arvo-bits-contracts`, `arvo-hash`, `arvo-transparent`, plus arvo facade pulling in re-exports, plus the four algorithm crates).
- `cargo test` passes (any pre-existing tests; no new ones in scope this round).
- `cargo mock --lint-only --commit` passes scoped to all touched crates.
- Bench shims at `mock/benches/variants/{fnv1a,xxhash3}/` compile and produce identical hash output to pre-round.
- The Round 4 sketches stay as audit trail (already committed, not changed).
