# Src changelist: the merge, and what it is checked against

**Date:** 2026-09-02
**Round:** the trunk grew a second existential under this branch
**Scope:** whatever `origin/dev` carries into `mock/crates/`, and nothing authored here.

## CHANGE: the trunk's `arvo-format` source lands under this branch
File: `mock/crates/arvo-format/src/format.rs`, `src/quantum.rs`, `src/ambient.rs`, `src/lib.rs`, and the
test tree beneath them.
Verification: `cargo test --workspace` from `mock/` on the merged tree, and the fourteen parity arms in
`mock/crates/arvo-format/tests/matlab_fi_parity.rs` among what it runs.

Not one line of this is written here. The trunk moved `has_additive_identity` onto a search over the
declared magnitude range, gave three more traits the `ADMITTED` obligation, and took `Phase` off
normalising arithmetic. The merge carries all of it and this changelist claims no authorship of any of
it.

What this round owes is the check, not the code. `standards.rs` names `Format` and was written against
the contract as it stood before tonight, so the question is whether `Fi` and `Ufi` still satisfy it and
whether the parity numbers survived the move. A green suite is the answer to both, and a red one would
be a finding about the alias rather than about the trunk.

**No file under `mock/crates/` is hand-edited in this round.** If one needs to be, this changelist is
wrong and the round reopens with a different scope.
