# Topic: the guard on the platform-width spelling, the carry past the index, and names that do not exist

**Date:** 2026-09-19
**Phase:** TOPIC
**Scope:** `crates/arvo-format/DESIGN.md.tmpl`, `PRINCIPLES.md.tmpl`, the agent templates under
`agent/rules/` and `agent/skills/type-surface-review/`, the lint
`the-platform-width-points-read-the-pointer-width`, `crates/arvo-format/src/apply.rs` and the
`apply` suite.
**Source:** the third review of `feat/platform-width-types-on-a-wider-slot-range` at `26bc2743`, each
claim checked against the tree, a compiler run and the registry before it is taken.

## The canon this answers to

`ruling::a_platform_width_type_is_a_target_bound_member_of_the_format_family`,
`obligation::a_platform_sized_unsigned_integer_at_an_api_position`,
`obligation::a_primitive_for_every_position_a_bare_number_would_take` and
`ruling::the_numeric_door_carries_the_coordinate_set_and_the_two_type_bound_is_not_canon` govern the
points. `ruling::the_overflow_panic_is_permitted_and_bounded` (I18, ratified) governs the carry:

> Panic is the go-to for what cannot be caught at compile time through the typestate, on debug builds,
> in every strategy ... What stands in its place on release is the guarantee plus an explicit
> declaration of the mode, saturate or wrap or whatever applies to the overflow or underflow at hand,
> which then lowers and behaves accordingly.

The intent read from it for this round: the debug verdict answers whether the position left the range,
and the release answer is the declared mode applied to that position. Neither may answer for a
different position than the one named. The ruling is kept loose on purpose and nothing here sharpens
where the panic appears; this round only makes the two answers true of the position.

## 1. The lint is a substring test

`mock/lints/the_platform_width_points_read_the_pointer_width.rs` accepts an alias whose line contains
`usize::BITS` anywhere. So `pub type USize = UFixed<64, 0>; // was usize::BITS` passes, and so do
`UFixed<{ if usize::BITS < 32 { 32 } else { usize::BITS } }, 0>` and `ISize` spelled over `UFixed`.
The first two also pass every host test on a 64-bit host. The other way round, a correct alias whose
right-hand side rustfmt wraps onto a second line is a finding, because the lint reads one line.

The lint reads each alias's declaration up to its `;`, across lines, strips comments, removes
whitespace, and compares the right-hand side exactly: `UFixed<{usize::BITS},0>` for `USize` and
`Integer<{usize::BITS}>` for `ISize`. A test per bypass above, and one for a wrapped correct alias.
It declares `per_file() -> false`, which mockspace's `CrateLint` carries for a lint judging the crate
as a whole (`lint-rules/src/lib.rs`, the dispatcher hands such a lint the context once), in place of
comparing `ctx.source` against the first of `all_sources`.

## 2. "A `cargo check` at a foreign target type-checks an assertion without evaluating it"

It stands at `DESIGN.md.tmpl:946`, the lint's module doc and the rendered `docs/FORMAT.md`. Run here at
the pinned toolchain, `mock/research/202609191140_foreign_target_probes/run.sh`:

- a `const _: () = assert!(..)` at `i686-unknown-linux-gnu`, over an alias written as a literal 64,
  fails `--emit=metadata` with E0080;
- the same over an alias reading `usize::BITS` exits zero;
- a `#[test]` arm with a run-time `assert_eq!` behind the same `cfg`, checked with `--test`, exits
  zero over the literal.

So the sentence is true of the arms that were deleted and false of a check in general. It goes, at all
three sites. What guards the property is then stated as what it is: the lint, which compares the
spelling exactly; the host tests, which read the aliases against the host's pointer integers; and the
family laws at 16, 32 and 64. No build here runs at a target of another width, and the design says so
rather than implying one could not help.

## 3. Names in the rules that name nothing

`&mut impl KeyValueStore<K, V>` at `agent/rules/no-heap.md.tmpl:26`,
`agent/skills/type-surface-review/SKILL.md.tmpl:33` and `PRINCIPLES.md.tmpl:239`; `UArith` and
`IArith` in the type categories and throughout `type-surface.md.tmpl`. The class is wider. Every
backticked type or trait name in every template, rule, skill and README was checked against the type,
trait, struct, enum and union declarations in the shipped `*.rs` of arvo at this branch and of notko and
hilavitkutin at `origin/dev`, excluding research, benches, tools, sketches and tests. Names that exist
only in hilavitkutin's own imports of an arvo that has since been cleared (`BitSequence`, `TotalOrd`,
`Mask64` and the rest, via `arvo_bitmask` and `arvo::traits`) do not exist in any of the three.

Each is removed, or its sentence reduced to what the tree has. Where the sentence is about a surface
nothing has replaced, it says none is in the tree yet, and names no replacement the canon does not
name. Left standing: sentences saying a name is not in the tree (`IFixed`, `FastFloat`, `StrictFloat`,
the `ContentHash` alias in `arvo-bits`' design); an example that writes out the definition it asks for
(`ContentHash = Bits<28>`, `NodeId` as a domain newtype of one's own); placeholders that are not types
(`Agent Name`); and mockspace's own `Lint` in the lint skill, which exists there.

## 4. `Exact::between` saturates past the ends of the index

The carry a remainder moves into the slot is at most `2^63` either way, and where the sum leaves the
index the constructor pins the slot and keeps the remainder. `between(MIN, -7/4)` stores `MIN + 1/4`,
so `Floor` over a range at the bottom calls it in range and adapts it onto `MIN`, where the position
named floors to `MIN - 2`, which is out of range and wraps to `MIN + 254`. The debug verdict says in
range for a position that left it, and the release wrap answers for a different position. Both are
what I18 rules out.

A flag saying the slot was pinned is not enough, and it fails at one place. `between(MIN, -1/4)` names
`MIN - 1/4`, and `Ceil` takes it onto `MIN`, in range. Pinned with the remainder kept, the stored
position `MIN + 3/4` ceils to `MIN + 1`, and a flag reading "pinned" would call it out of range.

`sum_position` saturates `a + b + phase` at the index's ends, and half of the gap is there too.
`sum_is_carried` doubles each end of the range in the index's own integer, so every range addition
admits lies within `[-2^126, 2^126)`, and only an operand outside the range reaches the saturation. A
saturated position sits at an end of the index, at least `2^126` from any such range and on the side
the named one is, so the verdict is right there. The wrap is not: it answers for the saturated
position, and `add` is total over every pair of slots, so that is an answer it gives for a declared
wrap.

What decides every answer past an end is where the named slot is, and a slot index plus a count of
laps around it says that exactly. The slot is kept as the index's own integer wraps it, and a lap of
`+1` or `-1` says the named slot is `2^128` above or below that. One lap is the most either site can
make: the constructor's carry is at most `2^63`, and three slots of the index sum to within one lap of
it. Every region reads the pair with nothing wider than the index:

- the sign-dependent modes read the named slot's sign, positive a lap up and negative a lap down;
  half-even reads its parity, which the wrapped slot shares because `2^128` is even;
- the in-range question is false a lap up, and a lap down it holds only where the named slot is one
  under the index's lowest, the rounding steps up, and the range starts at the index's lowest;
- the wrap reduces the wrapped slot and the lowest slot separately, adds `2^128` reduced by the span
  once per lap and the step, and reduces again, every term under the span;
- saturation pins to the end the position is past.

`Exact::slot()` answers the end of the index for a position past it, and says so. `addition_reach`
still saturates, and it computes the bounds a verdict reads rather than a position handed to the map;
`addition_is_associative` passes the map a position from it with a remainder under its denominator and
forces `Addable`, whose `sum_is_carried` keeps that reach inside the index.

The carry test in `apply/tests/the_top_of_the_index.rs` asserts the saturated answers today. It is
rewritten to the named positions first and shown failing. A law beside it feeds positions past each
end of the index through the carry, to every range at an end against a reference well inside, over
all 18 signatures. The test pinning `sum_position`'s saturation is rewritten to the named sums, with
the verdict asserted beside each.

## 5. The translation law at a span that is not a power of two

Spans 256 and `2^64` only. Two pairs are added, `TopOf200` against a reference at `[1002, 1201]` and
`BottomOf200` against `[-1998, -1799]`. Each reference's lowest slot is two modulo 200, off a multiple of
the span and off the residue of the range it is compared with, and each shift is even. Shown to fail
with the wrap reduction broken twice: reduced from zero, and the lowest slot reduced by 256 in place of
the span, which only a span of 200 can see.

## 6. Test hygiene

`old_law` in `the_translation_law.rs` asserts that a retired instrument misses `anchored_at_zero`,
which exercises nothing that ships, and goes. `the_far_end_of_the_index.rs:278` says "saturated
difference" over a map that uses `wrapping_sub`, and says wrapped.

## Outside this round

The four bench variants under `mock/benches/variants` that `cargo mock test` fails on with "failed to
find a workspace root" are not touched here.
