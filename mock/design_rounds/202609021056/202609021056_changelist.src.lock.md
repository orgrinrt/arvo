# SRC changelist: no source change

The round edits one agent template and nothing else. No `*.rs` file is touched,
no type moves, no signature changes, and no claim is made about source state.

## CHANGE: none

Verification: `git diff --name-only <round base>..HEAD -- '*.rs'` returns
nothing.

The generated files under `.claude/` and `.github/` moved with the template, as
they do on every `cargo mock`. They are gitignored here, so they are not in the
diff and are not claims this changelist can carry.
