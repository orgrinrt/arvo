# DOC changelist: quote the `standing` vocabulary instead of paraphrasing it

## CHANGE: mock/agent/MAIN.md.tmpl

Replace the one-line paraphrase of `standing` with the vocabulary itself.

The current line says the field records how many independent instances back the
claim. It drops `reached`, which is what the field counts, and it drops
`each deriving before reading the other`, which is what makes an instance count.

The replacement quotes the schema and says where it lives, so a reader who needs
the exact wording goes to the declaration rather than trusting the summary.
