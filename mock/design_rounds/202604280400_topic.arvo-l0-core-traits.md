# Topic: arvo L0 core::TryFrom trait annotations

**Date:** 2026-04-21
**Status:** TOPIC phase
**Task:** #120 round 6 (final round — completes arvo L2/L3 sweep series)

## Drift inventory (4 sites, all `core::TryFrom` impls)

| File | Line | Kind |
|------|-----:|------|
| ifixed.rs | 233 | `impl TryFrom<IFixed<I, F, Warm>> for IFixed<I, F, Hot>`: `fn try_from(..) -> Result<Self, Self::Error>` |
| ifixed.rs | 248 | `impl TryFrom<IFixed<I, F, Precise>> for IFixed<I, F, Hot>`: same |
| ufixed.rs | 262 | `impl TryFrom<UFixed<I, F, Warm>> for UFixed<I, F, Hot>`: same |
| ufixed.rs | 277 | `impl TryFrom<UFixed<I, F, Precise>> for UFixed<I, F, Hot>`: same |

## Decisions

All 4 sites are core trait contract: `core::convert::TryFrom::try_from` returns `Result<Self, Self::Error>`. Same class as the arvo-bitmask `PartialOrd`/`Iterator` sites (round 1). Solution: inline `lint:allow(no-bare-result) reason: core::convert::TryFrom::try_from trait-method signature returns Result<Self, Self::Error>; tracked: #115` on each `fn try_from` line.

No API change. Matches the established pattern from round 1's 3 core-trait sites.

## Out of scope

- Removing the TryFrom impls — they're useful public API. Consumers use `UFixed::<Hot>::try_from(val: UFixed::<Warm>)` to downcast Warm/Precise to Hot with bounded-range narrowing. Keeping with core::TryFrom is pragmatic per user's #115 clarification ("freedom from core is mostly for core concrete types in signatures, not for implementing core-defined contracts").
