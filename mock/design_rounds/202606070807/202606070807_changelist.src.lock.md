**Date:** 2026-06-07
**Phase:** SRC
**Scope:** arvo-graph (L2)
**Round:** const-waist-detect (202606070807)

Additive const analog of `waist_detect`. The runtime `waist_detect` is
untouched. Adds one feature gate, one module + const fn, one re-export, one test
file.

## CHANGE: feature gate `const_trait_impl` ADDED TO arvo-graph crate root
File: `mock/crates/arvo-graph/src/lib.rs`
Verification: `grep "feature(const_trait_impl)" mock/crates/arvo-graph/src/lib.rs` returns one match.

## CHANGE: const fn `waist_detect_const` ADDED in new module `waist_const`
File: `mock/crates/arvo-graph/src/waist_const.rs`
Verification: `grep "pub const fn waist_detect_const" mock/crates/arvo-graph/src/waist_const.rs` returns one match; the runtime `waist.rs` is unchanged (`git diff` shows no edit to `src/waist.rs`).
Signature: `waist_detect_const<C, W>(succ_rows: &C::Array<W>, topo_order: &C::Array<NodeId>) -> C::Array<Bool> where C: [const] ConstCapacity, W: [const] BitAccess`. Same algorithm as `waist_detect` (forward topo depth pass, level widths, occupied depths, strict-local-minimum waist flags at topo positions), expressed over the `ConstCapacity` const surface (R1a) with the predecessor scan inlined via `BitAccess::bit`.

## CHANGE: module declaration + re-export `waist_detect_const` FROM crate root
File: `mock/crates/arvo-graph/src/lib.rs`
Verification: `grep "pub mod waist_const" mock/crates/arvo-graph/src/lib.rs` and `grep "waist_detect_const" mock/crates/arvo-graph/src/lib.rs` (in the `pub use` line) each return a match.

## CHANGE: integration test `tests/waist_const.rs` ADDED
File: `mock/crates/arvo-graph/tests/waist_const.rs`
Verification: file exists; `cargo test -p arvo-graph --test waist_const` passes.
Covers: (1) a forced-const-eval correctness test (hourglass DAG, const-built successor words + const topo order, the waist flag lands at the waist node's topo position, proving const-evaluability); (2) a runtime cross-check asserting `waist_detect_const` agrees with the runtime `waist_detect` position-for-position on shared fixtures (linear chain = no waist, hourglass via `topo_sort` order).
