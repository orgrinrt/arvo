**Date:** 2026-06-07
**Phase:** DOC
**Scope:** arvo-graph (L2)
**Round:** const-waist-detect (202606070807)

## Doc changes

One file: `mock/crates/arvo-graph/DESIGN.md.tmpl`.

Add a `waist_detect_const` subsection to the existing `## Waist detection`
section. It documents the const analog: the `const fn` signature over
`ConstCapacity` + `BitAccess`, the raw-successor-word input (the const-contract
substitute for `BitMatrix`/`Mask`, neither of which is const), the
`C::Array<Bool>` flag output that agrees with the runtime `Mask<B>`
position-for-position, and that the runtime `waist_detect` is unchanged. The
backticked `waist_detect_const` resolves to the real source item once the src CL
lands (design-doc-source-mismatch lint).

No other doc template changes.
