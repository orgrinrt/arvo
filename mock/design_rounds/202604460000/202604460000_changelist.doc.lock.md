**Date:** 2026-04-21
**Phase:** DOC
**Scope:** FromConstant USize retrofit (#123)
**Source topics:** 202604460000_topic.from-constant-usize.md

# Doc template edits

None. `arvo/DESIGN.md.tmpl` lists `FromConstant` as a trait-surface
entry without the `u8` parameter type; `arvo-spectral/DESIGN.md.tmpl`
uses `FromConstant` only as a bound name. Both continue to match
after the source change.

The `u8 → USize` signature change lives in the src CL (traits.rs doc
table, trait method, macro bodies, consumer call sites).

# Validation

- `cargo mock --lint-only` clean; no new writing-style hits.
