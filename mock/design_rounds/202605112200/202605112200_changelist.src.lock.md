**Date:** 2026-05-11
**Phase:** SRC
**Scope:** arvo-storage source
**Doc CL:** `202605112200_changelist.doc.lock.md`

# Source changelist: round 202605112200

Mechanical plan implementing the four `impl const From` bridges named in the locked doc CL. Per `cl-claim-sketch-discipline.md`, structured `## CHANGE:` blocks claim each addition with a verification grep.

## Order of operations

1. Add `impl const From<Cap> for USize` and `impl const From<USize> for Cap` to `arvo-storage/src/platform.rs`.
2. Lift existing `impl From<bool> for Bool` and `impl From<Bool> for bool` to `impl const From`.
3. Add a smoke test exercising const-context evaluation of each impl.
4. Verify compile + tests + lints.

## CHANGE blocks

## CHANGE: `impl const From<Cap> for USize` ADDED
File: `mock/crates/arvo-storage/src/platform.rs`
Verification: `grep -q "impl const From<Cap> for USize" mock/crates/arvo-storage/src/platform.rs`

## CHANGE: `impl const From<USize> for Cap` ADDED
File: `mock/crates/arvo-storage/src/platform.rs`
Verification: `grep -q "impl const From<USize> for Cap" mock/crates/arvo-storage/src/platform.rs`

## CHANGE: existing `impl From<bool> for Bool` LIFTED to `impl const From<bool> for Bool`
File: `mock/crates/arvo-storage/src/platform.rs`
Verification: `grep -q "impl const From<bool> for Bool" mock/crates/arvo-storage/src/platform.rs` AND `! grep -E "^impl From<bool> for Bool" mock/crates/arvo-storage/src/platform.rs`

## CHANGE: existing `impl From<Bool> for bool` LIFTED to `impl const From<Bool> for bool`
File: `mock/crates/arvo-storage/src/platform.rs`
Verification: `grep -q "impl const From<Bool> for bool" mock/crates/arvo-storage/src/platform.rs` AND `! grep -E "^impl From<Bool> for bool" mock/crates/arvo-storage/src/platform.rs`

## CHANGE: const-context smoke test for the four From bridges ADDED
File: `mock/crates/arvo-storage/tests/from_bridges.rs` (new test file)
Verification: file exists with `const _: USize = ...` shape exercising each bridge at const time. `cargo test -p arvo-storage from_bridges` passes.

## Lock criteria (src CL)

1. Every `## CHANGE:` block above is verified by its named grep.
2. `cargo check --workspace` clean. `cargo test -p arvo-storage` green.
3. `mockspace --lint-only` clean at commit + push severity.

## Out of scope

- usize edges (`From<usize> for USize`, etc.).
- Workspace lint for `.0` field-access on arvo primitives (separate round).
- Dropping `pub` from inner fields (separate round).
- Sweeping existing consumer call sites to `.into()` (organic adoption).
