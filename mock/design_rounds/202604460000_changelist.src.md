**Date:** 2026-04-21
**Phase:** SRC-PLAN
**Scope:** FromConstant USize retrofit (#123)
**Source topics:** 202604460000_topic.from-constant-usize.md
**Doc CL:** 202604460000_changelist.doc.lock.md

# Source edits

## mock/crates/arvo/src/traits.rs

- Line 13 trait doc table: `from_constant(u8) -> Self` →
  `from_constant(USize) -> Self`.
- Line 75-77 trait doc: "Input is `u8`" → "Input is `USize`".
- Line 85-88 `pub trait FromConstant { fn from_constant(n: u8) ->
  Self; }` → `fn from_constant(n: USize) -> Self`.
- `impl_from_constant_ufixed!` / `_fractional` / `_ifixed` /
  `_ifixed_fractional` macro bodies: `n as $ctype` → `n.0 as
  $ctype`; `fn from_constant(n: u8)` → `fn from_constant(n: USize)`.
- Float impls (`FromU8Ieee`-gated): `fn from_constant(n: u8)` →
  `fn from_constant(n: USize)`; body `F::from_u8(n)` →
  `F::from_u8(n.0 as u8)` (the `FromU8Ieee` helper itself keeps its
  `u8` input — it's an internal helper trait for the IEEE-float
  convert, not the public surface). Add a trailing inline
  `lint:allow(no-bare-numeric) lint:allow(arvo-types-only) reason:
  internal `FromU8Ieee` bridge takes u8 by design; tracked: #123`
  to document the internal boundary.
- Add `use crate::newtype::USize;` at the top of traits.rs (USize
  already defined in `arvo::newtype`).

## mock/crates/arvo-graph/src/{rank,path}.rs + tests/{rank,path,spanning}.rs

- Replace `F::from_constant(n)` / `W::from_constant(n)` where `n`
  is an integer literal with `F::from_constant(USize(n))`.
- Add `use arvo::USize;` where needed.

## mock/crates/arvo-comb/src/{dp,binpack}.rs + tests/{dp,binpack}.rs

- Same pattern: `W::from_constant(n)` → `W::from_constant(USize(n))`.
- Add `use arvo::USize;`.

## mock/crates/arvo-spectral/src/{laplacian,partition,fiedler,power}.rs + tests/common/mod.rs

- Same pattern: `F::from_constant(n)` → `F::from_constant(USize(n))`.
- `fiedler.rs` lines 89-93: drop the `n_as_u8` cast and its
  `lint:allow`; `n` is already `usize` from the caller; pass
  `USize(n)` directly.
- `fiedler.rs` doc comment at line 89 updated accordingly.
- `tests/common/mod.rs` `impl FromConstant for TF { fn
  from_constant(n: u8) { ... } }` — update signature + body to
  take `USize`, cast `n.0 as f32` or adjust per existing body.

## mock/crates/arvo/tests/{ufixed,ifixed}_ops.rs

- `U::from_constant(5)` → `U::from_constant(USize(5))` for every
  call site.

# Validation

- `cargo check --all` clean.
- `cargo test --all` — existing test suites pass.
- `cargo mock --lint-only` clean; fiedler.rs `lint:allow(... tracked:
  #123)` annotation removed.
