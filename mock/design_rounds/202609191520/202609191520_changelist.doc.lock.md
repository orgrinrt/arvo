# Doc changelist: the sixth review's enumeration, spelling and narration fixes

**Date:** 2026-09-19
**Round:** the sixth review's blocking findings on
`feat/platform-width-types-on-a-wider-slot-range`
**Scope:** `crates/arvo-format/DESIGN.md.tmpl`, `PRINCIPLES.md.tmpl`, `agent/rules/cookbook.md.tmpl`.
**Topics consumed:** `202609191520_topic.sixth-review-shadowing-and-cross-end-fixes.md`, the only flat
topic.
**Evidence:** the lint's own test suite (33 tests before this round, more after, all green), a rustc
spike per new probe file in `mock/research/202609191400_core_shadowing_probe/` showing the leading-`::`
path resists a `use ... as core` alias and does not resist `extern crate self as r#core`, and a fourth
new probe file whose assertion is written to fail to compile as its own negative control.

## Canon

Same governing row as the fourth and fifth reviews' changelists
(`a_platform_width_type_is_a_target_bound_member_of_the_format_family`). No row changes; every edit here
narrows a claim about which forms of a `core` shadow the lint refuses, and fixes a stale respelling.
Neither touches what the canon states.

## `crates/arvo-format/DESIGN.md.tmpl`

The platform-width points section, "The laws the platform-width points owe tests for": every sentence
claiming the lint refuses "every way this crate could bind the name `core`" or "every shadow this crate
could declare against the alias" is rewritten to name the four enumerated forms exactly (`mod core`,
`use ... as core`, `extern crate ... as core`, `use ...::core;`, the raw identifier `r#core` on each),
and to say plainly that this is an enumeration rather than a closed guard: a `Cargo.toml` manifest rename
of a dependency to the key `core` reaches the same hazard and is unguarded, because this lint reads `.rs`
source only. The sentence claiming "no import or declaration reachable from inside this crate resolves
the leading-`::` path to anything else, short of a crate reference bound to the name `core`" is narrowed
to scope that claim to the four forms the lint's second arm actually enumerates.

The "Open items" section's opening sentence, "Two gaps a review found and this round did not close,
catalogued here rather than left in a review file nobody reads", is rewritten to say what the section is
without naming a review: two gaps this crate does not close, catalogued here rather than left
unrecorded.

## `PRINCIPLES.md.tmpl` and `agent/rules/cookbook.md.tmpl`

Both still spelled the platform-width alias `core::primitive::usize::BITS`, without the leading `::`,
after the fourth and fifth rounds fixed every other prose mention in the tree. Both instances (two lines
each) move to `::core::primitive::usize::BITS`, matching the shipped spelling and every other prose
mention of it. `docs/PRINCIPLES.md` regenerates from the first.

No other prose in this crate's DESIGN.md.tmpl, PRINCIPLES.md.tmpl or cookbook.md.tmpl is touched by this
round.
