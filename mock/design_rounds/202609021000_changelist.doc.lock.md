# Doc changelist: no template changes

**Date:** 2026-09-02
**Round:** three comments the first sweep could not see
**Scope:** none.

## CHANGE: nothing FROM the templates TO the templates
File: none
Verification: `git diff --name-only` over this round names no `*.md.tmpl` and no generated document.

The round cuts three provenance clauses out of shipped comments. Comments live in `*.rs`, which is
the source tier, and nothing here reaches a template or renders into `docs/`.

## CHANGE: nothing in the registry
File: none
Verification: `cargo mock` reports 708 rows across 10 namespaces before and after.

Cutting an attribution from a comment is not a canon act, for the same reason it was not one in the
round before this.
