# Doc changelist: no template changes

**Date:** 2026-09-01
**Round:** the ordering that ranked nothing
**Scope:** none.

## CHANGE: nothing FROM the templates TO the templates
File: none
Verification: `git diff --name-only` over this round names no `*.md.tmpl` and no generated document.

The round changes what one tool prints about its own ordering. `mock/tools/` is not a template surface
and nothing under it renders into `docs/`, so no `*.md.tmpl` moves and no generated document moves with
it.

The tool's own description string, which `mock tools` prints and which the generated tool catalogue
snapshots, is unchanged: the change is to the report body, not to the one-line summary. So the
catalogue does not go stale from this round.

This changelist exists because the phase machine requires a doc window to close before a src one opens,
not because there is doc work in it.
