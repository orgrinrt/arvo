**Date:** 2026-05-11
**Phase:** DOC
**Round:** 202605112039 (sparse SHAME entry removal)

# Doc changelist: arvo-sparse SHAME entry removal

This round has exactly one file to edit. The src CL ships an empty
source delta.

## `mock/crates/arvo-sparse/SHAME.md.tmpl`

- Delete the `## CsrBidirectional` heading (line 8 in the locked
  file) and its body paragraph (lines 10 through 19).
- Leave the intro paragraph (lines 1 through 6) unchanged. It
  documents what SHAME entries are; future entries will reuse it.
- Final state of the file: title + intro paragraph only, no entries.

Rationale: `CsrBidirectional<ROWS, NNZ, W>` exists in source as of
PR #63 (squash commit `a6d8cd7`). The push-gate
`design-doc-source-mismatch` lint that the SHAME entry silenced no
longer fires; the entry's purpose is served and the entry is stale.

## Out of scope

- No other arvo-sparse template edits.
- No other crate's SHAME.md.tmpl edits.
- No source edits.

## Verification at end of DOC phase

- `grep -n "CsrBidirectional" mock/crates/arvo-sparse/SHAME.md.tmpl`
  returns zero matches.
- The file's intro paragraph is unchanged.
- `mockspace --lint-only` clean.
