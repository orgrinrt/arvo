# Doc changelist: graph algorithms generic over row-width

**Date:** 2026-06-05
**Phase:** DOC
**Round:** 202606051246 (graph-algos-generic-width, branch `feat/arvo-graph-generic-width`)

One file: `mock/crates/arvo-graph/DESIGN.md.tmpl`. The doc currently states the algorithms "ship against `BitMatrix<Bits<64, Hot, Unsigned>, C>`" with up to 64 nodes, and frames wider-word instantiations as BACKLOG ergonomic helpers. This round makes the algorithm signatures themselves generic over the adjacency row-word, so the doc moves that genericity from a backlog gesture into the shipped signatures.

## `mock/crates/arvo-graph/DESIGN.md.tmpl`

- Intro (the "with `W = Bits<64, Hot, Unsigned>` as the canonical single-word backing for up to 64 nodes" sentence): restate that the algorithms are generic over the adjacency row-word, with `Bits<64, Hot, Unsigned>` as one instantiation; the consumer picks the word width its node count needs.
- Capacity section ("Algorithms ship against `BitMatrix<Bits<64, Hot, Unsigned>, C>`. Up to 64 nodes ... wider-W instantiations are tracked in BACKLOG.md as ergonomic helpers"): rewrite to state the algorithms are parameterized over the row-word now, not pinned to 64; the node ceiling is whatever the substrate's container projection supports for the chosen word, not a fixed 64. Drop the BACKLOG-helper framing for the generic statement.
- Signature blocks (`upward_rank`, `topo_sort`, the rank-computation block, and any other DAG-algorithm signature shown): change `dag: &BitMatrix<Bits<64, Hot, Unsigned>, C>` (and `Mask<Bits<64, Hot, Unsigned>>` returns) to the row-word type parameter the source carries, with its bit-sequence bound. Keep the existing edge-weight `W` where shown; the row-word parameter is named distinctly so it does not collide.

The design-doc-source-mismatch lint ties the backticked signatures here to source, so the doc signatures match the generalized source the src changelist lands. No other doc files change.
