# Topic: arvo-sparse sweep — eliminate bare primitives

**Date:** 2026-04-21
**Status:** TOPIC phase
**Task:** #120 round 4
**Round scope:** `arvo-sparse` source files (`block.rs`, `csr.rs`, `rcm.rs`).

## Drift inventory (5 unique sites)

| File | Line | Kind |
|------|-----:|------|
| block.rs | 32 | `let mut next_id: usize = 0;` local counter |
| csr.rs | 79 | `fn row_end(&self, r: usize) -> USize` private helper param |
| rcm.rs | 36 | `let mut head: usize = 0;` local counter |
| rcm.rs | 131 | `fn degree<...>(adj, n: NodeId) -> usize` private helper return |
| rcm.rs | 148 | `let mut best: Maybe<(usize, usize)> = Maybe::Isnt;` tuple of usize |

## Decisions (mechanical)

- Local counters (block.rs:32, rcm.rs:36) → `USize(0)` with `.0` extraction at use sites.
- Private helper `row_end(r: usize) -> USize` → take `r: USize` (change param type); caller updates.
- Private helper `degree(...) -> usize` → return `USize`; callers compare via `.0` where needed or via `USize`-native ops.
- Tuple `(usize, usize)` internal state → `(USize, USize)`. `Maybe<(USize, USize)>` for the best-candidate bookkeeping.

No new contracts or structural types. No public API change. No `lint:allow` annotations required.

## Out of scope

- Converting `[NodeId; cap_size(N)]` / `[USize; cap_size(N)]` arrays to `Array<T, N>` — not required; these arrays don't cross boundaries.
- Public `CsrMatrix` surface changes — stays as-is.
