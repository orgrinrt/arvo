# Source Changelist: arvo-sparse sweep

**Date:** 2026-04-21
**Phase:** SRC

## Files

- `src/block.rs:32` — `next_id: usize = 0;` → `= USize(0);` with `.0` at arithmetic sites.
- `src/csr.rs:79` — `fn row_end(&self, r: usize) -> USize` → `fn row_end(&self, r: USize) -> USize`; caller sites at `r + 1 < cap_size(ROWS)` / `r < cap_size(ROWS)` use `.0`.
- `src/rcm.rs:36` — `head: usize = 0;` → `USize(0)`.
- `src/rcm.rs:131` — `fn degree<...>() -> usize` → `-> USize`. Callers compare with `.0` where needed.
- `src/rcm.rs:148` — `Maybe<(usize, usize)>` → `Maybe<(USize, USize)>`; unwrap sites adjust.

## Verification

- `cargo test -p arvo-sparse` — all tests pass.
- `cargo mock --lint-only` — arvo-sparse drift 10 → 0.
