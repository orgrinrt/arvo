# Doc changelist: the lo-guard test, the carry bound, and two stale comments

**Date:** 2026-09-19
**Round:** the fourth review's remaining findings on `feat/platform-width-types-on-a-wider-slot-range`
**Scope:** `crates/arvo-format/DESIGN.md.tmpl`.
**Topics consumed:** `202609191600_topic.the-fourth-review-s-lo-guard-carry-bound-and-two-comments.md`,
the only flat topic.
**Evidence:** a compiled probe against `addition.rs`'s `doubled_plus`, recorded in the topic; the
existing `NearTheTop` test in `addition/tests/the_step.rs`, which already pins the boundary this
round's finding checks against.

## Canon

Same governing rows as the topic. No row changes; every edit here narrows or corrects a sentence to
what the code already does, or reflects a spelling change that closes a real gap (the `usize` shadowing
hole), none of which the canon rows speak to at that level of detail.

## `crates/arvo-format/DESIGN.md.tmpl`

The platform-width points section: the two aliases in the code block, and every prose mention of their
spelling, move from `usize::BITS` to `core::primitive::usize::BITS`, with one added sentence saying why
the absolute path is what closes the shadowing hole a module or item named `usize` in scope would open.
The lint's own description of the alias comparison moves with it.

"A const assertion" narrows to "a free `const _` item under a `cfg`", naming
`mock/research/202609191140_foreign_target_probes/` as the shape the probe actually covers, since an
associated-const assertion in a generic impl is evaluated only when monomorphised and the probe does
not test that shape.

The carry-bound sentence at `[-2^126, 2^126)` is **not** changed. The review's finding 2 asked for a
wider bound; checked against `addition.rs`'s `doubled_plus`, which gates `2 * slot` through
`checked_mul` before the phase's offset is ever added, the widened bound does not hold: `max` is capped
at `i128::MAX / 2` and `min` at `i128::MIN / 2` regardless of phase, which is exactly what the existing
sentence already says. This changelist leaves it as written and the src changelist adds a test pinning
that no phase moves either edge outward, so the next reader who doubts the sentence has an artifact
rather than a hand derivation to check it against.

No other prose in this crate's DESIGN.md.tmpl is touched by this round.