# Source Changelist: arvo-graph sweep

**Date:** 2026-04-21
**Phase:** SRC
**Scope:** `components.rs`, `waist.rs`, `spanning.rs` — 6 unique drift sites.

## Files

- `src/components.rs:27` — `let mut next_id: usize = 0;` → `let mut next_id = USize(0);`. Read sites use `next_id.0`.
- `src/waist.rs:66` — `max_depth_seen: usize = 0;` → `USize(0)`. Compare/write with `.0` extraction.
- `src/waist.rs:81` — `occ_n: usize = 0;` → `USize(0)`. Compare/write similarly.
- `src/waist.rs:94` — `[bool; cap_size(N)] = [false; cap_size(N)]` → `[Bool; cap_size(N)] = [Bool::FALSE; cap_size(N)]`. Read sites extract `.0`.
- `src/spanning.rs:110` — `head_idx: usize = 0;` → `USize(0)`.
- `src/spanning.rs:175` — `top_i: usize = 0;` → `USize(0)`.

Tests stay unchanged — no API change.

## Verification

- `cargo check -p arvo-graph` green.
- `cargo test -p arvo-graph` all tests pass.
- `cargo mock --lint-only` — arvo-graph drift 12 → 0.
