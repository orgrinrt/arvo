# Doc changelist: the readme gains the surface the branch added

**Date:** 2026-09-01
**Round:** three agents fixed one lint by three routes, and none of them knew
**Scope:** `README.md`, plus an addendum beside a locked changelist.

## CHANGE: the readme says the industrial conventions can be written directly
File: `README.md`
Verification: the paragraph enumerating what `arvo-format` carries read against the crate's public
surface, and the conventions named there resolving to items the crate exports.

"Where this is right now" enumerates the crate down to the six rounding modes and the overflow policies
and does not mention that MATLAB `fi` and `fimath` can be named over a declared signature. That is what
this branch adds and it is the thing a stranger landing from the repository page would most want to know.
`the-public-surface-is-checked-on-every-pr.md` calls omitting a surface the crate gained a blocking
defect, and it is right to: a reader has no way to discover it.

One sentence in that paragraph, in the register the rest of the file is in.

## CHANGE: the crate the measurement was taken against is recovered as a sketch
File: `mock/research/sketches/the-standards-alias-refusal/`, and an addendum at
`mock/design_rounds/202609010936/202609010936_changelist.src.lock.addendum.md`.
Verification: `git log --all --diff-filter=A --name-only` naming `d052f047` as the commit that added the
four files, `205b6fad` as the one that deleted them, and the recovered tree measuring 12, 158, 31 and 20
lines against that commit's own 221-line stat.

**The review said this crate was never committed anywhere, and that is wrong.** `arvo-standards` was
added on this branch at `d052f047` and deleted three commits later at `205b6fad`, so it is in the
branch's own history and nowhere in its tree, which is why a search of the working directory finds
nothing. The reviewer looked on disk and on the heads.

That changes the remedy from the one the review prescribed.
`evidence-lives-in-the-repo-or-it-never-happened.md` splits on exactly this: where the spike is
recoverable, it is recovered into a committed sketch beside the file citing it, the claim was unciteable
and now is not, and no audit pass and no rewrite of the citing file follows. Only where it cannot be
recovered is the number void.

So the four files come back verbatim as a sketch, and the addendum says where they went, what the figure
was measured against, and how it reconciles with the ten-and-fourteen a later round measured
independently. The two counts do not contradict each other: the lints report one hit per line and only
the first primitive on a line, so a count is a property of the probe's layout as much as of the contract,
which is the thing neither number said about itself.

**The locked file is not edited.** Its bytes stand and the addendum sits beside it.
