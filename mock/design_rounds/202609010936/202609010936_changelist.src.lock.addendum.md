# Addendum to 202609010936's src changelist

**Date:** 2026-09-01
**Written by:** round 202609011206, after a review reported the measurement as void.

The locked file is frozen and its bytes are unchanged.

## What was reported, and what actually holds

A review of this pull request found that the "fifteen positions" figure rests on
a crate called `arvo-standards` that it could not find, and read that as the
figure being void under
`evidence-lives-in-the-repo-or-it-never-happened.md`.

**The crate was committed.** It was added on this branch at `d052f047`, four
files and 221 lines, and deleted three commits later at `205b6fad` when its
contents moved into `arvo-format`. It is in the branch's history and in no
branch's tree, which is why a search of the working directory and of the heads
comes back empty.

That is the recoverable case rather than the void one, and the rule's remedy for
it is recovery, not an audit: the spike is committed beside the file citing it
and the claim that was unciteable becomes citeable. It is now at
`mock/research/sketches/the-standards-alias-refusal/`, verbatim, and anyone
doubting the figure can read the crate that produced it.

## What the figure is a measurement of

Fifteen refusals against the tree as it stood at `cb456dcf`. A later round, which
landed as pull request #55, built its own probe in a committed crate and counted
ten associated constants and fourteen const generic parameters, and reported that
it could not reproduce fifteen.

**The two are not in contradiction and neither said the thing that reconciles
them.** Both lints report one hit per line and only the first primitive on a
line, so what is counted is positions-as-laid-out rather than positions-in-the-
contract, and two probes writing the same contract differently count differently.
The later figure is the better one because it separates the two kinds of position
and says which tree it was taken on.

The headline both support is unchanged and is the part that mattered: no crate
outside `arvo-format` could implement the format contract without suppressing a
lint.
