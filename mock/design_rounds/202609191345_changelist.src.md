# src changelist

Executes the doc CL: closes the `core` shadowing hole (B1), the cross-end coverage gap on the
Saturate/Clamp completion arm (B2), and the smaller findings C1 through C5 from the fifth review of
PR #146. C6 and C7 need no source change, per the topic file.

## CHANGE: `probe` directory ADDED at `mock/research/202609191400_core_shadowing_probe/`
File: `mock/research/202609191400_core_shadowing_probe/`
Verification: `ls mock/research/202609191400_core_shadowing_probe/*.rs mock/research/202609191400_core_shadowing_probe/run.sh mock/research/202609191400_core_shadowing_probe/run.out` lists six `.rs` arms, the runner and its committed output. `bash mock/research/202609191400_core_shadowing_probe/run.sh` reproduces `run.out` on the pinned toolchain.

## CHANGE: `USize` and `ISize` aliases FROM `core::primitive::usize::BITS` TO `::core::primitive::usize::BITS`
File: `mock/crates/arvo-format/src/lib.rs`
Verification: `grep -c '{ ::core::primitive::usize::BITS }' mock/crates/arvo-format/src/lib.rs` returns 2.

## CHANGE: lint `the-platform-width-points-read-the-pointer-width` ADDS a second arm refusing `mod core`, `use ... as core` and `extern crate ... as core`
File: `mock/lints/the_platform_width_points_read_the_pointer_width.rs`
Verification: `grep -c 'fn core_bindings' mock/lints/the_platform_width_points_read_the_pointer_width.rs` returns 1, and the lint's own test module exercises all three shapes plus the silent controls.

## CHANGE: lint test module MOVED FROM inline `mod tests` TO `mock/lints/the_platform_width_points_read_the_pointer_width/tests.rs`
File: `mock/lints/the_platform_width_points_read_the_pointer_width.rs`, `mock/lints/the_platform_width_points_read_the_pointer_width/tests.rs`
Verification: the parent file ends in `#[path = "the_platform_width_points_read_the_pointer_width/tests.rs"]` ahead of `mod tests;`, which is what keeps mockspace's non-recursive lint discovery from registering the subdirectory as a second top-level lint. `cargo mock test` runs both crate trees' lint suites green.

## CHANGE: `the_broken_maps.rs` ADDS `m1_no_far_lo_guard` and `m2_no_far_hi_guard`
File: `mock/crates/arvo-format/src/apply/tests/the_broken_maps.rs`
Verification: `grep -c 'pub(super) fn m[12]_no_far_.*_guard' mock/crates/arvo-format/src/apply/tests/the_broken_maps.rs` returns 2.

## CHANGE: `the_cross_end_saturation.rs` ADDED, the cross-end matrix law for the Saturate/Clamp arm
File: `mock/crates/arvo-format/src/apply/tests/the_cross_end_saturation.rs`, `mock/crates/arvo-format/src/apply/tests/mod.rs`
Verification: `cargo test -p arvo-format apply::tests::the_cross_end_saturation` reports 4 tests passing against shipped `apply.rs`. Applying M1 or M2 directly to `apply.rs` line 237 and rerunning fails exactly 2 of the 4, confirmed by hand during this round and reverted before commit.

## CHANGE: `the_translation_law.rs`'s `new_law` FROM private TO `pub(super)`
File: `mock/crates/arvo-format/src/apply/tests/the_translation_law.rs`
Verification: `grep -c 'pub(super) fn new_law' mock/crates/arvo-format/src/apply/tests/the_translation_law.rs` returns 1.

## CHANGE: `the_carry_past_the_index.rs` test `the_step_onto_the_lowest_slot_is_in_range_only_where_the_range_starts_there` ADDS a `new_law(bad)` assertion ahead of its own hand-rolled cross-end checks
File: `mock/crates/arvo-format/src/apply/tests/the_carry_past_the_index.rs`
Verification: `grep -c 'assert_eq!(new_law(bad), Maybe::Isnt);' mock/crates/arvo-format/src/apply/tests/the_carry_past_the_index.rs` returns 1, `cargo test -p arvo-format apply::tests::the_carry_past_the_index` green.

## CHANGE: `the_platform_width.rs` comment near the const-assertion arm NARROWED to the free `const _` item shape
File: `mock/crates/arvo-format/src/tests/the_platform_width.rs`
Verification: `grep -c 'A free .const _. item' mock/crates/arvo-format/src/tests/the_platform_width.rs` returns 1; the comment no longer says a "const assertion" would be evaluated.

## CHANGE: `DESIGN.md.tmpl` ADDS an `## Open items` section naming review146d items 4 and 7
File: `mock/crates/arvo-format/DESIGN.md.tmpl`
Verification: locked in the doc CL, `202609191342_changelist.doc.lock.md`; unchanged by this src CL.

## Not actioned

C6 (commit subject too long): no fix, squash at merge replaces the subject.
C7: not actioned, the review states it is not a defect.
