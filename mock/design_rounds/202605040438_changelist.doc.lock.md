**Date:** 2026-05-04
**Phase:** DOC
**Round:** 202605040438
**Scope:** Doc-template prose alignment with shipped state from round 202605031748.

# Doc-side execution plan

Two doc surfaces drift from the chassis state shipped in round 202605031748. This round corrects each.

## arvo-mask-contracts

### `mock/crates/arvo-mask-contracts/README.md.tmpl`

The current header describes the trait as "abstract surface over `Mask64`, `Mask256`, `BitMatrix` and other concrete masks from `arvo-bitmask`". Both `Mask64` and `Mask256` were deleted in round 202605031748 per Decision C. The trait was also renamed `Mask` → `MaskOps` in the same round.

Rewrite to name the chassis form explicitly:

> "Const-trait surface for `Mask<W>` chassis instances and `BitMatrix<W, N>` concretes from `arvo-bitmask`. The trait is `MaskOps<const W: Width>` (renamed from `Mask` in round 202605031748). Concrete impls live in `arvo-bitmask` against `Mask<Bits<W, S, Sign>>` for the strategy/width/sign axes the consumer chose."

The replacement mentions both the chassis (`Mask<W>`) and the dispatch type (`Bits<W, S, Sign>`) so a reader landing here understands they pick the axes at the use site.

## arvo-bitmask

### `mock/crates/arvo-bitmask/src/node.rs` module doc

Line 5 of the module doc says `NodeId` is "Used by `BitMatrix64` / `BitMatrix256` and any consumer that speaks in nodes rather than raw integers." Both `BitMatrix64` / `BitMatrix256` are deleted; the chassis is `BitMatrix<W, const N: Cap>`.

Rewrite to chassis form:

> "Used by `BitMatrix<W, N>` and any consumer that speaks in nodes rather than raw integers."

This shape matches how `mask.rs` (line 11-15) and `ops.rs` (line 3-12) reference the chassis; the asymmetry was a missed pass in the round 202605031748 sweep.

## What is NOT in scope

- The locked SRC CL `202605031748_changelist.src.lock.md` is frozen per the mockspace state machine. The two CHANGE-block claim/source mismatches noted by the senior reviewer remain in place as recorded audit leaks; mockspace #318 lint will catch this class at lock time when shipped.
- `MaskOps` concrete impls on `Mask<Bits<W, ...>>` and `bitfield!` macro routing through `BitPrim::mask_low`: deferred to Round 5 (#315) per round 202605031748 src CL final notes.

## Cross-references

- Topic: `202605040438_topic_round_3_doc_drift_cleanup.md`.
- Prior round: `202605031748/` (closed, merged via PR #44).
