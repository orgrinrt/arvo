# Doc changelist: addition admitted on a declared signature

Consumes `202609111010_topic.addition-admitted-on-a-declared-signature.md`, the one open topic. It rests
on the sketch `mock/research/sketches/202609111010_addition-obligation-and-window/`, which establishes
that the obligation shape refuses at the tool the round pins it with, that a window family of slot
ranges builds without `generic_const_exprs`, that the exact step reproduces the canon's witness counts,
and that the carry refusal is reachable only from an outside format.

## `crates/arvo-format/DESIGN.md.tmpl`

Canon declaration, at the top. Add `ruling::the_additive_and_absorption_verdicts_are_canon` and the two
propositions it ratifies, `law::additive_associativity_under_wrapping`,
`law::additive_associativity_under_saturation` and
`law::additive_associativity_recovered_under_declared_nonnegative_operand_window`. Keep everything
already declared.

The admission rule section. Add a paragraph saying addition is the instance this crate ships, one
member under the rule rather than the start of a list, and pointing at the section that states it. The
fused multiply-add paragraph stays as it is.

What this crate does not decide. Add `mixed_numeral_addition`, which is why addition takes one
signature; `reduction_order_or_associativity` and `what_a_numeral_guarantees_to_a_fold`, which is why
the associativity verdict is about the binary operation and says nothing about a fold; and a numeral
value type, which no row licenses and which would be a second placement beside the one the derivation
performs, so addition takes and returns slots.

The surface table. The `Operation` row's shipped members become `Add`, with a consumer's own sitting
beside it. The free-function paragraph gains `sum_position`, `add`, `addition_reach` and
`addition_is_associative`, and the verdict paragraph gains `is_addable`.

The panic section under the applied map. `panic_on_overflow` takes the position and the dither, the two
things `adapt` takes, and reports whether the rounded slot leaves the range. The text already says the
verdicts refuse a position; the change makes the sentence about the function true, and records that no
operation panics on its own account: the caller's build decides, from the same inputs the operation
took.

The rounding region. Add the fourth way the thing a mode reads can fail to matter: no position is off
the grid, so the rounding region is never entered and every mode commutes.

The reach. It gains a grid state beside the tie state, as one coordinate of three values rather than
two flags, so an on-grid reach with a tie cannot be written. On the grid the rounded value is the
position itself, so the excursion above the highest slot is strict rather than inclusive. The paragraph
counting the facts the laws derive from the reach moves from five to six. The section's owed laws gain
the on-grid arm, and its surface paragraph gains `on_grid` and `reaches_off_the_grid`. The closing
paragraph saying no code reads the predicates is replaced: addition reads them.

A new closing section, addition, stating:

- the exact step, derived from the denotation, `a + b + phase` in slot units, and why no quantum and no
  radix appears in it;
- the obligation forced by the step, refusing a magnitude-indexed quantum and a sum the slot coordinate
  cannot carry, its verdict form, and where each refusal is seen;
- `Add`, the operation, and what reads its arity;
- the composition with `adapt`, and the dither passed through;
- the associativity verdict: the reach addition supplies, the completion question at a whole phase,
  the fixed offset rounding adds at a fractional phase where the rounding half holds, the conjunction
  over both offsets for stochastic rounding, and the refusal where the rounding varies across the reach,
  stated as a cost the tests count rather than a defect;
- what it does not build and why: the magnitude-indexed family, blocked in design by an applied map with
  no magnitude coordinate, and in the canon by the absence of any row saying what completion under
  wrapping means on a representable set that is a union of magnitudes;
- the laws it owes tests for, and the surface it names.

## `crates/arvo-format/README.md.tmpl`

One paragraph after the adaptation's: addition is the operation shipped, taking and returning slots,
with the verdict for where it is associative beside it.

## `DESIGN.md.tmpl` at the root

The crate table's `arvo-format` row names addition. The contracts paragraph lists `Add` among the
instances.

## The generated pages

`cargo mock` regenerates `docs/` from the templates above in the same commit as each template.

## What the source changelist will carry

The deletion of the tautological tests the topic names, before any addition source lands; the
vocabulary and inventory facts restated as exhaustive matches; totality over every admitted width of both
slot families; the grid state in `Reach` with the cross extended to it; `panic_on_overflow` at a
position; the `addition` module and its tests; the `trybuild` cases for the four refusals.
