# Doc changelist: `half_up` ties toward positive infinity

**Date:** 2026-09-19
**Round:** bring `arvo-format`'s `half_up` into line with `ruling::half_up_denotes_ties_toward_positive_infinity`,
and give the tree the instruments that would have caught the drift.
**Topics consumed:** `202609192202_topic.half-up-ties-toward-positive-infinity.md`, the only flat topic on
this branch; no ref carries another.
**Scope:** `mock/crates/arvo-format/DESIGN.md.tmpl` and `mock/crates/arvo-format/README.md.tmpl`.

## Canon

`ruling::half_up_denotes_ties_toward_positive_infinity` governs, ratified on two expert reads under
`ruling::the_panel_finishes_the_canon_without_him`. `ruling::the_ambiguous_rounding_word_is_retired_for_six_explicit_names`
already governs the vocabulary and is unchanged. No row changes in this round.

## CHANGE: the design declares the ruling
File: `mock/crates/arvo-format/DESIGN.md.tmpl`, the canon list at the top.
Add `ruling::half_up_denotes_ties_toward_positive_infinity`, for what `half_up` denotes.

## CHANGE: the adaptation section states the denotation, the note and the alias gap
File: `mock/crates/arvo-format/DESIGN.md.tmpl`, "The adaptation is a first-class object".
After the retired-word note: `half_up` is `floor(x + q/2)`, a tie toward positive infinity at every sign;
it is not Java's or Python's `HALF_UP`; ties away from zero is not one of the six and is reached as the
alias `toward_zero(x + sign(x) q/2)`. Then the gap: the surface does not let the shift be formed from a
position, only from the slot and ratio it was built from and only where the doubled denominator fits, and
no exact operation on a position is named. Named as a gap, not filled.

## CHANGE: the oracle's rounding half comes from the canon, and the sweeps plant wrong tie rules
File: `mock/crates/arvo-format/DESIGN.md.tmpl`, "The laws the applied map owes tests for".
The oracle computes each mode from its formula over an exact rational rather than choosing between
neighbours, so its one remaining reading taken from the implementation is `Clamp` as `Saturate`. The
sentence that it reads a `HalfUp` tie away from zero, and that the denotation is open, goes. Add the
planted wrong tie rules every sweep must report, and a third sweep over every tie at every admitted
width of 1 to 16 bits and at the ends of every wider range.

## CHANGE: MATLAB's Nearest lands and Round is the alias
File: `mock/crates/arvo-format/DESIGN.md.tmpl`, "The overflow map is total and the rounding map is not"
and "What a parity arm asserts".
Five of the six rounding methods land; Nearest is `HalfUp`. Round is ties away, the alias, and has no
mode. The no-tie control's reason is restated without the open question it cited.

## CHANGE: the symmetry section's classification, reflection and partition follow the ruling
File: `mock/crates/arvo-format/DESIGN.md.tmpl`, "The two symmetries of the grid".
- The comparison with the two fused multiply-add laws agrees at all five shared names on both rows; the
  hedge about the signed row goes.
- Half-up reads nothing besides the residue. The sign-at-a-tie cell has no shipped name and is kept as a
  planted rule so the derivation is shown to reach it.
- The rounding region reflects for toward zero and half-even only. The partition holds on a domain reaching
  a negative position and a tie, four and two, with the reason one half of it is a theorem and the other
  is measured. The sentence saying a ties-toward-positive-infinity rule would break it goes.
- Named, not expressed: half-up reflects on a reach with no tie, and every mode on an on-grid reach, and
  the mode-only reflection predicate refuses both. Conservative, same shape as the parity region.
- The partition law is asserted per mode and per symmetry, with each failure at a named witness.

## CHANGE: the readme carries the note
File: `mock/crates/arvo-format/README.md.tmpl`, the rounding paragraph.
One sentence: a tie at `-2.5` goes to `-2`, `half_up` is not Java's or Python's `HALF_UP`, and ties away is
reached by shifting half a step away from zero and rounding toward zero.
