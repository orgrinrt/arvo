**Date:** 2026-05-11
**Phase:** DOC
**Scope:** arvo + arvo-storage DESIGN.md.tmpl prose
**Topic:** `202605111110_topic_debug_on_numerics.md`

# Doc changelist: Debug on arvo numerics

Tiny mechanical doc round. The decision is captured in the topic; the doc CL only needs to surface that arvo numerics carry `Debug` in the prose readers consult when looking up the public surface.

## CHANGE: arvo-storage DESIGN.md.tmpl — Debug-on-numerics paragraph

File: `mock/crates/arvo-storage/DESIGN.md.tmpl`

Add a short paragraph in the "Primitive surface" or equivalent section noting that `Bits`, `USize`, `Bool`, `Cap` all impl `core::fmt::Debug` via projection through the `Transparent` chain. One paragraph; no new section needed unless the file has a natural Debug-related anchor already.

Verification: `grep 'core::fmt::Debug\|impl Debug' mock/crates/arvo-storage/DESIGN.md.tmpl` returns at least one match.

## CHANGE: arvo DESIGN.md.tmpl — Debug-on-numerics paragraph

File: `mock/crates/arvo/DESIGN.md.tmpl`

Add a parallel paragraph for the facade-layer numerics (`UFixed`, `IFixed`, `FastFloat`, `StrictFloat`). State that Debug delegates through the inner Bits / inner Ieee primitive. ConstDebug noted as BACKLOG (one-line).

Verification: `grep 'core::fmt::Debug\|impl Debug' mock/crates/arvo/DESIGN.md.tmpl` returns at least one match.

## CHANGE: arvo BACKLOG.md.tmpl — ConstDebug follow-up entry

File: `mock/crates/arvo/BACKLOG.md.tmpl`

Add a short entry: const-trait `ConstDebug` with fixed-buffer-write pattern + blanket `impl<T: ConstDebug> core::fmt::Debug`. Cite the soundness rationale (Formatter holds `&mut dyn fmt::Write` which isn't const-callable; fixed-buffer-and-adapt is the correct workaround). Unbacktick the type name per the BACKLOG convention so it doesn't trip the design-doc lint.

Verification: `grep -i 'ConstDebug' mock/crates/arvo/BACKLOG.md.tmpl` returns at least one match.
