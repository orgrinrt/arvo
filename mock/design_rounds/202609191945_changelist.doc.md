# Doc changelist: the twelfth review's premise controls

**Date:** 2026-09-19
**Round:** the twelfth review's findings on `feat/platform-width-types-on-a-wider-slot-range`
**Scope:** none of the templates; the round's change is inside the probe.
**Topics consumed:** `202609191923_topic.twelfth-review-premise-controls.md`, the only flat topic.
**Evidence:** `mock/research/202609191400_core_shadowing_probe/`, whose `run.sh` now runs a premise pass
and a spelling pass beside the flip pass, with the self-check as committed arms under `selfcheck/`.
Committed in `TOPIC` phase.

## Canon

Same governing row as the earlier platform-width changelists
(`a_platform_width_type_is_a_target_bound_member_of_the_format_family`). No row changes.

## No template changes

The review's findings are about what the probe's controls show and how its prose states it. No template
describes those controls. `crates/arvo-format/DESIGN.md.tmpl` states the shadowing findings without naming
the probe, and its sentence that an alias written against a literal width is indistinguishable on the host
from the right spelling agrees with the shipped-spelling arm now checking no value. Nothing it states
about which forms reach the leading-`::` path changes: every arm's own outcome in `run.out` is what it was
at the previous round's close.

Verification: `grep -rn 'core_shadowing_probe' mock/crates --include='*.md.tmpl'` returns nothing, and
`grep -n -i 'control' mock/crates/arvo-format/DESIGN.md.tmpl` returns no line about the probe.
