# Source Changelist: Strategy::NAME debug-gating

**Date:** 2026-04-21
**Phase:** SRC

## Files

- `src/strategy.rs:35` — wrap the `Strategy::NAME` trait item in `#[cfg(debug_assertions)]` and add a short doc line explaining the gate + pointer to `RANK`.
- `src/strategy.rs:82` — `#[cfg(debug_assertions)]` on `impl Strategy for Hot`'s `const NAME`.
- `src/strategy.rs:86` — same on `Warm`.
- `src/strategy.rs:90` — same on `Cold`.
- `src/strategy.rs:94` — same on `Precise`.

## Verification

- `cargo test -p arvo` passes (tests are under `cfg(test)`, which implies `debug_assertions`).
- `cargo check --release -p arvo` passes (release code does not reference `::NAME`).
- `cargo mock --lint-only` drift on `no-bare-static-str` for arvo: 5 → 0.
