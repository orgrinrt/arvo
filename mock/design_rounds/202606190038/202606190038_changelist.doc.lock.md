# Doc CL: const slice accessor on ConstCapacity

**Round:** 202606190038
**Phase:** DOC
**Target:** `mock/crates/arvo-tensor/DESIGN.md.tmpl`

Add `slice` / `slice_mut` to the documented `ConstCapacity` surface: two const
methods bridging the GAT backing array to `&[T]` / `&mut [T]` in const context,
the const analog of `Capacity`'s runtime `AsRef` / `AsMut`. For `Dim<N>` each
body is the built-in unsized coercion. Update the trait block, the `Dim<N>`
impl block, and add one sentence on why the const surface needed the bridge
(a `[const]` consumer that builds a scratch array and hands it to a
slice-taking const helper, the shape compile-time DAG grouping uses).
