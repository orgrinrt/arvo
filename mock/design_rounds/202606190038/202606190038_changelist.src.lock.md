# Src CL: const slice accessor on ConstCapacity

**Round:** 202606190038
**Phase:** IMPL

## CHANGE: trait `ConstCapacity` GAINS const methods `slice` / `slice_mut`
File: `mock/crates/arvo-tensor/src/capacity.rs`
Adds two const methods to the `pub const trait ConstCapacity` declaration:
`fn slice<T: Copy>(a: &Self::Array<T>) -> &[T];` and
`fn slice_mut<T: Copy>(a: &mut Self::Array<T>) -> &mut [T];`. The const analog
of `Capacity`'s runtime `AsRef` / `AsMut`. No default body (sole impl is
`Dim<N>`; pre-1.0 churn).
Verification: `grep -c "fn slice" mock/crates/arvo-tensor/src/capacity.rs`
returns 4 (two in trait, two in impl).

## CHANGE: `impl const ConstCapacity for Dim<N>` GAINS `slice` / `slice_mut` bodies
File: `mock/crates/arvo-tensor/src/capacity.rs`
Each body is the built-in array-to-slice unsized coercion (`a` where
`a: &[T; N]` -> `&[T]`).
Verification: `grep "fn slice_mut<T: Copy>(a: &mut \[T; N\]) -> &mut \[T\] { a }" mock/crates/arvo-tensor/src/capacity.rs` returns one match.

## CHANGE: test `tests/const_capacity.rs` GAINS const-slice round-trip arm
File: `mock/crates/arvo-tensor/tests/const_capacity.rs`
A `const` evaluation that builds a `Dim<N>::Array` scratch via `filled`, takes
`slice_mut`, writes through the slice with index arithmetic, takes `slice`,
and reads back: proves the const slice bridge round-trips in const context.
TDD: written before the methods exist (compile-fails on missing assoc fn).
Verification: `cargo test -p arvo-tensor const_slice` passes.
