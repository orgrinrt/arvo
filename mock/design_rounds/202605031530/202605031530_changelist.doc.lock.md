# Doc CL: post-#316 drift cleanup

**Date:** 2026-05-03
**Phase:** DOC (open).
**Round:** 202605031530
**Topic:** `202605031530_topic_post_316_drift_cleanup.md` (committed at HEAD 419fe49).

## Scope

Two DESIGN.md.tmpl files reference deleted types as if they were live:

- `mock/crates/arvo-strategy/DESIGN.md.tmpl`: `AlignedWideBits16<const BYTES: usize>` listed as a top-level type at lines 298-301; referenced in Pattern C wide-bucket example at line 371; mentioned in the prose framing at line 11, line 39, and line 464; named in the layout-soundness summary at line 525.
- `mock/crates/arvo-storage/DESIGN.md.tmpl`: `AlignedWideBits16<const BYTES: usize>` listed as a top-level type. `MultiContainer` family also referenced.

Both types were deleted in round 202605031400 (#316). Replacement is `WideBits<const BYTES: usize, A: Align = A1>` with marker types `A1` / `A16` / `A32` / `A64`.

Per `no-legacy-shims-pre-1.0.md`: no deprecation aliases retained. The DESIGN templates need to describe what actually exists in source.

## CHANGE: `AlignedWideBits16` MOVES FROM live-type-list TO history note

**Files:** `mock/crates/arvo-strategy/DESIGN.md.tmpl`, `mock/crates/arvo-storage/DESIGN.md.tmpl`

**Edit shape:** replace each `AlignedWideBits16<const BYTES: usize>` reference (declared as a top-level type or named in a Pattern C example) with the parametric `WideBits<const BYTES: usize, A: Align = A1>` form. Where the prose names `AlignedWideBits16` for the SIMD-aligned baseline, replace with `WideBits<BYTES, A16>` or describe the `Align` trait + `A1` / `A16` / `A32` / `A64` markers.

**Verification:** `! grep -n "AlignedWideBits16" mock/crates/arvo-strategy/DESIGN.md.tmpl` returns zero matches.
**Verification:** `! grep -n "AlignedWideBits16" mock/crates/arvo-storage/DESIGN.md.tmpl` returns zero matches.

## CHANGE: `MultiContainer` family removed from DESIGN narrative where named as live

**Files:** `mock/crates/arvo-storage/DESIGN.md.tmpl` (any prose still naming `MultiContainer<HiT, LoT>` or `MultiContainerHalf` as live).

**Edit shape:** replace with the actual `WideBits<BYTES, A>` storage shape.

**Verification:** `grep -c "MultiContainer" mock/crates/arvo-storage/DESIGN.md.tmpl` returns at most the count of intentional history-note mentions (e.g., "round 202605031400 deletes `MultiContainer<HiT, LoT>`"). Backticked occurrences in live-type lists return zero.

## Out of scope (defer to round 4 / #314)

The senior reviewer's findings about per-N `impl_fnv1a!` / `impl_xxhash3!` macros and per-N `hash_const` inherents are scope for round 4 (#314: ConstHash trait lift + bounded-generic narrowing). They do not land in this drift-cleanup round.

The broader updates to algorithm-crate DESIGN.md.tmpl prose (BACKLOG entries, etc.) follow naturally when round 4 lands; this round only fixes the active-type contradictions in the substrate-foundation DESIGN templates.

## Lock criteria

This doc CL locks when:

- Both verification greps return the expected results.
- DESIGN.md.tmpl files render cleanly (no stale type references appearing as live).
