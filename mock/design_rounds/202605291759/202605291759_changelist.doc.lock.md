# Doc changelist: scope generic_const_exprs gates

**Date:** 2026-05-29
**Phase:** DOC
**Round:** 202605291759 (scope generic_const_exprs gates)

## Scope

No documentation template changes. This round scopes the
`generic_const_exprs` crate-root gates and adds source-level
justification comments. The only `.md.tmpl` mention of the feature in a
crate losing the gate is `arvo-bits-contracts/DESIGN.md.tmpl`, which
already states the predicate-per-impl-block shape avoids the GCE cycle.
Removing that crate's unused gate aligns the source with the documented
intent, so the doc needs no edit.

This doc changelist exists to advance the round to the source-editing
phase. The substance lands in the source changelist.

## Files

None.
