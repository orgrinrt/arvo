# Source changelist: scope generic_const_exprs gates

**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605291759 (scope generic_const_exprs gates)

## Summary

Remove the `generic_const_exprs` crate-root gate from the four crates that
compile clean without it (empirically verified by remove-and-check plus
`cargo test --doc`). Add a uniform soundness-justification comment above the
retained gate in the eight crates that genuinely use the feature. Augment the
`cap_size` definition comment with the WATCH-soundness note. Add the #624
const_convert clarifying comment above arvo-storage's retained const_convert
gate. Pinned nightly: `nightly-2026-05-28`.

## CHANGE: remove `#![feature(generic_const_exprs)]` from `arvo-bits`
File: `mock/crates/arvo-bits/src/lib.rs`
Verification: `! grep -q "feature(generic_const_exprs)" mock/crates/arvo-bits/src/lib.rs`; `cargo +nightly-2026-05-28 check -p arvo-bits --all-targets` clean.

## CHANGE: remove `#![feature(generic_const_exprs)]` from `arvo-bits-contracts`
File: `mock/crates/arvo-bits-contracts/src/lib.rs`
Verification: `! grep -q "feature(generic_const_exprs)" mock/crates/arvo-bits-contracts/src/lib.rs`; `cargo +nightly-2026-05-28 check -p arvo-bits-contracts --all-targets` clean.

## CHANGE: remove `#![feature(generic_const_exprs)]` from `arvo-hash`
File: `mock/crates/arvo-hash/src/lib.rs`
Verification: `! grep -q "feature(generic_const_exprs)" mock/crates/arvo-hash/src/lib.rs`; `cargo +nightly-2026-05-28 check -p arvo-hash --all-targets` clean.

## CHANGE: remove `#![feature(generic_const_exprs)]` from `arvo-storage`; add #624 const_convert clarifying comment
File: `mock/crates/arvo-storage/src/lib.rs`
Verification: `! grep -q "feature(generic_const_exprs)" mock/crates/arvo-storage/src/lib.rs`; `grep -q "143773" mock/crates/arvo-storage/src/lib.rs`; `cargo +nightly-2026-05-28 check -p arvo-storage --all-targets` clean.

## CHANGE: add soundness-justification comment above retained gate in eight crates
File: `mock/crates/{arvo,arvo-bitmask,arvo-comb,arvo-graph,arvo-spectral,arvo-sparse,arvo-strategy,arvo-tensor}/src/lib.rs`
Verification: each crate root carries the comment block immediately above `#![feature(generic_const_exprs)]`; `grep -c "97156" mock/crates/<crate>/src/lib.rs` returns 1 per crate; `cargo +nightly-2026-05-28 check --workspace --all-targets` clean.

## CHANGE: augment `cap_size` definition comment with WATCH-soundness note
File: `mock/crates/arvo-tensor/src/cap.rs`
Verification: the existing `cap_size` doc/lint-allow comment gains the GCE WATCH-soundness note (#97156 unreachable, TypeId banned, #628 migration tracker); `grep -q "97156" mock/crates/arvo-tensor/src/cap.rs`.

## CHANGE: remove dead `generic_const_exprs` gate from 19 test targets
File: `mock/crates/{arvo,arvo-bits,arvo-bitmask,arvo-hash}/tests/*.rs` (19 files)
Verification: each removed test target compiles clean via `cargo +nightly-2026-05-28 check -p <crate> --test <stem>`. The `unstable-features.md` audit scope includes `tests/`, so dead test-target gates are in #626 scope. All 44 arvo test targets gating GCE were empirically probed (remove-and-check): 19 compile clean without the gate (removed), 25 fail with `E0275: overflow evaluating whether [(); ...::{constant#0}] is well-formed` because they instantiate GCE-bounded types (`Array`, `BitMatrix`, `Matrix`) and keep the gate. Removed: arvo-bits/{aliases,bit_access,bit_logic,bit_sequence,bit_width,bitfield,bits_const_arith}, arvo-bitmask/{mask256,mask_const_arith,mask_ops,mask_ops_blanket_const,mask_scan}, arvo-hash/algo, arvo/{bitfield_const_eq_default,ifixed_const_arith,logical_width_const_probe,numeric_contracts_const_probe,predicate_family_const_probe,ufixed_const_arith}.

## Out of scope (filed separately)

The nightly-2026-05-28 pin (#629) surfaced ~113 pre-existing non-GCE warnings across arvo (dead `adt_const_params`/`const_param_ty_trait`/`const_ops`/`const_cmp`/`try_trait_v2` gates, unused imports, private-in-public traits). None relate to GCE and none are introduced by this round. Tracked as task #633.

## Lock criteria

- All twelve crate roots in the intended end state (four without the gate, eight with gate + justification).
- Dead test-target GCE gates removed (19), needed ones kept (25), each empirically verified.
- `cargo +nightly-2026-05-28 check --workspace --all-targets`: 0 errors. The remaining warnings are the pre-existing non-GCE drift tracked in #633; zero cite `generic_const_exprs`.
- `cargo +nightly-2026-05-28 test --workspace` green.
