[package]
name = "arvo-placement"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Where a declared number's bits sit, which no declared operation reads"

[dependencies]
arvo-format.workspace = true

[lints.rust]
unsafe_op_in_unsafe_fn = "forbid"
