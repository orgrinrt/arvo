[package]
name = "arvo-bits"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "An exact-width bit container: masked construction and masked cast, no arithmetic"

[dependencies]

[lints.rust]
unsafe_op_in_unsafe_fn = "forbid"

[dev-dependencies]
trybuild = "1"
