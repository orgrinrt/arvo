# arvo-bits — Design

Bit-level primitive crate. Sits between arvo (numeric primitives) and
arvo-bitmask (fixed-width mask types). Ships two kinds of surface:

- **Contracts** — traits for bit-width introspection (`BitWidth`),
  individual bit manipulation (`BitAccess`), bit-sequence scanning and
  popcount (`BitSequence`), and whole-word bitwise logic (`BitLogic`),
  with concrete impls so all arvo fixed-point containers are
  bit-accessible.
- **Opaque-bit concretes** — fielded non-arithmetic containers for
  fixed-width bit-pattern identities. Arithmetic fielded types (UFixed,
  IFixed) still belong in L0 arvo; mask-specific concretes (Mask64,
  Mask256, BitMatrix) still belong in L2 arvo-bitmask.

`#![no_std]`, no alloc.

Depends on arvo. No other arvo-family dependencies.

## Why this crate exists

arvo defines numeric types (UFixed, IFixed, Float). arvo-bitmask
defines bit-level types (Mask64, Mask256, BitMatrix). These are
fundamentally different type families: numerics do Add/Mul/Sqrt,
bitmasks do OR/AND/popcount. The original structure had arvo-bitmask
depending directly on arvo, but bitmasks are not numeric — the
dependency existed only for the arvo-types-only lint, not for any
numeric operation.

arvo-bits is the contract layer. It defines what it means for a type
to be "a sequence of bits" without requiring numeric semantics. Both
arvo numerics and arvo-bitmask types implement these contracts, but
the contract itself is independent of either.

## Const generic bit-count types

arvo uses typed const generics for fixed-point bit counts. These are
defined in arvo and re-exported here for use in trait signatures:

```rust
#[derive(ConstParamTy, PartialEq, Eq)]
pub struct IBits(pub u8);  // integer bit count

#[derive(ConstParamTy, PartialEq, Eq)]
pub struct FBits(pub u8);  // fractional bit count
```

Requires `adt_const_params` (nightly). These ensure you cannot
accidentally swap integer and fractional bit counts at type
definition sites. Consumer code typically defines its own semantic
aliases over `UFixed` / `IFixed` and rarely touches `IBits` / `FBits`
directly.

## Traits

### BitWidth

How many logical bits this type occupies:

```rust
pub trait BitWidth {
    const WIDTH: USize;
}
```

For arvo types: `WIDTH = I + F` (UFixed) or `WIDTH = 1 + I + F`
(IFixed). The width is the logical bit count, not the container
size. Container size depends on the strategy marker and may be
larger. `USize` is arvo's newtype for `usize`.

### BitAccess

Individual bit manipulation:

```rust
pub trait BitAccess: BitWidth + Copy {
    fn bit(self, idx: USize) -> Bool;
    fn with_bit_set(self, idx: USize) -> Self;
    fn with_bit_cleared(self, idx: USize) -> Self;
    fn with_bit_toggled(self, idx: USize) -> Self;
}
```

All methods take `self` and return `Self` (functional style, no
mutation). `idx` is from LSB (bit 0 = least significant). Operates
on the raw bit representation of the backing container, not the
numeric value. `Bool` is arvo's boolean newtype (see below).

### BitSequence

Scanning and counting, mapping directly to hardware intrinsics:

```rust
pub trait BitSequence: BitWidth + Copy {
    fn trailing_zeros(self) -> USize;
    fn leading_zeros(self) -> USize;
    fn count_ones(self) -> USize;
    fn count_zeros(self) -> USize;
    fn is_zero(self) -> Bool;
}
```

Hardware mapping:
- `trailing_zeros` -> `cttz` (x86: `tzcnt`, aarch64: `rbit` + `clz`)
- `leading_zeros` -> `ctlz` (x86: `lzcnt`, aarch64: `clz`)
- `count_ones` -> `popcnt` (x86: `popcnt`, aarch64: hardware popcount)

These are the primitives that arvo-bitmask's `iter_set_bits`,
`lowest_set`, `highest_set` are built on.

### BitLogic

Whole-word bitwise logic. Single-instruction on the backing
primitive, loop-free:

```rust
pub trait BitLogic: BitWidth + Copy {
    fn bitor(self, other: Self) -> Self;
    fn bitand(self, other: Self) -> Self;
    fn bitnot(self) -> Self;
    fn bitxor(self, other: Self) -> Self;
    fn clear_lowest_set_bit(self) -> Self
    where
        Self: BitAccess + BitSequence;
}
```

`BitLogic` covers the four bitwise operations that map directly to
hardware instructions (`or` / `and` / `not` / `xor` on x86; identical
on aarch64). `BitSequence` covers SCAN / POPCOUNT; `BitAccess` covers
SINGLE-BIT read/write; `BitLogic` covers WHOLE-WORD logic. The three
are independent contracts that together describe a bit-bearing
container.

Concrete impls live on each `UFixed<I, F, Hot>` container family
(`Bit`, `Nibble`, `Byte`, `Word`, `DWord`, `QWord`) — each delegates
once via `UFixed::to_raw` / `from_raw` to the backing primitive's
core operator. Other strategies (`Warm`, `Precise`, `Cold`) do not
impl `BitLogic`: their wider containers make whole-word ops
meaningless at the logical-width level. `BitLogic` is a Hot-only
surface.

The `clear_lowest_set_bit` method has a default implementation on
`BitLogic` that uses `BitSequence::trailing_zeros` +
`BitAccess::with_bit_cleared`. Concrete types override with
`self.to_raw() & (self.to_raw() - 1)` (one instruction via x86 `BLSR`)
for the fast path. Exposed primarily for set-bit iterators that scan
and clear the lowest set bit at each step.

In addition to the trait methods, arvo-bits ships `impl core::ops::BitOr
/ BitAnd / BitNot / BitXor for UFixed<I, F, Hot>` (blanket on
`T: BitLogic` if orphan rules permit; otherwise per-type). Consumers can
use `a | b`, `a & b`, `!a`, `a ^ b` syntax. Implementing core TRAITS
(contracts) on arvo's own types is distinct from using core CONCRETE
types in signatures — the #115 "freedom from core" direction targets
the latter, not the former.

## Bool and Bit

Two types, same physical width, different semantic domains:

**`Bit`** = `UFixed<{IBits(1)}, {FBits(0)}, Hot>` — a 1-bit data
value. Stored in columns. Participates in bit operations. Backed
by u8 (Hot strategy, minimum container).

**`Bool`** = `Bool(bool)` — a newtype over Rust's `bool`. Returned
by predicates (`is_zero`, `bit`). Used in control flow.

`Bit` is data. `Bool` is control flow. The boundary between them is
explicit conversion.

### Bool in control flow (Try)

`Bool` implements `Try` with `Residual = Infallible`:

```rust
pub struct Bool(bool);

impl Try for Bool {
    type Output = bool;
    type Residual = Infallible;

    fn branch(self) -> ControlFlow<Infallible, bool> {
        ControlFlow::Continue(self.0)
    }
    fn from_output(output: bool) -> Self { Bool(output) }
}
```

This enables `?` to extract the inner `bool` for use in `if`:

```rust
fn execute(&self, ctx: Context<...>) -> Outcome {
    if val.is_zero()? { ... }
    Outcome::done()
}
```

The `?` always takes the `Continue` path (Infallible residual is
uninhabited). Zero runtime cost. Works in any function returning a
type that impls `FromResidual<Infallible>` — which includes
hilavitkutin-api's `Outcome`, `Just`, `Maybe`, and `Result`.

### AsBool trait

Cold-path bridge and compatibility escape hatch:

```rust
pub trait AsBool {
    fn as_bool(&self) -> bool;
}

impl AsBool for Bool { ... }
```

For code paths outside WU execution, or for libraries that need to
expose Rust-native `bool` on their public API. Day-one the internal
codebase uses `?`; `AsBool` exists for boundary compatibility.

## Type family marker traits

arvo defines marker traits for its type families. arvo-bits uses
these as reference points (for documentation of what types are
covered), not as blanket-impl bounds:

- `IntegerLike` — F=0, pure integer semantics
- `FractionLike` — F>0, has fractional part
- `BitPresentation` — raw bits are accessible (all UFixed/IFixed)
- `FloatLike` — FastFloat/StrictFloat

## Implementation strategy

`BitWidth` / `BitAccess` / `BitSequence` are implemented as **concrete
impls on `UFixed<I, F, S>` and `IFixed<I, F, S>`**, not as blanket
impls over a `T: BitPresentation` bound. Reason: the L0
`BitPresentation` marker exposes only `LOGICAL_WIDTH` — there is no
method for arvo-bits to reach the underlying container. Keeping the
blanket-impl language would require extending L0 (closed round).
Concrete impls over UFixed/IFixed from arvo-bits stay within the
orphan-rule boundary (trait is local to arvo-bits, types are
foreign — local-trait-foreign-type impls are permitted) and
delegate bit-level ops through `UFixed::to_raw()` / `from_raw()` and
the equivalents on `IFixed`.

A sealed `BitPrim` trait (internal to arvo-bits) is implemented for
`u8` / `u16` / `u32` / `u64`, carrying the bit-manipulation surface:
`count_ones`, `trailing_zeros`, `leading_zeros`, `bit`,
`with_bit_set`, `with_bit_cleared`, `with_bit_toggled`. An
`IBitPrim` sibling covers `i8` / `i16` / `i32` / `i64`. The concrete
`impl BitAccess for UFixed<I, F, S>` delegates via
`UFixed::to_raw()` to the primitive's `BitPrim` method, then
`UFixed::from_raw(...)` wraps the result.

`BitWidth::WIDTH` for UFixed = `I.0 + F.0`, for IFixed = `1 + I.0 +
F.0`. Both IntegerLike (F=0) and FractionLike (F>0) types get bit
access by this route — the F>0 case enables using any UFixed as a
packed flag field (`UFixed<{IBits::ZERO}, {FBits(8)}, Hot>` treated
as 8 individual flag bits).

## Semantic aliases

Bit-size type aliases. These are `Bits<N>` aliases — one family,
one primitive. Default strategy is Hot (bit-level work typically
wants minimum container and wrapping semantics):

```rust
pub type Bit<S: Strategy = Hot>    = Bits<1,  S>;
pub type Nibble<S: Strategy = Hot> = Bits<4,  S>;
pub type Byte<S: Strategy = Hot>   = Bits<8,  S>;
pub type Word<S: Strategy = Hot>   = Bits<16, S>;
pub type DWord<S: Strategy = Hot>  = Bits<32, S>;
pub type QWord<S: Strategy = Hot>  = Bits<64, S>;
```

`Nibble` = 4-bit value (half-byte). `DWord` = 32 bits (double word).
`QWord` = 64 bits (quad word, our maximum logical width). x86
naming convention where word = 16 bits. Because Bits is a newtype
over UFixed rather than a type alias, the named aliases share
`Bits`'s opaque-identity trait surface: `BitWidth`, `BitAccess`,
`BitSequence`, `BitLogic`, but no arithmetic.

Consumers that want arithmetic on an N-bit value reach for
`UFixed<{IBits(N)}, {FBits::ZERO}, S>` directly. Consumers that
want opaque identity (bit logic, scan, popcount, equality, hash)
use `Bits<N>` or one of its named aliases. Storage footprint is
identical — the wrapper is transparent.

No SIMD-register-width alias: a 128-bit type would exceed our
I+F <= 64 constraint. SIMD register width is a strategy concern
(how many lanes of your type fit in a register), not a type alias.

## Opaque-bit container: Bits

`Bits<const N: u8, S: Strategy = Hot>` is a fixed-width opaque
bit-pattern container: a `#[repr(transparent)]` newtype around the
strategy-dispatched container primitive `<S as UContainerFor<N>>::T`.
Dispatch reuses arvo's `UContainerFor<N, S>` table — u8 for 1..=8
bits, u16 for 9..=16, u32 for 17..=32, u64 for 33..=64 under Hot.
`Bits<32, Hot>` is 4 bytes; `Bits<8, Hot>` is 1 byte; `Bits<28, Hot>`
is 4 bytes. Compile-time asserted: N must be in 1..=64.

```rust
#[repr(transparent)]
pub struct Bits<const N: u8, S: Strategy = Hot>(<S as UContainerFor<N>>::T)
where
    S: UContainerFor<N>;
```

Bits is not a UFixed wrapper — it is a parallel primitive family
that shares UFixed's container-dispatch table but presents a
deliberately smaller trait surface. Two `Bits<N>` values compare
via `PartialEq` / `Hash`; there is no which-is-bigger question on
a bit pattern. `Bits` does NOT implement `Add` / `Sub` / `Mul` /
`Div` / `Ord`. Consumers that want arithmetic at width `N` reach
for `UFixed<{IBits(N)}, {FBits::ZERO}, S>` directly.

`Bits::from_raw(raw: <S as UContainerFor<N>>::T) -> Self` takes the
exact container value. `Bits::to_raw(self) -> <...>::T` returns it.
For the u64-erased path used by consumers that already think in u64
(arvo-hash hash digests, hilavitkutin-str handles), `Bits::new(raw:
u64) -> Self` masks to N bits and narrows; `Bits::bits(self) -> u64`
widens back. Both are per-N const fns generated via macro expansion.

`From` impls land two conversion paths for ergonomic non-const call
sites:

```rust
impl<N, S> From<<S as UContainerFor<N>>::T> for Bits<N, S>
where S: UContainerFor<N> { ... }

impl From<u64> for Bits<N, Hot> { ... }  // per-N via macro
```

Non-const callers write `let b: Bits<N, Hot> = value.into()` or
`Bits::from(value)`. Const paths (`str_const!()` macro bodies,
`bitfield!`-generated setters) keep the explicit `Bits::<N>::new(raw)`
form — `From::from` is not `const fn` on current nightly.

Implements `BitWidth` (reports N), `BitAccess` (per-bit read/write),
`BitSequence` (scan / popcount), and `BitLogic` (whole-word bitwise)
by delegating to the inner UFixed's impls. The Strategy parameter
defaults to `Hot` because bit-level work wants the minimum container
and wrapping semantics; other strategies work if a consumer genuinely
wants them.

Primary consumer: arvo-hash's ContentHash alias (Bits<28>, u32-
backed, 4 bytes). General-purpose: any domain that needs a fixed-
width opaque bit identity. Future consumer: clause pass-key identity
in codegen; also hilavitkutin-str's Str handle (32-bit packed
identity) once the structural Bitfield variant below is adopted.

## Bitfield — packed layouts with named sub-ranges

Where `Bits<N>` is "N bits, no structure", the `bitfield!` macro
generates a `#[repr(transparent)]` struct over the same
container-dispatched storage, with named sub-ranges and per-field
accessor methods. Use it for packed-layout identities where the
consumer cares about semantic sub-ranges (flag bits, reserved
blocks, ID ranges).

Example:

```rust
use arvo_bits::bitfield;

bitfield! {
    /// Interned string handle. 32 bits total.
    pub struct StrHandle: 32 {
        /// 1 = runtime-interned, 0 = compile-time-interned.
        origin: 1 at 31,
        /// Reserved for future use.
        reserved: 3 at 28,
        /// 28-bit interned identity.
        id: 28 at 0,
    }
}
```

Generates a `StrHandle` that is `#[repr(transparent)]` over
`Bits<32, Hot>` (= 4 bytes). Inherent methods:

```rust
impl StrHandle {
    pub const BITS: u8 = 32;
    pub const fn new() -> Self;
    pub const fn from_bits(raw: Bits<32, Hot>) -> Self;
    pub const fn to_bits(self) -> Bits<32, Hot>;

    // 1-bit sub-range → Bit<Hot> = Bits<1, Hot>
    pub const fn origin(self) -> Bit<Hot>;
    pub const fn with_origin(self, value: Bit<Hot>) -> Self;
    // 3-bit sub-range → Bits<3, Hot> (no named alias at this width)
    pub const fn reserved(self) -> Bits<3, Hot>;
    pub const fn with_reserved(self, value: Bits<3, Hot>) -> Self;
    // 28-bit sub-range → Bits<28, Hot>
    pub const fn id(self) -> Bits<28, Hot>;
    pub const fn with_id(self, value: Bits<28, Hot>) -> Self;
}
```

Accessor types are `Bits<W, Hot>` where `W` is the sub-range
width — the exact-width arvo primitive, never a raw `u8` / `u16` /
`u32`. Common widths match the named aliases (`Bit<Hot>` = 1,
`Nibble<Hot>` = 4, `Byte<Hot>` = 8, `Word<Hot>` = 16, `DWord<Hot>`
= 32, `QWord<Hot>` = 64); uncommon widths use the generic `Bits<W>`
form. Setters take the same type and mask to the sub-range width
before writing, so garbage-high-bits on callers never corrupt
neighbouring fields.

The macro also generates a pre-shifted `<FIELD_UPPER>_MASK: Bits<N, Hot>`
associated constant per sub-range. For boundary bit-arithmetic
(consumer code that holds raw values before wrapping them into the
bitfield), the mask saves redeclaring the layout's internal
structure:

```rust
// In the StrLayout example above, the macro generates:
impl StrLayout {
    pub const ORIGIN_MASK: Bits<32, Hot> = Bits::<32>::new(1u64 << 31);
    pub const RESERVED_MASK: Bits<32, Hot> = Bits::<32>::new(0b111u64 << 28);
    pub const ID_MASK: Bits<32, Hot> = Bits::<32>::new(0x0FFF_FFFFu64);
}
```

`SHIFT` and `WIDTH` constants are not generated — consumers that
need them can read the field declaration directly; avoiding
speculative consts keeps unused-symbol footprint minimal.

Compile-time checks inside the generated code:

- Every sub-range must fit within N. `$lo + $field_bits > N` =
  compile error.
- N ≤ 64 (same as `Bits<N>`).
- Gaps (unused bits) allowed. They read as zero and setters leave
  them alone.
- Overlap detection between sub-ranges is deferred; for now authors
  are responsible for disjoint layouts. Tracked in BACKLOG.

No arithmetic. No sign. Bitfield is `Bits<N>` with a named-layout
overlay, carrying the same opaque-identity trait surface.

## What this crate does NOT contain

- Bitmask types (Mask64, Mask256, BitMatrix); live in arvo-bitmask.
- Numeric operations (Add, Mul, Sqrt); live in arvo.
- Set operations (union, intersection); live in arvo-bitmask.
- Float types; live in arvo.
- Fallible-return types (Outcome / Just / Maybe); consumer concern.
- Arithmetic fielded types (UFixed, IFixed); live in L0 arvo. The
  fielded-concrete permission in arvo-bits is limited to
  non-arithmetic opaque-bit containers like the Bits type above.

## Consumers

- **arvo-bitmask**: builds Mask types on BitWidth + BitAccess +
  BitSequence + BitLogic contracts. Mask64's `count()` delegates to
  `BitSequence::count_ones`; `union` / `intersection` / `difference` /
  `complement` delegate to `BitLogic::{bitor, bitand, bitxor, bitnot}`
  (or equivalently via `core::ops`). Bit scanning uses `trailing_zeros`
  / `leading_zeros`. No reach-through to raw `u64`.
- Other arvo algorithm crates (graph, sparse, comb, spectral) depend
  on arvo-bitmask, not arvo-bits, for adjacency and set work.

## Sources

- 202603241400_topic.arvo-crate-dependency-revision.md (arvo-bits design)
- 202603241200_topic.arvo-strategy-markers.md (strategy definitions)
- 202603241300_topic.arvo-strategy-aliases-and-open-questions.md (alias pattern)
- 202604191900_changelist.doc.md (original round)
- 202604270000_changelist.doc.md (BitLogic contract + arvo-bitmask sweep)
