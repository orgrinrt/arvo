# arvo-bits

Bit-level contracts for arvo numeric types. BitWidth, BitAccess,
BitSequence traits. Semantic aliases (Bit, Byte, Word, DWord, QWord).
Blanket impls for all arvo fixed-point types. `#![no_std]`, no alloc.
