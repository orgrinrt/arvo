# arvo-storage. Backlog

Designed-but-deferred items for the L0 storage crate. Names stay
unbacktickd until they ship.

## ConstParamTy on UFixed / IFixed

Manual ConstParamTy_ impl on UFixed and IFixed lets fixed-point types
appear directly as const generic arguments. Verified-friction in
rustc 1.96-nightly per the Q-A spike scope notes.
Lands when the friction clears or the workaround pattern stabilises.

## USize as Bits-rooted

Inner field of USize migrating from raw usize to Bits<{cfg-platform-width}, Hot>
for unified bit-storage view. Currently, explored the direction.
Currently parked. Future round revisits when the platform-width const
generic ergonomics improve.

## Cold strategy bitpacking

Bits<N, Cold> for N > 64 with bitpacked storage across multiple bare
primitives. Out of scope for. Future round.

## Round 3 SHIPPED (post-#316 work)

Round 3 (#313) of the foundations work shipped in round
202605031748. Mask\<W\> generic over Bits with the per-width
parallel structs (Mask256, BitMatrix64, BitMatrix256) deleted
entirely. Consumer code names Mask\<Bits\<64, Hot, Unsigned\>\> and
Mask\<Bits\<256, Hot, Unsigned\>\> directly, keeping the Strategy
and Signedness axes statically visible. BitPrim::WIDTH typing
finalisation was already shipped in Round 2 (#311) so the round
focused on the chassis collapse and the dogfood sweep (BitPrim
mask_low method, FastFloat / StrictFloat Identity, Mask::empty via
Identity::ZERO).

## #316 follow-ups (post-redesign)

The #316 foundational redesign deletes MultiContainer entirely and
introduces WideBits + AlignedWideBits16. The MultiContainer-related
backlog items are gone; the Round 1 BitPrim impl on MultiContainer
shipped briefly in 202605021800 and is removed in #316 alongside the
type. Hot tier expansion (AlignedWideBits32 / AlignedWideBits64) is
deferred to #320 with bench-driven justification per audit H1.

## Const-arith follow-ups

- ISize: signed platform-pointer-width companion to USize. Currently, punted on the call. Arvo's signed needs are met today by IFixed and IBits, not platform-width signed integers. Ship if a consumer surfaces signed-platform-pointer arithmetic as load-bearing.
- USize and Cap checked-arithmetic helpers (checked_add returning Maybe of USize). Currently, documented overflow as matching primitive usize (panic on debug-asserts, wrap on release). A Maybe-returning shim ships when a consumer surfaces overflow-as-error semantic.
- Workspace lint rule forbidding.0 field-access on arvo primitives outside the implementing crate. The const-trait surface from is the typed access path; the lint enforces it once the sweep validates that no false-positive call sites remain. Lives in mockspace-hilavitkutin-stack-lints.

## NU8 / NU16 / NU32 / NU64 family

Niche-packed Maybe-of-primitive companions to NUSize. Same shape over
the corresponding NonZero primitive (NonZeroU8 / NonZeroU16 /
NonZeroU32 / NonZeroU64). The N-prefix naming convention was
established with NUSize; per-width variants
ship when a consumer demands them. Plausible early demand: NU32 for
hash slots; NU8 for byte-shaped enums; NU16 for short content
identifiers. No round opens until a concrete consumer surfaces.

## Removed entries

(none)
