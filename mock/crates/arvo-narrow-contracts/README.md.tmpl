# arvo-narrow-contracts

The `Narrow<T>` const trait and the `Narrowed<N, T>` typed alias. The
unified mask-and-cast bridge that replaces the per-size `from_raw_uN`
/ `to_raw_uN` helpers deleted from `Bits` per round 202604271346 D-7.
`#![no_std]`, no alloc.
