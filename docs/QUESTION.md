<!--
  AUTO-GENERATED: DO NOT EDIT DIRECTLY

  Generated by: mockspace (mock)
  Generated at: 2026-08-31T17:22:40Z
  Source: mock/
  To regenerate: cargo mock
-->

# Open questions

Something the canon has not settled, with the options it is open between and whose call it is.

100 rows. Identifiers are permanent: assigned once, never reused, never renumbered.

| id | asks | options | decider | answered | bound | unblocks | topic | note | keywords |
|---|---|---|---|---|---|---|---|---|---|
| <a id="what-then-validate-requires"></a>what_then_validate_requires | What does the acceptance criterion's "then validate" clause require of the typestate? | Admissibility: the typestate refuses declarations it cannot serve and does not refuse ones it could, which owes a two-directional sweep and for which the panel holds no evidence., Usage: the typestate refuses operations violating the declared invariants with a readable diagnostic, which owes a diagnostic battery and for which the panel's evidence is substantial., Self-validation: the derived container is checked at derivation time to hold the declared range, which owes a range assertion per declaration and for which the panel's evidence is incidental. | op |  |  |  | [the_format](TOPIC.md#the-format) | Recorded as answered at `28` batch one, with the three parts left separable because each owes a different instrument, and with a challenge route named: a member may argue a part is truly not worth it, and that phrase is the bar. The register also carries `17`'s structural addition, that the criterion's own phrase "no caveats left" is a claim about the absence of a residue that no instrument can close. | validate, validation, admissibility, usage, self-validation, acceptance criterion, erasure, trusted base, diagnostic |
| <a id="which-width-coordinates-a-consumer-writes"></a>which_width_coordinates_a_consumer_writes | Which pair of width coordinates does a consumer write at the surface? | Total and fraction, the pair the machinery wants, which makes the negative-integer-width corner naturals with no signed ladder., Integer and fraction, the familiar pair, converted at the door at zero cost, at the price that every reflective surface must choose which pair it shows., Both, with each surface declaring which it speaks, keeping both audiences at the price of making the dual vocabulary permanent., Neither as a definition: the numeral is one object, a grid cut down to a reach, and the width pair is what that object is called in the constant-canonical-exponent case, so the question is what a consumer-facing projection of one definition should be. | panel | The fourth reading, and it dissolves the first three rather than choosing among them. `ruling::the_format_spine_is_canon` is ratified and ratifies `proposal::membership_of_the_representable_set_is_one_affine_predicate`: membership is one predicate over one parameterisation, an affine slot function with a quantum per magnitude and a phase, of which integers, fixed point, scaled integers and floats are points. So a width pair is not a definition of anything. It is what that one object is called in the constant-canonical-exponent case, which is exactly what option four says and what the register carried as a reading rather than a rival.  **What that leaves is a projection choice, and it is not a canon question.** Which pair a consumer writes at the surface, or whether both surfaces exist with each declaring which it speaks, is a decision about the shape of an API over a definition the canon has already fixed. It belongs to the design round for the format, not here, and it is recorded as such rather than left sitting in the canon's queue looking unsettled.  Reached through the same unfollowed two-hop edge as its siblings. |  | Argued to be prior to Q8, because it changes what the shape space is a space of. | [the_format](TOPIC.md#the-format) | The fourth reading is `24`'s and the register carries it as a reading rather than a rival. `24` section 9 separately names as op's own the adjacent question of whether the design admits numerals its coordinates cannot name; the surface-pair choice itself carries no named decider, so this row's `decider` is the default. | coordinates, width pair, total width, fraction width, integer width, negative integer width, grid, reach, canonical exponent, surface |
| <a id="mixed-numeral-addition"></a>mixed_numeral_addition | Is there an addition that takes values from two different numerals? | None exists, and that is the design: addition joins the consumer-determined block and the inference surface collapses to multiplication plus the container., It exists and is inferred: the result numeral is derived, which keeps the join in the inference surface and keeps the negative-integer-width corner live., It exists only through an explicit conversion: a consumer converts one operand into the other's numeral first, which collapses the inference surface while keeping the operation reachable. | panel |  | Not his, and the refusal that covers it is already recorded. This asks whether one operation exists in the design's operation set, which is the question he was put and returned:  > This bears on the prior answer and isn't mine to call, sine I'm not an expert > or a domain specialist on maths. This is why you and the panel exist.  `which_operation_set_the_design_ships` carries that verbatim and now names the panel. A question about whether a particular operation is in the set is downstream of the set, so it inherits the same answer: deriving it is the panel's, ratifying the result is his.  The second constraint is the standards demand, filed as `obligation::every_standard_convention_expressible_as_an_alias_over_the_primitives`. It is an adequacy test rather than a preference: a convention that cannot be written as an alias over the primitives is a gap in the primitives. Both conventions op named carry an addition across differing formats, so the first option has to be reached with that test satisfied or not reached at all.  | A large part of one cluster has no caller if the answer is no. `23`'s words, carried in the register: "One sentence from op collapses it." | [the_format](TOPIC.md#the-format) | Load-bearing outside its own unit: `59` records that unit two's one unconditional result rests on this being open, because under the second option addition aligns scales, alignment is a shift, and a shift is the coarsening that kills multiplication. None of that unit's five files cites it. | mixed numeral, addition, join, inference surface, conversion, lossless conversion, interoperation, scale alignment |
| <a id="what-a-datum-stands-for"></a>what_a_datum_stands_for | What does one datum of a numeral stand for? | A point: a datum is one value, and saturation's absorbing behaviour is a documented restriction on where a fold is sound rather than a denotation., An absorbing top: the top denotes everything above it, which is sufficient for soundness while the computation stays at the endpoint and whose necessary condition is not known., A constructor-level clause: the denotation clause is a statement about the constructor wearing the grammar of a statement about every datum, and what it distinguishes is a partitioning set denotation from an overlapping one., A set, admitted generally: intervals and set-valued data are first class, which costs the value-level total order, multiplicative associativity and the additive inverse, and buys verified optimisation, rigorous ODE work and exact geometric predicates. | panel |  |  |  | [the_primitive](TOPIC.md#the-primitive) | Op explicitly refused to bound this option set, so what is listed is what has been written down rather than what is admissible; that refusal is about the options and the register names no decider for the question, so `decider` is the default. A live sub-fork runs inside every reading: soundness (the derived numeral holds every value the operation can produce) against bestness (and no numeral smaller does), which `07` offers as a sharper replacement for the tightness framing and which composes with all four. | datum, denotation, absorbing top, saturation, interval, set-valued, point, soundness, bestness, tightness, total order |
| <a id="arithmetic-column-one-axis-or-two"></a>arithmetic_column_one_axis_or_two | Is the arithmetic column of the strategy table one axis, two, or a product of several? | Two axes: overflow policy and intermediate precision are independent and every strategy needs a value on both, which the preset table's own asymmetry and a committed thirty-four-run bench family both support., One axis: the four names are values of a single arithmetic-policy axis and the asymmetry is a wording problem in the preset table, keeping the four-name bijection with the two-by-two intact and adding no degrees of freedom., A product of several axes: the column is a product of however many policy axes exist, with the presets being named sections over it rather than a partition of it, which makes the axis list itself open. | panel |  | Not his, and the ground for that is his own instruction about how the strategy material is to be read. `ruling::the_strategy_intents_are_not_clear_cut`:  > And as such, it's not clear cut. You should not write these as clear cut and > settled. The *intent* is clear I think, but nothing about them is absolute > otherwise.  He offered the strategy descriptions for the canon in the same breath as "though don't have to be; Open for discussion and exploration!" (`36`). The intent behind each strategy is his and is settled. How many axes the arithmetic column decomposes into is structure underneath that intent, and structure is what he opened.  It is also decided by evidence rather than by preference: whether overflow policy and intermediate precision are independent is a fact about the design space, and the register already carries the preset table's asymmetry and a committed thirty-four-run bench family on one side of it. The third option, presets as named sections over an open product rather than a partition of it, is the shape `ruling::the_work_is_predicated_arms_composed` predicts, and no candidate has priced it against the other two.  **This is one of the three least certain moves in this pass.** The attack on it: the preset set is his intent, and if the axes are what the presets are defined over then naming the axes names the presets. If that holds, this is his and the move is wrong.  | Prior to Q6, because it decides what a wrap-or-clamp answer would be an answer about. | [the_strategy_axis](TOPIC.md#the-strategy-axis) | `25` section 8 states the one-axis reading as the genuine alternative op is choosing between rather than as refuted, which is the ground for this row's decider. `40` adds a correction the entry carries: the axis list should also carry the observable-or-not classification beside each axis, and the entry's corroboration count reads three rather than four. | arithmetic column, axis, overflow policy, intermediate precision, preset, headroom, layout, two-by-two, sections over a product |
| <a id="does-warm-wrap-or-clamp"></a>does_warm_wrap_or_clamp | Does the `Warm` preset wrap or clamp? | Clamp, matching the ratified preset table, which makes the committed wrapping bench family measure a different strategy than the one it names., Wrap, matching the committed bench family as implemented, which makes the ratified preset table's `Warm` cell wrong., Neither: the question dissolves under Q5's two-axis answer, because `Warm` then states a value on the overflow axis and a value on the intermediate-precision axis separately and the two bench families may be measuring two different axes. | panel |  |  |  | [overflow_policy](TOPIC.md#overflow-policy) | Op has declared the ratified table's `Warm` cell stale under his restated intent that Warm behave like native primitives in regular old Rust would; the register records that as direction rather than as an answer to this question, so `decider` is the default. Standing caveat on the evidence: `20` section 1.5's clamp-family arity crossover is self-flagged as contaminated, written after reading a commit subject line carrying its own conclusion, and is owed an independent read that has not run. | Warm, wrap, clamp, saturate, preset table, headroom, arity crossover, interior safety |
| <a id="which-carrier-the-packing-claim-is-about"></a>which_carrier_the_packing_claim_is_about | Which carrier is the claim that packing pays a claim about? | An eight-byte carrier: the claim is about displacing a machine word, and against one it measures true past a threshold record count., A two-byte or four-byte carrier: against `u32` the single-core answer is false and the four-core answer is true, and against `u16` the single-core answer is false and the four-core answer, measured past the cache, is also true., No fixed carrier and no threshold: the inequality is the claim, packing paying when the bytes saved per element divided by available bandwidth exceeds the decode cost added per element, with any particular break-even figure a fact about one machine., Footprint rather than throughput: the claim is about resident memory and never was about loop speed, and the multi-fold cut in resident bytes holds whether or not a loop gets faster., Whichever regime is detected, with at least two arms behind that detection and the carrier the claim holds against differing per arm. | measurement |  |  |  | [the_realisation_map](TOPIC.md#the-realisation-map) | Op answered the regime half at `32` and the register records it as dissolving rather than selecting: the canon names no regime, so both the one-core and four-core break-even bands are correct as the two arms rather than as rivals. He marks it a ratifiable intent, which is not ratified. What remains open is measurement-shaped and named by `27` itself: writes are unpriced, every arm in both files reads only; random and strided access are unmeasured, and the prefetcher-hiding mechanism the thread turns on is specific to sequential access; and other hosts would move every crossing. The footprint option has not been priced by any file in the panel. | packing, carrier, bitpack, bandwidth, contention, cores, break-even, footprint, resident memory, prefetcher, cache |
| <a id="one-numeral-family-or-several"></a>one_numeral_family_or_several | Do the numerals form one family, or several? | One family: every numeral is a shape in one space and joins are computed within it, which holds within one radix, zero bias and a closed shape space., Several families: numerals are partitioned and cross-family relationships are named rather than computed., A tie-break reading: stop requiring a unique least upper bound, return the set of minimal upper bounds, and name a stated rule that picks one, buying totality with a policy instead of with closure., The seam is the step set: two numerals are in one family exactly when their admitted step sets are nested, computed from the members rather than declared., A route to one family surviving all three conditions, which nobody has proposed. | panel |  | Not his twice over. It is a mathematics question, about whether a set of shapes under an inclusion order has unique least upper bounds, and he has said where those go:  > This bears on the prior answer and isn't mine to call, sine I'm not an expert > or a domain specialist on maths. This is why you and the panel exist.  And the row already records him declining to supply his instinct as an answer: it is one family, and he said explicitly not to act on that, because acting on an instinct is how the previous panel locked itself into a shape that did not fit. An instinct withheld on purpose is not a decision waiting to be collected.  The bound the derivation runs inside is soundness, which here means the order's own algebra: whichever answer holds must hold for the three conditions the register records, and the third option buys totality with a stated tie-break rather than with closure, which is a property to establish rather than a taste to pick.  |  | [the_format](TOPIC.md#the-format) | Op's instinct is one and he said explicitly not to act on it, because acting on an instinct is how the last panel locked itself into a shape that did not fit. The register records that the question is not load-bearing where it was thought to be: `06`'s classification finds the no-determiner cell empty and coherence says it must be, so a design inferring a cross-family target picks in one impl rather than computing a least upper bound. A fork upstream of the whole question is `08`'s: whether the canonical exponent is a member of the design or its two currently-named values are the design. | family, numeral family, lattice, least upper bound, join, meet, inclusion order, step set, radix, canonical exponent, antichain |
| <a id="the-width-surface-crossing"></a>the_width_surface_crossing | What should the crossing be between a consumer's written width literal and the type system's representation of it? | Keyed on const generics directly with a per-width bridge, one row per written width., A raw nat surface with no const bridge, the consumer spelling the width as a hand-written binary digit tower., A nat surface with a shipped alias layer, names shipped for the towers., A nat surface with a consumer-side declaration macro minting the names it wants., A hybrid: a consumer-facing type alias carries the const parameter, the numeral is keyed on nats, and the bridge fires once at the alias so the algebra never re-enters it., The width type visible to a consumer is the literal itself, with the structural nat demoted to a hidden projection., Declare the output width explicitly and check it is wide enough by a free type-level comparison, so no reverse table exists because no output is ever named computationally. | panel |  | Not his, because the ergonomic bar this would be decided against is already stated in his own words, and every route is a compiled arm rather than a preference.  `obligation::a_primitive_for_every_position_a_bare_number_would_take` carries the quote, from a consumer's design round:  > We want arvo there. No bare usize other than in const generics for smoother and > more ergonomic api, and even there, only when truly painful otherwise.  That is twice bounded and both bounds are on this question: const generics are the excepted position, and inside the exception the bare form is used only where the alternative is genuinely painful. `ruling::warms_objective_is_the_intuitive_best_choice` supplies the other half, that intuition is the objective and convention is its baseline rather than its definition.  The third constraint is `obligation::every_standard_convention_expressible_as_an_alias_over_the_primitives`, an adequacy test rather than a feature: whichever crossing ships, the standard conventions have to be writable as first-class aliases over it, which is what the shipped-alias-layer option is about and is a falsifiable test on the others.  Every route listed was compiled and is a real arm, and the thread's sharpest finding is that the ceiling belongs to neither the bridge nor the const surface but to the price of crossing back. That is engineering with evidence, inside a stated bar.  **This is one of the three least certain moves in this pass.** The attack on it: what a consumer writes at every call site is the most visible taste surface arvo has, and a bar stated about bare primitives at API positions may not reach a question about how a width literal becomes a type-level natural.  | What a consumer writes at every arithmetic call site under the seventh option, which is outside where the ratified ergonomics bar was written to govern. | [the_format](TOPIC.md#the-format) | The container-derivation mechanism itself is established and is not what is open; this is the upstream question of how a written const becomes the type-level natural the derivation operates on. Every route listed was compiled and is a real arm. The sharpest finding of the thread is that the ceiling was never a property of the bridge or of the const surface: it is the price of crossing back, and a table is the only known implementation of either direction. A cross-cutting residue: whether any arrangement needs to represent a negative integer width, unresolved for every arrangement under a design keyed on integer-and-fraction width and dissolved only under the total-and-fraction keying of Q2. | width surface, const generic, type-level nat, bridge, typenum, type alias, diagnostic, digit tower, lazy_type_alias, ergonomics bar, crossing |
| <a id="container-derivation-output-count"></a>container_derivation_output_count | How many outputs does the container derivation have, and what is each one for? | One output, the carrier alone, which occupies more memory than the strategy promises because the map from width and strategy to carrier is not injective., Two outputs, the carrier and the stride, answering a per-value question and a per-aggregate question rather than giving two coordinates of one answer., One richer output that is a type with named projections, which compiles gate-free and repairs the injectivity collapse, and which is the pair wearing one name rather than a rival to it. | panel |  |  |  | [the_realisation_map](TOPIC.md#the-realisation-map) | The register carries this as a dedicated section rather than a numbered entry, and separately as a one-line pointer in its list of questions op has not been asked. `47` is recorded as dissolving the one-against-two fork: the value-valued spelling of a single output is compiled-refused six times across three syntactic positions, each naming a forbidden feature, while the type-valued spelling compiles. Under one reading two is already insufficient, since two strategies at the same width share carrier and stride and differ only in compute. A separate standing disagreement inside the same material, which neither file addresses: whether the strategy is an upstream selector the ladder never sees or a key of the ladder itself. | container derivation, carrier, stride, extent, alignment, access width, injectivity, Cold, packing, outputs, ladder |
| <a id="inclusion-order-singleton-amendment"></a>inclusion_order_singleton_amendment | Is the inclusion order's own predicate amended to identify shapes denoting the same value set? | Amend the predicate so inclusion is decided on denotation rather than on declaration, which removes every measured disagreement by construction and changes what "the same numeral" means wherever the order is used as a two-place relation., Restrict the predicate's necessity claim to sources carrying at least two values and state the singleton case as a documented exception, which is cheaper and leaves a predicate whose necessity half is conditional., Leave it, on the argument that a numeral carrying fewer than two values is not a case any consumer reaches. | panel |  | Not his. It asks whether a relation's defining predicate is stated over denotation or over declaration, which is a question about the order's algebra:  > This bears on the prior answer and isn't mine to call, sine I'm not an expert > or a domain specialist on maths. This is why you and the panel exist.  The evidence is already one-sided and is the panel's to finish rather than his to adjudicate. The instrument that first got it wrong is kept in the row: its shape list held exactly one numeral carrying fewer than two values, so the predicate was never offered the case that breaks it. That is a gap in a sweep, not a fork between intents.  The third option is the one part that is not algebra, since it argues from what a consumer reaches for, and it sits in I11's territory where `ruling::there_is_no_universal_answer_take_the_win_and_gate_it` already says the answer is a region and a gate rather than a ruling. `03` asked for a second read and none has run; that read is what this owes, and it is a panel seat rather than his.  |  | [the_format](TOPIC.md#the-format) | `02` names exactly two things as genuinely undetermined and op's, and this is the second; the first is whether `Precision` counts the sign digit. The third option is listed in the source so the space is not silently two-sided, was not offered by the file that raised the question, and nobody has tested its argument. `03` asked explicitly for a second read and none has run. The instrument that first got it wrong is kept: its shape list held exactly one numeral carrying fewer than two values, at the coarsest declared step in the box, so the predicate was never offered the case that breaks it. | inclusion order, predicate, singleton, denotation, declaration, value set, grid, phase, necessity, sufficiency |
| <a id="what-a-numeral-guarantees-to-a-fold"></a>what_a_numeral_guarantees_to_a_fold | What does the numeral guarantee to a fold, and what does a composition supply? | The numeral carries nothing extra and a fold is the consumer's problem, which is cheapest and costs the algorithm crates the ability to state their preconditions., The numeral names its algebraic structure, so a contract keys on the structure rather than on a numeral, at the cost of a vocabulary of structures the canon must name and keep., The numeral names its accumulator relation, keyed on a capacity, at the cost of a second input to a derivation that currently takes only numerals., Both, the structure naming what a fold may do and the capacity how wide the result is, which one probe compiles with independent refusal for each half., The composition supplies everything and the numeral stays a value type, at the cost of every composition re-deriving the accumulator relation. | panel |  |  |  | [the_chain](TOPIC.md#the-chain) | Added from `35`, which calls it the most valuable single item it found: Q1 through Q10 are without exception about a single value or a single binary operation and none is about arity-n or about laws under reassociation. `60` later undercuts the accumulator-derivation option as an additive-only mechanism, since the multiplicative fold needs linear growth with no closed form against addition's width-plus-log-of-capacity. Why option 3 is const-derivable while its length-keyed sibling is refused is a staging fact: capacity resolves at stage zero, length only at stage one, per `a_folds_compile_time_refusal_is_the_staging_boundary_reporting_its_own_position`. | fold, accumulator, capacity, algebraic structure, monoid, absorbing top, arity, reduction, loop-carried, trip count |
| <a id="reduction-order-or-associativity"></a>reduction_order_or_associativity | Is the reduction order specified, or is associativity required? | Require associativity: a fold may be split only where the operation is associative, so signed saturating folds run in one lane or under the strategy that permits a soundness trade., Specify the reduction shape as a fixed tree over the index range, independent of detected lane and core count, which makes the answer deterministic at any thread count and costs a single-core implementation a tree it does not need., Make the reduction shape part of the strategy, so the performance-first strategy splits freely and the others take the specified shape, which fits the per-strategy framing and costs an axis., Say nothing and let the answer depend on the core count, which under the soundness condition is a sacrifice for every strategy except the one whose purpose is to make it. | panel |  |  | Whether the layer above may split, thread or algebraically rewrite a reduction at all. | [the_chain](TOPIC.md#the-chain) | The mechanism behind the measured sign asymmetry is contested inside the register. `42` established a reachability condition, the register quoted it in a two-bound form, and `55b` refutes the quoted form exhaustively: of 952 divergent signed-clamp triples, 448 are ceiling-only, 504 floor-only and zero involve both bounds. `55b` states its own bound: it targets the form quoted in the register rather than `42` itself, and a second read of `42` against that probe is owed. A drafting note the register carries rather than an option: wrapping's four separately-measured properties are one theorem, that the representation realises the cyclic group of its width. And a standing caution: no entry here should acquire a rewriting or equality-saturation engine, which is a domain and extraction-strategy decision arvo may not make for a consumer. | reduction order, associativity, reassociation, fold, split, tree, saturating, wrapping, clamp, commutativity, determinism, threads |
| <a id="which-axes-a-build-arm-may-move"></a>which_axes_a_build_arm_may_move | Which axes may a build arm move? | Any axis, including overflow policy and intermediate precision, which adopts Rust's debug-and-release semantic split for every numeral and costs a downstream bound holding in one arm and failing in another., Unobservable axes only, so arms resolve headroom, layout, container and lane count freely and every observable axis is fixed across arms, which costs the imitation intent on the one axis where Rust's own behaviour is arm-dependent., Unobservable always and observable for the performance-first strategy only, which may move an observable axis per arm against a provable meaningful gain and which inherits the unset threshold., No classification at all, stating per axis whether it is arm-resolvable, which is cheapest to state and gives a new axis no rule to classify it by. | panel |  |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Added from `40`, which ranks it first among its own findings. The observable and unobservable split is not derivable from an axis's name and is what decides how the axis may be governed. `106` later replaces the definition of observability in play with a conservative closure plus a per-chain licence, on the ground that observability is a property of the chain rather than of the axis and that the definition `40` used carried a condition no file carried. | build arm, observable, unobservable, axis, cfg, headroom, layout, lane count, debug, release, imitation |
| <a id="the-exchange-rate-a-preference-yields-at"></a>the_exchange_rate_a_preference_yields_at | At what exchange rate does a strategy's preference yield? | A stated rate per objective., A lexicographic ordering with no rate at all, which is what every objective except the performance-first one already has., A rate supplied by the consumer., Silence, with the consequence that "meaningful" is decided case by case by whoever writes the arm. | op | Recorded as answered by op on 2026-08-31. No rate, because a rate is a generalisation over a category and the design rejects those by premise. Op, given all four options above: "This is case by case basis and has to be justified in words that get recorded. Even small wins are wins worth pursuing, however small, and however small a set they apply to. This is the \"patchwork\" approach where we don't even try to generalise, we operate on const predicates that choose the most optimal path always, from a million small different impls for different situations." Silence was one of the four and was not chosen, so a conflict still owes a written reason; what changes is that the reason is local to the arm rather than derived from a global rate. A small win is taken however small the region it applies to, and the size of that region is the arm's predicate rather than an argument against the arm. Ratified as `ruling::there_is_no_exchange_rate_because_there_is_no_generalisation`, recorded whole at `panel::202608072330_the-numeral-canon-panel::217_op_the_last_round_and_the_panel_finishes_alone`. |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Added from `40`, which records it as op's own question and as unset in two of his sentences at once: the provable meaningful gains that bound a soundness trade, and whether mimicking is consistently just a worse choice. `40` argues these are one hole rather than two and that naming the rate once answers both. | exchange rate, threshold, meaningful gain, soundness trade, lexicographic, objective, mimicking, imitation |
| <a id="are-the-axes-independently-resolvable"></a>are_the_axes_independently_resolvable | Are the strategy's axes independently resolvable, and in what order? | Independently resolvable: a strategy's assignment is a product of per-axis argmins, which is cheapest by far and is contradicted at a majority of widths on both matched pairs measured., Resolvable in a stated order, earlier coordinates fixed before later ones are measured, which is coherent with the observable split if observable axes come first and costs the ordering the canon must then justify., Jointly resolvable only, resolution ranging over the product, which is most faithful to the measurement and most expensive., Per axis with interactions named as exceptions, which is the pragmatic form and costs a list that grows by discovery with no rule behind it. | panel |  |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Distinct from Q5, which asks whether the axes are independently stateable; this asks whether they can be settled one at a time. Measured on two matched committed families nobody had cited: the set of containers in contention differs between wrapping and saturating at five of six widths, so the axes are independently stateable and not independently resolvable. | axes, resolvable, argmin, ordering, joint resolution, interaction, container, contention |
| <a id="which-sense-composition-carries"></a>which_sense_composition_carries | Which sense does the word "composition" carry, and does the canon need more than one word for it? | Keep one word and pick a sense., Keep both senses with distinct names., Find that they are the same concept at two scales, which nobody has argued. | panel |  | Returned by `ruling::the_panel_finishes_the_canon_without_him`. The option list is measured to be missing a member: the word does three jobs rather than two, configuration and construction being two and the chain, meaning the same carrier with operations in sequence and no new primitive, being neither. So the constraint is that any answer keeping one word has to say what becomes of the third sense, and an answer picking a sense has to say which of the three it picked. Both of op's own uses are preserved as evidence of what he meant on each object rather than as a vote. |  | [the_chain](TOPIC.md#the-chain) | It is op's to settle because both senses carry his word on different objects: a numeral kind bound to a strategy, which is what the panel's founding sentence means, and an aggregate over numerals, which is what he means at `32`. The dispatching agent briefed the file that raised this on the second sense while the founding sentence uses the first, and did not notice. The option list is measured to be missing a member: `173`'s ledger entry L20 finds the word does three jobs, configuration and construction being two and the chain, meaning the same carrier with operations in sequence and no new primitive, being neither. Naming that third sense is carried separately as op's. | composition, composite, aggregate, chain, configuration, construction, binding time, capacity, vocabulary, overloaded |
| <a id="does-the-canon-carry-a-numeric-threshold"></a>does_the_canon_carry_a_numeric_threshold | Does the canon carry a numeric threshold at all, or only the inequality that generates one? | A numeric threshold, named in the canon., Only the inequality, on the reading that an inequality is permanent while a figure is a fact about a machine. | panel |  |  |  | [canon_form](TOPIC.md#canon-form) | Both files that state the second reading mark it as a reading rather than a result. | threshold, inequality, numeric figure, permanence, break-even, canon sentence |
| <a id="is-the-derived-numeral-required-to-be-tightest"></a>is_the_derived_numeral_required_to_be_tightest | Is the derived numeral required to be the tightest honest answer? | State only soundness, that the derived numeral holds every value the operation can produce, which is cheap, always true of every formula in the design and needs no admissions., Derive the tight form and state bestness, that no numeral smaller does, which requires admitting the origin shape and negative integer width., Say nothing about tightness at all. | panel |  |  |  | [the_format](TOPIC.md#the-format) | The sum-of-widths product form is not tight and wastes exactly one bit on a characterised minority of pairs, so a canon sentence claiming tightness would be false as the design stands. The two admissions the second option needs are measured to be disjoint regions of one formula's codomain rather than two independent repairs. This is the same fork the register carries under Q4 as soundness against bestness. | tightness, soundness, bestness, product form, sum of widths, negative integer width, origin, clamp, codomain |
| <a id="does-precision-count-the-sign-digit"></a>does_precision_count_the_sign_digit | Does the precision coordinate count the sign digit? | It counts it, which is what one row of the record's own family table does., It does not, which is what an adjacent row of the same table does. | panel |  | Returned by `ruling::the_panel_finishes_the_canon_without_him`. Nothing decides this mathematically and both readings were computed through to their four consequences, so what is left is a convention, and a convention is chosen for what it makes expressible. Answered inside the arms paradigm: take the reading under which the three sign domains form a structure a const predicate can gate on rather than one leaving a domain incomparable, and repair the two rows of the record's own family table that disagree in the same act, because preserving the disagreement is what made this a question. | What the sign domain moves; whether the three sign domains at equal precision form a chain or leave one incomparable; whether the symmetric domain at precision one denotes exactly the zero set, which bears on whether a zero-width numeral already exists under one reading; and whether two of the three sign domains collapse at an odd radix. | [the_format](TOPIC.md#the-format) | `02` names exactly two things as genuinely undetermined and op's, and this is the first. All four consequences are computed under both readings so nothing in that derivation depends on the answer, and the answer itself is not decided by mathematics. | Precision, sign digit, sign domain, NonNegative, Symmetric, radix, zero-width numeral, chain, incomparable |
| <a id="does-narrowing-compose"></a>does_narrowing_compose | Does the design want narrowing to compose? | Yes, which is a constraint on which rounding modes a numeral may carry that nothing in the record currently states, since two narrowings equal one exactly when the rounding mode's direction switches only at points the coarser numeral's own grid holds., No, in which case the canon owes the sentence that narrowing twice is not narrowing once. | panel |  |  |  | [rounding](TOPIC.md#rounding) | Measured decisively by moving a pivot on and off the coarser grid: zero failures on-grid and seven off-grid across several trials. Round to nearest never satisfies the condition; the directed modes and round-toward-zero always do. | narrowing, composition, rounding mode, directed rounding, round to nearest, toward zero, grid, coarser |
| <a id="is-the-cross-kind-join-closed-or-priced"></a>is_the_cross_kind_join_closed_or_priced | Is the cross-kind join closed, or merely priced? | Close the shape space under intersection, the standard response from abstract interpretation, which costs a third family of segmented numerals sized at roughly sixteen to thirty-four percent more shapes than the two named families contain and which specifically does not reach tapered formats., Leave it unclosed and priced, with no admission from the original three repairing it. | panel |  |  |  | [the_format](TOPIC.md#the-format) | Nobody is proposing the closure as the design; two persona checkpoints mark it as the direction most likely to eat a week and produce nothing, which is persona judgement carrying no authority. The cheaper alternatives are Q8's tie-break and step-set readings. | cross-kind join, closure, Moore closure, intersection, segmented, posit, tapered, abstract interpretation, slope |
| <a id="should-phase-collide-across-two-vocabularies"></a>should_phase_collide_across_two_vocabularies | Should the word "phase" collide across the two vocabularies the panel uses for it? | Keep the collision, with both readings load-bearing in their own files., Keep "phase" for the value-space sense, which has literature behind it and sits inside the membership predicate, and rename the storage-space sense to something like bit offset or alignment residue. | panel |  |  |  |  | No topic in the register fits this: it is a vocabulary call spanning the value space and the storage space. Found by measurement rather than by intuition: the two senses are independent, all four combinations occur, and the prose of the file carrying the storage sense already half-reaches for a compound without naming the collision. `24` offers the second option as a suggestion rather than a call. | phase, bit offset, alignment residue, vocabulary, collision, grid, packed run, membership predicate |
| <a id="where-wrapping-lives"></a>where_wrapping_lives | Where does wrapping live: is it one reduction slot whose members are classified by law role, or an axis value with an exception list? | One slot, members classified along the two law roles, so every generic sentence over the slot quantifies over the family that carries it, no member is an exception to anything, and the induced-algebra grade is what the law layer states per policy., Wrap stays an axis value with a permanent exception list, whose cost runs both ways because chain-exactness sentences carry a saturation exception in the mirror direction. | panel |  |  |  | [overflow_policy](TOPIC.md#overflow-policy) | The register's earlier three options are now two. Two of them, wrap as a change of ambient domain and wrap as a named composite, were folded on an observational-equivalence argument: no probe, bench or consumer program distinguishes them, so the choice is drafting economy and must not be dispatched as an empirical question. The conversion-out cost the first version of the entry carried is withdrawn on the same argument. Owed under every filing equally, and therefore not a distinguisher: a wrapped numeral has no arithmetic-compatible order, so the canon owes one sentence saying which order comparison means there. | wrapping, wrap, saturation, reduction, retraction, coherence, adaptation laws, induced algebra, semiring, ring, magma, grade |
| <a id="adaptation-in-identity-or-realisation"></a>adaptation_in_identity_or_realisation | Does a number system's selected adaptation belong to its identity or to its realisation? | To its identity, folding the adaptation into what the system is., To its realisation, keeping the system's identity free of it., Neither: the format unit's two-layer split, format and then the strategy-selected induced algebra, dissolves the question rather than answering it. | panel | Realisation. Settled already by `ruling::the_format_spine_is_canon`, which is ratified and ratifies `proposal::a_format_is_identified_by_its_ambient_domain_and_its_representable_set`, whose own words are that adaptation choice and encoding are realisation, observable in computed values and in pattern-level properties respectively, and not part of identity. Nothing was decided here to close this; the edge was already in the registry and nothing followed it, because it runs `ruling.ratifies` into `proposal.answers` and every reader of the settled set read `ruling.answers` alone. |  |  | [the_number_system](TOPIC.md#the-number-system) | Three positions rather than two, and whether the disagreement is substantive or nominal is itself disputed. Two of the three files note that arvo's types carry the strategy either way, so type identity agrees under all three and the residue may be naming. What would distinguish them: whether two strategies over one format are one system with two realisations or two systems, and whether anything downstream reads the difference. | adaptation, identity, realisation, number system, strategy, format, induced algebra, two-layer |
| <a id="are-the-level-hierarchies-the-same-cut"></a>are_the_level_hierarchies_the_same_cut | Are the two proposed level hierarchies the same cut at different granularity, or different cuts? | Three levels: system, representation, format, with a change-test attached., Five levels: number, system, representation scheme, format, container. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | What would distinguish them: apply each one's own change-test to the other's levels and see which collapse. If they collapse cleanly, one cut is a refinement of the other and the question is granularity; if they do not, they disagree about what a level is. | levels, hierarchy, cut, refinement, granularity, change test, representation scheme, container |
| <a id="is-the-number-system-inventory-open"></a>is_the_number_system_inventory_open | Is the inventory of number systems open or closed? | Closed, which is checkable and lets a canon enumerate what it covers., Open, anticipating systems nobody has named, at the cost of the canon being unable to say what it covers. | panel | Open. Settled already by `ruling::the_format_spine_is_canon`, which is ratified and ratifies `proposal::the_concept_is_closed_and_the_inventory_is_open`: the canon defines once what a number system is and what admission requires, the set of admitted instances is open, and a new one earns admission by supplying the concept's obligations rather than by amending the canon. The concept being closed is the other half of the same sentence and is what keeps an open inventory from meaning anything goes. Reached through the same two-hop edge as its siblings, unfollowed until now. |  |  | [the_number_system](TOPIC.md#the-number-system) | Both cold derivations of the unit reach this and neither closes it. What would distinguish them: whether the concept has a membership test that does not enumerate. | inventory, open, closed, enumeration, membership test, number system, coverage |
| <a id="is-number-system-broad-enough-for-non-magnitude"></a>is_number_system_broad_enough_for_non_magnitude | Is "number system" broad enough to include things that are not about magnitude? | Broad, naming the two-element Boolean algebra and the vector space over the two-element field as number systems not about magnitude at all., Narrow, excluding them. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | One cold derivation leaning broad is one instance, and one instance decides nothing. The entry's stated discriminator is measured not to cut: every total order on the carrier was enumerated at widths two and three, and wrapping addition and exclusive-or over the vector space admit zero compatible total orders each for the same structural reason, while saturating addition and min admit some, so an order-or-magnitude reading groups a kernel item with the candidate the narrow reading exists to exclude. What remains is whether the ambient operation family is fixed or is a parameter, which is Q33, and the two entries should be read as one. | magnitude, Boolean algebra, GF(2), vector space, total order, compatible order, kernel, broad, narrow |
| <a id="are-set-valued-carriers-admitted"></a>are_set_valued_carriers_admitted | Are set-valued carriers admitted into the number-system concept, or scoped out? | Admitted, which generalises the concept cleanly and makes the carrier a set of sets., Scoped out, which keeps the concept smaller. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | What would distinguish them: whether certified accuracy should be expressible inside the system concept or beside it. Intervals and any future error-tracking pair are the instances. | set-valued, carrier, interval, error tracking, certified accuracy, admission |
| <a id="is-the-role-set-closed"></a>is_the_role_set_closed | Is the role set closed? | Closed, at storage, compute and interchange, which is checkable., Open, anticipating roles nobody has named, with chain-extent named as a possible fourth. | panel |  |  | Op's plural in "numeral representations", which the role set is what explains. | [the_number_system](TOPIC.md#the-number-system) | The file that raised it leans closed-with-amendment and holds it open. | role, storage, compute, interchange, chain extent, closed, open, representations |
| <a id="does-the-canon-speak-about-cost"></a>does_the_canon_speak_about_cost | Does the canon speak about cost at all? | Yes, as an axis in the canon with the numbers on the harness., No, the canon staying silent on cost entirely. | panel |  |  |  | [canon_form](TOPIC.md#canon-form) | The file that raised it proposes the first and calls the second defensible and smaller. | cost, canon, harness, axis, silence, numbers |
| <a id="how-the-law-inventory-is-named"></a>how_the_law_inventory_is_named | How is the law inventory named? | Marker contracts per law., A structured declaration the derivation reads., Both. | panel |  |  |  | [algebraic_laws](TOPIC.md#algebraic-laws) | Expressibility of the simplest form is established by a probe. What is open is which form the canon should describe without spelling an implementation. A one-expert, provisional candidate for the first option's shape: marker contracts per named sub-fact about the operation and the representable set, composed by conjunction into the predicate a specific law needs, never one flag per law and never a bundled soundness marker. Offered at the same confidence its own source, C6, carries: provisional, attacked once, held. Not yet a second derivation. | law inventory, marker contract, declaration, derivation, naming, expressibility |
| <a id="what-a-platform-width-type-is"></a>what_a_platform_width_type_is | What kind of thing is a platform-width type? | Storage rather than format, on the reading that a value set depending on other data has no representable set and is not a format., A degenerate instance of the shape family., An orthogonal axis., A different kind of thing the format concept need not account for. | panel |  |  |  | [the_format](TOPIC.md#the-format) | Weight it accordingly: "storage" is named once, in passing, in a file about a different topic. The register lists the fourth option as a third option beyond the carried fork; it is written here alongside the two the fork carried so the space is visible. | platform width, USize, Cap, storage, format, value set, degenerate, orthogonal axis |
| <a id="is-interoperation-conversion-or-resolution"></a>is_interoperation_conversion_or_resolution | Is interoperation conversion, resolution, or neither? | Conversion., Resolution., Neither. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | The file that raised it finds nothing in the unit's other files addressing it directly. The nearest material is Q3, which is open, explicitly op's, and load-bearing for the format unit's strongest unconditional result. Which strategy's laws govern a cross-strategy operation is adjacent to that and not the same question. The three options are the register's own three words and carry no elaboration in the source; each is named and none is described, which is as symmetric as the source allows. | interoperation, conversion, resolution, cross-strategy, crossing, mixed numeral |
| <a id="arbitrary-width-demands-in-the-canon"></a>arbitrary_width_demands_in_the_canon | Should the canon speak of arbitrary const-width demands at all? | Yes, the canon speaking of arbitrary const-width demands., No, leaving width families to design. | panel |  |  |  | [canon_form](TOPIC.md#canon-form) | The file that raised it deliberately did not probe the general width-to-container projection and rests its pipeline claim on width-as-marker. The question interacts with the forbidden-features boundary and needs its own evidence. | arbitrary width, const width, width family, forbidden features, width to container, marker, design tier |
| <a id="what-the-admission-contract-asks-a-candidate-to-expose"></a>what_the_admission_contract_asks_a_candidate_to_expose | What does the admission contract ask a candidate number system to expose? | The standing list prefixed with the reduction's two law verdicts, whose sufficient direction is refuted twice over because every system has a second declaration of itself naming its own computed algebra as its ambient domain, satisfying the list verbatim while computing the identical function., The same, plus the ambient domain's own law inventory, plus a third verdict for the retraction, which costs one more item and one more verdict and buys a collapse that is harmless rather than forbidden., Admission relative to a consumer-supplied ambient domain, the candidate exposing only its representable set and its reduction, which makes the collapse unstateable rather than caught and costs a candidate not determining its own identity until a frame is chosen. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | The necessary direction of the first option is untouched and survives. What would distinguish the second from the third: whether any canon sentence must be true of a system with no ambient domain named. The ingest door was proposed as that case and is measured not to be one. | admission contract, ambient domain, law verdict, retraction, reduction, collapse, representable set, ingest |
| <a id="is-admission-a-predicate-or-a-location"></a>is_admission_a_predicate_or_a_location | Is admission a predicate or a location? | A predicate returning member or not, which discards the coordinate a consumer needs and makes every mislocated member read as a non-member., A location returning which coordinate the candidate fixes and whose it is, which costs an output type rather than a truth value and presumes a coordinate count the canon may not want to commit to., A location for membership and a predicate for hosting, which is the shape Q31's split falls into and costs two procedures where a reader expects one. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | What would distinguish them: whether any canon sentence needs to quantify over non-members. Most of the disputed cases, a Gray code, two's complement and a stride, are coordinate choices rather than rejections. | admission, predicate, location, coordinate, ownership, membership, hosting, Gray code, two's complement |
| <a id="one-word-or-two-for-is-a-number-system"></a>one_word_or_two_for_is_a_number_system | Does the canon use one word or two for "is a number system" and "can arvo carry it"? | One word, folding the residue clauses into the concept, which makes the concept exclude unbounded exact rationals as a matter of mathematics, which is false, and makes every hosting clause conditional on unratified constraints inside a sentence about arithmetic., Two words, at the cost of two admission passages and a reader told which is which at every use., Two words with the second scoped to a target, composing with target-indexed families, at the same cost plus a quantifier over compilations. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | The file that raised it argues the two questions have different answers, different decidability and an inhabited two-by-two. What would distinguish them: whether the canon ever says something true of a system arvo cannot host, and it already does, since the bounded windows it admits are defined as bounded windows of systems it cannot host. | number system, hosting, admission, vocabulary, one word, two words, target, no_std, dyn, bounded window |
| <a id="is-the-ambient-operation-family-fixed"></a>is_the_ambient_operation_family_fixed | Is the ambient domain's operation family fixed at addition and multiplication, or is it a parameter? | Fixed: the concept describes one arithmetic, the law layer's entire evidence base is correctly scoped as it stands, and the tropical semiring the algorithm crates compute in is described by the algorithm crates. Cost: the named selling point computes in something the canon does not cover, and the one law those algorithms need is quantified over a pair of operations from two families, which no instrument in the panel measures., A parameter: one representable set carries several ambient domains, the Boolean and vector-space structures and the tropical semiring become ordinary members, and the index-one crossing becomes something the concept can talk about. Cost: every law sentence acquires a scope it did not need, prefix equality becomes a relation somebody must define, and the reduction space varies along an unenumerated axis. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | Labelled O-A in the consolidation it was carried from, and mapped to Q33 by the register. To be read as one with Q21, whose own discriminator was measured not to cut. Distinguisher: whether any consumer needs one generic algorithm spanning both families, or naming two families separately costs nothing because no code spans them. | ambient domain, operation family, tropical semiring, min-plus, distributivity, parameter, algorithm crates, Boolean, GF(2) |
| <a id="two-shapes-of-aggregate-composition"></a>two_shapes_of_aggregate_composition | Does the aggregate sense of composition contain one shape or two? | One shape, one aggregate concept covering every case., Two shapes, point compositions where a datum is a tuple of numerals travelling together and owes its own laws at the composition layer, against shared-parameter aggregates where many data share one parameter participating in each datum. | panel |  |  |  | [the_chain](TOPIC.md#the-chain) | Labelled O-B in the consolidation it was carried from. The source states the two-shape reading and describes it; the one-shape alternative is added here so the entry is not one-sided, and the source does not write it out. Distinguisher of treatment, which the source does state: whether the shared parameter participates in the arithmetic. If not, the aggregate is a layout fact and the element remains a format; if so, the element has no representable set of its own and the concept currently has no home for it. Arvo ships one shared-parameter case at the realisation layer, the packed stride, and excludes one at the identity layer, a block exponent. | aggregate, composition, point composition, shared parameter, block exponent, stride, interval, rational pair, layout |
| <a id="the-ownership-key-as-a-structural-axis"></a>the_ownership_key_as_a_structural_axis | Does the concept adopt an ownership key as a structural axis? | Adopt the key: every chain component carries an owner and a resolution time, subsuming the per-value and per-aggregate bottom tier, the two-layer shared parameters and the platform-width case. Cost: every per-value canon sentence acquires an implicit whose-components rider, and aggregate-owned cases need a per-aggregate quantifier, which is new surface., Do not: treat aggregate cases as compositions with no key on the base concept, keeping the base smaller and forcing every shared-parameter case into the composition layer, including the platform-width case, where that filing has no obvious content. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | Labelled O-C in the consolidation it was carried from. Distinguisher: whether any consumer writes one contract generic over ownership, or naming the cases separately costs nothing because no code spans them. | ownership, ownership key, resolution time, monomorphisation, aggregate, platform width, structural axis |
| <a id="whose-reduction-governs-a-lossy-crossing"></a>whose_reduction_governs_a_lossy_crossing | Whose reduction governs a lossy crossing? | The source's: a value finishes being a value of its system before it becomes one of another. Cost: the target's declared policy does not govern values entering it, which reads badly at an ingest boundary., The target's: the crossing is the target's business. Cost: a value can be adapted by a policy its own system never selected, and this is the route on which a wide wrapping value saturates., A third named at the crossing site: the most expressive, the choice visible where made. Cost: one more thing at every crossing site and no default for a consumer who writes nothing. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | Labelled O-D in the consolidation it was carried from, and scoped there: the question exists only at lossy crossings. Distinguisher: whether any consumer writes a crossing whose correct answer differs from both endpoints' own selections. The observable difference between the first two is 226 of 256 source values in the measured cell. | crossing, lossy, reduction, source, target, ingest, adaptation, saturation, policy |
| <a id="does-the-canon-name-crossing-classes"></a>does_the_canon_name_crossing_classes | Does the canon name crossing classes at all? | Name all five classes, so a crossing states which coordinate it moves and the law obligation attaches to the class. Cost: five names plus a rule that a composite names its intermediate., Name none, one crossing relation with a declared preservation set. Cost: the classes still exist and get respelled in every sentence needing them., Name two, values and patterns. Cheapest, and cannot state the order problem, which is about coordinates. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | Labelled O-E in the consolidation it was carried from, where two entries with one subject were consolidated with both authors' options intact. That entry carries a second question with its own option set, ported separately here as the ordering half; splitting it is this port's call and the source keeps them under one label. Distinguisher for this half: whether any canon sentence quantifies over one class and not another, and at least two do. | crossing class, naming, preservation set, coordinate, values, patterns, composite, intermediate |
| <a id="when-is-an-order-owed-at-a-crossing"></a>when_is_an_order_owed_at_a_crossing | When is an order over a multi-coordinate crossing owed? | Always, for every multi-coordinate crossing. Cost: ceremony with no content at lossless crossings., Only where lossy, stated conservatively on terms by asking whether the target's set contains the source's., Never, one global order fixed by the canon. Cost: wrong for one of the two readings at every lossy site. | panel |  |  |  | [the_number_system](TOPIC.md#the-number-system) | This is the second half of the register's Q37, which consolidates two entries with one subject under one label. Splitting it into its own row is this port's call, made because the two halves have disjoint option sets and disjoint distinguishers. Distinguisher for this half: whether any consumer writes a lossy crossing without an explicit narrowing already in view. | order, crossing, lossy, lossless, narrowing, coordinate, global order, ceremony |
| <a id="where-a-law-verdict-is-established"></a>where_a_law_verdict_is_established | Where is a law verdict's truth established? | In the compiler, per instantiation, exhaustively. Bounded by a const-eval frontier that is a curve in width and arity, and at a shipped width it produces only negative verdicts, because a false law exits early and a true one must exhaust. Buys: no trusted-base item at all., Offline at a model width, cited in the compiler, the typestate carrying a declaration of the verdict. Cost: the paper-checking-paper hole in full, plus the model-width transfer proviso. Buys: any width, any arity, constant compile cost., Offline as a closed form, cross-checked in the compiler against the sweep over a stated model band. Cost: the model band is a compile budget spent per law and is nearly exhausted at four widths and one law on one host, with the transfer proviso surviving as a single named residue. Buys: the declaration is checked against the maps everywhere it can be. | panel |  |  |  | [algebraic_laws](TOPIC.md#algebraic-laws) | What would distinguish them: whether the canon is willing to carry a named trusted-base item for law verdicts at all. The first needs none and reaches almost nothing; the second and third differ only in whether the item is auditable. That distinguisher is itself a canon-form question and is coupled to the coordinator's item 2, which asks what kind of sentence the canon may carry; this row is not answerable while that one is open. And the entry asks one question over the whole category of law verdicts, which is the shape op has rejected three times in this panel; it survives here because what it turns on is a single canon-level willingness rather than the shape any individual law takes, which is the thing he said the canon must not police. | law verdict, const eval, frontier, model width, transfer, closed form, trusted base, compile budget, declaration |
| <a id="may-an-arms-predicate-read-data"></a>may_an_arms_predicate_read_data | May an arm's predicate read data, or only the typestate? | Typestate only, which is the predicate intent read literally, since it says const predicates. Cost: every trajectory predicate the panel has measured is a characterisation rather than an arm until somebody lifts its value conditions into declarations, and nobody has tried to construct such a lifting., Typestate or data, with a value-gated arm permitted where it pays. Cost: measured, a value-gated selection materialises both arms and picks with a conditional select, thirteen instructions against six and three for the two const-gated arms, so it is worse than the unlicensed form rather than worse than the better one; and the erase clause acquires an exception., Typestate only for selection, data permitted at a declared ingest boundary, so a trajectory condition is checked once where values enter and is a typestate fact afterwards. Cost: a door, plus the measured per-datum residue for the runtime reading. | panel |  |  |  | [the_predicate_notation](TOPIC.md#the-predicate-notation) | The panel is producing two kinds of law region and calling both predicates: typestate predicates known at monomorphisation, and trajectory predicates known only at runtime. What would distinguish them: whether any trajectory predicate this panel has measured has a lifting into a declaration a consumer would actually write. Op has already been put this fork and declined to pick a side, on 2026-08-13, saying the axis is const-availability and that the admissible category is whatever is available at const time, which reaches const functions and const data from outside the typestate and leaves the typestate usable inside a const expression rather than being the only source. His own later file counting the rejections of this shape names this one as the third of three. The entry was added by a file numbered before that answer and has never been amended, so option (a)'s description of itself as the predicate intent read literally is stale: the intent was sharpened past the literal reading. What the answer explicitly does not settle, and what keeps a question here at all, is what happens to a condition that is genuinely not const-available, which is the trajectory case the entry is about. | predicate, arm, typestate, trajectory, runtime, const, monomorphisation, ingest boundary, csel, erasure |
| <a id="which-route-a-law-verdict-takes-to-closed-form"></a>which_route_a_law_verdict_takes_to_closed_form | Which route does a law verdict take to its closed form? | A proof lifting a lower-arity verdict, which is the route grouping-type chain laws take. Cost: the proof is audit-trail work and the canon must name the theorem, because a wrong lifting is invisible at every instantiation., A structural argument about the representation, which is the route wrapping's verdict takes, being a fact about the encoding realising a group rather than about a lower arity., No route, and the verdict stays swept, which is where schedule-conditional chain facts sit, vacuously clean at arity two and a fresh statement at every higher arity. | panel |  |  |  | [algebraic_laws](TOPIC.md#algebraic-laws) | What would distinguish them for a given law: whether the law is a consequence of a lower-arity law, which is decidable by inspection rather than by measurement, and no file in this panel has asked it of any law it measured. | closed form, arity, lifting, generalized associative law, structural argument, cyclic group, schedule, sweep |
| <a id="are-strategies-partially-ordered-by-chain-laws"></a>are_strategies_partially_ordered_by_chain_laws | Are the strategies partially ordered by how many chain-level laws they honour? | The order is real and is a refinement order, the honoured sets nesting, so a strategy's position in it is a derived fact rather than a stated one., The order is real and is not a refinement order, because the honoured sets overlap without nesting, so there is a structure and it is not a ladder., There is no order: the honoured sets are unrelated and each axis assignment's law set is its own thing. | panel |  |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Offered as an explicit falsifiable candidate by its author, who marks it as its own synthesis, states plainly that it is not stated anywhere in the premises, and offers it as a candidate to test rather than as a result. It is carried at exactly that weight. It was motivated partly from a clause op has since called filler noise he mused on the spot, and it does not rest on that clause. Recorded as engaged and not closed: one file's measurement found signedness deciding which of the three options holds, and a later file's attack on the conclusion drawn around that lands, naming three orders that disagree. | partial order, refinement, chain law, honoured set, nesting, Precise, Hot, signedness, conservative, join-semilattice |
| <a id="which-reassociated-arm-a-law-licenses"></a>which_reassociated_arm_a_law_licenses | Which reassociated arm does a law license, keyed on reduction length? | The fold as written, which is latency-bound in its reduction chain and whose nanoseconds per element climb with length while its instruction count stays constant., A sixteen-lane vectorised arm, at parity below its lane count and first paying at length thirty-two., A sixty-four-lane vectorised arm, at parity below its lane count and first paying at length sixty-four., A hand-written NEON arm, first paying at length sixteen., A four-way index-split arm emitting no vector instruction at all, faster at all twelve lengths measured, from 1.08x up to 14.51x at length sixty-four., Eight hand-written accumulators, which reach parity with the wrapping form and beat it at length 256. | measurement |  |  | Whether an instruction count may be used as a cost model anywhere in this corpus, which this entry measures it cannot be. | [algebraic_laws](TOPIC.md#algebraic-laws) | The only priced entry in its unit and the only bench. Recorded as answered in shape rather than in choice: what would distinguish the arms for a given consumer is the reduction length distribution, which is a consumer fact and not arvo's, so arvo ships the arms and the predicate rather than picking. The entry's own predicate names no dimension for strategy, so under the ratified notation none of it may be read as a statement about any named strategy. Not measured: signed, fractional, any other width, multiplication, more than one thread. The harness's between-dylib noise floor on that host measured 4.9 percent, which bounds every small claim in it. | reassociation, arm, lane count, crossover, NEON, vectorisation, accumulator, latency, instruction count, noise floor, predicate |
| <a id="what-goes-into-the-compiler-winner-or-cost-table"></a>what_goes_into_the_compiler_winner_or_cost_table | Which table goes into the compiler: the winner table, or the cost table? | The winner table, the arm chosen per region baked in., The cost table, which compiles to an alias at the point of use and occupies zero bytes once const-consumed, and which buys a consumer-supplied weighting that nobody had tabulated., The composition: ship the weighting, the cost table and the generated winner table, and assert at const time that the third is the argmin of the first over the second, so a mutant refuses at compile time. | panel |  |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | The register's own entry records that it was written as a binary, that the binary was the dispatcher's error, and that a canon gate corrected it: the original fork of checking a table against a weighting against generating the table from one has no consumer-visible content, because both emit the same artifact and both are maintainer workflows. This row carries the axis that does change what a consumer can do, which one member named and nobody picked up. Also recorded: generating relocates the check rather than removing it, since rationalisability catches none of five injected generator defect classes while cone membership of the stated weighting catches all of them; and the measurement that originally motivated generating is largely instrument, one family having been measured against its byte-identical noise-floor control, and a later test gate found that byte-identity is not uniformly true and has to be checked per family. | winner table, cost table, weighting, rationalisable, argmin, const assert, generator defect, cone membership, noise floor, control |
| <a id="must-a-weighting-be-strictly-positive"></a>must_a_weighting_be_strictly_positive | Must a canon-level weighting be strictly positive, or may it be non-negative? | Require strict positivity, under which the no-dominated-arm guarantee holds, at the cost of the ability to express a strategy that genuinely does not care about a coordinate., Allow non-negativity, under which a strategy may zero out a coordinate and the guarantee is lost rather than weakened, since 63 of 72 rationalisable sections select an arm no weighting can select., Allow non-negativity and carry the dominated-arm check separately rather than getting it free from the weighting's shape., Require a unique argmin, which buys the same no-dominated-arm guarantee as strict positivity without forbidding a zero weight. | panel |  |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | The fourth option is a later file's and is covered by none of the first three. The gap the entry is about is smaller than it reads: the entire 72-against-9 difference is one tie, between two arms that both declare thirteen bits, so a rule about ties settles it where a rule about the sign of every weight settles it by forbidding something nobody wanted to forbid. Three independent implementations on the same committed data reproduce the counts 72 and 9. | weighting, strictly positive, non-negative, Pareto, dominated arm, unique argmin, tie, rationalisable, coordinate |
| <a id="arms-no-weighting-can-select"></a>arms_no_weighting_can_select | What is to be done about arms no weighting can ever select? | They are a defect in the arm set and should be dropped, since a bench arm nothing can select is measurement spend with no decision attached., They are a defect in the coordinate set, and a coordinate the corpus does not carry would un-dominate them., They are correct to keep as a documented negative control, on the ground that an arm known to be dominated everywhere is what makes "dominated" checkable. | panel |  |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | The second option was tested directly: a third cost coordinate was added, reported as rescuing an arm, then tested against zero, and all four comparisons the rescue depended on had bootstrap confidence intervals crossing zero, so the rescue was withdrawn. It is measured and currently unsupported, though the coordinate does separate 58 of 90 arm pairs measurably. The entry exists in one file and is a fact about the shipped bench corpus rather than about any proposal. | Pareto dominated, arm, coordinate, negative control, bootstrap, confidence interval, bench corpus, rescue |
| <a id="the-cross-repo-strategy-name-collision"></a>the_cross_repo_strategy_name_collision | How is the cross-repo `Strategy` name collision resolved? | arvo renames, on the ground that the consumer-facing concept is arvo's and the canon is being written now, so it is the cheap moment., notko renames, on the ground that notko's is the profile-tier concept and op has ruled the two are not one mechanism but share synergy and nothing more, which is an argument that the shared name is actively misleading in both directions., Neither renames and the canon states the disambiguation, which is cheapest in edits and leaves every future reader to rediscover the collision. | op |  |  |  |  | No topic in the register fits: it is a cross-repo naming decision rather than a claim about any of arvo's subjects. Recorded as answered by op on 2026-08-14, with the record at `105` and the register's closure section carrying it; the answer is not written here. Two consequences the closure section states beside the answer: naming the replacement is the other repo's business and not this panel's, and this is not a licence for the panel to edit that repo. The underlying fact: two repositories one dependency edge apart both export a type called `Strategy`, meaning different things and sharing three marker names, and arvo depends on the other, so both are reachable in one file. | Strategy, name collision, notko, arvo, rename, disambiguation, marker, cross-repo, profile tier |
| <a id="which-reading-of-i3-at-a-non-native-width"></a>which_reading_of_i3_at_a_non_native_width | Which reading of the native-imitation intent is meant at a width Rust has no primitive for? | The declared width., The container it is stored in., Neither, on the ground that the intent is about ergonomics rather than about where arithmetic boundaries land. | op |  |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Recorded as closed by op on 2026-08-14, with the full record at `104`; the answer is not written here. The third option is op's own and was not among the two the panel built, so the fork as the panel posed it was never a fork about that intent. The measurement that the two readings disagree at all fourteen non-native widths swept stands and is about something else. The register numbers this Q47 together with a second, separable question about the same intent, ported here as its own row; the split is this port's call. | I3, native primitive, imitation, declared width, container, ergonomics, non-native width |
| <a id="does-the-native-imitation-cover-the-debug-panic"></a>does_the_native_imitation_cover_the_debug_panic | Does the native-imitation intent cover the native debug-overflow panic, given the intent that there are never any runtime checks? | It covers the panic, so the imitation extends to a runtime check and the no-runtime-checks intent bends for it., It does not, so the imitation is about representable-value behaviour only and the no-runtime-checks intent is unqualified. | op |  |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | This is the second half of the register's Q47 and is separable from the first: the first asks which of two readings a sentence has, the second asks whether one behaviour is inside the sentence at all. Splitting them into two rows is this port's call. Recorded as answered on 2026-08-14 and entered in the intent catalogue as a new intent, bounded and held as a rule of thumb rather than a gate; the answer is not written here. The tension is permanent on that one behaviour: Rust's native primitives signal overflow with a debug-mode panic, which is a runtime check. | I3, I15, I18, runtime check, debug assertions, overflow panic, imitation, rule of thumb |
| <a id="what-the-cost-coordinates-are"></a>what_the_cost_coordinates_are | What are the cost coordinates, and what is to be done about what the corpus cannot express? | Add the missing coordinates to the corpus, which is work in the harness and in ninety-four variant crates, and which one later file narrows to one missing hook rather than a design job, since the per-arm quality mechanism already exists in one crate and what is missing is the output-scoring hook that no variant crate implements., Treat what cannot be expressed as a region dimension instead, as one file proposes for chain length and as the corpus already does for thread count., Accept the ceiling and state in the canon which intents the coordinate set can and cannot distinguish, so the limit is declared rather than discovered. | panel |  |  | How many strategies can exist at all, since a coordinate set is a countable ceiling on that: one, then nine, then forty-two as coordinates are added. | [the_strategy_axis](TOPIC.md#the-strategy-axis) | The options are stated by the source as not exclusive, each right somewhere, and composing. The corpus measures one coordinate: across seventeen columns, 254 files and 104080 rows, nine carry any information and three vary between arms. An intent with no coordinate is not unmeasured but inexpressible. Accuracy specifically cannot be a per-arm scalar, because the per-operation and chain rankings cross. | cost coordinate, score_output, corpus, CSV, region dimension, chain length, threads, accuracy, ceiling, expressible |
| <a id="is-i9-about-a-strategy-or-one-component"></a>is_i9_about_a_strategy_or_one_component | Does the intent that strategies change what the correct answer is describe a strategy as a whole, or only one component of it? | The pair: both the observable policy assignment and the weighting over cost coordinates., The policy half only, the weighting half changing nothing observable. | panel |  |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Recorded as put to op on 2026-08-14 and declined by him as not his, and returned to the panel with a decision procedure attached; the wording of the decline is at `104` and is not restated here. It stays live as an implementation question, one file already states both readings and says it can build either, and it is not waiting on op and must not be put to him again. The register also records the dispatcher's lesson as a checkable test: if both answers leave the intent intact and differ only in what the panel calls things, it is not his. | I9, strategy, pair, policy assignment, weighting, observable, decomposition, implementation detail |
| <a id="what-transfers-from-a-model-width"></a>what_transfers_from_a_model_width | Does anything but the finiteness theorem transfer from a model width to a real one? | Yes, per width-dependent claim, on one of three honest transfer arguments: a symmetry making the property invariant under the width, a saturation point past which nothing changes, or an induction on the width, with the canon naming which one per claim., No, which the source names as the default answer, in which case a claim swept at width three is a claim about width three and its predicate should say so. | panel |  |  |  | [the_predicate_notation](TOPIC.md#the-predicate-notation) | The topic's structural result quantifies over finiteness rather than over size, so it needs no transfer argument and carries none; every other claim in the topic was established by sweeping small widths. Closing the specialization and type-identity doors is stated to be necessary for model-width transfer and not sufficient, because uniform monomorphisation is a claim about implementations and transfer is a claim about properties. What would not close it: another sweep at another small width, since two model widths agreeing is two model-width results and not a transfer. | transfer, model width, finiteness, symmetry, saturation, induction, monomorphisation, specialization, TypeId, predicate |
| <a id="are-consumer-terms-trees-or-dags"></a>are_consumer_terms_trees_or_dags | Are a consumer's terms trees or directed acyclic graphs? | Trees, which is what every condition set in the topic assumes, since each reads leaf identity and the whole leading condition family is about a leaf occurring at most once., Directed acyclic graphs, under which a shared subterm is one leaf or several depending on how the front end hands it over, and the conditions bind differently without any of them changing. | panel |  |  |  | [the_realisation_map](TOPIC.md#the-realisation-map) | What would close it: a statement of what the consumer-facing term type is, which is a design question at the tier above this topic rather than a measurement. Failing that, a measurement of whether the two readings ever disagree on the conditions, which would bound how much it matters. | tree, DAG, term, leaf identity, shared subterm, condition, front end, sharing |
| <a id="what-the-double-rounding-mechanism-is"></a>what_the_double_rounding_mechanism_is | Which reading of "the grid" does the double-rounding clause carry once grids chain? | The first reading, which differs from the second on 32 to 94 cells in one comparison., The second reading, which differs from the first on 124 to 170 cells in the other. | measurement |  |  |  | [rounding](TOPIC.md#rounding) | The clause first suspected is measured not to be at risk: both its arms round at every node and measure zero differences for every mode. What is at risk is staged-versus-direct narrowing, which no clause states. Neither reading reproduces its own arm. What would close it: a construction that reproduces one arm under one reading. Three were tried and all three were wrong, and they are committed rather than repaired away, so a fourth attempt starts from the three dead routes rather than from nothing. | double rounding, grid, staged, direct, narrowing, chained grids, dead routes, reading |
| <a id="which-operation-set-the-design-ships"></a>which_operation_set_the_design_ships | Which operation set does the design ship? | Name the operation set now, under which the shape-to-count table becomes writable., Declare it open-ended, under which the table is not writable by construction and the canon says so rather than leaving a gap that reads as unfinished work., Name a core set and declare the rest open. | panel |  | Put to op and returned, on the ground that it is not a question he is placed to answer:  > This bears on the prior answer and isn't mine to call, sine I'm not an expert > or a domain specialist on maths. This is why you and the panel exist.  **It was recorded as blocked on him and it never was.** It is downstream of `the_container_premise`, which is itself now the panel's under a stated bound, so the chain that ends here is workable from end to end without him. Whether the resulting set is right is his to ratify; deriving it is not his to do.  | The preset count and their names, which is op's own open intent. The topic's result is that it cannot be closed from below, because it is downstream of this, so this one question is the whole of that one. It also decides whether footprint is observable, and with it one clause of the primitive candidate, the contested one-vocabulary-or-two fork, and whether the count of primitives is container-relative. | [the_strategy_object](TOPIC.md#the-strategy-object) | No measurement can supply this: the topic established that the question is a decision, and that is itself the finding. Where it bites: at unsigned, a design shipping four of the five operations would report the intermediate axis dead and be wrong. The second option is stated in the register rather than in either candidate; the third is the coordinator's addition at `156`, added so the question does not read as a binary, and is marked there as its own. Coupled to the unpredicated-proposition question, which one signature records as the same decision. | operation set, observation set, visibility, saturates, shape to count, preset count, footprint, container premise, decision |
| <a id="is-the-firewall-carried-unpredicated"></a>is_the_firewall_carried_unpredicated | Is the firewall proposition's unpredicated form what the canon carries? | Yes: it ships with no predicate, because a predicate records where a claim was established and this one is imposed, so applying the notation would say the design may violate it everywhere unmeasured., No: the notation applies to it as to everything else. | panel |  | Not his any more, because the thing it was waiting on has been answered. The row names its own precondition: what would close it is op accepting an imposed proposition in the canon alongside established ones, and the canon stating which kind each of its sentences is.  He answered the general question and did not answer it in that shape. Filed as `ruling::a_thing_that_constrains_the_work_and_cannot_be_designed_away_is_canon`:  > This is something you should decide based on the workflow and soundness and > validity of the canon and work. If it's a real thing that constrains the work and > is needed to know, it's canon  So a sentence's form does not decide whether it is canon, and the predicated or unpredicated shape of the firewall proposition is not what settles it. What settles it is whether the proposition constrains the work and is needed in order to do it, which is a question the panel answers about the proposition. Both signers who examined it already agree the unpredicated form is correct rather than a gap, and the candidate carries the enforceability condition and the violation predicate, which are the parts that are predicable.  |  | [canon_form](TOPIC.md#canon-form) | Both signers who examined it agree the unpredicated form is correct rather than a gap. What is predicable is its enforceability condition and the violation predicate for candidate arms, and the candidate carries both. What would close it: op accepting an imposed proposition in the canon alongside established ones, and the canon stating which kind each of its sentences is. One signer draws the coupling explicitly: the step that closes the operation-set question is the firewall, so that question inherits whatever is decided here and they are one decision. The general form of this question is carried separately as the coordinator's item 2. | firewall, unpredicated, imposed, established, predicate, notation, enforceability, violation predicate |
| <a id="what-a-proof-marker-is-against-a-measurement"></a>what_a_proof_marker_is_against_a_measurement | How is the disagreement between the ratified predicate notation and this panel's own findings resolved? | A pass that supplies the missing dimension in each author's own voice, file by file, which is expensive and is the only form the never-widen-in-place rule permits., Op ruling that a proof carries a different marker from a measurement, so a width-free argument is written as one rather than being dressed as a sweep that happened to stop at three widths. | op | Recorded as answered by op on 2026-08-31. Both of the first two options, taken together. A proof carries a marker distinct from a measurement, so a width-free argument is written as one rather than dressed as a sweep that stopped at three widths; and the bounded whole-container range gets its own spelling, being neither a sample nor a universal. He declined the per-file restatement pass and declined leaving the notation as it stood, so no existing predicate is rewritten and the never-widen-in-place rule is untouched. Ratified as `ruling::a_proof_and_a_bounded_range_get_markers_the_notation_lacked`, recorded whole at `panel::202608072330_the-numeral-canon-panel::217_op_the_last_round_and_the_panel_finishes_alone`. |  |  | [the_predicate_notation](TOPIC.md#the-predicate-notation) | A finding about the corpus rather than about arvo. Counted across one topic's four files: 82 findings and not one carrying a width universal, against eighteen firings of the thread universal and thirty-nine of the target-feature one, so read literally that topic holds at no width in the library, which is not what any of its authors meant. The count's generalisation was corrected: it is right for the four files counted and does not prove the width axis's spellings were enumerated, since one file writes a width universal as two separately quantified widths that the instrument could not see. A third state exists the notation has no name for, a finding carrying a bounded whole-container range, which is neither a sample nor a universal, and this question should have that case in front of it. The second option is named in the source as cheaper and as the one the corpus is actually asking for; that is the source's judgement and not an answer. It is what makes this op's rather than the panel's. | predicate notation, marker, proof, measurement, width, any, absent dimension, corpus, retroactive, widen in place |
| <a id="may-the-canon-carry-an-unpredicated-proposition"></a>may_the_canon_carry_an_unpredicated_proposition | May the canon carry a proposition that has no predicate? | The canon may carry imposed propositions alongside established ones, and it marks which kind each sentence is., Everything in the canon carries a predicate, which means an imposed proposition is expressed as something else or not at all., An imposed proposition is a design-tier statement rather than canon, so it lives one tier down. | op |  |  | The form of every canon sentence, in every topic, including the ones already written. The largest of the queue's items in reach and the cheapest to answer. | [canon_form](TOPIC.md#canon-form) | Recorded as answered on 2026-08-31 by the test he gave in place of the three options, filed as `ruling::a_thing_that_constrains_the_work_and_cannot_be_designed_away_is_canon`; the answer is not written here. The instance is the firewall proposition, carried separately. The tenth unit couples a second half to this as one decision: whether a canon may carry a normative sentence beside established ones, its candidate marking every sentence with its kind precisely because a canon stating its partition and its licence as one kind of sentence loses the unit's main result. A further item folds in here rather than being answered on its own: whether a canon states four open domain dimensions as open or waits until they are measured, which the coordinator records as not derivable and the same question one level down. And a third, from the restoration pass: whether a canon statement may carry a measurement at all. | canon form, predicate, imposed, established, normative, theorem, measured, sentence kind, design tier, marking |
| <a id="is-the-rounding-candidates-pairing-section-canon-or-design"></a>is_the_rounding_candidates_pairing_section_canon_or_design | Where does the line between canon and design fall inside the rounding candidate's statement section? | The line as its author drew it: the impossibility, the decomposition, what the axis selects, the vacuity region and the coupling structure are canon, and the pairing recommendations are design, because a concrete pairing names a mode spelling and would need editing when the implementation moves., All of the section is canon., All of it is design, and the canon carries only the impossibility result. | op |  |  | The same line in every other topic, since each candidate has drawn it by instinct and none has had it confirmed. Answering it once fixes it everywhere. | [canon_form](TOPIC.md#canon-form) | Recorded as answered on 2026-08-31, by the same test and the same ruling as the canon-form question above, which he answered once for both; the answer is not written here. The author states his own reading and marks it as a reading rather than a finding, and says the line is his and that op may draw it elsewhere. | canon, design, tier, pairing, mode spelling, impossibility, decomposition, vacuity, permanence |
| <a id="why-the-default-rounding-position-is-chosen"></a>why_the_default_rounding_position_is_chosen | Is the default rounding position chosen for familiarity or for what it licenses? | Familiarity governs the default, and the licensing loss is accepted and named., Licensing governs, and the default is not the IEEE one, with the surprise named., There is no single default and the choice is carried per concern. | panel |  | Not his, because he has stated the rule that resolves the fork rather than leaving the fork open. `ruling::warms_objective_is_the_intuitive_best_choice`:  > I should add that Warm does not merely imitate, its intent is to be intuitive > best choice for most every use case, and the intuitive part demands it mimics, > but it does not make it absolutely required, if mimicking is consistently just > worse choice.  Familiarity is mimicry and licensing is the gain mimicry costs, so the fork is the one that ruling decides: convention is the baseline for what a user finds intuitive and is dropped where it is consistently the worse choice. Whether it is worse here is measured rather than argued, and the strategy-object topic measured it: the IEEE default is the one mode that is not free under either signedness, so a design defaulting to it has every relocation unavailable at the default.  The third option, no single default carried per concern, is the shape the ratified `ruling::the_work_is_predicated_arms_composed` predicts, and neither candidate wrote it down.  **This is one of the three least certain moves in this pass.** The attack on it: the ruling quoted is about one strategy's objective, and reading it as governing a crate-wide default is a widening. If that widening does not hold, the fork is a taste call and is his.  | Whether the rounding topic's arms are reachable at the default, which is the difference between an arm that exists and an arm anyone will ever hit. | [rounding](TOPIC.md#rounding) | An intent question rather than a measurement. The strategy-object topic's central result makes it sharp: the IEEE default is the one mode that is not free under either signedness, so a design defaulting to it has every relocation unavailable at the default. Familiarity and licensing point at different modes here, and the native-imitation intent pulls toward the first. The third option is the shape the ratified predicate intent would predict and neither candidate wrote it down; the coordinator adds it at `156`. | rounding, default, IEEE, ties to even, familiarity, licence, relocation, free, per concern |
| <a id="which-units-a-weighting-is-expressed-in"></a>which_units_a_weighting_is_expressed_in | Which units is a weighting expressed in, and is the named baseline fixed once or re-measured per target? | Fixed once, named in the canon, so portability is a property of the design., Re-measured per target, so portability is a property of each build., Units fixed and baseline re-measured, which the topic says does not buy the guarantee. | panel |  | Returned by `ruling::the_panel_finishes_the_canon_without_him`, and narrowed by `ruling::there_is_no_exchange_rate_because_there_is_no_generalisation`: there is no global rate between objectives, so a weighting is local to an arm and carries its justification in words. Answer inside that. Whether a weighting travels is then a property of the arm rather than of the design or of each build, and the strategy-object topic's measurement, that a weighting travels by construction while an arm does not, is the evidence to reconcile against rather than to restate. | Whether a weighting travels. | [the_strategy_axis](TOPIC.md#the-strategy-axis) | One candidate calls this a declaration rather than evidence; its revision sharpens it to two declarations rather than one and reports that only the conjunction buys portability. The strategy-object topic found the portability worry inverts: a weighting travels by construction while an arm does not, which is the opposite of what the panel assumed before it was measured. | weighting, units, baseline, portability, target, declaration, travels, arm |
| <a id="the-rounding-mode-vocabulary"></a>the_rounding_mode_vocabulary | Which mode vocabulary does the canon use for rounding, given that one word names two distinct operations on a signed domain? | Six mode names with the ambiguous word retired., The older spelling kept. | op | Recorded as answered by op on 2026-08-31. The word is retired in both spellings and the vocabulary is six explicit names: `toward_zero`, `floor`, `ceil`, `half_up`, `half_even`, `stochastic`. A note records that bit truncation of a two's complement value is floor, so the hardware operation is not read back into the name. He was given the two alternatives, keeping `trunc` defined once as toward-zero and renaming the hardware operation to `bit_drop`, and took neither. Ratified as `ruling::the_ambiguous_rounding_word_is_retired_for_six_explicit_names`, recorded whole at `panel::202608072330_the-numeral-canon-panel::217_op_the_last_round_and_the_panel_finishes_alone`. |  | Nothing structural. It is in the queue because naming is op's taste and taste does not converge by argument, so it will sit forever unless it is asked. | [rounding](TOPIC.md#rounding) | The coordinator deliberately did not restate the options at `156`, calling this the one item where a compression would change the question and directing op to read the source entry directly. The options above are taken from that entry rather than from the queue, and this row should be read as a pointer to it rather than as a substitute. The factual half is settled and is not contested: bit-drop is measured equal to floor on every row and different from toward-zero on signed rows only, so on a signed domain the word names two operations. What remains is the naming call, which is a canon-vocabulary decision rather than a measurement. | truncation, trunc, floor, toward zero, mode vocabulary, naming, signed domain, retirement, taste |
| <a id="the-container-premise"></a>the_container_premise | Is a declared numeral's footprint observable, so that behaviour is stated over the container rather than over the declared width? | Footprint is observable, under which the clause saying the realisation is not part of identity is false as written and is rewritten on that branch., A strategy changes a computed value, which is the escalation the panel has treated as an inter-topic pointer rather than a live fork., Two markers over one value set and one realisation map must not be two types, which contradicts the shipped rule rather than the candidate. | panel |  | Put to op and returned. It was filed as his and it is not; what he owed was the bound, and he gave it:  > Hmm, this is not mine to call, it is bounded by soundness and also the rule > that demands we provide first-class matlab and ieee754 compatible apis as > aliases over arvo primitives and such  So the panel decides it, inside two constraints. **Soundness**, which admits no branch that cannot be shown correct. And **the standards-alias demand**, filed as `obligation::every_standard_convention_expressible_as_an_alias_over_the_primitives`: a convention that cannot be written as an alias over the primitives is a gap in the primitives, which makes it an adequacy test on any answer here rather than a preference between the three options.  **A question he returns is a question that was misfiled.** Recorded so this one is not put to him a second time.  | The statement of what a primitive is, which is the panel's central noun and which four topics have deferred to. | [the_container_premise](TOPIC.md#the-container-premise) | This grew from an option into the thing the ninth unit could not finish without. Whether footprint is observable decides whether identity saturates at the literal; a container observation is measured to split every class, 32 to 64. The candidate's preamble claimed the premise was localised to one clause and two signatures refuted that independently: one found it reaches three clauses, one made it four, the strongest addition being that a clause's satisfiability moves, since under one branch the shipped rule's pair is connected both ways and separated by nothing, which is that clause's own definition of a spurious split. One signature refuses a clause as written and says it is not a wording complaint: no wording is true on both branches until this is ruled. It names three readings and chooses none, and two of the three are op's. | container premise, footprint, observable, internal, declared width, carrier, identity, realisation, spurious split, saturation at the literal |
| <a id="does-the-observability-principle-become-an-intent"></a>does_the_observability_principle_become_an_intent | Does the observability principle become an arvo intent of op's? | It becomes an intent and is quoted into the intent catalogue in his words, and the licence clause has ground., It stays a workspace rule and the canon states the clause as resting on it, visibly., The clause is not canon at all and drops to the design tier. | op | It becomes an intent. Op, given the three options: "It becomes your intent", and the pronoun is his, kept rather than resolved. Both surviving readings make it an intent and differ only in whose authority it carries: that he adopted it and phrased it loosely, or that he declined to own it and handed it to the panel. The licence clause that everything inside an unobserved stretch is arvo's to choose now rests on that designation rather than on a workspace rule, which is what three members could not derive it from. Carried as `ruling::the_observability_licence_is_an_intent_and_he_put_it_to_the_panel`, which is `stated` and carries no `ratified_by` for exactly that reason. This field used to read "It becomes his intent", which picks the reading his word does not carry. It landed the same day seat 210 established what observability is, that it is induced by a signature rather than carried by a representation, so the clause has both ground and a definition. Recorded whole at `panel::202608072330_the-numeral-canon-panel::212_op_the_alias_scope_the_demotion_and_observability`. |  | The ground under the tenth unit's licence clause, that everything inside an unobserved stretch is arvo's to choose. | [the_chain](TOPIC.md#the-chain) | The sharpest of the tenth unit's four. The licence clause is normative and underivable: three members tried to derive it from op's stated intents and all three failed for stated reasons. What it actually rests on is a workspace rule which none of the three cold derivations on the question names and which is line four of the auto-loaded set, and a workspace rule is not an op ratification. | observability, unobserved stretch, licence, normative, underivable, workspace rule, intent, ratification, perimeter |
| <a id="which-accuracy-target-the-accuracy-intent-names"></a>which_accuracy_target_the_accuracy_intent_names | Which accuracy target does the accuracy-first intent name? | Closeness to the exact composite, which is what deferring every interior resolution to the boundary implies., Reproducing what a named reference implementation does step by step, which would pick a different placement and is an equally coherent reading of the word. | op | Recorded as answered by op on 2026-08-31. Neither reading, because the question presumed a trade that does not exist. Op, given both placements and a third option of both per arm: "We never sacrifice performance. If we can't express something reliably and accurate enough for genereal computing (precision strategy is different, and would be used when more than usual precision is needed) without losing perf and efficiency, we haven't done out job well enough and lack the actual solutions and need to find them." So the placement is decided by which one is accurate enough at full speed, and where no such arm is known that is a gap in the solutions rather than a licence to rank one objective above the other. The 15.5x measurement stands as a fact about two candidate placements and stops being an argument for the slower one. Ratified as `ruling::accuracy_is_never_bought_with_performance`, recorded whole at `panel::202608072330_the-numeral-canon-panel::217_op_the_last_round_and_the_panel_finishes_alone`. |  |  | [the_chain](TOPIC.md#the-chain) | Priced rather than argued: the two placements differ by 15.5x in aggregate error and 16x in worst case at depth five, so it is not a distinction without a difference. The candidate's clause says which placement is distinguished, not which target is meant. The intent's own wording carries a second unresolved reading in the same sentence, ported here as its own row. | I7, accuracy, target, exact composite, reference implementation, placement, deferral, boundary, depth |
| <a id="chains-and-ops-as-two-things-or-one-phrase"></a>chains_and_ops_as_two_things_or_one_phrase | Does the accuracy intent's phrase "chains and ops" name two things or one? | Two things, chains and operations quantified separately., One phrase, naming a single subject. | panel |  | Returned by `ruling::the_panel_finishes_the_canon_without_him`. Read the sentence under `ruling::accuracy_is_never_bought_with_performance`, which is the same intent's other half and holds for chains and for single operations alike. The constraint is that the reading picked must not change what any arm does, since the intent it sits in is now settled independently of how the phrase parses; if a reading does change an arm, that is the finding and it outranks the parse. |  | [the_chain](TOPIC.md#the-chain) | Recorded as a second unresolved reading inside the same sentence as the accuracy-target question, and carried here as its own row because the two have disjoint answers; the split is this port's call. Nobody can settle either from the outside. | I7, chains, ops, wording, reading, quantifier, accuracy |
| <a id="which-chain-carrier-ships"></a>which_chain_carrier_ships | Which chain carrier ships? | The named accumulator, which carries the budget only., The combinator, which carries everything for the fold shape and nothing for the expression shape., The staged expression description, the only one reaching the backward facts because the sink triggers the lowering, and the one carrying a taste question about the native-imitation intent inside it., A composition of more than one of them, covering different shapes. | panel |  | Returned by `ruling::the_panel_finishes_the_canon_without_him`, and constrained by two rulings from the same round. `ruling::accuracy_is_never_bought_with_performance` means the carrier is chosen on what it costs at run time rather than on surface elegance, and a carrier that buys expressiveness with speed has not answered. `ruling::there_is_no_exchange_rate_because_there_is_no_generalisation` means the fourth option, several carriers each covering the shapes it is optimal for, is the paradigm rather than a compromise between the first three. The cost gradient across the first three, nothing new, then a crate surface, then a new binding-time surface, is evidence about each arm's price and not a ranking. |  | [the_chain](TOPIC.md#the-chain) | The discriminator and the doability evidence are in the tenth unit's ledger; the choice is op's. The cost gradient across the first three is stated there as nothing new, then a crate surface, then a new binding-time surface. The fourth option is the composition the unit's own option pass carries beside the three. | chain carrier, accumulator, combinator, staged, expression, sink, lowering, binding time, I3, obligations |
| <a id="chain-or-region-between-observations"></a>chain_or_region_between_observations | Is the object called a chain, or a region between observations? | Chain., Region between observations. | panel |  | Returned by `ruling::the_panel_finishes_the_canon_without_him`. What is at stake was settled by measurement before the naming was put: the object is the schedule rather than the carrier. So the constraint is that the name has to say schedule, and has to not let the path case read as the general case, since a path-only reading gets the schedule silently wrong at shared nodes. A name failing either of those is refused whatever its other merits. |  | [the_chain](TOPIC.md#the-chain) | A vocabulary decision for the canon writer with op, and one of two the tenth unit reserves. What is at stake was settled by measurement before the naming was put: the object is the schedule rather than the carrier. The path case is the easy instantiation either way, and a path-only reading gets the schedule silently wrong at shared nodes. | chain, region, observation, naming, vocabulary, schedule, path, shared node |
| <a id="does-the-position-keyed-dither-arm-ship"></a>does_the_position_keyed_dither_arm_ship | Does the position-keyed dither arm ship at all? | It ships., It does not. | coordinator |  |  |  | [rounding](TOPIC.md#rounding) | Handed to op by a candidate on the ground that the answer depends on what position means in a consumer and arvo does not know its consumers' access patterns, and answered in his stead by the coordinator at `156` as derivable; the answer is not written here. The call carries no authority and is recorded as overturnable in a sentence. The reasoning offered for it: the toolbox rule says arvo ships the choice and documents the tradeoff rather than deciding on the consumer's behalf, and not knowing the consumer's access pattern is that rule's own stated reason for shipping a knob rather than a reason for withholding one. | dither, position keyed, arm, ships, toolbox, policer, access pattern, knob, coordinator call |
| <a id="does-a-consumer-supplied-seed-surface-exist"></a>does_a_consumer_supplied_seed_surface_exist | Does a consumer-supplied-seed surface exist? | It exists., It does not, which picks determinism for every consumer. | coordinator |  |  |  | [rounding](TOPIC.md#rounding) | Raised rather than answered by a candidate, and answered in op's stead by the coordinator at `156` as derivable; the answer is not written here. The call carries no authority and is recorded as overturnable in a sentence. The reasoning offered: randomness cannot be sourced inside arvo, so either it crosses the boundary or the member is deterministic, which is why the question arises at all. | seed, entropy, randomness, stochastic rounding, determinism, surface, I14, coordinator call |
| <a id="is-the-non-terminating-contention-crate-a-defect"></a>is_the_non_terminating_contention_crate_a_defect | Is the non-terminating contention crate a gate defect to fix, or a measurement to record? | A gate defect to fix., A measurement to record. | coordinator |  |  |  |  | No topic in the register fits: it is a fact about the bench harness rather than about anything arvo measures. Answered in op's stead by the coordinator at `156` as derivable; the answer is not written here, and the call carries no authority and is overturnable in a sentence. The reasoning offered: the crate runs to green in 5.51 seconds under single-threaded test execution, and the hang is a process-wide pool shared across concurrently running tests, verified at source. Recorded as not yet fixed at the time of the call, because two dispatches were reading the bench directory in their blind phase and a file moving under a reader is its own defect. | contention, bench harness, hang, OnceLock, pool, test threads, gate defect, coordinator call |
| <a id="are-open-domain-dimensions-stated-as-open"></a>are_open_domain_dimensions_stated_as_open | Does a canon state the four open domain dimensions as open, or wait until they are measured? | State them as open., Wait until they are measured. | panel |  | Not his, by the same test that answers the question it was folded into. The row records the coordinator folding it into `may_the_canon_carry_an_unpredicated_proposition` on the ground that it is that question one level down, and that parent is answered by `ruling::a_thing_that_constrains_the_work_and_cannot_be_designed_away_is_canon`:  > This is something you should decide based on the workflow and soundness and > validity of the canon and work. If it's a real thing that constrains the work and > is needed to know, it's canon  Applying that test to this instance is a derivation rather than a second ruling, which is why the row moves rather than closing. The question to answer inside the bound is whether a reader needs to know a domain dimension is unmeasured in order to do the work, not whether an open obligation is the right kind of sentence.  |  | [canon_form](TOPIC.md#canon-form) | Handed to op by a candidate. The coordinator records it as not derivable and folds it into the unpredicated-proposition question rather than answering it, on the ground that it is the same question one level down: a canon carrying a visibly open obligation is carrying a sentence that is not an established claim. | domain dimension, open, obligation, canon, established claim, predicate, folded |
| <a id="does-a-named-policy-selection-survive-above-the-numeral"></a>does_a_named_policy_selection_survive_above_the_numeral | The panel holds that a selection among named policy alternatives does not survive above the numeral. The consumer's plan chain carries exactly one such selection, at its per-phase configuration step, with three named alternatives and a selector trait over them. Does that instance refute the claim, confirm it by turning out to be a different shape, or fall outside what the claim quantifies over? | It refutes the claim: a named policy selection does survive above the numeral, and the claim narrows to whatever excludes this case., It confirms the claim: the selection picks a grouping policy rather than an arithmetic realisation, and the two are different shapes wearing one word., It is outside the claim's scope, and the claim has to say so, because as written it reads as covering this and does not. | panel |  |  | Whether the strategy shape is a numeral-local thing or a general one, which decides what a composition contract may carry. | [the_strategy_object](TOPIC.md#the-strategy-object) | **The consumer implements this itself, so it is not an obligation and no obligation was filed for it.** Filing one would have been inventing demand: nothing in the consumer asks arvo for the selection machinery, and an obligation nobody asked for is how a design acquires a requirement out of somebody's memory. What it is instead is the only datum in that chain whose shape is a selection among named alternatives, which makes it the one thing available that could test the claim rather than illustrate it. Reported by an expert attacking a neighbouring derivation, which also states plainly that it is not claiming the consumer's selector is arvo's strategy. The type, its three alternatives, its selector trait and the chain step naming it were all verified in the consumer's own source. | strategy, policy, selection, named alternatives, phase, selector, grouping, above the numeral, test case, step 10 |
| <a id="which-closed-panel-statements-the-absolute-framing-carve-out-reaches"></a>which_closed_panel_statements_the_absolute_framing_carve_out_reaches | Op demoted his own voice across the prior corpus, and stated one exception: the instances where, in this panel's transcripts and checkpoints "or other files", he explicitly frames something as absolute, of which he recalls two, both abstract intents rather than concrete calls. The closed formalization panel is an "other file" and several of its statements are framed that way in his own words. Which of them does the carve-out reach? | None of them: "other files" meant other files of the live panel, so the closed panel is covered by the demotion whole and its statements return only if he restates them, as the standards bound did., The two he recalls, and he names which: everything else in the closed panel stays demoted regardless of how it reads., Every statement he framed as absolute there, identified by reading rather than by recollection, since he worded the count as recall rather than as a limit., The carve-out is about currency rather than authority, so a reached statement becomes something the panel must re-derive and put back to him, never something a row may cite as his call. | op | Reshaped rather than picked. All four options presented the demotion as total with the carve-out as its one exception, and op's answer says that premise is wrong. His words: "It's a rule of thumb that absolutes will fit but not itself an absolute. There must be exceptions. But as a general rule. Otherwise yes, demoted a bit, but not entirely. My calls and remarks and statements are still valid for the time, and valuable as precedence". So the demotion is partial, the absolute-framing carve-out is a rule of thumb rather than a rule and admits exceptions both ways, and his prior statements stay valid for their time and carry precedent value. The nine statements in the closed panel are precedent: readable and citable as such, and a member wanting one to bind puts it to him again rather than arguing it through the carve-out. Recorded whole at `panel::202608072330_the-numeral-canon-panel::212_op_the_alias_scope_the_demotion_and_observability`. |  | Whether nine statements of his in the closed panel are lost material or correctly absent, and therefore whether the `ruling` namespace is missing rows it should hold. | [panel_conduct](TOPIC.md#panel-conduct) | **Asked because the carve-out was ported and then not applied to the decision it bears on.** `179` section 17 is the porting seat declining to carry any `PRIOR_CALLS.md` substance into `ruling.toml`. It reasons from the demotion and from the D-number ruling, names its own generalisation from twenty-one numbered decisions to the whole corpus as the step to test, and names the pricing pillar as the single best argument against itself with no clean answer to it. It also minted the carve-out row and never ran it against its own refusal. Section 18 asks for a second reader on exactly this; the seat that catalogued the closed panel's op material is that read, agrees with the general refusal, and disagrees about the statements he framed as absolute.  The candidates found by reading every op file at source, with their framing: the four-part erasure gate, "anything less than that, no caveats left, is unacceptable for this design and canon"; "a constant is a function ... I call this as intent, settled canon, right now", which is the only place in that panel he uses the word canon of his own statement; and the pricing pillar, "this should be stated clearly and absolutely". A word list does not identify the class, which was tried and failed in both directions in one run, so the identification is by reading.  The options were not put to him as a preference. They are regions the carve-out could hold over, and the fourth is not a compromise between the others: it answers a different question, whether being reached restores authority or only currency. | absolute, carve-out, demotion, prior calls, closed panel, framing, authority, currency, erasure gate, pricing pillar, settled canon |
| <a id="is-root-check-cost-stated-within-a-behaviour-or-across-a-design"></a>is_root_check_cost_stated_within_a_behaviour_or_across_a_design | Should arm W1's cost accounting (the root-check licence) be stated within one behaviour, where it is free, or across the whole design, where the behaviour it belongs to is not free? | Within the behaviour: the cost is priced against what a consumer is told that one behaviour costs, and the licence is free there., Across the design: the behaviour itself is priced against every design that might reach for it, and is not free considered that way. | panel |  | Not his. Both readings are true in their own region, which makes this a predicate to state rather than a fork to rule on, and the one ratified entry in the registry says so directly.  `ruling::the_work_is_predicated_arms_composed`, in his words:  > We explicitly reject a universal solution. That is ass. The strategies themselves > as a concept make a universal solution impossible by premise. We collect and > compound answers to specific regions where a predicate holds and write the > expression where it holds ... We should add a rule where all findings have to be > predicated, even "universal" ones, so that they are exact  The licence is free within the behaviour and not free across the design. Those are two regions with two answers, both established, and the canon states both with the region each holds in. Collapsing them into one stated price is the unpredicated form the ratification rejects.  What stays genuinely open, and is the panel's to derive, is the pricing itself: the source places whether any consumer wants both the root-check licence and an order-based construction on the same values in I11's territory, and nothing in the repository measures it.  | Whether the root-check licence's cost gets a single stated price or is left to the design that hosts it. | [the_realisation_map](TOPIC.md#the-realisation-map) | A located precision rather than a disagreement between the two files that raised it: neither has answered the other. What would decide it, per the source: whether any real consumer wants both the root-check licence and an order-based construction on the same values, which the source places in I11's territory and which nothing in the repository measures. | root check, cost accounting, behaviour, design, realisation map, W1, I11 |
| <a id="is-w1bs-theorem-stated-on-its-argument-or-its-sweep"></a>is_w1bs_theorem_stated_on_its_argument_or_its_sweep | Should the realisation map's W1b theorem be stated on the argument that supports it (any fraction width) or on the sweep that measured it (three fraction widths)? | On the argument: addition and subtraction never enter the rounding region, which is fraction-width-free reasoning, so the theorem is stated for any fraction width and the sweep is a check., On the sweep: the predicate stays at the three widths actually measured, and the wider argument is recorded separately as unconfirmed. | panel |  |  | Whether the theorem's stated region is the argument's full reach or the sweep's narrower one. | [the_realisation_map](TOPIC.md#the-realisation-map) | The candidate's own prose and its predicate disagree on this (any fraction width in the prose, three widths in the predicate) and the author who would settle it has not answered. What would decide it: that author's own reading of whether the argument is evidence for the wider claim or a gloss on the sweep. | W1b, theorem, predicate, sweep, fraction width, realisation map, rounding region |
| <a id="does-the-order-based-realisation-family-have-more-than-one-member"></a>does_the_order_based_realisation_family_have_more_than_one_member | Does the order-based realisation family (the map's third character) have more than one member, or does the word "family" currently name one instance? | More than one member exists and an enumeration will find it., One member is all there is, and the word overstates a singleton. | panel |  |  | Whether a clause claiming a 'family' of order-based realisations is entitled to that word, and how much weight the clause built on it can carry. | [the_realisation_map](TOPIC.md#the-realisation-map) | A candidate section proposed enumerating this family and expected a list rather than a single number; nobody has run the survey. Until it exists, the clause resting on the word 'family' is weaker than it reads. | order-based, family, realisation map, enumeration, survey, character |
| <a id="are-a-consumers-derivations-trees-or-dags"></a>are_a_consumers_derivations_trees_or_dags | Are a consumer's arithmetic derivations trees, or directed acyclic graphs where a value used twice at the source level becomes a shared node? | Trees: leaf multiplicity is a syntactic property and is computable the way the realisation map's condition computes it., DAGs: a compiler-level shared node makes leaf multiplicity not syntactically computable, and the condition needs a different formulation. | panel |  |  | Whether the realisation map's leaf-multiplicity condition is computable as currently stated, since two files now name it as the assumption they are least comfortable with and neither has looked. | [the_realisation_map](TOPIC.md#the-realisation-map) | Named independently by two files as what they would most want checked, and neither checked it. This is a modelling-assumption question about how a consumer's terms are represented once compiled, not a design choice, so it wants an investigation of what the compiler actually produces rather than an argument. | trees, directed acyclic graph, DAG, leaf multiplicity, shared node, realisation map, syntactic |
| <a id="does-the-rounding-variance-form-hold-at-a-second-fraction-width"></a>does_the_rounding_variance_form_hold_at_a_second_fraction_width | Do the rounding axis's variance forms, stated as algebraic in the fraction width from a check at one fraction, actually hold at a second fraction width? | Yes, and the algebraic claim generalises as stated., No, and the claim narrows to the one fraction width it was actually checked at. | measurement |  |  | Whether the variance forms may be cited as `fraction_width any` or only at the one width checked. | [rounding](TOPIC.md#rounding) | The source states the forms are algebraic in the fraction width on the strength of one checked fraction. What would decide it: one sweep at a second fraction, described in the source as cheap and as something nobody has run. | rounding, variance, fraction width, algebraic, sweep, generalisation |
| <a id="do-arvos-consumers-want-value-keying-or-position-keying"></a>do_arvos_consumers_want_value_keying_or_position_keying | For the rounding axis's two keying members (value-keyed and position-keyed), which does arvo's actual consumer base want, given the two diverge and a design must choose? | Value keying., Position keying., Both, gated per consumer or per call site. | panel |  | Not his, and the shape of the question is what he has already refused. It asks which single keying a whole category wants, given the two diverge and a design must choose. He rejected exactly that shape, on a question in the same I11 territory, filed as `ruling::there_is_no_universal_answer_take_the_win_and_gate_it`:  > Again, we don't need to settle for one universal solution, it's the anti-pattern > I've already named. Case by case. If commutative is void of soundness issues in > one case, guard it with const expressions so it benefits, but it doesn't leak > unsound premise to anything it does not apply to. I don't understand what is so > hard to get about this? This is just me re-stating this same fucking thing over > and over, just under different names. Be it law, sentinel predicates, whatever > the fuck, it's the same. Take the win where it applies, gate it out from where it > does not. No single one-fits-all solutions, it's impossible  The refusal there was on a question shaped as whether consumers would write a particular declaration, so the design could decide whether to build the surface at all, which is this question with the nouns changed. His note on that occasion is that it was the third of the shape in one sitting.  So the third option is not a compromise between the first two, it is the answer the ratified `ruling::the_work_is_predicated_arms_composed` already gives, and what the panel owes is the predicate each keying is gated on rather than a winner.  | Which keyed member a strategy pairs with by default, or whether both ship. | [rounding](TOPIC.md#rounding) | The divergence between the two members is measured; which one real consumers reach for is not, and the source places this in I11's territory, naming it a wall the panel has hit before on the analogous realisation-map question. | value keyed, position keyed, rounding, divergence, consumer demand, I11 |
| <a id="does-the-position-keyed-members-monotonicity-failure-rate-differ-from-the-independent-members"></a>does_the_position_keyed_members_monotonicity_failure_rate_differ_from_the_independent_members | Does the position-keyed rounding member's monotonicity-failure rate differ meaningfully from the independent member's, or are the two counts simply not comparable as measured? | The rates differ meaningfully once measured on a shared construction and input shape., The rates do not differ meaningfully, and the apparent gap is an artifact of the two members being measured differently. | measurement |  |  | Whether the two keyed members carry a stated reliability difference or are treated as interchangeable on this axis. | [rounding](TOPIC.md#rounding) | Two source files each hold one count and neither measured the other member under the same construction, so the counts as they stand are not comparable. What would decide it: a sweep with one construction and one input shape held fixed and varied across both members, which nobody has built. | monotonicity, position keyed, independent, failure rate, rounding, comparability |
| <a id="which-selector-does-the-design-ship-linear-or-augmented-chebyshev"></a>which_selector_does_the_design_ship_linear_or_augmented_chebyshev | Does the strategy object's selector ship as a plain linear weighting, which reaches every hull vertex, or as an augmented Chebyshev form, which reaches every non-dominated arm at the same measured compile-time and portability cost? | Linear, kept as the default until a real case needs more., Augmented Chebyshev, shipped now since it costs no more and reaches a strictly larger set of arms. | measurement |  |  | Which selector construction the strategy object's weighting compiles to. | [the_strategy_object](TOPIC.md#the-strategy-object) | Decided by whether an arm set arvo ships ever contains an established unsupported-but-efficient arm, which the source measured as zero of 29 askable families today. The source's own reading: the honest default is linear, and the question reopens the day a first such instance is found. | selector, linear, augmented Chebyshev, hull vertices, non-dominated, strategy object, Pareto |
| <a id="is-the-accumulator-cell-stated-conservatively-or-with-a-schedule-dimension"></a>is_the_accumulator_cell_stated_conservatively_or_with_a_schedule_dimension | Is the strategy object's accumulator cell stated conservatively (one predicate covering every schedule), or does it need an explicit schedule dimension because different consumer kernels fix different accumulation orders? | Conservative: one predicate, the schedule left unstated and treated as free., Schedule-dimensioned: the predicate names the accumulation order because at least one committed kernel fixes one. | measurement |  |  | Whether the accumulator clause of the strategy-object statement carries a schedule dimension. | [the_strategy_object](TOPIC.md#the-strategy-object) | Decided by whether any consumer kernel has a fixed declarable accumulation order. The source notes `satfold-*` and `warm-clamp-*` under `mock/benches/variants/` already carry accumulator arms, so the answer is a computation over committed artifacts rather than a new measurement. | accumulator, schedule, conservative, strategy object, kernel, accumulation order |
| <a id="is-the-dominance-gate-symmetric-or-conservative"></a>is_the_dominance_gate_symmetric_or_conservative | Is the strategy object's dominance gate (under F144-18) symmetric or conservative, given the two readings produce different askable-family counts (20 against 29)? | Symmetric., Conservative. | panel |  |  | The askable-family count the strategy object's dominance gate produces, and therefore which of the selector question's two readings the zero-instance count in that question is measured against. | [the_strategy_object](TOPIC.md#the-strategy-object) | Decided by stating which the design means, a definitional call rather than a measurement: both readings are internally coherent and differ only in how many families they let through. | dominance gate, symmetric, conservative, F144-18, askable families, strategy object |
| <a id="is-a-profitability-predicate-resolved-pessimistically-when-not-const-available"></a>is_a_profitability_predicate_resolved_pessimistically_when_not_const_available | When a profitability predicate (choosing a reassociated arm the law does not always hold for, but which is right for runs long enough to matter) cannot be evaluated at const time, is it resolved pessimistically (treated as false, arm not chosen, no lie possible), or does the design leave that condition unhandled? | Resolved pessimistically: unavailable evaluates to false, the arm is not chosen, correctness is preserved at the cost of a missed optimisation., Left unhandled: the design states no answer for a non-const-available profitability condition. | panel |  | Never put to op, and it is not his: it is a soundness question, and soundness is a bound he named rather than a thing he settles case by case.  > Hmm, this is not mine to call, it is bounded by soundness and also the rule > that demands we provide first-class matlab and ieee754 compatible apis as > aliases over arvo primitives and such  The row's own distinction is the derivation. A correctness predicate must be const, because an arm chosen where the law does not hold is wrong at every length. A profitability predicate only wants to be const, and where it is not, resolving it pessimistically is slow and never wrong. One option preserves correctness under every evaluation order; the other leaves a soundness-bearing condition with no stated behaviour, which the bound does not admit.  `ruling::the_work_is_predicated_arms_composed` states the same constraint from the other side: an arm optimises one nameable region and applies nowhere else, so a predicate that cannot be evaluated has not been shown to hold and the arm is not reached.  | Whether a profitability predicate that is not const-available at a given call site silently degrades to the arm being skipped, or is an undefined case the design has to close before it can be relied on. | [the_strategy_axis](TOPIC.md#the-strategy-axis) | The distinction the question rests on: a correctness predicate must be const (an arm chosen where the law does not hold is wrong at every length, and cannot be resolved by falling back on 'usually right'); a profitability predicate merely wants to be const, and where it is not, resolving it pessimistically is slow but never wrong. Op's ruling `83` is cited by the source as leaving open what happens to a non-const-available condition, and nothing in this panel has touched it since. | correctness predicate, profitability predicate, const, pessimistic, reassociation, 83 |
| <a id="is-the-strategy-weighting-objective-linear-or-log-scale"></a>is_the_strategy_weighting_objective_linear_or_log_scale | Is the strategy weighting's objective function linear (a weighted sum of the coordinates as measured), or log-scale (a weighted sum of logarithms, equivalently a weighted geometric mean, scale-invariant per coordinate)? | Linear: the objective sums the coordinates directly, as every existing weighting in this panel assumes without stating it., Log-scale: the objective sums logarithms of the coordinates, which would dissolve the units question (`do_arvos_consumers_want_value_keying_or_position_keying` and its neighbours) rather than answering it, since a geometric mean is scale-invariant per coordinate. | measurement |  |  | Which laws the strategy weighting obeys, and every cell count in the weighting-ceiling section of whichever candidate states it, since a different objective is a different computation over the same table rather than a re-derivation of the same one. | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Explicitly untested. The linear objective is what every weighting in this panel has assumed, including the source that raises this question, without anyone stating the assumption. Deciding this before the weighting-ceiling counts are treated as settled matters because the two objectives are not two readings of one computation; they produce different laws and every count would have to be recomputed under the log-scale reading. | log-scale, geometric mean, linear objective, weighting, scale-invariant, units |
| <a id="which-strategys-laws-govern-a-cross-strategy-operation"></a>which_strategys_laws_govern_a_cross_strategy_operation | When an expression composes operands under two different strategies and the resolution rule picks the more conservative side, does the resolved strategy inherit the union of both operands' laws, or does it have its own laws that merely dominate the weaker operand's? | Inheritance: the composed operation is guaranteed every law either operand individually satisfies., Domination without inheritance: the resolved strategy is a third thing with its own laws, chosen to be at least as strong as the weaker operand's, never assumed to be the union of both. | panel |  |  | What a canon may state about a cross-strategy expression's guarantees without re-deriving them per pair of strategies. | [algebraic_laws](TOPIC.md#algebraic-laws) | Adjacent to Q27 (is interoperation conversion, resolution, or neither) and not the same question; Q27's own note already flags this one as adjacent and unaddressed. A first mechanically-checked instance now bears on it: a two-axis strategy-resolution rule, checked as a semilattice join at compile time, supports the second option as the honest meta-law rather than settling it, offered as a direction by its own author rather than a resolution. | strategy, resolution, cross-strategy, inheritance, monotonicity, join, Q27, adjacent |
| <a id="which-repair-the-refuted-kleene-identification-takes"></a>which_repair_the_refuted_kleene_identification_takes | Two retirement rows name the nine-point view lattice as what replaces the claim they retire, and one of them names the position where a view preserves values and events while losing definedness. The identification of the Kleene equation with that position was compile-refuted, three repair shapes were offered, and none was chosen. Which repair does it take, and do the two replacement pointers survive it? | The identification is dropped and the lattice keeps its other two named points, so the replacement pointers stand but name one fewer correspondence than they read as naming., Definedness becomes a component of its own, so the lattice grows an axis and the position the second retirement names is not the position it currently denotes., The third repair the refuting file offered, which is recorded there and is not restated here because no registry row carries it and restating it from a summary would be the compression this panel keeps catching., The replacement pointer is wrong and the two retirements need a different one, which would make the retired claims open again rather than superseded. | panel |  |  |  | [algebraic_laws](TOPIC.md#algebraic-laws) | Filed because the hazard is invisible from inside the registry. `retirement::dl_the_ordered_three_relation_ladder` and `retirement::dl_partial_associativity_as_a_named_gap` both point at the nine-point view lattice, which appears in no `proposal`, `law` or `probe` row of its own: it is named only as something other claims were replaced by. The refutation is that definedness is not recoverable from the grade's cause component, established against the design's own division chapter, where a value can carry a nonempty cause and still be defined. The probe that had made the identification look safe set its own definedness flag from its own cause counter, so the model made the invariant true rather than testing it, and twenty-four files carried the claim unchallenged; that instrument is the worked example the workspace's own spike rule cites. Read as a question about the registry rather than about the archive: what is at stake here is whether two live replacement pointers say something true.  The closed panel's op checkpoint records him treating the calls resting on this as stale rather than patched, and asking for a re-evaluation that never ran. That is not cited as authority and does not set `decider`: his voice across that panel is demoted by his own word, and the re-evaluation he asked for was asked of a panel that then closed. | Kleene, view lattice, nine-point, definedness, Presence, Ignore, grade, cause component, finest view, partial associativity, repair, replacement |
| <a id="which-successor-carries-the-strategy-shape-ratification"></a>which_successor_carries_the_strategy_shape_ratification | Op ratified the lineage of `ruling::a_strategy_is_a_preset_naming_a_point_with_a_weighting_in_it` and directed the ratification to its successor rather than to it. Three proposals refine what a strategy is and all postdate that ruling. Which of them is the successor he meant, or is it a fourth thing nobody has written? | `proposal::a_strategy_is_a_pair_of_an_observable_assignment_and_a_weighting`., `proposal::a_strategy_is_a_declared_semantics_together_with_a_weighting_over_the_arms_that_realise_it`., `proposal::a_strategy_is_a_consumer_supplied_weighting_over_measurements_never_over_implementations`., None of the three, and the successor is a formulation the panel has yet to write, in which case the ratification is a standing promise against a row that does not exist. | panel |  |  | Where a ratification already granted actually lands. Until it is answered, the strategy axis has a blessing with nothing under it, and the ruling it was granted through stays at `stated` because op sent it past that row on purpose. | [the_strategy_axis](TOPIC.md#the-strategy-axis) | His words, in `panel::202608072330_the-numeral-canon-panel::211_op_the_second_ratification_round`: "Again, on the option 1, I think there's a better wording for that since that one you mention, so the ratification goes for the actual succeeding one rather than this, but the lineage is ratified and blessed thus". Two halves and they are different. The lineage is blessed, which is settled. Which row is its tail is not, and the three candidates are panel output rather than his words, so picking one is a reading and not a transcription. Filed as the panel's rather than his because he has already given the ratification; what is missing is the identification, and that is derivable from the three texts against the intent he blessed. The ruling stays at `stated` deliberately: stamping it would take the ratification he explicitly sent past it. | strategy, successor, lineage, ratification, preset, weighting, observable assignment, declared semantics |
| <a id="which-standards-the-alias-obligation-covers"></a>which_standards_the_alias_obligation_covers | Which industrial conventions must be expressible as first-class aliases over arvo's primitives? | The two named when the bound was stated, MATLAB `fi`/`fimath` and IEEE 754, with nothing else owed., Every convention the corpus already carries: those two plus SystemC `sc_fixed`, Vitis `ap_fixed` and Algorithmic C `ac_fixed`., A named core with the rest declared open, so the parity suite starts somewhere and the set grows as consumers arrive. | op | The two named are the core the parity suite is built against, MATLAB `fi`/`fimath` and IEEE 754, and the other three the corpus carries are reserved rather than excluded. Op: "Let's start with the two named, and reserve the rest named for future, so it can't close them out". The reservation is the operative half: a candidate that cannot express `sc_fixed` is neither refuted nor cleared by this, and naming them in the canon is what stops this answer being read later as having ruled them out. Recorded whole at `panel::202608072330_the-numeral-canon-panel::212_op_the_alias_scope_the_demotion_and_observability`. |  | Applying the standards bound at all. It is one of the two constraints the container premise and the operation set were handed back under, four topics defer to those two, and an adequacy test whose scope is unnamed cannot refute a candidate or clear one. | [arvo_identity](TOPIC.md#arvo-identity) | Filed because it existed only as a `gap` on an obligation, which no roster reads: every list of what is owed here is built from `question` rows and their `decider`, so this was invisible to the queue while being upstream of the two largest questions in it. The obligation's own words: which conventions are in scope is unstated, five appear in the corpus, two were named, and no row says whether the set is those, more, or a core with the rest open.  Why it is his rather than derivable: the bound is a demand he made, and how far a demand reaches is the demand's author's to say. The panel can establish whether any given convention is expressible, which is the adequacy test working, and cannot establish which ones were promised.  The second half of the same gap is not asked here and is not his: the parity suite is a mandate with no artifact, and building it is work rather than a decision. It cannot start until this is answered, which is most of why this ranks where it does. | MATLAB, fi, fimath, IEEE 754, SystemC, sc_fixed, ap_fixed, ac_fixed, alias, convention, standard, parity suite, scope, adequacy |
| <a id="whether-a-law-layers-consumer-is-the-lowering-or-one-of-several"></a>whether_a_law_layers_consumer_is_the_lowering_or_one_of_several | Two `algebraic_laws` rows define a law's consumer differently. One says the question a law layer answers is whether a law reaches a lowering the backend could not prove for itself, which makes the lowering the consumer and narrows the paying region to where the backend is blind. The other says the laws split into two families with two consumer classes, adaptation laws read by order transport and coherence read by a reduction that may be split or reassociated, all four combinations inhabited. Order transport is not the lowering. Which is the law layer's consumer, and is the second row's count of two a closure or a floor? | The lowering is the consumer and the second row's families are a classification inside it, so order transport reaches the design through a lowering like anything else and the two rows compose., There are several consumers and the first row states one of them, so its sentence narrows to the backend-facing case and stops defining what a law layer is for in general., Both stand at their own addresses because they answer different questions, one about what makes a law pay and one about who reads it, and the conflict is in reading either as a definition of the layer., The count of two is a floor rather than a closure, which leaves the first row's sentence standing and makes the second row's `says` overstate what its evidence reaches. | panel |  | Answered inside `ruling::there_is_no_exchange_rate_because_there_is_no_generalisation` and the patchwork it states: a single answer covering the whole category is the shape op has refused by name, so a reading that makes one of these rows the definition and the other a special case needs to say what region each holds in rather than which one wins. `I13` supplies the form. A law that pays only where the backend is blind and a law that pays because order transport reads it are two arms with two predicates, and the question is what those predicates are, not which sentence survives.  | Promotion of either row. They sit in one topic at one standing, and seat 215 established that they cannot both be promoted as written, so the pair is blocked on this rather than on evidence. | [algebraic_laws](TOPIC.md#algebraic-laws) | Filed by the coordinator from seat 215's second read, which reached the conflict from the two rows' texts rather than from either author. Neither row is refuted and neither author saw the other's: `80`'s law-layer finding came out of a probe that refuted its own thesis, and `67`'s families were measured independent in the format topic with all four combinations inhabited. What is open is only whether the first sentence is a definition of the layer or a statement about one of its consumers, and the second row's `note` already concedes that the mapping from families to consumers is the one-expert part while the families themselves are carried as a law row.  Seat 215 refuses the closure at two separately from the conflict, which is why the fourth option exists: nothing establishes that no third consumer class arrives, and a count stated as a closure is the kind of category-wide claim this workspace files as an arm rather than a ruling. | law layer, consumer, lowering, backend, order transport, coherence, law families, closure, floor, conflict |
| <a id="what-the-ingest-row-claims-and-what-turns-on-it"></a>what_the_ingest_row_claims_and_what_turns_on_it | `ruling::ingest_is_the_consumers_and_the_c_abi_is_where_it_ends_up` carries op's words about the C ABI and about helpers arvo may ship and may not use in place of the consumer. He was asked to bless it and declined, saying the explanation did not explain what the thing is. What does the row claim, concretely enough to judge, and what turns on it? Specifically: does it constrain arvo's surface at all, or is it a statement about what arvo declines to do? | It constrains the surface: arvo ships casting and conversion helpers, and the constraint is that no arvo entry point calls one on the consumer's behalf, which is checkable against any proposed API., It is a refusal and constrains nothing arvo builds: it says a validating ingest constructor is not arvo's to write, which the never-a-runtime-check intent already reaches, so the row is a restatement., It constrains the consumer-facing generics instead: the claim is that arvo's shapes are expressive enough to define an API against, which is an adequacy demand on the shapes rather than a rule about ingest., The row conflates two claims, one about the C ABI being unavoidable and one about helper usage, and they are separate rows with separate consequences. | panel |  | Answered inside `ruling::the_panel_finishes_the_canon_without_him`. He declined to bless this and the panel file recorded that it returns to him once it carries what the row actually claims; it cannot, because he has since handed the canon over. So the elaboration he asked for is the panel's to write, and it is written to his standard rather than sent to him.  Two of his rulings bound what the answer may say. The never-a-runtime-check intent means no option here reintroduces a validating constructor, and `arvo-toolbox-not-policer` means an answer that has arvo making the consumer's choice for them is refused on its face. The quote itself already draws the line, that helpers may exist and may not be used in place of the consumer, so what is open is what that constrains rather than whether it holds.  | Promotion of the row. It sits at `stated` carrying his words, reads as ready, and is the one row he was asked about and refused, with nothing on it saying so until now. | [arvo_identity](TOPIC.md#arvo-identity) | Filed by the coordinator from seat 218's check of the stated rows against his own words. The refusal is at `211:106` and the row is faithful to the quote it carries; what it lacked was any mark that a promotion is blocked, so a coordinator working the `stated` list top to bottom would have promoted it. That is the same shape as the `awaiting-a-ruling` tool listing declined and held rows unmarked, which is filed separately.  The row's own `says` also opened on a boundary sentence the quote does not contain, which is corrected on the row rather than here. This question is about what the remaining sentence commits arvo to, which is what he said was missing. | ingest, C ABI, helper, cast, convert, boundary, consumer, blessing, declined, elaboration |
| <a id="what-stops-two-files-by-one-persona-reading-as-two-experts"></a>what_stops_two_files_by_one_persona_reading_as_two_experts | The two-expert tier is the strongest standing a proposition can hold, and nothing mechanical distinguishes two files by two personas from two files by the same persona wearing two seat numbers. What closes that? | A seat records its persona in the registry and the standing check reads it, so the count is over distinct personas rather than distinct files. Cost: a field somebody has to fill honestly, and a persona that changes its mind between sittings still counts once when arguably it should not., Nothing mechanical, and the requirement moves to the dispatching side: a coordinator checks persona history before pairing a seat, and records the check with the promotion. Cost: it is a discipline rather than a gate, and the incident that produced this question was a coordinator failing exactly that check., Independence is not a property of the author at all but of the instruments, so the check is over what each file actually varied. Cost: it is the honest reading and no instrument in this repository can compute it today. | panel |  |  | Whether any two_experts standing in the registry can be trusted without a human reading both files and recognising the authors. | [panel_conduct](TOPIC.md#panel-conduct) | **Filed because the corpus currently reads as though this were solved.** The mechanical check verifies a `two_experts` or `three_or_more` row cites more than one file, and its own text says plainly that it cannot tell whether two authors arrived separately and does not pretend to. That honesty is inside the check and invisible to anybody reading the registry, where the standing just says `two_experts`.  **It has already happened once and was caught only by self-disclosure.** Seats 210 and 221 are the same persona, and 221 reported the collision itself rather than any instrument finding it. Nothing in the registry rests on that pair as though it were two, which is luck plus one honest author rather than a mechanism working.  The third option is what `expert-dispatch-defends-the-canon` already says in the workspace: two agreeing instances agree about the intersection of the dimensions they each varied, which is sometimes empty. That framing makes persona identity a proxy rather than the thing itself. | two experts, standing, persona, independence, seat, provenance, promotion, collision, self-disclosure, instrument |
| <a id="can-a-claim-about-the-canons-own-structure-carry-a-region"></a>can_a_claim_about_the_canons_own_structure_carry_a_region | All declared dimensions are numeric or machine axes, so a reasoned claim about the canon's own contents has no expressible region and can only be filed `normative` or `definition`. Is that the intended shape or a gap in the notation? | A gap: the dimension vocabulary grows an axis over the canon's own structure, so a structural claim can say where it holds the way every other claim does. Cost: an axis whose values are topics or namespaces, which is a different kind of thing from a width, and the notation would then range over two kinds., Intended: claims about the canon are governance rather than findings, so `normative` is the right filing and the absence of a region is correct rather than missing. Cost: a reasoned structural claim and an imposed one become indistinguishable once filed, and the reasoning stops being checkable., Neither: such a claim does not belong in the registry at all, and the canon should carry claims about arvo while claims about the canon live in the workspace rules. Cost: the layering derivation and its siblings move out, and the registry loses the ability to state what it rests on. | panel |  |  | Whether `normative` is currently doing duty for a third thing nobody named, and therefore whether any structural claim in the registry is checkable. | [the_predicate_notation](TOPIC.md#the-predicate-notation) | **Filed rather than left inside one proposal's prose, so something forces it to be revisited.** The twenty-one declared dimensions are integer width, fraction width, total width, signedness, overflow policy, rounding, operation, arity, chain length, container, alignment, access pattern, target features, threads, strategy, ambient domain, radix, accumulator width, toolchain, build profile and operand window. Every one is about a number or a machine.  **So the check that refuses an `argument` carrying no region is right to fire on a structural claim, and the only way past it is to file the claim as something it may not be.** Three rows hit this in one sitting and were refiled `normative`, which is defensible for a claim that imposes an order and wrong for one that reasons its way to a conclusion. The notation gives no way to tell those apart afterwards.  The second option is not a concession. A canon sentence about how the canon is written may genuinely be governance, in which case nothing is broken and this closes. | predicate, region, dimension, structural, normative, definition, sentence kind, governance, notation, gap |
