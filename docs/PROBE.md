<!--
  AUTO-GENERATED: DO NOT EDIT DIRECTLY

  Generated by: mockspace (mock)
  Generated at: 2026-08-31T02:08:43Z
  Source: mock/
  To regenerate: cargo mock
-->

# Probes

A committed instrument, what it establishes, and the case that had to fail before its number counted.

84 rows. Identifiers are permanent: assigned once, never reused, never renumbered.

| id | establishes | control | reproduced | defect | standing | note | keywords |
|---|---|---|---|---|---|---|---|
| <a id="chain-resolution-degeneracy"></a>chain_resolution_degeneracy | For a depth-D chain over an 8-bit domain swept exhaustively, wrapping over ring-affine steps has resolution degeneracy exactly 1, so every interior placement computes one function; rounding has degeneracy above 1 and rising with depth, so the placements disagree and the design must name which one is the answer. | Three cases stated in the header before the run and all three fired. Wrapping over ring-affine steps had to report degeneracy exactly 1, or the homomorphism claim p1 rests on would be false. Rounding had to report above 1 and rising, or the probe counts nothing. And at least one input had to exist on which the fully eager placement is closer to exact than the fully deferred one, because error cancels; had none existed, deferring everything would be pointwise optimal and the aggregate framing unnecessary. | Widened and reproduced by `chain_every_placement_widened`, which regenerates this probe's own chains from its xorshift seed and alphabet, reproduces its published 0 / 0 / 91 and 1330 winning inputs exactly at two placements, and then runs every placement. The degeneracy result stands; the pointwise-optimality search was thin by 71 percent of the counterexamples it could reach. | Scope, and no control could have caught it. The pointwise-optimality search `eager_wins` opens with `let fm = full_mask(steps.len())`, so it compares fully eager against fully deferred: two placements of up to sixteen, while the claim it supported quantified over all 2^(D-1). Found by `169` section 2 reading the claim's quantifier beside the loop bound. A control tests whether an instrument measures what it points at and cannot test whether it points at the whole claim. | sound | The claim survived the scope defect. Round and clamp come back at zero win-chains over every placement, so `168` was right and right for the reason it gave. What it had was one placement's worth of evidence for a statement about sixteen. | degeneracy, chain, resolution, placement, deferral, interior, eager, exhaustive, 2^(n-1), wrapping, rounding |
| <a id="chain-every-placement-widened"></a>chain_every_placement_widened | Over `168`'s own chains, regenerated from its seed, widening the search from two placements to all of them takes the truncate row from 91 win-chains to 317 and from 1330 winning inputs to 13527, and leaves nearest-round and clamp at zero win-chains under both searches. | Three, all fired and all printed with their required verdict. C1: arm one had to reproduce `168`'s published 0 / 0 / 91, which is what says this is their experiment and not a neighbouring one. C2: truncate had to win in both arms. C3: the widened arm had to find strictly more than the narrow one, or the widening bought nothing and the scope defect would have been cosmetic. |  |  | sound | C1 is the arm worth copying. An instrument built to widen somebody else's search proves it is the same search by reproducing their number first, and without that arm a disagreement is unattributable between the widening and a different setup. | placement, widened, reproduction, clamp, truncate, scope defect, win-chains, deferral, pointwise optimal |
| <a id="chain-affine-collapse-under-both-profiles"></a>chain_affine_collapse_under_both_profiles | For a per-element affine chain at 13-bit width in a `u16` container, both deferred arms vectorise and both eager arms do not, at cargo's default release profile and at the documented fat-LTO one-codegen-unit profile alike, including in a chain where the affine collapse is impossible. | Stated in the header before the run and it fired in the wrong direction, which is the useful outcome. The prediction was that `affine_deferred` would differ from `affine_eager` while `blocked_deferred` and `blocked_eager` would not, because the interposed right shift is not affine over the ring. Both deferred arms vectorised and both eager arms did not, `blocked` included, so the difference is not the affine collapse. Its author records the refutation of the hypothesis rather than the confirmation, and built `chain_which_projection_blocks_the_vectoriser` to find the real mechanism. |  |  | sound | Its own header calls it an ad-hoc quick spike with no substance as a measurement, correctly: no timing appears in it and it prices nothing. What it establishes is qualitative, that the shape is the same under both profiles, and that is exactly the qualitative claim an ad-hoc spike may carry. | affine collapse, vectorise, profile, lto, codegen-units, deferred, eager, assembly, refuted prediction, qualitative |
| <a id="chain-which-projection-blocks-the-vectoriser"></a>chain_which_projection_blocks_the_vectoriser | In a chained reduction at 13-bit width in a `u16` cdylib on Apple silicon, the projection on the loop-carried accumulator is what the backend reacts to and the projection on the per-element value is not, at cargo's default release profile and at the documented fat-LTO one-codegen-unit profile alike. | Two projection-free reductions, `control_plain_sum` and `control_indexed_sum`, which must vectorise; if they do not, the setup is what suppresses vectorisation and nothing in the run may be read as a fact about the arms. The runner prints CONTROL FAILED and says do not read this run. It fired, and a second case was stated too: at least one pair of arms must differ in whether the loop vectorises, or the probe has isolated nothing. |  | Three, all in earlier versions of this instrument, all found by adding the control and none visible from reading the code. A staticlib was tried first, in which every object file starts at address zero, so the identical-code-folding check compared addresses across objects and reported a hundred unrelated symbols as folded. `--emit asm` under fat LTO reports the pre-LTO module in which nothing vectorises including the positive control, and an earlier version concluded from that that the documented profile suppresses vectorisation entirely, which is a statement about the instrument. And the vector-register pattern required `v0.`, which is ARM's syntax; Apple's puts the element form on the mnemonic, so the pattern matched nothing and reported a fully vectorised function as scalar. | sound | The three defects are kept in the runner's own header rather than in a report, which is why they are recoverable. The wrong-dialect regex is the class the corpus names third of its nine, and it is the one that produced a false negative reading as a real result. | vectorisation, accumulator, projection, mask, assembly, dialect, lto, pre-LTO, identical code folding, cdylib, otool, aarch64 |
| <a id="chain-two-realisations-definedness-is-profile-dependent"></a>chain_two_realisations_definedness_is_profile_dependent | For one pair of realisations over a 256-input domain, realisation A is total at `debug-assertions = off` and partial at `on`, failing on 203 of 256 inputs, while realisation B is total at both. So whether two realisations share a definedness domain is a function of the build profile rather than of the realisations alone. | The hypothesis was the control and it refused. Its author set out to show a candidate clause false at `debug-assertions = on` and the measurements do not show it: at `on` the two realisations no longer share a definedness domain, so the clause's hypothesis fails and it is vacuous rather than false. Recorded in the header as the third of that dispatch's own refuted hypotheses rather than deleted. |  |  | sound | The reason to keep this row is the axis rather than the number. `build_profile` is a declared dimension and this is the instrument showing that a claim can range over it without saying so, which is what an absent dimension means under the predicate rule. | definedness, profile, debug-assertions, partial, total, vacuous, overflow panic, realisation, build_profile |
| <a id="chain-const-channel-follows-debug-assertions"></a>chain_const_channel_follows_debug_assertions | Const evaluation does not check arithmetic unconditionally: separating optimisation level from the assertion flag, the const channel follows `debug-assertions` and not `opt-level`, and at `opt-level=3 debug-assertions=off` the narrow const compiles and evaluates to the wide arm's value. | The wide arm had to compile in every cell of the 2x2, and it reported NO in all four, which is impossible if the table is about the two arms. That is the control firing. |  | The harness rather than the instrument, and the corpus names it as the ninth defect of its unit and a new class. The first version looped over quoted flag strings in fish, which does not word-split, so `rustc` received one bogus argument and every cell reported does-not-compile. The measurement code was correct and the shell that invoked it was not. The tell is cheap and general: a 2x2 whose every cell agrees, and the control is one cell that must differ. | sound | A separate file in the same directory is named `const_channel_narrow_MUST_NOT_COMPILE.rs`, which is the refusal kept as an artifact rather than described. That is the right shape and it is rare here. | const, channel, debug-assertions, opt-level, word-split, fish, harness defect, 2x2, compile, profile |
| <a id="chain-the-third-definition-is-not-observation-bounded"></a>chain_the_third_definition_is_not_observation_bounded | Of three files whose definitions of a chain were counted as one convergence, one is not observation-bounded: it carries 2 observation-vocabulary occurrences in 7,299 words against 22 in 14,690 and 26 in 16,986 for the other two, and its defining sentence says what a chain contains rather than what bounds one. | None stated in the material read. The instrument counts vocabulary occurrences and normalises per thousand words, and the separation it reports is between 0.27 and 1.50 per thousand, which is wide. What no arm tests is whether the vocabulary count tracks the property: a file could be observation-bounded and say so once. The qualitative half, that the defining sentence says contains rather than bounds, is read rather than counted and is what carries the finding. |  |  | uncontrolled | Marked sound rather than defective on a judgement, and the judgement is that the counted half is corroboration for a claim the quoted sentences establish on their own. A reader who thinks a word-frequency count needs a control before it may be printed beside a definition should read this row as `defective`; the finding does not move either way, because the two definitions are quoted in full beside the counts. | chain, definition, observation, vocabulary, word frequency, convergence, three hats, independence, bounded |
| <a id="chain-heading-overlap-against-a-null"></a>chain_heading_overlap_against_a_null | Over 27 headings against 28, best match per heading, mean containment overlap is 0.327 against a random re-pairing null of 0.024 over 200 draws, and 7 of 27 headings reach overlap of at least 0.50. | C1 required the two known prefix pairs to score at least 0.50 under Jaccard, and it fired: the second scores 0.44, because the longer heading carries five extra tokens and a symmetric similarity penalises the longer side. So Jaccard reports a subsumption as a partial match, which is the relation the instrument existed to measure. Containment returns 1.00 on both pairs. Both metrics are kept and reported. |  |  | sound | The control did not find a bug in the code. It found that the chosen metric answers a different question from the one asked, which is the rarer and more useful kind of control failure. The null is what makes the 0.327 mean anything; without it a mean overlap is a number with no scale. | heading, overlap, jaccard, containment, null, random re-pairing, metric, convergence, shared framing |
| <a id="citation-checker-normalising-whitespace"></a>citation_checker_normalising_whitespace | A citation checker matching with `grep -F` returns false negatives on any quotation spanning a line break in the source, because the source carries a newline where the quotation carries a space. Four of fifteen quotations in one pass, all four true, all four resolved by normalising whitespace first. | Added after the defect and stated as the fix's own requirement: a fragment known to be present must match, or the checker is not reading the file. Two were used, named in the source file by document and line. |  | This row IS the defect. The instrument is the checker and the finding is about checkers, so it is filed here rather than as a repair note. The class matters more than the count: a checker that reports a true citation as missing is as dangerous as one reporting a false one as present, because the next action is to correct something that was right. | defective | No script for this run is committed under a probe directory; the account and the counts live in the member file, which is what `lives` cites. So the figures cannot be re-derived, only re-read. The two successors below are committed and are what to build on. | citation, checker, grep -F, line break, whitespace, false negative, quotation, normalisation |
| <a id="citation-checker-stripping-markup"></a>citation_checker_stripping_markup | Whitespace normalisation is necessary and not sufficient for citation checking in a markdown corpus: blockquote markers and emphasis survive it, and stripping them additionally mattered on 5 of 12 quotations in one file and 4 of 13 in another. | Both carry a planted-present and a planted-absent fragment, and the run is reported only with the first found and the second not found. That is the pair: a checker that finds the planted-absent one matches too much, and one that misses the planted-present one is not reading the file. | Two files, two authors, same tool lineage. The second adopted the first's fix and found the markup class biting in its own file at 4 of 13, which is what says the class is real in this corpus rather than one author's habit. | Three, and the third was not found by the panel. Two are in successive layers of the same tool, each found by the fix for the previous one: the first run of the whitespace-normalising version reported two of its author's own true quotations missing, one inside a blockquote where the wrap inserts a marker mid-sentence and one carrying emphasis around its numbers; the next layer was case, since a quotation taken mid-sentence with a lowercased leading capital is ordinary English and a case-sensitive checker calls it missing. The third is that both copies hardcode an absolute path to a different clone of this repository, so run from any other checkout they verify that tree rather than the one they are committed beside, and on a host where that path exists they succeed and report OK. Counted in `panel::202608072330_the-numeral-canon-panel::185_probes::p6_absolute_paths.out::1`, where twenty citation checkers in this corpus carry it. | sound | Three defects in one small tool, in three layers, each surfaced only by fixing the one above it. The general form worth carrying is that a text matcher over a markdown corpus is defeated by the markup the corpus wraps its quotations in, and every fix for that is partial until a planted-present control says otherwise. | citation, checker, markup, blockquote, emphasis, casefold, whitespace, planted control, false negative, quotation |
| <a id="the-heading-anchor-slug-rule"></a>the_heading_anchor_slug_rule | The reference resolver's heading anchor rule is not the one the reference documentation implies. Every run of non-alphanumeric characters collapses to a single hyphen and leading and trailing hyphens are dropped, so an apostrophe becomes a hyphen rather than being deleted: `## Op's instruction` is `#op-s-instruction`, and both `#op's-instruction` and `#opsinstruction` are refused. | Established by control run rather than read off a spec, which is what the emitter's own header says. Three candidate spellings of one heading were put through a lint run and two were refused, so the rule is the surviving spelling rather than a reading of the documentation. The refused spellings are the control: had all three resolved, the run would have said nothing about which rule is in force. | Reused unchanged by the next port, which is a rerun of the same code rather than an independent rebuild and is worth less. Both copies are committed. |  | sound | Eleven of one port's first citations failed and every failure contained an apostrophe, which is how the rule was found. It is not written into the generated reference documentation, which still says punctuation is dropped, and that file is generated so neither port could fix it. | anchor, slug, heading, apostrophe, citation, reference, resolver, hyphen, punctuation |
| <a id="a-measured-proposal-is-refused-without-a-probe-row"></a>a_measured_proposal_is_refused_without_a_probe_row | The committed checks refuse a `measured` proposal that names no evidence, with `measured-claim-cites-no-probe`, and refuse an `evidence` entry naming a probe row with `unknown-row-reference` reporting that the `probe` namespace has no rows at all. So no measured claim could be written into this registry until this namespace existed. | The negative arm is the one that matters and it is present: an `argument` row carrying no evidence is silent. One reported row, not three. Without that arm the two positive arms would be satisfied by a checker that reports every row. |  |  | sound | The row is kept after the blocker it names is gone, because it is the evidence for why five topics' measured claims were unwritable, and the next reader meeting that gap in the audit trail needs to get from the gap to the instrument that measured it. | measured, evidence, probe, gate, blocked, registry, check, sentence_kind, unknown-row-reference |
| <a id="the-registrys-citation-guards-fire"></a>the_registrys_citation_guards_fire | In the committed checks, a citation naming no file is reported, a heading anchor naming no heading is reported, a line citation into a living ledger is reported by `no_line_citation_into_a_living_ledger`, and a line citation into a numbered member file passes. | The fourth arm is the control and its author says so: the other three would fire on an instrument that reports everything, and only the passing arm distinguishes a checker from a shouter. | Independently rebuilt over the `lives` field rather than `provenance` by `probe_lives_citation_controls`, which adds the two arms this one does not have: what happens when the terminal segment is a bare filename, and what happens at a line past the end of a real file. |  | sound |  | citation, control, living ledger, line number, anchor, checker, guard, provenance |
| <a id="probe-lives-citation-controls"></a>probe_lives_citation_controls | A probe row's `lives` field is resolved and guarded: a path matching no file is reported as `unresolvable-provenance`, a line past the end of a real file is reported, a bare root is reported as `malformed-provenance`, a terminal segment that is a bare filename with no line or anchor is refused as malformed, a real file at a real line is silent, and a line citation into a living ledger fails `the_committed_canon_cites_no_moving_line`. | Arm B is the control: a real committed artifact at a real line must be silent, and it is. The other five arms would all fire on a checker that reports everything. |  | Two of my own, both in earlier runs and both kept in the script's header. Run one used capital letters in the planted row's id, so every arm failed on the id regex rather than on the citation, and the existence arm and the control arm produced identical output. Run two ran the ledger half of the suite after the loop had already overwritten the plant with a different arm, so eight greens were printed against material that was never a ledger citation and the arm established nothing while looking complete. | sound | The terminal-anchor arm is the one that changed how every row in this file is written. There is no citation form for a whole file, so a probe artifact is cited at line 1, which is a line number that means the file. | lives, citation, control, malformed-provenance, unresolvable-provenance, living ledger, terminal anchor, line 1 |
| <a id="rounding-is-vacuous-at-zero-fraction-width"></a>rounding_is_vacuous_at_zero_fraction_width | Over scaled-integer operands at W = 5, with the rounding mode varied over floor, ceiling, toward-zero, half-up and half-even and no overflow policy applied, the choice of mode changes no answer for addition, subtraction and multiplication at F = 0, and none for addition and subtraction at any F. Division is the residue at F = 0. | Two, both stated in the output line beside the figure they qualify, and both fired. Multiplication at F = 2, W = 5 must show mode-differing cells or the sweep cannot see a rounding difference at all: 512 of 1024, signed and unsigned. And division at F = 0 must differ between floor and toward-zero, which is the must-fail arm: 386 of 992 pairs. |  |  | sound | The overflow axis is deliberately outside this instrument: no overflow policy is applied, because the claim is about the quantisation step alone. A reader taking this as a statement about a range-limited type is taking it past what it swept. | rounding, vacuous, fraction width, F=0, quantisation, division, mode, scaled integer, exhaustive |
| <a id="rounding-commutes-with-the-overflow-policies"></a>rounding_commutes_with_the_overflow_policies | At W in {3, 4, 5} over exact points at sixteen subquanta per quantum, wrapping commutes with floor, ceiling, half-up and half-even and fails to commute with toward-zero, and saturating commutes with all five deterministic modes. | Two must-fail arms, both fired and both printed with the count. Wrapping against toward-zero had to mismatch, and does, at 240 of 625 at W = 3 rising with width. And for saturating, an artificial boundary-violating mode that maps off-grid values just under the maximum to above the range had to mismatch, and does, at 15 of 497. Without the second arm the saturating result would be a row of zeroes from a sweep that might not reach the boundary at all. |  | A first run is kept beside the corrected one as `p3_output_run1_representative_level.txt`, so the correction is recoverable rather than described. | sound | The artificial mode is the shape worth copying. Where a policy is expected to commute with everything real, the control has to be an unreal member constructed to break it, or the sweep's zero is unfalsifiable. | commutation, overflow, wrap, saturate, rounding, toward zero, boundary, must-fail control, subquanta |
| <a id="the-two-toward-zero-spellings-differ-and-by-how-much"></a>the_two_toward_zero_spellings_differ_and_by_how_much | Bit-drop and toward-zero are different operations on signed domains, and the size of the difference at W = 8 is 64 of 256 values at F = 1, 96 at F = 2, 120 at F = 4 and 127 at F = 7, with integer division over all nonzero-divisor pairs differing on 31,231 of 65,280. | A control on the neighbouring question in the same instrument: one grid for the whole chain, the setting the claim under test is stated in, where the two arms must agree. It is the case in which a difference would mean the harness is wrong rather than the claim. |  |  | sound | This instrument is also what refutes a figure that had entered the corpus. A dispatch cited `21,204 of 32,768` at W = 8 for this difference; the sweep that could produce a count of that size produces 31,231 of 65,280, and the per-fraction-width sweeps produce 64, 96, 120 and 127 of 256. The figure matches nothing this instrument can compute. It was checked here rather than repeated, which is the whole reason the row is worth keeping: the quoted number existed in no committed output. | toward zero, bit-drop, truncation, vocabulary, signed, division, 21204, fabricated figure, sweep, W=8 |
| <a id="staged-narrowing-depends-on-its-staging"></a>staged_narrowing_depends_on_its_staging | At W = 9, F = 4 with a four-to-two-to-zero staging, half-even does not carry exact staged composition: it fails first at -247/16, where the direct result is -15 and the staged result is -16. | The corrected instrument prints the first failing value per member, so a True cannot mean an empty sweep. That is the repair rather than a widening: the defect was an empty region and the fix makes emptiness visible. | Three witnesses agree on the corrected result: this instrument, one earlier probe's P4 at 500 half-even mismatches of 4001, and a signature's own s2. The agreement is what closed it, not the correction alone. | The first run swept a window that could not enter the failing path. At W = 7, F = 3 with a three-to-two-to-one staging it reported half-even carrying exact staged composition, contradicting two other instruments, because the window held no value whose intermediate stage lands on a tie. A sweep reporting a clean result over a region containing no instance of the phenomenon is the class, and it is the one an author is least likely to see, because the output is exactly what a true claim looks like. | sound | Traced by an independent check that asked whether anything downstream still rests on the defective first run, and found nothing did. | staged narrowing, half-even, composition, empty sweep, witness, tie, fixed point, W=9, F=4 |
| <a id="a-domain-widening-with-a-control-that-can-fire"></a>a_domain_widening_with_a_control_that_can_fire | Under a truncating cell coordinate, where the fractional part is the value minus its truncation, a construction claimed to hold over any ambient domain is sign-dependent at both m = 5 and m = 8, and the negative cell produces a distribution containing a negative mass. | The replacement controls fire and the output shows them firing rather than asserting they would, printing CONFIRMED for the threshold arm and a difference at k = -4 against k = +4 for the variance arm. |  | The first version's two controls were structurally incapable of firing, in a probe written to criticise a probe with no control. The threshold control reversed the subpoint list for negative k and then sorted that list by value, making the reversal a no-op. The variance control mapped f to 1 - f, and f(1 - f) is invariant under exactly that map. Both reported no difference, and both would have reported no difference under any hypothesis, at any width, forever. | sound | The honest form of the widened claim is not a bare `any` on the ambient domain but `any` given a floor-based cell coordinate. That was found by building the control the criticised probe had not built, which is the argument for controls stated as well as anything in this corpus. | domain, widening, control, invariant under the map, no-op, sign dependence, negative mass, truncating, cell coordinate |
| <a id="the-predicate-sweep-for-inherited-dimensions"></a>the_predicate_sweep_for_inherited_dimensions | Across one candidate's clauses the predicate count is eleven rather than the nine a first extraction reported, and four predicates are vacuous, which no signature had reported. | The corrected run carries its own control line in the output, that `domain` is not split at the `in` inside it, printed with `(must be True)` beside it. Independently reproduced by a later extraction that agrees with the corrected count. |  | Four failure modes in one instrument in one sitting. The dimension key pattern matched the `in` inside the word `domain`, so every domain dimension parsed as a key named `doma` and the sweep reported zero domain flags, a confident False on the exact question the probe existed to answer. The verdict block then printed True for the control because the author had written the literal rather than the variable, so the instrument reported itself sound while failing. The fixed version still missed a predicate, because the span pattern required the literal `holds for:` and one clause reads `hold for:` with a plural subject, and the missing one was the single predicate a dissent was about. And the inheritance fingerprint compared only against the immediately preceding clause, which cannot reach a clause inheriting from three above. | sound | The fourth mode is the general one: a matching heuristic applied to a question about absence is blind to absence by construction. The absence table exists because of it, and it is what found the four vacuous predicates. | predicate, extraction, delimiter in content, vacuous, inheritance, holds for, verdict prints a literal, absence |
| <a id="no-dependent-survives-the-rounding-units-defects"></a>no_dependent_survives_the_rounding_units_defects | Of the four instrument defects recorded in one unit, none has a surviving dependent claim: each was traced to what cites it and every citing clause either states the corrected result or does not reach the defective one. | None stated as a must-fail arm. What stands in its place is that the check is a trace rather than a count: each of the four is followed to the clauses that cite it and each clause is opened and read, and the companion instrument checks the surviving predicate against the sweep header the corrected run actually printed rather than against what the clause claims it swept. |  |  | uncontrolled | Marked sound on a judgement. A trace over four named defects has no obvious failing case to plant, since the population is enumerated rather than sampled and the verdict per item is a reading. What would make it defective is if the four were not the whole population, and the count of four comes from the unit's own record rather than from an independent sweep. | defect, dependent, trace, survives, half-even, corrected, citation, audit |
| <a id="packing-is-a-weighting-not-a-policy"></a>packing_is_a_weighting_not_a_policy | At W in {3, 5, 6, 7, 11} and F in {0, 2}, over bit offsets {0, 1, 7, 13, 59, 61}, a value packed at an arbitrary bit offset and the same value padded into the next native slot agree on every operation over the whole domain, with zero roundtrip failures. | A third representation packed one bit short, which must differ, and does: 119 differing operations of 192 at W = 3 and 7,364,931 of 12,582,912 at W = 11. It runs in every cell beside the two real arms, so a cell reporting zero difference between packed and padded is reporting it from a sweep shown to be able to see a difference. Control failures: 0. |  |  | sound | The swept width set is the one to carry: {3, 5, 6, 7, 11}. A later value-level intersection found this instrument and the two it was grouped with share no width at all, because the other two swept 4, so a convergence stated over all three holds at no width. | packing, weighting, policy, bit offset, padded, one bit short, container, answer-visible, roundtrip |
| <a id="the-container-is-not-observable-at-the-column"></a>the_container_is_not_observable_at_the_column | Extending an assignment tuple with a container choice over three lossless containers, packed, minimum whole-byte rung and twice that rung, leaves the joint partition at 24 classes over 30 assignments, the same count the assignments alone produce, and 90 configurations collapse to those same 24. | A fourth container storing W - 1 bits, the negative control, which must separate. It does: adding it takes the count from 24 to 48. The output says in its own words that the lossy container separates, so the sweep can see a container when there is something to see. |  |  | sound | The negative control is what makes the zero mean anything. Three containers adding zero classes is the shape a broken partition produces too, and only the fourth distinguishes them. | container, observable, partition, classes, assignment, lossy, negative control, column, packed |
| <a id="fusion-under-unsigned-over-six-rounding-modes"></a>fusion_under_unsigned_over_six_rounding_modes | On an unsigned domain, at both wrapping and saturating overflow, the six rounding modes floor, ceiling, toward-zero, away-from-zero, nearest-half-up and nearest-half-even give a zero fusion difference for the first five, and nearest-half-even does not. Separately, the shared one-sided-clamp congruence does not reach the wrapping half: under wrapping the zero comes from reduction being a ring homomorphism, and only under saturating can it come from the clamp being one-sided. | C1: the signed wrapping rows must reproduce an earlier instrument's published table, and they do, with control failures: 0. Predictions were written into the header before the first run, itemised A1 through A3, so the run could disagree with its author. |  |  | sound | Built because the candidate's unsigned row was wider than anything measured: one earlier instrument swept unsigned at toward-zero only, and another swept six modes but pinned signed wrapping. The gap was found by reading two sweeps' scopes against one clause's quantifier, which is the check no control performs. | fusion, unsigned, rounding modes, clamp, congruence, wrapping, saturating, multiply-add, ring homomorphism |
| <a id="equivariance-is-domain-restricted"></a>equivariance_is_domain_restricted | Fusion is answer-preserving where the rounding position is translation equivariant on the domain the cell actually reaches, rather than on the whole rational line. Over six modes at W = 6 under wrapping, exhaustively, the domain-restricted test mispredicts 0 of 12 cells and the unrestricted test mispredicts 2, both of them unsigned toward-zero and unsigned away-from-zero. | C1: the two tests must disagree on more than zero modes, or the refinement is empty and the instrument has distinguished nothing. They disagree on 2. Predictions were recorded in the header before the run. | Independently rebuilt in a different language by `the_false_unsigned_fusion_clause_and_its_repair`, which reproduces 0 against 2 mispredictions of 12 and reaches the same two cells. |  | sound | The finding is a quantifier rather than a value. The earlier partition of the six modes three against three by equivariance is over the whole line; under unsigned the effective partition is five against one, because on non-negative arguments toward-zero is floor and away-from-zero is ceiling. | equivariance, translation, domain restricted, fusion, rounding, quantifier, unsigned, half-even, misprediction |
| <a id="the-false-unsigned-fusion-clause-and-its-repair"></a>the_false_unsigned_fusion_clause_and_its_repair | A candidate clause claiming the whole rounding axis free under unsigned is false, and the domain-restricted repair predicts the measured table where the unrestricted form does not: 0 mispredictions against 2, over six modes, four denominators and eighty-one numerators, with zero disagreements on the arm that must agree. | A7: the signed wrapping rows must reproduce the earlier published table, which is what says this is the same experiment. Plus a must-be-zero arm over six modes, four denominators and eighty-one numerators, and two further arms A5 and A6 reported in the output. | This is itself the independent reproduction, in Python, of a result first reached in Rust. Both arms match, which is worth more than a rerun of one implementation. |  | sound | The clause it replaces was never written into `retirement`, so a reader of the registry alone can see the corrected clause and cannot see that the earlier one claiming the whole axis free was found false. That is a hole in the record and it is named here because this row is the instrument that found it. | fusion, unsigned, false clause, repair, domain restricted, reproduction, misprediction, rounding axis |
| <a id="the-convergence-intersections-are-empty-by-value"></a>the_convergence_intersections_are_empty_by_value | Intersecting four convergence rows over the values their instruments swept rather than over the dimension names they mention, three of the four have at least one dimension whose value intersection is empty: the container row on width and on rounding, the accumulator row on rounding, and the class-count row on assignments and on witness sets. | The fourth row, fusion, must come back with no empty dimension, and does. A version reporting every row empty would be reporting its own parser. |  | The instrument this one replaces intersected over dimension names, so a dimension every instance lists with disjoint values vanished from the report while looking present in every input. It was named by a signature that checked one row and said it had not checked the others; checking them found the defect reaches three of four. | sound | The predicted carrier was wrong and the author says so. At the container row the empty dimensions are not signedness, which one instrument omits entirely so the name-level check had already dropped it, but width, where the three instruments swept {3,5,6,7,11}, {4} and {4}, and rounding, where one swept truncate and another floor and toward-zero. The mechanism was diagnosed correctly by somebody who identified the wrong carrier. | intersection, convergence, dimension, values not names, empty, two experts, partition, signedness, width, rounding |
| <a id="a-free-adaptation-point-gives-a-boundary-function-family"></a>a_free_adaptation_point_gives_a_boundary_function_family | Over inputs exhaustive on 0 to 255, a stretch with a free interior adaptation point has more than one distinct boundary function on three of four chains under a round-to-8 resolution: four on one chain and three on two others. So a definite description naming the stretch's boundary function has no unique referent there. | Three, all reported with their required verdict. C-A: an identity resolution must give exactly one distinct boundary function on every chain, or the enumeration is counting placements rather than functions. C-C: more than zero chains must show a family. C-D: more than zero inputs must actually differ at the boundary, or the families differ nowhere anybody can observe, at 30 of 256. |  | The first run is kept as `clause23_v1_CONTROLS_FAILED.out` and C-A failed in it: the identity resolution reported two distinct boundary functions on one chain where it must report one. The number that run printed for the round-to-8 column is the same as the corrected run's, which is exactly why the control is the thing that decides whether to read it. | sound | The naming convention is the part to copy. A run whose control failed is kept under a filename saying so, which makes a defective run greppable rather than described in prose somewhere else. | boundary function, family, adaptation point, definite description, clause 2, clause 3, identity control, degeneracy, exhaustive |
| <a id="the-licence-family-biconditional-fails-in-one-direction"></a>the_licence_family_biconditional_fails_in_one_direction | Of the three directions a claimed biconditional decomposes into, two hold with no counterexample and one fails: a licence holding implies the boundary-function family has one member, a family above one implies the licence refuses, and a family of one does NOT imply a licence holds. The counterexample is the chain `+97 *3 +13` under round-to-8, where both the algebra licence and the range licence refuse and the family has one member. | K3: on every cell where a licence holds the family must be one, which passes over the two such cells. Without it the failing direction would be unattributable between a real asymmetry and an instrument that reports refusals everywhere. |  |  | sound | The failing direction is the one the repair does not need, which is why this is an amendment to two words rather than a refutation. The words are `exactly` and `precisely`, and the instrument exists because somebody decomposed a sentence into its three implications before believing any of them. | biconditional, licence, family, counterexample, converse, deletion, algebra licence, range licence, exactly |
| <a id="the-bench-tree-was-built-at-the-undocumented-profile"></a>the_bench_tree_was_built_at_the_undocumented_profile | The bench harness builds every variant and the bench binary with a plain release build and no profile flags, none of the variant manifests declares a profile, the bench root declares none, and no cargo config supplies one. So the whole bench directory was built at cargo's default release profile, `lto = false` and `codegen-units = 16`, rather than the fat-LTO one-codegen-unit profile the harness's own scaffolding documents. | None run, and the shape does not admit an obvious one: the finding is three greps returning zero and a fourth returning the documented values from the scaffolding, so the failing case is a profile declaration existing somewhere unlooked-at. What stands in its place is that the zero-returning greps are printed with their commands and the file paths they searched, so a reader re-runs them rather than trusting the count. |  | None in the instrument. Its own containing note carries a correction to a neighbouring claim, made an hour after first writing and left visible: it had called the variant test surface untested, in a panel whose members had run those tests ten times, and the real defect is smaller and differently shaped. | uncontrolled | The consequence for this namespace is direct. `codegen-units = 16` partitions unstably across builds, so two runs of the same unchanged variant can differ in inlining and layout, which is the contamination the per-variant isolation exists to prevent. It does not say any particular finding is wrong. It says every number in that directory was taken under a profile nobody recorded, and which findings rest on a harness artifact is a pass nobody has done. | bench, profile, lto, codegen-units, release, harness, variants, reproducibility, documented profile |
| <a id="no-bench-artifact-records-its-build-profile"></a>no_bench_artifact_records_its_build_profile | Of the meta files beside the committed bench results, none records an optimisation level, an LTO setting, a codegen-unit count or a cargo profile. Each carries cpu, os, rustc, git_commit, timestamp, counter_freq and framework, and all but one record a git commit marked dirty. | C4: a synthetic meta carrying a `profile` field is planted and the same grep must find it, or a zero over the real files is a fact about the grep. It is found. Plus C3, which plants six command lines and checks that `-O` and `opt-level` count as naming a profile while `--emit asm`, `cargo test`, `--crate-type` and `nm -g` do not. |  |  | sound | This is the mechanical half of the row above. That one establishes what profile the tree was actually built at; this one establishes that the artifacts cannot say. `build_profile` is a declared axis, and under the absence rule a result that cannot name it holds at no profile at all. The dirty-tree count is the second half of the same problem: a result whose commit is dirty cannot be tied to a tree either. | bench, meta, profile, dirty, git commit, provenance, build_profile, artifact, unrecorded |
| <a id="the-debug-release-gap-that-retired-a-true-finding"></a>the_debug_release_gap_that_retired_a_true_finding | On one host in one session, back to back, the same crate's test suite finishes in 109.08s under `cargo test` and 3.78s under `cargo test --release`, a factor of 29. | None, and none is needed for what it establishes: it is two runs of one command differing in one flag, on one host, back to back, and both numbers are printed with the command that produced them. The back-to-back-on-one-host part is what a control would otherwise have to buy. |  |  | uncontrolled | The reason this row exists is what the gap cost rather than the gap itself. A true finding was retired as unreproducible because the figure came from `cargo test`, which defaults to debug, and the three measurements refuting it were taken at `--release`. Nothing was contested and nobody was wrong; a dimension was missing from all four. This instrument is the one that priced it, and the rule that every timing carries its build profile is downstream of exactly this number. | debug, release, profile, 29x, compile time, retired, unreproducible, cargo test, predicate, missing dimension |
| <a id="six-ways-this-corpus-produces-a-meaningless-green"></a>six_ways_this_corpus_produces_a_meaningless_green | Six distinct ways a test-gate runner over the bench variant crates exits zero having examined nothing, all six observed in this corpus. Among them: `cargo test --workspace` from the bench root compiles the bench binary, runs zero tests and exits 0, because the variant crates are path dependencies rather than workspace members; a runner piping through a `timeout` binary not installed on the host produces no output and exits 0 for every crate; and a batch runner under a two-minute cap silently reports twelve of thirteen, because one crate alone takes 107 seconds. | The count itself is the control and it fired against its own author: the runner that produced the fifth green was this instrument's own first version, caught because the gate demands running the suite rather than citing it. The positive case is the per-crate count, reproduced independently and matching every per-crate figure exactly. |  | It hardcodes an absolute path to a different clone of this repository, so run from any other checkout it examines that tree. That is the seventh way, and it is not in the list of six. | sound | The population is stated as six and is not established as complete. Two were found by one member, one by the next, one by this instrument against itself, and the seventh above by a later census, so the honest reading is that the count rises whenever somebody looks. | test gate, meaningless green, exit zero, workspace, timeout, false green, variants, per-crate, bench |
| <a id="translation-equivariance-is-the-fusion-mechanism"></a>translation_equivariance_is_the_fusion_mechanism | Writing the exact rational x for a multiply scaled by 2^F and an integer addend c, the stepwise form round(x) + c and the one-round form round(x + c) agree for every x and c exactly when the rounding position is translation equivariant over the integers. Floor is, and confirms at zero differing inputs in every cell; toward-zero is not, and its failure is fully characterised by where the argument is negative and non-integral. | Two earlier mechanism guesses were refuted by this instrument's own controls, which is why there are three versions and all three are kept. The final one does not guess: it derives the equivalence condition and then checks the derivation, so the arms are a proof and its verification rather than two hypotheses raced against each other. |  |  | sound | The lineage is the point and is why all three files are cited rather than only the last. A reader who takes only `p3c` gets the answer and not the two refutations that produced it, and the refutations are what say the answer was derived rather than landed on. | equivariance, translation, fusion, multiply-add, stepwise, one-round, floor, toward zero, mechanism, refuted guess |
| <a id="the-width-invariance-control-was-toothless"></a>the_width_invariance_control_was_toothless | Nothing usable about width invariance. A sweep over three axes, rounding, overflow and intermediate placement, reports the observability count unchanged between widths, and its sensitivity control at a degenerate width of 2 fails to move it: 0 of 31 subsets disagree between W = 2 and W = 3. | The control came out toothless and its author says so in the successor's own docstring: a check reporting that the count does not move with width, which cannot be made to move with width at all, is indistinguishable from a check blind to width. What was missing is an axis position whose observability genuinely depends on the integer width, and the three swept axes do not have one. |  |  | defective | Marked `defective` rather than `sound` on the author's own reading, which downgraded the result from confirmed to unestablished. The zero is not wrong, it is unearned, and the honest residue is narrower: if there is a finding here it is a finding about those three axes rather than about width. The row exists so nobody cites the zero. | width, invariance, toothless control, unearned zero, unestablished, sensitivity, degenerate, blind, observability |
| <a id="a-partial-interior-operation-distinguishes-at-every-profile"></a>a_partial_interior_operation_distinguishes_at_every_profile | Two realisations of one stretch that agree on final values wherever both are defined are distinguishable at `opt-level = 3, debug-assertions = off` when an interior intermediate is the divisor of a division: on a wrapped zero the narrow realisation panics and the wide one returns a value. Division by zero is not governed by `debug-assertions`, so the channel is open at both profiles rather than only at `on`. | Declared in the header before the run: a control arm of the same shape with the divisor an input rather than an interior intermediate, whose two realisations must agree on every swept input at `off` with no panic on either side. Over 200,000 inputs it gives 0 disagreements and 0 panics, so the instrument detects the interior partiality rather than something about the shape. |  |  | sound | The build flags are in the header rather than only in the runner, which is what lets this row state the profile. It is one of the few instruments in the corpus that treats `debug-assertions` as an axis to sweep rather than as an ambient condition. | definedness, partial, division by zero, interior, intermediate, debug-assertions, profile, distinguishing channel, realisation |
| <a id="an-equivalence-checker-that-skips-panics-certifies-a-definedness-difference"></a>an_equivalence_checker_that_skips_panics_certifies_a_definedness_difference | An equivalence checker that skips inputs where one side panics certifies pairs differing in definedness rather than in value, and in this corpus 200,000 random inputs found zero occurrences of an event whose probability is about 2^-32. | None. It is a structural argument about what `catch_unwind` around a comparison does, plus one arithmetic observation about how many samples a 2^-32 event needs, and neither has a failing case to plant. |  |  | uncontrolled | Recorded as a row because it is the class rather than a result, and because it is the second half of a pair with the row above: one shows the channel exists, this one shows why a random sweep will not find it. The general form is that a sweep's zero on a rare-event channel is a claim about the sweep, and the repair is a definedness column rather than more samples. | equivalence, definedness, panic, catch_unwind, rare event, sampling, 2^-32, zero, trap, value-only |
| <a id="operation-erasure-holds-at-every-optimisation-level"></a>operation_erasure_holds_at_every_optimisation_level | For one typed body against its native equivalent, the opcode multiset is identical at all six optimisation levels 0, 1, 2, 3, s and z, at three instructions from level 1 upward and fourteen at level 0. Symbol identity reports not-erased at levels 0 and 1 and erased from 2 upward. | The two oracles disagree with each other across the sweep, which is the arm: symbol folding is an identical-code-folding pass that does not run below `-O2`, so a certification resting on symbol identity alone answers a question about the build setting. Running both across all six levels is what separates the instrument's failure from the design's. |  |  | sound | The result is in the design's favour and nobody had it: erasure survives more build settings than the certification claimed, including level zero. What fails below `-O2` is the oracle. This is the corpus's cleanest treatment of `build_profile` as an axis, and it is the one to copy: sweep the level rather than pick one and hope. | erasure, opcode, symbol folding, identical code folding, opt-level, oracle, certification, profile, sweep, assembly |
| <a id="the-axis-census-over-the-governing-files"></a>the_axis_census_over_the_governing_files | Across twelve governing files there are 64 predicate spans naming 115 distinct keys, of which 19 map onto a declared axis and 96 do not, leaving 5 spans writable as a registry predicate and 59 blocked. | C2: `radix`, a four-letter key present in fourteen spans, must appear in the extracted key list. It fired twice before passing. |  | Two, both in the span boundary and both caught by C2. Version one cut each span at the end of the flattened file rather than at the end of its paragraph, so every key list came back full of swallowed sentences and `radix` was absent; the run is kept as `axis_census_first_attempt.out`. Version two split on semicolons only, and one topic writes its predicates comma-separated, so eleven spans became one phrase each and `radix` was still missing. Fixed with an awk splitter that cuts on either at brace depth zero. Its author records that four extractors in this panel have now been defeated by the same boundary. | sound | One classifier line is wrong and its author names it rather than adjusting the total: the key `S` is mapped to strategy, which is right in two topics and wrong in one, where `S = [0,255]` is the representable set. It moves no total because that span was already blocked on five other phrases, and the stated consequence is that the per-span reason lists are not exhaustive. | axis, census, predicate, span, dimension, blocked, radix, boundary, extractor, semicolon, comma |
| <a id="the-ranking-of-what-a-missing-axis-blocks"></a>the_ranking_of_what_a_missing_axis_blocks | Grouping the undeclared predicate keys by the axis family each is a spelling of, ten families account for the blocked spans, and the largest are the term and declaration shape at 27, the ambient domain at 26, the cost-model population at 25 and the stochastic-coupling parameters at 19. | U2, and the first version could not fail. It summed a column inside a pipeline, so the loop ran in a subshell and the total came back zero whatever the data said; it printed PASS against any input. The total is now computed from a file the loop writes, and the negative control is kept as its own artifact: the arm fed an impossible total, failing. |  |  | sound | The subshell class is the one worth carrying out of this row. A shell loop feeding a counter inside a pipeline loses the counter, the arm reports the initial value, and the initial value is usually the one that passes. | ranking, unblock, axis family, ambient domain, subshell, pipeline, counter, negative control, blocked spans |
| <a id="the-inventory-of-clauses-that-could-not-be-written"></a>the_inventory_of_clauses_that_could_not_be_written | Across five topics' 65 statement clauses, 24 could not be written because their region names an axis no dimension row declares, 16 because the source states no region and the kind is not normative, 23 were written, one was refused by a signature and one folded into another row. | A deliberately broken table, kept as its own artifact, on which the checker must report two problems where it reports zero on the real one. Every cited line is opened and confirmed to be a clause opening rather than prose, and every slug named is confirmed to exist in the registry. |  | The wrong-line arm could not fail in the first version. It accepted any heading followed by a digit and a period as a clause opening, which matches the file's own title, since a title numbered 161 contains the string `1.`, so the control cited a prose line and passed. It now requires a blockquote item, a heading or an emphasis run, and the control fires. | sound | The two counts this instrument produces and the census's 64 are different sets rather than nearly the same one, and its author says so: the census counts predicate spans, of which two topics contribute none, and the inventory counts statement clauses, of which the same two contribute thirty. They were both 64 before one clause was split, which is the coincidence that nearly fused them in prose. | inventory, blocked, clause, no-axis, no-region, refused, checker, control table, clause opening, port |
| <a id="the-droplist-entries-are-verbatim"></a>the_droplist_entries_are_verbatim | The entries transcribed into the droplist register reproduce their sources word for word rather than in paraphrase. | Recorded in a control-runs file of its own rather than inline, which is why this row can state what fired. One arm used a `sed` range substitution that BSD sed does not support: it changed nothing, and the run reported success. Caught because the control's required outcome was written down first and the run did not produce it. |  | The BSD sed arm above, and it is the class worth carrying: a GNU extension used on a host whose sed does not have it mutates nothing, exits zero, and a checker downstream then compares two identical files and reports agreement. Both the mutation and the check succeed, and neither did anything. | sound |  | verbatim, droplist, transcription, sed, BSD, GNU extension, mutation, silent no-op, control run |
| <a id="the-pareto-arm-that-rests-on-one-size-point"></a>the_pareto_arm_that_rests_on_one_size_point | Nothing about a Pareto-optimal arm, and that is the result. One arm reported as unreachable by any linear weighting is dominated the moment the largest size point is dropped, and at that point its margin is 3.8 nanoseconds against a within-arm interquartile range of 79.2 nanoseconds on the arm it has to beat, in the same run. | The successor is the control on its predecessor, and it fired against its own author: an intermediate version found the verdict rests entirely on the largest size point, and this one prices the margin against the harness's own scatter. The comparison of a claimed effect to the instrument's noise on the same arm in the same run is the arm every one of these should carry. |  |  | withdrawn | Its own header says NOT A BENCH RUN and that it reads committed harness CSVs and does exact arithmetic, which is the correct labelling. The withdrawal is the useful output: a finding one twentieth the size of the measurement scatter is not a finding, and the gate that should have caught it was at the wrong granularity. | pareto, withdrawn, noise, interquartile, size point, margin, scatter, linear weighting, granularity, harness csv |
| <a id="the-tail-coordinate-is-the-wrong-statistic"></a>the_tail_coordinate_is_the_wrong_statistic | Three separate negative findings about one cost coordinate were all measured on one estimator of it, the interquartile range of the per-batch samples, so they are findings about that estimator rather than about the coordinate. | The construction is the control: a different estimator of the same intent is built and run over the same data, so the two disagree or they do not, and a disagreement locates the problem in the estimator rather than in the coordinate. |  |  | sound | The class is worth more than the result. Three independent negative findings that share an estimator are one finding, in the same way that three probes sharing a model are one instance wearing three hats, and nothing in a count of three says which. | estimator, tail, coordinate, interquartile, independence, three findings one instrument, cost model, noise |
| <a id="a-mutant-the-encoding-must-reject"></a>a_mutant_the_encoding_must_reject | Whether a product-of-axes structure closed under a join is expressible on the pinned nightly without `generic_const_exprs`, as a trait with an associated type rather than a const expression in type position. | The file is the control. It is a mutant carrying a deliberately wrong join, committed beside the correct encoding so the check is that the compiler separates them rather than that the correct one builds. A probe establishing only that its intended shape compiles has not established that a wrong shape does not. |  |  | sound | A mutant kept as its own committed file is the shape a refusal should take, and it is rare in this corpus. Most compile checks here record that the intended construction built and nothing about what the compiler would have refused. | mutant, join, product of axes, generic_const_exprs, associated type, compile, refusal, pinned nightly, expressible |
| <a id="double-rounding-disagreement-has-a-closed-form"></a>double_rounding_disagreement_has_a_closed_form | The disagreement count for a double rounding across an intermediate width is a formula in the fraction width and the intermediate width, rather than a result read off a sweep of three widths. | The swept widths are the control on the closed form: a formula that does not reproduce the counts the earlier sweep measured at each of its widths is the wrong formula, and the sweep is what it has to agree with before it may replace it. |  |  | sound | Built for a reason worth stating: the earlier result was a proof recorded with a measured predicate, which under-claims in a notation where a predicate is never widened in place. A closed form turns the sweep into a theorem and lets the widening be stated rather than hedged to the widths tested. | double rounding, closed form, intermediate width, fraction width, predicate, widening, theorem, sweep, disagreement count |
| <a id="every-placement-rebuilt-in-the-other-language"></a>every_placement_rebuilt_in_the_other_language | The pointwise-optimality claim over every interior placement, re-run on an instrument written independently in Rust rather than read off the Python that first widened it, together with a matched non-nearest partner for the clamp row: wrap projects onto the same 256 in-range values and is not nearest. | The matched pair is the control the clamp row previously lacked: a projection onto the identical value set that is not nearest, so a clamp result cannot be attributed to the value set rather than to nearestness. | This IS the reproduction, and its author says why it is a rewrite rather than a reading: agreement between a Rust instrument and a Python one is two instruments, and agreement reached by reading the other's code is one. |  | sound | Worth more than the numbers it reports. Two implementations in two languages by two authors agreeing is the strongest form of corroboration in this corpus, and it appears here and in the fusion repair and almost nowhere else. | placement, reproduction, rust, python, independent, clamp, matched control, nearest, pointwise optimal |
| <a id="translation-equivariance-partitions-the-rounding-axis"></a>translation_equivariance_partitions_the_rounding_axis | Translation equivariance partitions the six rounding positions rather than distinguishing the two that were tested, and the partition is the arm's predicate. | The derivation is written out in the header from scratch before the measurement, so the sweep either matches the derivation or refutes it. The companion instrument is a second accumulator model built independently, which is the control on the first one's model rather than on its arithmetic. |  |  | sound | Its author states the rung it does not earn, which is the part to keep: the mechanism was re-derived after reading the earlier file's prose claim but before reading its probe code, so this is a re-derivation and a re-measurement rather than a blind instance. A later value-level intersection then found this partition stated over the whole rational line while an arm needs it on the domain the cell reaches, which is a quantifier the partition alone does not carry. | equivariance, partition, rounding axis, six modes, re-derivation, rung, accumulator, independent model, predicate |
| <a id="absorption-and-associativity-agree-for-clamped-addition"></a>absorption_and_associativity_agree_for_clamped_addition | Over 4248 configurations and again over a widened set, absorption and associativity of the induced operation agree exactly for clamped addition: zero cases absorbing but not associative, and zero associative but not absorbing. On a neighbouring configuration set the necessity direction shows 153 violations, and those are separately established to be degenerate. | Both directions are counted separately and both are printed with the sentence they would refute, in the output: sufficiency violations refute absorbing-implies-associative and necessity violations refute the converse. The 153 in the second configuration set is the arm firing, which is what says a zero elsewhere is a measurement rather than a checker that cannot report. |  |  | sound | The two directions are what makes this usable as a criterion rather than as a correlation, and the 153 are the reason a follow-up instrument exists. A reader taking the zeros without the 153 has taken the half that agrees. | absorption, associativity, clamped, reduction, sufficiency, necessity, criterion, degenerate, 4248 |
| <a id="the-accumulator-width-is-the-exact-sum-width-less-one-bit"></a>the_accumulator_width_is_the_exact_sum_width_less_one_bit | For an n-operand fold of a W-bit signed saturating format, the minimum sufficient accumulator width is the exact-sum width less one bit, at every fold length measured: 4, 5, 5, 6 against a predicted 5, 6, 6, 7. The final adaptation absorbs the outermost bit, because the accumulator has only to decide which side of the format the result fell on rather than represent how far outside it fell. | A row with only one verdict cannot locate a minimum and the output excludes it by name, which is the arm that stops a width being reported from a sweep that never saw it fail. The predecessor is the other half: its prediction was sound at every n and wrong by exactly one bit at every n, and a sound-but-loose bound was attacked rather than reported. |  | The predecessor's first run overstated its reading and is kept as `p5_output.v1_overstated_reading.txt`. | sound | One bit per accumulator decides container selection exactly at the boundaries where container selection is interesting, which is why the looseness was worth chasing rather than rounding up. | accumulator, width, fold, saturating, exact sum, one bit, grading, adaptation, container selection |
| <a id="which-factor-of-a-reduction-breaks-the-semiring"></a>which_factor_of_a_reduction_breaks_the_semiring | The reduction at nonzero fraction width is a composite of a grid coarsening and a range clamping, and running each factor alone attributes the semiring failure to a named map rather than to the composite. | The two coherence arms are each other's control and the output says so: a coherent reduction is a homomorphism so reducing at every step must agree, an incoherent one must not, and the run reports the same checker on the same code path giving opposite verdicts at the same fold length. A checker returning one verdict for both would be measuring the fold rather than the coherence. |  | The first run failed its own assertion and is kept as `p4_output.v1_failed_assertion.txt` rather than deleted. | sound | Its author states the stake before running: if the clamp alone is a semiring and the coarsening alone is not, then saying saturation breaks distributivity is false in a way that matters, because it sends a law layer to condition on the wrong axis of the format. That is a probe built to change a design decision rather than to confirm a number. | semiring, distributivity, clamp, coarsening, attribution, factor, coherence, homomorphism, fraction width |
| <a id="a-half-step-biased-grid-is-not-closed-under-addition"></a>a_half_step_biased_grid_is_not_closed_under_addition | On a half-step-biased grid, 0 of 256 exact sums of two grid points land on the grid and every one sits exactly half a step away, and the grid contains neither zero nor one. Round-to-nearest onto the same biased grid keeps every adaptation law tested, exhaustively: total, retraction, monotone and distance minimising. | The affine membership predicate is written once and instantiated at two phases, and each instantiation is compared exhaustively against a direct enumeration that never mentions the predicate. That is the arm: a predicate agreeing with an enumeration written independently of it cannot be agreeing with itself. |  |  | sound | The zero is the load-bearing half and the tie is what makes it actionable: every sum lands exactly on a tie, so phase promotes the tie rule from a corner case to the common case rather than merely shifting the grid. | phase, bias, grid, closure, addition, identity, tie, affine predicate, retraction, adaptation law |
| <a id="a-declared-law-marker-compiles-clean-when-false"></a>a_declared_law_marker_compiles_clean_when_false | Over a four-bit signed window with two overflow policies declaring one associativity marker, one declaration is false, the compiler raises nothing, and the consumer licensed by the marker reassociates and produces a wrong answer silently. | The companion arm is the control and it is a separate file: the same law computed rather than declared refuses the false case. A declaration that compiles and a computation that refuses, over one model, is what separates the finding from a claim about that particular window. |  | The first run is kept with a note naming two defects in it. The wrapping map was not wrapping addition: it added the two offset-binary representations rather than the values, so zero was not the identity under it. And the fold was seeded with a literal zero, so the measurement was partly of whether zero was the map's identity rather than of grouping. The corrected fold starts from the first element and measures grouping alone. | sound | The header states the toolchain and the exact build line, which is rare here and is what lets this row carry a profile at all. It is the law-layer analogue of an earlier probe that found the same shape one coordinate lower, where an overstated declaration about a container window passed the whole validation suite because nothing tied the declaration to the executable maps. | declared law, marker, false declaration, reassociation, silent, overflow policy, paper checking paper, compiles clean |
| <a id="a-trajectory-condition-lifts-exactly-when-it-survives-closure"></a>a_trajectory_condition_lifts_exactly_when_it_survives_closure | Whether a measured trajectory region can become a declaration is decided per region by closure: the condition lifts over the operand set exactly when it survives the set's closure under the operations the law ranges over. | The degeneracy arm is the control and it is a separate probe: if every lifted region were degenerate the criterion would be sorting nothing, and the run exists to find out. A third arm asks whether the interval is the limit of what lifts, which bounds the criterion from the other side. |  |  | sound | The criterion is the deliverable rather than any one region's verdict. A reader wanting a list of which regions lift is reading the wrong instrument; this one says what decides it. | lifting, trajectory, declaration, closure, operand set, box, region, degenerate, criterion |
| <a id="the-collapsed-declaration-cannot-be-made-to-fail"></a>the_collapsed_declaration_cannot_be_made_to_fail | A system that names its own computed algebra as its ambient domain passes a reduction-verdict exposure test, and the pass survives mutation: a mutation set including a reduction that adapts every value to zero cannot make the collapsed verdict fail. | A positive arm that this row's first wording failed to name, and which is what makes the zeros mean anything: the mutation set is shown to be detectable, breaking the HONEST declaration's verdicts in 3 of the 4 cases, so the collapsed column's zeros are a fact about the collapsed declaration rather than about a checker that notices nothing. One of the four, `honest saturate`, is labelled the control in the source. Its header is also the clearest statement of the principle in this corpus: a row that cannot fail is not a measurement, and the way to show it cannot fail is to corrupt what it is nominally about and watch it not notice. The predecessor reported zero coherence and zero monotonicity failures for every collapsed declaration, and this one says that number is worth nothing until somebody tries to make it nonzero. |  |  | sound | The finding is about a test rather than about a numeral. It refutes the sufficient direction of a standing admission test, which is the useful shape: the test was passed by something that computes nothing, so passing it says nothing. | collapse, mutation, cannot fail, exposure test, ambient domain, self-declaration, admission, sufficiency, refuted |
| <a id="the-rationalisable-sections-on-the-committed-carrier-table"></a>the_rationalisable_sections_on_the_committed_carrier_table | On the committed carrier-width table, of 15625 sections 9 are rationalisable by a non-negative weighting at zero tolerance, rising to 134 at one percent. So a section and an argmin under a weighting are different objects and the gap between the two definitions is a number rather than an argument. | The tolerance sweep is the arm: a count that does not move as the tolerance widens would be measuring the parser rather than the table, and it moves from 9 to 134 at one percent. The successor asks separately whether the gap is structural rather than an artifact of the particular table. |  |  | sound | Built because two files had been recorded as reaching one proposition by two experts when they state different propositions, one that a strategy is a named section and one that it is a preference whose argmin produces that section. This instrument is what turns that into a countable difference. | rationalisable, section, argmin, weighting, carrier table, 15625, tolerance, two experts, strategy |
| <a id="one-congruence-property-predicts-every-law-verdict"></a>one_congruence_property_predicts_every_law_verdict | Where the realisation map respects an operation, the representable set with its induced operations is a quotient of the exact structure, so every identity of exact arithmetic transfers at every arity. Measured against the per-law verdict tables the panel had been collecting one law at a time: zero soundness mismatches and zero conservative mismatches. | The two mismatch directions are counted apart, soundness against conservative, so a predictor that simply says yes everywhere shows up as conservative mismatches rather than as a clean sheet. And the companion arm derives a closed-form failure rate for one cell before measuring it, `1 - 2^(1-W)`, and prints predicted against measured per width. A prediction written before the sweep is the strongest control shape in this corpus and it appears twice. |  |  | sound | The companion also removes a cell from evidence rather than adding one: wrapping subtraction failing to associate is integer arithmetic, since `(a - b) - c` and `a - (b - c)` differ by `2c` in the integers, so the cell says nothing about the policy and cannot support the independence claim it was offered for. | congruence, quotient, law verdict, predictor, identity, transfer, closed form, subtraction, arity, 552 |
| <a id="no-conservatism-order-exists-on-the-overflow-axis"></a>no_conservatism_order_exists_on_the_overflow_axis | Two candidate orders on the overflow axis, both computed exhaustively for signed and unsigned side by side, disagree. Under the law order wrapping and saturating are incomparable, so a join over it does not exist; under the fidelity order they are ranked, and that order itself splits into two metrics which disagree and tie wrapping against saturating. | Computing both candidate orders rather than one is the arm. A single order computed alone comes back with an answer whichever it is, and only the disagreement between two independently motivated candidates shows that the phrase naming the order does not name one. |  |  | sound | The finding is that a documented behaviour is under-specified rather than wrong: resolving toward the more conservative side is a phrase that does not say which order is meant, and the two available readings disagree on the pair the documentation uses as its own example. | conservatism, order, join, overflow, wrap, saturate, incomparable, fidelity, resolve, underspecified |
| <a id="the-demand-closure-is-generated-rather-than-enumerated"></a>the_demand_closure_is_generated_rather_than_enumerated | The closure priced as a cost is the free join-semilattice on its own generators, so it is generated rather than enumerated and costs no names, against a prior report that four markers carrying one demand each leave twelve of sixteen ordered pairs unresolvable and that the smallest closed set has fifteen elements. | A mutant with a demand dropped, committed as its own file, which the construction must reject. A generated lattice that accepts a mutant is generating something other than the closure, and the mutant is the only thing that separates the two. |  |  | sound | The second mutant in this corpus kept as a separate committed file rather than described. The other is in the strategy-set topic and both are in the same shape: the correct construction beside the wrong one, so the check is that the machinery separates them. | demand, closure, join-semilattice, generated, enumerated, mutant, markers, resolution, free |
| <a id="most-committed-bench-regions-predate-the-validation-gate"></a>most_committed_bench_regions_predate_the_validation_gate | Of 254 committed bench regions, 175 were produced before the harness driver called cross-variant validation at all and 79 after. Of the 79, all are answer-pinning routines and none was unresolvable. | The preceding arm is what makes the join worth computing: a committed region taking the harness default would be refused by that comparison at all four of its sizes on all one hundred of the harness's own validation seeds, so the comparison cannot have been running when that region was produced. The join then dates every region against the wiring commit and turns one region's impossibility into a count. | Redone independently by a second author who resolved each meta file's commit against the wiring commit's time rather than accepting the first result, because the first author had named it as the finding it most wanted re-derived. |  | sound | This is the row every claim about the bench corpus's validation should cite, and it composes with the profile rows already here: the regions were built at an undocumented profile and most of them predate the validation gate as well. | bench, regions, cross-variant, validation, gate, wiring, 175, 254, prewiring, harness, join |
| <a id="a-compile-time-strategy-selection-leaves-no-residue"></a>a_compile_time_strategy_selection_leaves_no_residue | A strategy resolved at compile time leaves no residue in the emitted body, on four instruments: a const-fn argmin over a cost table against the hand-written arm at nine instructions each and identical after label normalisation; entry bodies carrying a single tail branch with zero conditionals; and both encodings compiled and compared. | The case that had to fail is `197_probes/c1`, which supplies the form the original four lack: the identical argmin over a weighting that arrives as an argument, so nothing can fold it. It comes out the other way and by a wide margin. The const form lowers to nine instructions, identical to the hand-written arm after label normalisation, with its two branches being the loop rather than the selection; the runtime form lowers to seventy-six carrying eleven branches and all three arms. Label normalisation before comparison is the arm that stops two identical bodies reading as different, and the emitted assembly is committed beside every source so the comparison is redone rather than believed. |  |  | sound | **The standing was `uncontrolled` and the control was built rather than argued for.** What each of the original four arms does is compile two forms and compare the emitted bodies, and a comparison of two bodies has no natural failing case unless somebody supplies a form that must differ. None of the four did, so the finding was corroborated across three authors and never controlled, which is agreement rather than measurement. The missing arm was named in this row's own note, in one sentence, and building it was an hour: same cost model, same table, same three arms, same argmin, and the weighting's binding time as the only thing that moves. | compile time, residue, argmin, const fn, emitted body, assembly, label normalisation, tail branch, selection |
| <a id="the-axes-are-not-independently-resolvable"></a>the_axes_are_not_independently_resolvable | The overflow policy changes which container a time objective prefers. Over widths 8, 16, 32, 60 and 64 the container ranking under wrapping and under saturating differ at every width, so the best value on one axis depends on the value taken on another and a resolver may not settle them one at a time. | The two policies are run over the identical container set and the identical widths, so a ranking that came out the same would be the case that refutes the claim, and the output prints both rankings per width side by side rather than reporting a verdict. A reader disagreeing can read the orderings off the run. |  |  | sound | The distinction the instrument was built for is the useful part and it is stated in its header: independently stateable is a claim about what the cells say and was already established; independently resolvable is a claim about whether the answers interact, and nobody had tested it. Two axes can be separate questions and still not be separately answerable. | axes, independence, resolvable, stateable, overflow, container, ranking, time objective, resolver |
| <a id="the-design-closed-forms-are-not-the-tightest-numeral"></a>the_design_closed_forms_are_not_the_tightest_numeral | The design's natural closed forms are not the least numeral containing the exact result: multiplication is tight on 1099 of 1296 cases, addition on 1175 of 1296 and the subtraction candidate on 751 of 1296. The mechanism of the overshoot is then located rather than left as a count. | The three operations are counted against one another over the same 1296 cases, so a count identical across all three would say the enumeration is not reading the operation. They differ, 1099 against 1175 against 751. The successor is the arm that matters more: its own header says the formula overshoots identifies a question rather than answering one, and it goes after the mechanism. |  |  | sound | Its assembly capture `06_probes/p3_asm.out` is committed at zero bytes and is therefore uncitable: a probe citation carries a terminal line number and a zero-byte file has no line one. Measured in `panel::202608072330_the-numeral-canon-panel::192_probes::p3_empty_artifacts.out::1`. The tightness fractions are the citable part and the mechanism is what a design would act on. A reader taking only the fractions has the finding and not the reason, and the reason is what says whether a tighter formula exists. | tightness, closed form, least numeral, overshoot, 1296, multiplication, addition, subtraction, extremum, mechanism |
| <a id="closing-the-family-under-intersection-is-priced-and-does-not-reach-tapered-formats"></a>closing_the_family_under_intersection_is_priced_and_does_not_reach_tapered_formats | The join of a fixed-point numeral and a float numeral has two incomparable minimal upper bounds and no least one, and no admission of more fixed-point shapes repairs it. Closing the family under intersection costs a 16 to 34 percent enlargement, every added shape a segmented grid neither family names, and the closure does not reach tapered formats. | The later instrument asks the same question one level up, at the general concept rather than at the two named families, which is the arm that decides whether the failure is about those families or about the construction. A closure result that held at one level and not the other would locate it; the run is what says which. |  |  | sound | Two instruments in two files by two authors, and the second was built to attack the first's scope rather than to confirm its number. The percentage band is a price rather than a verdict, and the tapered-format gap is the part a design has to answer. | closure, intersection, moore family, join, incomparable, least upper bound, tapered, segmented grid, price, 16 to 34 |
| <a id="a-single-richer-output-suffices-only-as-a-type"></a>a_single_richer_output_suffices_only_as_a_type | A single richer output from a container derivation suffices if and only if it is an associated type. Eleven files on the pinned nightly with zero feature gates: six compile and five are refused, and the value spelling is refused in three syntactic positions, naming the forbidden feature each time. The native rung partition and the access rung partition of widths 1 to 128 share no jump point, so one ladder cannot key both. | Five negative controls, each a separate committed file whose refusal is committed as a `.err` beside it, and the runner reports each refusal's error count rather than only that it refused. Two arms are sharper than a refusal: one asserts three false type equalities to show a bridge is not vacuous, and one carries two must-refuse claims together with a must-not-refuse claim, where the absence of the third from the error list is stated as half the result. The toolchain is pinned in the runner and the gate-free property is checked by the runner rather than asserted. |  |  | sound | The best-documented probe directory in this corpus: a table of every file with its expected outcome and error count, a runner that rebuilds the lot, and a stated cross-check that names its own weakness, that reproducing a figure from a transcribed closed form is one instance of agreement between two codings rather than two independent derivations. | single output, associated type, value spelling, refused, negative control, compile-fail, generic_const_exprs, ladder, access width, eleven files |
| <a id="narrowing-composes-where-the-modes-direction-switches-at-coarser-grid-points"></a>narrowing_composes_where_the_modes_direction_switches_at_coarser_grid_points | Narrowing composes exactly when the mode's direction switches only at coarser-grid points, tested through two consequences of an adjunction rather than through its defining biconditional: the forced adjoint is unique and equals the maximum below the value, and the composition law holds on a grid triple exactly where the failure count is zero. | The method is the control and its header says so: the predecessor tested the defining biconditional pointwise, and this one tests two consequences by a route that does not use that biconditional at all, so a mode passing one and failing the other is a real disagreement between two methods rather than a repetition. The three outcomes are separated in the output, zero failures with the adjoint equal to the embedding, zero with a different adjoint, and nonzero, so a zero cannot be read as the wrong one of the three. |  |  | sound | Two methods that would agree trivially are one method. This is the clearest case in the corpus of an author paying for independence by choosing a harder route, and it is worth copying wherever a second instrument is being built to confirm a first. | narrowing, composition, adjunction, forced adjoint, biconditional, grid, rounding mode, independence, method, embedding |
| <a id="three-of-the-four-proposed-axes-change-no-answer"></a>three_of_the_four_proposed_axes_change_no_answer | Of four proposed strategy axes, run against the polarity test that an observable coordinate is one where moving it changes the value computed, one changes the answer and three do not. On the corrected instrument the overflow policy separates and headroom, packing and intermediate precision do not. | The overflow axis is the arm that must separate and it does, so a zero on the other three is a zero from a sweep shown able to report a difference. Without a separating axis in the same run, three zeroes and a fourth would be indistinguishable from a comparator that never fires. |  | The first version is kept under a filename naming its own failure kind, `p2_first_version_setup_that_helped`, which is the test gate's own vocabulary for feeding an implementation only the inputs it handles. Its figures are on disk beside the corrected ones. | sound | The test it applies was stated by a different file and had never been run against this axis list, which is the gap worth naming: a polarity criterion existed and the list it was supposed to sort had not been put through it. | axes, observable, polarity, headroom, packing, overflow, intermediate precision, setup that helped, answer-visible |
| <a id="the-const-eval-frontier-by-arity"></a>the_const_eval_frontier_by_arity | The widest exhaustively const-evaluable width falls with arity: 19 at arity 1, 9 at arity 2, 5 at arity 3, 3 at arity 5 and 1 at arity 8, every refusal being the long-running-const-eval limit rather than a width ceiling. | The refusal is read for which limit it hit rather than counted, which is what distinguishes a total-step budget from a width ceiling and is the whole content of the finding. A frontier measured by counting failures alone would report the same numbers and support the wrong conclusion. |  | The first run is kept in full, script, json and output, under `BROKEN_FIRST_RUN` names with a note beside it. | sound | The frontier is a fact about the evaluation procedure rather than about any law, which is a distinction the corpus later writes into its own claims. A width in this table is a budget observed, and one earlier file reported the same wall as a nuisance in a parenthetical while it was the thing being measured. | const eval, frontier, arity, width, budget, step limit, exhaustive, E0080, collapse |
| <a id="generating-a-winner-table-from-a-stated-weighting"></a>generating_a_winner_table_from_a_stated_weighting | Generating a winner table from a stated weighting removes every defect a human writing the table can introduce and admits one a human cannot, so the check is relocated rather than removed. | The stability arm is the prerequisite and is a separate instrument: a table not stable enough to be an object cannot be generated from anything, so the generation result is conditional on it and the two are cited together. |  |  | sound | The claim is a trade rather than a win, and the row says so because the sentence it supports does. A reader looking for whether generation is better than hand-writing will not find it here; what is here is which defect class each admits. | generation, winner table, weighting, relocated, defect class, stability, robust summary, tooling |
| <a id="fusion-is-an-axis-position-rather-than-a-new-axis"></a>fusion_is_an_axis_position_rather_than_a_new_axis | The fused and the stepwise multiply-add are two positions on the rounding axis rather than two operations, and at signed saturating they are two distinct realisations whose difference the axis already names. The slack an earlier reading attributed to fusion buys nothing. | The first arm reproduces the predecessor's published table before anything is built on it, which is what says the later arms are about the same object. The slack arm is a refutation of its own author's earlier reading rather than a confirmation. |  |  | sound | Cited by the claim that where fusion changes the answer it is not a lowering. The reproduction-first shape appears three times in this corpus and it is the cheapest way to make a disagreement attributable: without it, a later instrument disagreeing with an earlier one is unattributable between the widening and a different setup. | fusion, axis position, multiply-add, slack, signed saturating, lowering, reproduction, policy classes, realisation |
| <a id="chain-accuracy-needs-an-intermediate-wider-than-the-operand-type"></a>chain_accuracy_needs_an_intermediate_wider_than_the_operand_type | An operator closed over its operand type cannot serve chain accuracy: the intermediate width required grows linearly in chain length, measured at width 8 with fraction width 4 and independently at width 16 with fraction width 8, with the growth rate reported in both. | None was run as a case that had to fail. The two instruments are at different widths and fraction widths by different authors, so what stands in place of a control is that the growth rate agrees across a change of both, which a single-width sweep could not have shown. What neither arm carries is a chain shape on which the width must NOT grow, and without one the linear growth is measured rather than isolated. |  |  | uncontrolled | Marked sound on a judgement and it is one of the two thinnest sound rows here. The agreement across two widths is real corroboration and it is not a control: both instruments could share a modelling assumption about what an intermediate must hold, and nothing in either would report that. | chain, accuracy, intermediate width, linear growth, operand type, closed, exactness, demands, closure |
| <a id="the-four-const-available-constructions-bind-at-four-times"></a>the_four_const_available_constructions_bind_at_four_times | Const-available is not one moment. Four constructions bind at four different times, and the lattice among them is computed rather than asserted. | Every rung compiles a licensed declaration beside the offending one, so the case that had to fail is present four times over and fires four times: refused at type check where the bound is structural, accepted at type check and refused at codegen where the const sits in a non-generic function, accepted even at codegen while the generic is unreached and refused the moment it is reached, and refused at type check where the const is attached to no function at all. An instrument that could not discriminate would accept or refuse both arms of a rung, and none of the four does. |  |  | sound | **The row cited the wrong instrument and the standing followed from that.** It named `p6`, whose subject is the lattice of which operand subsets license an arm, and the finding here is about when a refusal fires. The instrument is `p8`, the binding-time ladder, and it carries paired arms at every rung, which is why the standing is sound rather than uncontrolled. Re-run here and reproduces its committed output exactly, on the pinned nightly. What the ladder does not establish, and this is the objection worth keeping: four constructions binding at four times is a fact about those four, and nothing here would have reported a fifth binding time had one existed. | const available, binding time, lattice, four constructions, compile time, dead code, structural bound, crate-level const |
| <a id="the-cost-coordinate-census-and-its-calibration-control"></a>the_cost_coordinate_census_and_its_calibration_control | What cost coordinates this repository actually carries, counted rather than assumed: three had been used across three members, being the median of one timing series, a bytes-per-element figure typed into a probe by hand, and the interquartile range of the same timing samples the first is a median of. | A calibration control run before everything else in the directory, and it is the best-designed control in this corpus. The bench declares pairs of arms that compile to the same machine code, and every reading in the directory treats a measured difference between such a pair as the instrument rather than the arms. That reading is only as good as the pair actually being byte-identical, so the arm checks it in every family that declares one, before any measurement is taken. A pre-normalisation run is kept beside it. |  |  | sound | Two coordinates of the three are functions of one sample series, which is the finding a census produces and an argument does not: the corpus believed it had three coordinates and two of them are a median and a spread of the same numbers. | cost coordinate, census, calibration, noise floor, control pair, byte-identical, interquartile, median, adrp |
| <a id="the-argmin-mechanism-has-never-run-on-arms-that-disagree"></a>the_argmin_mechanism_has_never_run_on_arms_that_disagree | That the argmin-over-a-cost-table mechanism has never been run on arms that disagree. Its first arm reported that every committed family's arm set is answer-equivalent, and **that half is refuted**; see `defect`. What survives is the second: the accuracy intents range over arms that disagree, and the mechanism has not been run on that region. The refutation does not reach it, because arms that disagree existing is what makes the region real rather than what removes it. | The first arm establishes the premise the second one needs, that the committed families are answer-equivalent, so the absence is a measured property of the corpus rather than an impression. Neither arm carries a planted case that had to fail, and the second is a construction over a region rather than a sweep. |  | Its first arm's proposition was refuted. `Every committed family's arm set is answer-equivalent` was claimed in bold by the file this row's first citation comes from, and a second expert dispatched to verify it built its own instrument before opening it and refuted it with three independently sufficient measurements, recorded in `the_committed_corpus_does_contain_answer_differing_arms`. The mechanical cause is named there too: a census whose unit is the shared crate directory cannot see a region whose bridge is declared inline in the driver's own table. **So this row may not be cited for the answer-equivalence premise.** What survives is the second arm's finding, that the mechanism has never been run on arms that disagree, which the refutation strengthens rather than weakens: the arms exist and the mechanism still has not met them. | uncontrolled | The barrier is the absent coordinate rather than the arm sets, which is the part a design acts on: an accuracy intent needs a coordinate that moves when the answer moves, and the corpus's coordinates are all cost at a fixed answer. Composes with the census row above, which is what says how many coordinates there are and what they are made of. | argmin, answer-equivalent, accuracy intent, coordinate, absent, cost table, arms disagree, region never run |
| <a id="the-multiplicative-rescale-saving-is-adaptation-fusion"></a>the_multiplicative_rescale_saving_is_adaptation_fusion | The multiplicative fold's guard saves exactly one rescale, at the exact-sum width less the fraction width and constant in fold length, and the saving is adaptation fusion in the schedule rather than a property of the guard: at that width every per-step narrowing except the last is exact and the last is adjacent to the final narrowing, so the two compose into one. The saving therefore exists under truncation, which composes, and shrinks or vanishes under round-to-nearest-even, which does not. | The rounding split is the arm and it was registered as a prediction before the run: truncation composes so the saving must survive, nearest does not compose so it must shrink or vanish, and a saving present under both would mean the mechanism is not fusion. A separate result on narrowing composition predicts the same split from the other side, which is two routes to one prediction rather than one. | Re-run by the consolidation that cites it, committed as its own artifact under `63_probes/rerun/`, which is a rerun of the same code rather than an independent rebuild and is worth less than the second author's accumulator instrument beside it. |  | sound | Its header states what it did not sweep and why: an earlier file asked for fold lengths five and six before trusting the pattern, five is swept here and six is not, on cost, and the omission is written down rather than left to be discovered. That is the shape a bounded sweep should take. | multiplicative, guard, rescale, adaptation fusion, fold length, truncation, nearest even, double rounding, schedule, linear |
| <a id="a-coherent-reduction-diverges-nowhere-at-any-fold-length"></a>a_coherent_reduction_diverges_nowhere_at_any_fold_length | For coherent reduction policies, reducing at every step agrees with reducing once at the end at zero divergences for every fold length from two to six, while the incoherent policy diverges and gets worse with length: signed saturation is at zero for two operands and 476 divergences at three. So the split is not an artifact of one fold length. | The incoherent policy is the arm that must diverge and it does, in the same run on the same code path at the same fold length. The output states the requirement before the table: a coherent reduction is a homomorphism so reducing at every step must agree, and an incoherent one must not. A run reporting zero for both would be measuring the fold rather than the coherence. |  |  | sound | Written as its own row because the neighbouring row in this directory, `the_accumulator_width_is_the_exact_sum_width_less_one_bit`, describes the incoherent arm: `57_probes/p5_output.txt:72` says in its own words that its format is the one both `q1` and `p4` measure as the incoherent policy. One directory, two policies, two arms, two rows. An edge pointed at the other row would have cited a measurement of the opposite case. | coherent, reduction, homomorphism, divergence, fold length, accumulator, eager, saturation, wrap, split |
| <a id="the-rationalisability-counts-reproduced-and-rungged"></a>the_rationalisability_counts_reproduced_and_rungged | On a six-region, five-arm, two-coordinate model over the committed carrier table, 72 of 15625 sections are rationalisable by a non-negative weighting and 9 by a strictly positive one. The two figures sit at different rungs, and 63 of the 72 select an arm the predecessor's own section 10 says no weighting can select, so the two sentences in that file are about different notions of weighting rather than one of them being wrong. | The reproduction is the arm and it is run first: the model is rebuilt and must return the predecessor's published 72 and 9 before anything is read off it, which is what says the disagreement located afterwards is about the notion of weighting rather than about a different model. A rebuild returning different counts would have made every later line unattributable. | A third instance exists and agrees on both counts, in `the_cost_coordinate_census_and_its_calibration_control`'s directory, whose own calibration section reproduces 72 and 9 on the same model before measuring anything new. |  | sound | Not the same measurement as `the_rationalisable_sections_on_the_committed_carrier_table`, which is the confusion that declined an edge. That row is a different sweep over a different model reporting 9 at zero tolerance and 134 at one percent; this one is the 72-and-9 pair on the five-arm two-coordinate model. Both are real and neither substitutes for the other. | rationalisable, 72, 9, 15625, reproduction, rung, weighting, non-negative, strictly positive, carrier table |
| <a id="how-many-strategies-a-coordinate-set-can-distinguish"></a>how_many_strategies_a_coordinate_set_can_distinguish | The number of distinguishable sections a coordinate set supports, measured as a function of the set rather than of the arms: on the committed carrier table at strictly positive weights, two coordinates reach 9 and three reach 42, and adding a sixth arm to the two-coordinate model leaves both counts unchanged at 72 and 9. So the ceiling is a property of the coordinate set. | A calibration section that runs before the new measurement and must reproduce the two published counts, 72 at non-negative and 9 at strictly positive, on the predecessor's own model. It fired: the first version returned 9 where 72 was published, and that run is kept as its own file. The ceiling numbers below it are readable only because the calibration above them agrees. |  | The first version conflated two knobs and its calibration caught it, returning 9 for the non-negative count against a published 72. Kept rather than deleted. | sound | Written as its own row because the neighbouring row in this directory, `the_cost_coordinate_census_and_its_calibration_control`, counts which coordinates the repository carries. This one counts what a set of them can distinguish. Same directory, adjacent file, different question, and an edge pointed at the census would have cited an inventory in place of a ceiling. | ceiling, coordinate set, distinguishable, sections, 42, 9, 72, calibration, strictly positive, strategies |
| <a id="the-committed-corpus-does-contain-answer-differing-arms"></a>the_committed_corpus_does_contain_answer_differing_arms | The committed corpus does contain answer-differing arms, on three independently sufficient measurements. Two committed arms of one family denote different values on 1989 of 2048 lanes at one committed size. The same two arms, given the identical pair of reals so that the exact sum they must round is the same integer, answer identically on 92,556 of 200,000 trials and differently on the rest. And over the right unit, one committed CSV per bench name and size, 234 of 254 regions are pinned to one value by an exact-value oracle, 10 only to a property, 4 have no oracle at all and 6 have no bridge. | The comparison path carries no float anywhere in either arithmetic arm, exact rationals over `i128`, which is the case that had to be excluded: a difference measured through a float comparison would be a fact about the comparison. And the third measurement joins three separately committed things, the CSVs, the declared sizes and the driver's own bridge table, so a region the census cannot see shows up as a join failure rather than as an absence. | Three measurements of one proposition by one author, each sufficient alone and each by a different route: a per-lane denotation comparison, a same-input trial sweep, and a census over a different unit. Independence is across method rather than across author, which is worth less than two authors and more than one method run twice. |  | sound | Its author built these before opening the file it refutes, which is what the claim resting on it records. The census's own header names the mechanical cause of the refuted count: a region's answer discipline is decided by the bridge the driver registers, one region's bridge is declared inline in that table with no shared crate anywhere, and a census whose unit is the shared crate directory cannot see it or its flag. | answer-differing, arms, refutation, region census, oracle, bridge, 234, 254, exact rational, denotation, radix |
| <a id="a-widening-operation-cannot-be-folded"></a>a_widening_operation_cannot_be_folded | A fold cannot take a widening operation. Four independent formulations of a widening fold over a runtime-length slice are refused by rustc, each because the accumulator's type after one step is not its type before. The wall is the runtime trip count: the same widening operation folded over a static-length list compiles, as does a closed operation over a runtime-length slice. | Four positive arms, and all four compile clean in the same run: a widening operation in a fixed-arity expression, the same over a static-length list, a closed operation in a runtime-length loop, and a closed operation into a separately named wider accumulator. They are what locates the boundary. Without them the result reads as widening never working, and the actual wall, the trip count rather than the widening, would be invisible. |  |  | sound | A compiled refusal rather than a count, so it carries no magnitude and needs none. The four refused arms report one diagnosis between them, which is what says they are one wall met four ways rather than four separate defects. | fold, widening, closed operation, accumulator, trip count, loop-carried, compiled refusal, capacity, E0308 |
| <a id="which-sign-and-policy-cells-survive-reassociation"></a>which_sign_and_policy_cells_survive_reassociation | Of the four sign-and-policy cells of a bounded integer addition, three give the same answer however the reduction is split and one does not. Signed saturating disagrees on 28,336 of 65,536 vectors at width 4 over 4 operands, and on 11,760,675 of 16,777,216 at width 3 over 8 operands. Unsigned wrapping, unsigned saturating and signed wrapping disagree on none, at either shape. | Every vector is folded four ways on the same code path, a strict left fold, a balanced tree, two lanes and four lanes, and the instrument counts the vectors on which the four do not agree. Three cells report zero and the fourth reports a majority, so the same counter comes out both ways; one reporting zero everywhere would be measuring the harness rather than the arithmetic. |  |  | sound | A fifth row in the same output samples `f32` at 554,034 of 1,000,000 trials. It is a sample rather than a sweep and a different ambient domain, and it is not part of what this row establishes. | reassociation, split, reduction order, associativity, signed saturating, lanes, threads, 70.1, 43.2, tree fold |
| <a id="the-top-absorbs-under-saturation-and-never-under-wrapping"></a>the_top_absorbs_under_saturation_and_never_under_wrapping | Over 63 unsigned width-and-fraction cells, the top of the range absorbs addition at every cell under saturation and at no cell under wrapping. Separately and on the same sweep, the multiplicative identity is not representable at the 9 cells where the fraction width fills the container, and a product fold seeded with the nearest representable value is wrong at every non-zero element there. | Both directions are counted per cell and printed side by side, so the same column reports 0 failures of 63 under one policy and 63 of 63 under the other. The identity column is a second arm moving independently of the first: it goes false at exactly the nine cells where the fraction width equals the total width and stays true everywhere else, which is what says the sweep is reading the cell rather than the policy. |  |  | sound | Unsigned throughout; the model is `u128` and there is no signed arm. So this says nothing about a signed top, which is a different question and one a min-plus consumer would also need answered. | absorbing top, absorption, saturation, wrapping, min-plus, tropical, identity, seed, fold seed, 63, infinity |
| <a id="a-dag-dynamic-programme-returns-wrong-answers-on-in-range-instances"></a>a_dag_dynamic_programme_returns_wrong_answers_on_in_range_instances | Two DAG dynamic programmes run over the numeral and compared against exact unbounded arithmetic, exhaustively over every four-node DAG respecting one topological order and every weight assignment. Under wrapping the min-plus shortest path disagrees with the exact answer on 5,414,255 of 11,932,446 in-range instances at width 3 and on 407,293,133 of 832,398,764 at width 4. Under saturation it disagrees on none. The max-plus longest path agrees under both policies at both widths. | An instance is counted only where the exact answer and every exact intermediate fit inside the numeral's range, so a disagreement cannot be range exhaustion reported as a law failure. Three of the four routine-and-policy cells report zero wrong answers, including the longest-path routine under the same wrapping policy the shortest path fails under, which is what says the failure is the operation's and not the harness's. |  |  | sound | The topological order is an input to both routines rather than something either computes. This measures what a numeral does to an algorithm given an ordering; it establishes nothing about producing one. | DAG, shortest path, longest path, min-plus, max-plus, dynamic programme, in-range control, wrapping, end to end, algorithm layer |
| <a id="no-derivation-reads-the-numerals-grid"></a>no_derivation_reads_the_numerals_grid | The carrier, the stride and the fold accumulator derive to literally the same types across two numerals differing in adjustment, bias, phase and canonical exponent, and a law bound is satisfied or refused by the strategy alone at every grid. Type equality is asserted through a blanket implementation, so a mismatch is a compile error rather than a runtime comparison. | Three negative arms, each of which must fail to build and each of which does, with its diagnostic recorded. A value map that genuinely does read the grid fails its equality assertion; a wrapping strategy fails the absorbing-top bound; and a deliberately mismatched pair fails the sameness bound, which is what says the sameness relation is not vacuous. The main arm builds at exit 0 with no feature gate anywhere. |  |  | sound | The outputs live in `RUN.md` rather than in a `.out` file beside the source, because the result of a compile arm is its diagnostic. Both are committed. | grid, adjustment, bias, phase, canonical exponent, binding time, derivation, type equality, negative control, composition, dynamic coordinate |
