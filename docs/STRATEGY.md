<!--
  AUTO-GENERATED: DO NOT EDIT DIRECTLY

  Generated by: mockspace (mock)
  Generated at: 2026-09-02T09:02:25Z
  Source: mock/
  To regenerate: cargo mock
-->

# Strategies

A behaviour a consumer selects. The set is open: what is here is what has been named, not what the design ships.

4 rows. Identifiers are permanent: assigned once, never reused, never renumbered.

| id | name | intent | weighs | standing | note | keywords |
|---|---|---|---|---|---|---|
| <a id="hot"></a>hot | Hot | Performance and efficiency, at the cost of accuracy or soundness where the loss buys something. Soundness may be sacrificed and that is its explicit purpose, but never for nothing: the gain has to be provable and meaningful. | Speed above accuracy and above footprint. | prior_attempt | The bound is the whole of it. `Hot may sacrifice soundness` on its own is the half that reads as a licence; the sentence op said carries `but it should not lose it for nothing, instead, provable meaningful gains` in the same breath. | Hot, fast, fastest, speed, performance, efficiency, unsound, soundness sacrifice, hot path |
| <a id="cold"></a>cold | Cold | Cold paths and cold storage. It aggressively minimises and bitpacks, and because the path is cold it has leeway to do things that are not efficient. The value is sitting rather than working. | Footprint above speed. | prior_attempt | Two bounds, both op's. The leeway is not an obligation: Cold may use the same paths Hot uses, and nothing in its intent fights that, but a path that fights the intent is not for Cold. And the intent survives the set being reshaped, resized or renamed, in his words, whether the strategies are four or seventeen or a billion. | Cold, small, smallest, storage, bitpack, packed, footprint, cold path, disk, memory |
| <a id="precise"></a>precise | Precise | The most precise answer available, sacrificing as much performance and efficiency as makes sense to get it. Accuracy across chains and ops rather than per operation alone. | Accuracy above everything, and across a chain rather than at one step. | prior_attempt | The `especially within chains and ops, not only alone` clause is the load-bearing half and the one the panel keeps returning to: a primitive is a property of a value and a chain is not a value, so an intent stated over chains may not be expressible over a per-value primitive at all. Op's own wording also carries a second unresolved reading, whether `chains and ops` is two things or one phrase, and that is his to settle. | Precise, precision, accurate, accuracy, chain accuracy, exact, error, fidelity |
| <a id="warm"></a>warm | Warm | The intuitive best choice for most every use case. Behaving as a native Rust primitive would serves that objective, and is dropped where mimicking is consistently the worse choice. | The compromise, and which measurements it weighs is exactly what nobody has established. | prior_attempt | The imitation is about ergonomics and nothing else, settled by op in one word when the panel asked whether it targets the declared width or the container: `Neither, it's ergonomics`. So it says nothing about where arithmetic boundaries land, and the two-way fork the panel built under it, measured to disagree at all fourteen non-native widths swept, is about something the intent does not range over. He has restated the imitation call four times over two days because it kept failing to stick. | Warm, intuitive, default, compromise, native, primitive, imitation, ergonomics, unsurprising |
