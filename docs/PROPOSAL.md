<!--
  AUTO-GENERATED: DO NOT EDIT DIRECTLY

  Generated by: mockspace (mock)
  Generated at: 2026-08-31T02:45:26Z
  Source: mock/
  To regenerate: cargo mock
-->

# Proposals

A claim the panel established and op has not seen. It is canon only once a ruling stamps it.

102 rows. Identifiers are permanent: assigned once, never reused, never renumbered.


### the_primitive

| id | kind | sentence_kind | standing | topic | says | because | instead | obligation | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="a-primitive-is-a-value-set-with-one-realisation-map"></a>a_primitive_is_a_value_set_with_one_realisation_map | answer | normative | one_expert | [the_primitive](TOPIC.md#the-primitive) | A primitive is a value set together with one realisation map taking an exact result back into it, over a declared operation set. Its identity is that structure up to denotation-preserving isomorphism, relative to the declared operation set. Of the three sameness relations, nominal, representational and denotational, each licensing a different operation, only the denotational one is a congruence under composition, which is why it and only it licenses substitution inside a composite. | The composed clause survived attack at both ends: the packed end does not touch it, because it is a statement about denotation while the compile refusal that would refute it is a statement about representability. Types are the degenerate case of lenses, which is the synthesis the two cold derivations could not reach and which arrived in the reply round, and it gives one vocabulary across the whole declared range without the cost the single-vocabulary option carried. |  |  | The composed wording is one expert's and the parts underneath it have stronger provenance of their own. The identity half is conditional on a decision that is not the panel's: under one branch of the container premise the isomorphism quotients the carrier and this clause's extension is the coarser partition, and under the other it must preserve the carrier and the extension refines, measured at 32 primitives against 64 on the swept grid. The wording is parametric in the operation set, so its truth value is stable and only its extension moves.  `normative` because the sentence is a definition. It is stipulated rather than established, so it carries no region, and the kind enumeration has no separate value for a definition.  | Which branch of the container premise holds is not the panel's to choose, and the extension of this clause moves with it. | primitive, value set, realisation map, identity, denotation, isomorphism, sameness, nominal, representational, denotational, congruence, substitution, composite |
| <a id="the-realisation-map-is-one-map-with-two-regions"></a>the_realisation_map_is_one_map_with_two_regions | answer | normative | three_or_more | [the_primitive](TOPIC.md#the-primitive) | The realisation map is one map with two regions: rounding between grid points, and completion outside the representable range. They are never two mechanisms. | A magnitude bound switches off the completion and not the rounding, and a grid bound does the reverse, which is what two regions of one map predict and what two independent axes do not. |  |  | Three derivations, one of them blind, plus a fourth separately built instrument on the licence side, which is why this carries a stronger standing than the clause it sits inside.  `normative` because it is definitional. None of the three derivations states a region, and the claim is about the map's structure rather than about a numeral's behaviour at a width.  |  | realisation map, two regions, rounding, completion, range, grid, one map, not two mechanisms, magnitude bound, grid bound |
| <a id="an-axis-the-realisation-map-does-not-read-is-not-a-type-parameter"></a>an_axis_the_realisation_map_does_not_read_is_not_a_type_parameter | refusal | normative | one_expert | [the_primitive](TOPIC.md#the-primitive) | An axis the realisation map does not read must not be a type parameter. The cost of two names for one primitive is a property of where the spellings meet: nothing at a monomorphic site, one threaded parameter at a polymorphic signature, and no repair at a homogeneous container, which is why a spurious parameter's whole cost lands on the storage path the design protects. | The three-armed cost is measured rather than argued, and the armless arm is the one the storage discipline exists for. An axis that changes no answer and selects no arm buys nothing anywhere and costs a threaded parameter wherever two spellings meet. | An axis the arm selection reads may be a type parameter, because weakening repairs it and weakening is free. |  | The panel records this as CONVERGED. Demoted here to one expert on the test the schema states, which is that each party derived before reading the other: the two later contributions sharpen the first after reading it, one correcting the scope and one compiling the spurious-against-refinement asymmetry in both directions. Both are real instruments and neither is an independent arrival.  Carried as its own row because the clause it comes from is refused as written. Under one branch of the container premise the clause's first sentence is false and no wording is true on both branches until that is ruled on, so the first sentence is not in this file at all. The second and third parts are untouched by the premise and are signed.  |  | type parameter, spurious axis, parameterisation, weakening, arm selection, missed merge, two names, monomorphic, polymorphic, homogeneous container, storage path |
| <a id="the-lens-degenerates-to-an-ordinary-value-at-sole-occupancy"></a>the_lens_degenerates_to_an_ordinary_value_at_sole_occupancy | answer | normative | one_expert | [the_primitive](TOPIC.md#the-primitive) | The realisation is a lens: a placement of the value's bits, a carrier, an offset and a width, within a carrier allocation. It degenerates to an ordinary value exactly where its focus is the sole logical occupant of its carrier allocation, with padding permitted and sharing not. At a degenerate point the language supplies a standalone type and the native end never mentions the lens; everywhere else the primitive is reached through its carrier and no size-bounded contract ranges over it. | Sole occupancy is correct in three failure directions where the positional condition it replaces is wrong in all three: under-strict on sharing at offset zero, over-strict on padding, and over-strict on sole occupancy at a nonzero offset. A thirteen-bit value at offset three of a sixteen-bit carrier is two bytes, sized, referenceable, and round-trips over all 8192 values; the positional condition rejects it and sole occupancy accepts it. |  |  | The third failure direction is the one that decides it, and it was found by a separately built instrument extending the first. Standing stays at one expert because the second party built its instrument after reading the first rather than deriving independently.  What the unrepaired condition would have cost is recorded rather than inferred: the canon would have classified the first element of every packed column as an ordinary value.  Whether a placement has a standalone name is a property of the target's addressing rather than of the primitive, so this states the reason and no arity. The lens's form is invariant under the container decision; only identity's granularity moves with it.  `normative` because it is a definition with a criterion, stipulated rather than measured. The three failure directions are measured and belong to the criterion's justification rather than to a region the definition holds in.  |  | lens, realisation, placement, carrier, offset, width, sole occupancy, padding, sharing, degenerate, standalone type, Sized, packed column |
| <a id="configuration-is-not-composition-and-a-composite-is-a-primitive"></a>configuration_is_not_composition_and_a_composite_is_a_primitive | answer | normative | two_experts | [the_primitive](TOPIC.md#the-primitive) | Configuration is not composition. Choosing a format, a system or a strategy fills in a record; composition is a construction taking an algebra to an algebra; and a composite is a primitive under the same definition, so one concept serves and every contract written for a primitive applies to a composite unchanged. A construction carries two things of its own, a predicate on its base and a transformer for its base's refinements, and equality transports through a construction for free where a predicate never does. | Borrowing the base's rule for a construction's predicate is unsound where borrowing it for equality is not, which is what makes the two halves different obligations rather than one. The distinction and the closure were reached blind twice from different instrument sets. |  | [composition_contracts_above_the_numeral](OBLIGATION.md#composition-contracts-above-the-numeral) | Two instances on the distinction and on the closure, the second a blind arrival in a later sitting from a different instrument set, classified by the attacking member as a genuine convergence with the shared bench corpus named. Any particular categorical frame offered beyond the distinction stays at one expert and is not in this row.  `normative` because the sentence stipulates what the words mean and what follows from that. The unsoundness of borrowing is measured and sits underneath as the reason.  |  | configuration, composition, construction, composite, algebra, one concept, transformer, predicate on the base, equality transports, refinement |
| <a id="membership-in-the-type-and-identity-are-two-criteria"></a>membership_in_the_type_and_identity_are_two_criteria | answer | normative | two_experts | [the_primitive](TOPIC.md#the-primitive) | Membership in the type and identity of the primitive are two criteria answering two questions, and a design needs both. What must be const-available decides membership; what is preserved up to denotation-preserving isomorphism decides identity, and neither answers the other's question. | An axis can be const-available and carry no denotational content, and an axis can be identity-bearing and be decidable at runtime, so a design collapsing the two either puts something in the type that nothing reads or leaves out something that changes the answer. |  |  | Split from the const-availability row so the stronger standing is visible. Both members reached the membership-and-identity split in their second phase and stated it independently, which is what the panel records as converged; the entailment about compulsion in the neighbouring row is the part that is one instance, and folding the two together would have made this inherit the weaker rung.  `normative` because it is a criterion the design is told to apply, stipulated rather than measured.  |  | membership, identity, two criteria, type, const-available, denotation, isomorphism, collapse, axis |

### algebraic_laws

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | law | instead | answers | obligation | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="a-law-is-read-off-the-algebra-and-never-declared"></a>a_law_is_read_off_the_algebra_and_never_declared | refusal | normative | one_expert | [algebraic_laws](TOPIC.md#algebraic-laws) | A law is read off the algebra and never declared. A declared law is a claim nothing constrains: it compiles while false, and an optimisation gated on it substitutes wrong answers along a name. | Closure is prior to every law. An operation that leaves its value set is not yet the function the law would be about, so a law asserted about it asserts nothing, and nothing in the type system is in a position to refuse the assertion. |  |  |  | Read as a demand rather than as an assertion, a law is a predicate over the configuration space: a surface a consumer may query, and never a field a consumer may set. | [how_the_law_inventory_is_named](QUESTION.md#how-the-law-inventory-is-named) |  | `normative` because it is a prohibition on the design rather than a measured claim. A region on it would say the design may declare laws everywhere unmeasured. |  | law, declared law, read off, algebra, demand, predicate, configuration space, closure, query, gated optimisation |
| <a id="absorption-decides-associativity-of-a-clamped-reduction"></a>absorption_decides_associativity_of_a_clamped_reduction | answer | measured | two_experts | [algebraic_laws](TOPIC.md#algebraic-laws) | Whether a reduction's induced operation is associative is decided by absorption, quantified over the values the format can hold: reducing a reachable exact value before combining it with a stored operand must not change the adapted result. For clamped addition this is exactly associativity; for clamped multiplication, exactly modulo operations the clamp has collapsed to a constant. Stated over ambient values beyond the format it is a different and stronger predicate, and the stronger form is the one that gets real folds wrong. | A reduction is absorbing over an operand set when reducing before combining agrees with reducing after, for every reachable exact value and every operand. For clamped addition, absorption and associativity of the induced operation agree with zero sufficiency and zero necessity violations over 4248 configurations and again over a widened 7744-configuration sweep. For clamped multiplication the exceptions, 153 on the first sweep and 150 on the widened one, are all constant-collapsed, residue zero. The quantification bound was measured after being assumed: absorption and the coherence law are the identical predicate exactly when the operand box is a subset of the representable set, and off it coherence is the one that gets associativity wrong, never absorption. The restriction is not a caveat on the law but the statement of the law's subject, because every stored value of a numeral type is already an element of the representable set. | total_width: W in 3..=7, fraction_width: F = 0, signedness: signedness in {unsigned, signed}, overflow_policy: overflow policy in {saturate, clamp}, operation: operations {add, mul}, arity: arity = 3, container: interval numerals containing zero, 100 of them | [absorption_and_associativity_agree_for_clamped_addition](PROBE.md#absorption-and-associativity-agree-for-clamped-addition) | [additive_associativity_under_saturation](LAW.md#additive-associativity-under-saturation), [multiplicative_associativity_under_saturation](LAW.md#multiplicative-associativity-under-saturation) |  |  |  | The instruments are `57_probes/p2`, `p2b`, `p8` and `p1`, `61_probes/q1`, all committed in the panel and all re-run byte-identically by the consolidator. The width entry is the cube's own width instances, which the counts section names as the format-shaped part of the sweep; the raw 4248 and 7744 figures count clamp-and-operand-box combinations, most of which are no format at all, and a reader quoting 4248 as a count of formats checked would be wrong. The sign-confinement closed form is a corollary and never the criterion: the opposite-bound mutant is sign-confined with a reachable ceiling, the closed form predicts associativity, and it diverges 2240 times while absorption predicts the failure. | The pullback mechanism underneath has exactly one genuinely independent arrival, a blind phase-one witness at `i16` built with no knowledge the mechanism existed. Everything else about the mechanism is one author's, kept by a second as the dynamic description. | absorption, criterion, associativity, clamp, biconditional, 4248, coherence, quantifier, C5 |
| <a id="the-laws-of-a-format-are-derived-from-two-hypotheses-rather-than-enumerated-per-policy"></a>the_laws_of_a_format_are_derived_from_two_hypotheses_rather_than_enumerated_per_policy | answer | theorem | one_expert | [algebraic_laws](TOPIC.md#algebraic-laws) | The laws of a format's operations are derived, not enumerated per policy. The induced operation is associative when the ambient operation is associative on the reachable set and the reduction's kernel is a congruence for it. The congruence half is decided by the range's geometry per operation, mirror symmetry for multiplication and sign confinement for addition, and is repairable by choosing the representable set. The ambient half fails wherever a fixed-width rescale is part of the operation itself, for every policy, and nothing downstream repairs it. | Both hypotheses hold and the induced operation on the representable set is associative, because that set is the quotient. Evaluated mechanically over the unit's cube: zero sufficiency violations, zero cells associative without both hypotheses, zero residue, with each hypothesis and the measured truth observed both true and false across the cube. Read through it the unit's table rows become derivations rather than observations: addition's ambient is always associative so additive verdicts are decided by the congruence half alone and are scale-blind; wrapping's kernel is always a ring congruence so wrapping is decided by the ambient half alone; saturation's kernel is a congruence only under the stated geometry; and no multiplicative cell survives a nonzero fraction width anywhere. The closed forms are exact: the kernel is a multiplicative congruence iff the range is mirror-symmetric or nonnegative, and an additive congruence iff the range is sign-confined, with the predictor mismatching nothing over all 100 zero-containing intervals. | total_width: W = 4, fraction_width: F in {0, one unnamed value above 0}, signedness: three sign domains, unsigned and signed two's complement and signed symmetric range, overflow_policy: overflow policy in {wrap, saturate}, operation: operations {add, mul} |  | [multiplicative_associativity_under_saturation](LAW.md#multiplicative-associativity-under-saturation), [additive_associativity_under_saturation](LAW.md#additive-associativity-under-saturation) |  |  |  | The consolidation marks its equivalence **provisionally passes** and says so in load-bearing terms rather than as a hedge: the twenty-four-cell prediction with zero residue is an equivalence trial passed, but the frame itself is one expert and unattacked, and nobody has tried to break it the way two earlier claims in the same unit were broken. It names this as the thing it most wants attacked next. It was subsequently attacked from the number-system topic, from a direction it was not built to face, and it held, which the attacker states as a failed refutation rather than a proof. The fraction entry names the cube's two columns and one of them is genuinely unnamed: the source says two scales and never says which nonzero one. The sign entry conflates the range's geometry into the signedness axis because no `dimension` row declares the geometry, and the geometry is exactly what the congruence half turns on. | The frame is stated over associativity. Whether it derives the other laws the design cares about is not established, and nobody has tried. | law frame, H1, H2, congruence, quotient, derived, cube, provisional, C6 |
| <a id="no-multiplicative-structure-survives-a-nonzero-fraction-width"></a>no_multiplicative_structure_survives_a_nonzero_fraction_width | finding | theorem | three_or_more | [algebraic_laws](TOPIC.md#algebraic-laws) | No multiplicative structure survives a nonzero fraction width for any policy, sign domain, range or rescale spelling, because the rescale destroys ambient associativity before any reduction acts. | At a nonzero fraction width a representable-width multiply discards F bits at every pairwise step before any clamp is consulted, so the failure is in the ambient operation and no choice of reduction reaches it. The rounding rule moves failure magnitudes and never their existence, measured on two separate rounding axes: truncation against round-half-up moves counts in both directions with no row reaching zero, and truncation against arithmetic shift on signed operands has the same shape. The only escape, never narrowing until the end, abandons fixed width at a cost linear in fold length. Three instruments converge with a structural argument: nine of nine fractional configurations fail, wrapping's ring collapses identically at nine of nine, and 54 of 54 signed rows fail under both spellings. | total_width: W in 3..=7, fraction_width: F > 0, signedness: signedness in {unsigned, signed}, overflow_policy: overflow policy in {wrap, saturate}, rounding: rounding any, operation: operation = mul |  | [multiplicative_associativity_under_wrapping](LAW.md#multiplicative-associativity-under-wrapping), [multiplicative_associativity_under_saturation](LAW.md#multiplicative-associativity-under-saturation) |  |  |  | `rounding any` is the source's own and rests on the structural argument that the rounding rule cannot reach the mechanism, not on a sweep over modes; two modes were measured on each of two axes. The attribution was corrected twice before it stabilised: the first reading blamed grid coarsening and reported the clamp factor clean, the second showed that no `F > 0` clamp ablation can exist because coarsening **is** what `F > 0` multiplication is, and the third showed the clamp-is-clean converse is unsigned-only, so signed multiplication above zero fraction has two independent sufficient mechanisms that converge at deep fraction where the two policies' failure sets coincide exactly. |  | fraction width, F > 0, multiplicative, rescale, coarsening, ambient, scale asymmetry, C7 |
| <a id="an-additive-verdict-is-independent-of-the-fraction-width"></a>an_additive_verdict_is_independent_of_the_fraction_width | finding | argument | three_or_more | [algebraic_laws](TOPIC.md#algebraic-laws) | Addition at a common scale never rescales, so an additive verdict, survival and breakage alike, is independent of the fraction width. | Addition of two values at a common scale never reads the scale, and the instruments are scale-blind by construction, so an additive result at `F = 0` transfers verbatim to every `F` by inspection rather than by sweep. What is independent of the fraction width is the **verdict**, not survival: the broken counts transfer too, 952 at `w = 4` for every measured fraction width. Three separately built instruments converge, and a fourth generated months earlier outside the unit folds in: a divergence table reading unsigned wrapping zero, signed wrapping zero, unsigned saturating zero and signed saturating 70.1 percent is a same-scale clipped add, so it is fraction-independent by the same argument. | total_width: W any, fraction_width: F any, signedness: signedness in {unsigned, signed}, overflow_policy: overflow policy in {wrap, saturate}, operation: operation = add, arity: arity = 2 |  | [additive_associativity_under_wrapping](LAW.md#additive-associativity-under-wrapping), [additive_associativity_under_saturation](LAW.md#additive-associativity-under-saturation) |  |  |  | `W any` is derived from the route rather than from a sweep: the argument is that the operation does not read the scale, which does not vary with width, and the source calls it inspection rather than measurement. `threads` and `target_features` are absent because the source names neither, so under I13 this holds in no situation where threads exist. That is the strongest unconditional result the format unit has, and transcribed faithfully it is unavailable to any threaded program. The neighbouring composed-operation law carries `threads any` for a claim of the same kind because its own source wrote the axis. | The premise is same-scale addition throughout, and nobody in the first five files of the unit had named it. Whether a mixed-scale addition exists and what result format it infers is an open question the whole additive column depends on, and the sweep that would settle it, varying both the mixedness option and the result-format rule, has not run. Aligning two scales to the join is a widening and is exact always; the threat is the result-format rule and the schedule, not mixedness itself. | additive, scale, fraction width, F-independent, same-scale, 952, 70.1, Q3, C7 |
| <a id="the-two-law-families-have-two-consumer-classes"></a>the_two_law_families_have_two_consumer_classes | answer | normative | one_expert | [algebraic_laws](TOPIC.md#algebraic-laws) | The adaptation laws are what order transport consumes, and the algorithms whose additive operation is the order are their consumer; coherence is what a reduction that may be split or reassociated consumes. The two are independent, all four combinations occur, and a numeral serves one class, the other, both, or neither. | The two families were measured independent in the format topic, with all four combinations inhabited, and each family has a distinct consumer that reads it and not the other. So a numeral's usefulness is two verdicts rather than one, and neither implies the other. |  |  | [coherence_of_a_reduction_onto_its_induced_operation](LAW.md#coherence-of-a-reduction-onto-its-induced-operation) |  |  |  | One expert on the mapping from families to consumers; the families and their inhabitation are the format topic's and are carried as a law row. |  | law families, consumer, order transport, reassociation, coherence, adaptation laws, N22 |
| <a id="a-law-is-a-fact-about-an-operation-composed-under-a-fixed-arithmetic-semantics"></a>a_law_is_a_fact_about_an_operation_composed_under_a_fixed_arithmetic_semantics | answer | normative | two_experts | [algebraic_laws](TOPIC.md#algebraic-laws) | A law is a fact about an operation composed under a fixed arithmetic semantics, never about a type or an operation alone. | Two authors derived it cold, in parallel, blind to each other, from I9, in two different formal traditions: one as a congruence closed under a strategy's exposed operations, the other as a judgment between expression forms of arbitrary finite length. Two traditions reaching one shape is the strongest thing the topic produced. |  |  |  |  |  |  | The unit's only two-expert claim, and the count was corrected downward during the unit: a third file's attempt to count an earlier same-author instance as further corroboration was reduced to two independent instances plus one earlier same-author instance at lesser weight. The dimension list a law's region needs grew all unit and is not closed; treating any snapshot of it as closed is the shape op rejected three times in one sitting. | The dimension list is a floor rather than an enumeration. Named so far and not all declared as `dimension` rows: operation, sign domain, overflow policy, fraction width, representable-set shape, the declared operand window, the schedule, the rounding rule, the constant-embedding convention, the width-indexing of a law family, and a placeholder reading whatever named axis a strategy resolves to. | law, fact, operation, arithmetic semantics, congruence, judgment, dimension list, R1 |
| <a id="a-composed-expressions-region-is-never-inherited-from-its-parts"></a>a_composed_expressions_region_is_never_inherited_from_its_parts | answer | normative | one_expert | [algebraic_laws](TOPIC.md#algebraic-laws) | A composed expression's region is never inherited from its parts. Even where a component operation's own hypotheses hold unconditionally, the composition needs its own derivation. | The counterexample is exact rather than illustrative: unsigned saturating addition associates on every triple at eight bits, and the composition of that addition with the matching subtraction fails on 82.7484% of triples at the same coordinates. Nothing about the components predicts it, and the exact holding region turns out to be a four-way case split on the two clamp events plus a residual operand condition. |  |  | [associativity_of_a_composed_saturating_add_and_subtract](LAW.md#associativity-of-a-composed-saturating-add-and-subtract) |  |  | [composition_contracts_above_the_numeral](OBLIGATION.md#composition-contracts-above-the-numeral) | `normative` because the sentence is a rule about how a region may be derived rather than a claim an instrument could refute; the instrument's own claim is the law row it points at, which carries the region. Both later attackers re-ran the establishing probe and it reproduced to the digit. |  | composition, region, inheritance, derivation, hypotheses, R2 |
| <a id="the-model-band-transfer-is-defeated-in-both-fragments"></a>the_model_band_transfer_is_defeated_in_both_fragments | refusal | theorem | two_experts | [algebraic_laws](TOPIC.md#algebraic-laws) | Agreement between a law's verdicts over a band of model widths and its verdict at the shipped width carries nothing. Two independently constructed families place a law's truth flip exactly at the shipped width, where no model band, at any guard setting, on any host, could have disagreed. | In the wrapping ring fragment, the product of `k` consecutive integers vanishes modulo two to the width up to a threshold given by Legendre's formula and not above it, so the family member indexed at the shipped width is true at every width below it and false at it. In the unsigned saturating fragment the exponent-absorbing identity has the same shape for a different reason. Both were run through the band construction end to end: band agreement green, perturbation control still refusing, arm licensed at the shipped width, law false at the shipped width. The family is not exotic, because magic constants have exactly this downward-correct, upward-false shape. | total_width: W any, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy in {wrap, saturate}, operation: operation = mul, arity: arity = 1 |  | [the_falling_factorial_family_vanishes_modulo_two_to_the_width](LAW.md#the-falling-factorial-family-vanishes-modulo-two-to-the-width), [the_saturating_exponent_absorbing_identity](LAW.md#the-saturating-exponent-absorbing-identity) | Compute the verdict at the shipped width inside a fragment that has a complete test set, which is a degree box rather than an exhaustive enumeration. Outside such a fragment there is no established route, and the narrower use of a band, validating a checker's own implementation, survives. | [where_a_law_verdict_is_established](QUESTION.md#where-a-law-verdict-is-established), [what_transfers_from_a_model_width](QUESTION.md#what-transfers-from-a-model-width) |  | Two authors, two fragments, one structural shape, which is the strongest multi-instance result in the topic. `W any` is the claim's own: the defeat is by construction and works at whatever the shipped width is. The proviso a band mechanism had relied on, that no law-relevant path dispatches on width, is satisfied by the defeating family and is therefore not sufficient either. Composed with I15 the weight doubles: with the runtime column gone, the compile-time verdict mechanism is the only validation path there is, so a false soundness story there has no catch anywhere. |  | band transfer, model width, shipped width, threshold family, defeat, polarity, R5 |
| <a id="inside-a-fragment-with-a-complete-test-set-the-verdict-is-computed-at-the-shipped-width"></a>inside_a_fragment_with_a_complete_test_set_the_verdict_is_computed_at_the_shipped_width | answer | theorem | two_experts | [algebraic_laws](TOPIC.md#algebraic-laws) | Inside a fragment with a complete test set, a law's verdict is computed at the shipped width, and two fragments now have one. For the wrapping ring fragment, agreement on the degree grid decides the whole domain, a false law has a witness inside its own degree box, and equation truth sets are initial segments at every arity. For the monotone unsigned saturating fragment, saturating evaluation equals the exact integer polynomial clamped once, and agreement on the degree box decides any width at any arity. | For the wrapping fragment: a law vanishes identically on the ring to the power of the arity exactly when two to the width divides every mixed forward difference at the origin, which by classical results and a complete tensor proof reduces to evaluation on the degree grid. Measured at genuine degree, 1,975 pairs with zero mismatches and 25 tensor members matching the Legendre prediction exactly. For the saturating fragment: saturating arithmetic is exact integer arithmetic composed with clamps rather than modular arithmetic, verified at 23,950,484 evaluations with zero disagreements, and the same falling-factorial triangularity carries the box theorem through 9,503 univariate and 3,006 multivariate verdicts with zero mismatches, with 507 cases attaining the bound exactly so it is not slack. | total_width: W in 1..=65, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy in {wrap, saturate}, operation: operations {add, sub, mul, neg} with integer constants for the wrapping fragment, and {saturating add, saturating mul} with nonnegative clamp-embedded constants for the saturating one, arity: arity any, build_profile: const gates on the pinned toolchain, no_std, no feature gates |  | [equation_truth_sets_are_initial_segments_in_the_width](LAW.md#equation-truth-sets-are-initial-segments-in-the-width) |  | [where_a_law_verdict_is_established](QUESTION.md#where-a-law-verdict-is-established) |  | Both gates are const gates on the pinned toolchain with no feature gates, no dynamic dispatch, no type identity and no allocation. The controls are the load-bearing part rather than the counts: a perturbation build refuses at the implementation check, a non-fragment build refuses at the fragment check, and two unchecked builds **accept while the same crate proves the licensed law false at the shipped width**. Violating a hypothesis does not degrade the verdict, it inverts it. Width-64 instances at genuine degree include one pair true at width 64 and false at 65, which is the sharpest available reminder that a verdict is per width. | `F = 0` is a hard boundary of the criterion, with a two-point witness true on its box and false in the domain, and 82,002 of 417,384 shift-carrying pairs true-on-box-false-in-domain. General signed with both clamps reachable has no procedure at all: the cheap saturation-radius route is closed by a non-constant tail and the expensive route is named and unbuilt. **The transfer is not on the trusted base list, because there is none.** The one item on that list with no mechanical check is the encoding of the law into the term array. | degree box, degree grid, test set, fragment, shipped width, const gate, Theorem A, trusted base, R6 |
| <a id="a-law-layer-answers-whether-a-law-reaches-a-lowering-the-backend-cannot-prove"></a>a_law_layer_answers_whether_a_law_reaches_a_lowering_the_backend_cannot_prove | answer | normative | one_expert | [algebraic_laws](TOPIC.md#algebraic-laws) | The question a law layer answers is not whether the law holds but whether it reaches a lowering the backend could not prove for itself. Where a law pays is strictly narrower than where it is true. | The probe that established it refuted its own thesis: at zero fraction width the emitted assembly aliases the fused and general forms to one symbol, so the backend performed the distributive rewrite unaided and the arm bought nothing in exactly the region where the law holds. Where it does pay is the case a backend structurally cannot reassociate, a saturating reduction, and there the control decides the attribution: the bounds proof **without** the law gets an unrolled loop with the serial clamp chain intact and no vector instruction, so the law is load-bearing and the bounds proof is not. |  |  |  |  |  |  | `normative` because it is a rule about what a law layer is for. Every instruction count behind it is read off emitted assembly and the source calls them **an ad-hoc quick spike, unpriced**, carried for which instruction appears where and nothing else, so the numbers are not a row here and are listed in `182` as unportable. The value-gated alternative was measured worse than either static arm and then closed on principle by I15 regardless of its cost. |  | microkernelling, lowering, backend, vectorisation, symbol alias, pays, unpriced, R10 |
| <a id="the-law-frame-was-attacked-from-another-topic-and-held"></a>the_law_frame_was_attacked_from_another_topic_and_held | finding | argument | cross_topic | [algebraic_laws](TOPIC.md#algebraic-laws) | The format topic's law frame, marked provisionally passing and named by its own author as the thing most wanting attack, was attacked from a different topic, from a direction it was not built to face, and held. The number-system topic built the attack as a self-collapsed system naming its own computed algebra as its ambient domain; the derived-laws topic then extended the frame's ambient-associativity hypothesis into a const predicate over a declaration rather than re-testing it. | A single-expert claim from one topic being independently pressure-tested by a different topic and coming out intact is stronger evidence than either topic states alone, and neither topic states it. The attacker calls it a failed refutation rather than a proof, and the provisional marking stays. | total_width: W = 4, fraction_width: F in {0, one unnamed value above 0}, signedness: three sign domains, unsigned and signed two's complement and signed symmetric range, overflow_policy: overflow policy in {wrap, saturate}, operation: operations {add, mul} |  |  |  |  |  | The predicate is the frame's own region, carried from the row it is about, because an attack that fails establishes nothing outside the region the thing was stated in. A failed refutation is not a second instance of the frame, so the frame itself stays at one expert; what is `cross_topic` is the fact that the attack happened and failed. |  | law frame, attack, failed refutation, provisional, self-collapsed, H1, cross topic |
| <a id="a-trajectory-condition-lifts-into-a-declaration-exactly-when-it-survives-closure"></a>a_trajectory_condition_lifts_into_a_declaration_exactly_when_it_survives_closure | answer | measured | one_expert | [algebraic_laws](TOPIC.md#algebraic-laws) | A trajectory condition lifts into a declaration over the operand set exactly when the condition survives the set's closure under the operations the law is about. A condition that does not survive closure reaches only the degenerate sub-cases: the maximal lifted box is 21.98% of the holding set and every non-degenerate holding box is clamp-free, at zero residue against a live control. | The word that moves a trajectory fact across the binding-time boundary is `declared`, and a declared range only means something if the operations the law is about cannot leave it. Sign uniformity survives closure and buys a recovered law; a bounded length survives closure up to the bound, so a length-aware predicate licenses strictly more and needs the length at const time; the composed saturating condition does not survive closure and its lifting reaches only boxes that pin an operand to zero. The control is what makes the zero residue mean something: the same widths carry 916, 44940, 2461192 and 144873168 boxes in which a clamp can fire, so the instrument had every opportunity to find a counterexample. | total_width: W in 2..=5, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = saturate, operation: op pair {add, sub} composed, arity: arity = 3 | [a_trajectory_condition_lifts_exactly_when_it_survives_closure](PROBE.md#a-trajectory-condition-lifts-exactly-when-it-survives-closure) | [associativity_of_a_composed_saturating_add_and_subtract](LAW.md#associativity-of-a-composed-saturating-add-and-subtract) |  | [may_an_arms_predicate_read_data](QUESTION.md#may-an-arms-predicate-read-data) |  | The instruments are `82_probes/p1_box_lifting_of_p4.rs` and `p1b_is_every_lifted_box_degenerate.rs`, committed with output. The four widths the consolidation names without naming are 2, 3, 4 and 5, read off the instrument's own table. **The consolidation marks this low confidence for a reason unrelated to its content**: an earlier draft of that consolidation misattributed which of the supporting instances shared an author, and it records itself as having made and repaired the error. Nothing downstream cites it for an independence count. | Stated as one-directional sufficiency by its own author. The length-aware construction that a bounded length would license is unbuilt. And the declaration a consumer would naturally write instead, that the exact result stays in range, is unsound: true while the law is false on 49.80% of the domain and wrong in 87.5% of the boxes satisfying it. | lifting, closure, trajectory, declaration, declared range, degenerate, box, 21.98, R8 |
| <a id="a-law-is-inherited-where-the-realisation-map-is-a-congruence-for-every-nesting-it-contains"></a>a_law_is_inherited_where_the_realisation_map_is_a_congruence_for_every_nesting_it_contains | answer | theorem | one_expert | [algebraic_laws](TOPIC.md#algebraic-laws) | Where the realisation map respects every ordered nesting of operations a law contains, the representable set under the induced operations is a quotient of exact arithmetic, so the law holds there whenever it is an identity of exact arithmetic. A verdict is therefore inherited rather than swept: the retraction table has one entry per ordered operation pair and is finite, while the space of identities is infinite at every arity, so deciding the finite table decides the infinite family. | A map that changes nothing when it is applied to an intermediate result is a congruence for the operation, and the induced structure on its image is the quotient by that congruence. A quotient satisfies every identity its source satisfies, because an identity is an equation between terms and the quotient map sends a proof of the equation to a proof of its image. So no sweep is needed for a law whose every nesting is respected, and the per-law, per-policy, per-fraction-width, per-signedness sweeps the panel has been running are consequences of one property that is decidable by inspection of a table nobody has to grow. | total_width: W in {4, 5, 6}, fraction_width: F in {0, 1, 2}, signedness: signedness in {unsigned, signed}, overflow_policy: overflow policy in {wrap, saturate}, rounding: rounding in {truncate, nearest}, operation: operations {add, sub, mul}, arity: arity in {2, 3}, threads: threads = 1, target_features: target features any |  |  |  | [which_route_a_law_verdict_takes_to_closed_form](QUESTION.md#which-route-a-law-verdict-takes-to-closed-form), [how_the_law_inventory_is_named](QUESTION.md#how-the-law-inventory-is-named) |  | **The mark is `theorem` and the reason is the order the source did it in.** The criterion is derived from the quotient construction and the instrument states it before running, so the sweep is a control on a prediction rather than the thing that established it. The sweep is real and is not this row's evidence: 552 cells over twenty-three configurations, zero soundness mismatches and zero conservative mismatches, both directions counted separately on purpose. Its instrument is `97_probes/p2_congruence_predicts_the_laws.py`, committed, and no `probe` row names it, so `evidence` is empty and the figure stays in this note rather than in a `measured` sentence it could not support.  **The region is the source's own, minus what is not an axis.** The laws the probe enumerated and the exhaustiveness of the value sweep are instrument parameters rather than situations the world can be in. `operand window in {full, declared non-negative}` is dropped because no `dimension` row declares that axis; the sweep covered both of its values, so the written region is narrower than what was established and the absence here is a gap in the vocabulary rather than a claim.  **The quotient route does not depend on the axes written above**, and the region is nonetheless the source's, because a predicate is never widened in place. A later expert wanting the wider claim states it with its own evidence.  **Whether this is a second arrival is not established here.** The format topic's law frame states the same criterion for associativity alone, as ambient associativity plus the reduction's kernel being a congruence, and this is its general form over every identity. The two were reached in separately dispatched topics and neither cites the other. Somebody who has read both should say whether that is a convergence; this row does not claim the rung.  | Only the sufficient direction is proved. That the criterion is also necessary, so a law failing it fails in the representable set, is measured at zero conservative mismatches over the 552 cells and is not derived. A design licensing a rewrite needs only the direction proved here; a design refusing one needs the other. | congruence, quotient, retraction table, identity, law verdict, inherited, finite table, infinite family, structural argument, F-F, closed form |
| <a id="a-min-plus-fold-needs-an-absorbing-top-and-wrapping-supplies-none"></a>a_min_plus_fold_needs_an_absorbing_top_and_wrapping_supplies_none | finding | measured | one_expert | [algebraic_laws](TOPIC.md#algebraic-laws) | A min-plus computation stands infinity on the numeral's top, so it needs that top to absorb addition. Over 63 unsigned width-and-fraction cells the top absorbs at every cell under saturation and at no cell under wrapping, and the consequence is measured end to end rather than left as a violated identity: a DAG shortest path under wrapping disagrees with exact unbounded arithmetic on 5,414,255 of 11,932,446 in-range instances at width 3 and on 407,293,133 of 832,398,764 at width 4, and on none under saturation. | Counting a law failure is not the same as counting a wrong answer, because an algorithm may never visit the triples that break. So the identity was counted first and then an actual routine was run over the numeral and compared against exact arithmetic, with an instance counted only where the exact answer and every exact intermediate fit inside the range. The max-plus longest path over the same graphs and the same policy agrees everywhere, which is what says the failure belongs to the operation rather than to the harness. | total_width: W in {3, 4} for the end-to-end run, and W in 2..=10 for the absorption sweep, fraction_width: F = 0 for the end-to-end run, and F in 0..=W for the absorption sweep, signedness: signedness = unsigned, overflow_policy: overflow policy in {wrap, saturate}, operation: operations {add, min, max}, arity: arity = 2, threads: threads = 1 | [the_top_absorbs_under_saturation_and_never_under_wrapping](PROBE.md#the-top-absorbs-under-saturation-and-never-under-wrapping), [a_dag_dynamic_programme_returns_wrong_answers_on_in_range_instances](PROBE.md#a-dag-dynamic-programme-returns-wrong-answers-on-in-range-instances) |  |  |  | [composition_contracts_above_the_numeral](OBLIGATION.md#composition-contracts-above-the-numeral) | Unsigned throughout, on both instruments, so it says nothing about a signed top. The width and fraction entries are two regions rather than one because two instruments established the two halves; the conjunction holds where they intersect, which is the narrower of the two. | Absorption is one of the two properties a min-plus computation needs and monotonicity is the other, measured in the same file and not filed here. A numeral that absorbs and is not monotone still gets the answer wrong, which is what `dl_interior_wrapping_with_a_reserved_absorbing_top` retires. | min-plus, tropical, absorbing top, infinity, shortest path, DAG, saturation, wrapping, in-range, end to end, graph |

### naming

| id | kind | sentence_kind | standing | topic | says | because | note | keywords |
|---|---|---|---|---|---|---|---|---|
| <a id="naming-is-partial-and-injective-or-it-is-broken"></a>naming_is_partial_and_injective_or_it_is_broken | answer | normative | one_expert | [naming](TOPIC.md#naming) | Naming is partial and injective, or it is broken. A name is an existence claim: the set of names is the set of supported compositions, the boundary between nameable and unnameable is where compile-time validation happens, and the naming function is the validator. | The partial half is a compile refusal naming the exact composition that overflows the container, so the absence of a name is what refuses an unsupported shape. The injective half is a type error wall, so two names for one primitive is a missed merge rather than a convenience. | Recorded by the panel as CONVERGED and demoted here to one expert, because the two halves were derived by two members and neither derived the composed claim. The composed statement, that the two halves are complementary, is one file's.  | naming, partial, injective, existence claim, validator, nameable, unnameable, compile-time validation, missed merge, composition |

### the_realisation_map

| id | kind | sentence_kind | standing | topic | says | because | predicate | answers | supersedes | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="the-realisation-map-has-an-algebraic-character"></a>the_realisation_map_has_an_algebraic_character | answer | normative | one_expert | [the_realisation_map](TOPIC.md#the-realisation-map) | A primitive's realisation map takes an exact result back into the primitive's value set. Beyond what it computes, the map has an algebraic character: the set of algebraic properties it possesses. The map is a homomorphism for an operation when reducing before that operation and reducing after it agree, and it is order-preserving when it never inverts the order of two exact results. | The two properties are what a design may act on: one licenses deferring a reduction, and the other licenses constructions and rewrites that read the map's order. Naming the character is what makes the licence families statable at all. |  |  |  | Signed as definitional by the one signature that reached it. `normative` for the same reason as the other definitions here: it is stipulated, and the source marks it `Definitional; no predicate`.  What follows from the character is a family of claims about which licence is available where, and none of those is in this file: every one of them is stated over an ambient domain and its closure under negation, and no `dimension` row declares such an axis.  | The whole of what this character licenses is unported. The topic's ten remaining clauses all carry regions naming axes the registry does not declare. | realisation map, algebraic character, homomorphism, order-preserving, monotone, reduce, defer, licence family, exact result, value set |
| <a id="a-law-verdict-is-invariant-under-change-of-encoding-and-container"></a>a_law_verdict_is_invariant_under_change_of_encoding_and_container | finding | argument | one_expert | [the_realisation_map](TOPIC.md#the-realisation-map) | A law contract is decided at the pair of identity and selected adaptation. It reads neither the encoding nor the container, and the same identity with only the adaptation moved flips a bound from accepted to refused. So the law layer's compile-time computation is keyed on strictly less than the container derivation's, and a verdict can be computed once per identity-and-adaptation pair and reused across every container realising it. | Carried from the number-system topic and read at source rather than through a summary. If a verdict read the container, moving the container would move the verdict, and it does not; moving the adaptation does. The reuse consequence had not been stated by anyone before, and it follows immediately from the keying. | container: container any, overflow_policy: overflow policy in {wrap, saturate} |  |  | The claim's other half is invariance under change of **encoding**, and no `dimension` row declares an encoding axis, so the predicate above cannot state it. An undeclared axis is invisible to the notation rather than absent from it, so the omission is a blind spot rather than a negative claim, and a reader must take the encoding half from the source. This keying is also why every finding in the topic is predicated on the overflow policy rather than on any marker name, which is what lets the whole topic survive the strategy set being reshaped. | Whether the reuse matters is unpriced. Nobody has measured what a per-identity-and-adaptation verdict cache saves. | verdict, encoding, container, invariant, keying, adaptation, marker name, R12 |
| <a id="an-output-of-a-derivation-is-a-fact-a-downstream-site-cannot-recover"></a>an_output_of_a_derivation_is_a_fact_a_downstream_site_cannot_recover | answer | normative | one_expert | [the_realisation_map](TOPIC.md#the-realisation-map) | What a derivation must make observable is settled by the three separated predicates, and none of them quantifies over the codomain, so all three together settle what must be observable and say nothing about how the derivation packages what it returns. Any product is one thing: a pair is a single element of a single set, and anything with two projections is a pair. So one output and two outputs are one mechanism at two levels of packaging, and the count of associated items on a derivation trait is not the count the predicates produce. What is left to decide is how many of the derived facts must be available as types rather than as values, and that number is at least two. | Without a settled account of what counts as an output the question is unfalsifiable, because everything downstream is a function of the declaration and one can always claim a single output and call the rest recomputation. With one, the two candidate answers stop being rivals: a single richer output with named projections is the pair wearing one name, and every argument the panel reached by counting associated items was adjudicated on packaging while claiming to be about observability. |  | [container_derivation_output_count](QUESTION.md#container-derivation-output-count) |  | **This row used to open by restating the retired criterion and to mark itself a definition of the word `output`.** That sentence is retired for being incapable of defining anything: the defined set appears on both sides of its own third clause, the operator is antitone, and no fixpoint tie-breaks it, so enumerated across the three background parameters it leaves unstated the one sentence admits answers of four different sizes. Its replacement is the three separated predicates, filed as their own rows, and what stands here is the packaging argument the retirement explicitly does not touch, which rests on the injectivity argument and the compiled evidence rather than on the criterion.  **The kind moved to `normative` and the regionlessness was right all along.** It was marked `definition` and carried no region, which the old note defended on the ground that a definition claims nothing about where something holds. That defence was sound and the label under it was not, because the sentence it defined is the retired one. What is left constrains what the design must do about packaging rather than reporting where anything holds, so it is normative and still owes no region. It is not an argument in this vocabulary's sense: an argument row here names the axes it ranges over, and this ranges over none of them.  **The measured half of the same file is not in this row and cannot be.** It establishes that a single richer output suffices if and only if it is a type: the type-valued spelling compiles gate-free and separates eight declarations that collapse to one carrier, while the value-valued spelling, which is lossless as information, is refused six times in three syntactic positions, each naming `generic_const_exprs`, which the panel forbids. That is a compiler measurement, its instruments are `47_probes/p1`, `p1b`, `p2` and `p2b`, all committed with their outputs and errors and five negative controls among the eleven, and no `probe` row names any of them. Marking it as an argument to get it written would be the dodge the evidence gate exists to catch, so it stays out and stays named.  **The register records a standing disagreement inside the same material** that this does not touch: whether the strategy is an upstream selector the ladder never sees or a key of the ladder itself.  | How many facts must be types is stated as at least two and is not settled. The same file argues that two is already insufficient under a widening accuracy strategy, and separately that two strategies at one width can share carrier and stride and differ only in compute, which the criterion above makes a question about the observation surface rather than about arity. | output, derivation, codomain, packaging, projection, carrier, stride, injectivity, observation surface, recoverable, kind boundary |
| <a id="a-lowering-site-holds-the-numerals-full-type"></a>a_lowering_site_holds_the_numerals_full_type | answer | normative | one_expert | [the_realisation_map](TOPIC.md#the-realisation-map) | A lowering site holds the numeral's full type: the declaration with its width, sign and strategy, the derivation's result, and the language's primitives. | It is every file's unstated practice rather than anybody's finding, and writing it down is what removes the two-readings ambiguity that made the criterion it replaces undefinable. Nobody proposes dropping the strategy early, and the acceptance criterion places erasure at codegen. |  |  | [an_output_of_a_derivation_is_a_fact_a_downstream_site_cannot_recover](PROPOSAL.md#an-output-of-a-derivation-is-a-fact-a-downstream-site-cannot-recover) | **This is a design commitment for op to bless and not a finding**, in the source's own words, which is why it is `normative` and why its standing is one expert rather than the convergence the neighbouring clause carries. It is stated rather than inferred on purpose: inferring it is what let one sentence be read two ways with opposite results inside the file that stated it. |  | site, lowering, premise, full type, declaration, strategy, erasure, codegen, site model |
| <a id="a-fact-is-carried-when-producing-it-applies-a-rule-the-strategy-owns"></a>a_fact_is_carried_when_producing_it_applies_a_rule_the_strategy_owns | answer | normative | two_experts | [the_realisation_map](TOPIC.md#the-realisation-map) | A fact belongs in the derivation's result exactly when producing it requires applying a rule the strategy owns. A pure function of what every site already holds is recomputed rather than carried. | Emitting the extent and recomputing the carrier at each use would re-enter, at every use site, the problem the derivation exists to solve once. Getting such a value requires consulting the strategy as an actual decision rather than a formula, so a site cannot safely re-derive it without risking disagreement. |  |  | [an_output_of_a_derivation_is_a_fact_a_downstream_site_cannot_recover](PROPOSAL.md#an-output-of-a-derivation-is-a-fact-a-downstream-site-cannot-recover) | **Two independent statements of the idea exist and that is the whole of the standing**, deliberately not more: one in instance form inside the file that stated the retired criterion, in the passage that does not use it, and one derived cold in general form before its author read any panel file, committed before its own second phase. On top of those sit one formalisation and one second read, which are not further independent derivations of the same principle. The source says so itself and its own risk section marks the claim that the three formulations are one principle as the part of its verdict most likely to move, so this row does not claim three. |  | ownership, fact, carried, recomputed, strategy, rule, pure function, re-derive, extent, carrier |
| <a id="a-carried-fact-takes-the-sort-its-consuming-site-uses-it-in"></a>a_carried_fact_takes_the_sort_its_consuming_site_uses_it_in | answer | normative | one_expert | [the_realisation_map](TOPIC.md#the-realisation-map) | Whatever the ownership clause selects is carried in the sort its consuming site uses it in: as a type where a generic body would otherwise have to reach a type from computed values, and as a constant otherwise. | Reaching a type from computed values is refused by the compiler in that position, so the sort is decided by the site rather than chosen. The derivation's result must make available, as types, every fact a lowering site cannot recompute from a constant. |  |  | [an_output_of_a_derivation_is_a_fact_a_downstream_site_cannot_recover](PROPOSAL.md#an-output-of-a-derivation-is-a-fact-a-downstream-site-cannot-recover) | One expert's sentence, judged true and insufficient as a whole criterion and then relocated as exactly one clause of three. **Both of those judgements are right and neither diminishes it**: it was never wrong, it was doing one of three jobs while being asked to do all three. The count of facts is a consequence of the three clauses rather than a fourth clause, and it is not settled here. |  | kind, sort, type, constant, generic body, computed value, consuming site, form |

### rounding

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | answers | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="the-realisation-map-factors-into-quantisation-and-range-policy"></a>the_realisation_map_factors_into_quantisation_and_range_policy | answer | normative | one_expert | [rounding](TOPIC.md#rounding) | A realisation map factors into two decisions acting on disjoint parts of its input: a quantisation, deciding what happens to a value falling between representable points, and a range policy, deciding what happens to a value falling outside the representable extent. Neither is a modifier of the other. | Selecting a quantisation mode cannot move a realisation between the overflow families, which is what makes the two axes independent rather than merely separable. The two govern disjoint regions of the input and compose canonically as quantise-then-reduce. |  |  |  | `normative` because it is definitional and the source marks it so.  The topic's substance is that the quantisation axis is not a second copy of the range axis: no deterministic quantisation is additive off the grid, on either domain shape, and every deterministic member is order-preserving, so the property pair that classifies the range policy degenerates here in both directions at once. That claim is not in this file, because its region is stated over the ambient domain and nothing declares that axis.  | The obstruction result this definition exists to carry cannot be written as a row. Its region names the ambient domain, and its two halves are claimed and proved separately over the two domain shapes. | realisation map, factors, quantisation, range policy, overflow, disjoint, quantise then reduce, modifier, two axes, independent |
| <a id="staged-narrowing-disagrees-with-direct-narrowing-under-round-to-nearest-even"></a>staged_narrowing_disagrees_with_direct_narrowing_under_round_to_nearest_even | finding | measured | three_or_more | [rounding](TOPIC.md#rounding) | Narrowing through an intermediate grid is not the same function as narrowing once, under round to nearest even. On a nine-bit signed fixed-point grid staged from four fraction bits through two to zero, the two disagree, first at -247/16, where narrowing directly gives -15 and narrowing in stages gives -16. So a design that wants narrowing to compose cannot carry the mode every other numeric surface defaults to, and a design that keeps that mode owes the sentence saying narrowing twice is not narrowing once. | Round to nearest switches its direction at every cell midpoint of the finer grid, and none of those midpoints is a point of the coarser grid, so an intermediate stage can move a value across a coarse boundary that the direct narrowing would never have crossed. The directed modes never switch direction at all, and toward zero switches only at zero, which is a point of every anchored grid, which is why those compose and this one does not. | total_width: W = 9, fraction_width: F = 4, signedness: signedness = signed, rounding: rounding = round to nearest even, radix: radix = 2, threads: threads = 1 | [staged_narrowing_depends_on_its_staging](PROBE.md#staged-narrowing-depends-on-its-staging) | [does_narrowing_compose](QUESTION.md#does-narrowing-compose) | **The general rule behind it is in the corpus and cannot be written here.** The adjunction file establishes the characterisation directly: a mode composes across nested grids exactly when its direction switches only at points of the coarser grid, tested by building a family of away-from-pivot modes and moving the pivot on and off the coarse grid, at zero failures on-grid and seven off-grid. That is the sentence a canon would carry. Its instrument is `07_probes/p4_composition_and_forced_adjoint.py`, committed with its output, and no `probe` row names it, so the general form is not writable and what is written is the one instance whose instrument is registered.  **Three witnesses, which is what the standing rests on**, and they are the cited probe's own: this instrument, an earlier probe's P4 at 500 half-even mismatches of 4001, and a signature's `s2`. They agree on the corrected result, and the correction is why the count matters: the instrument's first run reported half-even carrying exact staged composition, over a window holding no value whose intermediate stage lands on a tie.  **The staging is stated in the sentence and not in the predicate**, because no `dimension` row declares the intermediate fraction width of a staged narrowing. `accumulator_width` is the nearest and is about what a chain accumulates into rather than what a narrowing passes through, so writing it would name the wrong axis.  | Whether the design wants composable narrowing is a preference and is untouched. What is settled is the price of wanting it, which is the rounding mode, and the price of not wanting it, which is a canon sentence saying so. | narrowing, composition, staged, round to nearest even, half even, directed rounding, toward zero, grid, pivot, coarser grid, double rounding |

### the_strategy_object

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | law | instead | answers | supersedes | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="a-strategy-is-an-assignment-and-a-weighting"></a>a_strategy_is_an_assignment_and_a_weighting | answer | normative | two_experts | [the_strategy_object](TOPIC.md#the-strategy-object) | A strategy is a pair. Component one is an assignment on observable policy axes and it fixes the denoted answer. Component two is a weighting over cost coordinates and it ranges over realisations of that denotation. The two are told apart by one mechanical test rather than by taste: take the candidate axis, hold everything else fixed, sweep the domain, and look for an input where the answer changes. | The components have different closure properties, so the question of whether the strategy set is closed has two answers and conflates them. The assignment space is closed and enumerable, because an axis position with no lowering cannot be supplied from outside. The weighting space is open and consumer-supplied, because a weighting re-ranks arms that already exist. The axes are closed; the named points are open. |  |  |  |  |  |  | Two cold derivations reached the pair blind by different routes, one from a bytes-level model and one from two of the stated intents set against each other. **The blindness evidence is weaker than the pair's own files claim and the discount travels with the row.** The commit ordering establishes the within-file half for each and nothing about the between-file half, and for one member it runs the wrong way: its phase one landed two minutes after the other member's file was already in the tree. Blindness rests on the two coverage statements and on one member's specific disclaimer, and on nothing else. Both also read the same auto-loaded workspace rules, one of which states a mechanism they both used, so wherever that mechanism does the work they are one instance wearing two hats.  `normative` because the pair is a definition, and the mechanical test is a criterion rather than a measurement.  | The closure asymmetry carries a region naming an assignment set, which no `dimension` row declares, so that half of the clause is stated here without its region and cannot be gated on. The membership test's own region names an assignment set and an observation set and is likewise unwritable.  | strategy, pair, two components, assignment, weighting, policy axis, cost coordinate, denotation, closed, open, membership test, observable |
| <a id="no-cost-model-may-move-an-answer"></a>no_cost_model_may_move_an_answer | refusal | normative | one_expert | [the_strategy_object](TOPIC.md#the-strategy-object) | No cost model may move an answer. Every difference in an answer traces to the declared policy and nothing else may move one. With the firewall, a target change moves which arm runs and moves no answer; without it, a target change moves answers at the arms' full disagreement rate and no predicate names the change, because the cost table is not in the type. | The constraint is imposed rather than established, and it is what the whole two-component split is for: if a weighting could move an answer, the assignment would not fix the denotation and the pair would not be a pair. |  |  |  | Where an arm realises no assignment in the assignment set, the firewall has no repair available in the type. The obligation then falls on the arm set rather than on the cost model: admit only arms that realise the denotation of some assignment, and a conforming pair becomes two arms of one assignment that the weighting may pick between freely. | [is_i9_about_a_strategy_or_one_component](QUESTION.md#is-i9-about-a-strategy-or-one-component), [reduction_order_or_associativity](QUESTION.md#reduction-order-or-associativity) |  | **This row carries no region on purpose and both signatures agree that is right.** A predicate records the region a claim was established in. The firewall is not established, it is imposed, so applying the notation would say the constraint holds only where somebody measured, and under the absence rule that means the design is free to violate it everywhere unmeasured. The notation would not merely misdescribe the claim, it would invert it.  Three files endorse it and none derives it independently: one proposes it, one endorses the proposition while attacking the repair, and one keeps it. Standing is one expert on that reading.  Op has an open question underneath this, and it reaches further than the file that raised it: whether a canon may carry an unpredicated proposition at all. A later clause is shown to inherit the answer, so the two are one decision and should be put to him together.  | What is gateable about the firewall is three separate things and none of them is this row: a predicated enforceability condition on the arm set, a predicated measurement of the consequence, and a predicated violation test for a candidate arm. All three carry regions naming an assignment set, an arm set and cost coordinates, none of which any `dimension` row declares, so none of the three is written into the canon.  | firewall, cost model, answer, denoted answer, policy, unpredicated, imposed, enforceability, conforming arm, target change, arm set |
| <a id="pick-the-policy-before-the-weighting"></a>pick_the_policy_before_the_weighting | answer | normative | one_expert | [the_strategy_object](TOPIC.md#the-strategy-object) | The policy is chosen from the consumer's semantics and the substrate cannot know it. The weighting is chosen from the consumer's measurements and nobody else can take them. Pick the policy first, because a wrong policy is a wrong answer and a wrong weighting is a slow answer. | It follows from the object rather than from a measurement: the assignment fixes the denotation and the weighting ranges over realisations of it, so an error in the first is an error in what is computed and an error in the second is an error in how long it takes. |  |  |  |  |  |  | Two presets sharing a policy assignment compute the same answers on every input and no test distinguishes them. That is not a defect, and a reviewer expecting four presets to be four semantics will keep looking for a difference that is not there.  `normative` because it is guidance to a consumer. The source marks its argument kind as a derivation from the object, which is how it was reached rather than what kind of statement it is; the statement is a prescription and carries no region.  |  | policy first, weighting, consumer, semantics, measurements, preset, wrong answer, slow answer, indistinguishable presets |
| <a id="fusing-a-multiply-add-is-free-exactly-at-translation-equivariance"></a>fusing_a_multiply_add_is_free_exactly_at_translation_equivariance | answer | theorem | one_expert | [the_strategy_object](TOPIC.md#the-strategy-object) | Fusing a multiply-add is answer-preserving exactly where the rounding position is translation equivariant on the domain the cell reaches. Under unsigned that is five of six positions, because the domain excludes the negatives where toward-zero and away-from-zero differ from their equivariant twins. Under signed wrapping it is three of six. Signedness stops being a case split and becomes what determines the domain. | Equivariance restricted to the domain the cell reaches predicts the measured table at twelve of twelve cells, where the unrestricted test mispredicts two, both under unsigned and both in the direction of calling a free arm unavailable. The restriction is what turns two case tables into one property. | total_width: 6, fraction_width: in 0..=5, signedness: in {unsigned, signed}, overflow_policy: in {wrap, saturating}, rounding: in {floor, ceiling, toward zero, away from zero, nearest-half-up, nearest-half-even}, operation: multiply-add, arity: 3, chain_length: 2, container: declared width, threads: 1, target_features: host aarch64-apple-darwin, rustc 1.98.0-nightly 57d06900f |  | [fusing_a_multiply_add_preserves_the_answer_under_unsigned](LAW.md#fusing-a-multiply-add-preserves-the-answer-under-unsigned), [fusing_a_multiply_add_preserves_the_answer_under_signed_wrapping](LAW.md#fusing-a-multiply-add-preserves-the-answer-under-signed-wrapping) |  |  |  | **One expert on the formulation and three instruments on the table underneath it**, which is the split the source itself draws and not a discount added here. The author of the formulation names its own exposure: the restricted equivariance test was decided over a window it chose, and a clean sweep does not settle a property the way a counterexample settles its negation. A second instrument uses a different window and agrees, which is a second reading of the restricted test and not a proof that the window is right. If the formulation falls, the enumerated table stands on the measurement alone.  The predicate carries the region the table was swept over. The clause's own scope note, that inputs were exhaustive over the declared range, is coverage rather than region and is recorded here rather than in the predicate.  This replaces a clause of the first candidate that was **false**: it claimed the whole rounding axis free under unsigned, on a congruence argument covering the reduction relocation and not the rounding relocation. Two signatures found it independently and a third instrument in a third language reproduced it. The false clause is not in this file and no row supersedes it, because it was never ported.  | The property is stated over 'the domain the cell reaches', and no `dimension` row declares an axis for that domain. The predicate above is the swept region and does not carry the quantifier the statement turns on. | fusion, fused multiply-add, fma, translation equivariance, reachable domain, rounding position, signedness, free lowering, relocation, nearest-half-even |
| <a id="where-fusion-changes-the-answer-it-is-not-a-lowering"></a>where_fusion_changes_the_answer_it_is_not_a_lowering | answer | enumeration | two_experts | [the_strategy_object](TOPIC.md#the-strategy-object) | Where fusion changes the answer it is not a lowering, and the axis already names it. The fused and stepwise realisations at signed saturating are two denotations, so a consumer wanting the fused answer declares the exact-intermediate position, keeps full determinism, and gets the fast arm. | The two realisations are extensionally identical everywhere the intermediate axis does not separate them, established twice on independent instruments over 6,356,992 triples with a cross-pairing control differing at 757,954. Where they are not identical the difference is a difference of denotation, which the declared axis is exactly the mechanism for naming. | total_width: in {4, 6}, fraction_width: in 0..=W-1, signedness: in {unsigned, signed}, overflow_policy: in {wrap, saturating}, rounding: truncate toward zero, operation: multiply-add, arity: 3, chain_length: 2, container: declared width, threads: 1, target_features: host | [fusion_is_an_axis_position_rather_than_a_new_axis](PROBE.md#fusion-is-an-axis-position-rather-than-a-new-axis) | [the_fused_and_the_stepwise_multiply_add_denote_one_function](LAW.md#the-fused-and-the-stepwise-multiply-add-denote-one-function) |  |  |  | Two instances, one building both arms directly and one assembling the comparison from a third member's two committed probes. Whether the two share every axis they both name is unchecked: the value-level intersection instrument that found empty intersections in three of four convergence rows was run over four rows and this is not one of them, so the standing is over the region the two files state rather than over a verified intersection.  |  | fusion, fused, stepwise, denotation, lowering, intermediate axis, exact intermediate, determinism, signed saturating, extensional identity |
| <a id="a-strategy-is-a-pair-of-an-observable-assignment-and-a-weighting"></a>a_strategy_is_a_pair_of_an_observable_assignment_and_a_weighting | answer | normative | one_expert | [the_strategy_object](TOPIC.md#the-strategy-object) | A strategy is a pair. Its first component is an assignment on the axes a consumer can observe, supplied and never derived. Its second is a weighting over cost coordinates which selects among the arms that produce the answer the first component fixed, resolved and never observed. The two have different carriers: the first travels with the value, the second is supplied where the operation happens. The number of strategies is not a design parameter. | A consumer of a value cannot recover the observable assignment from the bits, so every consumer of that value must agree about it, which forces it onto the value. Only the site knows the arity, the access pattern and the target, which forces the weighting to the site. Nothing a consumer can see depends on which cost-equivalent arm was taken, which is why the second component erases. |  |  |  |  |  |  | **Superseded.** Its own consolidation records that a later attack found five of its eight clauses needing repair, and says in terms that whoever writes the canon takes the statement from the attack rather than from here. It is kept because the superseding row is a repair of it rather than a replacement, and because a reader meeting the pair in the register or the checkpoint needs to know which version they have. One expert, arrived seventh in its unit, and nobody attacked it before the consolidation shipped. | Clause three is false and it is the one that mattered: component two was defined as ranging over the arms that produce the answer the first component fixed, and in that region a fidelity column would measure a constant, so op's accuracy intent is expressible in neither component while the same file claims the mechanism serves it. | strategy, pair, observable, weighting, carrier, superseded, clause three |
| <a id="a-strategy-is-a-declared-semantics-together-with-a-weighting-over-the-arms-that-realise-it"></a>a_strategy_is_a_declared_semantics_together_with_a_weighting_over_the_arms_that_realise_it | answer | normative | one_expert | [the_strategy_object](TOPIC.md#the-strategy-object) | A strategy is a declared semantics together with a weighting over the cost coordinates of the arms that realise it, the second chosen from a set the first determines. The declared semantics is an assignment on the axes a consumer can observe, and an axis belongs there if there is any reachable chain on which moving it is observable; where a particular chain cannot observe it, that is a licence the resolver may take under a predicate over the chain, not a reclassification of the axis. An arm realises the declared semantics exactly or approximately, and the distance from the declaration is a cost coordinate like any other, which is what lets a strategy weigh accuracy at all. The weighting is a support, the set of coordinates demanded, together with a rate, the exchange among them: supports join uniquely and totally by union, rates do not join, and where two rates meet the site names one or the operation reports. | Component one fixing the **denoted** answer rather than the computed one is the single change that restores the region in which an accuracy coordinate can vary, and without it the accuracy intent has nowhere to live. Observability is a property of the chain rather than of the axis, measured at 0% against 89.081% for the same overflow-policy assignment read at the declared width against the container width, and the repair is a conservative closure plus a per-chain licence with zero unsound predictions over 8,019 exhaustively swept chains. Supports and rates are two objects and one word had been naming both: supports join canonically at nine of nine, rates disagree on 71.4% of 42 pairs. |  |  |  |  |  | [a_strategy_is_a_pair_of_an_observable_assignment_and_a_weighting](PROPOSAL.md#a-strategy-is-a-pair-of-an-observable-assignment-and-a-weighting) | This is the version the superseded row's own consolidation directs a canon writer to, and it comes from the attack rather than from any of the four consolidations this pass was sent to port. It is one expert's converged statement over ten files, offered as a suggestion with op deciding, and it is not a tier above the row it repairs. Its author states where it is weaker than wanted: the chain licence is measured on one axis at two widths on one operation alphabet, whether it extends to intermediate precision is untested, and the whole statement inherits an open question about whether the axis list is complete. | Whether the observable-axis list is complete. Nothing in the unit closes it. | strategy, declared semantics, weighting, support, rate, arm, fidelity, denoted, repaired |
| <a id="the-join-over-demands-is-union-and-it-is-free"></a>the_join_over_demands_is_union_and_it_is_free | answer | theorem | two_experts | [the_strategy_object](TOPIC.md#the-strategy-object) | The closure of `d` one-demand generators under union is the free join semilattice on `d` generators, whose carrier is the non-empty subsets. It is generated rather than enumerated, so a design writes down `d` names and gets the rest, and the fifteen-element closure that had been priced as a cost is two to the fourth less one. | An independent enumeration reproduces both of the numbers that had been read as a cost and identifies the object, and a compiled instance asserts on the pinned toolchain that the join is the union and commutes over all 256 ordered pairs, that it associates over all 4096 triples, and that all 16 elements are idempotent, with a mutant dropping one coordinate failing to compile. Two things fall out that a flat set cannot give: silence becomes a first-class element, so the absence of a demand says the consumer asked nothing there and the resolver is free, rather than being a cell somebody has to fill; and the escalation pathology disappears, where every join semilattice satisfying the intent-derived constraints on a flat marker set makes the accuracy-first preset the top and escalates every mixed expression to it. | build_profile: no_std, zero feature gates, on the pinned toolchain, arity: arity in {2, 3} for the asserted join laws |  |  |  |  |  | The establishing file's own probe output already said this and its prose priced the closure as a cost anyway, so the option register inherited the prose. That is a compression loss inside one file, between its probe and its own text, at the smallest possible scale, and it is the same failure a consolidation exists to prevent. Two instances: the enumeration and the compiled assertion, by two authors. |  | demand lattice, join, union, free, semilattice, silence, escalation, preset |
| <a id="a-coordinate-set-is-a-countable-ceiling-on-how-many-strategies-can-exist"></a>a_coordinate_set_is_a_countable_ceiling_on_how_many_strategies_can_exist | finding | measured | one_expert | [the_strategy_object](TOPIC.md#the-strategy-object) | A coordinate set is a countable ceiling on how many strategies can exist, exactly rather than approximately. On the committed carrier table with the control dropped, at strictly positive weights, one coordinate reaches 1 section, two reach 9 and three reach 42. With one coordinate the weighting cancels and exactly one section is reachable, by algebra rather than by the arms happening to be similar. | The corpus measures one coordinate and no more: of seventeen columns across 254 files and 104080 rows, nine carry information and exactly three vary between arms at a fixed region, and those three are one timing and its decomposition. Eight are identically empty or zero. Independently confirmed on the reachable surface: no variant crate implements a scoring hook or a dimension hook, none implements a relative-error bound, fifteen define an output validator and one mentions a may-differ flag. | threads: threads = 1 |  |  |  |  |  | **The predicate carries almost none of this claim's scope.** Its real coordinates are the committed table, the number of arms and the coordinate count, all bench parameters. The degenerate case is the one a design falls into without noticing: with one coordinate the count is one, and it is one by algebra, so a design that ships a single cost coordinate has exactly one strategy however many names it gives them. |  | ceiling, coordinate, countable, sections, 1, 9, 42, degenerate, cancels |
| <a id="the-named-strategies-are-points-in-a-product-and-the-flat-set-is-a-slice"></a>the_named_strategies_are_points_in_a_product_and_the_flat_set_is_a_slice | answer | normative | two_experts | [the_strategy_object](TOPIC.md#the-strategy-object) | The named strategies are points in a product, and the flat named set is a slice through it. A name is a binding rather than a member of a closed set. | Two disjoint arguments, blind: one from the resolution side by counting, one from the component side by measuring that a fifth unnamed point costs one alias symbol and eight bytes. If a name were a member of a closed set, adding one would be a design change; measured, it is an alias. |  |  |  |  |  |  | Two experts. It is the same shape as I1's refusal to presume a strategy count, and the number of strategies being bounded above by the coordinate set rather than chosen is the quantitative half, which is a separate row at one expert. |  | named strategies, product, slice, point, binding, alias, closed set, I1 |

### the_chain

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | law | instead | answers | obligation | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="within-an-unbound-stretch-the-design-may-select-any-realisation"></a>within_an_unbound_stretch_the_design_may_select_any_realisation | answer | normative | one_expert | [the_chain](TOPIC.md#the-chain) | Within a maximal unbound stretch, the design may select any realisation whose boundary behaviour meets the stretch's declared grade on its definedness domain. The exact grades fix the boundary function uniquely: composite correct rounding owes the resolution of the exact value, structural exactness owes the exact value, stepwise owes the fully-eager function, and the bounded-drift grade admits the family of placements within its bound. | The licence rests on two normative premises and both are named. The observability perimeter bounds obligations above, so nothing beyond the observation surface is owed; the declaration bounds them below, so the declared grade is owed. Neither follows from the partition theorem the clause sits beside. |  |  |  | Where a stretch contains an operation that is partial at the profile in force, agreement on definedness is part of the declared grade at every profile, because partiality is a binding-free observation channel that no assertion flag governs. | [reduction_order_or_associativity](QUESTION.md#reduction-order-or-associativity) | [composition_contracts_above_the_numeral](OBLIGATION.md#composition-contracts-above-the-numeral) | **This is the underivable one, and it is why the topic's own result is that its central claim splits by provenance.** Three members tried to derive it from the stated intents and all three failed for stated reasons: one intent licenses removing unused paths rather than roaming among used ones, one says optimal rather than behaviour-preserving, and two constrain the surface and supply no licence. It rests on a workspace rule that was in every member's context and named by none of the cold derivations until the attack found it.  **Standing is recorded as one expert and the honest number is zero.** The panel's own ledger puts this at zero independent instances, and the enumeration has no value for that. A reader counting instances from this field will count one too many.  It holds in a shipped artifact and fails in a development build, which is a build bound arriving from a direction the intent that already states that bound was never derived from. The profile does not merely add an observation channel: it changes which operations are partial, so the same pair of realisations can share a definedness domain at one profile and not at the other.  | Whether the observability principle becomes an intent of arvo's is not the panel's, and a workspace rule is not a ratification. Whether a canon may carry a sentence of this kind beside established ones is a second question of op's, coupled to the canon-form question as one decision.  The build bound is real and is not written as a region, because a region on an imposed proposition would invert it. The bound is `debug-assertions = off` for the licence to hold, and it is stated in the sentence rather than in a predicate.  | licence, normative, unbound stretch, boundary function, declared grade, definedness domain, observability perimeter, underivable, build profile, debug-assertions, partial operation, composite correct rounding, structural exactness |
| <a id="a-chain-is-exact-operations-together-with-a-schedule-of-adaptation-points"></a>a_chain_is_exact_operations_together_with_a_schedule_of_adaptation_points | answer | normative | one_expert | [the_chain](TOPIC.md#the-chain) | A chain is a composition of exact operations together with a schedule of adaptation points, and the schedule is part of the function's meaning. The per-operation model extends to chains exactly when adaptation is unfused from the operations. Chains factor into windows, bounded subterms evaluated exactly and adapted once, whose capacity is derived by the same typestate that derives the container. A concept that hides the adaptation inside each operation cannot state the chain-accuracy intent at all. | The last clause is the load-bearing one and it is why the concept is shaped this way: closing operations over the format so that adaptation fuses invisibly into each one makes op's chain-accuracy intent unstatable, not merely harder to serve. One cold derivation reached it, reconciled without contradiction, and nobody in the unit attacked it. |  |  |  |  |  | [composition_contracts_above_the_numeral](OBLIGATION.md#composition-contracts-above-the-numeral) | One expert, cold, unattacked. It assumes one of three live directions for where a chain lives, and adopting it must not be read as closing that question: under the first direction it has no home by construction, and under the third the wording shifts in kind rather than in content, the chain becoming a typed term denoting the composition rather than being it. The consolidation restored the third direction after its own first draft dropped it, catching the loss only through the independent entailment check that followed. | Where the chain lives is a three-way fork none of which is killed. The third carries named costs: a second vocabulary, type sizes growing with expression size, and drift toward a computation-graph framing. | chain, schedule, adaptation points, window, unfused, I7, chain accuracy, C9 |
| <a id="chain-laws-split-by-whether-a-lifting-theorem-exists"></a>chain_laws_split_by_whether_a_lifting_theorem_exists | finding | argument | one_expert | [the_chain](TOPIC.md#the-chain) | Chain laws split into two kinds. Grouping questions lift from arity three by the generalised associative law, so the const-eval frontier's arity axis never touches them. Schedule questions, stepwise rounding against rounding once, are vacuous at arity two and a fresh statement at every higher arity, with no lower-arity statement implying them. The schedule kind carries rounding, sits outside both fragments with a test set, and is the kind op's accuracy intent is stated over. | Measured as wrapping showing zero divergence from arity two through five and saturation showing nonzero divergence from three, which is the signature of grouping lifting and schedule not lifting. A schedule question at arity n says something no arity below n can express, because the number and position of the adaptation points is what varies. | chain_length: chain length in 2..=5, overflow_policy: overflow policy in {wrap, saturate}, operation: operations {add, mul}, arity: arity = 3 for the grouping kind, and arity in 2..=5 for the schedule kind |  |  |  | [which_route_a_law_verdict_takes_to_closed_form](QUESTION.md#which-route-a-law-verdict-takes-to-closed-form) |  | One expert, correcting its own earlier section. The consequence for the box procedure was priced by a second author: a multilinear chain law must reach length 15 before the default guard notices, so the residual exposure is high-arity non-liftable ring laws, of which the unit exhibited none. | The schedule kind sits outside both fragments that have a test set, so nothing established in this topic decides a schedule question at the shipped width, and that is the kind op's accuracy intent is stated over. | chain, grouping, schedule, lifting, generalized associative law, rounding, I7, R11 |
| <a id="chain-accuracy-cannot-be-served-by-an-operator-closed-over-its-operand-type"></a>chain_accuracy_cannot_be_served_by_an_operator_closed_over_its_operand_type | finding | measured | cross_topic | [the_chain](TOPIC.md#the-chain) | Chain accuracy cannot be served by an operator closed over its operand type, and the intermediate width required grows linearly in chain length with no logarithmic closed form. | Two authors reached it blind and in parallel at two different parameter settings, one deriving the growth rule in closed form with an availability table against a 128-bit widest tier. The same structural conclusion was then reached independently in two further topics without any of the three citing the other two: the format topic reaches it as a chain being exact operations plus a schedule of adaptation points, so that closing operations over the format makes the accuracy intent unstatable; the derived-laws topic reaches it as multiplicative chains needing width linear in fold length, with the schedule kind being what the accuracy intent is stated over. | total_width: W in {8, 16}, fraction_width: F in {4, 8}, operation: operation = mul, chain_length: chain length any |  |  |  |  |  | `cross_topic` rather than `two_experts` because three separately dispatched topics computed it independently and none cites the prior two, which is the strongest standing this panel produces. The width and fraction entries are the two blind derivations' own parameter settings; `chain length any` is the growth rule's own quantifier rather than a swept range. The strategy topic later sharpened it into chain length being a region dimension and the crossing point where one accuracy arm overtakes another being itself decided by a weighting. |  | chain, accuracy, closed operator, intermediate width, linear growth, fold length, cross topic, I7 |
| <a id="a-coherent-reduction-needs-no-accumulator"></a>a_coherent_reduction_needs_no_accumulator | finding | measured | one_expert | [the_chain](TOPIC.md#the-chain) | A coherent reduction needs no accumulator: the format's own width already suffices at every fold length. Eager saturation in the format itself agrees with exact-then-adapt at the narrowest accumulator width there is. | Where the reduction is coherent there is no grade to collapse, because the ambient operation is associative and the reduction's kernel is a congruence for it, so reducing at every step and reducing once are the same function. Measured across sixteen unsigned rows, every one reporting that the format width already suffices, exhaustively over every tuple at each row. | total_width: W in 3..=5, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = saturate, operation: operation = add, chain_length: chain length in 2..=8 |  | [additive_associativity_under_saturation](LAW.md#additive-associativity-under-saturation) |  |  |  | The instrument is `57_probes/p6_the_adaptation_absorbs_one_bit.rs` with its output beside it. The region here comes from the instrument's own table rather than from the consolidation, which states the sixteen rows and the fold lengths and no width at all. Four signed rows at fold length two report the same thing, which is the degenerate case where no accumulator question arises, and they are not in this region because the general signed verdict is the neighbouring row's. |  | accumulator, coherent, reduction, format width suffices, fold, grade, collapse, C8 |
| <a id="an-incoherent-clamped-addition-needs-the-exact-sum-width-less-one-bit"></a>an_incoherent_clamped_addition_needs_the_exact_sum_width_less_one_bit | finding | measured | one_expert | [the_chain](TOPIC.md#the-chain) | For incoherent clamped addition, agreement with the once-adapted exact answer needs the exact-sum width less one bit. The final adaptation absorbs the outermost bit, because the accumulator must decide which side of the format the result fell on rather than represent how far outside it fell. | The gap between the exact-sum width and the smallest sufficient accumulator width is exactly one bit in every row of the sweep where a minimum can be located, with zero anomalies: nineteen signed rows, all at a gap of one, of which fifteen locate a minimum by divergence and four are the degenerate fold-length-two case. Two predicates are distinguished on the way and the panel had one phrase for both: interior safety, where no clamp fires, and adaptation agreement, where the answer equals exact-then-adapt. They differ by exactly this bit. | total_width: W in 3..=6, fraction_width: F = 0, signedness: signedness = signed, overflow_policy: overflow policy = saturate, operation: operation = add, chain_length: chain length in 2..=8 | [the_accumulator_width_is_the_exact_sum_width_less_one_bit](PROBE.md#the-accumulator-width-is-the-exact-sum-width-less-one-bit) |  |  |  |  | The instrument is `57_probes/p6` with its output beside it, and its own closing note bounds it exactly: measured at the widths and fold lengths in the table and no further, exhaustive within each row, not a proof, and the unsigned and signed rows are the same claim at two sign domains rather than two independent instances. The consolidation writes the candidate canon sentence to survive without the constant, as `at most the exact-sum width`, which is how a canon holds a constant that is measured and not proven, and that weaker form is the one to promote. |  | accumulator, exact-sum width, one bit, incoherent, clamped, interior safety, adaptation agreement, C8 |
| <a id="the-multiplicative-guard-grows-linearly-and-the-saving-is-adaptation-fusion"></a>the_multiplicative_guard_grows_linearly_and_the_saving_is_adaptation_fusion | finding | measured | two_experts | [the_chain](TOPIC.md#the-chain) | For multiplication no bounded closed form exists: the exact guard grows linearly in fold length at `(n-1)F` bits with no logarithmic closed form. The saving below full precision is adaptation fusion, and it is rounding-conditional rather than constant: worth exactly `F` under a composing rounding rule and zero under round-to-nearest-even at fold lengths three and four, with a second growing rule-independent slack appearing at five. So any accumulator statement derived from a capacity is an additive-only mechanism. | The first author measured the saving under truncation and read it as exactly `F`, constant. The second parameterised the rounding rule, predicted the fusion from the schedule algebra in the probe header before running, and refuted the constant at fold length five: truncation savings of 3, 3 and 4 against round-to-nearest-even savings of 0, 0 and 3. The third extended it to the signed domain on a second rounding axis, where the floor spelling shows pure fusion at exactly `F` for fold lengths three, four and five while truncation is irregular. | total_width: W = 4, fraction_width: F = 3, signedness: signedness in {unsigned, signed}, operation: operation = mul, chain_length: chain length in 2..=5, rounding: rounding in {truncate, floor, round to nearest even} | [the_multiplicative_rescale_saving_is_adaptation_fusion](PROBE.md#the-multiplicative-rescale-saving-is-adaptation-fusion) |  |  | [what_a_numeral_guarantees_to_a_fold](QUESTION.md#what-a-numeral-guarantees-to-a-fold) |  | The instruments are `60_probes/p_d_rescale_saving_is_adaptation_fusion.rs` and `62_probes/p4_signed_multiplicative_accumulator.rs`, both committed with their output. The region is the two instruments' own stated parameters, `M = 15, F = 3` for the unsigned side and `Q = [-8, 7], F = 3` for the signed, exhaustive over the representable domain at each fold length. **Two experts and the second was a blind prediction that refuted the first's constant**, which is a stronger shape than agreement. The consolidation states none of these parameters. | Nothing here is priced. No bench harness ran on any of it and every member says so. | accumulator, guard, linear, fold length, adaptation fusion, rounding, capacity, additive-only, C8 |
| <a id="a-fold-needs-a-closed-operation-and-a-separately-determined-accumulator"></a>a_fold_needs_a_closed_operation_and_a_separately_determined_accumulator | answer | measured | one_expert | [the_chain](TOPIC.md#the-chain) | A fold cannot take a widening operation, so a composition needs a closed operation together with an accumulator determined separately. The accumulator's width is not a function of the operand widths at all: it is a function of the element width and the capacity, and the capacity is a quantity that lives in the composition rather than in the numeral. The wall is the runtime trip count and nothing else, which is why the same widening operation folds fine over a static-length list. | A fold's accumulator is loop-carried, so it has exactly one type, and a widening operation gives it a different type on every iteration. Four independent formulations are refused with one diagnosis between them, and four positive arms compile in the same run, which locates the boundary at the trip count rather than at the widening. The escape is not a trick: the bound was known at compile time all along and had been classified as runtime because the count is. | toolchain: rustc 1.98.0-nightly (57d06900f 2026-05-27), edition 2021, build_profile: no feature gates, opt level 3, threads: threads any, the refusal being a type-check outcome that precedes execution | [a_widening_operation_cannot_be_folded](PROBE.md#a-widening-operation-cannot-be-folded) |  |  |  | [composition_contracts_above_the_numeral](OBLIGATION.md#composition-contracts-above-the-numeral) | One expert, and the `threads any` is argued rather than swept: no run varied the thread count, and the refusal happens before any thread exists, so it cannot vary with one. A reader wanting that dimension established by measurement should treat it as unmeasured. | It says a capacity is the second input and does not say where a consumer writes one, nor what the derivation from element width and capacity is. `35` sections 3.2 and 3.8 have both and neither is filed here. | fold, closed operation, widening, accumulator, capacity, trip count, composition, loop-carried, layer above, algorithm layer |
| <a id="no-derivation-reads-the-grid-so-a-composition-may-hold-it-at-run-time"></a>no_derivation_reads_the_grid_so_a_composition_may_hold_it_at_run_time | answer | measured | one_expert | [the_chain](TOPIC.md#the-chain) | The carrier, the stride and the fold accumulator derive to the same types across numerals differing in adjustment, bias, phase and canonical exponent, and a law bound is decided by the strategy alone at every grid. So a numeral's grid coordinates are the ones a composition may hold at run time, shared over a run, with every derivation unchanged. That is what a composition's static part is for: it supplies the quantities a numeral's own type cannot carry, because they are facts about a run rather than about a value. | Which coordinates a derivation reads is a binding-time question and nobody had asked it. A coordinate no derivation reads may move to run time for free; one a derivation does read takes the derivation with it. Type equality is asserted through a blanket implementation so a mismatch is a compile error, and three negative arms fail as required, including one whose map genuinely does read the grid and one that shows the sameness relation is not vacuous. | toolchain: rustc 1.98.0-nightly (57d06900f 2026-05-27), edition 2021, build_profile: no feature gates anywhere, main arm at exit 0, threads: threads any, the equalities being decided at compile time | [no_derivation_reads_the_numerals_grid](PROBE.md#no-derivation-reads-the-numerals-grid) |  |  |  | [composition_contracts_above_the_numeral](OBLIGATION.md#composition-contracts-above-the-numeral) | One expert. The operations do not all agree with the derivations and the split is exact: a same-grid addition reads neither the adjustment nor the canonical exponent, a multiplication reads the exponent, and the bias is in neither set. That is measured separately in the same file and is not filed here, so this row states what the derivations read and not what the operations do. | It says the grid may be held dynamically and does not say what holds it, nor what a consumer writes to say so. Whether arvo ships the container that would is a separate question its own source declines to settle. | grid, binding time, composition, run time, adjustment, bias, phase, canonical exponent, shared scale, frame of reference, stride |

### binding_time

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | instead | answers | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="the-type-carries-whatever-must-be-const-available"></a>the_type_carries_whatever_must_be_const_available | answer | normative | one_expert | [binding_time](TOPIC.md#binding-time) | The type carries whatever must be const-available to decide validity or to select a lowering, and that criterion is compelled rather than chosen: a parameter left to runtime forces the check the design's own intent forbids. | The compulsion is read off emitted code rather than argued: where the parameter is const, one path is emitted and nothing branches on it, and where it is not, the check the intent forbids is what stands in its place. |  |  | An axis that decides neither validity nor a lowering is not a member of the type on this criterion, and belongs wherever the identity criterion puts it. |  | **The entailment behind this is one expert's and is explicitly asking for a second read.** The panel's first ledger entry claimed two instances; the author of the second refused that rung on its own row, a signature concurred after verifying at source, and the corrected entry puts the entailment at one instance. Nothing about it is weakened by that, and the row carries the request rather than burying it.  The wording the correction also fixes is worth carrying: the claim is that neither build branches on the build and each emits one path, rather than that no runtime check exists, because the base build's compare-and-select is the declared saturating arithmetic and is exactly what the intent permits.  `normative` because the sentence tells the design what the type must carry. The compulsion is its reason and sits in `because`.  | The second read the rung requests has not happened. A reader treating the criterion as settled is treating a one-instance entailment as two. | type, const-available, compelled, validity, lowering, runtime check, parameter, binding time, emitted path, monomorphisation |
| <a id="each-choice-in-the-sequence-has-an-owner-and-a-resolution-time"></a>each_choice_in_the_sequence_has_an_owner_and_a_resolution_time | answer | normative | one_expert | [binding_time](TOPIC.md#binding-time) | Each choice in the sequence has an owner and a resolution time: a type, an aggregate, or the compilation, resolving at monomorphisation, or a runtime datum. The concept's per-value obligations are owed by choices resolved at monomorphisation whoever owns them; a choice owned by an aggregate makes its obligations per-aggregate; and a platform-width numeral is a target-indexed family of formats whose exclusion grounds apply only to dependence that survives to runtime. | Who owns a choice and when it resolves are the two facts that decide what a value at rest has to carry, and they are independent: an aggregate-owned choice resolved at monomorphisation owes nothing per value. The platform-width clause falls out rather than being an exception, because a target-indexed family is resolved before runtime. |  |  |  | [the_ownership_key_as_a_structural_axis](QUESTION.md#the-ownership-key-as-a-structural-axis) | One expert on the key; the platform-width half carries a provenance condition from an open question of op's. |  | ownership, owner, resolution time, monomorphisation, aggregate, platform width, target indexed, N16 |
| <a id="a-const-eval-frontier-is-a-fact-about-the-procedure-rather-than-about-the-law"></a>a_const_eval_frontier_is_a_fact_about_the_procedure_rather_than_about_the_law | answer | normative | one_expert | [binding_time](TOPIC.md#binding-time) | A const-eval frontier is a function of domain size times per-tuple cost times procedure, and a frontier cited without all three is a defect. | Three factors were discovered separately and never stated together before this unit. Domain size: the wall is a total-step budget rather than a width ceiling, so the frontier collapses along arity. Per-tuple cost: three spellings of one identical check, agreeing on the same violation count, reach three different widths, so a table's rows are a bit low for a lighter spelling. Procedure: inside a fragment with a complete test set the positive verdict at the shipped width costs a degree box rather than an exponential enumeration, so the wall is the price of semantic ignorance about the law rather than the price of a positive verdict. |  |  |  |  | `normative` because it is a rule about how a frontier number may be quoted, not a measurement. The measurements behind it are a separate row. A live workspace rule had quoted a per-bit rate without its arity, which the unit corrected: the rate is two to the power of the arity, and there is no such thing as the rate. |  | const eval, frontier, domain size, per-tuple cost, procedure, arity, guard, R4 |
| <a id="the-const-eval-frontier-collapses-along-arity-and-buys-three-bits-from-the-guard"></a>the_const_eval_frontier_collapses_along_arity_and_buys_three_bits_from_the_guard | finding | measured | one_expert | [binding_time](TOPIC.md#binding-time) | The widest exhaustively evaluable width is 19 at arity 1, 9 at arity 2, 5 at arity 3, 3 at arity 5 and 1 at arity 8, every refusal being the long-running-const-eval guard. Allowing the guard buys three bits and no more: widths 6, 7 and 8 accept at 5.85, 49.06 and 370.95 seconds, a per-bit ratio of about eight, and width 9 did not finish. | The refusals are all the same guard, so the frontier is a step budget rather than a width ceiling, and the per-bit ratio is the enumeration's own factor of eight at arity three. The asymmetry at a shipped width is the part that matters for a design: a false verdict exits at its first counterexample in half a second, while the true verdict, which is the one that licenses an arm, is refused after four and a half. | arity: arity in {1, 2, 3, 5, 8}, total_width: W in 1..=19, the widest evaluable width per arity, build_profile: default const-eval guard, and guard-allowed for the three-bit measurement, operation: operations {add, sub, mul} | [the_const_eval_frontier_by_arity](PROBE.md#the-const-eval-frontier-by-arity) |  |  | The instruments are `80_probes/p2_frontier.py`, `p2b_swept_verdict_at_shipped_width.rs` and `p5_allow_the_guard.py`, all committed. The table's rows are one bit low for a lighter per-tuple spelling, established later in the same unit, and one bit of sign-uniform operand restriction crosses eight bits: the positive verdict accepts at width 9 guard-allowed where the full set does not finish. Neither host nor toolchain is named where the consolidation carries these numbers, so the predicate has no `target_features` entry and the figures are not transferable to another machine. | No host, no toolchain pin and no optimisation level, so every timing here is a fact about one unnamed machine. | frontier, const eval, arity, guard, long_running_const_eval, width 19, asymmetry, R4 |
| <a id="the-licensed-category-is-const-available-and-four-constructions-bind-at-four-times"></a>the_licensed_category_is_const_available_and_four_constructions_bind_at_four_times | finding | enumeration | one_expert | [binding_time](TOPIC.md#binding-time) | Const-available is not one moment. Four constructions bind at four times: a crate-level const and a structural trait bound refuse on dead code under metadata-only emission; an inline const in a non-generic function refuses at code generation; and a const assertion in a generic's associated const refuses only where the instantiation is reached, so a wrong declaration in an unreached public function compiles clean at every setting. | Each construction was built and run at each setting, and the four outcomes are distinct rather than shades of one. The reachability-dependent construction is the one that matters: it is not a banned category, it is a construction that fails the functional test when it is used for a library-wide claim, and the specification that makes the split safe is one sentence, that a reachability-conditioned permission quantifies over reached instantiations while a library claim must sit at the unconditional level. | build_profile: emission in {metadata only, full codegen}, debug-assertions any, target_features: target features any | [the_four_const_available_constructions_bind_at_four_times](PROBE.md#the-four-const-available-constructions-bind-at-four-times) |  |  | Op dissolved the axis this was originally stated over, from typestate-against-trajectory to const-available-against-not, and separately refused the ranking question as policing: the requirement on a law's expression is functional, it must reach one lowered path through monomorphisation and const solving, and which construction gets it there is case by case. So this row is an enumeration of what each construction does and is **not** a ranking, and reading it as one would reinstate exactly what he refused. The bound-position spelling is refused by the compiler naming a forbidden feature, which is a fifth outcome of a different kind. What his words do not reach, and this unit did not extend them to, is whether a genuinely non-const condition may gate anything. | Whether a genuinely non-const condition may gate anything. Op's words do not reach it and the unit did not extend them. | const available, binding time, reachability, metadata, codegen, associated const, rung 0, I16, R9 |

### the_format

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | law | answers | obligation | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="arithmetic-on-a-format-factors-as-an-adaptation-of-an-exact-operation"></a>arithmetic_on_a_format_factors_as_an_adaptation_of_an_exact_operation | answer | normative | two_experts | [the_format](TOPIC.md#the-format) | Arithmetic on a format is an exact operation in an ambient domain composed with a named, total adaptation onto the representable set. The adaptation is a first-class object with its own laws; an operation that fuses one invisibly is an operation and an adaptation point wearing one name, and the model's statements apply to it only when the two are separated. | The factoring is what makes error analysis possible: the error of a computation is the composition of adaptation errors, and the adaptation error is a property the format can state. Two authors derived it cold, one at the per-operation level and one at the chain level, each committing phase one before reading the other. The negative case completes it rather than qualifying it: eager fixed-point multiplication at nonzero fraction cannot supply an exactly associative ambient operation, because the rescale is baked into every pairwise step, and the fused spelling is precisely what cannot be re-expressed as exact composition. |  |  | [multiplicative_associativity_under_wrapping](LAW.md#multiplicative-associativity-under-wrapping) |  |  | The standing carries a discount its own authors stated: both derivations draw on one numerical-analysis literature, so this is two independent instruments over one shared premise rather than two arrivals from nothing. The consolidation's own bound is that it is worth more than a read and less than two arrivals, and there is no tier between the two that the schema can express. The negative case is a structural argument from two authors rather than a proven theorem, and its author says so. |  | standard model, adapt, exact, ambient, adaptation, fused, error analysis, unfused, C1 |
| <a id="a-format-is-identified-by-its-ambient-domain-and-its-representable-set"></a>a_format_is_identified_by_its_ambient_domain_and_its_representable_set | answer | normative | two_experts | [the_format](TOPIC.md#the-format) | A format is identified by its ambient domain and its representable set. The representable set is a constant of the type: a value set that depends on other data is not a format but storage. Adaptation choice and encoding are realisation, observable in computed values and in pattern-level properties respectively, and not part of identity. | Not the representable set alone, because the same representative set under two ambient algebras is two formats, which is precisely what the wrapping question is about. Not the full four-tuple, because two's complement and offset binary denote the same sixteen values through different pattern maps and are one format filed two ways. The consequence it absorbs for free is that at a nonzero fraction width the two wrap sections compute different products, 96 of 256 class pairs at `w = 4, F = 1` rising to 168 of 256 at `F = 3`, which under this identity is the section being the representable set rather than a complication. |  |  |  | [adaptation_in_identity_or_realisation](QUESTION.md#adaptation-in-identity-or-realisation) |  | The standing is disaggregated in the source and the parts do not share it. The value-level half, that identity is denotation rather than encoding, is two experts with the same shared-literature discount as the standard model, and one of the two was read through intermediaries rather than opened by the consolidator. The refinement from the representable set alone to the pair, the dissolution of the reduction into derived structure, and the locus rider are **converged by attack and concession** rather than independently derived, which the source marks explicitly as weaker than two experts because the conceding file read rather than derived. A reader wanting the stronger half must take the value-level clause alone. | It makes the declaration arithmetic-bearing at a nonzero fraction width, which narrows an earlier deflation sentence saying the section is chosen at declaration and no cast consults a policy. That sentence is exact at `F = 0` only, and the register's wrapping entry does not yet carry the scope. | identity, ambient domain, representable set, D and Q, denotation, encoding, storage, locus, C2 |
| <a id="membership-of-the-representable-set-is-one-affine-predicate"></a>membership_of_the_representable_set_is_one_affine_predicate | answer | normative | two_experts | [the_format](TOPIC.md#the-format) | Membership is one predicate over one parameterisation: an affine slot function, a quantum per magnitude and a phase, of which integers, fixed point, scaled integers and floats are points. The phase is stated explicitly: a nonzero phase decides whether the identity adaptation ever occurs and whether the set carries an additive identity at all. | The unification was checked against textbook enumerations at 16, 16 and 47 values with both mutants detected, and subnormals fall out of a maximum with no special case. The phase is not a passive parameter: a half-step-biased grid is not closed under exact addition, no exact sum lands on it, every one sits exactly half a step away, and the grid contains neither zero nor one, so phase promotes the tie rule from a corner case to the policy deciding every sum and produces a numeral that is not even a monoid carrier. |  |  |  | [which_width_coordinates_a_consumer_writes](QUESTION.md#which-width-coordinates-a-consumer-writes) |  | The one-predicate unification carries the shared-literature discount. The necessity of the phase term does not: it is one expert's measurement, plus a concession, plus a constructive repair, and the consolidation offers it as an argument for stating phase explicitly rather than as a two-expert standing. Its evidence is that two independent instruments erred at the phase coordinate **in opposite directions**, which is a reason to name the coordinate rather than a second derivation of the claim. |  | representable set, affine, slot function, quantum, phase, subnormal, membership, grid, C3 |
| <a id="the-adaptation-slot-is-derived-and-a-strategy-selects-a-member-per-operation"></a>the_adaptation_slot_is_derived_and_a_strategy_selects_a_member_per_operation | answer | normative | one_expert | [the_format](TOPIC.md#the-format) | Given the identity, the space of total reductions onto the representable set is derived, not chosen; a strategy selects a member per operation, which is where the strategies changing what the correct answer is attaches. Members classify along two independent law roles: the adaptation laws face the source and are what error and order transport consume; coherence faces the target and decides whether the member computes exactly in an induced algebra. All four combinations occur. | The space of total reductions is a derived mathematical object the way the ulp function is derived, and nothing about a format selects members of it. What selects a member per operation is the strategy layer, which is op's own I9 landing at the position he put it. Neither law family subsumes the other, so expelling wrapping while retaining saturation has no criterion that does not empty the slot, and the domain filing of wrapping was withdrawn by its own proposer. |  |  | [coherence_of_a_reduction_onto_its_induced_operation](LAW.md#coherence-of-a-reduction-onto-its-induced-operation) | [where_wrapping_lives](QUESTION.md#where-wrapping-lives) |  | The slot-is-derived claim is one author's. The two-family inhabitation beneath it is a separate measurement by a different author with an independent instrument, and it is carried as the law row this points at. Nothing presumes a strategy count, which is what I1 requires. |  | adaptation slot, reduction, derived, strategy, law roles, coherence, ulp, I9, C4 |
| <a id="the-format-concept-carries-three-things-upward-and-compositions-owe-their-own-laws"></a>the_format_concept_carries_three_things_upward_and_compositions_owe_their_own_laws | answer | normative | one_expert | [the_format](TOPIC.md#the-format) | The format concept supplies the layers above with the width algebra of exact results, the named adaptation carrying its rounding rule, its overflow member and its granularity function, and the exactness predicate naming the conditions under which nothing is lost. Compositions over formats, stored pairs, intervals, error-carrying values, are not format instances; they consume these three things and owe their own laws. | The list is not optional because of the statability argument: a layer that is not handed the width algebra, the named adaptation and the exactness predicate cannot state its own error behaviour at all. Stored-pair rationals, intervals, error-carrying pairs and complexes are compositions over formats rather than format instances, uncontested through seven subsequent files of the unit. |  |  |  |  | [composition_contracts_above_the_numeral](OBLIGATION.md#composition-contracts-above-the-numeral) | One expert, uncontested rather than corroborated. Absence of contest is not a second instance, and the consolidation does not claim it is. |  | width algebra, exactness predicate, granularity, composition, interval, rational, complex, C10 |
| <a id="a-nonzero-phase-leaves-the-representable-set-without-an-additive-identity"></a>a_nonzero_phase_leaves_the_representable_set_without_an_additive_identity | finding | measured | one_expert | [the_format](TOPIC.md#the-format) | A half-step-biased grid is not closed under exact addition. No exact sum of two grid points lands on the grid, every one sits exactly half a step away, and the grid contains neither zero nor one. So a nonzero phase decides whether the identity adaptation ever occurs for addition, promotes the tie rule from a corner case to the policy that decides every sum, and produces a numeral that is not even a monoid carrier. | Measured exhaustively over every pair of grid points, with the affine membership predicate cross-checked against an independent enumeration and a mutant with the bias dropped detected. The repair is contained in the representable set and costs the adaptation slot nothing: rounding to nearest onto the biased grid keeps totality, retraction, monotonicity and distance minimisation, all four, while a mutant rounder targeting the phase-zero grid fails retraction. | total_width: W = 4, fraction_width: F = 0, signedness: signedness = unsigned, operation: operation = add, arity: arity = 2, rounding: rounding = nearest, against a phase-zero mutant | [a_half_step_biased_grid_is_not_closed_under_addition](PROBE.md#a-half-step-biased-grid-is-not-closed-under-addition) |  |  |  | The instrument is `56_probes/q2_affine_membership.rs` with its output beside it. `W = 4` is the instrument's own, stated as sixteen representable values and 256 pairs, rather than an inference from the pair count. The consolidation carries the counts and no width. This is the argument for the canon stating the phase term explicitly rather than leaving it to be inferred, and its evidence is that two independent instruments erred at this coordinate in opposite directions. |  | phase, bias, grid, additive identity, monoid, tie rule, affine, nearest, C3 |

### the_number_system

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | law | instead | answers | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="the-numeral-concept-is-a-dependent-sequence-of-choices"></a>the_numeral_concept_is_a_dependent_sequence_of_choices | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | The numeral concept is a dependent sequence of choices, not a tuple of independent ones: an ambient domain, a representable set over it, a reduction from the space that pair derives, an encoding of the set, a container for the encoding. Each choice ranges over a set the earlier choices determine, the components range over relations with the function case ordinary, and the dependency runs one way. | A tuple of independent choices cannot say that the space of reductions is derived from the pair above it, which is the fact the format topic reached separately, and it cannot say that a redundant encoding makes a relation rather than a function. The sequence says both, and the one-way dependency is what lets a sentence name the prefix it quantifies over. |  |  |  |  | [is_the_ambient_operation_family_fixed](QUESTION.md#is-the-ambient-operation-family-fixed) | One expert with two instruments, which is one author's two arrivals rather than two authors'. The word `format` collides here: two units use it for different prefixes of this same sequence, and the collision is unresolved and is op's naming call. |  | sequence, dependent, coordinates, ambient domain, representable set, reduction, encoding, container, N1 |
| <a id="derivation-is-completion-of-the-sequence-by-the-typestate"></a>derivation_is_completion_of_the_sequence_by_the_typestate | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | The consumer supplies a prefix and the typestate completes the sequence; validation checks that the completion satisfies what the prefix demanded; erasure discards the completion's evidence and keeps its consequences. A completion may branch, and the branches are what op's plural names. | It is the sequence read as a mechanism rather than as a taxonomy, and the branching clause is what makes op's plural, that one demand derives representations rather than a representation, a consequence of the shape instead of a separate requirement. |  |  |  |  |  | One expert, read together with the split of the validate verb into two acts. |  | derivation, completion, typestate, prefix, erasure, branch, plural, N3 |
| <a id="a-crossing-carries-two-relations-and-a-verdict-per-law-family"></a>a_crossing_carries_two_relations_and_a_verdict_per_law_family | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | A value crosses between two systems along the coordinates at which they differ, one class per coordinate. A crossing carries a relation on values, a relation on patterns, and a verdict per law family, and none determines another; the two relations need not even be the same kind of thing, since a redundant encoding makes the pattern relation a section and a retraction rather than a function, and a named section is then owed by neither endpoint. | Five crossing classes fall out of the sequence, one per coordinate, and the three carried things are independent because each is decided at a different depth. The redundant-encoding case is what forces the pattern relation to be more general than a function, and it leaves an owner unassigned, which is a real gap rather than a curiosity. |  |  |  |  | [does_the_canon_name_crossing_classes](QUESTION.md#does-the-canon-name-crossing-classes) | One expert. | Who owns a named section when a redundant encoding makes the pattern relation a section and a retraction is stated and not answered. | crossing, conversion, relation, pattern, law family, redundant encoding, section, retraction, N4 |
| <a id="a-crossing-preserves-an-operation-exactly-when-it-moves-no-coordinate-that-operation-reads"></a>a_crossing_preserves_an_operation_exactly_when_it_moves_no_coordinate_that_operation_reads | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | A crossing preserves the value-level operation exactly when it moves no coordinate that operation reads, which is at the encoding and at the container and nowhere earlier. | True by construction over the concept's own coordinates: an operation reads the ambient domain, the representable set and the selected reduction, so a crossing that moves only the encoding or the container cannot change what it computes, and one that moves anything earlier can. The source says it therefore holds at any width in any language, which is what a canon sentence needs. |  |  |  |  |  | `normative` is the porter's call rather than the source's word: the sentence is offered as true by construction over stipulated definitions, so there is nothing an instrument could refute and no region to state, and if the concept's coordinates change the sentence is voided rather than falsified. A reader preferring to treat it as an established universal should note that the axes it ranges over, the coordinates of the dependent sequence, are not `dimension` rows, so it could not be predicated in this registry either way. It is the corrected form of an earlier universal the unit refuted. |  | crossing, preservation, universal, encoding, container, coordinate, corrected, N5 |
| <a id="meaning-is-decided-by-the-first-three-coordinates-and-cost-by-the-last-two"></a>meaning_is_decided_by_the_first_three_coordinates_and_cost_by_the_last_two | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | What a crossing means is decided by the ambient domain, the representable set and the selected reduction on both sides. What it costs is decided by the encoding and the container on both sides, and those two can never change what it computes. | It is the previous sentence read as a partition rather than as a test, and it is what scopes every other claim in the topic: a claim about meaning and a claim about cost are claims about disjoint halves of the sequence, so neither transfers to the other. |  |  |  |  | [does_the_canon_name_crossing_classes](QUESTION.md#does-the-canon-name-crossing-classes), [does_the_canon_speak_about_cost](QUESTION.md#does-the-canon-speak-about-cost) | One expert, and the unit's own files name it the sentence most wanting attack, because everything else in the topic is scoped by it. It has not had a second derivation. |  | two depths, meaning, cost, partition, scope, crossing, N6 |
| <a id="a-crossings-preservations-are-the-two-law-families"></a>a_crossings_preservations_are_the_two_law_families | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | A crossing preserves order exactly when the reduction it adapts through is monotone, and preserves operations exactly when that reduction is coherent; the crossing is the two families' third consumer beside order transport and reassociating folds. | The two law families were already established by the format topic as facing the source and the target respectively, and a crossing consumes exactly those two properties of the reduction it adapts through, so the pairing is forced rather than chosen. |  |  | [coherence_of_a_reduction_onto_its_induced_operation](LAW.md#coherence-of-a-reduction-onto-its-induced-operation) |  |  | One expert on the pairing; the families themselves are the format topic's and are carried as a law row. `normative` is the porter's call, on the same reading as the crossing universal above. |  | crossing, order transport, monotone, coherent, law families, fold, N7 |
| <a id="an-order-is-named-exactly-where-a-crossing-is-lossy"></a>an_order_is_named_exactly_where_a_crossing_is_lossy | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | A composite crossing that loses nothing is determined by its endpoints. Where it loses, it is determined by its endpoints together with an order, the two candidate orders being the source's policy governing the loss and the target's, and the typestate cannot break the tie. An order must be named exactly where a crossing is lossy, and nowhere else. | Composition of lossless crossings commutes with the endpoints, so nothing extra is owed. Composition of lossy ones does not, and the two candidate orders give different answers, so something outside the typestate has to say which. Naming an order where a crossing is lossless would be a knob with no content. |  |  |  |  | [when_is_an_order_owed_at_a_crossing](QUESTION.md#when-is-an-order-owed-at-a-crossing) | One expert on each half. Whose order it is remains a located disagreement with three coherent positions, none forced by anything measured, and it is explicitly op's. | Whose reduction governs a lossy crossing, the source's, the target's, or one named at the crossing site, is unresolved and is op's. | lossy, crossing, order, composition, endpoints, schedule, D6, N8 |
| <a id="conversion-and-resolution-are-one-obligation-at-two-arities"></a>conversion_and_resolution_are_one_obligation_at_two_arities | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | Moving one value into a declared system and combining several values from disagreeing systems are one obligation at two arities: both name a common ambient domain, a target representable set, and which reduction governs the loss, and both are schedules, the number and position of adaptation points being part of what the expression means. | The three things owed are identical in both cases, and the only difference is how many values arrive, so treating them as two mechanisms duplicates the obligation and lets the two drift. The schedule clause is what connects it to the chain concept: adaptation points are part of the meaning at any arity. |  |  |  |  | [is_interoperation_conversion_or_resolution](QUESTION.md#is-interoperation-conversion-or-resolution) | The split, and its correct half, that resolution needs a rule rather than a conversion function, was posed by one file and credited by another; the two-arity unification and the schedule content are the second file's. One expert on the unification. |  | conversion, resolution, arity, schedule, adaptation points, common ambient, N9 |
| <a id="membership-and-hosting-are-two-questions"></a>membership_and_hosting_are_two_questions | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | Whether something is a number system and whether this implementation can carry one are different questions. The first is about structure and is answered by locating the candidate on the chain of choices; the second is about residue at runtime and is answered by what a value at rest may carry. Neither enumerates, both are open, and a system the implementation cannot host is still a system. | Conflating them makes the concept's boundary a fact about one implementation's runtime, which would move every time the implementation did. Keeping them apart is what lets the concept be closed while the inventory stays open. |  |  |  |  | [one_word_or_two_for_is_a_number_system](QUESTION.md#one-word-or-two-for-is-a-number-system) | One expert on the split; the hosting half is a different author's and is conditional on an open question of op's. |  | membership, hosting, admission, boundary, residue, runtime, N10 |
| <a id="a-system-exposes-its-ambient-laws-its-set-and-its-reductions-verdicts"></a>a_system_exposes_its_ambient_laws_its_set_and_its_reductions_verdicts | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | A system exposes its ambient domain together with that domain's own law inventory, its representable set, and its selected reduction with that reduction's verdicts, including that the reduction moves no value already representable. The reduction's verdicts carry no information alone: the properties a consumer relies on are conjunctions of an ambient law and a reduction verdict, and a system that names its own computed algebra as its ambient domain satisfies every verdict while voiding every conclusion. | The self-collapsed system is the counterexample that forced the ambient law inventory into the exposure list: it passes a verdict-only test while computing nothing, because with the computed algebra as its own ambient every reduction verdict is trivially true. So a verdict is only worth anything conjoined with the ambient law it is a verdict about. |  |  |  |  | [what_the_admission_contract_asks_a_candidate_to_expose](QUESTION.md#what-the-admission-contract-asks-a-candidate-to-expose), [is_the_ambient_operation_family_fixed](QUESTION.md#is-the-ambient-operation-family-fixed) | One expert. It supersedes the sufficient direction of an earlier exposure test while keeping that test's necessary direction whole. The refutation that forced it is a row of its own, `an_exposure_test_over_reduction_verdicts_alone_is_satisfied_by_a_system_that_computes_nothing`, and it carries the region its instrument states. |  | exposure, admission test, ambient laws, verdict, self-collapsed, refuted, sufficient, N11 |
| <a id="a-closed-ambient-operation-makes-the-reduction-the-identity"></a>a_closed_ambient_operation_makes_the_reduction_the_identity | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | Where an ambient operation is closed on the representable set, no adaptation is possible and the reduction is the identity. Such a system exposes everything the concept asks, needs none of the adaptation machinery, and is a member for that reason rather than in spite of it. | Closure leaves the space of total reductions with one member, so there is nothing for a strategy to select and nothing for a verdict to be about. Reading such a system as a degenerate case would put the concept's simplest members outside it. |  |  |  |  |  | One expert with a construction. |  | closure, identity reduction, magnitude-free, member, degenerate, N12 |
| <a id="the-concepts-edge-is-not-an-order-and-wrapping-is-the-test"></a>the_concepts_edge_is_not_an_order_and_wrapping_is_the_test | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | The concept's edge is not an order and not a magnitude: order-compatibility is a property of the selected reduction, varies within a single family the design must contain, and groups a wrapped integer with a bit vector. A wrapped numeral satisfies fewer of the properties a numeral is expected to satisfy than almost anything else the concept contains, and it sits in the kernel on op's four-times-restated I3; so any clause proposed as a boundary of the concept is checked against wrapping before anything else. | The discriminator was measured empty: over every total order on the carrier, wrapping addition and xor over a bit vector admit zero compatible orders each, while saturating addition and minimum admit some. So an order-shaped boundary groups a kernel item with the candidate the narrow reading exists to exclude, and splits wrapping from saturating inside one kernel item. |  |  | [existence_of_a_translation_invariant_total_order](LAW.md#existence-of-a-translation-invariant-total-order) |  | [is_number_system_broad_enough_for_non_magnitude](QUESTION.md#is-number-system-broad-enough-for-non-magnitude) | One expert. The measurement is carried as a law row rather than here, because the law has a region and the boundary claim does not. |  | boundary, order, magnitude, wrapping, kernel, I3, GF(2), test, N13 |
| <a id="admission-returns-a-coordinate-rather-than-a-verdict"></a>admission_returns_a_coordinate_rather_than_a_verdict | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | Asking whether a candidate is a number system is usually the wrong question. The useful question is which choice in the chain the candidate fixes and whose it is, because most things that are not systems are not outside the concept: they are choices at a later coordinate, and an encoding, a container, a stride and a housing are all answers rather than rejections. | Every dispute the unit had about admission dissolved into a dispute about an address. A candidate that looked like a rejection turned out to be a coordinate somebody had not named, so the verdict question was producing arguments where the address question produced answers. |  |  |  |  | [is_admission_a_predicate_or_a_location](QUESTION.md#is-admission-a-predicate-or-a-location) | One expert, composing the sequence, the ownership key and the crossing classes rather than adding a new claim. |  | admission, coordinate, address, verdict, encoding, container, stride, N14 |
| <a id="the-concept-commits-to-its-choices-and-to-no-count-of-levels"></a>the_concept_commits_to_its_choices_and_to_no_count_of_levels | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | Identity and quantification cut the concept along its dependency order; compatibility and crossings cut it by preserved observables. Both partitions are real and neither refines the other. The concept commits to its choices and their order and to no count of levels: a level is a partition class relative to the observables a client brings, and a cut earns a name by having a caller. | Two instruments produced two partitions that do not refine each other, so neither is the partition and a count of levels would be a count of one of them. A level with no caller is a name for a class nobody distinguishes. |  |  |  |  | [are_the_level_hierarchies_the_same_cut](QUESTION.md#are-the-level-hierarchies-the-same-cut) | One expert on the joint statement. It is the same shape as I1's refusal to presume a strategy count, reached in a different topic and without citing it. |  | levels, partition, observables, client, count, cut, N15 |
| <a id="one-container-hosts-many-systems-so-the-canon-types-the-system"></a>one_container_hosts_many_systems_so_the_canon_types_the_system | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | One container hosts many systems at once, arithmetic and non-arithmetic, and the canon types the system, never the box. | The container is the last coordinate of the sequence and everything above it varies freely underneath one choice of it, so typing the box types nothing a consumer relies on. |  |  |  |  |  | One expert with a third blind instrument on the arithmetic cells. The sentence is stated over the sequence's coordinates rather than over either unit's use of the word `format`, which is the vocabulary caution the unit attached to it, and the word collision is unresolved. |  | container, box, hosting, typing, format collision, vocabulary, N19 |
| <a id="the-concept-is-closed-and-the-inventory-is-open"></a>the_concept_is_closed_and_the_inventory_is_open | answer | normative | two_experts | [the_number_system](TOPIC.md#the-number-system) | The canon defines once what a number system is and what admission requires; the set of admitted instances is open, and a new one earns admission by supplying the concept's obligations rather than by amending the canon. The kernel derived from the stated intents is carried as guaranteed; everything else is admitted by contract and decided by measurement. | A closed inventory would put every new numeral through a canon amendment, and an open concept would make admission mean nothing. Closing the concept and opening the inventory is what makes admission a check rather than a negotiation. |  |  |  |  | [is_the_number_system_inventory_open](QUESTION.md#is-the-number-system-inventory-open) | Two instruments over one premise set, plus the format topic's prior text carried through the reconciliation, so the discount is the same shape as the format topic's shared-literature one. The guarantee clause inherits the intents' STATED status: it is aligned with op's stated direction, is not settled by it, and firms only when those intents are ratified. |  | closed concept, open inventory, admission, kernel, contract, guaranteed, N20 |
| <a id="roles-derive-representations-and-a-realisation-variant-computes-nothing-new"></a>roles_derive_representations_and_a_realisation_variant_computes_nothing_new | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | A demand derives representations, plural, keyed by role; the roles named so far differ in who re-establishes the invariant. A role that moves nothing before the encoding is a realisation variant and cannot change what anything computes; whether every proposed role is of that kind is exactly the open question of whether a compute role may widen the representable set, and a chain's extent is not a role at all but a schedule. | The realisation-variant clause follows from the two-depths partition: a role moving nothing before the encoding moves only cost coordinates. Whether the role set is homogeneous under that reading turns entirely on the one case that would break it, a compute role widening the representable set, which is a live and unresolved register entry. |  |  |  |  |  | One expert per part, and the sentence passes its equivalence test conditionally, naming its own open condition rather than resolving it. | Whether the role set is homogeneous, which turns on whether any role may widen the representable set before the encoding. | roles, storage, compute, interchange, realisation variant, widen, schedule, D3, N21 |
| <a id="every-dispute-in-the-number-system-topic-was-a-dispute-about-an-address"></a>every_dispute_in_the_number_system_topic_was_a_dispute_about_an_address | answer | normative | one_expert | [the_number_system](TOPIC.md#the-number-system) | Every question this unit disputed was a dispute about an address: which coordinate of the sequence a claim lives at, and who owns that coordinate. A canon sentence about numerals therefore carries its address, the prefix it quantifies over and the owner that resolves it, and a candidate for admission, a crossing, a role and a level are all answered by giving an address rather than a verdict. | It is offered as a composition of four earlier sentences in the same topic, the quantifier rule, the admission-returns-a-coordinate rule, the two-instruments-no-count rule and the ownership key, rather than as a new claim. |  |  |  |  |  | **The consolidator's own contribution, marked as such by its author and offered for attack rather than adoption.** On the provenance ladder that is the most suspect tier available: an agent's own call with no human and no second derivation. It is a composition rather than a claim, so if any of the four sentences it composes falls, it falls with them. |  | address, coordinate, owner, composition, consolidator, attack, N23 |
| <a id="an-exposure-test-over-reduction-verdicts-alone-is-satisfied-by-a-system-that-computes-nothing"></a>an_exposure_test_over_reduction_verdicts_alone_is_satisfied_by_a_system_that_computes_nothing | refusal | measured | one_expert | [the_number_system](TOPIC.md#the-number-system) | A test that asks a candidate only for its reduction's verdicts is satisfied by a system that names its own computed algebra as its ambient domain. Such a system reports coherence and monotonicity holding everywhere while computing nothing, and it does so even where the honest declaration of the same arithmetic reports 322 coherence failures and 952 associativity failures. The sufficient direction of the exposure test is refuted; the necessary direction survives untouched. | Collapsing the ambient domain onto the computed algebra makes every reduction verdict trivially true, because there is nothing left for the reduction to be a homomorphism away from. Measured against the honest declaration on four reductions at once: the collapsed form reports zero coherence failures and zero monotonicity failures on all four, including the signed saturating one whose honest form fails coherence on 322 of 961 pairs, and it agrees with the honest form as a function in every case, so nothing about the computed answers distinguishes them. The control is what makes the refutation a refutation rather than an observation: a separate instrument asks whether the collapsed verdict can be made to fail at all, and it cannot. | total_width: W = 4, fraction_width: F = 0, signedness: signedness in {unsigned, signed}, overflow_policy: overflow policy in {wrap, saturate}, operation: operation = add, arity: arity = 3 | [the_collapsed_declaration_cannot_be_made_to_fail](PROBE.md#the-collapsed-declaration-cannot-be-made-to-fail) |  | A system exposes its ambient domain together with that domain's own law inventory, so that the properties a consumer relies on are conjunctions of an ambient law and a reduction verdict rather than verdicts alone. | [what_the_admission_contract_asks_a_candidate_to_expose](QUESTION.md#what-the-admission-contract-asks-a-candidate-to-expose) | The instruments are `73_probes/p1_collapse.py` and `p1c_collapse_cannot_fail.py`, committed with their output. This row was in `182` section 5.1's first draft as a construction with no region over any declared axis, on the strength of the consolidation stating none. The instrument states `W = 4` in its second line and enumerates its four reductions in its table, which is the sixth recovery of the second pass. |  | admission, exposure test, self-collapsed, ambient domain, refuted, sufficient, 322, 952, N11 |

### canon_form

| id | kind | sentence_kind | standing | topic | says | because | note | keywords |
|---|---|---|---|---|---|---|---|---|
| <a id="every-canon-sentence-names-the-prefix-it-quantifies-over"></a>every_canon_sentence_names_the_prefix_it_quantifies_over | answer | normative | one_expert | [canon_form](TOPIC.md#canon-form) | Every canon sentence about numerals names the prefix of that sequence it quantifies over. A property decided by the identity, one decided by the identity with its selected reduction, and one decided by the encoding are three different claims, and stating one at the wrong depth is how a true sentence becomes false. | The unit recorded five separate failures of exactly this kind in its own files, and the consolidation rests the sentence on them rather than on an argument. It is the same discipline the predicate notation imposes on regions, applied to the structural coordinates instead of the measurement axes. | One expert. This sentence and the predicate notation are two halves of one discipline over two disjoint sets of axes, and neither names the other. The structural coordinates it quantifies over are not `dimension` rows, so no predicate in this registry can express a depth. | quantifier, prefix, depth, canon sentence, scope, N2 |

### validation

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | law | answers | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="validation-is-two-acts-keyed-by-boundary-and-it-validates-maps"></a>validation_is_two_acts_keyed_by_boundary_and_it_validates_maps | answer | normative | one_expert | [validation](TOPIC.md#validation) | At derivation, compile-time, per type: the declared representation is checked through its executable maps, exhaustively at a model width. At ingest, runtime, per datum: bits arriving without their construction history pass a membership door that is a pure function of the type's parameters and the bits. Between doors nothing checks and nothing needs to. A property a representation declares about itself counts only where a check ties it to the maps; a declared constant nothing reads is a comment with a type. | The two acts have different units, different times and different subjects, so one mechanism cannot serve both. The last clause is the same failure the derived-laws topic found one coordinate up, where an author-written law marker is a declaration checked by nothing, and it is the reason a declaration has to be tied to the maps rather than trusted. |  |  |  |  | One expert, and it contains an open question of op's: whether both acts are wanted, and where. | Whether both acts are wanted, and where, is op's and is unanswered. | validation, derivation, ingest, membership door, declared constant, maps, model width, N17 |
| <a id="erasures-guarantee-is-a-conditional-with-a-stated-base"></a>erasures_guarantee_is_a_conditional_with_a_stated_base | answer | normative | one_expert | [validation](TOPIC.md#validation) | What survives erasure is the theorem that reachable values of a type satisfy its predicate, conditional on a closed construction perimeter, validated operations, the model-width transfer proviso, and the language's own guarantees; the canon lists these, because a guarantee whose perimeter is unstated is not one. Stored bits are not self-describing: one format hosts many systems, so after erasure a value witnesses its system only through its type, interchange carries the system identification out of band, and canonicity at interchange is necessary but not sufficient. | Each of the four conditions is separately defeasible and each has been defeated somewhere in this panel, the model-width transfer proviso most sharply: the derived-laws topic constructed families placing a law's truth flip exactly at the shipped width, so that proviso is satisfiable by a law that is false where it ships. |  |  | [the_falling_factorial_family_vanishes_modulo_two_to_the_width](LAW.md#the-falling-factorial-family-vanishes-modulo-two-to-the-width) |  | One expert. The model-width transfer proviso this sentence lists as a condition was subsequently defeated by construction in another topic, in both fragments, so a reader taking the list as a set of conditions to satisfy should know that one of them is not sufficient even when satisfied. |  | erasure, guarantee, perimeter, transfer proviso, interchange, canonicity, self-describing, N18 |
| <a id="a-law-stated-as-an-author-written-marker-is-checked-by-nothing"></a>a_law_stated_as_an_author_written_marker_is_checked_by_nothing | finding | measured | two_experts | [validation](TOPIC.md#validation) | A law stated as an author-written marker is a declaration checked by nothing. Two overflow policies declare one associativity marker over a four-bit signed window, one declaration is false, the compiler raises nothing, and the licensed reassociating consumer returns a different answer on 16,268 of 65,536 vectors. | The marker is a name the author writes and no path ties it to the policy's own map, so a false declaration is indistinguishable from a true one at every stage of compilation. The repair works: computing the permission from the policy's own map makes the false instantiation a const-evaluation error. A second author built the same mechanism independently, including the refused inline-const spelling that the compiler answers by naming a forbidden feature. | total_width: W = 4, fraction_width: F = 0, signedness: signedness = signed, overflow_policy: overflow policy in {wrap, saturate}, operation: operation = add, arity: arity = 4, threads: threads = 1, target_features: target features any | [a_declared_law_marker_compiles_clean_when_false](PROBE.md#a-declared-law-marker-compiles-clean-when-false) |  | [how_the_law_inventory_is_named](QUESTION.md#how-the-law-inventory-is-named) | The predicate is the source's own, verbatim, and it is one of only two in the whole derived-laws consolidation. The instruments are `80_probes/p1a_declared_law_lies.rs` and `p1b_computed_law_refuses.rs`, both committed, and `probe.toml` does not exist for them to be named in. The 24.8% figure has a zero arity-2 control, so the number is not an artefact of the sweep. |  | marker, declaration, checked by nothing, 16268, 24.8, E0080, computed permission, R3 |
| <a id="a-region-whose-arms-may-differ-is-validated-arm-by-arm-against-its-own-semantics"></a>a_region_whose_arms_may_differ_is_validated_arm_by_arm_against_its_own_semantics | answer | normative | one_expert | [validation](TOPIC.md#validation) | A region whose arms may produce different answers is validated arm by arm, each against its own declared semantics, rather than arm against arm. Cross-arm agreement is then a consequence where the semantics coincide, and its absence is not a defect where they do not. | One family in the corpus already carries the mechanism: two oracles, one per arm, and none across arms, with one arm checked against native hardware bit for bit and the other checked against the **definition** rather than against the other arm, because no silicon exists to check it against. Both neighbours are tested in exact integer arithmetic, ties are checked separately, and the check count itself is asserted so a shortened sweep fails rather than passes. |  |  |  |  | One expert. `normative` because it states how validation is to be done rather than reporting a measurement; the measurement that motivates it is the previous row. |  | validation, oracle, per arm, declared semantics, cross-arm, agreement, radix |

### arvo_identity

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | instead | supersedes | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="arvo-owes-laws-as-checkable-facts-and-not-as-a-rewriting-engine"></a>arvo_owes_laws_as_checkable_facts_and_not_as_a_rewriting_engine | refusal | normative | one_expert | [arvo_identity](TOPIC.md#arvo-identity) | arvo owes algebraic laws as a vocabulary of checkable facts, never as a rewriting engine that acts on them. | The facts belong in arvo because only arvo knows its own axis values. The decision to act on a fact belongs to whoever performs the rewrite, and a foundation layer that acts on its own facts has made a policy choice on the consumer's behalf. |  |  | arvo states the verdict and the region it holds in; the consumer, the build layer or the code generator decides whether to rewrite on it. |  | Reached inside the topic's own existence-and-locus half. Op refused the engine relocation directly, and that refusal is recorded in the droplist rather than here; this row is the panel's statement of the same shape and does not carry his authority. |  | locus, rewriting engine, vocabulary, checkable facts, toolbox, policer, R1 |
| <a id="a-min-plus-computation-needs-monotonicity-as-well-as-an-absorbing-top"></a>a_min_plus_computation_needs_monotonicity_as_well_as_an_absorbing_top | finding | measured | one_expert | [arvo_identity](TOPIC.md#arvo-identity) | A min-plus shortest path needs two properties from the numeral's addition and having one of them is not most of the way to having both. It needs a top that absorbs, so infinity behaves, and it needs monotonicity, so extending a path never shortens it. The two are separable and a numeral can hold either alone. Measured over three overflow policies on identical instance sets: wrapping fails both and is wrong on 48.08% of 621,961,320 in-range instances; saturation holds both and is wrong on none of them; and a numeral wrapping in its interior below a reserved absorbing top holds absorption at 0 of 16 cells, fails monotonicity at 560 of 2176 triples, and is still wrong on 12.57% of the same instances. | The relaxation's greedy argument rests on a path never getting shorter by being extended, which is monotonicity and not absorption. Absorption only makes the sentinel behave. So a shape buying the sentinel alone buys about three quarters of the improvement and gets the answer wrong on one instance in eight, which is a different thing from getting it nearly right. | total_width: W in {3, 4}, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy in {wrap, saturate, wrapping below a reserved absorbing top}, operation: operation = add, arity: arity = 2, threads: threads = 1 | [monotonicity_and_absorption_are_two_properties_and_a_reserved_top_buys_one](PROBE.md#monotonicity-and-absorption-are-two-properties-and-a-reserved-top-buys-one) |  | [a_min_plus_fold_needs_an_absorbing_top_and_wrapping_supplies_none](PROPOSAL.md#a-min-plus-fold-needs-an-absorbing-top-and-wrapping-supplies-none) | **The absorption half was filed and this half was not, and the pair is the point.** The canon carried a row establishing that the top absorbs under saturation and never under wrapping, whose own `gap` field said monotonicity was measured in the same file and not filed. So the canon stood at exactly the position this measurement rules out: absorption established, monotonicity absent, which is the reserved-top row of the table and is wrong 12.57% of the time. It does not supersede that row's content, which stands; it supersedes the state of having only that row. Reported by a seat replying to an attack on a neighbouring derivation, which found the gap belonged to neither party in the argument. | Unsigned integers only, at two widths, on one thread, over one graph shape of four nodes and six edges. Whether the same separation holds signed, at a non-zero fraction width, or on a larger graph is not established here. The reserved-top policy is not a shipped shape and is in the predicate because it is the only measured point holding one property without the other. | monotonicity, absorbing top, min-plus, shortest path, relaxation, reserved top, wrapping, saturation, two properties, semiring, infinity |

### panel_conduct

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | law | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="an-instrument-is-mutated-and-the-battery-is-made-to-notice"></a>an_instrument_is_mutated_and_the_battery_is_made_to_notice | answer | normative | one_expert | [panel_conduct](TOPIC.md#panel-conduct) | Mutate the validated thing and demand the battery notice. Assert the battery's own non-degeneracy inside the instrument. Commit first runs with notes rather than replacing them. | Four instruments in one unit failed in one repeated way and nobody's intention was at fault: none of the four authors asked what the instrument would say if the thing it validates were broken. One battery contained no law of true per-variable degree above one, so the interesting branch of its criterion had zero instances. Another could not distinguish the procedure it tested from one with the entire breakpoint apparatus deleted, which a later mutant established by changing not one verdict. A third measured nothing on its first run because every swept law was false and both sides exited early. Beside all of it the reproduction chain never broke and every rerun matched to the digit, so **every defect found in this unit was in what an instrument could not see, never in whether it reproduced.** |  |  |  | `normative` because it is the discipline rather than the observation; the four instances are enumerated in `182` as a finding with no region over any declared axis. One further hazard is worth carrying verbatim: the toolchain resolves from the working directory, so a probe run in a scratch directory silently uses the wrong toolchain and nothing in the transcript says so. |  | control, mutant, instrument, non-degeneracy, first run, reproduction, toolchain, R13 |
| <a id="one-measured-count-was-attributed-to-the-wrong-operation-in-two-topics-and-corrected-in-both"></a>one_measured_count_was_attributed_to_the_wrong_operation_in_two_topics_and_corrected_in_both | finding | argument | cross_topic | [panel_conduct](TOPIC.md#panel-conduct) | One measured count, 952 divergent triples, was independently mis-attributed to the wrong operation in two different topics and corrected in both. Its true owner is signed saturating addition, measured directly at 4096 triples, and not signed saturating multiplication. | The number-system topic corrected one member file's attribution of it to multiplication; the strategy topic later cites the corrected figure as a load-bearing count in its own rationalisability work and separately establishes that this exact number is the whole gap between two rationalisability counts, reducing to a single tie between two arms. It is one number, correctly re-derived and re-used across two topics after being wrong in between, and a reader meeting it in only one consolidation would not see that it was ever wrong. | total_width: W = 4, fraction_width: F any, signedness: signedness = signed, overflow_policy: overflow policy = saturate, operation: operation = add, arity: arity = 3 |  | [additive_associativity_under_saturation](LAW.md#additive-associativity-under-saturation) | Carried because the correction is the finding rather than the count. A count cited without its operation is the same defect class the derived-laws topic named for a frontier number cited without its three factors, arrived at independently in a different topic. |  | 952, attribution, operation, addition, multiplication, correction, cross topic, 4096 |
| <a id="the-four-consolidations-contradict-each-other-nowhere"></a>the_four_consolidations_contradict_each_other_nowhere | finding | enumeration | one_expert | [panel_conduct](TOPIC.md#panel-conduct) | No contradiction was found among the four consolidations' candidate canon text or stated findings. The closest thing to one is internal to a single topic rather than between topics, and two pairs of measurements are close enough in shape to invite conflation without contradicting. | The internal case is the strategy topic's own section claiming a weighting component ranges over answer-equivalent arms, against its own later section finding a fidelity coordinate entirely absent from the corpus; the same file's later repair resolves it by fixing the first claim rather than the second. The conflatable pair is the format topic's signed saturating **multiplication** associativity counts, 28, 160, 780 and 3516 triples at widths 3 through 6, against the strategy topic's signed saturating **distributivity** counts, 47.72% at width 7 and 34.52% at width 6: two different laws, two different instrument sets, two different points in the panel's timeline. Neither supersedes the other and **neither was checked against the other by either consolidation**, because they are not the same claim. | total_width: W in 3..=7, fraction_width: F = 0, signedness: signedness = signed, overflow_policy: overflow policy = saturate, operation: operations {add, mul}, arity: arity = 3 |  | [multiplicative_associativity_under_saturation](LAW.md#multiplicative-associativity-under-saturation), [distributivity_of_multiplication_over_addition](LAW.md#distributivity-of-multiplication-over-addition) | The predicate is the union of the two conflatable measurements' regions, which is the region a reader has to be careful in. One expert: the ledger's author read the four consolidations end to end and opened no member file, no probe directory and no register, so every standing and every characterisation in it is the consolidation's own rather than one the ledger derived. It states that limit itself. |  | contradiction, conflation, 952, 47.72, 34.52, distributivity, associativity, ledger |
| <a id="most-committed-bench-regions-predate-the-harness-cross-variant-validation"></a>most_committed_bench_regions_predate_the_harness_cross_variant_validation | finding | measured | two_experts | [panel_conduct](TOPIC.md#panel-conduct) | 175 of 254 committed bench regions were produced before the harness driver called cross-variant validation at all. 79 came after, none was unresolvable. | The join resolves each artifact's recorded commit against the commit time of the change that added the validation call to the driver, verified present there and absent at its parent. One author measured it and the consolidator redid the join independently from the raw metadata and the commit log, reaching the same split exactly. The one reconciliation is not a correction: there are 24 distinct commit strings and 23 distinct commits, because one commit appears both clean and dirty. | threads: threads = 1 | [most_committed_bench_regions_predate_the_validation_gate](PROBE.md#most-committed-bench-regions-predate-the-validation-gate) |  | **The predicate carries almost none of this claim's scope**: its coordinates are a named branch head, 254 files and 24 producing commits, none of which is a declared axis. The reproduction has a precondition sitting 81 files back and uncited in the unit: every recorded commit value but one carries a dirty suffix, because the harness writes its artifacts into the tree it then hashes, so a naive resolution returns 253 unresolvable and the finding evaporates. Stripping the suffix is correct and it is correct **for a reason nobody in the unit carried**, so a later reader redoing the join without that fact concludes the corpus has no resolvable provenance at all. | What the 175 regions are worth is not established. The finding is that the gate was not running for them, not that they are wrong. | 175, 254, cross-variant validation, harness, gate, dirty, provenance, join |

### the_strategy_axis

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | instead | answers | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="a-compile-time-strategy-selection-leaves-no-residue-in-the-emitted-body"></a>a_compile_time_strategy_selection_leaves_no_residue_in_the_emitted_body | finding | measured | three_or_more | [the_strategy_axis](TOPIC.md#the-strategy-axis) | A strategy resolved at compile time leaves no residue in the emitted body. | Four instruments across three files agree. A const-function argmin over a cost table against the hand-written arm gives nine instructions each, identical after label normalisation. Three probes reading entry bodies find a single tail branch with zero conditionals. Compiling both encodings makes the assembler emit one as a symbol alias of the other. And a committed harness run puts the arm reached through a const verdict at 1438 ns median against 1456 ns reached directly, intervals overlapping, with the false-verdict gate at 38391 ns. | target_features: target aarch64-apple-darwin, rustc 1.98.0-nightly (57d06900f), edition 2024, opt-level 3, panic = abort, build_profile: no_std, feature gates = 0, opt level = 3, threads: threads = 1 for the timed instance and threads any for the compile-time artifacts | [a_compile_time_strategy_selection_leaves_no_residue](PROBE.md#a-compile-time-strategy-selection-leaves-no-residue) |  |  | The source calls this the single best-supported claim in its unit and the one the whole mechanism rests on. The `arms in {2, 3, 4, 5}` entry from the source's own predicate is a bench parameter rather than a design axis and is not carried here; it belongs on the probe row. The thread entry is the source's own and is the only split-valued one in the corpus: the timing holds at one thread and the compile-time artifacts hold at any. |  | selection, erases, residue, argmin, symbol alias, const verdict, 1438, compile time |
| <a id="no-total-join-exists-over-the-observable-axes-so-the-operation-reports"></a>no_total_join_exists_over_the_observable_axes_so_the_operation_reports | refusal | measured | one_expert | [the_strategy_axis](TOPIC.md#the-strategy-axis) | A total silent join over the observable axes, resolving toward the more conservative side, is the wrong mechanism, because there is no referent for `more conservative`. Three candidate orders computed exhaustively over the whole representable domain disagree: by honoured laws wrapping and saturating are incomparable in three of four configurations, by how often the answer is wrong they are exactly tied, and by worst-case magnitude saturating is above wrapping. Two of the three do not give an order at all. | Two of the three candidate orders are not orders, and the third disagrees with them, so the phrase has no fixed meaning until somebody says which is intended. A fourth result in the same probe kills a related reading outright: saturating and exact-in-a-wider-tier are incomparable at zero fraction width under both signednesses and both widths, because the absorbing top is a law only a lossy policy has, so an accuracy-first policy is not the top of the law order and no choice of inventory rescues it. | total_width: W in {5, 6}, fraction_width: F in {0, 1}, signedness: signedness in {unsigned, signed}, overflow_policy: overflow policy in {wrap, saturate, exact}, operation: operations {add, sub, mul}, arity: arity = 3 for the algebraic family and 2 for the order family, threads: threads = 1, target_features: target features any | [no_conservatism_order_exists_on_the_overflow_axis](PROBE.md#no-conservatism-order-exists-on-the-overflow-axis) | The operation reports that two operands demand different computed answers and the site says which. The predicate separating the reporting arm from the resolving one is not where the demands happen to have a join; it is the far simpler question of whether the coordinate is observable, which is static. | [are_strategies_partially_ordered_by_chain_laws](QUESTION.md#are-strategies-partially-ordered-by-chain-laws) | The source calls this the unit's clearest refutation of a shipped shape. The predicate is the source's own, verbatim, minus the clause naming which laws the probe enumerated, which is an instrument parameter. |  | join, conservative, resolution, cross-strategy, incomparable, report, observable, Q41 |
| <a id="headroom-and-intermediate-precision-are-unobservable-inside-a-pure-ring-region"></a>headroom_and_intermediate_precision_are_unobservable_inside_a_pure_ring_region | finding | measured | one_expert | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Headroom and intermediate precision are invisible across any composition of addition, subtraction and multiplication, and become visible at the first step that is not a ring operation: a shift, a division, a saturation, a comparison. Inside a pure ring region those two axes are the resolver's to choose and a strategy need not fix them. | Reduction modulo two to the width is a ring homomorphism, so any composition of the three ring operations gives the same low bits whatever width it was computed at. Running the observability test one axis at a time with the others held gives headroom at 0 of 640 within ring chains and 500 of 640 past a non-ring step, intermediate precision at 0 of 640 and 570 of 640, packing unobservable everywhere swept, and overflow policy observable in both regimes at 511 of 640. | total_width: W = 13, signedness: signedness = unsigned, operation: operations {+, -, *, >>}, chain_length: column lengths in {1, 2, 3, 4, 8, 16, 32, 64, 128, 1024}, threads: threads = 1, target_features: host aarch64-apple-darwin, rustc nightly-2026-05-28 | [three_of_the_four_proposed_axes_change_no_answer](PROBE.md#three-of-the-four-proposed-axes-change-no-answer) |  | [which_axes_a_build_arm_may_move](QUESTION.md#which-axes-a-build-arm-may-move) | The probe's first version is worth more than its result: it swept only additive chains ending in a mask and reported both axes unobservable at 0 of 640, and those zeros were the sweep proving the ring homomorphism it stood on rather than testing the axis. Both versions are committed. The region is large, it is const-visible wherever the chain is written, and it is exactly the arm-with-a-predicate shape I13 asks for. It is unpriced and unbuilt and the source carries it as a live option rather than as a recommendation. | Unpriced and unbuilt. Nobody has measured what taking the licence saves, and nobody has built the const predicate that would gate it. | headroom, intermediate precision, observable, ring, homomorphism, licence, arm, predicate, 640 |
| <a id="the-rationalisability-counts-on-the-committed-carrier-table"></a>the_rationalisability_counts_on_the_committed_carrier_table | finding | measured | three_or_more | [the_strategy_axis](TOPIC.md#the-strategy-axis) | On the committed carrier-width table, of 15625 sections, 72 are rationalisable by a non-negative weighting and 9 by a strictly positive one, and 63 of the 72 select an arm no weighting can select. The whole gap between 72 and 9 is one tie: two arms declare the same size, so a pure-size weighting ties them at all six regions, making 64 sections weakly rationalisable of which 63 name the dominated arm. | Three independent implementations from three different geometries agree: extreme-ray enumeration of a pointed cone, interval arithmetic on the one-dimensional weight simplex, and polygon clipping written without opening either predecessor. The tie reduction turns a constraint about the sign of every weight into a rule about ties. | threads: threads = 1, target_features: target features any |  |  |  | **The predicate carries almost none of this claim's scope, and that is the finding rather than an oversight.** The source's own region is `regions = 6, arms = 5, cost coordinates = 2, cost source = the committed carrier-width CSVs, arithmetic exact rational`, every one of which is a bench parameter rather than a design axis: an arm can be gated on a fraction width and cannot be gated on `arms = 5`. Stripped to declared axes, what remains is a thread count and a target-feature entry, so the row is findable and its region is nearly empty. The instruments are `97_probes/p9_the_decider.py`, `98_probes/p6`, `101_probes/p4` and `p10`, all committed. | Whether the counts transfer to any other table is unestablished, and the ratio of weighting-realisable sections to Pareto-admissible ones varies by a factor of 47, so the counts are a property of one table while the underlying bound, polynomial against exponential in the number of regions, is structural and does transfer. | rationalisability, 72, 9, 63, weighting, cone, tie, dominated arm, Pareto |
| <a id="generation-relocates-the-check-rather-than-removing-it"></a>generation_relocates_the_check_rather_than_removing_it | finding | measured | one_expert | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Generating a winner table from a stated weighting removes every defect a human writing the table can introduce and admits one a human cannot: the tool computes the wrong argmin. A tool's wrong argmin is in general the right argmin of something else, so the output stays rationalisable and the rationalisability criterion is looking for exactly the property those defects preserve. Rationalisability catches 0 of 190 unit errors, 0 of 147 column swaps and 0 of 152 dropped coordinates at either the non-negative or the strictly positive setting; cone membership of the stated weighting catches all 489. | A coordinate read in the wrong unit is the exact argmin of the rescaled weighting, and a column swap is the exact argmin of the permuted one, so the defect is invisible to a test for the property it preserves. Cone membership is invariant to tie-break policy where independent recomputation is not, recomputation flagging a tie as a defect 48 of 48 times when a tie is precisely where the weighting declines to choose. The two proposals then compose into an assertion neither author saw: the committed winner table equals the argmin of the stated weighting over the committed cost table at every region, compiled as a const item, costing one const evaluation, nothing at runtime, and refusing at build time. | threads: threads = 1, target_features: target features any | [generating_a_winner_table_from_a_stated_weighting](PROBE.md#generating-a-winner-table-from-a-stated-weighting) | Generate, and check cone membership of the stated weighting rather than rationalisability of the output. |  | **The predicate carries almost none of this claim's scope**, for the same reason as the rationalisability row: the source's region is `regions = 5, arms = 5, cost coordinates = 3, 400 random models across two families, plus the committed carrier table, defect classes {unit scale, coordinate swap, tie-break policy, region off-by-one, dropped coordinate}`, all bench parameters. The const assertion has been seen to fail twice, once as a mutant and once accidentally when the first hand-written winner table was wrong, which is the control this row rests on. |  | generation, check, argmin, cone membership, rationalisability, defect class, 489, const assertion |
| <a id="the-corpus-cannot-exhibit-the-accuracy-intents-because-a-coordinate-is-absent"></a>the_corpus_cannot_exhibit_the_accuracy_intents_because_a_coordinate_is_absent | finding | measured | two_experts | [the_strategy_axis](TOPIC.md#the-strategy-axis) | The committed corpus cannot exhibit the accuracy intents, and the barrier is the absent coordinate rather than the arm sets. The corpus does contain answer-differing arms, and one region with a strict accuracy ordering between them, and it records nothing about that ordering anywhere. Of 254 committed regions, 234 are pinned to one value by an exact-value oracle, 10 only to a property, 4 have no oracle at all, and 6 are single-arm. | One file claimed in bold that every committed region is answer-equivalent; a second was dispatched to verify it, built its own instrument before opening the first, and refuted it with three independently sufficient measurements. Twenty regions are not answer-pinned and eight of those have arms measured to differ or are two different algorithms by construction. But the conclusion survived the refutation one tier lower and better: an absent coordinate is something somebody can go and add, whereas a structural inability to hold such arms reads as a property to design around. | threads: threads = 1 |  | Add the scoring hook. The harness already has the consent switch, already has an intermediate bounded-disagreement regime that no variant uses, and one family already writes per-arm oracles, so the missing piece is one hook rather than a set of missing coordinates. | [what_the_cost_coordinates_are](QUESTION.md#what-the-cost-coordinates-are) | **The predicate carries almost none of this claim's scope**: its coordinates are the committed corpus at a named commit, 254 files, 104080 rows, 94 variant crates, none of which is a declared axis. Two mechanical causes of the original overclaim are worth keeping: the census unit could not structurally see one crate whose bridge is declared inline, and it treated the presence of an output validator as answer-pinning where two of fifteen validators check a property rather than a value. | The cost-only question is refused rather than answered, because it asks for one verdict over a category. Where arms are answer-equivalent, cost-only is correct and complete and a fidelity column would measure a constant. Where they differ with no fidelity column, cost-only is correct and incomplete. Where they differ with no per-arm oracle, the region is unvalidated, which is the only defect and it has four instances in one family. | answer pinned, oracle, score_output, fidelity, 254, coordinate, I5, I7, I9 |
| <a id="what-ships-is-four-arms-with-disjoint-predicates-over-one-generated-artifact-set"></a>what_ships_is_four_arms_with_disjoint_predicates_over_one_generated_artifact_set | answer | normative | one_expert | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Ship the weighting, the cost table, and the winner table generated from them, with a const assertion that the third is the argmin of the first over the second at every region. Four arms then apply on disjoint regions: generate and assert equality where every weighed coordinate has an expressible value, the competing arms are separated by more than the instrument's resolution, and the arms compute the same value; check without generating where a weighed coordinate has no expressible value at all, so no weight vector can be written; use a band rather than an equality where the arms are separated by less than the coordinate's resolution but more than zero and compute the same value; and where the resolution does not separate the arms it must distinguish, change the statistic rather than accept the arm. | Written as one recommendation it reads as a single answer, which is the shape op has rejected four separate times. Each of the four predicates is a const-checkable property of the corpus rather than a judgement, so the composition is arms with regions rather than a policy. Two of op's four intents sit in the second region today, which is why that arm is the interim rather than the target. |  |  |  | [what_goes_into_the_compiler_winner_or_cost_table](QUESTION.md#what-goes-into-the-compiler-winner-or-cost-table) | `normative` because it is what the topic recommends shipping rather than a measurement. The fourth arm's condition was subsequently shown to be usually a property of the estimator rather than of the coordinate, which is why its action is to change the statistic. The four predicates are stated over corpus properties rather than over declared axes, so they are not writable as a `predicate` here and are in the `says` instead. | The generate-against-check fork is the unit's largest unclosed item: named by the checkpoint as the first thing later members should attack, attacked around its edges twice, and never attacked at its core. Separately, the fork with consumer-visible content is a different one nobody picked up, whether the compiler is handed a winner table or a cost table. | four arms, disjoint predicates, winner table, cost table, weighting, const assertion, I13, fork |
| <a id="three-topics-independently-terminate-on-the-strategy-axis-as-their-shared-placeholder"></a>three_topics_independently_terminate_on_the_strategy_axis_as_their_shared_placeholder | finding | argument | cross_topic | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Three separately dispatched topics each terminate on the strategy axis as the open coordinate everything else defers to. The format topic's adaptation slot names whatever a strategy resolves to as an open dimension; the number-system topic's crossing and pipeline sections defer the same question; the derived-laws topic's dimension list for a law's region ends in the identical phrase. | The three arrived at it separately, and op then named the strategy axis as the next unit **for that reason**, which makes this the one cross-topic convergence stated by the ratifying human rather than observed by a consolidator afterwards. | operation: operation any, overflow_policy: overflow policy any, rounding: rounding any |  |  |  | The predicate names the three axes the placeholder was reached from in the three topics, each quantified because each topic reached the dead end whatever value it held. It is the weakest kind of region in this file and it is written rather than omitted because omitting all three would say the convergence holds nowhere any of them exists, which is the opposite of what three topics found. A reader wanting a defensible region should treat this row as a pointer to the three sources rather than as a gate. |  | strategy axis, placeholder, dead end, deferral, unit ordering, cross topic, op |
| <a id="a-strategy-is-a-preference-over-measurements-resolved-as-a-compile-time-argmin"></a>a_strategy_is_a_preference_over_measurements_resolved_as_a_compile_time_argmin | answer | normative | two_experts | [the_strategy_axis](TOPIC.md#the-strategy-axis) | A strategy is a preference over measurements, resolved as an argmin over candidate arms at compile time. The container, the codegen choice and the overflow rule are effects of that one cause rather than three components of a marker. | Two authors reached it blind and in parallel from different premises, one from a partial order on cost vectors and one from two of op's intents read together. Both then corrected themselves against their own evidence: one withdrew licence as a co-equal axis on the ground that which laws hold is **computed** from the policy and the region rather than chosen, with its own probe as the evidence it had misread; the other withdrew a claim about where the preference sits, conceding the better evidence and noting that its own earlier probe had already put the preference at the call site without its author noticing. |  |  |  |  | Nine claims matched across the two blind derivations, which is the two-expert standing by the panel's own definition rather than by file count. Four of the unit's eight files corrected a claim of their own against their own evidence and kept the refuted output committed. |  | strategy, preference, argmin, compile time, marker, container, codegen, overflow rule |

### the_predicate_notation

| id | kind | sentence_kind | standing | topic | says | because | law | note | keywords |
|---|---|---|---|---|---|---|---|---|---|
| <a id="a-predicate-carries-every-dimension-it-depends-on-and-the-fraction-width-is-the-one-that-keeps-going"></a>a_predicate_carries_every_dimension_it_depends_on_and_the_fraction_width_is_the_one_that_keeps_going | answer | normative | cross_topic | [the_predicate_notation](TOPIC.md#the-predicate-notation) | A predicate carries every dimension the claim depends on, and the fraction width is the dimension this panel has repeatedly dropped. Dropping one does not weaken a sentence, it converts it into a claim about a region nobody established. | All four topics reach it, as content and as method, and none of them names it as a convergence. The format topic establishes the cube showing unsigned multiplicative structure holding at zero fraction width and dying above it, with the signed cell broken even at zero under clamping. The derived-laws topic restates the same boundary as its dimension list and treats a frontier number quoted without all three factors it depends on as a defect class in its own right. The strategy topic re-measures the signed failure with two new instruments and records that this is the **third** time the qualifier has been lost from one sentence in the panel's own history, once inside a standing workspace rule that was a live licence to emit a wrong rewrite until it was corrected. The number-system topic contributes the structural half, that a sentence stated at the wrong depth of the choice sequence becomes false. | [distributivity_of_multiplication_over_addition](LAW.md#distributivity-of-multiplication-over-addition) | `normative` because it is the discipline; the measurements are the law rows it points at. The three losses are not one author being careless: each happened in a different unit, to a different writer, and the third happened **after** the loss had been diagnosed twice in the same unit, in a compression whose whole purpose was catching that class. | predicate, dimension, F = 0, qualifier, dropped, cross topic, method, I13 |
| <a id="arms-with-const-predicates-are-the-organising-shape-all-four-topics-reached"></a>arms_with_const_predicates_are_the_organising_shape_all_four_topics_reached | answer | normative | cross_topic | [the_predicate_notation](TOPIC.md#the-predicate-notation) | The work is arms with const predicates over the regions where something holds, composed, rather than a single answer over a category. This is independently the organising principle of the later topics and the shape the earlier one reached before the rule that would require it existed. | The derived-laws topic names I13 as the one ratified entry at its gate and structures its whole result set as region-scoped arms. The strategy topic records op rejecting a category-wide policy question four separate times across the panel, three of them inside these two topics, and composes its own recommendation explicitly as four arms with disjoint predicates. The format topic, written before I13 existed, arrives at a structurally identical shape independently: its absorption criterion and the sign-confinement closed form are carried as corollary and criterion rather than merged, which is the same region-scoped posture stated before the rule. |  | `normative` and it restates a ratified intent rather than establishing one, so its standing is about the convergence rather than about the intent. The format topic's instance is the interesting one, because it predates the rule and therefore cannot have been copied from it. | arms, const predicate, region, composition, universal, I13, cross topic, anti-pattern |

### execution_environment

| id | kind | sentence_kind | standing | topic | says | because | predicate | evidence | obligation | precondition_for | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| <a id="splitting-a-reduction-is-sound-in-three-of-the-four-sign-and-policy-cells"></a>splitting_a_reduction_is_sound_in_three_of_the_four_sign_and_policy_cells | finding | measured | one_expert | [execution_environment](TOPIC.md#execution-environment) | Splitting a reduction changes the association order, so an implementation free to choose how many lanes it uses gives an answer that depends on that choice wherever the operation is not associative. Of the four sign-and-policy cells of a bounded integer addition, three give the same answer under every split measured and one does not: signed saturating disagrees on 28,336 of 65,536 vectors at width 4 over 4 operands and on 11,760,675 of 16,777,216 at width 3 over 8. | Op's intent is that arvo runs at one thread, two, or any finite n, detected rather than declared, and his correction is that this may not cost soundness for any strategy except the speed-first one. Those two together impose a precondition stated nowhere: a reduction may only be split where the operation is associative. The measurement says which cells that permits and which it does not, on a single code path that reports zero for three of them and a majority for the fourth. | total_width: W in {3, 4}, fraction_width: F = 0, signedness: signedness in {unsigned, signed}, overflow_policy: overflow policy in {wrap, saturate}, operation: operation = add, arity: arity = 2, chain_length: fold length in {4, 8}, threads: threads = 1, the splits being computed rather than executed on lanes | [which_sign_and_policy_cells_survive_reassociation](PROBE.md#which-sign-and-policy-cells-survive-reassociation) | [composition_contracts_above_the_numeral](OBLIGATION.md#composition-contracts-above-the-numeral) | [ordering_a_directed_acyclic_graph](OBLIGATION.md#ordering-a-directed-acyclic-graph) | One expert on the measurement. The structural half of it, that coherence is what a reduction which may be split or reassociated consumes, was reached separately in the number-system topic and is carried by `the_two_law_families_have_two_consumer_classes`, which states the mapping and carries no region and no instrument. This row is the region, and the two should be read together rather than as two instances of one claim.  **The precondition edge is the first one filed and it is filed narrowly.** A consumer asking for a graph ordering cannot say that the ordering is unmeetable on more than one core unless the addition beneath it is associative, because that is a fact about two layers interacting and the consumer knows only its own. The one obligation named here is the one somebody argued in full. The same argument plainly reaches any obligation whose computation reduces over numerals on more than one core, which on inspection includes the spectral split and the cost programme, and **nobody has made that argument per obligation**, so it is not filed. Widening it is a claim, not bookkeeping. | Measured on addition alone, at two widths, at zero fraction width and on one thread. Whether the same three cells survive at a nonzero fraction width, under multiplication, or when the splits are genuinely executed on separate lanes is not established here. | reassociation, split, lanes, cores, associativity, reduction order, signed saturating, soundness, detection, I10, adaptation |
