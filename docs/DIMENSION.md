<!--
  AUTO-GENERATED: DO NOT EDIT DIRECTLY

  Generated by: mockspace (mock)
  Generated at: 2026-08-30T20:34:06Z
  Source: mock/
  To regenerate: cargo mock
-->

# Predicate dimensions

An axis a finding's region is stated over. A finding that does not name one does not hold anywhere that axis exists.

16 rows. Identifiers are permanent: assigned once, never reused, never renumbered.

| id | what | grammar | moves | note | keywords |
|---|---|---|---|---|---|
| <a id="integer-width"></a>integer_width | How many bits of the declared format sit above the point. | `I = <n>`, `I in <range>`, `I in {<set>}`, or `I any`. `n` is a count of bits and the corpus stays inside 1..=64. | The representable range, and therefore whether an operation overflows at all. Several laws hold at one width and fail at the next because the intermediate stops fitting. | Not independent of `total_width`: a predicate stating `W` and `F` has fixed `I` implicitly. Both spellings appear in the corpus and neither is wrong; a row states whichever its instrument varied. | I, integer width, integer bits, whole part, width, above the point |
| <a id="fraction-width"></a>fraction_width | How many bits of the declared format sit below the point. | `F = <n>`, `F in <range>`, `F in {<set>}`, or `F any`. `F = 0` names the exact-integer case. | Whether an operation is exact. This is the axis the panel has dropped from a sentence three separate times, including once inside a standing workspace rule, and each time the sentence became a claim about a region it had never been established in: unsigned multiplicative structure holds at `F = 0` and dies above it. |  | F, fraction width, fractional bits, below the point, exact, F = 0, scale |
| <a id="total-width"></a>total_width | How many bits the declared format occupies in total, point included. | `W = <n>`, `W in <range>`, `W in {<set>}`, or `W any`. `W in 1..=64` is the whole domain of a `u64` container and is neither a sample nor `any`. | The same thing `integer_width` moves, stated over the format as a whole. A signed format's sign bit is inside it. | Redundant with `integer_width` and `fraction_width` taken together, and kept because the measurement corpus is written in it. A third notation state exists with no name: `W in 1..=64` is the entire container domain and reads as a sample, which is how a proof got recorded as a sweep that stopped at three widths. | W, total width, declared width, bit width, format width |
| <a id="signedness"></a>signedness | Whether the format carries negative values. | `signedness = unsigned`, `signedness = signed`, `signedness in {unsigned, signed}`, or `signedness any`. | Which laws survive. The signed cell is the worst one in the corpus's own cube and is broken even at `F = 0` under clamping, and two instances sweeping disjoint values of this axis intersect to nothing, which is how a clause came to hold nowhere while reading as merely unqualified. |  | signed, unsigned, signedness, sign, negative, two's complement |
| <a id="overflow-policy"></a>overflow_policy | What the operation does when a result leaves the representable range. | `overflow policy = <name>`, `overflow policy in {<set>}`, or `overflow policy any`. Named values seen in the corpus: `wrap`, `saturate`, `saturate-both-ends`, `clamp`, `panic`. | Which family of licences is available at all. This is structural rather than incidental: no realisation map onto a container is licence-free, and the policy is what selects the family. | `saturate` and `clamp` are used for the same thing in places and are distinguished in others. A row states whichever its source did; the two spellings are not asserted here to be one policy. | overflow, wrap, wrapping, saturate, saturating, clamp, panic, policy, edge behaviour |
| <a id="rounding"></a>rounding | How a result that is not representable is placed on the representable grid. | `rounding = <mode>`, `rounding in {<set>}`, or `rounding any`. `rounding = exact` names the case where nothing is discarded, which is a value of the axis rather than its absence. | Which results a chain produces, and which relocations are free. The IEEE default is the one mode that is not free under either signedness, so a design defaulting to it has every relocation unavailable at its default. |  | rounding, round, truncate, ties to even, nearest, IEEE, double rounding, grid, quantise |
| <a id="operation"></a>operation | Which arithmetic operation the claim is about. | `operation = <op>`, `operation in {<set>}`, or `operation any`. Values are the operations the design ships, which is itself an open question. | Nearly everything. A law is a statement relating operations, and a claim about addition transfers to multiplication only where somebody showed it does. The corpus has twice mis-attributed a measured count to the wrong operation. | `operation any` is not writable while the operation set is open, because `any` quantifies over a set nobody has closed. A row wanting it says which operations instead. | operation, op, add, addition, mul, multiply, shift, subtract, operation set |
| <a id="arity"></a>arity | How many operands the operation takes. | `arity = <n>`, `arity in {<set>}`, or `arity any`. | Whether a claim about a binary operation says anything about the fused or n-ary form. It usually does not, and the corpus separates them. |  | arity, operands, binary, unary, n-ary, fused |
| <a id="chain-length"></a>chain_length | How many operations run before a result is observed. | `chain length = <n>`, `chain length in {<set>}`, or `chain length any`. | Accuracy, and which arm wins. Multiplicative chains need width growing linearly in fold length with no logarithmic closed form, and the crossing point where one accuracy arm overtakes another is itself decided by a weighting. Three separate topics reached this independently. |  | chain, chain length, fold, fold length, reduction, depth, accumulate, sequence |
| <a id="container"></a>container | The machine storage the declared format is realised onto. | `container = <type>`, `container in {<set>}`, or `container any`. Values seen: a machine integer type, `dense bit stream`, and a named model. | Whether an intermediate fits, and therefore whether a law's proof over exact values survives at all. One clause of a candidate was found unexecutable in 663 cells because its proof was set over exact values while the container is finite. | Whether a claim is stated over the declared width or over this axis is the container premise, and it is blocking: no wording of several downstream clauses is true on both branches. See `topic::the_container_premise`. | container, carrier, backing, storage, machine type, u8, u64, bit stream, packed |
| <a id="alignment"></a>alignment | Whether a packed value sits on a container boundary or straddles one. | `alignment = aligned`, `alignment = straddling`, `alignment in {aligned, straddling}`, or `alignment any`. | The cost of a read and a write at the packed end, and whether a decode is one instruction or several. |  | alignment, aligned, straddling, boundary, packed, bitpack, unaligned |
| <a id="access-pattern"></a>access_pattern | How the values are reached: sequentially, at random, contended between threads, or otherwise. | `access pattern = <name>` or `access pattern any`. | Cost, and only cost. Nothing about which answer is correct depends on it, so a correctness claim leaving it out is not making the strong negative statement the notation otherwise assigns to an absent axis. | Named as an axis in the corpus's own predicates and rarely varied. A cost claim that leaves it out has not established anything about a pattern it did not run. | access pattern, sequential, random, strided, contended, locality |
| <a id="target-features"></a>target_features | Which instruction-set features the compiler was allowed to use. | `target features any`, `target features = host default`, or a named feature set. | Whether a lowering vectorises, and which instructions a backend reaches for. A measurement taken under one feature set says nothing about another. |  | target features, ISA, instruction set, SIMD, NEON, AVX, vectorisation, target |
| <a id="threads"></a>threads | How many threads the claim was established over. | `threads = 1`, `threads = <n>`, or `threads any`. | Nothing about single-threaded correctness, which is exactly why the axis is dangerous: work done on one thread proves `threads = 1` and nothing wider, and an absent `threads` means the claim holds in no situation where threads exist at all. `threads any` is the positive universal and has to be written; nothing infers it. | arvo takes no stance on core count and runs at one, two or any finite n, so a `threads = 1` corpus is a body of real findings over a real region rather than a pile of claims awaiting rescue. | threads, thread count, cores, parallel, single threaded, concurrency |
| <a id="strategy"></a>strategy | Which strategy the claim was established under. | `S = <name>`, `S in {<set>}`, or `S any`. | What counts as the correct answer. The strategies weigh measurements differently, so a claim about which arm is best is a claim about one strategy unless it names them all. | `S any` quantifies over a set op has stated is open, so a row writing it is claiming more than the corpus can currently close. Rows naming the strategies they ran are the honest form. | S, strategy, Hot, Cold, Warm, Precise, preset, policy |
| <a id="build-profile"></a>build_profile | Which build the claim was established under, and specifically whether debug assertions are on. | `debug-assertions = on`, `debug-assertions = off`, or `debug-assertions in {on, off}`. Where optimisation level matters it is stated beside it as `opt level = <n>`. | Whether a binding-free distinguishing channel exists at all. One licence is false at `debug-assertions = on` and true at `off`, because the overflow panic plus a const-eval refusal is such a channel and neither exists in a release artifact. That bound is exactly the one op's own I18 draws, reached from a direction I18 was not derived from. |  | build profile, debug, release, debug-assertions, opt level, cfg, panic, dev build |
