<!--
  AUTO-GENERATED: DO NOT EDIT DIRECTLY

  Generated by: mockspace (mock)
  Generated at: 2026-08-31T23:30:23Z
  Source: mock/
  To regenerate: cargo mock
-->

# Predicate dimensions

An axis a finding's region is stated over. A finding that does not name one does not hold anywhere that axis exists.

24 rows. Identifiers are permanent: assigned once, never reused, never renumbered.

| id | what | grammar | moves | note | keywords |
|---|---|---|---|---|---|
| <a id="integer-width"></a>integer_width | How many bits of the declared format sit above the point. | `I = <n>`, `I in <range>`, `I in {<set>}`, or `I any`. `n` is a count of bits and the corpus stays inside 1..=64. | The representable range, and therefore whether an operation overflows at all. Several laws hold at one width and fail at the next because the intermediate stops fitting. | Not independent of `total_width`: a predicate stating `W` and `F` has fixed `I` implicitly. Both spellings appear in the corpus and neither is wrong; a row states whichever its instrument varied. | I, integer width, integer bits, whole part, width, above the point |
| <a id="fraction-width"></a>fraction_width | How many bits of the declared format sit below the point. | `F = <n>`, `F in <range>`, `F in {<set>}`, or `F any`. `F = 0` names the exact-integer case. | Whether an operation is exact. This is the axis the panel has dropped from a sentence three separate times, including once inside a standing workspace rule, and each time the sentence became a claim about a region it had never been established in: unsigned multiplicative structure holds at `F = 0` and dies above it. |  | F, fraction width, fractional bits, below the point, exact, F = 0, scale |
| <a id="total-width"></a>total_width | How many bits the declared format occupies in total, point included. | `W = <n>`, `W in <range>`, `W in {<set>}`, or `W any`. `W in 1..=64` is the whole domain of a `u64` container and is neither a sample nor `any`. | The same thing `integer_width` moves, stated over the format as a whole. A signed format's sign bit is inside it. | Redundant with `integer_width` and `fraction_width` taken together, and kept because the measurement corpus is written in it. A third notation state exists with no name: `W in 1..=64` is the entire container domain and reads as a sample, which is how a proof got recorded as a sweep that stopped at three widths. | W, total width, declared width, bit width, format width |
| <a id="signedness"></a>signedness | Whether the format carries negative values. | `signedness = unsigned`, `signedness = signed`, `signedness in {unsigned, signed}`, or `signedness any`. | Which laws survive. The signed cell is the worst one in the corpus's own cube and is broken even at `F = 0` under clamping, and two instances sweeping disjoint values of this axis intersect to nothing, which is how a clause came to hold nowhere while reading as merely unqualified. |  | signed, unsigned, signedness, sign, negative, two's complement |
| <a id="overflow-policy"></a>overflow_policy | What the operation does when a result leaves the representable range. | `overflow policy = <name>`, `overflow policy in {<set>}`, or `overflow policy any`. Named values seen in the corpus: `wrap`, `saturate`, `saturate-both-ends`, `clamp`, `panic`. | Which family of licences is available at all. This is structural rather than incidental: no realisation map onto a container is licence-free, and the policy is what selects the family. | `saturate` and `clamp` are used for the same thing in places and are distinguished in others. A row states whichever its source did; the two spellings are not asserted here to be one policy. | overflow, wrap, wrapping, saturate, saturating, clamp, panic, policy, edge behaviour |
| <a id="rounding"></a>rounding | How a result that is not representable is placed on the representable grid. | `rounding = <mode>`, `rounding in {<set>}`, or `rounding any`. `rounding = exact` names the case where nothing is discarded, which is a value of the axis rather than its absence. | Which results a chain produces, and which relocations are free. The IEEE default is the one mode that is not free under either signedness, so a design defaulting to it has every relocation unavailable at its default. |  | rounding, round, truncate, ties to even, nearest, IEEE, double rounding, grid, quantise |
| <a id="operation"></a>operation | Which arithmetic operation the claim is about. | `operation = <op>` or `operation in {<set>}`. Values are the operations the design ships, which is itself an open question. **`operation any` is not admissible**, because `any` quantifies over a set nobody has closed. | Nearly everything. A law is a statement relating operations, and a claim about addition transfers to multiplication only where somebody showed it does. The corpus has twice mis-attributed a measured count to the wrong operation. | The grammar used to admit `any` while this note forbade it, which is a gate with a gap exactly the width of the failure it guards: a checker reads a grammar and cannot read a note, so the admissible-but-forbidden spelling would eventually be written and would validate. The spelling becomes admissible when the operation set is closed and not before. | operation, op, add, addition, mul, multiply, shift, subtract, operation set |
| <a id="arity"></a>arity | How many operands the operation takes. | `arity = <n>`, `arity in {<set>}`, or `arity any`. | Whether a claim about a binary operation says anything about the fused or n-ary form. It usually does not, and the corpus separates them. |  | arity, operands, binary, unary, n-ary, fused |
| <a id="chain-length"></a>chain_length | How many operations run before a result is observed. | `chain length = <n>`, `chain length in {<set>}`, or `chain length any`. | Accuracy, and which arm wins. Multiplicative chains need width growing linearly in fold length with no logarithmic closed form, and the crossing point where one accuracy arm overtakes another is itself decided by a weighting. Three separate topics reached this independently. |  | chain, chain length, fold, fold length, reduction, depth, accumulate, sequence |
| <a id="container"></a>container | The machine storage the declared format is realised onto. | `container = <type>`, `container in {<set>}`, or `container any`. Values seen: a machine integer type, `dense bit stream`, and a named model. | Whether an intermediate fits, and therefore whether a law's proof over exact values survives at all. One clause of a candidate was found unexecutable in 663 cells because its proof was set over exact values while the container is finite. | Whether a claim is stated over the declared width or over this axis is the container premise, and it is blocking: no wording of several downstream clauses is true on both branches. See `topic::the_container_premise`. | container, carrier, backing, storage, machine type, u8, u64, bit stream, packed |
| <a id="alignment"></a>alignment | Whether a packed value sits on a container boundary or straddles one. | `alignment = aligned`, `alignment = straddling`, `alignment in {aligned, straddling}`, or `alignment any`. | The cost of a read and a write at the packed end, and whether a decode is one instruction or several. |  | alignment, aligned, straddling, boundary, packed, bitpack, unaligned |
| <a id="access-pattern"></a>access_pattern | How the values are reached: sequentially, at random, contended between threads, or otherwise. | `access pattern = <name>` or `access pattern any`. | Cost, and only cost. Which answer is correct does not depend on it. | Named as an axis in the corpus's own predicates and rarely varied. A cost claim that leaves it out has not established anything about a pattern it did not run. **A correctness claim untouched by it writes `access pattern: any`**, with the structural argument as the warrant, exactly as a compile-time result writes `threads any`. This note used to say the opposite, that a correctness claim could leave the axis out without making the strong negative statement, and that was a fourth omission state invented per axis. Op's mechanism has three and no fourth: nothing infers a universal, it has to be written, and the notation gives no middle ground where a hedge could sit. | access pattern, sequential, random, strided, contended, locality |
| <a id="target-features"></a>target_features | Which instruction-set features the compiler was allowed to use. | `target features any`, `target features = host default`, or a named feature set. | Whether a lowering vectorises, and which instructions a backend reaches for. A measurement taken under one feature set says nothing about another. |  | target features, ISA, instruction set, SIMD, NEON, AVX, vectorisation, target |
| <a id="threads"></a>threads | How many threads the claim was established over. | `threads = 1`, `threads = <n>`, or `threads any`. | Nothing about single-threaded correctness, which is exactly why the axis is dangerous: work done on one thread proves `threads = 1` and nothing wider, and an absent `threads` means the claim holds in no situation where threads exist at all. `threads any` is the positive universal and has to be written; nothing infers it. | arvo takes no stance on core count and runs at one, two or any finite n, so a `threads = 1` corpus is a body of real findings over a real region rather than a pile of claims awaiting rescue. | threads, thread count, cores, parallel, single threaded, concurrency |
| <a id="strategy"></a>strategy | Which strategy the claim was established under. | `S = <name>` or `S in {<set>}`. **`S any` is not admissible**, because it quantifies over a set op has stated is open. | What counts as the correct answer. The strategies weigh measurements differently, so a claim about which arm is best is a claim about one strategy unless it names them all. | The grammar used to admit `any` while this note said a row writing it claimed more than the corpus can close, which is the same gap the `operation` row carried: nothing parses a note. Rows naming the strategies they ran are the honest form, and the spelling becomes admissible when the set is closed. | S, strategy, Hot, Cold, Warm, Precise, preset, policy |
| <a id="ambient-domain"></a>ambient_domain | The mathematical domain a computation's values are taken to live in, which the format approximates. | `ambient domain = <domain>`, `ambient domain in {<set>}`, or `ambient domain any`. Values name a domain: the integers, the rationals, the reals, a finite ring. | Which laws are even askable. Associativity over the reals and associativity over a finite ring are different questions with different answers, and a claim proved over one says nothing about the other. It is the single largest blocker in the corpus's unwritable predicates, sole blocker of four spans and present in eleven. | Not the same axis as `container`. The container is what a value is stored in; the ambient domain is what it is understood to approximate, and a claim can be exact in one and lossy in the other. | ambient, ambient domain, domain, reals, rationals, integers, ring, field, approximates, exact values, denotation |
| <a id="radix"></a>radix | The base a format's fraction is expressed in. | `radix = <n>`, `radix in {<set>}`, or `radix any`. | Which values are representable at all, and therefore which roundings are exact. A result established at radix two does not transfer to a decimal format, and the corpus has measurements at both. |  | radix, base, binary, decimal, base two, base ten, quantiser, grid |
| <a id="accumulator-width"></a>accumulator_width | How wide the intermediate a chain accumulates into is, where it differs from the declared width. | `accumulator width = <n>`, `accumulator width in {<set>}`, or `accumulator width any`. `accumulator width = declared` names the case where no widening happens. | Whether a chain's intermediate overflows, and therefore whether a fold's result depends on its order. It is a design choice an implementation sits at a value of, which is what makes it an axis rather than a parameter of whichever run measured it. |  | accumulator, accumulator width, intermediate, widening, staged narrowing, fold, headroom, chain |
| <a id="toolchain"></a>toolchain | Which compiler built it, and under which language edition. | `rustc = <version>` and `edition = <year>`, written as separate entries where both were fixed. `toolchain any` where a result is established to be independent of both. | What the backend does with the same source. The corpus carries results at two editions and pins one nightly across the workspace precisely because a floating one could roll past a behaviour change unnoticed. | Distinct from `target_features`, which is what the machine offers, and from `build_profile`, which is how the compiler was asked to optimise. This is which compiler, and the corpus writes all three separately. Whether it should instead fold into those two neighbours was raised and left as a fork; it is declared because the corpus writes an edition and a rustc version as their own coordinates and neither neighbour has anywhere to put them. | toolchain, rustc, nightly, compiler, version, edition, 2021, 2024, compilation environment |
| <a id="build-profile"></a>build_profile | Which build the claim was established under, and specifically whether debug assertions are on. | `debug-assertions = on`, `debug-assertions = off`, or `debug-assertions in {on, off}`. Where optimisation level matters it is stated beside it as `opt level = <n>`. | Whether a binding-free distinguishing channel exists at all. One licence is false at `debug-assertions = on` and true at `off`, because the overflow panic plus a const-eval refusal is such a channel and neither exists in a release artifact. That bound is exactly the one op's own I18 draws, reached from a direction I18 was not derived from. |  | build profile, debug, release, debug-assertions, opt level, cfg, panic, dev build |
| <a id="operand-window"></a>operand_window | Whether the operands are declared to lie within a restricted sub-range of the format's representable domain (for example, non-negative), rather than ranging over the whole of it. | `operand window = declared non-negative`, `operand window = full range`, or `operand window any` where a claim is established independent of it. Not the same axis as `signedness`, which is about the format's representable set; this is about which subset of that set the operands are declared to be drawn from. | At least three algebraic laws that fail over a signed type's full representable range under saturation (additive associativity, multiplicative associativity, distributivity over addition) are recovered when the operands are restricted to a declared non-negative window, because a one-sided clamp is a congruence there and a two-sided one is not. Three existing `law` rows already carry this fact in a `gap` field because no axis existed to state it on. | Declared explicitly because three `law` rows (`additive_associativity_under_saturation`, `distributivity_of_multiplication_over_addition`, `coherence_of_a_reduction_onto_its_induced_operation`) each independently recorded the same missing axis as a `gap`, naming the identical cell: the declared-non-negative signed case at `W in {4, 5, 6}, F = 0`. Adding the axis here is what lets a `holds` region name that cell instead of stranding it in prose. | operand window, declared non-negative, one-sided clamp, congruence, signed, saturation, associativity, distributivity, F-H |
| <a id="occupancy"></a>occupancy | Whether a value is the sole logical occupant of its carrier allocation, or shares that allocation with others. It is an axis rather than a detail because the ratified floor makes the footprint observable at sole occupancy and not observable at shared: the layout observation exists for the allocation, and at a shared placement no per-element footprint exists to observe at all. | `occupancy = sole`, `occupancy = shared`, or `occupancy any` where a claim is established independent of it. Not admissible bare: a claim that does not name it does not hold anywhere occupancy is defined, which is everywhere a value is placed. | The container derivation's own output count moves along it, measured: one output at sole occupancy, the carrier; three at shared, the carrier, the access width and the stride. Two seats reached that split independently. It also moves what is observable at all, which is the ratified floor's own statement. | **Declared because two seats reached the same missing axis from opposite directions and neither could gate on it.** One derived that the footprint observation exists only where the value is the sole occupant, which is now `ruling::behaviour_is_stated_per_declared_signature_and_the_premise_dissolves`. The other derived that the container derivation has a different output count on each side of that split, and reported that no declared axis could carry the arms it produces.  That is the shape the axis vocabulary exists for: a region something genuinely moves along, found by measurement rather than posited. Without it the two arms are real and ungateable, which is the state where a finding gets written into prose and lost. | occupancy, sole, shared, placement, packed, carrier, allocation, footprint, observable, stride, access width, column |
| <a id="association"></a>association | How a multi-operand expression is parenthesised, independent of how many operands it has and of what its leaves are. | `association = <shape>`, `association in {<set>}`, or `association any` where a claim is established independent of it. Shapes seen in the corpus: `left`, `right`, `balanced`. Not admissible bare on any claim over an expression of arity above two: such a claim does not hold anywhere association is defined, which is everywhere more than one operator is applied. | The answer itself, not merely a verdict about it. Exhaustive at `W = 4` signed saturating with every leaf a distinct occurrence, the left fold and the balanced tree disagree on 16268 of 65536 tuples, witness `[-8, -8, -8, 1]` giving `-7` against `-8`. Two negative controls bound it: the same arm under wrapping disagrees on none, and a declared non-negative window disagrees on none, the second agreeing exhaustively with an earlier seat's four-million-sample measurement. | **Declared because two seats reached it independently and neither instrument could vary the other's coordinate**, which is why they read as disagreeing for a whole sitting. One decomposed the term and found the decomposition assigns depth to `chain_length`, arity to `arity`, leaf count to `chain_length` and residue to aliasing, with **no slot for parenthesisation**. The other built the two-by-two directly and found two cells pointing opposite ways: at a non-negative window with distinct leaves the value moves with association and the corner rule stays exact, and at a narrow window with an aliased leaf the value does not move and the corner rule goes wide.  The two are orthogonal and `a_predicate_names_an_axis_once` is a hard error, so folding them under one key makes a predicate over either unwritable.  **What the two instruments share, and it bounds every claim above.** Neither varied `strategy`, `threads`, `target_features` or `toolchain`, so the region they agree about is the registry as committed, one thread, one host and the pinned toolchain. Widening any of those is a new claim and belongs in whoever establishes it. | association, associativity, parenthesisation, grouping, left fold, right fold, balanced tree, reduction shape, term shape |
| <a id="leaf-aliasing"></a>leaf_aliasing | Whether the leaves of a term are distinct occurrences, or the same value appears at more than one of them. | `leaf aliasing = distinct`, `leaf aliasing = aliased`, or `leaf aliasing any` where a claim is established independent of it. Not admissible bare on a claim about an interval or range rule over a term of arity above one: such a rule is exact on distinct leaves and can lose exactness on aliased ones, so a claim that does not name the axis holds nowhere aliasing is defined. | Whether an interval rule stays exact. Sweeping 136 declared operand boxes at `W = 4` with association held fixed, the rule agrees with the oracle on every box for `(x + y) - z` and disagrees on 5 for `(x + y) - x`, witness box `[-4, 1]` where the rule says may-overflow and the oracle says it cannot. A negative control on the distinct-leaf term passes at zero disagreements, so the rule is being evaluated correctly in both arms. | The companion to `association` and separated from it for the same reason. The measurement that first named this region drew every operand independently, so aliasing was fixed at `distinct` across all of it and could not have moved; the measurement that named association stated its condition across both discharge checks, so association could not have moved there. **Two instruments, each blind to the other's coordinate, agreeing perfectly about it because neither varied it.**  Same intersection bound as `association`: one thread, one host, the pinned toolchain, the registry as committed. | leaf aliasing, aliasing, aliased, distinct, repeated operand, shared leaf, interval rule, range rule, exactness, occurrence |
