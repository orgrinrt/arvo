<!--
  AUTO-GENERATED: DO NOT EDIT DIRECTLY

  Generated by: mockspace (mock)
  Generated at: 2026-08-31T02:42:28Z
  Source: mock/
  To regenerate: cargo mock
-->

# Obligations

Something arvo must deliver, named from the consumer's side rather than derived from what the design already covers.

13 rows. Identifiers are permanent: assigned once, never reused, never renumbered.

| id | need | consumer | why | topic | gap | keywords |
|---|---|---|---|---|---|---|
| <a id="set-operations-over-a-fixed-size-bit-set"></a>set_operations_over_a_fixed_size_bit_set | Union, intersection and difference over sets of a size fixed at compile time, for a scheduler's read and write sets and for the dirty masks its plan ends on. | hilavitkutin, and vehje for a compiler's artifact read and write sets | Two steps of a twelve-step plan chain rest on it, and the same surface is asked for again by a different consumer for a different purpose, which is what makes it an obligation rather than one consumer's convenience. | [arvo_identity](TOPIC.md#arvo-identity) |  | set, sets, bitmask, bit set, mask, union, intersection, difference, read set, write set, dirty mask, access set |
| <a id="ordering-a-directed-acyclic-graph"></a>ordering_a_directed_acyclic_graph | Topological order over a dependency graph, an upward rank on it, a waist detection, and a bandwidth-reducing reordering. | hilavitkutin for a scheduler's plan, vehje for a compiler's pass order | Four of the twelve plan steps are this one surface asked for four ways, and the second consumer wants the same thing for an unrelated reason. | [arvo_identity](TOPIC.md#arvo-identity) |  | graph, DAG, dependency graph, topological sort, topo, rank, waist, RCM, reverse Cuthill McKee, bandwidth, ordering, pass order |
| <a id="a-sparse-adjacency-a-plan-can-be-built-on"></a>a_sparse_adjacency_a_plan_can_be_built_on | A compressed sparse representation of a dependency graph, sized at compile time, that stays the fastest shape at every graph size the consumer sees. | hilavitkutin | Its design records the choice as bench-locked canonical at all N rather than as a preference, so what is owed is the representation and the property that it does not lose at some size. | [arvo_identity](TOPIC.md#arvo-identity) |  | sparse, CSR, compressed, adjacency, dependency graph, row offsets, bench locked |
| <a id="a-spectral-partition-of-a-dependency-graph"></a>a_spectral_partition_of_a_dependency_graph | A spectral split of a graph into groups that can run apart, taken over an operator the consumer supplies rather than over a representation arvo picks, and reproducible bit for bit at a fixed iteration count the consumer names. | hilavitkutin | One named step of the plan chain, and the one that decides which work is allowed to run in parallel. The operator being the consumer's is the load-bearing half: it builds a symmetric Laplacian of its own because a directed one is the wrong object for partitioning, and it describes that as composition over a contract rather than as reimplementing anything. So what is owed is the contract and the split over it. Reproducibility is not a nicety either: the consumer pins the iteration count precisely so the schedule it derives stays statically analysable, and a split that moved between runs would move the schedule with it. | [arvo_identity](TOPIC.md#arvo-identity) | **This row briefly demanded the Laplacian construction, the power iteration and both split forms, and that was wrong in the direction that matters.** It was written from a table in the consumer's `research/seed/`, which is demoted material, and reported as the consumer's design. The consumer's actual source builds its own Laplacian and says why in the file's opening comment. Reading a seed file and calling it a design is the failure of claiming what a design says without opening the design, and it inflated the one measurement op's exhaustiveness bar is checked by. Reported by an expert dispatched on a neighbouring question and verified here against committed source. What remains genuinely unstated is where the iteration-count determinism is written down: it is in the consumer's roadmap research rather than in a design document. | spectral, Fiedler, eigenvector, partition, graph partition, block diagonal, parallel groups, linear operator, matvec, operator contract, deterministic, iteration count |
| <a id="a-cost-dynamic-program"></a>a_cost_dynamic_program | Combinatorial grouping over a cost model: packing items into bins, and ordering a chain of operations so the total cost of the chain is least. | hilavitkutin | Its grouping step is named as greedy together with a matrix-chain dynamic program, and a separate ordering step reaches for bin packing by name. Both decide how work is grouped rather than how much of it fits anywhere. | [arvo_identity](TOPIC.md#arvo-identity) | **This row read `a dynamic program over costs, deciding how to group work under a budget`, and the budget half was wrong.** The consumer's budgets are cache-footprint constants that size morsels, which is a different step of the same chain, so the row attached a real quantity to the wrong need and could have been met by something the consumer never asked for. Reported by an expert dispatched on a neighbouring question; the two routines and the misplaced budget were verified here against the consumer's own documents. What is still not stated anywhere is what cost model the ordering minimises over, which is the thing an implementation would need first. | cost, DP, dynamic programming, combinatorics, grouping, bin packing, matrix chain, greedy, knapsack, ordering |
| <a id="a-platform-sized-unsigned-integer-at-an-api-position"></a>a_platform_sized_unsigned_integer_at_an_api_position | An unsigned integer of the platform's size, usable in a public error type, whose range covers both a unix errno and a Windows GetLastError value with no negative case. | hilavitkutin | The consumer states the property rather than the type: non-negative across both platforms' ranges. A fixed-shape error enum with no heap payload has to carry the code somewhere, and the stack's own primitive is what its public positions are allowed to use. | [the_format](TOPIC.md#the-format) |  | usize, USize, platform size, pointer width, errno, GetLastError, error code, non-negative, public API, primitive |
| <a id="an-exact-width-container-a-consumer-can-alias-and-pin"></a>an_exact_width_container_a_consumer_can_alias_and_pin | A container of an exactly declared bit width that a consumer can name, alias under its own name, and convert to a fixed wider width at a boundary it controls. | hilavitkutin | Its content hash is a 28-bit value in the runtime API and a bare 32-bit value on disk, and the on-disk width is pinned by a serialisation format rather than chosen. So the demand is both halves: the exact width in memory and a conversion the consumer writes at the boundary. | [the_format](TOPIC.md#the-format) |  | exact width, Bits, 28 bit, container, alias, newtype, content hash, serialisation, on disk, byte layout, conversion |
| <a id="a-content-hash"></a>a_content_hash | A hash of content that is stable across sessions and across machines, wide enough that a cache keyed on it does not collide at the population its consumer holds, and whose width the consumer names rather than inherits. | hilavitkutin | Its persistence layer keys on it, and stability across sessions is the property that makes a cache a cache. Nothing else about a cache key is free: two entries colliding is a wrong answer served from disk rather than a slow one. | [arvo_identity](TOPIC.md#arvo-identity) | The consumer states the stability and not the width or the collision bound, and the earlier wording here stated neither, so anything returning bytes met it. What the population actually is, and therefore what width is enough, is not established anywhere. | hash, content hash, digest, stable, across sessions, cache key, persistence |
| <a id="debug-output-from-every-numeral-shape"></a>debug_output_from_every_numeral_shape | Debug output from every numeral, at every width and under every strategy, written into a caller-supplied fixed-size buffer with no alloc and no std. | any | A check written for it exists and cannot run, because the tree it was written against was deleted. The obligation is what outlives the file: a substrate whose types cannot be printed is one nobody can debug, and the no-alloc constraint means the ordinary route is unavailable. | [arvo_identity](TOPIC.md#arvo-identity) |  | Debug, fmt, format, print, no_std, no alloc, core::fmt::Write, buffer, smoke test, diagnostics |
| <a id="a-build-flag-that-changes-float-semantics"></a>a_build_flag_that_changes_float_semantics | A way for a consumer to say that within some part of its own program the stricter float guarantees may be traded for speed, and to have arvo's floats honour that. | hilavitkutin | Its build tooling exists partly to turn this on, driven by a pragma a person writes in the consumer's source. The need is the relaxation and the region it applies to; nothing about it requires the mechanism the consumer currently uses. | [binding_time](TOPIC.md#binding-time) | **This row used to state the mechanism**, a named cfg the consumer's build sets, which the field description forbids in as many words: the need, never the mechanism that serves it. As worded it could only be met by the one thing the corpus argues against, which is not a coverage finding about arvo but a defect in the row. What the corpus has measured is that one source one flag apart gives one type name two policies, and a compile-time check cannot catch it, because each build emits one lowered path and satisfies the check while the denotation differs between builds. So the need stands and the current mechanism is what is in doubt. It also conflicts with `a_content_hash`, from the same consumer: a hash stable across machines and a float regime that changes with a build flag cannot both hold over the same values, and nothing in the registry says so. **The slug still names the mechanism and stays that way.** It is an address, five landed member files cite it by name, and this namespace has no way to supersede a row, so renaming it would orphan those five for a cosmetic gain. Read the `need`, not the slug. | cfg, build flag, fast math, unsafe-fp-math, float semantics, pragma, rustc wrapper, denotation, one lowered path, soundness hole |
| <a id="composition-contracts-above-the-numeral"></a>composition_contracts_above_the_numeral | Contracts for units bigger than a single numeral, so that what a chain of operations guarantees is expressible at all. | any | Op names it as half of what arvo is for, beside the algorithm surface, and explicitly not the numeral alone. The panel reaches the same place from the other direction: a chain is not a value, so an accuracy intent stated over chains has no expression over a per-value primitive, and that was recorded as a hole in the whole framing rather than as a gap in one topic. | [the_chain](TOPIC.md#the-chain) |  | composition, contract, chain, beyond the numeral, fold, accuracy across chains, bigger than a numeral, guarantee, trait contract |
| <a id="a-primitive-for-every-position-a-bare-number-would-take"></a>a_primitive_for_every_position_a_bare_number_would_take | A primitive of arvo's own for every public API position across the stack where a bare integer, float, bool or platform-sized index would otherwise sit, so that no such position anywhere in the stack has to fall back on the machine's own types. **A const generic parameter is excepted**, and even there the bare form is used only where the alternative is genuinely painful. | any, and the whole stack by rule | The constraint forbids the bare types at those positions and is enforced by lint, so it is a demand on arvo whether or not anyone states it as one: every position it closes is a position arvo has to have something for. A rule that forbids without supplying is a rule consumers cannot follow. The exception is op's own and is twice bounded: const generics, and inside those only where the alternative is painful. | [the_format](TOPIC.md#the-format) | **This is the widest obligation here and it was enumerated nowhere**, because it comes from a prohibition rather than from a consumer naming a want, and the enumeration was built by reading what consumers ask for. It is not satisfied by counting the primitives that exist: it is satisfied by the positions, and nothing has enumerated those. One consumer's error type already produced a narrower version of it, recorded separately as the platform-sized unsigned integer.  **The exception clause was missing until a consumer nobody had read supplied it.** This row was written from op's general statement of the constraint and stated it without limit; he had already bounded it, in a design round of a consumer that two prior passes recorded as saying nothing about arvo. One of those passes reported that consumer at zero and called the figure controlled. It was reading a single-branch clone. | primitive, bare, u8, u32, u64, i32, f32, f64, bool, usize, public API, position, no bare primitives, stack's own |
| <a id="the-surface-expressible-as-contracts-before-anything-implements-it"></a>the_surface_expressible_as_contracts_before_anything_implements_it | Every part of arvo's surface stated as a trait contract that holds and is checkable before an implementation of it exists, so a consumer can build against the shape while the implementation behind it is unfinished. | any, and hilavitkutin as the live instance | Op names the contract structure as a paradigm arvo keeps, for the reason that it lets the shapes stick while implementations are still in progress, and he names a consumer that is in exactly that condition now. So what is owed is not merely that traits are used, but that the surface is expressible as contracts first and implementations second. | [arvo_identity](TOPIC.md#arvo-identity) | One place enforces where traits may live, which is narrower than the paradigm and does not reach the surface as a whole. Nothing here says what a contract has to carry to be usable before its implementation exists, and no row anywhere establishes that the surface can be stated that way. | trait, contract, paradigm, shape, before implementation, wip, unimplemented, surface, sticks, buildable against |
