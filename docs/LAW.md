<!--
  AUTO-GENERATED: DO NOT EDIT DIRECTLY

  Generated by: mockspace (mock)
  Generated at: 2026-08-31T10:39:41Z
  Source: mock/
  To regenerate: cargo mock
-->

# Laws

An algebraic law the design reasons about, with the regions it has been established to hold and to fail in.

23 rows. Identifiers are permanent: assigned once, never reused, never renumbered.

| id | statement | holds | fails | witness | topic | note | gap | keywords |
|---|---|---|---|---|---|---|---|---|
| <a id="fusing-a-multiply-add-preserves-the-answer-under-unsigned"></a>fusing_a_multiply_add_preserves_the_answer_under_unsigned | Fusing a multiply-add, so that the intermediate product is not resolved before the addition, computes the same answer as resolving it. The property is translation equivariance of the rounding position on the domain the cell reaches, so under an unsigned range policy the domain excludes the negatives where toward-zero and away-from-zero differ from their equivariant twins, and five of the six positions are free. | total_width: 6, fraction_width: in 0..=5, signedness: unsigned, overflow_policy: in {wrap, saturating}, rounding: in {floor, ceiling, toward zero, away from zero, nearest-half-up}, operation: multiply-add, arity: 3, chain_length: 2, container: declared width, threads: 1, target_features: host aarch64-apple-darwin, rustc 1.98.0-nightly 57d06900f | total_width: 6, fraction_width: in 1..=5, signedness: unsigned, overflow_policy: wrap, rounding: nearest-half-even, operation: multiply-add, arity: 3, chain_length: 2, container: declared width, threads: 1, target_features: host aarch64-apple-darwin, rustc 1.98.0-nightly 57d06900f |  | [the_strategy_object](TOPIC.md#the-strategy-object) | The failing rates at wrapping are 12.50, 12.50, 9.38, 6.25 and 3.91 percent of triples at F = 1 through 5, and zero at F = 0, so the F = 0 cell of half-even belongs to the holding region and is left out of both fields rather than repeating `rounding` in one of them. Under saturating the same mode differs on 0.93 to 2.18 percent of triples, reported as a range without a per-F breakdown, so the saturating half of the failing region is not written here.  This region is the repair of a clause the first candidate got wrong. `146` section 5.5's first predicate block claimed the whole rounding axis free under unsigned, on a congruence argument that covers the reduction relocation and not the rounding relocation, and nobody had swept the unsigned half over six modes. Two signatures found it independently and a third instrument in a third language reproduced it.  | No witness. The failing region is established as a rate over exhaustive triples rather than by a named instance, so nothing here is reproducible from the row alone. The three instruments are `147_probes`, `149_probes/y2` and `151_probes/v1`; no `probe` row exists to point `evidence` at.  The formulation is one expert's. `151` names its own exposure: the restricted equivariance test was decided over a window its author chose, and a clean sweep does not settle a property the way a counterexample settles its negation.  | fusion, fused multiply-add, fma, equivariance, translation equivariant, rounding, unsigned, nearest-half-even, IEEE default, relocation, free lowering |
| <a id="fusing-a-multiply-add-preserves-the-answer-under-signed-wrapping"></a>fusing_a_multiply_add_preserves_the_answer_under_signed_wrapping | The same property under a signed wrapping range policy, where the domain the cell reaches includes the negatives and three of the six rounding positions are translation equivariant on it. Signedness stops being a case split and becomes what determines the domain. | total_width: 6, fraction_width: in 0..=5, signedness: signed, overflow_policy: wrap, rounding: in {floor, ceiling, nearest-half-up}, operation: multiply-add, arity: 3, chain_length: 2, container: declared width, threads: 1, target_features: host aarch64-apple-darwin, rustc 1.98.0-nightly 57d06900f | total_width: 6, fraction_width: in 1..=5, signedness: signed, overflow_policy: wrap, rounding: in {toward zero, away from zero, nearest-half-even}, operation: multiply-add, arity: 3, chain_length: 2, container: declared width, threads: 1, target_features: host aarch64-apple-darwin, rustc 1.98.0-nightly 57d06900f |  | [the_strategy_object](TOPIC.md#the-strategy-object) | Split from its unsigned twin rather than folded into one row, because one law row carries one region per field and two disjoint holding regions differing on signedness and on the mode set cannot both be written without naming an axis twice.  The measured signed wrapping rates reproduce an earlier unit's digit for digit, toward-zero at 1.64, 5.54, 12.34, 22.22 and 33.40 percent and half-even at 12.50, 12.50, 9.38, 6.25 and 3.91, which is what establishes that the two sittings measured one thing rather than two that happen to disagree.  | No witness, for the same reason as the unsigned row. The signed saturating cell is not covered by either row: it is where the fused and the stepwise forms denote different functions outright, which is the separate law row `the_fused_and_the_stepwise_multiply_add_denote_one_function`.  | fusion, fused multiply-add, fma, equivariance, signed, wrapping, rounding, toward zero, away from zero, relocation |
| <a id="the-fused-and-the-stepwise-multiply-add-denote-one-function"></a>the_fused_and_the_stepwise_multiply_add_denote_one_function | The fused realisation of a multiply-add and the stepwise one denote the same function, so a design may substitute either for the other. Where they do not, they are two denotations rather than two lowerings of one, and the intermediate axis already names the difference: a consumer wanting the fused answer declares the exact-intermediate position, keeps full determinism, and gets the fast arm. | total_width: in {4, 6}, fraction_width: in 0..=W-1, signedness: in {unsigned, signed}, overflow_policy: wrap, rounding: truncate toward zero, operation: multiply-add, arity: 3, chain_length: 2, container: declared width, threads: 1, target_features: host | total_width: in {4, 6}, fraction_width: in 0..=W-1, signedness: signed, overflow_policy: saturating, rounding: truncate toward zero, operation: multiply-add, arity: 3, chain_length: 2, container: declared width, threads: 1, target_features: host |  | [the_strategy_object](TOPIC.md#the-strategy-object) | Established twice: once by building both arms, and once from a third member's two committed probes, over 6,356,992 triples, with a cross-pairing control differing at 757,954. The two arrivals are independent instruments; whether their regions intersect on every axis is unchecked, because the value-level intersection instrument that found empty intersections in three of four convergence rows was run over four rows and this is not one of them.  The unsigned saturating cell holds and only the signed saturating cell fails, which is the same one-cell answer the accumulator question gives, and a later clause states the three tables as one question: a relocation is free exactly where the thing relocated commutes with what it is relocated across, on the domain the cell reaches.  | No witness. The failing cell is established by a count of differing triples rather than by a named instance.  The clause that states the three tables as one question carries a predicate naming an accumulator width and an operation set that no `dimension` row declares, so that clause is not written into the canon at all and this row carries only the cell it measured.  | fused, stepwise, multiply-add, denotation, extensional identity, intermediate, exact intermediate, signed saturating, accumulator, substitution |
| <a id="quantise-then-reduce-commutes"></a>quantise_then_reduce_commutes | Quantising a value and then reducing it into range gives the same answer as reducing it and then quantising, so the composition order of the two axes is unobservable and a canon need not state it. |  | rounding: toward-zero, overflow_policy: wrapping, signedness: signed, or unsigned with signed intermediates, threads: 1 |  | [rounding](TOPIC.md#rounding) | The failing pairing is the one member that respects no translation set against the range policy built on translation. Every other pairing commutes, and the equality is read after the final range reduction, which is where a wrapping policy's values live. Before that reduction, at the representative level, the floor member alone commutes, measured at zero disagreements against 60, 32 and 32 for ceiling, half-up and half-even, so a design that ever observes an intermediate representative needs the stronger statement.  The failing region is written exactly as the restoration pass restored it. An earlier revision had written `domain: OPEN` into this predicate and that token was deleted, because unmeasured goes unstated rather than hedged.  | **The holding region cannot be written.** The commutation predicate is stated over an ambient domain closed under negation, and no `dimension` row declares an axis for the ambient domain, so the region where this law holds has no expressible form in the canon's own notation. That is the single largest gap this port hit: the ambient domain appears in 26 of the 60 predicate spans that could not be ported, and is the sole blocker of 4 of them.  No witness. The source establishes the failure by existence, one witness, and records that its rate does not carry; the instance itself is not quoted in the governing files.  | commutation, commute, quantise, reduce, range policy, toward zero, wrapping, decomposition, composition order, representative, quotient |
| <a id="a-deletion-licence-holds-exactly-where-the-boundary-function-family-is-a-singleton"></a>a_deletion_licence_holds_exactly_where_the_boundary_function_family_is_a_singleton | Within a maximal unbound stretch, a licence to delete an interior resolution holds if and only if the placements of that stretch induce exactly one boundary function. The forward direction holds and the converse does not: a family can be a singleton with both licences refusing, so the two licences under-approximate the safe region rather than characterise it, which is what a sound licence is for. |  |  | The chain `+97 *3 +13` under a round-to-8 resolution has a singleton boundary-function family and both deletion licences refuse it, which refutes the converse. The forward direction has no counterexample over the four chains at three resolutions swept against it. | [the_chain](TOPIC.md#the-chain) | Carried as a law rather than as a proposal because the two licences are algebraic: the range licence reads widths and a bound, the algebraic licence reads whether the resolution commutes with or is absorbed by the composition. The algebraic one is a conjunction over every step, so one non-conforming operation anywhere revokes it with the endpoints unchanged, and the carrier of a chain's licence therefore has to see every step rather than only its ends.  The revision stated this as a biconditional using the words "exactly" and "precisely". The independent check split it into three directions, measured that one holds and one fails, and the restoration pass replaced the biconditional with the implication that holds. This row states the corrected form and keeps the refuted direction, because the refutation is what says the licences are sound rather than complete.  | **No region, in either field.** The chain topic's statement carries no `holds for:` predicate on any of its twelve clauses, and neither does its revision, so there is nothing to transcribe. The forward direction's own support is the soundness of the licences established at a grid with four firing controls over 4096 inputs per cell, and the check's reproduction reached only two cells of that grid, which its own coverage note calls a thin base for a universal. The sweep's thinness bounds the reproduction rather than the claim.  | licence, deletion licence, range licence, algebraic licence, congruence, absorption, boundary function, singleton, stretch, biconditional, chain, interior resolution |
| <a id="double-rounding-is-innocuous-at-an-intermediate-width-between-f-and-2f"></a>double_rounding_is_innocuous_at_an_intermediate_width_between_f_and_2f | Narrowing in stages through an intermediate fraction width strictly between the final width and twice it gives the same answer as narrowing directly, so a design may pick such a width and stop worrying about double rounding. It does not: no intermediate width strictly between F and 2F makes double rounding innocuous, at any F. |  | fraction_width: any |  | [rounding](TOPIC.md#rounding) | Established by constructive witness rather than by a sweep, which is why it carries a fraction width and nothing else: the construction is parametric in F and produces a counterexample at every one.  This is the closed half of a hole the rounding topic left open. What remains open is the double-rounding mechanism itself, after three wrong constructions, with the realisation-map topic's reduction-locality clause shown not to be at risk and staged-versus-direct narrowing shown to be what is.  | The failing region names one axis. Under the notation an absent axis says the claim holds in no situation where that axis exists, so as written this row claims a failure only where no width, no signedness, no operation and no container exist, which is not what the source means. The source states the region as `F any` and states nothing else, and this row transcribes that rather than widening it. **Whether the corpus's partial regions should be read as the notation reads them, or as their authors meant them, is unsettled and is not a question a port may answer.**  No witness quoted. The construction is named in the governing files and its instance is not reproduced there.  | double rounding, staged narrowing, intermediate width, innocuous, grades, composite correct rounding, stepwise, no threshold, F, fraction width |
| <a id="additive-associativity-under-wrapping"></a>additive_associativity_under_wrapping | `(a + b) + c == a + (b + c)`, where addition is the exact ambient sum reduced onto the representable set by wrapping. | total_width: W in {4, 8}, fraction_width: F any, signedness: signedness in {unsigned, signed}, overflow_policy: overflow policy = wrap, operation: operation = add, arity: arity = 3 |  |  | [algebraic_laws](TOPIC.md#algebraic-laws) | Two instruments at two widths: the format unit's cube at four-bit widths, and `76`'s exhaustive sweep over `u8`. `F any` is licensed by the format unit's own scale-blindness argument, that addition at a common scale never reads the scale, rather than by a sweep over F. The source states no thread count and no target features, so both axes are absent, which under I13 says the law holds in no situation where threads exist. That reading is an artefact of the consolidation's notation discipline rather than of the algebra: the neighbouring row `associativity_of_a_composed_saturating_add_and_subtract` carries `threads any` for a claim of exactly the same kind, because its source wrote the axis. | The premise is same-scale addition throughout. Whether a mixed-scale addition exists, and what result format it infers, is an open question the additive column depends on and nobody has swept. | associativity, addition, wrap, wrapping, abelian group, additive, scale blind |
| <a id="additive-associativity-under-saturation"></a>additive_associativity_under_saturation | `(a + b) + c == a + (b + c)`, where addition is the exact ambient sum reduced onto the representable set by saturation. | total_width: W in {4, 8}, fraction_width: F any, signedness: signedness = unsigned, overflow_policy: overflow policy = saturate, operation: operation = add, arity: arity = 3 | total_width: W = 4, fraction_width: F any, signedness: signedness = signed, overflow_policy: overflow policy = saturate, operation: operation = add, arity: arity = 3 | Signed two's complement, `W = 8`: 4,177,792 of 16,777,216 triples diverge, first counterexample `a = -128, b = -128, c = 1` (`76_probes/probe1_associativity_exhaustive.rs`). At `W = 4` the count is 952 triples, and it is invariant in F: 448 ceiling-only, 504 floor-only, zero involving both bounds. | [algebraic_laws](TOPIC.md#algebraic-laws) | The unsigned side is stronger than a sweep: the format unit's congruence argument, that `equal, or both at or beyond the bound` is a congruence on the naturals, covers all widths at once, and the semiring axioms were measured at bounds M in {1, 2, 3, 7, 15, 31, 63, 127, 255} with zero failures. The width entry here carries only what an instrument ran, because no source restated the structural argument as a width predicate. The signed failure count is F-invariant by the same scale-blindness argument that licenses `F any` on the holds side, so the fraction axis carries `any` on both. | Both lists are silent on the signed case restricted to a declared non-negative operand window, where the strategy unit measured additive associativity recovered at `W in {4, 5, 6}`. That cell is separated from the failing signed cell by the declared operand window alone, which no `dimension` row declares, so placing it on `holds` would put one region on both lists. | associativity, addition, saturate, saturating, clamp, 952, monoid, additive |
| <a id="multiplicative-associativity-under-wrapping"></a>multiplicative_associativity_under_wrapping | `(a * b) * c == a * (b * c)`, where multiplication is the exact ambient product reduced onto the representable set by wrapping. | total_width: W in 3..=8, fraction_width: F = 0, signedness: signedness in {unsigned, signed}, overflow_policy: overflow policy = wrap, operation: operation = mul, arity: arity in {2, 3} | total_width: W in 3..=7, fraction_width: F > 0, signedness: signedness in {unsigned, signed}, overflow_policy: overflow policy = wrap, operation: operation = mul, arity: arity in {2, 3} | At `F > 0` a representable-width multiply discards F bits at every pairwise step before any clamp is consulted, so the ambient operation is no longer associative and no reduction repairs it. Measured three ways: nine of nine fractional configurations fail in `57_probes/p3` section 3, wrap's ring collapses identically at nine of nine in `61_probes/q2`, and 54 of 54 signed rows fail in `62_probes/p2` under both rounding spellings. | [algebraic_laws](TOPIC.md#algebraic-laws) | The `F = 0` holds region combines two instrument sets at different widths: the format unit's cube at four-bit widths with the `w = 3 through 7` width instances, and the strategy unit's `93` F1 sweep at `W in 3..8` for the unsigned case. The signed `F = 0` wrap cell is the cube's `ring` row and is separately marked `section-dependent` at `F > 0`, meaning the choice of representative range becomes arithmetic-bearing there. | Nothing here is established above `W = 8`. Every width in both lists is a model width, and the derived-laws unit established that a model band below the shipped width can be defeated by construction, so no row of this law transfers to a shipped width by band agreement. | associativity, multiplication, wrap, wrapping, ring, fraction, F > 0, coarsening |
| <a id="multiplicative-associativity-under-saturation"></a>multiplicative_associativity_under_saturation | `(a * b) * c == a * (b * c)`, where multiplication is the exact ambient product reduced onto the representable set by saturation. | total_width: W in 3..=8, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = saturate, operation: operation = mul, arity: arity in {2, 3} | total_width: W in 3..=6, fraction_width: F = 0, signedness: signedness = signed, overflow_policy: overflow policy = saturate, operation: operation = mul, arity: arity = 3 | Signed two's complement saturating multiplication fails on the integer grid at 28, 160, 780 and 3516 triples for `W` = 3, 4, 5 and 6, with the three-value witness `(7 * 7) * -1 = -7` against `7 * (7 * -1) = -8` and no coarsening anywhere in it. The mechanism is the range's asymmetry under negation: negation carries one absorbed tail into the interior, so `both beyond the same bound` is not preserved by multiplication by a negative and no congruence can be stated. | [algebraic_laws](TOPIC.md#algebraic-laws) | The unsigned holds region rests on a congruence argument the format unit states covers all widths at once, measured at bounds M in {1, 2, 3, 7, 15, 31, 63, 127, 255}; the width entry carries only the widths an instrument ran. The signed cell is the worst corner of the format unit's cube and it is the corner a general-purpose default lands in, because I3 points the default at Rust's primitives and those are signed. | The signed cell under a mirror-symmetric representable range is established to hold, at zero associativity failures and zero coherence violations at every width measured up to `w = 7`, and it is on neither list. It differs from the failing signed cell only in the geometry of the representable range, which no `dimension` row declares, so writing it would put one region on both lists at once. One code point, the most negative value, is the entire difference. The `F > 0` cells are likewise absent from both lists here and are carried by `multiplicative_associativity_under_wrapping`'s fails region, which the cube states holds for saturation too. | associativity, multiplication, saturate, saturating, clamp, signed, symmetric range, semiring |
| <a id="distributivity-of-multiplication-over-addition"></a>distributivity_of_multiplication_over_addition | `a * (b + c) == (a * b) + (a * c)`, over one overflow policy applied to both operations. | total_width: W in 3..=8, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy in {wrap, saturate}, operation: operations {add, mul}, arity: arity in {2, 3}, threads: threads = 1, target_features: target features any | total_width: W in {6, 7}, fraction_width: F = 0, signedness: signedness = signed, overflow_policy: overflow policy = saturate, operation: operations {add, mul}, arity: arity = 3, threads: threads = 1, target_features: target features any | At signed saturating and `F = 0`, two independently written models measure the law failing: `93_probes/p7` gives 47.72% of triples at `W = 7`, and `97_probes/p2` gives 34.52% at `W = 6`. The mechanism is that a one-sided clamp is a congruence and a two-sided one is not. | [algebraic_laws](TOPIC.md#algebraic-laws) | `F = 0` is necessary and it is not sufficient, and the unqualified form of this sentence is false. The strategy consolidation records that this is the **third** time the qualifier has been lost from the sentence in this panel's own history: it appears unqualified at `35:311` where it originated, at `94:887`, and in the standing workspace rule `arvo-always-optimal-internals.md`, which was a live licence to emit a wrong rewrite until it was corrected during that unit. The unsigned holds region also swept `F in 0..2`; only the `F = 0` slice holds, and the `F > 0` slice is carried by `multiplicative_associativity_under_wrapping`'s fails region, since the mechanism is the same rescale. | The signed cell restricted to a declared non-negative operand window is established to hold at `W in {4, 5, 6}` and is on neither list, because the declared operand window is not a declared axis and the cell is otherwise identical to the failing one. The strategy unit records that as its only positive law result and its cleanest region-scoped arm, and the notation cannot hold it. | distributivity, distributive, multiplication, addition, F = 0, 47.72, 34.52, signed saturating |
| <a id="distributivity-of-multiplication-over-subtraction"></a>distributivity_of_multiplication_over_subtraction | `a * (b - c) == (a * b) - (a * c)`, over one overflow policy applied to both operations. |  | fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = saturate, operation: operations {sub, mul}, arity: arity = 3 | 45.79% of triples, measured inside the region where distributivity over addition holds. | [algebraic_laws](TOPIC.md#algebraic-laws) | This is the second qualifier, sitting **inside** the surviving region of the previous row, and it is why a law permission names the operations it covers and not only the fraction width. The source states the percentage and the region's other coordinates without restating the widths its instrument ran, so no width entry appears here: the fails region as written is silent about width, which under I13 says the failure is established at no width at all. That is a defect of the transcription source, not of the measurement, and it is the reason this row carries no `holds`. | No width, no thread count and no target features, because `97` section 6.3's parameters are not restated where the consolidation carries the result. The row is therefore weaker than the finding behind it, and reading it needs `97` opened. | distributivity, subtraction, multiplication, 45.79, operations covered, law permission |
| <a id="associativity-of-a-composed-saturating-add-and-subtract"></a>associativity_of_a_composed_saturating_add_and_subtract | `(a + b) - c == a + (b - c)`, where both operations are exact ambient operations reduced onto the representable set by saturation. |  | total_width: W = 8, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = saturate, operation: op pair {add, sub} composed, threads: threads any, target_features: target features any | 13,882,880 of 16,777,216 triples, 82.7484%. The exact holding region is a four-way case split on the two clamp events plus a residual operand condition, with zero sufficiency and zero necessity violations, and a mutant control reintroducing 32,640 violations so the zero residue is not an artefact. | [algebraic_laws](TOPIC.md#algebraic-laws) | The point of this row is that **each component operation is associative on its own** at exactly these coordinates, and the composition is not: unsigned saturating addition associates on every triple here. So a composed expression's region is never inherited from its parts, and even where a component's own hypotheses hold unconditionally the composition needs its own derivation. `threads any` and `target features any` are the source's own, claimed on a semantic argument about pure value functions rather than on a run, and they are carried because the source wrote them. | The four-way case split that exactly characterises the holding region is not written here, because it is a condition over operand values rather than over any declared axis. A region the notation cannot state is a region no arm can be gated on, and this is the sharpest instance of that in the corpus. | composition, composed, associativity, saturating, add, subtract, 82.7484, inheritance, region |
| <a id="existence-of-a-translation-invariant-total-order"></a>existence_of_a_translation_invariant_total_order | There is a total order on the representable set that the induced additive operation respects. |  | total_width: W any, signedness: signedness in {unsigned, signed}, overflow_policy: overflow policy = wrap, operation: operation = add | Over every total order on the carrier at widths 2 and 3, exhaustively, wrapping addition and xor over `GF(2)^n` admit zero compatible total orders each, while saturating addition and min admit some. The structural reason holds at every width: a finite cyclic group admits no translation-invariant total order. | [the_number_system](TOPIC.md#the-number-system) | `W any` is the source's own, licensed by the structural argument rather than by the sweep, which ran at two widths. The consequence the format unit draws is that comparison on a wrapped numeral is representative-order, which arithmetic does not respect, and it says the canon owes that sentence under every filing equally. The number-system unit draws a different one: an order-or-magnitude boundary for `number system` is measured empty, because it groups wrapping with a bit vector and splits wrapping from saturating inside one kernel item. | The format unit records a further, untested hypothesis that the signed encoding trade, no bijective encoding of a signed value set having both raw-order agreement and raw-adder correctness, is this same theorem in disguise. Nobody has built it, and it would close the redundant-encoding hole if it were true. | order, total order, translation invariant, wrapping, cyclic group, comparison, GF(2), boundary |
| <a id="rounding-retraction-is-the-identity"></a>rounding_retraction_is_the_identity | Rounding a value already on the representable grid returns it unchanged, so the reduction retracts. | total_width: W in {4, 6, 8}, fraction_width: F = 0, rounding: both rounding modes as swept by the instrument, operation: operation = mul | total_width: W in {4, 6, 8}, fraction_width: F in 1..=W, rounding: both rounding modes as swept by the instrument, operation: operation = mul |  | [rounding](TOPIC.md#rounding) | The instrument swept `F` from 0 to `W` at each of three widths under both of its rounding modes and found retraction exact at `F = 0` and nowhere above it. The rounding entry names two modes without naming which two, because the consolidation carrying the result does not name them; the axis is listed rather than absent because the instrument did vary it, and a listed axis with an imprecise value is weaker than the notation wants and stronger than dropping it. | Which two rounding modes, and whether a third behaves differently. The format unit separately measured that truncation against round-half-up moves failure magnitudes in both directions and never their existence, which is consistent and is not the same sweep. | rounding, retraction, retract, identity, F = 0, exact, grid |
| <a id="equation-truth-sets-are-initial-segments-in-the-width"></a>equation_truth_sets_are_initial_segments_in_the_width | For an equation law over the wrapping ring fragment, the set of widths at which it is true is an initial segment: true at every width up to a threshold and false above it. Disequation laws are the dual. | total_width: W any, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = wrap, operation: operations {add, sub, mul, neg} with integer constants, arity: arity any | fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = saturate, operation: operations {saturating add, saturating mul} with nonnegative clamp-embedded constants, arity: arity = 2 | Saturating laws have neither polarity: gapped and interior-run truth sets exist among constant-free depth-2 terms, 16 and 24 of 21,945 pairs, including a law true at width 2 alone and a genuine ring identity false at width 2 alone. The mechanism is that the gapped sets occur exactly where the min-form property fails, a ceiling clamp discarding magnitude a later negative operand would have restored. | [algebraic_laws](TOPIC.md#algebraic-laws) | This is what survives of the model-band transfer after that mechanism was defeated. Polarity is the whole of it: a band FALSE embeds upward with its witness and a band TRUE exports nothing, so a band can refute and can never license. `84` measured the polarity at arity 1 by its own admission; `86` proved it at every arity as a corollary of the degree-grid criterion, which discharged that limit, so `arity any` is carried on the strength of the proof rather than of the sweep. | Nothing here is a licence. The consequence a reader needs is that the band mechanism cannot carry a positive verdict in either fragment, and this row states why for one of them. | polarity, initial segment, truth set, band, transfer, width, ring fragment, gapped |
| <a id="the-falling-factorial-family-vanishes-modulo-two-to-the-width"></a>the_falling_factorial_family_vanishes_modulo_two_to_the_width | `L_k`: the product of `k` consecutive integers is zero modulo `2^W`. | total_width: W in 1..=k - s2(k), where s2 is the binary digit sum of k, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = wrap, operation: operation = mul, arity: arity = 1 | total_width: W above k - s2(k), fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = wrap, operation: operation = mul, arity: arity = 1 | `L_64` is true at every width 1 through 63 and false exactly at 64, so no model band below the shipped width, at any guard setting, on any host, could have disagreed. `L_16` run through the band construction gives band agreement green, perturbation control still refusing, arm licensed at width 64, and the law false at width 64, refuted in sixteen const multiplies. | [algebraic_laws](TOPIC.md#algebraic-laws) | The threshold is Legendre's formula and the family is verified exhaustively for nine members with the instrument shown able to fail. This is the construction that killed model-band transfer, and the family is not exotic: magic constants have exactly this downward-correct, upward-false shape. The width entries name a closed form rather than a range because the region genuinely depends on the law's own index, and the notation has no vocabulary for a region parameterised by the claim. | The proviso a band mechanism relied on, that no law-relevant path dispatches on width, is satisfied by `L_64` and is therefore not sufficient either. Nobody has proposed a replacement proviso. | threshold family, Legendre, falling factorial, band transfer, L_64, magic constant, shipped width |
| <a id="the-saturating-exponent-absorbing-identity"></a>the_saturating_exponent_absorbing_identity | `E_d`: `x^d == x^(d+1)` under unsigned saturating multiplication, constant-free, with the clamps live. | total_width: W in 1..=d, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = saturate, operation: operation = mul, arity: arity = 1 | total_width: W above d, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = saturate, operation: operation = mul, arity: arity = 1 | The truth set is exactly widths 1 through `d`, for all eleven swept members, so `E_63` is true below the shipped width and false at it, and the band mechanism licenses it green end to end. | [algebraic_laws](TOPIC.md#algebraic-laws) | The saturating sibling of the previous row, and it matters because it puts the band defeat outside the ring fragment. Two independently constructed families in two fragments place the flip exactly at the shipped width, which is the strongest multi-instance result in the derived-laws unit. |  | saturating, exponent, absorbing, E_d, threshold family, band transfer, clamp |
| <a id="the-multiplicative-inverse-round-trip"></a>the_multiplicative_inverse_round_trip | `(x * 3) * INV3 == x`, with `INV3` the 64-bit multiplicative inverse of 3 as a wrapping constant. | total_width: W in 1..=65, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = wrap, operation: operation = mul, arity: arity = 1 | total_width: W = 128, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy = wrap, operation: operation = mul, arity: arity = 1 | Witness 1 at width 128, computed rather than searched. | [algebraic_laws](TOPIC.md#algebraic-laws) | Carried because it is the one member of the threshold class that a working engineer would write on purpose: an inverse constant chosen for one width behaves exactly like a threshold family across widths. Widths 66 through 127 are on neither list, because the source states truth through 65 and failure at 128 and nothing between. |  | inverse, INV3, magic constant, round trip, width, threshold, wrapping |
| <a id="coherence-of-a-reduction-onto-its-induced-operation"></a>coherence_of_a_reduction_onto_its_induced_operation | The reduction is a homomorphism onto the operation it induces on the representable set, so reducing before combining agrees with reducing after. | total_width: W = 4, fraction_width: F = 0, signedness: signedness = unsigned, overflow_policy: overflow policy in {wrap, saturate}, operation: operations {add, mul} | total_width: W = 4, fraction_width: F = 0, signedness: signedness = signed, overflow_policy: overflow policy = saturate, operation: operations {add, mul} | Signed saturation holds the adaptation laws and fails coherence at 476 chain-divergent triples; the opposite-bound mutant holds neither, at 897. | [the_format](TOPIC.md#the-format) | Coherence is one of two independent law roles: the adaptation laws face the source and are what error transport and order transport consume, coherence faces the target and decides whether the member computes exactly in an induced algebra. All four combinations of the two roles are inhabited, which is why neither family subsumes the other and why expelling wrapping while retaining saturation has no criterion that does not empty the adaptation slot. The width entry is the instrument's own: `56_probes/q1_two_law_families.rs` declares its ranges as `LO = -8, HI = 7` and `ULO = 0, UHI = 15` at the top of the file, which is four bits in both sign domains, and it happens to agree with the cube. The consolidation states no width for it, so a reader working from the consolidation alone has nothing. | The unsigned add-only saturating case over a nonnegative window holds both roles and is on neither list, because the operand window that distinguishes it is not a declared axis. | coherence, homomorphism, adaptation laws, law roles, induced algebra, 476, 897, quotient |
| <a id="additive-associativity-recovered-under-declared-nonnegative-operand-window"></a>additive_associativity_recovered_under_declared_nonnegative_operand_window | `(a + b) + c == a + (b + c)`, where addition is the exact ambient sum reduced onto the representable set by saturation, and the operands are declared to lie in a non-negative sub-range of a signed format rather than over its full representable range. | total_width: W in {4, 5, 6}, fraction_width: F = 0, signedness: signed, overflow_policy: saturate, operand_window: declared non-negative, operation: add, arity: 3 |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Named F-H in its source, the panel's only positive law result in that unit and its cleanest region-scoped arm: a region where something that generally fails does hold. Predicted before running, on the ground that a one-sided clamp is a congruence and a two-sided one is not, and reported as independently retrodicting an earlier unit's declared-window result that had not been read. `additive_associativity_under_saturation` records the same cell in a `gap` field, unable to place it on either list because no `dimension` row named the operand window; `operand_window` now does. | The finding's own predicate is one expert's, restated in a consolidation rather than re-derived by a second reader. The originating file and its probe sit outside this porting pass's slice (files 100-149) and were not opened directly; this row is filed from the consolidation's quotation of the finding. | associativity, addition, saturate, signed, operand window, declared non-negative, congruence, F-H |
| <a id="multiplicative-associativity-recovered-under-declared-nonnegative-operand-window"></a>multiplicative_associativity_recovered_under_declared_nonnegative_operand_window | `(a * b) * c == a * (b * c)`, where multiplication is the exact ambient product reduced onto the representable set by saturation, and the operands are declared to lie in a non-negative sub-range of a signed format rather than over its full representable range. | total_width: W in {4, 5, 6}, fraction_width: F = 0, signedness: signed, overflow_policy: saturate, operand_window: declared non-negative, operation: mul, arity: 3 |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Named F-H in its source, alongside the additive and distributivity results at the same predicate. See `additive_associativity_recovered_under_declared_nonnegative_operand_window` for the shared reasoning and citation history. Distinct from `multiplicative_associativity_under_saturation`'s own `gap`, which names a mirror-symmetric representable range as its missing dimension: that is a different condition (range geometry) from a declared non-negative operand window, and the two gaps are not the same claim. | As `additive_associativity_recovered_under_declared_nonnegative_operand_window`: one expert, restated rather than re-derived, and the originating probe sits outside this porting pass's slice. | associativity, multiplication, saturate, signed, operand window, declared non-negative, congruence, F-H |
| <a id="distributivity-recovered-under-declared-nonnegative-operand-window"></a>distributivity_recovered_under_declared_nonnegative_operand_window | `a * (b + c) == (a * b) + (a * c)`, where addition and multiplication are the exact ambient operations each reduced onto the representable set by saturation, and the operands are declared to lie in a non-negative sub-range of a signed format rather than over its full representable range. | total_width: W in {4, 5, 6}, fraction_width: F = 0, signedness: signed, overflow_policy: saturate, operand_window: declared non-negative, operation: mul, arity: 3 |  |  | [the_strategy_axis](TOPIC.md#the-strategy-axis) | Named F-H in its source, alongside the additive and multiplicative associativity results at the same predicate. See `additive_associativity_recovered_under_declared_nonnegative_operand_window` for the shared reasoning and citation history. `distributivity_of_multiplication_over_addition` records the same cell in a `gap` field for the identical reason: no `dimension` row named the operand window until this porting pass added `operand_window`. | As `additive_associativity_recovered_under_declared_nonnegative_operand_window`: one expert, restated rather than re-derived, and the originating probe sits outside this porting pass's slice. The source states this recovery holds over addition specifically; it says nothing about distributivity over subtraction under the same window, which is untested. | distributivity, multiplication, addition, saturate, signed, operand window, declared non-negative, congruence, F-H |
